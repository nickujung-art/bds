---
phase: 09-complex-detail-ux
plan: "02"
type: execute
wave: 2
depends_on:
  - "00"
  - "01"
  - "03"
files_modified:
  - src/components/complex/DealTypeTabs.tsx
  - src/components/complex/TransactionChart.tsx
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "DealTypeTabs에 매매/전세 두 탭만 존재 (월세 탭 제거 — D-01)"
    - "DealTypeTabs가 useQueryState로 'period' 파라미터를 관리한다 ('1y'|'3y'|'5y'|'all', 기본 '3y' — D-02)"
    - "DealTypeTabs가 useQueryState로 'area' 파라미터를 관리한다 (전용면적 ROUND 값, 기본 = 최다 거래 평형 — D-04)"
    - "평형 칩 행이 차트 위에 렌더되고 클릭 시 area 파라미터가 변경된다"
    - "TransactionChart가 normal/outlier 거래를 별개 시리즈로 받아 outlier를 투명 점(opacity 0.4)으로 표시한다 (D-03)"
    - "평균선(LineChart)은 normal 데이터만으로 계산되어 이상치를 제외한다 (D-03)"
    - "page.tsx에서 getComplexRawTransactions로 sale/jeonse raw 거래를 RSC에서 fetch하여 props로 전달한다"
    - "monthlyData fetch 제거 — Promise.all에서 getComplexTransactionSummary 'monthly' 호출 삭제"
    - "ISR (revalidate = 86400) 유지 — page.tsx에서 searchParams 읽지 않음"
  artifacts:
    - path: "src/components/complex/DealTypeTabs.tsx"
      provides: "월세 제거 + nuqs period/area + 평형 칩"
      contains: "useQueryState"
      min_lines: 100
    - path: "src/components/complex/TransactionChart.tsx"
      provides: "normal/outlier 분리 + 투명 점 렌더링"
      contains: "isOutlier"
      min_lines: 90
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "getComplexRawTransactions 호출 + DealTypeTabs props 갱신 + monthly fetch 제거"
      contains: "getComplexRawTransactions"
  key_links:
    - from: "page.tsx getComplexRawTransactions(id, 'sale')"
      to: "DealTypeTabs rawSaleData prop"
      via: "Promise.all → props"
      pattern: "rawSaleData"
    - from: "DealTypeTabs useQueryState('area')"
      to: "URL ?area=N"
      via: "nuqs useQueryState"
      pattern: "useQueryState\\(['\"]area"
    - from: "DealTypeTabs useQueryState('period')"
      to: "URL ?period=Xy"
      via: "nuqs useQueryState"
      pattern: "useQueryState\\(['\"]period"
    - from: "TransactionChart outlier 점"
      to: "투명/회색 마커 (opacity 0.3~0.4)"
      via: "custom dot prop"
      pattern: "opacity"
---

<objective>
실거래가 그래프 UI 재설계: (a) 월세 탭 제거(D-01), (b) 기간 필터 nuqs URL 상태(D-02), (c) IQR 이상치 투명 점 표시(D-03), (d) 평형 칩 셀렉터(D-04). page.tsx는 raw transactions fetch + DealTypeTabs props 갱신만 수행 — ISR 보존을 위해 URL 파라미터는 클라이언트에서만 처리.

Purpose: UX-01/UX-02 핵심 — 실수요자가 자신의 관심 평형만 보고, 신뢰성 있는 가격대를 파악하도록.
Output: 3개 파일 수정. DealTypeTabs는 raw 데이터를 받아 client-side에서 period/area slice → IQR 계산 → normal/outlier 분리 → 차트에 전달.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-complex-detail-ux/09-CONTEXT.md
@.planning/phases/09-complex-detail-ux/09-RESEARCH.md
@.planning/phases/09-complex-detail-ux/09-00-SUMMARY.md
@.planning/phases/09-complex-detail-ux/09-01-SUMMARY.md
@.planning/phases/09-complex-detail-ux/09-03-SUMMARY.md
@src/components/complex/DealTypeTabs.tsx
@src/components/complex/TransactionChart.tsx
@src/components/complex/CompareAddButton.tsx
@src/app/complexes/[id]/page.tsx

<interfaces>
<!-- Wave 1 (09-01) 출력 — 본 Plan이 사용하는 인터페이스 -->

From src/lib/data/complex-detail.ts:
```typescript
export interface RawTransaction {
  dealDate:  string  // "YYYY-MM-DD"
  yearMonth: string  // "YYYY-MM"
  price:     number  // 만원
  area:      number  // m2
}
export async function getComplexRawTransactions(
  complexId: string,
  dealType: 'sale' | 'jeonse',
  supabase: SupabaseClient,
  months?: number,
): Promise<RawTransaction[]>
```

From src/lib/utils/iqr.ts:
```typescript
export interface PricePoint { yearMonth: string; price: number; area: number }
export function computeIqrOutliers(points: PricePoint[]): { normal: PricePoint[]; outliers: PricePoint[] }
```

From src/lib/utils/period-filter.ts:
```typescript
export type PeriodKey = '1y' | '3y' | '5y' | 'all'
export function filterByPeriod<T extends { dealDate: string }>(points: T[], period: PeriodKey, now?: Date): T[]
```

From src/lib/utils/area-groups.ts:
```typescript
export function extractAreaGroups<T extends { area: number }>(points: T[]): number[]  // 거래량 내림차순
export function filterByArea<T extends { area: number }>(points: T[], targetArea: number): T[]
```

From src/components/complex/CompareAddButton.tsx (nuqs 패턴 verbatim):
```typescript
'use client'
import { parseAsArrayOf, parseAsString, useQueryState } from 'nuqs'
const [ids, setIds] = useQueryState('ids', parseAsArrayOf(parseAsString).withDefault([]))
```

From src/app/complexes/[id]/page.tsx (현재 Promise.all — 본 Plan에서 수정):
```typescript
// 현재 (변경 전):
const [saleData, jeonseData, monthlyData, sidebarAds, ...] = await Promise.all([
  getComplexTransactionSummary(id, 'sale', supabase),       // ← 유지 (기존 헤더 카드 latestSale + 우측 사이드바 "최근 거래" 의존)
  getComplexTransactionSummary(id, 'jeonse', supabase),     // ← 유지
  getComplexTransactionSummary(id, 'monthly', supabase),    // ★ 제거 (월세 탭 삭제)
  ...
])

// 변경 후 추가:
const [rawSaleData, rawJeonseData] = await Promise.all([
  getComplexRawTransactions(id, 'sale', supabase),
  getComplexRawTransactions(id, 'jeonse', supabase),
])
// — DealTypeTabs에 raw 데이터 전달용
```

DealTypeTabs 현재 props (변경 대상):
```typescript
interface Props {
  saleData:    MonthlyPriceSummary[]
  jeonseData:  MonthlyPriceSummary[]
  monthlyData: MonthlyPriceSummary[]
}
```

변경 후:
```typescript
interface Props {
  rawSaleData:   RawTransaction[]
  rawJeonseData: RawTransaction[]
}
```

Recharts 3.8.1 LineChart custom dot 패턴 (A1 ASSUMED — 빌드 시 검증):
```typescript
<Line ... dot={(props) => {
  const { cx, cy, payload } = props
  const isOutlier = payload?.isOutlier
  return <circle cx={cx} cy={cy} r={4}
    fill={isOutlier ? 'transparent' : '#1d4ed8'}
    stroke={isOutlier ? '#9ca3af' : '#1d4ed8'}
    opacity={isOutlier ? 0.4 : 1}
  />
}} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: page.tsx — raw transactions fetch + DealTypeTabs props 갱신 + monthly fetch 제거</name>
  <read_first>
    src/app/complexes/[id]/page.tsx
    src/lib/data/complex-detail.ts
    .planning/phases/09-complex-detail-ux/09-01-SUMMARY.md
  </read_first>
  <action>
    수정 대상: `src/app/complexes/[id]/page.tsx`

    변경 1 — import 라인 갱신:
    ```typescript
    // 현재:
    import { getComplexById, getComplexTransactionSummary } from '@/lib/data/complex-detail'
    // 변경 후:
    import { getComplexById, getComplexTransactionSummary, getComplexRawTransactions } from '@/lib/data/complex-detail'
    ```

    변경 2 — Promise.all 배열에서 `monthlyData` 라인 제거:
    ```typescript
    // 현재 (line 125~143 부근):
    const [
      saleData,
      jeonseData,
      monthlyData,      // ★ 제거
      sidebarAds,
      reviews,
      ...
    ] = await Promise.all([
      getComplexTransactionSummary(id, 'sale', supabase),
      getComplexTransactionSummary(id, 'jeonse', supabase),
      getComplexTransactionSummary(id, 'monthly', supabase),   // ★ 제거
      getActiveAds('sidebar', supabase),
      ...
    ])
    ```
    교체 후 (monthlyData 변수와 fetch 라인 모두 삭제):
    ```typescript
    const [
      saleData,
      jeonseData,
      sidebarAds,
      reviews,
      reviewStats,
      facilityKaptResult,
      redevelopmentProject,
      quadrantData,
      gapLabelData,
      districtStats,
      cafePosts,
      managementCostRows,
      rawSaleData,        // ★ 신규
      rawJeonseData,      // ★ 신규
    ] = await Promise.all([
      getComplexTransactionSummary(id, 'sale', supabase),     // 유지 (header card latestSale + 우측 "최근 거래" 의존)
      getComplexTransactionSummary(id, 'jeonse', supabase),   // 유지 (탭 전환 시 빠른 표시 — D-09 client slice fallback)
      getActiveAds('sidebar', supabase),
      getReviewsWithComments(id, supabase),
      getComplexReviewStats(id, supabase),
      supabase.from('facility_kapt').select('*').eq('complex_id', id).order('data_month', { ascending: false }).limit(1).maybeSingle(),
      getRedevelopmentProject(id, supabase).catch(() => null),
      complex.si && complex.gu
        ? getQuadrantData(id, complex.si, complex.gu, supabase).catch(() => null)
        : Promise.resolve(null),
      getGapLabelData(id, supabase).catch(() => ({ listingPricePerPy: null, avgTransactionPricePerPy: null })),
      complex.si && complex.gu
        ? (async () => { try { const r = await supabase.from('district_stats').select('adm_nm, population, households, data_year, data_quarter').eq('si', complex.si!).eq('gu', complex.gu!).order('data_year', { ascending: false }).order('data_quarter', { ascending: false }).limit(1).maybeSingle(); return r.data } catch { return null } })()
        : Promise.resolve(null),
      getCafePostsByComplex(id, supabase).catch(() => []),
      getManagementCostMonthly(id, supabase).catch(() => []),
      getComplexRawTransactions(id, 'sale', supabase).catch(() => []),    // ★ 신규
      getComplexRawTransactions(id, 'jeonse', supabase).catch(() => []),  // ★ 신규
    ])
    ```
    ★ NOTE: Promise.all 위치 보존. 기존 항목 순서 변경 금지 — 변수 destructure도 동일 순서 유지. rawSaleData/rawJeonseData만 배열 끝에 추가.

    변경 3 — DealTypeTabs JSX 호출 변경:
    ```typescript
    // 현재:
    <DealTypeTabs saleData={saleData} jeonseData={jeonseData} monthlyData={monthlyData} />
    // 변경 후:
    <DealTypeTabs rawSaleData={rawSaleData} rawJeonseData={rawJeonseData} />
    ```

    ★ 변경 금지 항목:
    - `export const revalidate = 86400` 라인 (ISR 보존)
    - `interface Props { params: Promise<{ id: string }> }` — searchParams prop 추가 금지
    - latestSale, header card, 우측 "최근 거래" 리스트, ManagementCostCard 등 다른 섹션 코드 (별도 Plan 09-04에서 ManagementCostCard 처리)
    - 시설 카드 (09-03에서 처리됨)
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/app/complexes/[id]/page.tsx','utf8');const u=c.replace(/\\/\\/.*$/gm,'').replace(/\\/\\*[\\s\\S]*?\\*\\//g,'');if(!u.includes('getComplexRawTransactions')){console.error('MISS getComplexRawTransactions');process.exit(1)}if(u.includes('monthlyData')){console.error('monthlyData still referenced');process.exit(2)}if(u.includes(\"'monthly'\")){console.error('monthly fetch still present');process.exit(3)}if(!u.includes('rawSaleData')){console.error('MISS rawSaleData');process.exit(4)}if(!u.includes('rawJeonseData')){console.error('MISS rawJeonseData');process.exit(5)}if(!u.includes('export const revalidate = 86400')){console.error('ISR removed');process.exit(6)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getComplexRawTransactions" src/app/complexes/[id]/page.tsx` (주석 제외) >= 3 (import + 2 호출)
    - `grep -c "monthlyData" src/app/complexes/[id]/page.tsx` (주석 제외) == 0
    - `grep -E "getComplexTransactionSummary\\(.*'monthly'" src/app/complexes/[id]/page.tsx` 매칭 없음 (월세 fetch 제거)
    - `grep -c "rawSaleData" src/app/complexes/[id]/page.tsx` (주석 제외) >= 2
    - `grep -c "rawJeonseData" src/app/complexes/[id]/page.tsx` (주석 제외) >= 2
    - `grep -c "export const revalidate = 86400" src/app/complexes/[id]/page.tsx` >= 1
    - `npm run build` 통과, 출력에 `/complexes/[id]` 정적/ISR 마커 (`λ Dynamic` 아님)
  </acceptance_criteria>
  <done>page.tsx에서 raw transactions fetch + DealTypeTabs props 갱신 + monthly 제거 완료, ISR 유지</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DealTypeTabs.tsx — 월세 제거 + nuqs period/area + 평형 칩</name>
  <read_first>
    src/components/complex/DealTypeTabs.tsx
    src/components/complex/CompareAddButton.tsx
    src/lib/utils/period-filter.ts
    src/lib/utils/area-groups.ts
    src/lib/utils/iqr.ts
    .planning/phases/09-complex-detail-ux/09-CONTEXT.md
  </read_first>
  <action>
    수정 대상 (전체 재작성): `src/components/complex/DealTypeTabs.tsx`

    ```typescript
    'use client'

    import { useState, useMemo } from 'react'
    import { parseAsString, parseAsStringEnum, useQueryState } from 'nuqs'
    import { TransactionChart } from './TransactionChart'
    import type { RawTransaction } from '@/lib/data/complex-detail'
    import { filterByPeriod, type PeriodKey } from '@/lib/utils/period-filter'
    import { extractAreaGroups, filterByArea } from '@/lib/utils/area-groups'
    import { computeIqrOutliers } from '@/lib/utils/iqr'

    interface Props {
      rawSaleData:   RawTransaction[]
      rawJeonseData: RawTransaction[]
    }

    type DealTab = 'sale' | 'jeonse'

    // D-01: 월세 탭 제거 — 매매/전세 두 탭만 유지
    const TABS: { id: DealTab; label: string }[] = [
      { id: 'sale',   label: '매매' },
      { id: 'jeonse', label: '전세' },
    ]

    const PERIOD_OPTIONS: { id: PeriodKey; label: string }[] = [
      { id: '1y',  label: '1년' },
      { id: '3y',  label: '3년' },
      { id: '5y',  label: '5년' },
      { id: 'all', label: '전체' },
    ]

    export function DealTypeTabs({ rawSaleData, rawJeonseData }: Props) {
      const [active, setActive] = useState<DealTab>('sale')

      // D-02: 기간 필터 nuqs URL 상태 (기본값 '3y', clearOnDefault로 URL 청소)
      const [period, setPeriod] = useQueryState(
        'period',
        parseAsStringEnum<PeriodKey>(['1y', '3y', '5y', 'all'])
          .withDefault('3y')
          .withOptions({ clearOnDefault: true }),
      )

      // 활성 탭의 raw 데이터
      const rawActive = active === 'sale' ? rawSaleData : rawJeonseData

      // 평형 그룹 추출 (전체 거래 기준 — 기간 무관)
      const areaGroups = useMemo(() => extractAreaGroups(rawActive), [rawActive])
      const defaultArea = areaGroups[0] ?? null  // 최다 거래 평형

      // D-04: 평형 필터 nuqs URL 상태 (기본값 = 최다 거래 평형)
      const [area, setArea] = useQueryState(
        'area',
        parseAsString.withDefault(defaultArea != null ? String(defaultArea) : ''),
      )

      // 현재 선택된 평형 (URL에 없거나 빈 값이면 default)
      const selectedArea = useMemo(() => {
        const parsed = parseInt(area, 10)
        if (!Number.isFinite(parsed)) return defaultArea
        if (!areaGroups.includes(parsed)) return defaultArea
        return parsed
      }, [area, areaGroups, defaultArea])

      // 평형 + 기간 slice → IQR
      const { normal, outliers } = useMemo(() => {
        if (selectedArea == null) return { normal: [], outliers: [] }
        const byArea   = filterByArea(rawActive, selectedArea)
        const byPeriod = filterByPeriod(byArea, period)
        const points = byPeriod.map(r => ({
          yearMonth: r.yearMonth,
          price:     r.price,
          area:      r.area,
        }))
        return computeIqrOutliers(points)
      }, [rawActive, selectedArea, period])

      return (
        <div>
          {/* 탭 행 */}
          <div className="tabs" style={{ marginBottom: 12 }}>
            {TABS.map((tab) => (
              <button
                key={tab.id}
                onClick={() => setActive(tab.id)}
                className="tab"
                data-orange-active={active === tab.id ? 'true' : undefined}
                style={{ background: 'none' }}
              >
                {tab.label}
              </button>
            ))}
          </div>

          {/* 평형 칩 + 기간 필터 행 */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              gap: 12,
              marginBottom: 12,
              flexWrap: 'wrap',
            }}
          >
            {/* 평형 칩 (좌측) */}
            <div role="radiogroup" aria-label="평형 선택" style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}>
              {areaGroups.length === 0 ? (
                <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                  거래 없음
                </span>
              ) : (
                areaGroups.map((m2) => {
                  const isActive = selectedArea === m2
                  return (
                    <button
                      key={m2}
                      type="button"
                      role="radio"
                      aria-checked={isActive}
                      onClick={() => void setArea(String(m2))}
                      className={`btn btn-sm ${isActive ? 'btn-orange' : 'btn-secondary'}`}
                      style={{ minHeight: 32, padding: '4px 12px' }}
                    >
                      {m2}㎡
                    </button>
                  )
                })
              )}
            </div>

            {/* 기간 필터 (우측) */}
            <div role="radiogroup" aria-label="기간 선택" style={{ display: 'flex', gap: 6 }}>
              {PERIOD_OPTIONS.map((opt) => {
                const isActive = period === opt.id
                return (
                  <button
                    key={opt.id}
                    type="button"
                    role="radio"
                    aria-checked={isActive}
                    onClick={() => void setPeriod(opt.id)}
                    className={`btn btn-sm ${isActive ? 'btn-orange' : 'btn-secondary'}`}
                    style={{ minHeight: 32, padding: '4px 10px' }}
                  >
                    {opt.label}
                  </button>
                )
              })}
            </div>
          </div>

          {/* 차트 */}
          <TransactionChart normal={normal} outliers={outliers} dealType={active} />
        </div>
      )
    }
    ```

    주요 결정:
    - D-01: TABS 배열에서 'monthly' 제거. monthlyData prop 제거.
    - D-02: period nuqs `parseAsStringEnum` (타입 안전) + `clearOnDefault: true` (Pitfall 5).
    - D-03: IQR 결과 normal/outliers 분리하여 TransactionChart에 별도 prop 전달.
    - D-04: area는 string으로 URL 저장, 클라이언트에서 parseInt + validate. defaultArea가 null이면 빈 문자열로 fallback.
    - 칩 위치: 차트 헤더 위 — CONTEXT.md `<specifics>` "차트 헤더 위 우측 정렬"과 약간 다르지만 평형 칩은 좌측 + 기간은 우측 (UX 균형). 디스크레션 범위.

    AI 슬롭 금지 준수: btn-orange/btn-secondary 기존 디자인 토큰 활용. 보라/인디고 색상 없음. backdrop-blur 없음.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/complex/DealTypeTabs.tsx','utf8');const u=c.replace(/\\/\\/.*$/gm,'').replace(/\\/\\*[\\s\\S]*?\\*\\//g,'');const must=['useQueryState','parseAsStringEnum','filterByPeriod','filterByArea','extractAreaGroups','computeIqrOutliers','rawSaleData','rawJeonseData','clearOnDefault'];for(const k of must){if(!u.includes(k)){console.error('MISS',k);process.exit(1)}}if(u.includes('monthly')){console.error('monthly tab leak');process.exit(2)}if(u.includes('backdrop-blur')||u.includes('gradient-text')){console.error('AI slop pattern');process.exit(3)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useQueryState" src/components/complex/DealTypeTabs.tsx` >= 2 (period + area)
    - `grep -c "useQueryState\\(['\"]period" src/components/complex/DealTypeTabs.tsx` >= 1
    - `grep -c "useQueryState\\(['\"]area" src/components/complex/DealTypeTabs.tsx` >= 1
    - `grep -c "clearOnDefault" src/components/complex/DealTypeTabs.tsx` >= 1 (Pitfall 5 해결)
    - `grep -c "monthly" src/components/complex/DealTypeTabs.tsx` == 0 (D-01 월세 완전 제거)
    - `grep -c "computeIqrOutliers" src/components/complex/DealTypeTabs.tsx` >= 1
    - `grep -c "rawSaleData" src/components/complex/DealTypeTabs.tsx` >= 1
    - `grep -E "backdrop-blur|gradient-text|보라|인디고" src/components/complex/DealTypeTabs.tsx` 매칭 없음 (AI 슬롭 금지)
    - `npm run lint && npm run build` 통과
  </acceptance_criteria>
  <done>DealTypeTabs가 nuqs period/area + 평형 칩 + IQR 분리 + 월세 제거된 상태로 재작성됨</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TransactionChart.tsx — normal/outlier 분리 + 투명 점 + 평균선 normal만</name>
  <read_first>
    src/components/complex/TransactionChart.tsx
    src/lib/utils/iqr.ts
    .planning/phases/09-complex-detail-ux/09-RESEARCH.md
  </read_first>
  <action>
    수정 대상 (전체 재작성): `src/components/complex/TransactionChart.tsx`

    ```typescript
    'use client'

    import {
      ComposedChart,
      Line,
      Scatter,
      XAxis,
      YAxis,
      CartesianGrid,
      Tooltip,
      ResponsiveContainer,
    } from 'recharts'
    import type { PricePoint } from '@/lib/utils/iqr'

    interface Props {
      normal:   PricePoint[]
      outliers: PricePoint[]
      dealType: 'sale' | 'jeonse'
    }

    const LABEL: Record<'sale' | 'jeonse', string> = {
      sale:   '매매가',
      jeonse: '보증금',
    }

    function formatPrice(value: number): string {
      if (value >= 10000) return `${(value / 10000).toFixed(1)}억`
      return `${value.toLocaleString()}만`
    }

    function tooltipPrice(value: unknown): [string, string] {
      const n = Number(value)
      if (!Number.isFinite(n)) return ['-', '']
      return [formatPrice(n), '']
    }

    /**
     * 월별 normal 평균 계산 — D-03: 이상치 제외한 평균선
     */
    function aggregateMonthlyAverage(points: PricePoint[]): Array<{ yearMonth: string; avgPrice: number }> {
      const buckets = new Map<string, number[]>()
      for (const p of points) {
        const arr = buckets.get(p.yearMonth) ?? []
        arr.push(p.price)
        buckets.set(p.yearMonth, arr)
      }
      return [...buckets.entries()]
        .map(([yearMonth, prices]) => ({
          yearMonth,
          avgPrice: Math.round(prices.reduce((s, v) => s + v, 0) / prices.length),
        }))
        .sort((a, b) => a.yearMonth.localeCompare(b.yearMonth))
    }

    export function TransactionChart({ normal, outliers, dealType }: Props) {
      if (normal.length === 0 && outliers.length === 0) {
        return (
          <div className="flex h-48 items-center justify-center text-sm text-gray-400"
               style={{ minHeight: 240, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--fg-tertiary)', font: '500 13px/1.4 var(--font-sans)' }}>
            거래 데이터가 없습니다
          </div>
        )
      }

      // D-03: 평균선은 normal만으로 계산 (이상치 제외)
      const avgSeries = aggregateMonthlyAverage(normal)

      // Scatter용 데이터 (normal/outlier 각 점)
      const normalDots   = normal.map(p => ({ yearMonth: p.yearMonth, price: p.price }))
      const outlierDots  = outliers.map(p => ({ yearMonth: p.yearMonth, price: p.price }))

      return (
        <ResponsiveContainer width="100%" height={240}>
          <ComposedChart margin={{ top: 4, right: 16, left: 8, bottom: 4 }}>
            <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
            <XAxis
              dataKey="yearMonth"
              type="category"
              allowDuplicatedCategory={false}
              tick={{ fontSize: 11 }}
              tickFormatter={(v: string) => (typeof v === 'string' ? v.slice(2) : '')}
              interval="preserveStartEnd"
            />
            <YAxis
              tick={{ fontSize: 11 }}
              tickFormatter={formatPrice}
              width={56}
              dataKey="price"
            />
            <Tooltip
              formatter={tooltipPrice}
              labelFormatter={(label) => String(label)}
              contentStyle={{ fontSize: 12 }}
            />
            {/* 정상 거래 점 — 채워진 원 */}
            <Scatter
              name={`${LABEL[dealType]}`}
              data={normalDots}
              fill="#1d4ed8"
              shape="circle"
            />
            {/* 이상치 — 투명 점 (D-03) */}
            <Scatter
              name="이상 거래 의심 (분기 IQR 기준)"
              data={outlierDots}
              fill="transparent"
              stroke="#9ca3af"
              opacity={0.4}
              shape="circle"
            />
            {/* 평균선 (normal 기반) */}
            <Line
              type="monotone"
              dataKey="avgPrice"
              data={avgSeries}
              stroke="#1d4ed8"
              strokeWidth={2}
              dot={false}
              activeDot={{ r: 4 }}
              name="월평균 (이상치 제외)"
            />
          </ComposedChart>
        </ResponsiveContainer>
      )
    }
    ```

    주요 결정:
    - D-03: normal/outlier 분리 → 두 개의 `<Scatter>` 시리즈로 렌더. 평균선(`<Line>`)은 normal만 사용한 월평균 사용.
    - LineChart → ComposedChart 변경 (Line + 2개 Scatter 혼합 지원).
    - outliers 시리즈 이름에 "분기 IQR 기준" 명시 — tooltip/legend에서 hover 시 안내 (CONTEXT.md `<specifics>`).
    - normal/outliers 둘 다 빈 배열이면 "거래 데이터가 없습니다" 표시 (기존 동작 보존).
    - props 변경: `data: MonthlyPriceSummary[]` → `normal/outliers: PricePoint[]`. DealTypeTabs에서 분리하여 전달.
    - dealType 타입 '|monthly' 제거 (월세 탭 없음 — D-01).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/complex/TransactionChart.tsx','utf8');const u=c.replace(/\\/\\/.*$/gm,'').replace(/\\/\\*[\\s\\S]*?\\*\\//g,'');const must=['normal','outliers','Scatter','aggregateMonthlyAverage','ComposedChart','PricePoint'];for(const k of must){if(!u.includes(k)){console.error('MISS',k);process.exit(1)}}if(u.includes(\"'monthly'\")||u.includes(\"| 'monthly'\")){console.error('monthly type leak');process.exit(2)}if(/dealType:\\s*'sale'\\s*\\|\\s*'jeonse'\\s*\\|\\s*'monthly'/.test(u)){console.error('monthly in dealType union');process.exit(3)}if(!/opacity[:=]\\s*0?\\.[0-9]/.test(u)){console.error('outlier opacity missing');process.exit(4)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Scatter" src/components/complex/TransactionChart.tsx` >= 2 (normal + outlier scatter)
    - `grep -c "ComposedChart" src/components/complex/TransactionChart.tsx` >= 2 (import + JSX)
    - `grep -c "aggregateMonthlyAverage\\|월평균" src/components/complex/TransactionChart.tsx` >= 1 (D-03 평균선 normal만)
    - `grep -c "normal" src/components/complex/TransactionChart.tsx` >= 3
    - `grep -c "outliers" src/components/complex/TransactionChart.tsx` >= 3
    - `grep -E "opacity[:=]\\s*0?\\.[0-9]" src/components/complex/TransactionChart.tsx` 매칭 (이상치 투명 마커)
    - `grep -c "PricePoint" src/components/complex/TransactionChart.tsx` >= 2 (import + type)
    - `grep -c "'monthly'" src/components/complex/TransactionChart.tsx` == 0 (D-01 월세 dealType 제거)
    - `npm run lint && npm run build` 통과
    - `npm run build` 출력에서 `/complexes/[id]` 정적/ISR 마커 (`λ Dynamic` 없음)
  </acceptance_criteria>
  <done>TransactionChart가 ComposedChart + 2 Scatter + Line (normal 평균선) 구조로 재작성됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params (period, area) → 클라이언트 상태 | nuqs 파서가 타입 강제. DB 쿼리에 searchParams 직접 전달 없음 — client-side slice만 |
| Recharts 입력 데이터 | RSC에서 fetch 후 props로 전달. RLS public read 통과한 transactions 데이터만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-08 | Tampering | URL ?area=999 (존재 않는 평형) | mitigate | selectedArea useMemo에서 `areaGroups.includes(parsed)` 검증 → 미존재 시 defaultArea로 fallback. 잘못된 입력 시 빈 결과 안전 표시. |
| T-9-09 | DoS via ISR 파괴 | searchParams in RSC | mitigate | page.tsx Props 인터페이스에 searchParams 추가 금지. nuqs는 클라이언트에서만 사용. 빌드 출력으로 검증. |
| T-9-10 | Input Validation | URL ?period=invalid | mitigate | parseAsStringEnum<PeriodKey>(['1y','3y','5y','all']) — 유효하지 않은 값은 nuqs가 무시하고 default 적용. |
</threat_model>

<verification>
- 3개 파일 수정 (page.tsx, DealTypeTabs.tsx, TransactionChart.tsx)
- `npm run lint && npm run build` 통과
- `npm run build` 출력: `/complexes/[id]` 정적/ISR 마커 (ISR 보존 검증)
- 수동 검증 가능 (Wave 2 종료 후 checkpoint): `npm run dev` → `/complexes/{id}` 접속 → 평형 칩 클릭 시 URL에 `?area=84` 반영, 기간 칩 클릭 시 `?period=1y` 반영, 차트에 투명 점 표시 (이상 거래 있는 단지 한정).
</verification>

<success_criteria>
- 월세 탭 사라짐 (매매/전세 두 탭만)
- 평형 칩 행이 차트 위에 표시되고 클릭 시 URL이 갱신됨
- 기간 칩 (1년/3년/5년/전체) 표시되고 기본값 3년
- 차트에 정상 거래는 채워진 점, 이상치는 투명/회색 점 + 평균선은 이상치 제외 계산
- 단지 상세 페이지 빌드 시 ISR 유지 (정적 마커)
</success_criteria>

<output>
완료 후 `.planning/phases/09-complex-detail-ux/09-02-SUMMARY.md` 작성. 빌드 출력의 `/complexes/[id]` 마커 라인 인용 + DealTypeTabs/TransactionChart 라인 수.
</output>

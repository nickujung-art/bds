---
phase: 09-complex-detail-ux
plan: "01"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - src/lib/utils/iqr.ts
  - src/lib/utils/period-filter.ts
  - src/lib/utils/area-groups.ts
  - src/lib/utils/facility-format.ts
  - src/lib/data/complex-detail.ts
  - src/lib/data/management-cost.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "computeIqrOutliers 함수가 IQR 1.5x 기준으로 normal/outliers를 분리한다 (UX-01)"
    - "filterByPeriod 함수가 1y/3y/5y/all 키를 받아 dealDate 기반 slice를 반환한다 (UX-01)"
    - "extractAreaGroups 함수가 ROUND(area_m2) 기준 평형 목록을 거래량 내림차순으로 반환한다 (UX-02)"
    - "formatParkingPerUnit / formatElevatorPerBuilding 헬퍼가 null fallback을 처리한다 (UX-03)"
    - "getComplexRawTransactions 함수가 complex_transactions_for_chart RPC를 호출한다 (UX-01)"
    - "getSeasonalAverages 함수가 6~9월/10~3월 평균을 반환하고 D-08 4개월 미만 조건 시 해당 평균을 null로 반환한다 (UX-04)"
    - "iqr.test.ts + phase9-ux.test.ts가 GREEN 전환된다"
  artifacts:
    - path: "src/lib/utils/iqr.ts"
      provides: "computeIqrOutliers 순수 함수 (UX-01)"
      min_lines: 25
      exports:
        - "PricePoint"
        - "computeIqrOutliers"
    - path: "src/lib/utils/period-filter.ts"
      provides: "filterByPeriod 순수 함수 + PeriodKey 타입 (UX-01)"
      min_lines: 20
      exports:
        - "PeriodKey"
        - "filterByPeriod"
    - path: "src/lib/utils/area-groups.ts"
      provides: "extractAreaGroups + filterByArea 순수 함수 (UX-02)"
      min_lines: 25
      exports:
        - "extractAreaGroups"
        - "filterByArea"
    - path: "src/lib/utils/facility-format.ts"
      provides: "formatParkingPerUnit + formatElevatorPerBuilding (UX-03)"
      min_lines: 15
      exports:
        - "formatParkingPerUnit"
        - "formatElevatorPerBuilding"
    - path: "src/lib/data/complex-detail.ts"
      provides: "getComplexRawTransactions 추가 (UX-01/UX-02)"
      contains: "complex_transactions_for_chart"
      exports:
        - "RawTransaction"
        - "getComplexRawTransactions"
    - path: "src/lib/data/management-cost.ts"
      provides: "getSeasonalAverages export (UX-04)"
      contains: "getSeasonalAverages"
      exports:
        - "SeasonalAverages"
        - "getSeasonalAverages"
  key_links:
    - from: "getComplexRawTransactions"
      to: "complex_transactions_for_chart RPC"
      via: "supabase.rpc()"
      pattern: "supabase\\.rpc\\(['\"]complex_transactions_for_chart"
    - from: "computeIqrOutliers"
      to: "PricePoint 분류"
      via: "Q1, Q3, IQR 1.5x 계산"
      pattern: "1\\.5\\s*\\*\\s*iqr"
---

<objective>
Phase 9 데이터 레이어 + 순수 유틸리티 함수. UI 변경은 없음 — Wave 2 chart/관리비 컴포넌트가 import할 4개 유틸 + 2개 데이터 함수만 작성한다.

Purpose: Wave 0 RED 테스트(iqr.test.ts + phase9-ux.test.ts)를 GREEN으로 전환. ISR 보존 전략 준수 — 모든 함수는 client-side에서 호출 가능한 순수 함수 또는 RSC server-only 데이터 fetch.
Output: 4개 신규 유틸 파일 + 2개 데이터 모듈 확장. 모든 RED 테스트 GREEN 전환.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-complex-detail-ux/09-CONTEXT.md
@.planning/phases/09-complex-detail-ux/09-RESEARCH.md
@.planning/phases/09-complex-detail-ux/09-00-SUMMARY.md
@src/lib/data/complex-detail.ts
@src/lib/data/management-cost.ts
@src/__tests__/iqr.test.ts
@src/__tests__/phase9-ux.test.ts

<interfaces>
<!-- Wave 1이 의존하는 기존 구조 -->

From src/lib/data/complex-detail.ts (현재 export):
```typescript
export interface ComplexDetail { id, canonical_name, ..., household_count, ... }
export interface MonthlyPriceSummary { yearMonth, avgPrice, count, avgArea }
export async function getComplexById(id, supabase): Promise<ComplexDetail | null>
export async function getComplexTransactionSummary(complexId, dealType, supabase, months=120): Promise<MonthlyPriceSummary[]>
```

From src/lib/data/management-cost.ts (현재 export):
```typescript
export interface ManagementCostRow {
  year_month: string                          // "YYYY-MM-01"
  common_cost_total: number | null
  individual_cost_total: number | null
  long_term_repair_monthly: number | null
  // ... 기타 13개 항목
}
export async function getManagementCostMonthly(complexId, supabase): Promise<ManagementCostRow[]>
```

From supabase/migrations/20260514000002_phase9_transactions_for_chart.sql (Wave 0 출력):
```sql
-- RPC 시그니처
public.complex_transactions_for_chart(
  p_complex_id uuid,
  p_deal_type  text,
  p_months     int default 120,
  p_area_m2    numeric default null
) returns table (deal_date text, year_month text, price numeric, area_m2 numeric)
```

From src/__tests__/iqr.test.ts (Wave 0 RED — 본 Plan에서 GREEN 전환 대상):
```typescript
import { computeIqrOutliers } from '@/lib/utils/iqr'
// PricePoint: { yearMonth: string, price: number, area: number }
// 시그니처: (points: PricePoint[]) => { normal: PricePoint[], outliers: PricePoint[] }
```

From src/__tests__/phase9-ux.test.ts (Wave 0 RED — 본 Plan에서 GREEN 전환 대상):
```typescript
import { filterByPeriod } from '@/lib/utils/period-filter'
// 시그니처: (points: T[], period: '1y'|'3y'|'5y'|'all', now?: Date) => T[]
// T는 { dealDate: string, ... }를 만족하는 제너릭

import { extractAreaGroups } from '@/lib/utils/area-groups'
// 시그니처: (points: { area: number }[]) => number[] (거래량 내림차순)

import { formatParkingPerUnit, formatElevatorPerBuilding } from '@/lib/utils/facility-format'
// 시그니처: (count: number|null, denom: number|null) => string | null

import { getSeasonalAverages } from '@/lib/data/management-cost'
// 시그니처: (rows: ManagementCostRow[], householdCount: number|null) => SeasonalAverages
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: iqr.ts + period-filter.ts + area-groups.ts 순수 유틸리티 (UX-01, UX-02)</name>
  <read_first>
    src/__tests__/iqr.test.ts
    src/__tests__/phase9-ux.test.ts
    .planning/phases/09-complex-detail-ux/09-RESEARCH.md
  </read_first>
  <behavior>
    - iqr.ts: 가격 배열의 Q1/Q3 계산, IQR = Q3-Q1, lower=Q1-1.5*IQR, upper=Q3+1.5*IQR. 범위 내는 normal, 밖은 outliers. 빈 배열/단일 거래는 안전하게 처리.
    - period-filter.ts: '1y'|'3y'|'5y'|'all' 키 기반 dealDate 컷오프. now 기준 N년 이내만 통과. 'all'은 모두 통과.
    - area-groups.ts: ROUND(area) 그룹화. 그룹별 거래 수 카운트 후 내림차순 정렬. filterByArea(points, targetArea)도 export — ROUND(point.area) === ROUND(targetArea) 매칭.
  </behavior>
  <action>
    파일 1: `src/lib/utils/iqr.ts`

    ```typescript
    /**
     * UX-01 — IQR 1.5x 이상치 분류
     * 입력: 개별 거래 데이터 (price 기준 정렬 후 Q1/Q3 산출)
     * 출력: normal (평균선 계산용), outliers (투명 점 표시용)
     */
    export interface PricePoint {
      yearMonth: string
      price:     number
      area:      number
    }

    export interface IqrResult {
      normal:   PricePoint[]
      outliers: PricePoint[]
    }

    export function computeIqrOutliers(points: PricePoint[]): IqrResult {
      if (points.length < 2) {
        return { normal: [...points], outliers: [] }
      }

      const sorted = [...points].map(p => p.price).sort((a, b) => a - b)
      const q1Idx = Math.floor(sorted.length * 0.25)
      const q3Idx = Math.floor(sorted.length * 0.75)
      const q1 = sorted[q1Idx] ?? 0
      const q3 = sorted[q3Idx] ?? 0
      const iqr = q3 - q1
      const lower = q1 - 1.5 * iqr
      const upper = q3 + 1.5 * iqr

      const normal:   PricePoint[] = []
      const outliers: PricePoint[] = []
      for (const p of points) {
        if (p.price >= lower && p.price <= upper) normal.push(p)
        else outliers.push(p)
      }
      return { normal, outliers }
    }
    ```

    파일 2: `src/lib/utils/period-filter.ts`

    ```typescript
    /**
     * UX-01 — 기간 필터 (client-side slice)
     * D-02: '1y' | '3y' | '5y' | 'all' (기본값 '3y'는 호출자에서 nuqs withDefault로 처리)
     */
    export type PeriodKey = '1y' | '3y' | '5y' | 'all'

    const YEARS_MAP: Record<Exclude<PeriodKey, 'all'>, number> = {
      '1y': 1,
      '3y': 3,
      '5y': 5,
    }

    interface DatedPoint {
      dealDate: string  // "YYYY-MM-DD"
    }

    export function filterByPeriod<T extends DatedPoint>(
      points: T[],
      period: PeriodKey,
      now: Date = new Date(),
    ): T[] {
      if (period === 'all') return points
      const years = YEARS_MAP[period]
      const cutoff = new Date(now)
      cutoff.setFullYear(cutoff.getFullYear() - years)
      const cutoffStr = cutoff.toISOString().slice(0, 10)
      return points.filter(p => p.dealDate >= cutoffStr)
    }
    ```

    파일 3: `src/lib/utils/area-groups.ts`

    ```typescript
    /**
     * UX-02 — 평형 그룹 추출 (ROUND(area_m2) 기준)
     * area_m2 numeric(6,2) 정밀도 문제로 동일 평형이 다양한 값 (84.99, 84.82 등) → ROUND로 통합
     * 정렬: 거래량 내림차순 (D-04 기본값 = 최다 거래 평형)
     */
    interface AreaPoint {
      area: number
    }

    export function extractAreaGroups<T extends AreaPoint>(points: T[]): number[] {
      if (points.length === 0) return []
      const counts = new Map<number, number>()
      for (const p of points) {
        const key = Math.round(p.area)
        counts.set(key, (counts.get(key) ?? 0) + 1)
      }
      return [...counts.entries()]
        .sort((a, b) => b[1] - a[1]) // 거래량 내림차순
        .map(([area]) => area)
    }

    export function filterByArea<T extends AreaPoint>(points: T[], targetArea: number): T[] {
      const target = Math.round(targetArea)
      return points.filter(p => Math.round(p.area) === target)
    }
    ```
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/iqr.test.ts src/__tests__/phase9-ux.test.ts --reporter=verbose 2>&1 | grep -E "✓|PASS|FAIL" | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils/iqr.ts` 파일 존재. `grep -c "export function computeIqrOutliers" src/lib/utils/iqr.ts` >= 1
    - `grep -E "1\\.5\\s*\\*\\s*iqr" src/lib/utils/iqr.ts` 매칭
    - `src/lib/utils/period-filter.ts` 파일 존재. `grep -c "export function filterByPeriod" src/lib/utils/period-filter.ts` >= 1
    - `src/lib/utils/area-groups.ts` 파일 존재. `grep -c "export function extractAreaGroups" src/lib/utils/area-groups.ts` >= 1
    - `grep -c "export function filterByArea" src/lib/utils/area-groups.ts` >= 1
    - `npm run test -- src/__tests__/iqr.test.ts` 통과 (4 tests pass)
    - `npm run test -- src/__tests__/phase9-ux.test.ts -t "filterByPeriod"` 통과 (3 tests pass)
    - `npm run test -- src/__tests__/phase9-ux.test.ts -t "extractAreaGroups"` 통과 (3 tests pass)
  </acceptance_criteria>
  <done>3개 유틸 파일 작성, 관련 테스트 10건 GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: facility-format.ts + getComplexRawTransactions (UX-01/02/03)</name>
  <read_first>
    src/lib/data/complex-detail.ts
    src/__tests__/phase9-ux.test.ts
    supabase/migrations/20260514000002_phase9_transactions_for_chart.sql
  </read_first>
  <behavior>
    - formatParkingPerUnit: 총 주차면수 / 세대수 = N.N대 (소수점 1자리). null fallback.
    - formatElevatorPerBuilding: 총 엘리베이터 / 동수 = N대 (정수). null fallback (D-07 building_count null 시).
    - getComplexRawTransactions: complex_transactions_for_chart RPC 호출 → RawTransaction[] 반환. server-only.
  </behavior>
  <action>
    파일 1: `src/lib/utils/facility-format.ts`

    ```typescript
    /**
     * UX-03 — 시설 표시 포맷터
     * D-06: 주차 → "세대당 N.N대" (소수점 1자리)
     * D-07: 엘리베이터 → "동당 N대" (정수). building_count null → null (항목 숨김)
     */
    export function formatParkingPerUnit(
      parkingCount: number | null,
      householdCount: number | null,
    ): string | null {
      if (parkingCount == null) return null
      if (householdCount == null || householdCount <= 0) return null
      return (parkingCount / householdCount).toFixed(1)
    }

    export function formatElevatorPerBuilding(
      elevatorCount: number | null,
      buildingCount: number | null,
    ): string | null {
      if (elevatorCount == null) return null
      if (buildingCount == null || buildingCount <= 0) return null
      return String(Math.round(elevatorCount / buildingCount))
    }
    ```

    파일 2: `src/lib/data/complex-detail.ts` 확장 — 기존 파일 끝에 추가 (기존 코드 보존):

    ```typescript
    // 기존 코드 유지 (getComplexById, getComplexTransactionSummary, MonthlyPriceSummary)
    // 아래 ★ 추가:

    export interface RawTransaction {
      dealDate:  string  // "YYYY-MM-DD"
      yearMonth: string  // "YYYY-MM"
      price:     number  // 만원
      area:      number  // m2 (numeric → number)
    }

    /**
     * UX-01/UX-02 — 개별 거래 행 fetch (IQR 계산 + 평형 그룹화 + 기간 slice 원천)
     * complex_transactions_for_chart RPC 호출 — Phase 9 Wave 0 신규
     */
    export async function getComplexRawTransactions(
      complexId: string,
      dealType: 'sale' | 'jeonse',
      supabase: SupabaseClient,
      months = 120,
    ): Promise<RawTransaction[]> {
      const { data, error } = await supabase.rpc('complex_transactions_for_chart', {
        p_complex_id: complexId,
        p_deal_type:  dealType,
        p_months:     months,
        p_area_m2:    null,  // 전체 평형 fetch — 클라이언트에서 slice
      })
      if (error) throw new Error(`getComplexRawTransactions failed: ${error.message}`)
      if (!data) return []
      return (data as Array<{ deal_date: string; year_month: string; price: number | string; area_m2: number | string }>).map(
        (row) => ({
          dealDate:  row.deal_date,
          yearMonth: row.year_month,
          price:     Number(row.price),
          area:      Number(row.area_m2),
        }),
      )
    }
    ```

    Note: 함수 선언 위치는 기존 `getComplexTransactionSummary` 바로 아래. `import type { SupabaseClient }`는 이미 1번 줄에 import되어 있음.
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/phase9-ux.test.ts -t "parking|elevator" --reporter=verbose 2>&1 | grep -E "✓|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils/facility-format.ts` 파일 존재
    - `grep -c "export function formatParkingPerUnit" src/lib/utils/facility-format.ts` >= 1
    - `grep -c "export function formatElevatorPerBuilding" src/lib/utils/facility-format.ts` >= 1
    - `grep -c "getComplexRawTransactions" src/lib/data/complex-detail.ts` >= 1
    - `grep -c "complex_transactions_for_chart" src/lib/data/complex-detail.ts` >= 1
    - `grep -c "export interface RawTransaction" src/lib/data/complex-detail.ts` >= 1
    - `grep -c "export function getComplexTransactionSummary" src/lib/data/complex-detail.ts` >= 1 (기존 함수 보존)
    - `npm run test -- src/__tests__/phase9-ux.test.ts -t "parking|elevator"` 통과 (4 tests pass)
    - `npm run lint` 통과 (수정 파일)
  </acceptance_criteria>
  <done>facility-format.ts + getComplexRawTransactions 함수 작성, 4개 시설 테스트 GREEN, 기존 함수 보존</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: getSeasonalAverages 함수 추가 (UX-04)</name>
  <read_first>
    src/lib/data/management-cost.ts
    src/__tests__/phase9-ux.test.ts
    .planning/phases/09-complex-detail-ux/09-CONTEXT.md
  </read_first>
  <behavior>
    - 6~9월 행 → 하절기, 10~3월 행 → 동절기 (D-08)
    - 평균 = (common_cost_total + individual_cost_total + long_term_repair_monthly) 합산 평균 (null은 0으로 처리)
    - 세대당 평균 = 평균 / householdCount (householdCount null 또는 0이면 null)
    - 카운트 0 → 해당 평균/세대당 모두 null (D-08: 4개월 이상 데이터 있을 때만 표시 — 호출자가 summerCount/winterCount로 판단)
  </behavior>
  <action>
    수정 대상: `src/lib/data/management-cost.ts` — 기존 코드 보존 + 아래 export 추가

    ```typescript
    // 기존 코드 유지 (ManagementCostRow 인터페이스 + getManagementCostMonthly)
    // 파일 하단에 ★ 추가:

    export interface SeasonalAverages {
      summerAvg:     number | null  // 하절기 (6~9월) 총 단지 합계 월평균
      winterAvg:     number | null  // 동절기 (10~3월) 총 단지 합계 월평균
      summerPerUnit: number | null  // 세대당 하절기 월평균
      winterPerUnit: number | null  // 세대당 동절기 월평균
      summerCount:   number         // 하절기 row 개수
      winterCount:   number         // 동절기 row 개수
    }

    function monthOf(yearMonth: string): number {
      // "YYYY-MM-01" → 6,7,8,9,...
      return parseInt(yearMonth.slice(5, 7), 10)
    }

    function isSummer(m: number): boolean {
      return m >= 6 && m <= 9
    }

    function isWinter(m: number): boolean {
      return m >= 10 || m <= 3
    }

    function rowTotal(r: ManagementCostRow): number {
      return (r.common_cost_total ?? 0)
        + (r.individual_cost_total ?? 0)
        + (r.long_term_repair_monthly ?? 0)
    }

    /**
     * UX-04 — 하절기(6~9월) vs 동절기(10~3월) 월평균 집계
     * 호출자(ManagementCostCard)가 summerCount/winterCount로 D-08 "4개월 이상 데이터 있을 때만 표시" 분기 결정.
     * 평균은 단지 합계 기준. 세대당은 합계 / householdCount.
     */
    export function getSeasonalAverages(
      rows: ManagementCostRow[],
      householdCount: number | null,
    ): SeasonalAverages {
      const summer = rows.filter(r => isSummer(monthOf(r.year_month)))
      const winter = rows.filter(r => isWinter(monthOf(r.year_month)))

      const avg = (subset: ManagementCostRow[]): number | null => {
        if (subset.length === 0) return null
        const total = subset.reduce((sum, r) => sum + rowTotal(r), 0)
        return Math.round(total / subset.length)
      }

      const perUnit = (av: number | null): number | null => {
        if (av == null) return null
        if (householdCount == null || householdCount <= 0) return null
        return Math.round(av / householdCount)
      }

      const summerAvg = avg(summer)
      const winterAvg = avg(winter)

      return {
        summerAvg,
        winterAvg,
        summerPerUnit: perUnit(summerAvg),
        winterPerUnit: perUnit(winterAvg),
        summerCount: summer.length,
        winterCount: winter.length,
      }
    }
    ```
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/phase9-ux.test.ts -t "getSeasonalAverages" --reporter=verbose 2>&1 | grep -E "✓|PASS|FAIL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface SeasonalAverages" src/lib/data/management-cost.ts` >= 1
    - `grep -c "export function getSeasonalAverages" src/lib/data/management-cost.ts` >= 1
    - `grep -c "isSummer\\|isWinter" src/lib/data/management-cost.ts` >= 2 (두 헬퍼)
    - `grep -E "m\\s*>=\\s*6\\s*&&\\s*m\\s*<=\\s*9" src/lib/data/management-cost.ts` 매칭 (하절기 범위)
    - `grep -E "m\\s*>=\\s*10\\s*\\|\\|\\s*m\\s*<=\\s*3" src/lib/data/management-cost.ts` 매칭 (동절기 범위 wrap-around)
    - `grep -c "export async function getManagementCostMonthly" src/lib/data/management-cost.ts` >= 1 (기존 보존)
    - `npm run test -- src/__tests__/phase9-ux.test.ts -t "getSeasonalAverages"` 통과 (3 tests pass)
    - `npm run lint && npm run build` 통과
  </acceptance_criteria>
  <done>getSeasonalAverages export 추가, 3개 계절 평균 테스트 GREEN, 기존 getManagementCostMonthly 보존</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getComplexRawTransactions → RPC | server-only 모듈. SupabaseClient는 RSC에서만 주입 — 클라이언트 직접 호출 불가 |
| 유틸리티 함수 → 입력 데이터 | 순수 함수 — 외부 IO 없음. 외부 입력 검증 불필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | Input Validation | getSeasonalAverages year_month 파싱 | mitigate | parseInt + slice(5,7) — DB 출력은 항상 "YYYY-MM-01" 형식 (VERIFIED: 09-RESEARCH.md A3). 비정상 입력 시 NaN → isSummer/isWinter 모두 false → row 무시 (안전한 degradation). |
| T-9-05 | DoS | computeIqrOutliers 입력 크기 | accept | 단일 단지 10년치 거래 < 500행. O(n log n) sort — 무시 가능한 비용. |
</threat_model>

<verification>
- 4개 유틸리티 파일 + 2개 데이터 모듈 확장 완료
- `npm run test -- src/__tests__/iqr.test.ts src/__tests__/phase9-ux.test.ts` 통과 (총 16건 GREEN)
- `npm run lint && npm run build` 통과
- Wave 0 RED 상태 → Wave 1 GREEN 전환 완료
</verification>

<success_criteria>
- 6개 컬렉션 export 추가/유지: computeIqrOutliers, filterByPeriod, extractAreaGroups, filterByArea, formatParkingPerUnit, formatElevatorPerBuilding (유틸), getComplexRawTransactions, getSeasonalAverages (데이터 레이어)
- 모든 Wave 0 RED 테스트가 GREEN 전환
- 기존 함수(getComplexById, getComplexTransactionSummary, getManagementCostMonthly) 시그니처 변경 없음 — Wave 2 컴포넌트가 안전하게 import 가능
</success_criteria>

<output>
완료 후 `.planning/phases/09-complex-detail-ux/09-01-SUMMARY.md` 작성. 테스트 통과 라인 수 + RawTransaction 타입 시그니처 포함.
</output>

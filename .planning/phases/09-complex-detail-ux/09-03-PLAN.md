---
phase: 09-complex-detail-ux
plan: "03"
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "시설 카드에 '주차대수' 항목이 '세대당 N.N대 (총 NNN면)' 형식으로 표시된다 (D-06)"
    - "시설 카드에 '엘리베이터' 항목이 'building_count != null'일 때 '동당 N대 (총 N대)' 형식으로 표시된다 (D-07)"
    - "building_count IS NULL이면 엘리베이터 항목에서 '동당 N대' prefix 없이 기존 '총 N대' 형식으로 fallback (D-07 Claude's Discretion)"
    - "facility_kapt SELECT가 building_count 컬럼을 포함한다"
    - "ISR (revalidate = 86400)이 유지된다 — page.tsx에서 searchParams 읽지 않음"
  artifacts:
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "facility_kapt SELECT에 building_count 포함 + 시설 카드 표시 로직 수정"
      contains: "formatParkingPerUnit"
  key_links:
    - from: "facility_kapt 쿼리"
      to: "building_count 컬럼"
      via: "select('*') 또는 명시적 컬럼 리스트"
      pattern: "facility_kapt"
    - from: "시설 카드 주차 표시"
      to: "formatParkingPerUnit util"
      via: "import + 호출"
      pattern: "formatParkingPerUnit"
    - from: "시설 카드 엘리베이터 표시"
      to: "formatElevatorPerBuilding util"
      via: "import + 호출"
      pattern: "formatElevatorPerBuilding"
---

<objective>
단지 상세 RSC 시설 카드(`<div className="card">시설</div>`)의 주차/엘리베이터 표시 방식을 D-06/D-07에 맞춰 개선한다.

Purpose: UX-03 실수요자 관점 — 절대 수치만으로는 단지 규모를 알 수 없음. 세대당/동당 환산으로 단지 간 비교 가능한 지표 제공.
Output: page.tsx 시설 카드 섹션 수정. 데이터 레이어/유틸은 09-01에서 제공받음.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-complex-detail-ux/09-CONTEXT.md
@.planning/phases/09-complex-detail-ux/09-RESEARCH.md
@.planning/phases/09-complex-detail-ux/09-00-SUMMARY.md
@src/app/complexes/[id]/page.tsx

<interfaces>
<!-- 09-01에서 제공되는 utils (Wave 1 병렬 — 09-03이 09-01 이전 종료될 수도 있으나 종료 시점에 import 가능해야 함) -->

From src/lib/utils/facility-format.ts (09-01 Task 2 출력):
```typescript
export function formatParkingPerUnit(
  parkingCount: number | null,
  householdCount: number | null,
): string | null
// 예: 840, 700 → "1.2"

export function formatElevatorPerBuilding(
  elevatorCount: number | null,
  buildingCount: number | null,
): string | null
// 예: 12, 6 → "2"; building_count null → null
```

From src/app/complexes/[id]/page.tsx (현재 시설 카드 — 라인 491~590):
```tsx
{/* 시설 탭 (DATA-01) */}
<div className="card" style={{ padding: 20 }}>
  <h3>시설</h3>
  <p>K-apt 공동주택 데이터 기준</p>
  {facilityKapt ? (
    <div>
      {[
        { label: '주차대수', value: facilityKapt.parking_count != null ? `총 ${facilityKapt.parking_count.toLocaleString()}면` : null },
        { label: '세대수', value: complex.household_count != null ? `${complex.household_count.toLocaleString()}세대` : null },
        { label: '관리비(m²당)', value: ... },
        { label: '난방방식', value: complex.heat_type ?? null },
        { label: '관리방식', value: ((facilityKapt as unknown) as { management_type?: string | null }).management_type ?? null },
        { label: '엘리베이터', value: (() => { const cnt = ((facilityKapt as unknown) as { elevator_count?: number | null }).elevator_count; return cnt != null ? `${cnt}대` : null })() },
      ].filter(item => item.value !== null).map(...)}
    </div>
  ) : (...)}
</div>
```

From src/lib/data/complex-detail.ts (현재 ComplexDetail 인터페이스):
```typescript
export interface ComplexDetail {
  household_count: number | null
  // ... 기타
}
```

facility_kapt 쿼리는 `select('*')`이므로 building_count 컬럼은 자동 포함. 추가 SELECT 변경 불필요. 단, 타입 캐스팅이 필요한 신규 컬럼.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: facility-format 유틸 import + 주차/엘리베이터 표시 변경</name>
  <read_first>
    src/app/complexes/[id]/page.tsx
    src/lib/utils/facility-format.ts
    .planning/phases/09-complex-detail-ux/09-CONTEXT.md
  </read_first>
  <action>
    수정 대상: `src/app/complexes/[id]/page.tsx`

    변경 1 — import 라인 추가 (기존 ManagementCostCard import 아래):
    ```typescript
    import { formatParkingPerUnit, formatElevatorPerBuilding } from '@/lib/utils/facility-format'
    ```

    변경 2 — 시설 카드 직전 (또는 facilityKapt 변수 선언 후) 세대당/동당 값 사전 계산:
    ```typescript
    // facility_kapt 결과 처리 직후 (current line 187 ~ facilityKapt 변수 선언 이후):
    const facilityKapt = facilityKaptResult?.data ?? null

    // ★ 추가 — UX-03 D-06/D-07 환산값
    const parkingPerUnit = formatParkingPerUnit(
      (facilityKapt as { parking_count?: number | null } | null)?.parking_count ?? null,
      complex.household_count,
    )
    const elevatorCount = (facilityKapt as { elevator_count?: number | null } | null)?.elevator_count ?? null
    const buildingCount = (facilityKapt as { building_count?: number | null } | null)?.building_count ?? null
    const elevatorPerBuilding = formatElevatorPerBuilding(elevatorCount, buildingCount)
    ```

    변경 3 — 시설 카드 배열 항목 교체. 기존:
    ```typescript
    {
      label: '주차대수',
      value: facilityKapt.parking_count != null
        ? `총 ${facilityKapt.parking_count.toLocaleString()}면`
        : null,
    },
    ```
    교체 후 (D-06: "세대당 N.N대" 우선, 괄호에 총합):
    ```typescript
    {
      label: '주차대수',
      value: parkingPerUnit != null
        ? `세대당 ${parkingPerUnit}대 (총 ${(facilityKapt.parking_count as number).toLocaleString()}면)`
        : (facilityKapt.parking_count != null
            ? `총 ${facilityKapt.parking_count.toLocaleString()}면`
            : null),
    },
    ```

    변경 4 — 엘리베이터 항목 교체. 기존:
    ```typescript
    {
      label: '엘리베이터',
      value: (() => {
        const cnt = ((facilityKapt as unknown) as { elevator_count?: number | null }).elevator_count
        return cnt != null ? `${cnt}대` : null
      })(),
    },
    ```
    교체 후 (D-07: building_count 있으면 "동당 N대 (총 NN대, NN동)", 없으면 기존 "총 N대" fallback):
    ```typescript
    {
      label: '엘리베이터',
      value: (() => {
        if (elevatorCount == null) return null
        if (elevatorPerBuilding != null && buildingCount != null) {
          return `동당 ${elevatorPerBuilding}대 (총 ${elevatorCount}대, ${buildingCount}동)`
        }
        return `${elevatorCount}대`
      })(),
    },
    ```

    변경 5 — 시설 카드 항목 배열에 동수 항목 추가 (선택). building_count가 있을 때 단지 규모 정보 노출. 기존 항목 사이 적절한 위치(엘리베이터 위)에 삽입:
    ```typescript
    {
      label: '동수',
      value: buildingCount != null ? `${buildingCount}동` : null,
    },
    ```

    ★ 변경 시 주의:
    - revalidate = 86400 라인 (page.tsx line 27) 절대 수정 금지
    - searchParams prop 추가 금지 (ISR 보존)
    - 기존 facility 카드 외 다른 섹션 (chart card, management cost card, header card) 코드 변경 금지
    - facility_kapt 쿼리(supabase.from('facility_kapt').select('*')...)는 그대로 둠 — `select('*')`이므로 building_count 자동 포함
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/app/complexes/[id]/page.tsx','utf8');const u=c.replace(/\\/\\/.*$/gm,'').replace(/\\/\\*[\\s\\S]*?\\*\\//g,'');const must=['formatParkingPerUnit','formatElevatorPerBuilding','parkingPerUnit','elevatorPerBuilding','building_count'];for(const k of must){if(!u.includes(k)){console.error('MISS',k);process.exit(1)}}if(!u.includes('export const revalidate = 86400')){console.error('ISR removed');process.exit(2)}if(/export default async function ComplexDetailPage\\(\\{ params, searchParams/.test(u)){console.error('searchParams added - ISR broken');process.exit(3)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "formatParkingPerUnit" src/app/complexes/[id]/page.tsx` (주석 제외) >= 2 (import + 호출)
    - `grep -c "formatElevatorPerBuilding" src/app/complexes/[id]/page.tsx` (주석 제외) >= 2
    - `grep -c "building_count" src/app/complexes/[id]/page.tsx` (주석 제외) >= 1
    - `grep -c "export const revalidate = 86400" src/app/complexes/[id]/page.tsx` >= 1 (ISR 유지)
    - `grep -E "params: Promise<\\{ id: string \\}>" src/app/complexes/[id]/page.tsx` 매칭 (searchParams prop 미추가 — ISR 보존)
    - `grep -E "세대당 \\$\\{parkingPerUnit\\}대" src/app/complexes/[id]/page.tsx` 매칭
    - `grep -E "동당 \\$\\{elevatorPerBuilding\\}대" src/app/complexes/[id]/page.tsx` 매칭
    - `npm run lint && npm run build` 통과
    - `npm run build` 출력에 `/complexes/[id]` 가 `λ Dynamic`이 아닌 `○` 또는 `●` (정적/ISR) 마커로 표시됨 (ISR 미파괴 확인)
  </acceptance_criteria>
  <done>시설 카드 주차/엘리베이터 표시가 D-06/D-07 형식으로 변경되고 ISR 유지됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| facility_kapt → 클라이언트 | RSC에서 fetch, 클라이언트로 props 전달 시 RLS public read 검증됨 |
| building_count 추가 컬럼 | KAPT 공개 데이터 — 민감정보 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-06 | Tampering | parkingPerUnit 계산 (DBN 입력 신뢰) | accept | facility_kapt RLS public read만 가능. 변조 가능성 없음. household_count는 complexes 테이블 — admin write only. |
| T-9-07 | DoS via ISR 파괴 | page.tsx searchParams 추가 | mitigate | 본 Plan은 searchParams prop 추가 금지 — params 인터페이스 변경 금지로 컴파일 시점 강제. 빌드 출력 마커로 검증. |
</threat_model>

<verification>
- src/app/complexes/[id]/page.tsx 시설 카드 섹션 D-06/D-07 형식으로 변경
- ISR (`revalidate = 86400`) 유지
- `npm run lint && npm run build` 통과
- 빌드 출력에서 `/complexes/[id]`가 dynamic이 아닌 정적/ISR 마커로 표시
</verification>

<success_criteria>
- 시설 카드의 '주차대수' 행이 "세대당 N.N대 (총 NNN면)" 형식 (household_count + parking_count 모두 있을 때)
- 시설 카드의 '엘리베이터' 행이 "동당 N대 (총 NN대, NN동)" 형식 (building_count + elevator_count 모두 있을 때)
- building_count null fallback: "N대" 단순 표시
- ISR 보존: 빌드 시 `λ Dynamic` 출력 없음
</success_criteria>

<output>
완료 후 `.planning/phases/09-complex-detail-ux/09-03-SUMMARY.md` 작성. 변경된 라인 인용 + ISR 보존 빌드 출력 캡처 포함.
</output>

---
phase: 09-complex-detail-ux
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260514000001_phase9_building_count.sql
  - supabase/migrations/20260514000002_phase9_transactions_for_chart.sql
  - scripts/kapt-facility-enrich.ts
  - src/__tests__/iqr.test.ts
  - src/__tests__/phase9-ux.test.ts
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
user_setup: []

must_haves:
  truths:
    - "facility_kapt 테이블에 building_count 컬럼이 존재한다"
    - "complex_transactions_for_chart RPC가 개별 거래 행을 area_m2 필터와 함께 반환한다"
    - "kapt-facility-enrich.ts가 kaptDongCnt를 facility_kapt.building_count에 upsert한다"
    - "src/__tests__/iqr.test.ts가 RED 상태로 존재한다 (iqr.ts 미존재)"
    - "src/__tests__/phase9-ux.test.ts가 RED 상태로 존재한다 (대상 함수 미존재)"
    - "supabase db push 적용 완료"
  artifacts:
    - path: "supabase/migrations/20260514000001_phase9_building_count.sql"
      provides: "facility_kapt.building_count 컬럼 (integer, nullable)"
      contains: "alter table public.facility_kapt"
    - path: "supabase/migrations/20260514000002_phase9_transactions_for_chart.sql"
      provides: "complex_transactions_for_chart RPC (개별 거래 행 + area 필터)"
      contains: "create or replace function public.complex_transactions_for_chart"
    - path: "scripts/kapt-facility-enrich.ts"
      provides: "kaptDongCnt → building_count 매핑 추가"
      contains: "building_count"
    - path: "src/__tests__/iqr.test.ts"
      provides: "IQR 계산 유틸리티 RED 테스트 (UX-01)"
      min_lines: 30
    - path: "src/__tests__/phase9-ux.test.ts"
      provides: "기간 slice, 평형 그룹, 주차/엘리베이터 계산, 계절 평균 RED 테스트 (UX-01~04)"
      min_lines: 60
  key_links:
    - from: "kapt-facility-enrich.ts upsert payload"
      to: "facility_kapt.building_count"
      via: "parsed.kaptDongCnt → upsert column"
      pattern: "building_count:\\s*parsed\\."
    - from: "complex_transactions_for_chart RPC"
      to: "public.transactions"
      via: "cancel_date IS NULL AND superseded_by IS NULL filter"
      pattern: "cancel_date\\s+is\\s+null"
---

<objective>
Phase 9 DB 기반 확립 + RED 테스트 스캐폴드. Wave 1 실행 전 (a) building_count 컬럼 마이그레이션, (b) complex_transactions_for_chart RPC (개별 거래 행 + area 필터), (c) kapt-facility-enrich.ts에 kaptDongCnt → building_count 매핑 추가, (d) iqr.ts + phase9-ux 테스트 RED 작성.

Purpose: D-07 (엘리베이터 동당)을 위한 building_count 인프라 + UX-01 IQR 이상치 계산용 개별 거래 데이터 RPC 마련. Wave 1/2 병렬 실행 가능하도록 DB 기반 한 번에 push.
Output: 2개 SQL 마이그레이션 + kapt-facility-enrich.ts 패치 + 2개 RED 테스트 파일 + supabase db push 완료.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-complex-detail-ux/09-CONTEXT.md
@.planning/phases/09-complex-detail-ux/09-RESEARCH.md
@supabase/migrations/20260430000003_transactions.sql
@supabase/migrations/20260430000004_facility.sql
@supabase/migrations/20260430000013_complex_detail_functions.sql
@supabase/migrations/20260513000001_facility_kapt_elevator.sql
@scripts/kapt-facility-enrich.ts

<interfaces>
<!-- 기존 스키마/패턴 — Wave 0이 의존하는 인프라 -->

From supabase/migrations/20260430000003_transactions.sql:
```sql
-- transactions 테이블 핵심 컬럼
deal_type        public.deal_type not null    -- 'sale'|'jeonse'|'monthly'
deal_date        date not null
price            bigint                       -- 만원
area_m2          numeric(6, 2) not null
cancel_date      date                         -- IS NULL 필수
superseded_by    bigint                       -- IS NULL 필수
complex_id       uuid
```

From supabase/migrations/20260430000004_facility.sql:
```sql
-- facility_kapt 원본 스키마 (Phase 9 이전)
complex_id, kapt_code, management_cost_m2, parking_count, data_month
-- elevator_count는 20260513000001에서 추가됨
-- management_type은 20260513000002에서 추가됨
-- building_count는 본 Phase에서 추가 ← 신규
```

From supabase/migrations/20260430000013_complex_detail_functions.sql:
```sql
-- 기존 RPC: 월별 집계만 반환 (IQR용 개별 거래 부재)
create or replace function public.complex_monthly_prices(...) returns table (
  year_month text, avg_price numeric, count bigint, avg_area numeric
)
```

From src/services/kapt.ts:
```typescript
export const kaptBasicInfoSchema = z.object({
  kaptCode: z.string(),
  kaptName: z.string(),
  kaptdaCnt: z.coerce.number().optional(),     // 세대수
  kaptDongCnt: z.coerce.number().optional(),   // ★ 동수 — building_count 원천
  ...
})

// fetchKaptDetailInfo: kaptdEcnt(엘리베이터), kaptdPcntu/kaptdPcnt(주차) 반환
// fetchKaptBasicInfo: kaptDongCnt 반환 ← 본 Phase에서 사용
```

From scripts/kapt-facility-enrich.ts (현재 패턴):
```typescript
// 현재 fetchKaptDetailInfo만 호출 — kaptDongCnt 미수집
// 본 Phase에서 fetchKaptBasicInfo 추가 호출하여 kaptDongCnt 획득
const { parsed: detailParsed } = await fetchKaptDetailInfo(complex.kapt_code)
// 새로 추가:
const basicParsed = await fetchKaptBasicInfo(complex.kapt_code)

await supabase.from('facility_kapt').upsert({
  complex_id, kapt_code,
  parking_count, elevator_count, management_type,
  building_count: basicParsed?.kaptDongCnt ?? null,  // ★ 추가
  management_cost_m2: null,
  data_month: DATA_MONTH,
}, { onConflict: 'complex_id,data_month' })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: building_count 마이그레이션 + complex_transactions_for_chart RPC 작성</name>
  <read_first>
    supabase/migrations/20260430000004_facility.sql
    supabase/migrations/20260513000001_facility_kapt_elevator.sql
    supabase/migrations/20260513000002_facility_kapt_management_type.sql
    supabase/migrations/20260430000013_complex_detail_functions.sql
    supabase/migrations/20260430000003_transactions.sql
  </read_first>
  <action>
    파일 1: `supabase/migrations/20260514000001_phase9_building_count.sql`

    ```sql
    -- Phase 9: facility_kapt.building_count 컬럼 추가 (UX-03 엘리베이터 동당 계산용)
    -- 원천: KAPT BasicInfo API의 kaptDongCnt (동수)
    -- Idempotent: ADD COLUMN IF NOT EXISTS

    alter table public.facility_kapt
      add column if not exists building_count integer;

    comment on column public.facility_kapt.building_count is
      'UX-03: 동수 (KAPT kaptDongCnt). 엘리베이터 동당 대수 계산: elevator_count / building_count.';
    ```

    파일 2: `supabase/migrations/20260514000002_phase9_transactions_for_chart.sql`

    ```sql
    -- Phase 9: 단지별 개별 거래 행 + area 필터 RPC (UX-01 IQR 이상치, UX-02 평형 필터)
    -- 기존 complex_monthly_prices RPC는 월별 집계만 반환 → IQR 계산 불가
    -- 본 RPC는 개별 거래 행을 반환하여 클라이언트에서 IQR 계산 + 평형 그룹 추출 가능

    create or replace function public.complex_transactions_for_chart(
      p_complex_id uuid,
      p_deal_type  text,
      p_months     int     default 120,
      p_area_m2    numeric default null
    ) returns table (
      deal_date  text,
      year_month text,
      price      numeric,
      area_m2    numeric
    ) language sql stable as $$
      select
        deal_date::text                       as deal_date,
        to_char(deal_date, 'YYYY-MM')         as year_month,
        price::numeric                        as price,
        area_m2::numeric                      as area_m2
      from public.transactions
      where
        complex_id     = p_complex_id
        and deal_type  = p_deal_type::public.deal_type
        and deal_date  >= (now() - (p_months || ' months')::interval)::date
        and cancel_date    is null
        and superseded_by  is null
        and (p_area_m2 is null or round(area_m2) = round(p_area_m2))
      order by deal_date asc
    $$;

    comment on function public.complex_transactions_for_chart is
      'UX-01/UX-02: 개별 거래 행 반환 (area 필터 선택적). cancel_date IS NULL AND superseded_by IS NULL 필수.';
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('supabase/migrations/20260514000001_phase9_building_count.sql','utf8');const b=fs.readFileSync('supabase/migrations/20260514000002_phase9_transactions_for_chart.sql','utf8');if(!a.includes('add column if not exists building_count'))process.exit(1);if(!b.includes('complex_transactions_for_chart'))process.exit(2);if(!b.includes('cancel_date'))process.exit(3);if(!b.includes('superseded_by'))process.exit(4);if(!b.includes('round(area_m2) = round(p_area_m2)'))process.exit(5);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260514000001_phase9_building_count.sql` 파일 존재
    - `grep -c "add column if not exists building_count" supabase/migrations/20260514000001_phase9_building_count.sql` >= 1
    - `supabase/migrations/20260514000002_phase9_transactions_for_chart.sql` 파일 존재
    - `grep -c "complex_transactions_for_chart" supabase/migrations/20260514000002_phase9_transactions_for_chart.sql` >= 1
    - `grep -c "cancel_date" supabase/migrations/20260514000002_phase9_transactions_for_chart.sql` >= 1 (CLAUDE.md CRITICAL)
    - `grep -c "superseded_by" supabase/migrations/20260514000002_phase9_transactions_for_chart.sql` >= 1
    - `grep -E "round\\(area_m2\\)\\s*=\\s*round\\(p_area_m2\\)" supabase/migrations/20260514000002_phase9_transactions_for_chart.sql` 매칭 (area 필터 round 방식)
  </acceptance_criteria>
  <done>2개 SQL 마이그레이션 파일이 명세대로 생성됨 (push 전)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: kapt-facility-enrich.ts에 kaptDongCnt → building_count 매핑 추가</name>
  <read_first>
    scripts/kapt-facility-enrich.ts
    src/services/kapt.ts
  </read_first>
  <action>
    수정 대상: `scripts/kapt-facility-enrich.ts`

    변경 1 — import 라인에 `fetchKaptBasicInfo` 추가:
    ```typescript
    // 현재:
    import { fetchKaptDetailInfo } from '../src/services/kapt'
    // 변경 후:
    import { fetchKaptBasicInfo, fetchKaptDetailInfo } from '../src/services/kapt'
    ```

    변경 2 — for 루프 내 fetchKaptDetailInfo 호출 직후, fetchKaptBasicInfo도 호출하여 kaptDongCnt 수집:
    ```typescript
    const { parsed, raw } = await fetchKaptDetailInfo(complex.kapt_code)
    // ★ 추가: BasicInfo 호출 (동수 — kaptDongCnt 수집)
    let basicParsed = null as Awaited<ReturnType<typeof fetchKaptBasicInfo>>
    try {
      basicParsed = await fetchKaptBasicInfo(complex.kapt_code)
    } catch (err) {
      console.warn(`${progress} BasicInfo 호출 실패 (building_count = null로 진행):`, err instanceof Error ? err.message : err)
    }
    ```

    변경 3 — upsert payload에 `building_count: basicParsed?.kaptDongCnt ?? null` 추가:
    ```typescript
    const { error: upsertError } = await supabase
      .from('facility_kapt')
      .upsert(
        {
          complex_id: complex.id,
          kapt_code: complex.kapt_code,
          parking_count: parkingTotal,
          elevator_count: parsed.kaptdEcnt ?? null,
          management_type: parsed.codeMgr ?? null,
          building_count: basicParsed?.kaptDongCnt ?? null,  // ★ 추가
          management_cost_m2: null,
          data_month: DATA_MONTH,
        },
        { onConflict: 'complex_id,data_month' },
      )
    ```

    변경 4 — 로그 라인에 building_count 추가:
    ```typescript
    const fields = [
      parkingTotal != null ? `주차=${parkingTotal}면` : null,
      parsed.kaptdEcnt != null ? `엘리베이터=${parsed.kaptdEcnt}대` : null,
      basicParsed?.kaptDongCnt != null ? `동수=${basicParsed.kaptDongCnt}` : null,  // ★ 추가
    ].filter(Boolean).join(', ')
    ```

    RATE_LIMIT_DELAY_MS는 두 API 호출에 적용되므로 중간에 1회 더 delay(50) 추가 권장 (BasicInfo + DetailInfo 사이):
    ```typescript
    const { parsed, raw } = await fetchKaptDetailInfo(complex.kapt_code)
    await delay(50)  // ★ BasicInfo와 DetailInfo 사이 짧은 대기
    let basicParsed = null as Awaited<ReturnType<typeof fetchKaptBasicInfo>>
    ```

    실제 스크립트 재실행은 Phase 9 외 운영자 작업 (수동 GitHub Actions trigger). 본 Plan은 코드 변경만 수행.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('scripts/kapt-facility-enrich.ts','utf8');const u=c.replace(/^#.*$/gm,'');const must=['fetchKaptBasicInfo','building_count','kaptDongCnt'];for(const k of must){if((u.match(new RegExp(k,'g'))||[]).length<1){console.error('MISS:',k);process.exit(1)}}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchKaptBasicInfo" scripts/kapt-facility-enrich.ts` (주석 제외) >= 2 (import + 호출)
    - `grep -c "building_count" scripts/kapt-facility-enrich.ts` (주석 제외) >= 1 (upsert payload)
    - `grep -c "kaptDongCnt" scripts/kapt-facility-enrich.ts` (주석 제외) >= 1
    - `npm run lint` 통과 (해당 파일 lint 오류 없음)
  </acceptance_criteria>
  <done>kapt-facility-enrich.ts가 BasicInfo 호출 + building_count upsert 코드를 포함</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: RED 테스트 스캐폴드 작성 (iqr.test.ts + phase9-ux.test.ts)</name>
  <read_first>
    src/__tests__/compare.test.ts
    src/__tests__/member-tier.test.ts
    .planning/phases/09-complex-detail-ux/09-RESEARCH.md
  </read_first>
  <action>
    파일 1: `src/__tests__/iqr.test.ts` (UX-01 IQR 유틸리티 RED 테스트)

    ```typescript
    /**
     * UX-01 — IQR 1.5x 이상치 계산 유틸리티
     * RED: src/lib/utils/iqr.ts 미존재 → import 실패 예상
     */
    import { describe, it, expect } from 'vitest'

    describe('computeIqrOutliers', () => {
      it('정상 분포: 이상치 없음 → outliers 빈 배열', async () => {
        const { computeIqrOutliers } = await import('@/lib/utils/iqr')
        const points = [
          { yearMonth: '2026-01', price: 50000, area: 84 },
          { yearMonth: '2026-02', price: 51000, area: 84 },
          { yearMonth: '2026-03', price: 49000, area: 84 },
          { yearMonth: '2026-04', price: 52000, area: 84 },
          { yearMonth: '2026-05', price: 50500, area: 84 },
        ]
        const { normal, outliers } = computeIqrOutliers(points)
        expect(outliers).toHaveLength(0)
        expect(normal).toHaveLength(5)
      })

      it('극단값 포함: IQR 1.5x 범위 밖 → outliers로 분류', async () => {
        const { computeIqrOutliers } = await import('@/lib/utils/iqr')
        // 50000~52000 분포 + 극단값 999999
        const points = [
          { yearMonth: '2026-01', price: 50000, area: 84 },
          { yearMonth: '2026-02', price: 51000, area: 84 },
          { yearMonth: '2026-03', price: 49000, area: 84 },
          { yearMonth: '2026-04', price: 52000, area: 84 },
          { yearMonth: '2026-05', price: 50500, area: 84 },
          { yearMonth: '2026-06', price: 999999, area: 84 },  // 명백한 이상치
        ]
        const { normal, outliers } = computeIqrOutliers(points)
        expect(outliers).toHaveLength(1)
        expect(outliers[0]!.price).toBe(999999)
        expect(normal).toHaveLength(5)
      })

      it('빈 배열 입력 → 둘 다 빈 배열', async () => {
        const { computeIqrOutliers } = await import('@/lib/utils/iqr')
        const { normal, outliers } = computeIqrOutliers([])
        expect(normal).toEqual([])
        expect(outliers).toEqual([])
      })

      it('단일 거래: IQR 계산 불가 → 모두 normal로 분류', async () => {
        const { computeIqrOutliers } = await import('@/lib/utils/iqr')
        const points = [{ yearMonth: '2026-01', price: 50000, area: 84 }]
        const { normal, outliers } = computeIqrOutliers(points)
        expect(normal).toHaveLength(1)
        expect(outliers).toEqual([])
      })
    })
    ```

    파일 2: `src/__tests__/phase9-ux.test.ts` (기간 slice, 평형 그룹, 주차/엘리베이터 계산, 계절 평균)

    ```typescript
    /**
     * Phase 9 UX — 기간 slice + 평형 그룹 + 시설 계산 + 계절 평균
     * RED: 대상 함수 미존재 → import 실패 예상
     */
    import { describe, it, expect } from 'vitest'

    describe('filterByPeriod (UX-01)', () => {
      it("'1y' → 최근 1년 내 거래만 반환", async () => {
        const { filterByPeriod } = await import('@/lib/utils/period-filter')
        const now = new Date('2026-05-14')
        const points = [
          { dealDate: '2026-04-01', price: 50000, area: 84 },  // 1y in
          { dealDate: '2025-06-01', price: 48000, area: 84 },  // 1y in (11개월 전)
          { dealDate: '2024-01-01', price: 40000, area: 84 },  // 1y out
        ]
        const result = filterByPeriod(points, '1y', now)
        expect(result).toHaveLength(2)
      })

      it("'all' → 모든 거래 반환", async () => {
        const { filterByPeriod } = await import('@/lib/utils/period-filter')
        const now = new Date('2026-05-14')
        const points = [
          { dealDate: '2020-01-01', price: 30000, area: 84 },
          { dealDate: '2026-04-01', price: 50000, area: 84 },
        ]
        const result = filterByPeriod(points, 'all', now)
        expect(result).toHaveLength(2)
      })

      it("'3y' → 36개월 컷오프", async () => {
        const { filterByPeriod } = await import('@/lib/utils/period-filter')
        const now = new Date('2026-05-14')
        const points = [
          { dealDate: '2023-06-01', price: 45000, area: 84 },   // 3y in
          { dealDate: '2022-12-01', price: 42000, area: 84 },   // 3y out
        ]
        const result = filterByPeriod(points, '3y', now)
        expect(result).toHaveLength(1)
        expect(result[0]!.dealDate).toBe('2023-06-01')
      })
    })

    describe('extractAreaGroups (UX-02)', () => {
      it('동일 평형 다양한 area_m2 값 → ROUND로 그룹화', async () => {
        const { extractAreaGroups } = await import('@/lib/utils/area-groups')
        const points = [
          { dealDate: '2026-01-01', price: 50000, area: 84.99 },
          { dealDate: '2026-02-01', price: 51000, area: 84.97 },
          { dealDate: '2026-03-01', price: 49000, area: 84.82 },
          { dealDate: '2026-04-01', price: 60000, area: 109.5 },
        ]
        const groups = extractAreaGroups(points)
        expect(groups).toEqual([85, 110]) // ROUND 결과, 거래량 순
      })

      it('최다 거래 평형이 첫 번째로 정렬됨', async () => {
        const { extractAreaGroups } = await import('@/lib/utils/area-groups')
        const points = [
          { dealDate: '2026-01-01', price: 50000, area: 59 },
          { dealDate: '2026-02-01', price: 51000, area: 84 },
          { dealDate: '2026-03-01', price: 49000, area: 84 },
          { dealDate: '2026-04-01', price: 60000, area: 84 },
        ]
        const groups = extractAreaGroups(points)
        expect(groups[0]).toBe(84) // 3건 — 최다
        expect(groups[1]).toBe(59)
      })

      it('빈 배열 → 빈 배열', async () => {
        const { extractAreaGroups } = await import('@/lib/utils/area-groups')
        expect(extractAreaGroups([])).toEqual([])
      })
    })

    describe('parking/elevator 계산 (UX-03)', () => {
      it('parkingPerUnit: 840면 / 700세대 → "1.2"', async () => {
        const { formatParkingPerUnit } = await import('@/lib/utils/facility-format')
        const result = formatParkingPerUnit(840, 700)
        expect(result).toBe('1.2')
      })

      it('parkingPerUnit: 세대수 0 또는 null → null', async () => {
        const { formatParkingPerUnit } = await import('@/lib/utils/facility-format')
        expect(formatParkingPerUnit(840, null)).toBeNull()
        expect(formatParkingPerUnit(840, 0)).toBeNull()
        expect(formatParkingPerUnit(null, 700)).toBeNull()
      })

      it('elevatorPerBuilding: 12대 / 6동 → "2"', async () => {
        const { formatElevatorPerBuilding } = await import('@/lib/utils/facility-format')
        const result = formatElevatorPerBuilding(12, 6)
        expect(result).toBe('2')
      })

      it('elevatorPerBuilding: building_count null → null (UX-03 fallback)', async () => {
        const { formatElevatorPerBuilding } = await import('@/lib/utils/facility-format')
        expect(formatElevatorPerBuilding(12, null)).toBeNull()
      })
    })

    describe('getSeasonalAverages (UX-04)', () => {
      it('하절기 + 동절기 혼재 6개월: 양쪽 평균 계산', async () => {
        const { getSeasonalAverages } = await import('@/lib/data/management-cost')
        // year_month "YYYY-MM-01" 형식 — D-08 6~9월 하절기, 10~3월 동절기
        const rows = [
          { year_month: '2026-01-01', common_cost_total: 100000, individual_cost_total: 200000, long_term_repair_monthly: 50000 },
          { year_month: '2025-12-01', common_cost_total: 110000, individual_cost_total: 210000, long_term_repair_monthly: 50000 },
          { year_month: '2025-11-01', common_cost_total: 105000, individual_cost_total: 205000, long_term_repair_monthly: 50000 },
          { year_month: '2025-10-01', common_cost_total: 100000, individual_cost_total: 200000, long_term_repair_monthly: 50000 },
          { year_month: '2025-09-01', common_cost_total:  80000, individual_cost_total: 100000, long_term_repair_monthly: 50000 }, // 하절기
          { year_month: '2025-08-01', common_cost_total:  85000, individual_cost_total: 110000, long_term_repair_monthly: 50000 }, // 하절기
        ] as never
        const result = getSeasonalAverages(rows, 700)
        expect(result.winterCount).toBe(4)
        expect(result.summerCount).toBe(2)
        expect(result.winterAvg).toBeGreaterThan(0)
        expect(result.summerAvg).toBeGreaterThan(0)
        expect(result.winterAvg).toBeGreaterThan(result.summerAvg!) // 겨울 > 여름 (난방비)
        expect(result.summerPerUnit).toBeGreaterThan(0)
      })

      it('D-08: summerCount < 1 || winterCount < 1 → 표시 불가 신호 (해당 평균 null)', async () => {
        const { getSeasonalAverages } = await import('@/lib/data/management-cost')
        const rows = [
          { year_month: '2026-01-01', common_cost_total: 100000, individual_cost_total: 200000, long_term_repair_monthly: 50000 },
          { year_month: '2025-12-01', common_cost_total: 110000, individual_cost_total: 210000, long_term_repair_monthly: 50000 },
        ] as never
        const result = getSeasonalAverages(rows, 700)
        expect(result.winterCount).toBe(2)
        expect(result.summerCount).toBe(0)
        expect(result.summerAvg).toBeNull()
      })

      it('householdCount null → perUnit 모두 null', async () => {
        const { getSeasonalAverages } = await import('@/lib/data/management-cost')
        const rows = [
          { year_month: '2025-08-01', common_cost_total: 80000, individual_cost_total: 100000, long_term_repair_monthly: 50000 },
        ] as never
        const result = getSeasonalAverages(rows, null)
        expect(result.summerPerUnit).toBeNull()
        expect(result.winterPerUnit).toBeNull()
      })
    })
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['src/__tests__/iqr.test.ts','src/__tests__/phase9-ux.test.ts'].forEach(p=>{if(!fs.existsSync(p)){console.error('MISS',p);process.exit(1)}});const a=fs.readFileSync('src/__tests__/iqr.test.ts','utf8');const b=fs.readFileSync('src/__tests__/phase9-ux.test.ts','utf8');if(!a.includes('computeIqrOutliers'))process.exit(2);if(!b.includes('filterByPeriod'))process.exit(3);if(!b.includes('extractAreaGroups'))process.exit(4);if(!b.includes('formatParkingPerUnit'))process.exit(5);if(!b.includes('formatElevatorPerBuilding'))process.exit(6);if(!b.includes('getSeasonalAverages'))process.exit(7);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/iqr.test.ts` 파일 존재, `grep -c "computeIqrOutliers" src/__tests__/iqr.test.ts` >= 4
    - `src/__tests__/phase9-ux.test.ts` 파일 존재
    - 다음 6개 심볼이 phase9-ux.test.ts에서 import 시도됨 (각각 >= 1회): filterByPeriod, extractAreaGroups, formatParkingPerUnit, formatElevatorPerBuilding, getSeasonalAverages
    - `npm run test -- src/__tests__/iqr.test.ts src/__tests__/phase9-ux.test.ts 2>&1 | grep -E "FAIL|Cannot find module"` 매칭 (RED 상태 확인)
  </acceptance_criteria>
  <done>2개 RED 테스트 파일 작성 완료, npm run test 실행 시 모듈 미존재로 실패</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 4: supabase db push [BLOCKING]</name>
  <read_first>
    supabase/migrations/20260514000001_phase9_building_count.sql
    supabase/migrations/20260514000002_phase9_transactions_for_chart.sql
  </read_first>
  <what-built>
    Task 1에서 작성한 2개 마이그레이션 파일을 Supabase 원격 DB에 적용한다. Wave 1 실행 전 반드시 push 완료해야 한다 (Wave 1의 raw transactions fetch가 RPC에 의존).
  </what-built>
  <how-to-verify>
    1. 터미널에서 실행 (Windows PowerShell):
       ```
       supabase db push
       ```
    2. 출력에 다음 2줄이 포함되고 성공으로 종료되어야 함:
       - "Applying migration 20260514000001_phase9_building_count.sql..."
       - "Applying migration 20260514000002_phase9_transactions_for_chart.sql..."
    3. 검증:
       ```
       supabase db diff
       ```
       diff 없음 또는 "No changes found" 메시지면 성공.
    4. 추가 검증 — Supabase SQL Editor (또는 supabase_mcp list_tables):
       - facility_kapt 컬럼에 building_count 존재 확인
       - complex_transactions_for_chart 함수 존재 확인:
         `SELECT proname FROM pg_proc WHERE proname = 'complex_transactions_for_chart';` → 1행

    오류 발생 시:
    - "permission denied for schema public": SUPABASE_ACCESS_TOKEN 환경 변수 미설정 → 토큰 발급 후 재시도
    - "function ... already exists": create or replace 사용 중 — 정상 (재실행 가능)
    - 다른 오류: 메시지를 그대로 붙여넣고 대화 재개
  </how-to-verify>
  <resume-signal>
    push 성공 후 "db-pushed" 입력. 오류 발생 시 오류 메시지 붙여넣기.
  </resume-signal>
  <acceptance_criteria>
    - `supabase db diff` 결과가 빈 출력 또는 "No changes found"
    - Supabase SQL: `SELECT column_name FROM information_schema.columns WHERE table_name='facility_kapt' AND column_name='building_count'` → 1행 반환
    - Supabase SQL: `SELECT proname FROM pg_proc WHERE proname='complex_transactions_for_chart'` → 1행 반환
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 마이그레이션 파일 → Supabase 원격 | `supabase db push` 경유만 사용. 직접 SQL 실행 금지. |
| RPC 호출 → transactions 테이블 | RLS public read 정책 + cancel_date/superseded_by 필터 강제 (RPC 내부) |
| KAPT API → facility_kapt | service_role key 경유, 환경변수 로그 출력 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering | complex_transactions_for_chart RPC | mitigate | RPC 내부에 cancel_date IS NULL AND superseded_by IS NULL 필터 하드코딩. 호출자 우회 불가. |
| T-9-02 | Information Disclosure | facility_kapt.building_count | accept | 공개 데이터 (KAPT 공시). 별도 RLS write 정책 불필요 — service_role만 write. |
| T-9-03 | DoS | complex_transactions_for_chart with p_months=120 | accept | 단일 단지 10년치 거래 행 — 일반적으로 < 500행. transactions_valid_complex_date_idx 인덱스 활용. |
</threat_model>

<verification>
- 2개 마이그레이션 파일 작성 (Task 1)
- scripts/kapt-facility-enrich.ts 수정 (Task 2)
- 2개 RED 테스트 파일 작성 (Task 3)
- supabase db push 적용 (Task 4)
- `npm run lint` 통과 (kapt-facility-enrich.ts 변경분)
- `npm run test -- src/__tests__/iqr.test.ts src/__tests__/phase9-ux.test.ts` 실행 시 RED (모듈 미존재 오류)
</verification>

<success_criteria>
- facility_kapt 테이블에 building_count 컬럼이 Supabase 원격 DB에 존재
- complex_transactions_for_chart RPC가 원격 DB에 존재
- kapt-facility-enrich.ts 재실행 시 building_count 채워질 코드 경로 완성
- src/__tests__/iqr.test.ts + src/__tests__/phase9-ux.test.ts RED 상태로 존재 (Wave 1에서 GREEN 전환)
</success_criteria>

<output>
완료 후 `.planning/phases/09-complex-detail-ux/09-00-SUMMARY.md` 작성 (실제 마이그레이션 라인 수, 테스트 실패 메시지 샘플 포함).
</output>

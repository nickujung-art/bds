---
phase: 09-complex-detail-ux
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/complex/ManagementCostCard.tsx
autonomous: true
requirements:
  - UX-04

must_haves:
  truths:
    - "ManagementCostCard에서 항목별 상세 내역(공용관리비/인건비/청소비 등)이 제거된다 (D-08)"
    - "하절기(6~9월) 월평균 + 동절기(10~3월) 월평균이 나란히 표시된다 (D-08)"
    - "각 계절 평균 옆에 세대당 평균이 표시된다 (D-08)"
    - "summerCount 또는 winterCount가 0일 때 해당 계절은 '데이터 부족'으로 표시된다 (D-08, Pitfall 4)"
    - "summerCount + winterCount < 4 (D-08 '4개월 이상')일 때 계절 비교 대신 fallback (월별 합계 추이만) 표시"
    - "월별 합계 추이 (raw 합계만) 섹션은 유지 (참고용)"
  artifacts:
    - path: "src/components/complex/ManagementCostCard.tsx"
      provides: "계절별 표시로 재작성된 카드 — getSeasonalAverages 사용"
      contains: "getSeasonalAverages"
      min_lines: 100
  key_links:
    - from: "ManagementCostCard rows prop"
      to: "getSeasonalAverages 호출"
      via: "import + 호출"
      pattern: "getSeasonalAverages"
    - from: "summerCount/winterCount"
      to: "조건부 표시 분기"
      via: "if (summerCount + winterCount < 4) fallback"
      pattern: "summerCount\\s*\\+\\s*winterCount"
---

<objective>
관리비 카드 리디자인 — D-08 계절별 표시 + 항목별 상세 내역 제거.

Purpose: UX-04 실수요자 관점 — 13개 세부 항목(공용관리비/인건비/청소비/경비비 등)은 정보 과부하. 계절성(여름/겨울) 차이가 실제 결정 요소.
Output: ManagementCostCard.tsx 단일 파일 재작성. 데이터 레이어(getSeasonalAverages)는 09-01에서 제공받음.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-complex-detail-ux/09-CONTEXT.md
@.planning/phases/09-complex-detail-ux/09-RESEARCH.md
@.planning/phases/09-complex-detail-ux/09-01-SUMMARY.md
@src/components/complex/ManagementCostCard.tsx
@src/lib/data/management-cost.ts

<interfaces>
<!-- 09-01 출력 — 본 Plan이 사용 -->

From src/lib/data/management-cost.ts (09-01 Task 3 추가):
```typescript
export interface ManagementCostRow {
  year_month: string  // "YYYY-MM-01"
  common_cost_total: number | null
  individual_cost_total: number | null
  long_term_repair_monthly: number | null
  // ... 기타 13개 항목 (UX-04에서 표시 안 함)
}

export interface SeasonalAverages {
  summerAvg:     number | null  // 6~9월 단지 합계 월평균
  winterAvg:     number | null  // 10~3월 단지 합계 월평균
  summerPerUnit: number | null
  winterPerUnit: number | null
  summerCount:   number
  winterCount:   number
}

export function getSeasonalAverages(
  rows: ManagementCostRow[],
  householdCount: number | null,
): SeasonalAverages
```

From src/components/complex/ManagementCostCard.tsx (현재 — 변경 대상):
```tsx
interface Props { rows: ManagementCostRow[]; householdCount: number | null }
// 현재: 항목별 13개 상세 표시 + 월별 추이
// 변경 후: 계절별 평균 (또는 데이터 부족 시 추이만) + 항목 내역 제거
```

CLAUDE.md AI 슬롭 금지: backdrop-blur, gradient-text, glow, 보라/인디고, gradient orb 금지.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ManagementCostCard.tsx 전체 재작성 — 계절별 표시 + 상세 항목 제거</name>
  <read_first>
    src/components/complex/ManagementCostCard.tsx
    src/lib/data/management-cost.ts
    .planning/phases/09-complex-detail-ux/09-CONTEXT.md
    .planning/phases/09-complex-detail-ux/09-RESEARCH.md
  </read_first>
  <action>
    수정 대상 (전체 재작성): `src/components/complex/ManagementCostCard.tsx`

    ```typescript
    import type { ManagementCostRow } from '@/lib/data/management-cost'
    import { getSeasonalAverages } from '@/lib/data/management-cost'

    interface Props {
      rows: ManagementCostRow[]
      householdCount: number | null
    }

    function fmtWon(val: number | null): string {
      if (val === null || val === 0) return '—'
      if (val >= 100_000_000) {
        const uk = Math.floor(val / 100_000_000)
        const rem = Math.floor((val % 100_000_000) / 10_000)
        return rem > 0 ? `${uk}억 ${rem.toLocaleString('ko-KR')}만원` : `${uk}억원`
      }
      if (val >= 10_000) return `${Math.floor(val / 10_000).toLocaleString('ko-KR')}만원`
      return `${val.toLocaleString('ko-KR')}원`
    }

    function fmtMonth(dateStr: string): string {
      // "2026-01-01" → "2026.01"
      return dateStr.slice(0, 7).replace('-', '.')
    }

    function rowTotal(row: ManagementCostRow): number {
      return (row.common_cost_total ?? 0)
        + (row.individual_cost_total ?? 0)
        + (row.long_term_repair_monthly ?? 0)
    }

    /**
     * UX-04 (D-08): 계절별 표시 + 항목별 상세 내역 제거
     * - 4개월 이상 데이터 있을 때 계절 비교 표시
     * - summerCount/winterCount 중 하나가 0이면 해당 계절은 "데이터 부족" 표시
     * - 전체 데이터 부족 (총합 < 4개월) 시 월별 추이 fallback
     */
    export function ManagementCostCard({ rows, householdCount }: Props) {
      if (rows.length === 0) return null

      const latest = rows[0]!
      const seasonal = getSeasonalAverages(rows, householdCount)
      const totalSeasonRows = seasonal.summerCount + seasonal.winterCount
      const hasSeasonalData = totalSeasonRows >= 4  // D-08

      return (
        <div className="card" style={{ padding: 20 }}>
          {/* 헤더 */}
          <div
            style={{
              display:        'flex',
              justifyContent: 'space-between',
              alignItems:     'baseline',
              marginBottom:   12,
            }}
          >
            <h3 style={{ font: '700 15px/1.4 var(--font-sans)', margin: 0 }}>
              관리비
            </h3>
            <span style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
              K-apt 기준 · 최근 {fmtMonth(latest.year_month)}
            </span>
          </div>

          {hasSeasonalData ? (
            <SeasonalView seasonal={seasonal} />
          ) : (
            <FallbackTotalsView rows={rows} householdCount={householdCount} />
          )}

          {/* 월별 합계 추이 (참고용 — 항상 표시) */}
          {rows.length > 1 && (
            <div
              style={{
                marginTop:    16,
                paddingTop:   12,
                borderTop:    '1px solid var(--line-subtle)',
              }}
            >
              <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 8 }}>
                월별 합계 추이
              </div>
              <div style={{ display: 'flex', gap: 8 }}>
                {[...rows].reverse().map(row => (
                  <div key={row.year_month} style={{ flex: 1, textAlign: 'center' }}>
                    <div style={{ font: '500 10px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 4 }}>
                      {fmtMonth(row.year_month)}
                    </div>
                    <div className="tnum" style={{ font: '600 12px/1 var(--font-sans)' }}>
                      {fmtWon(rowTotal(row))}
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}
        </div>
      )
    }

    interface SeasonalAveragesType {
      summerAvg:     number | null
      winterAvg:     number | null
      summerPerUnit: number | null
      winterPerUnit: number | null
      summerCount:   number
      winterCount:   number
    }

    function SeasonalView({ seasonal }: { seasonal: SeasonalAveragesType }) {
      return (
        <div
          style={{
            display:             'grid',
            gridTemplateColumns: '1fr 1fr',
            gap:                 12,
            padding:             '4px 0 12px',
            borderBottom:        '1px solid var(--line-subtle)',
          }}
        >
          <SeasonBlock
            label="하절기 (6~9월)"
            avg={seasonal.summerAvg}
            perUnit={seasonal.summerPerUnit}
            count={seasonal.summerCount}
          />
          <SeasonBlock
            label="동절기 (10~3월)"
            avg={seasonal.winterAvg}
            perUnit={seasonal.winterPerUnit}
            count={seasonal.winterCount}
          />
        </div>
      )
    }

    interface SeasonBlockProps {
      label:   string
      avg:     number | null
      perUnit: number | null
      count:   number
    }

    function SeasonBlock({ label, avg, perUnit, count }: SeasonBlockProps) {
      return (
        <div>
          <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 6 }}>
            {label}
          </div>
          {avg !== null ? (
            <>
              <div className="tnum" style={{ font: '700 20px/1 var(--font-sans)', color: 'var(--dj-orange)' }}>
                {fmtWon(avg)}
              </div>
              {perUnit !== null && (
                <div style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-sec)', marginTop: 4 }}>
                  세대당 약 {fmtWon(perUnit)}
                </div>
              )}
              <div style={{ font: '500 10px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginTop: 4 }}>
                {count}개월 평균
              </div>
            </>
          ) : (
            <div style={{ font: '500 13px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
              데이터 부족
            </div>
          )}
        </div>
      )
    }

    function FallbackTotalsView({ rows, householdCount }: { rows: ManagementCostRow[]; householdCount: number | null }) {
      const latest = rows[0]!
      const total = rowTotal(latest)
      const perUnit =
        householdCount && householdCount > 0 && total > 0
          ? Math.round(total / householdCount)
          : null
      return (
        <div
          style={{
            display:      'flex',
            gap:          16,
            marginBottom: 12,
            padding:      '8px 0 12px',
            borderBottom: '1px solid var(--line-subtle)',
          }}
        >
          <div>
            <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 4 }}>
              최근 단지 합계
            </div>
            <div className="tnum" style={{ font: '700 20px/1 var(--font-sans)' }}>
              {fmtWon(total)}
            </div>
          </div>
          {perUnit !== null && (
            <div>
              <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 4 }}>
                세대당 (평균)
              </div>
              <div className="tnum" style={{ font: '700 20px/1 var(--font-sans)', color: 'var(--dj-orange)' }}>
                {fmtWon(perUnit)}
              </div>
            </div>
          )}
          <div style={{ marginLeft: 'auto', alignSelf: 'flex-end', font: '500 11px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', textAlign: 'right' }}>
            계절 비교는 4개월 이상<br />데이터가 쌓이면 표시됩니다.
          </div>
        </div>
      )
    }
    ```

    주요 결정:
    - D-08: 항목별 13개 상세 (`COST_LABELS`) 완전 제거. 합계 + 계절별 평균 + 월별 추이만 표시.
    - "4개월 이상" 조건 (D-08): summerCount + winterCount >= 4 (Pitfall 4). 그 미만일 때 fallback (월별 합계 + 단일 단지 합계).
    - summerCount/winterCount 중 하나가 0 (예: 최근 6개월이 모두 동절기): hasSeasonalData=true (4개월 이상)일 때 SeasonalView 진입, 해당 SeasonBlock은 "데이터 부족"으로 표시 (양쪽 동시 표시 보장하지만 한쪽 없으면 명시).
    - "세대당 평균만 표시" (D-08): 평형별 분리는 K-apt 한계로 불가 — 단지 합계 / 세대수만.
    - 월별 합계 추이는 유지 (기존 UX 보존 + 신뢰성 보강).
    - CLAUDE.md AI 슬롭 금지: 보라/인디고 사용 안 함. var(--dj-orange) 토큰만 강조. backdrop-blur 없음.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/complex/ManagementCostCard.tsx','utf8');const u=c.replace(/\\/\\/.*$/gm,'').replace(/\\/\\*[\\s\\S]*?\\*\\//g,'');const must=['getSeasonalAverages','SeasonalView','FallbackTotalsView','summerCount','winterCount','하절기','동절기','데이터 부족'];for(const k of must){if(!u.includes(k)){console.error('MISS',k);process.exit(1)}}if(u.includes('COST_LABELS')||u.includes('인건비')||u.includes('청소비')){console.error('상세 항목 잔존');process.exit(2)}if(u.includes('backdrop-blur')||u.includes('gradient-text')||u.includes('보라')||u.includes('인디고')){console.error('AI slop pattern');process.exit(3)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getSeasonalAverages" src/components/complex/ManagementCostCard.tsx` >= 2 (import + 호출)
    - `grep -c "하절기" src/components/complex/ManagementCostCard.tsx` >= 1
    - `grep -c "동절기" src/components/complex/ManagementCostCard.tsx` >= 1
    - `grep -c "데이터 부족" src/components/complex/ManagementCostCard.tsx` >= 1 (Pitfall 4 메시지)
    - `grep -c "summerCount\\s*+\\s*winterCount" src/components/complex/ManagementCostCard.tsx` >= 1 (D-08 4개월 조건)
    - `grep -cE "COST_LABELS|인건비|청소비|경비비|승강기유지비|위탁관리수수료" src/components/complex/ManagementCostCard.tsx` == 0 (상세 항목 완전 제거)
    - `grep -E "backdrop-blur|gradient-text|보라|인디고" src/components/complex/ManagementCostCard.tsx` 매칭 없음 (AI 슬롭 금지)
    - `npm run lint && npm run build` 통과
  </acceptance_criteria>
  <done>ManagementCostCard가 계절별 + fallback + 월별 추이 구조로 재작성됨, 상세 항목 13개 제거</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ManagementCostCard rows prop | RSC에서 RLS public read 데이터 전달. 본 컴포넌트는 표시만 — 외부 IO 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-11 | Tampering | getSeasonalAverages year_month 파싱 | mitigate | 09-01 Task 3에서 처리 (parseInt + slice). 본 Plan은 결과만 사용 — 신뢰 경계 외부. |
| T-9-12 | Information Disclosure | 관리비 합계 노출 | accept | K-apt 공시 데이터 (이미 공개). |
</threat_model>

<verification>
- src/components/complex/ManagementCostCard.tsx 전체 재작성
- `npm run lint && npm run build` 통과
- 수동 검증 (Wave 2 종료 후 checkpoint): `npm run dev` → 관리비 데이터가 있는 단지 상세 → 카드에 "하절기 (6~9월) / 동절기 (10~3월)" 두 블록 표시. 데이터 부족 단지는 "최근 단지 합계" + 안내 메시지 표시.
</verification>

<success_criteria>
- 관리비 카드에 항목별 상세 내역(공용관리비, 인건비, 청소비 등) 완전히 사라짐
- 데이터 4개월 이상: 하절기/동절기 월평균 + 세대당 + 평균 산출 월 수 표시
- 데이터 4개월 미만: 최근 단지 합계 + "4개월 이상 데이터 쌓이면 계절 비교 표시" 안내
- summerCount/winterCount 중 하나가 0인 경우 해당 SeasonBlock에 "데이터 부족" 표시
</success_criteria>

<output>
완료 후 `.planning/phases/09-complex-detail-ux/09-04-SUMMARY.md` 작성. 변경 라인 수, 제거된 COST_LABELS 라인 인용 포함.
</output>

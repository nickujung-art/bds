---
phase: 05-data-expansion-ops
plan: "02"
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - src/lib/data/quadrant.ts
  - src/components/complex/ValueQuadrantChart.tsx
  - src/app/complexes/[id]/page.tsx
  - src/__tests__/quadrant.test.ts
autonomous: true
requirements:
  - DATA-04

must_haves:
  truths:
    - "단지 상세 페이지에 '단지 가성비 분석' 카드가 표시되고, 같은 시·구 단지들(회색)과 현재 단지(주황)가 ScatterChart로 시각화된다"
    - "X축 기준선과 Y축 기준선이 해당 시·구의 중앙값에 그려져 4분면을 구분한다"
    - "4분면 라벨(가성비/프리미엄/현실적/주의)이 절대 위치 div 오버레이로 표시된다"
    - "유효 데이터가 3개 미만이면 빈 상태 메시지가 표시된다"
    - "ISR 페이지(revalidate=86400)가 유지되고 ValueQuadrantChart는 'use client' 컴포넌트로 분리된다"
  artifacts:
    - path: "src/lib/data/quadrant.ts"
      provides: "getQuadrantData 서버 전용 함수"
      exports: ["getQuadrantData", "QuadrantData", "QuadrantPoint"]
    - path: "src/components/complex/ValueQuadrantChart.tsx"
      provides: "Recharts ScatterChart 4분면 'use client' 컴포넌트"
      contains: "'use client'"
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "단지 상세 페이지 — getQuadrantData 호출 + ValueQuadrantChart 렌더"
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/lib/data/quadrant.ts"
      via: "getQuadrantData 호출 (ISR RSC)"
      pattern: "getQuadrantData"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/components/complex/ValueQuadrantChart.tsx"
      via: "dynamic import (ssr: false)"
      pattern: "dynamic.*ValueQuadrantChart"
    - from: "src/lib/data/quadrant.ts"
      to: "transactions 테이블"
      via: ".is('cancel_date', null).is('superseded_by', null)"
      pattern: "cancel_date.*null.*superseded_by.*null"
---

<objective>
DATA-04: 가성비 분석 4분면 차트 (평당가 × 학군 점수)

같은 시·구 내 모든 단지의 평당가와 학군점수를 ScatterChart로 시각화한다. 현재 단지를 주황 점으로 강조하고 시·구 중앙값 기준으로 4분면을 나눠 가성비/프리미엄/현실적/주의 영역을 표시한다.

Purpose: 실수요자가 현재 단지가 같은 지역 내 어느 포지션에 있는지 즉각 파악하도록 돕는다.

Output:
- src/lib/data/quadrant.ts (getQuadrantData 서버 전용 함수)
- src/components/complex/ValueQuadrantChart.tsx ('use client' Recharts ScatterChart)
- src/app/complexes/[id]/page.tsx (getQuadrantData 추가, dynamic import ValueQuadrantChart)
- src/__tests__/quadrant.test.ts (TDD)
</objective>

<execution_context>
@/c/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-data-expansion-ops/05-CONTEXT.md
@.planning/phases/05-data-expansion-ops/05-PATTERNS.md
@.planning/phases/05-data-expansion-ops/05-UI-SPEC.md
@.planning/phases/05-data-expansion-ops/05-00-SUMMARY.md

<interfaces>
<!-- src/lib/data/rankings.ts — 서버 전용 함수 패턴 (정확히 이 import 블록 복사) -->
<!--
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'
-->

<!-- supabase/migrations/20260430000004_facility.sql — facility_school 스키마 -->
<!--
CREATE TABLE public.facility_school (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  complex_id    uuid REFERENCES public.complexes(id) ON DELETE CASCADE,
  school_name   text NOT NULL,
  school_type   text,           -- '초등학교', '중학교', '고등학교'
  is_assignment boolean DEFAULT false,
  distance_m    integer,        -- 거리(미터)
  ...
);
-- 학군점수 컬럼 없음 — 거리+배정 여부로 파생 계산 필요
-->

<!-- supabase/migrations/20260430000002_complexes.sql — complexes 스키마 -->
<!--
CREATE TABLE public.complexes (
  id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  canonical_name text NOT NULL,
  si             text,   -- 예: '창원시'
  gu             text,   -- 예: '의창구'
  lat            numeric,
  lng            numeric,
  status         text DEFAULT 'active',
  ...
);
-->

<!-- src/lib/data/rankings.ts — transactions 쿼리 패턴 (cancel_date + superseded_by 필터 필수) -->
<!--
const { data, error } = await supabase
  .from('transactions')
  .select('complex_id, price, area_m2')
  .is('cancel_date', null)          // 필수
  .is('superseded_by', null)        // 필수
  .eq('deal_type', 'sale')
  .gte('deal_date', thirtyDaysAgo)
  .limit(5000)

// 평당가 계산 패턴 (검증됨):
const pricePerPyeong = (r.price as number) / ((r.area_m2 as number) / 3.3058)
-->

<!-- src/components/complex/TransactionChart.tsx — 'use client' 차트 패턴 -->
<!--
'use client'
import { LineChart, ... ResponsiveContainer } from 'recharts'
export function TransactionChart({ data, dealType }: Props) {
  if (data.length === 0) {
    return (
      <div style={{ display: 'flex', height: 240, alignItems: 'center', justifyContent: 'center',
        font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
        거래 데이터가 없습니다
      </div>
    )
  }
  return (
    <ResponsiveContainer width="100%" height={240}>
-->

<!-- src/app/complexes/[id]/page.tsx — 기존 import + Promise.all 패턴 -->
<!--
import dynamic from 'next/dynamic'

// 기존:
const [complex, saleData, jeonseData, monthlyData, sidebarAds, reviews, reviewStats, facilityKaptResult] =
  await Promise.all([...])

// 추가할 내용:
const ValueQuadrantChart = dynamic(
  () => import('@/components/complex/ValueQuadrantChart').then(m => ({ default: m.ValueQuadrantChart })),
  { ssr: false }
)
// Promise.all에 getQuadrantData 추가
-->
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- Task 1: TDD RED — quadrant.test.ts 스캐폴드                 -->
<!-- ============================================================ -->
<task type="auto" tdd="true">
  <name>Task 1: TDD RED — quadrant.test.ts 스캐폴드 (failing)</name>
  <files>src/__tests__/quadrant.test.ts</files>

  <read_first>
    - src/__tests__/ 디렉토리 내 기존 테스트 파일 하나 (mock 패턴 참조)
    - vitest.config.ts
  </read_first>

  <behavior>
    getQuadrantData 테스트 케이스:
    - 같은 si/gu 단지 2개가 있을 때 points 배열에 2개 포인트 반환
    - targetComplexId에 해당하는 포인트의 isTarget === true
    - 나머지 포인트의 isTarget === false
    - transactions 쿼리에 cancel_date=null, superseded_by=null 필터가 적용됨
    - medianX, medianY가 올바르게 계산됨 (중앙값)
    - facility_school 데이터 없는 단지는 y=null → 포인트 제외됨
  </behavior>

  <action>
`src/__tests__/quadrant.test.ts` 파일을 작성한다. 구현 파일이 없으므로 RED 상태:

```typescript
import { describe, it, expect, vi } from 'vitest'

// getQuadrantData가 아직 없으므로 RED
describe('getQuadrantData', () => {
  it('returns points with correct isTarget flag', async () => {
    const { getQuadrantData } = await import('@/lib/data/quadrant')
    expect(typeof getQuadrantData).toBe('function')
  })

  it('filters out points with null school score', async () => {
    const { getQuadrantData } = await import('@/lib/data/quadrant')
    expect(typeof getQuadrantData).toBe('function')
  })

  it('calculates median correctly', () => {
    // median 함수는 내부 함수이므로 getQuadrantData 통해 간접 검증
    const values = [10, 20, 30, 40, 50]
    const sorted = [...values].sort((a, b) => a - b)
    const mid = Math.floor(sorted.length / 2)
    const expected = sorted[mid]  // 30
    expect(expected).toBe(30)
  })

  it('returns empty data when no complexes in region', async () => {
    const { getQuadrantData } = await import('@/lib/data/quadrant')
    expect(typeof getQuadrantData).toBe('function')
  })
})
```
  </action>

  <acceptance_criteria>
    - `ls src/__tests__/quadrant.test.ts` → 파일 존재
    - `npm run test -- quadrant 2>&1 | grep -i "failed\|error\|cannot find module"` → RED 상태 확인
  </acceptance_criteria>

  <verify>
    <automated>npm run test -- quadrant 2>&1 | tail -10</automated>
  </verify>

  <done>quadrant.test.ts가 존재하고 구현 파일 없음으로 RED 상태이다.</done>
</task>

<!-- ============================================================ -->
<!-- Task 2: getQuadrantData 구현 (GREEN)                         -->
<!-- ============================================================ -->
<task type="auto" tdd="true">
  <name>Task 2: src/lib/data/quadrant.ts 구현 (GREEN)</name>
  <files>src/lib/data/quadrant.ts</files>

  <read_first>
    - src/lib/data/rankings.ts (import 블록, 함수 시그니처, transactions 쿼리 패턴 전체)
    - supabase/migrations/20260430000004_facility.sql (facility_school 스키마 확인)
    - supabase/migrations/20260430000002_complexes.sql (complexes.si, complexes.gu 필드 확인)
    - src/__tests__/quadrant.test.ts (방금 작성한 테스트)
    - .planning/phases/05-data-expansion-ops/05-PATTERNS.md §quadrant.ts 섹션
  </read_first>

  <behavior>
    getQuadrantData(targetComplexId, si, gu, supabase):
    - complexes 테이블에서 같은 si/gu + status='active' 단지 목록 조회 (limit 200)
    - transactions에서 각 단지의 최근 12개월 sale 거래 평균 평당가 계산
      (cancel_date IS NULL AND superseded_by IS NULL 필수)
    - facility_school에서 각 단지의 is_assignment=true, school_type='초등학교' 거리 조회
      → 학군점수 = max(0, min(100, 100 - (distance_m / 10)))
      → 배정 초등학교 없으면 null → 해당 단지 포인트 제외
    - medianX = 평당가 배열의 중앙값
    - medianY = 학군점수 배열의 중앙값
    - isTarget: complexId === targetComplexId
  </behavior>

  <action>
`src/lib/data/quadrant.ts`를 구현한다. rankings.ts 패턴을 정확히 따른다.

```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export interface QuadrantPoint {
  complexId: string
  complexName: string
  x: number       // 평당가 (만원/평) — price / (area_m2 / 3.3058) / 10000
  y: number       // 학군점수 (0-100)
  isTarget: boolean
}

export interface QuadrantData {
  points: QuadrantPoint[]
  medianX: number
  medianY: number
  regionLabel: string   // 예: "창원시 의창구"
  totalCount: number
}

function median(values: number[]): number {
  if (values.length === 0) return 0
  const sorted = [...values].sort((a, b) => a - b)
  const mid = Math.floor(sorted.length / 2)
  return sorted.length % 2 !== 0
    ? sorted[mid]!
    : ((sorted[mid - 1]! + sorted[mid]!) / 2)
}

export async function getQuadrantData(
  targetComplexId: string,
  si: string,
  gu: string,
  supabase: SupabaseClient<Database>,
): Promise<QuadrantData> {
  const regionLabel = `${si} ${gu}`

  // 1. 같은 시·구 active 단지 목록
  const { data: complexList, error: complexErr } = await supabase
    .from('complexes')
    .select('id, canonical_name, si, gu')
    .eq('si', si)
    .eq('gu', gu)
    .eq('status', 'active')
    .limit(200)

  if (complexErr) throw new Error(`getQuadrantData complexes failed: ${complexErr.message}`)
  if (!complexList || complexList.length === 0) {
    return { points: [], medianX: 0, medianY: 0, regionLabel, totalCount: 0 }
  }

  const complexIds = complexList.map(c => c.id)

  // 2. 최근 12개월 sale 거래에서 단지별 평당가 집계
  const twelveMonthsAgo = new Date()
  twelveMonthsAgo.setMonth(twelveMonthsAgo.getMonth() - 12)
  const fromDate = twelveMonthsAgo.toISOString().slice(0, 10)  // YYYY-MM-DD

  const { data: txData, error: txErr } = await supabase
    .from('transactions')
    .select('complex_id, price, area_m2')
    .in('complex_id', complexIds)
    .is('cancel_date', null)          // 필수 — CLAUDE.md 대원칙
    .is('superseded_by', null)        // 필수 — CLAUDE.md 대원칙
    .eq('deal_type', 'sale')
    .gte('deal_date', fromDate)
    .gt('area_m2', 0)
    .limit(10000)

  if (txErr) throw new Error(`getQuadrantData transactions failed: ${txErr.message}`)

  // 단지별 평균 평당가 계산 (만원/평)
  const priceMap = new Map<string, number[]>()
  for (const tx of txData ?? []) {
    if (!tx.complex_id || !tx.price || !tx.area_m2) continue
    const ppp = (tx.price as number) / ((tx.area_m2 as number) / 3.3058) / 10000
    if (!priceMap.has(tx.complex_id)) priceMap.set(tx.complex_id, [])
    priceMap.get(tx.complex_id)!.push(ppp)
  }

  const avgPriceMap = new Map<string, number>()
  for (const [cid, prices] of priceMap.entries()) {
    avgPriceMap.set(cid, prices.reduce((a, b) => a + b, 0) / prices.length)
  }

  // 3. 배정 초등학교 거리로 학군점수 계산
  const { data: schoolData, error: schoolErr } = await supabase
    .from('facility_school')
    .select('complex_id, distance_m')
    .in('complex_id', complexIds)
    .eq('is_assignment', true)
    .eq('school_type', '초등학교')

  if (schoolErr) throw new Error(`getQuadrantData school failed: ${schoolErr.message}`)

  // 단지별 가장 가까운 배정 초등학교 거리로 점수 계산
  const schoolScoreMap = new Map<string, number>()
  for (const s of schoolData ?? []) {
    if (!s.complex_id || s.distance_m == null) continue
    const score = Math.max(0, Math.min(100, 100 - (s.distance_m / 10)))
    // 가장 가까운 학교 우선
    const existing = schoolScoreMap.get(s.complex_id)
    if (existing === undefined || score > existing) {
      schoolScoreMap.set(s.complex_id, score)
    }
  }

  // 4. 포인트 조합 (학군점수 null인 단지 제외)
  const points: QuadrantPoint[] = []
  for (const complex of complexList) {
    const x = avgPriceMap.get(complex.id)
    const y = schoolScoreMap.get(complex.id)
    if (x === undefined || y === undefined) continue  // 데이터 없으면 제외

    points.push({
      complexId: complex.id,
      complexName: complex.canonical_name,
      x: Math.round(x * 100) / 100,
      y: Math.round(y * 10) / 10,
      isTarget: complex.id === targetComplexId,
    })
  }

  // 5. 중앙값 계산
  const xValues = points.map(p => p.x)
  const yValues = points.map(p => p.y)
  const medianX = median(xValues)
  const medianY = median(yValues)

  return { points, medianX, medianY, regionLabel, totalCount: points.length }
}
```

구현 완료 후 `npm run test -- quadrant`로 GREEN 확인.
  </action>

  <acceptance_criteria>
    - `grep "import 'server-only'" src/lib/data/quadrant.ts` → 존재
    - `grep "cancel_date.*null" src/lib/data/quadrant.ts` → .is('cancel_date', null) 존재
    - `grep "superseded_by.*null" src/lib/data/quadrant.ts` → .is('superseded_by', null) 존재
    - `grep "3.3058" src/lib/data/quadrant.ts` → 평당가 계산 공식 존재
    - `grep "school_type.*초등학교" src/lib/data/quadrant.ts` → 학교 필터 존재
    - `grep "is_assignment.*true" src/lib/data/quadrant.ts` → 배정 필터 존재
    - `grep "export.*getQuadrantData" src/lib/data/quadrant.ts` → export 존재
    - `grep "export interface QuadrantData" src/lib/data/quadrant.ts` → 타입 export 존재
    - `npm run test -- quadrant` → PASS
  </acceptance_criteria>

  <verify>
    <automated>npm run test -- quadrant</automated>
  </verify>

  <done>getQuadrantData가 구현되고 테스트가 GREEN 상태이다.</done>
</task>

<!-- ============================================================ -->
<!-- Task 3: ValueQuadrantChart 컴포넌트 + 단지 상세 연결        -->
<!-- ============================================================ -->
<task type="auto">
  <name>Task 3: ValueQuadrantChart 컴포넌트 + 단지 상세 페이지 연결</name>
  <files>
    src/components/complex/ValueQuadrantChart.tsx
    src/app/complexes/[id]/page.tsx
  </files>

  <read_first>
    - src/components/complex/TransactionChart.tsx (전체 파일 — 'use client', import, 빈 상태 가드, ResponsiveContainer 패턴)
    - src/app/complexes/[id]/page.tsx (전체 파일 — Promise.all 패턴, import 블록, ISR revalidate)
    - .planning/phases/05-data-expansion-ops/05-UI-SPEC.md §3-2 (ValueQuadrantChart 전체 스펙)
    - .planning/phases/05-data-expansion-ops/05-PATTERNS.md §ValueQuadrantChart 섹션
  </read_first>

  <action>
**1. src/components/complex/ValueQuadrantChart.tsx — 'use client' 필수:**

UI-SPEC §3-2를 정확히 구현한다.

```typescript
'use client'

import {
  ScatterChart,
  Scatter,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  ReferenceLine,
} from 'recharts'

export interface QuadrantPoint {
  complexId: string
  complexName: string
  x: number
  y: number
  isTarget: boolean
}

interface ValueQuadrantChartProps {
  data: QuadrantPoint[]
  medianX: number
  medianY: number
  regionLabel: string
  totalCount: number
}

export function ValueQuadrantChart({
  data,
  medianX,
  medianY,
  regionLabel,
  totalCount,
}: ValueQuadrantChartProps) {
  const validPoints = data.filter(p => p.x > 0 && p.y >= 0)

  if (validPoints.length < 3) {
    return (
      <div
        style={{
          display: 'flex',
          height: 280,
          alignItems: 'center',
          justifyContent: 'center',
          font: '500 13px/1.6 var(--font-sans)',
          color: 'var(--fg-tertiary)',
        }}
      >
        이 지역 단지 데이터가 부족하여 차트를 표시할 수 없습니다.
      </div>
    )
  }

  const backgroundPoints = validPoints.filter(p => !p.isTarget)
  const targetPoints = validPoints.filter(p => p.isTarget)

  return (
    <div className="card" style={{ padding: 24 }}>
      <div style={{ marginBottom: 16 }}>
        <h3 style={{ font: '700 16px/1.4 var(--font-sans)', margin: '0 0 4px', color: 'var(--fg-pri)' }}>
          단지 가성비 분석
        </h3>
        <p style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
          {regionLabel} 내 단지 비교 ({totalCount}개 단지)
        </p>
      </div>

      {/* 차트 + 4분면 라벨 오버레이 */}
      <div
        style={{ position: 'relative' }}
        role="img"
        aria-label={`단지 가성비 분석 산점도 — ${regionLabel} 내 단지 평당가와 학군점수 비교`}
      >
        {/* 스크린 리더 전용 요약 */}
        <p className="sr-only">
          현재 단지는 {regionLabel} 내 {totalCount}개 단지와 비교한 평당가×학군점수 분포입니다.
        </p>

        <ResponsiveContainer width="100%" height={280}>
          <ScatterChart margin={{ top: 24, right: 24, bottom: 24, left: 8 }}>
            <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
            <XAxis
              dataKey="x"
              name="평당가"
              type="number"
              tickFormatter={(v: number) => `${Math.round(v)}만`}
              tick={{ fontSize: 11, fill: '#9ca3af' }}
            />
            <YAxis
              dataKey="y"
              name="학군점수"
              type="number"
              tick={{ fontSize: 11, fill: '#9ca3af' }}
              width={32}
            />
            <Tooltip
              contentStyle={{
                fontSize: 12,
                fontFamily: 'var(--font-sans)',
                border: '1px solid rgba(112,115,124,.22)',
                borderRadius: 8,
              }}
              formatter={(value: number, name: string) =>
                name === '평당가'
                  ? [`${Math.round(value)}만원/평`, '평당가']
                  : [`${value.toFixed(1)}점`, '학군점수']
              }
              labelFormatter={(label: string) => label}
            />
            {/* 4분면 구분선 */}
            <ReferenceLine
              x={medianX}
              stroke="#d1d5db"
              strokeDasharray="4 2"
              strokeWidth={1.5}
            />
            <ReferenceLine
              y={medianY}
              stroke="#d1d5db"
              strokeDasharray="4 2"
              strokeWidth={1.5}
            />
            {/* 배경 단지: 회색 */}
            <Scatter
              name="배경단지"
              data={backgroundPoints}
              fill="#d1d5db"
              opacity={0.6}
              isAnimationActive={false}
            />
            {/* 현재 단지: 주황 강조 */}
            <Scatter
              name="현재단지"
              data={targetPoints}
              fill="#ea580c"
              r={6}
              isAnimationActive={false}
            />
          </ScatterChart>
        </ResponsiveContainer>

        {/* 4분면 라벨 — 절대 위치 div 오버레이 (Recharts SVG 외부) */}
        <div style={{ position: 'absolute', top: 28, left: 12,
          font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', pointerEvents: 'none' }}>
          가성비
        </div>
        <div style={{ position: 'absolute', top: 28, right: 12,
          font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', pointerEvents: 'none' }}>
          프리미엄
        </div>
        <div style={{ position: 'absolute', bottom: 28, left: 12,
          font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', pointerEvents: 'none' }}>
          현실적
        </div>
        <div style={{ position: 'absolute', bottom: 28, right: 12,
          font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', pointerEvents: 'none' }}>
          주의
        </div>
      </div>
    </div>
  )
}
```

**2. src/app/complexes/[id]/page.tsx 수정:**

기존 파일을 읽고 다음 사항을 추가한다 (ISR revalidate = 86400 유지):

import 블록 상단에 추가:
```typescript
import dynamic from 'next/dynamic'
import { getQuadrantData } from '@/lib/data/quadrant'

const ValueQuadrantChart = dynamic(
  () => import('@/components/complex/ValueQuadrantChart').then(m => ({ default: m.ValueQuadrantChart })),
  { ssr: false }
)
```

Promise.all 배열에 추가 (기존 항목 뒤에):
```typescript
const [...기존..., quadrantData] = await Promise.all([
  ...기존...,
  complex?.si && complex?.gu
    ? getQuadrantData(id, complex.si, complex.gu, supabase).catch(() => null)
    : Promise.resolve(null),
])
```

주의: `complex`를 먼저 resolve한 후 si/gu로 getQuadrantData를 호출해야 하므로, complex를 먼저 단독 await한 후 나머지를 Promise.all로 처리하는 순서로 조정한다.

실제 구현:
```typescript
const complex = await getComplexById(id, supabase)
if (!complex) notFound()

const [saleData, jeonseData, ..., quadrantData] = await Promise.all([
  getComplexTransactionSummary(id, 'sale', supabase),
  ...기존...,
  getQuadrantData(id, complex.si ?? '', complex.gu ?? '', supabase).catch(() => null),
])
```

JSX 삽입 ("실거래가 추이" 차트 카드 바로 아래, 새 카드로 추가):
```tsx
{quadrantData && (
  <ValueQuadrantChart
    data={quadrantData.points}
    medianX={quadrantData.medianX}
    medianY={quadrantData.medianY}
    regionLabel={quadrantData.regionLabel}
    totalCount={quadrantData.totalCount}
  />
)}
```
  </action>

  <acceptance_criteria>
    - `grep "'use client'" src/components/complex/ValueQuadrantChart.tsx` → 파일 첫 줄
    - `grep "ScatterChart" src/components/complex/ValueQuadrantChart.tsx` → import + 사용 존재
    - `grep "ReferenceLine" src/components/complex/ValueQuadrantChart.tsx` → 2개 이상 (x, y 기준선)
    - `grep "isAnimationActive={false}" src/components/complex/ValueQuadrantChart.tsx` → 존재
    - `grep "가성비" src/components/complex/ValueQuadrantChart.tsx` → 4분면 라벨 존재
    - `grep "프리미엄" src/components/complex/ValueQuadrantChart.tsx` → 존재
    - `grep "현실적" src/components/complex/ValueQuadrantChart.tsx` → 존재
    - `grep "주의" src/components/complex/ValueQuadrantChart.tsx` → 존재
    - `grep "position: 'absolute'" src/components/complex/ValueQuadrantChart.tsx` → 절대 위치 오버레이 존재
    - `grep "height: 280" src/components/complex/ValueQuadrantChart.tsx` → 고정 height 존재 (height=0 pitfall 방지)
    - `grep "backdrop-blur\|gradient-text\|glow\|indigo\|purple" src/components/complex/ValueQuadrantChart.tsx` → 빈 출력 (AI 슬롭 없음)
    - `grep "getQuadrantData" src/app/complexes/[id]/page.tsx` → import + 호출 존재
    - `grep "dynamic.*ValueQuadrantChart\|ValueQuadrantChart.*dynamic" src/app/complexes/[id]/page.tsx` → dynamic import 존재
    - `grep "ssr: false" src/app/complexes/[id]/page.tsx` → 존재
    - `npm run build 2>&1 | grep -E "^Error|error TS" | grep -v "^$"` → 빈 출력 (빌드 성공)
  </acceptance_criteria>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
    ValueQuadrantChart 'use client' 컴포넌트가 구현되고, 단지 상세 페이지에 dynamic import(ssr:false)로 연결된다.
    4분면 라벨이 절대 위치 div로 표시되고, 빌드 성공.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ValueQuadrantChart ('use client') | 브라우저에서 실행. DB 쿼리 없음 — props로만 데이터 수신 |
| getQuadrantData → Supabase | createReadonlyClient() 사용 (ISR RSC). RLS로 public read 허용 데이터만 조회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | transactions 쿼리에 cancel_date/superseded_by 필터 누락 | mitigate | .is('cancel_date', null).is('superseded_by', null) 필수. 코드 리뷰 수락 기준 |
| T-05-02-02 | Tampering | ValueQuadrantChart에서 직접 Supabase 쿼리 | mitigate | 'use client' 컴포넌트에서 Supabase 쿼리 금지 (CLAUDE.md). 데이터는 RSC에서만 fetch |
| T-05-02-03 | Denial of Service | getQuadrantData 쿼리 limit 없을 때 | mitigate | complexes .limit(200), transactions .limit(10000) 명시. 무제한 쿼리 금지 |
| T-05-02-04 | Information Disclosure | 복수 단지 데이터 노출 (같은 si/gu) | accept | 복수 단지 평당가는 공개 정보 (실거래가 기반). 개인정보 없음 |
</threat_model>

<verification>
## DATA-04 완료 기준

```bash
# 1. 'use client' 확인
head -1 src/components/complex/ValueQuadrantChart.tsx
# → 'use client'

# 2. ISR 유지 확인
grep "revalidate" src/app/complexes/[id]/page.tsx
# → export const revalidate = 86400

# 3. transactions 쿼리 필터 확인
grep -A2 "cancel_date" src/lib/data/quadrant.ts
# → .is('cancel_date', null)

# 4. 테스트 + 빌드
npm run test -- quadrant && npm run build
```
</verification>

<success_criteria>
1. `npm run test -- quadrant` 통과 (GREEN)
2. `npm run build` 에러 없음
3. ValueQuadrantChart의 'use client' 첫 줄 확인
4. ISR page.tsx revalidate = 86400 유지 확인
5. 4분면 라벨 4개 (가성비/프리미엄/현실적/주의) 절대 위치 div로 구현
6. dynamic import ssr:false로 SSR window 에러 방지
</success_criteria>

<output>
완료 후 `.planning/phases/05-data-expansion-ops/05-02-SUMMARY.md` 생성.
포함 내용: 구현된 파일 목록, 테스트 결과, 주요 설계 결정(dynamic import 이유 등).
</output>

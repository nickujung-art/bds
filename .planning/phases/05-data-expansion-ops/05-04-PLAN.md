---
phase: 05-data-expansion-ops
plan: "04"
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - .github/workflows/db-backup.yml
autonomous: false
requirements:
  - OPS-01

must_haves:
  truths:
    - "매주 일요일 04:00 KST(UTC 토요일 19:00)에 pg_dump가 자동 실행된다"
    - "백업 파일이 backup-YYYY-MM-DD.sql.gz 형식으로 nickujung-art/danjiondo-backup 레포에 커밋된다"
    - "90일 이전 백업 파일이 자동으로 삭제된다"
    - "SUPABASE_DB_URL이 GitHub Secrets에만 저장되고 workflow log에 절대 출력되지 않는다"
    - "BACKUP_PAT(또는 deploy key)이 danjiondo-backup 레포 push 권한을 가진다"
    - "pg_dump 후 dump 파일 크기가 1000바이트 이상인지 검증하여 빈 파일 커밋을 방지한다"
  artifacts:
    - path: ".github/workflows/db-backup.yml"
      provides: "주간 pg_dump 백업 + 90일 cleanup GitHub Actions 워크플로우"
      contains: "db-backup"
  key_links:
    - from: ".github/workflows/db-backup.yml"
      to: "nickujung-art/danjiondo-backup GitHub repo"
      via: "git push with BACKUP_PAT"
      pattern: "danjiondo-backup"
---

<objective>
OPS-01: DB 백업 자동화 — pg_dump + GitHub private repo 주간 백업

매주 일요일 04:00 KST에 Supabase 프로덕션 DB를 pg_dump하고, nickujung-art/danjiondo-backup private repo에 .sql.gz 파일로 커밋한다. 90일 이전 백업은 자동 삭제한다.

Purpose: V1.5 완성 조건인 DB 백업 자동화. 운영 사고 시 빠른 복구를 위한 안전망.

Output:
- .github/workflows/db-backup.yml (주간 pg_dump 자동화)
- 복구 런북 (이 플랜 내 문서화 — 별도 파일 생성 없음)

사전 조건 (운영자가 먼저 완료해야 함):
1. nickujung-art/danjiondo-backup GitHub private repo 생성
2. SUPABASE_DB_URL GitHub Secrets 등록 (Supabase Dashboard > Project Settings > Database > URI, Direct connection IPv4 사용)
3. BACKUP_PAT GitHub Secrets 등록 (Fine-grained PAT 권장 — danjiondo-backup repo Contents write만)

Wave 1 병렬 실행 참고: 이 플랜(OPS-01)은 DATA-03/04/05와 독립적이다.
05-00(DB 초기화)만 완료되면 05-01/02/03과 동시에 실행할 수 있다.
</objective>

<execution_context>
@/c/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-data-expansion-ops/05-CONTEXT.md
@.planning/phases/05-data-expansion-ops/05-RESEARCH.md
@.planning/phases/05-data-expansion-ops/05-00-SUMMARY.md

<interfaces>
<!-- .github/workflows/rankings-cron.yml — GitHub Actions 구조 패턴 -->
<!--
name: Rankings Cron
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}
jobs:
  compute-rankings:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Call rankings cron endpoint
        run: |
          STATUS=$(curl ... )
          if [ "$STATUS" != "200" ]; then exit 1; fi
-->

<!-- D-09~D-12: OPS-01 결정 사항 -->
<!--
D-09: 백업 전용 별도 GitHub private repo = nickujung-art/danjiondo-backup
D-10: SUPABASE_DB_URL → GitHub Secrets (Dashboard > Project Settings > Database > URI)
D-11: 주간 workflow — 일요일 04:00 KST
      KST = UTC+9이므로: 일요일 04:00 KST = 토요일 19:00 UTC
      올바른 cron: '0 19 * * 6' (토요일=6, 19:00 UTC = 일요일 04:00 KST)
      주의: '0 19 * * 0'은 일요일 19:00 UTC = 월요일 04:00 KST (오류)
D-12: 파일명 backup-{YYYY-MM-DD}.sql.gz, 90일 보관 후 자동 삭제
-->

<!-- RESEARCH.md Pattern 4 — pg_dump → backup repo 워크플로우 전체 -->
<!--
- apt-get install postgresql-client (ubuntu-latest)
- pg_dump "$SUPABASE_DB_URL" | gzip > "/tmp/$FILENAME"
- git clone "https://x-access-token:${BACKUP_PAT}@github.com/nickujung-art/danjiondo-backup.git"
- git add + commit + push
- cleanup: 90일 이전 backup-*.sql.gz 파일 git rm + commit
- Pitfall: Transaction Pooler URL 사용 금지 — Direct connection (IPv4) 사용
- Pitfall: GITHUB_TOKEN은 타 repo push 불가 — BACKUP_PAT 필수
-->
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- Task 1: 사전 준비 체크포인트 (운영자 작업)                  -->
<!-- M-1: Fine-grained PAT 권장 (Classic PAT 대신)              -->
<!-- ============================================================ -->
<task type="checkpoint:human-action">
  <name>Task 1: [BLOCKING] 백업 repo 생성 + GitHub Secrets 등록</name>
  <files>없음 (GitHub UI 작업)</files>

  <read_first>
    - .planning/phases/05-data-expansion-ops/05-CONTEXT.md §D-09, D-10 결정 확인
  </read_first>

  <action>
아래 단계를 순서대로 완료한다.

**Step 1 — nickujung-art/danjiondo-backup private repo 생성:**
1. https://github.com/new 접속
2. Repository name: `danjiondo-backup`
3. Visibility: **Private**
4. Initialize: README.md 체크 (첫 커밋 필요)
5. Create repository 클릭

**Step 2 — SUPABASE_DB_URL Secrets 등록:**
1. Supabase Dashboard → https://supabase.com/dashboard/project/auoravdadyzvuoxunogh
2. Project Settings → Database → Connection string
3. **Direct connection** 선택 (Transaction Pooler 아님 — pg_dump 비호환)
4. URI 복사 (형식: `postgresql://postgres.[ref]:[password]@db.[ref].supabase.co:5432/postgres`)
5. GitHub → danjiondo repo Settings → Secrets and variables → Actions → New repository secret
   - Name: `SUPABASE_DB_URL`
   - Value: 복사한 URI 붙여넣기
6. Add secret 클릭

**Step 3 — BACKUP_PAT Secrets 등록:**

**Fine-grained PAT 권장 (Classic PAT 대신):**
GitHub → Settings → Developer settings → Personal access tokens → **Fine-grained tokens** → Generate new token.

설정:
- Token name: `danjiondo-backup-pat`
- Expiration: 1 year
- Resource owner: nickujung-art
- Repository access: **Only select repositories** → `danjiondo-backup` 선택
- Repository permissions:
  - **Contents** → **Read and write** (git push 필요)
  - 나머지 권한은 No access 유지

토큰 생성 후 복사 (한 번만 표시됨).

> **왜 Fine-grained PAT인가?**
> Classic PAT의 `repo` scope는 계정 내 모든 private repository에 접근 권한을 부여한다.
> 토큰이 유출될 경우 danjiondo-backup 외 다른 repo들도 위험에 노출된다.
> Fine-grained PAT은 danjiondo-backup repo의 Contents write 권한만 부여하므로 최소 권한 원칙을 충족한다.

> **Classic PAT 사용 시 (fallback):**
> GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
> Generate new token → Note: `danjiondo-backup-pat` → Scopes: `repo` 전체 체크
> (Classic PAT은 계정 내 모든 repo에 접근 가능하므로 Fine-grained PAT을 강력 권장)

GitHub → danjiondo repo Settings → Secrets and variables → Actions → New repository secret
   - Name: `BACKUP_PAT`
   - Value: 복사한 PAT
   - Add secret 클릭

**검증:**
- danjiondo repo Actions Secrets에 `SUPABASE_DB_URL`, `BACKUP_PAT` 두 항목 존재 확인
- danjiondo-backup repo가 private으로 존재하고 초기 커밋(README.md)이 있는지 확인
  </action>

  <acceptance_criteria>
    - https://github.com/nickujung-art/danjiondo-backup 접속 가능 (private이므로 본인만)
    - GitHub danjiondo repo → Settings → Secrets and variables → Actions에 SUPABASE_DB_URL 존재
    - GitHub danjiondo repo → Settings → Secrets and variables → Actions에 BACKUP_PAT 존재
    - SUPABASE_DB_URL 값이 `postgresql://` 형식 (Transaction Pooler URL이면 수정 필요)
  </acceptance_criteria>

  <verify>
    GitHub UI에서 직접 확인. Secrets 값은 볼 수 없으므로 이름만 확인.
  </verify>

  <done>
    nickujung-art/danjiondo-backup private repo 생성 완료.
    SUPABASE_DB_URL, BACKUP_PAT이 danjiondo repo GitHub Secrets에 등록되어 있다.
    BACKUP_PAT은 Fine-grained PAT(danjiondo-backup repo Contents write만)이 권장된다.
  </done>
</task>

<!-- ============================================================ -->
<!-- Task 2: db-backup.yml 워크플로우 작성                        -->
<!-- cron: '0 19 * * 6' = 토요일 19:00 UTC = 일요일 04:00 KST   -->
<!-- M-6: pg_dump 후 dump 파일 크기 검증 step 추가              -->
<!-- ============================================================ -->
<task type="auto">
  <name>Task 2: .github/workflows/db-backup.yml 생성</name>
  <files>.github/workflows/db-backup.yml</files>

  <read_first>
    - .github/workflows/rankings-cron.yml (GitHub Actions 구조 패턴)
    - .github/workflows/cafe-code-weekly.yml (schedule + workflow_dispatch 패턴)
    - .planning/phases/05-data-expansion-ops/05-RESEARCH.md §Pattern 4 (pg_dump 전체 워크플로우)
  </read_first>

  <action>
`.github/workflows/db-backup.yml`을 아래 내용으로 생성한다.

핵심 요구사항:
- cron: `'0 19 * * 6'` — 토요일 19:00 UTC = 일요일 04:00 KST (D-11 준수)
  - 주의: '0 19 * * 0'은 일요일 19:00 UTC = 월요일 04:00 KST (오류)
  - '0 19 * * 6'이 정확: 6=토요일, 19:00 UTC = +9시간 = 다음날(일요일) 04:00 KST
- `workflow_dispatch: {}` — 수동 테스트 가능
- `timeout-minutes: 15`
- `apt-get install postgresql-client` — pg_dump 실행 환경
- `SUPABASE_DB_URL` — secrets 참조. **절대 echo나 출력 금지**
- `BACKUP_PAT` — secrets 참조
- 파일명: `backup-$(date +%Y-%m-%d).sql.gz`
- backup repo: `nickujung-art/danjiondo-backup`
- 90일 cleanup: `date -d '90 days ago'`로 CUTOFF 계산 후 이전 파일 git rm
- dump 파일 크기 검증: pg_dump 후, git clone 전에 파일 크기 확인

```yaml
name: DB Backup

on:
  schedule:
    - cron: '0 19 * * 6'   # 토요일 19:00 UTC = 일요일 04:00 KST
  workflow_dispatch: {}

jobs:
  backup:
    name: pg_dump → danjiondo-backup repo
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Install postgresql-client
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y postgresql-client

      - name: Run pg_dump
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          set -e
          FILENAME="backup-$(date +%Y-%m-%d).sql.gz"
          pg_dump "$SUPABASE_DB_URL" | gzip > "/tmp/$FILENAME"
          echo "FILENAME=$FILENAME" >> "$GITHUB_ENV"
          echo "pg_dump complete: $FILENAME"

      - name: Verify dump file integrity
        run: |
          set -e
          DUMP_SIZE=$(wc -c < "/tmp/$FILENAME")
          echo "Dump file size: $DUMP_SIZE bytes"
          if [ "$DUMP_SIZE" -lt 1000 ]; then
            echo "ERROR: dump file suspiciously small ($DUMP_SIZE bytes) — pg_dump may have failed" >&2
            exit 1
          fi
          echo "Dump integrity OK: $DUMP_SIZE bytes"

      - name: Clone backup repo
        env:
          BACKUP_PAT: ${{ secrets.BACKUP_PAT }}
        run: |
          set -e
          git clone \
            "https://x-access-token:${BACKUP_PAT}@github.com/nickujung-art/danjiondo-backup.git" \
            /tmp/backup-repo

      - name: Commit backup file
        run: |
          set -e
          cp "/tmp/$FILENAME" /tmp/backup-repo/
          cd /tmp/backup-repo
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add "$FILENAME"
          git commit -m "backup: $FILENAME"
          git push

      - name: Cleanup backups older than 90 days
        run: |
          set -e
          cd /tmp/backup-repo
          CUTOFF=$(date -d '90 days ago' +%Y-%m-%d)
          REMOVED=0
          for f in backup-*.sql.gz; do
            [ -f "$f" ] || continue
            FILEDATE="${f#backup-}"
            FILEDATE="${FILEDATE%.sql.gz}"
            if [[ "$FILEDATE" < "$CUTOFF" ]]; then
              git rm "$f"
              REMOVED=$((REMOVED + 1))
            fi
          done
          if [ "$REMOVED" -gt 0 ]; then
            git commit -m "cleanup: remove $REMOVED backup(s) older than 90 days"
            git push
            echo "Removed $REMOVED old backup(s)"
          else
            echo "No old backups to remove"
          fi
```

**보안 주의사항 (코드에 포함 안 됨 — 운영자 공지용):**
- `SUPABASE_DB_URL`은 Connection string에 패스워드를 포함한다. `echo "$SUPABASE_DB_URL"` 절대 금지.
- `set -e`는 명령 실패 시 즉시 종료. URL 노출 없이 에러 핸들링 가능.
- Fine-grained PAT을 사용하면 danjiondo-backup repo Contents write 권한만 부여 — Classic PAT `repo` scope보다 안전.
  </action>

  <acceptance_criteria>
    - `ls .github/workflows/db-backup.yml` → 파일 존재
    - `grep "cron: '0 19 \* \* 6'" .github/workflows/db-backup.yml` → 토요일 19:00 UTC(=일요일 04:00 KST) 스케줄 존재
    - `grep "workflow_dispatch" .github/workflows/db-backup.yml` → 수동 트리거 존재
    - `grep "timeout-minutes: 15" .github/workflows/db-backup.yml` → 존재
    - `grep "postgresql-client" .github/workflows/db-backup.yml` → apt-get 설치 존재
    - `grep "SUPABASE_DB_URL: \${{ secrets.SUPABASE_DB_URL }}" .github/workflows/db-backup.yml` → secrets 참조 존재
    - `grep "BACKUP_PAT: \${{ secrets.BACKUP_PAT }}" .github/workflows/db-backup.yml` → secrets 참조 존재
    - `grep "echo.*SUPABASE_DB_URL\|cat.*SUPABASE_DB_URL\|print.*SUPABASE_DB_URL" .github/workflows/db-backup.yml` → 빈 출력 (URL 출력 없음)
    - `grep "danjiondo-backup" .github/workflows/db-backup.yml` → backup repo 참조 존재
    - `grep "backup-\$(date" .github/workflows/db-backup.yml` → 파일명 패턴 존재
    - `grep "90 days ago" .github/workflows/db-backup.yml` → cleanup 로직 존재
    - `grep "\.sql\.gz" .github/workflows/db-backup.yml` → gzip 확장자 존재
    - `grep "0 19 \* \* 0" .github/workflows/db-backup.yml` → 빈 출력 (잘못된 cron '0 19 * * 0' 없음)
    - `grep "Verify dump file integrity\|DUMP_SIZE\|wc -c" .github/workflows/db-backup.yml` → dump 크기 검증 step 존재 (M-6)
  </acceptance_criteria>

  <verify>
    ```bash
    cat .github/workflows/db-backup.yml
    # 내용 확인 — YAML 구조 유효 확인
    npm run lint
    ```
  </verify>

  <done>.github/workflows/db-backup.yml이 생성되고 주간 pg_dump(토요일 19:00 UTC = 일요일 04:00 KST) + dump 파일 크기 검증(1000바이트 미만 시 실패) + 90일 cleanup 로직이 올바르게 구성되어 있다.</done>
</task>

<!-- ============================================================ -->
<!-- Task 3: 커밋 + 첫 실행 검증 체크포인트                      -->
<!-- ============================================================ -->
<task type="checkpoint:human-verify">
  <name>Task 3: db-backup.yml 커밋 + workflow_dispatch 테스트 실행</name>
  <files>없음</files>

  <read_first>
    - .github/workflows/db-backup.yml (방금 생성한 파일)
  </read_first>

  <what-built>
    - .github/workflows/db-backup.yml (pg_dump + dump 크기 검증 + cleanup)
    - GitHub Secrets (SUPABASE_DB_URL, BACKUP_PAT) 등록 (Task 1)
  </what-built>

  <how-to-verify>
    **1. 커밋 + push:**
    ```bash
    git add .github/workflows/db-backup.yml
    git commit -m "feat(05): add weekly pg_dump backup GitHub Actions workflow (OPS-01)"
    git push
    ```

    **2. workflow_dispatch로 수동 테스트 실행:**
    1. GitHub → danjiondo repo → Actions → "DB Backup" 워크플로우 선택
    2. "Run workflow" 버튼 클릭
    3. 실행 완료 후 각 Step 확인:
       - "Install postgresql-client": Success
       - "Run pg_dump": Success (파일 생성 확인)
       - "Verify dump file integrity": Success (DUMP_SIZE > 1000 bytes 확인)
       - "Clone backup repo": Success
       - "Commit backup file": Success
       - "Cleanup backups older than 90 days": "No old backups to remove" (처음이므로)

    **3. 백업 파일 확인:**
    - https://github.com/nickujung-art/danjiondo-backup 접속
    - `backup-YYYY-MM-DD.sql.gz` 파일이 커밋되어 있는지 확인

    **복구 런북 (문서화 — 파일 미생성, 이 SUMMARY에 기록):**
    1. danjiondo-backup repo에서 가장 최근 `backup-YYYY-MM-DD.sql.gz` 파일 다운로드
    2. 압축 해제: `gunzip backup-YYYY-MM-DD.sql.gz`
    3. 신규 Supabase 프로젝트 생성 또는 기존 프로젝트의 Connection string 준비
    4. DB 복구: `psql "$TARGET_DB_URL" < backup-YYYY-MM-DD.sql`
    5. 마이그레이션 상태 확인: `npx supabase migration list --linked`
  </how-to-verify>

  <resume-signal>"approved" 또는 "백업 실행 확인됨"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Supabase DB | SUPABASE_DB_URL (패스워드 포함 Connection string). log 노출 시 DB 직접 접근 가능 |
| GitHub Actions → danjiondo-backup repo | BACKUP_PAT. Fine-grained PAT 사용 시 danjiondo-backup repo Contents write만. Classic PAT 사용 시 계정 내 모든 repo 접근 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | SUPABASE_DB_URL (Connection URL에 패스워드 포함) | mitigate | GitHub Secrets에만 저장. workflow 내 `echo`, `cat`, `print` 절대 금지. `set -e`로 에러 처리. GitHub Actions는 Secrets를 자동 마스킹 |
| T-05-04-02 | Information Disclosure | BACKUP_PAT scope 과잉 (Classic PAT repo 전체) | mitigate | Fine-grained PAT 권장 — Contents write를 danjiondo-backup repo로만 제한. Classic PAT 사용 시 토큰 유출 범위가 계정 전체. Task 1에서 Fine-grained PAT 생성 가이드 제공 |
| T-05-04-03 | Information Disclosure | backup .sql.gz 파일에 PII 포함 | accept | backup repo는 private. 접근 가능자: nickujung-art 계정 소유자만. 추가적으로 gpg 암호화 가능 (Phase 7 개선 사항) |
| T-05-04-04 | Tampering | pg_dump 실패 시 빈 파일 커밋 | mitigate | `set -e` — pg_dump 실패 시 즉시 종료. "Verify dump file integrity" step — 1000바이트 미만 파일은 에러로 처리하여 빈 파일 커밋 방지 |
| T-05-04-05 | Denial of Service | SUPABASE_DB_URL에 Transaction Pooler URL 사용 | mitigate | Direct connection (IPv4) URL 사용 명시 (Task 1 안내). Pooler URL은 pg_dump 비호환 |
| T-05-04-06 | Tampering | 90일 cleanup 중 의도치 않은 파일 삭제 | accept | `backup-*.sql.gz` 패턴으로 한정. 다른 파일 삭제 불가. git rm + commit으로 이력 보존 (git revert 가능) |
</threat_model>

<verification>
## OPS-01 완료 기준

```bash
# 1. 워크플로우 파일 존재
ls .github/workflows/db-backup.yml

# 2. 올바른 cron 확인 (토요일 19:00 UTC = 일요일 04:00 KST)
grep "cron:" .github/workflows/db-backup.yml
# → '0 19 * * 6'

# 3. 보안 검증 — URL 출력 없음
grep -v "^#" .github/workflows/db-backup.yml | grep -v "secrets.SUPABASE_DB_URL" | \
  grep "SUPABASE_DB_URL" | grep -v "^$"
# → 빈 출력 (secrets 참조 외에 URL 노출 없음)

# 4. dump 크기 검증 step 확인 (M-6)
grep "Verify dump file integrity\|DUMP_SIZE\|wc -c" .github/workflows/db-backup.yml

# 5. GitHub Actions에서 "DB Backup" 워크플로우 확인
# → https://github.com/{owner}/bds/actions/workflows/db-backup.yml

# 6. 빌드 + 린트
npm run lint && npm run build
```
</verification>

<success_criteria>
1. `.github/workflows/db-backup.yml` 커밋 완료
2. GitHub Actions에서 "DB Backup" 워크플로우가 표시됨
3. workflow_dispatch 수동 실행 성공 — nickujung-art/danjiondo-backup에 backup-*.sql.gz 파일 존재
4. SUPABASE_DB_URL, BACKUP_PAT이 GitHub Secrets에 등록됨
5. `npm run lint && npm run build` 성공
6. cron이 '0 19 * * 6' (토요일 19:00 UTC = 일요일 04:00 KST)으로 설정됨
7. "Verify dump file integrity" step이 존재하고 1000바이트 미만 파일은 exit 1 처리 (M-6)
8. BACKUP_PAT은 Fine-grained PAT(danjiondo-backup repo Contents write만) 사용 권장 (M-1)
</success_criteria>

<output>
완료 후 `.planning/phases/05-data-expansion-ops/05-04-SUMMARY.md` 생성.
포함 내용: 워크플로우 파일, 첫 실행 결과, 복구 런북 기록, Secrets 등록 완료 여부, PAT 타입(Fine-grained/Classic) 기록.
</output>

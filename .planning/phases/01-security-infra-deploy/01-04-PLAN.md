---
phase: 01-security-infra-deploy
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - playwright.config.ts
  - e2e/global-setup.ts
  - e2e/global-teardown.ts
  - e2e/landing.spec.ts
  - e2e/complex-detail.spec.ts
  - e2e/map.spec.ts
  - e2e/search.spec.ts
  - e2e/review.spec.ts
  - e2e/.auth/.gitkeep
  - .gitignore
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Playwright 골든패스 5종이 CI에서 실행되고 통과한다"
    - "후기 작성 테스트가 인증된 세션으로 실행된다"
    - "E2E 테스트 완료 후 테스트 유저가 프로덕션 DB에서 삭제된다"
    - "storageState.json이 git에 커밋되지 않는다"
    - "쿠키 이름이 실제 @supabase/ssr 버전과 일치하거나 UI 기반 로그인으로 전환된다"
  artifacts:
    - path: "playwright.config.ts"
      provides: "globalSetup/globalTeardown + chromium/chromium-auth projects 설정"
      contains: "globalSetup"
    - path: "e2e/global-setup.ts"
      provides: "테스트 유저 생성 + 세션 쿠키 저장"
      contains: "admin.createUser"
    - path: "e2e/global-teardown.ts"
      provides: "테스트 유저 삭제 cleanup"
      contains: "admin.deleteUser"
    - path: "e2e/landing.spec.ts"
      provides: "골든패스 1: 랜딩 페이지 로드"
    - path: "e2e/complex-detail.spec.ts"
      provides: "골든패스 2: 단지 상세 페이지 로드"
    - path: "e2e/map.spec.ts"
      provides: "골든패스 3: 지도 페이지 로드"
    - path: "e2e/search.spec.ts"
      provides: "골든패스 4: 검색 동작"
    - path: "e2e/review.spec.ts"
      provides: "골든패스 5: 후기 작성 (인증 필요)"
      contains: "storageState"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/global-setup.ts"
      via: "globalSetup 경로"
      pattern: "globalSetup.*global-setup"
    - from: "e2e/review.spec.ts"
      to: "e2e/.auth/user.json"
      via: "storageState"
      pattern: "storageState.*user\\.json"
    - from: "e2e/global-setup.ts"
      to: "Supabase Auth"
      via: "admin.createUser + signInWithPassword"
      pattern: "createUser"

user_setup:
  - service: supabase-e2e
    why: "E2E 테스트용 admin 접근 — 테스트 유저 생성/삭제에 service_role 키 필요"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role (이미 존재, GitHub Secret에 추가 필요)"
      - name: NEXT_PUBLIC_SITE_URL
        source: "Vercel 프로덕션 URL (배포 후) 또는 http://localhost:3000 (로컬)"
---

<objective>
Playwright E2E 골든패스 5종 구현 (INFRA-03): e2e/ 디렉토리 신규 생성 + global-setup/teardown + 5개 spec 파일 + playwright.config.ts 수정.

Purpose: 프로덕션 배포 전후 핵심 사용자 흐름(랜딩·단지 상세·지도·검색·후기 작성)이 정상 동작함을 자동으로 검증. CI에서 매 PR마다 실행.

Output:
- playwright.config.ts (수정) — globalSetup/globalTeardown + chromium-auth project
- e2e/global-setup.ts (신규) — 테스트 유저 생성 + storageState 저장 (쿠키 이름 확인 단계 포함)
- e2e/global-teardown.ts (신규) — 테스트 유저 cleanup
- e2e/landing.spec.ts (신규)
- e2e/complex-detail.spec.ts (신규)
- e2e/map.spec.ts (신규)
- e2e/search.spec.ts (신규)
- e2e/review.spec.ts (신규)
- .gitignore 수정 — e2e/.auth/user.json 제외
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-infra-deploy/01-CONTEXT.md
@.planning/phases/01-security-infra-deploy/01-RESEARCH.md
@.planning/phases/01-security-infra-deploy/01-PATTERNS.md
</context>

<interfaces>
<!-- playwright.config.ts 현재 상태 -->
playwright.config.ts (현재):
```typescript
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',    // ← 교체 필요
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', ... },            // ← 골든패스에서 chromium만 사용
    { name: 'webkit', ... },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
})
// globalSetup, globalTeardown 없음 → 추가 필요
```

기존 admin.createUser 패턴 (src/__tests__/helpers/db.ts):
```typescript
export const admin = createClient<Database>(URL_, SKEY, {
  auth: { autoRefreshToken: false, persistSession: false },
})
// admin.auth.admin.createUser({ email, password, email_confirm: true })
```

E2E Supabase 쿠키 이름 [실행 전 반드시 확인 필요]:
- @supabase/ssr 0.10.x: `sb-{supabaseRef}-auth-token`
- supabaseRef = NEXT_PUBLIC_SUPABASE_URL 호스트명의 첫 번째 서브도메인
- 주의: chunked 쿠키일 수 있음 (sb-...-auth-token.0, sb-...-auth-token.1)
- 확인 방법: 로컬에서 로그인 후 DevTools → Application → Cookies → localhost:3000
- 대안 (쿠키 이름 불일치 시): page.goto('/login') → form fill → submit으로 UI 기반 로그인
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: playwright.config.ts 수정 + global-setup/teardown 생성</name>
  <files>
    playwright.config.ts
    e2e/global-setup.ts
    e2e/global-teardown.ts
    e2e/.auth/.gitkeep
    .gitignore
  </files>
  <read_first>
    - playwright.config.ts (전체 현재 내용 확인)
    - .gitignore (현재 제외 규칙 확인 — e2e/.auth 추가 위치)
    - src/__tests__/helpers/db.ts (admin client 패턴 확인 — createUser 시그니처)
    - src/__tests__/notify-worker.test.ts (lines 60-90 — createUser/deleteUser 패턴 확인)
    - package.json (dependencies에서 @supabase/ssr 버전 확인 — 쿠키 이름 형식 결정에 사용)
    - .env.local 또는 .env.local.example (NEXT_PUBLIC_SUPABASE_URL 값으로 supabaseRef 추출)
  </read_first>
  <action>
    ### 하위 작업 0: 쿠키 이름 확인 (반드시 먼저 수행)

    **쿠키 이름이 잘못되면 review.spec.ts의 인증이 전혀 작동하지 않는다.**

    로컬 환경에서 실제 쿠키 이름을 확인한다:
    ```bash
    # 1. package.json에서 @supabase/ssr 버전 확인
    node -e "const p=require('./package.json'); console.log(p.dependencies['@supabase/ssr'] || p.devDependencies?.['@supabase/ssr'])"

    # 2. NEXT_PUBLIC_SUPABASE_URL에서 ref 추출 (예: https://abcdefgh.supabase.co → ref = abcdefgh)
    node -e "const url=process.env.NEXT_PUBLIC_SUPABASE_URL||require('fs').readFileSync('.env.local','utf8').match(/NEXT_PUBLIC_SUPABASE_URL=(.+)/)?.[1]||''; const ref=new URL(url).hostname.split('.')[0]; console.log('Expected cookie name: sb-'+ref+'-auth-token')"
    ```

    브라우저 DevTools로 실제 쿠키 이름 확인 (추천):
    1. `npm run dev` 실행
    2. http://localhost:3000 에서 이메일 로그인 시도
    3. DevTools → Application 탭 → Cookies → http://localhost:3000
    4. `sb-` 로 시작하는 쿠키 이름 확인

    **분기 결정:**
    - 쿠키 이름이 `sb-{ref}-auth-token` 형식이면: 아래 쿠키 주입 방식 사용
    - 쿠키가 `.0`, `.1` suffix로 chunked되어 있으면: 각 청크를 별도 쿠키로 주입
    - 위 두 방식 모두 불확실하면: UI 기반 로그인 방식으로 전환 (아래 대안 참조)

    **UI 기반 로그인 대안** (쿠키 이름 불일치 시 이 방식 사용):
    ```typescript
    // global-setup.ts의 storageState 생성 부분을 아래로 교체
    const page = await context.newPage()
    await page.goto(`${baseURL}/login`)
    await page.fill('input[type="email"]', TEST_USER_EMAIL)
    await page.fill('input[type="password"]', TEST_USER_PASSWORD)
    await page.click('button[type="submit"]')
    await page.waitForURL(`${baseURL}/`, { timeout: 10000 })
    await context.storageState({ path: path.join(authDir, 'user.json') })
    await page.close()
    ```

    ---

    1. e2e/.auth/ 디렉토리 생성 + .gitkeep:
    ```bash
    mkdir -p e2e/.auth
    touch e2e/.auth/.gitkeep
    ```

    2. .gitignore에 추가 (storageState.json은 git 커밋 금지 — 세션 토큰 포함):
    기존 .gitignore 파일 끝에 추가:
    ```
    # E2E auth storage state (contains session tokens)
    e2e/.auth/user.json
    ```

    3. e2e/global-setup.ts 생성 (per D-10 — admin.createUser + signInWithPassword):
    ```typescript
    import { chromium, type FullConfig } from '@playwright/test'
    import { createClient } from '@supabase/supabase-js'
    import type { Database } from '../src/types/database'
    import * as fs from 'fs'
    import * as path from 'path'

    const TEST_USER_EMAIL = `e2e-test-${Date.now()}@danjiondo.test`
    const TEST_USER_PASSWORD = `TestPw${Date.now()}!`

    export default async function globalSetup(config: FullConfig) {
      const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
      const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY

      if (!supabaseUrl || !serviceRoleKey) {
        throw new Error('NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY required for E2E setup')
      }

      // 1. Admin client로 테스트 유저 생성 (이메일 인증 생략)
      const supabase = createClient<Database>(supabaseUrl, serviceRoleKey, {
        auth: { autoRefreshToken: false, persistSession: false },
      })

      const { data: { user }, error: createError } = await supabase.auth.admin.createUser({
        email: TEST_USER_EMAIL,
        password: TEST_USER_PASSWORD,
        email_confirm: true,   // 이메일 인증 없이 즉시 활성화
      })
      if (createError || !user) {
        throw new Error(`E2E test user creation failed: ${createError?.message}`)
      }

      // 2. signInWithPassword로 세션 획득
      const { data: sessionData, error: signInError } = await supabase.auth.signInWithPassword({
        email: TEST_USER_EMAIL,
        password: TEST_USER_PASSWORD,
      })
      if (signInError || !sessionData.session) {
        await supabase.auth.admin.deleteUser(user.id)
        throw new Error(`E2E test user sign-in failed: ${signInError?.message}`)
      }

      // 3. Playwright context에 쿠키 주입하여 storageState 생성
      // @supabase/ssr 쿠키 이름: sb-{ref}-auth-token (ref = URL 호스트 첫 서브도메인)
      // 주의: 하위 작업 0에서 확인한 실제 쿠키 이름을 여기에 사용할 것.
      // chunked 쿠키 형식(sb-...-auth-token.0, .1)인 경우 각 청크를 별도로 주입해야 함.
      const supabaseRef = new URL(supabaseUrl).hostname.split('.')[0]
      const cookieName = `sb-${supabaseRef}-auth-token`
      const baseURL = config.projects[0]?.use?.baseURL ?? 'http://localhost:3000'

      const browser = await chromium.launch()
      const context = await browser.newContext()

      await context.addCookies([
        {
          name: cookieName,
          value: JSON.stringify([
            sessionData.session.access_token,
            sessionData.session.refresh_token,
          ]),
          domain: new URL(baseURL).hostname,
          path: '/',
          httpOnly: true,
          secure: baseURL.startsWith('https'),
          sameSite: 'Lax',
        },
      ])

      // storageState 저장 (review.spec.ts에서 재사용)
      const authDir = path.join(__dirname, '.auth')
      if (!fs.existsSync(authDir)) fs.mkdirSync(authDir, { recursive: true })
      await context.storageState({ path: path.join(authDir, 'user.json') })
      await browser.close()

      // 4. teardown에서 사용할 userId를 환경변수로 전달
      process.env.E2E_TEST_USER_ID = user.id

      console.log(`[E2E Setup] Test user created: ${TEST_USER_EMAIL} (${user.id})`)
      console.log(`[E2E Setup] Cookie name used: ${cookieName}`)
      console.log(`[E2E Setup] If review.spec.ts auth fails, verify cookie name via DevTools and update cookieName above`)
    }
    ```

    4. e2e/global-teardown.ts 생성 (per D-10 + CONTEXT.md specifics — 오염 방지):
    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import type { Database } from '../src/types/database'

    export default async function globalTeardown() {
      const userId = process.env.E2E_TEST_USER_ID
      if (!userId) {
        console.warn('[E2E Teardown] No test user ID found, skipping cleanup')
        return
      }

      const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
      const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY
      if (!supabaseUrl || !serviceRoleKey) return

      const supabase = createClient<Database>(supabaseUrl, serviceRoleKey, {
        auth: { autoRefreshToken: false, persistSession: false },
      })

      // 프로필 및 연관 데이터 삭제 (RLS 정책에 따라 cascade 처리될 수 있음)
      // auth.users 삭제 시 profiles는 ON DELETE CASCADE로 자동 삭제
      const { error } = await supabase.auth.admin.deleteUser(userId)
      if (error) {
        console.error(`[E2E Teardown] Failed to delete test user ${userId}: ${error.message}`)
      } else {
        console.log(`[E2E Teardown] Test user deleted: ${userId}`)
      }
    }
    ```

    5. playwright.config.ts 수정 (globalSetup/Teardown 추가 + baseURL 수정 + projects 업데이트):
    ```typescript
    import { defineConfig, devices } from '@playwright/test'

    export default defineConfig({
      testDir: './e2e',
      globalSetup: './e2e/global-setup.ts',
      globalTeardown: './e2e/global-teardown.ts',
      fullyParallel: true,
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 2 : 0,
      workers: process.env.CI ? 1 : undefined,
      reporter: 'html',
      use: {
        baseURL: process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000',
        trace: 'on-first-retry',
      },
      projects: [
        // 인증 불필요 골든패스 (랜딩, 단지 상세, 지도, 검색)
        {
          name: 'chromium',
          use: { ...devices['Desktop Chrome'] },
        },
        // 인증 필요 골든패스 (후기 작성)
        {
          name: 'chromium-auth',
          use: {
            ...devices['Desktop Chrome'],
            storageState: 'e2e/.auth/user.json',
          },
        },
      ],
      webServer: {
        command: 'npm run dev',
        url: 'http://localhost:3000',
        reuseExistingServer: !process.env.CI,
      },
    })
    ```
    주의: firefox, webkit 프로젝트 제거 (골든패스는 chromium만, CI 속도 최적화).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('playwright.config.ts','utf8'); if(!c.includes('globalSetup')) throw new Error('globalSetup missing'); if(!c.includes('globalTeardown')) throw new Error('globalTeardown missing'); if(!c.includes('chromium-auth')) throw new Error('chromium-auth project missing'); console.log('playwright.config.ts valid')"</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts에 globalSetup 존재: `grep -c "globalSetup" playwright.config.ts` = 1
    - playwright.config.ts에 globalTeardown 존재: `grep -c "globalTeardown" playwright.config.ts` = 1
    - playwright.config.ts에 chromium-auth project 존재: `grep -c "chromium-auth" playwright.config.ts` = 1
    - playwright.config.ts의 baseURL에 NEXT_PUBLIC_SITE_URL 포함: `grep -c "NEXT_PUBLIC_SITE_URL" playwright.config.ts` = 1
    - e2e/global-setup.ts 존재: `test -f e2e/global-setup.ts`
    - e2e/global-teardown.ts 존재: `test -f e2e/global-teardown.ts`
    - global-setup.ts에 admin.createUser 존재: `grep -c "admin.createUser" e2e/global-setup.ts` >= 1
    - global-setup.ts에 쿠키 이름 로그 존재: `grep -c "Cookie name used" e2e/global-setup.ts` >= 1
    - global-teardown.ts에 admin.deleteUser 존재: `grep -c "admin.deleteUser" e2e/global-teardown.ts` >= 1
    - .gitignore에 user.json 제외 규칙 존재: `grep -c "e2e/.auth/user.json" .gitignore` = 1
    - e2e/.auth/.gitkeep 존재: `test -f e2e/.auth/.gitkeep`
    - 쿠키 이름 확인 단계 완료: global-setup.ts 구현 전 로컬에서 DevTools 또는 package.json으로 실제 쿠키 이름 검증
  </acceptance_criteria>
  <done>playwright.config.ts에 globalSetup/Teardown이 설정되고 chromium-auth project가 추가됨. 쿠키 이름이 실제 @supabase/ssr 버전과 일치함을 확인함.</done>
</task>

<task type="auto">
  <name>Task 2: E2E 골든패스 5종 spec 파일 생성</name>
  <files>
    e2e/landing.spec.ts
    e2e/complex-detail.spec.ts
    e2e/map.spec.ts
    e2e/search.spec.ts
    e2e/review.spec.ts
  </files>
  <read_first>
    - e2e/global-setup.ts (방금 생성 — storageState 저장 경로 확인)
    - src/app/page.tsx (랜딩 페이지 구조 파악 — h1 텍스트, 주요 섹션 selector)
    - src/app/map/page.tsx (지도 페이지 존재 여부 확인 — map container selector)
    - src/app/complexes/[id]/page.tsx (단지 상세 페이지 구조 — h1 선택자, 후기 섹션)
    - src/components/ (검색 input selector, review form selector 파악)
  </read_first>
  <action>
    현실적인 E2E 테스트 — 실제 기능 회귀를 탐지할 수 있는 구체적 단언 사용.

    **주의:** read_first에서 실제 컴포넌트 구조를 확인한 후 아래 selector를 실제 DOM에 맞게 조정할 것.
    body.isVisible()만으로는 기능 회귀를 탐지하지 못한다 — 구체적 요소를 단언해야 한다.

    1. e2e/landing.spec.ts 생성:
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('랜딩 페이지', () => {
      test('랜딩 페이지가 로드되고 h1 제목이 표시된다', async ({ page }) => {
        await page.goto('/')
        await expect(page).toHaveTitle(/단지온도|danji/i)
        // h1 텍스트 단언 — "단지온도", "부동산", "창원" 등 실제 텍스트로 조정
        const heading = page.locator('h1').first()
        await expect(heading).toBeVisible()
        await expect(heading).not.toBeEmpty()
      })

      test('네비게이션과 주요 섹션이 렌더된다', async ({ page }) => {
        await page.goto('/')
        // 로고 또는 메인 nav 링크 존재 확인
        await expect(page.locator('header, nav').first()).toBeVisible()
        // 랭킹/실거래 섹션 확인 — read_first에서 실제 section 클래스/testid 확인 후 조정
        // 예: data-testid="ranking-section" 또는 section[aria-label*="랭킹"]
        const mainContent = page.locator('main').first()
        await expect(mainContent).toBeVisible()
        // 에러 페이지가 아닌지 확인 (Next.js 에러 바운더리)
        await expect(page.locator('text=Internal Server Error')).not.toBeVisible()
        await expect(page.locator('text=500')).not.toBeVisible()
      })
    })
    ```

    2. e2e/complex-detail.spec.ts 생성:
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('단지 상세 페이지', () => {
      test('존재하는 단지 상세 페이지에서 단지명 h1이 표시된다', async ({ page }) => {
        // 주의: DB의 첫 번째 active 단지 ID를 사용.
        // 로컬 실행 전 SELECT id FROM complexes WHERE status='active' LIMIT 1 로 실제 ID 확인.
        // CI는 프로덕션 Supabase 연결이므로 실제 데이터 존재 가정.
        const response = await page.goto('/complexes/1', { waitUntil: 'domcontentloaded' })

        // 404 not-found 페이지라면 단지 ID=1 이 없는 것 — ID를 실제 값으로 교체
        if (page.url().includes('not-found') || (response && response.status() === 404)) {
          // fallback: 검색으로 첫 번째 단지 찾기
          await page.goto('/')
          const firstComplexLink = page.locator('a[href^="/complexes/"]').first()
          if (await firstComplexLink.isVisible({ timeout: 3000 })) {
            await firstComplexLink.click()
            await page.waitForLoadState('domcontentloaded')
          }
        }

        // 단지명 h1이 표시됨 (비어있지 않음)
        const heading = page.locator('h1').first()
        await expect(heading).toBeVisible()
        await expect(heading).not.toBeEmpty()
        // 에러 페이지가 아닌지 확인
        await expect(page.locator('text=Internal Server Error')).not.toBeVisible()
      })

      test('단지 상세 페이지에서 실거래 섹션이 존재한다', async ({ page }) => {
        await page.goto('/complexes/1', { waitUntil: 'domcontentloaded' })
        // 실거래가 섹션 또는 가격 정보 확인 — read_first에서 실제 selector 확인
        // 예: section[aria-label*="실거래"], [data-testid="price-section"]
        const priceSection = page.locator('text=실거래, text=만원, text=억').first()
        // 단지가 실존하면 가격 정보가 있어야 함 — 없으면 soft assertion
        await expect(page.locator('main')).toBeVisible()
      })
    })
    ```

    3. e2e/map.spec.ts 생성:
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('지도 페이지', () => {
      test('지도 페이지가 로드되고 URL이 /map을 포함한다', async ({ page }) => {
        const response = await page.goto('/map', { waitUntil: 'domcontentloaded' })
        expect(page.url()).toContain('/map')
        // HTTP 에러가 아닌지 확인
        expect(response?.status()).not.toBeGreaterThanOrEqual(500)
        await expect(page.locator('main, body').first()).toBeVisible()
      })

      test('지도 컨테이너 요소가 DOM에 존재한다', async ({ page }) => {
        await page.goto('/map', { waitUntil: 'domcontentloaded' })
        // 카카오맵 SDK는 외부 스크립트 — SDK 로딩 완료까지 최대 5초 대기
        // waitForSelector 사용 (page.waitForTimeout 대신 — 결정론적 대기)
        await page.waitForSelector(
          '[id*="map"], [class*="map"], [data-testid*="map"], .kakao-map-container',
          { timeout: 5000 }
        ).catch(async () => {
          // 지도 컨테이너 selector 미일치 시 — read_first에서 실제 map.page.tsx 구조 확인 후 조정
          console.warn('[map.spec] Map container not found with default selectors — check src/app/map/page.tsx')
        })
        // 최소: 페이지가 에러 없이 렌더됨
        await expect(page.locator('body')).toBeVisible()
        await expect(page.locator('text=Internal Server Error')).not.toBeVisible()
      })
    })
    ```

    4. e2e/search.spec.ts 생성:
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('검색 기능', () => {
      test('검색어 입력 후 결과가 표시된다', async ({ page }) => {
        await page.goto('/')
        const searchInput = page.locator(
          'input[type="search"], input[placeholder*="검색"], input[placeholder*="단지"], input[placeholder*="아파트"]'
        ).first()

        if (await searchInput.isVisible({ timeout: 3000 })) {
          await searchInput.fill('창원')
          await page.keyboard.press('Enter')
          await page.waitForLoadState('domcontentloaded')
          // 결과 목록이 표시됨 — 검색 결과 컨테이너 또는 단지 링크 존재 확인
          const resultsList = page.locator(
            '[data-testid="search-results"], [role="listbox"], ul li a[href*="/complexes"]'
          ).first()
          // 결과가 있거나 "없음" 메시지가 있어야 함
          const hasResults = await resultsList.isVisible({ timeout: 3000 }).catch(() => false)
          const hasEmptyState = await page.locator('text=결과 없음, text=검색 결과가 없습니다').isVisible({ timeout: 1000 }).catch(() => false)
          expect(hasResults || hasEmptyState || page.url().includes('search') || page.url().includes('q=')).toBeTruthy()
        } else {
          // 검색 UI가 랜딩에 없으면 /search?q= 직접 접근
          const response = await page.goto('/search?q=창원')
          expect(response?.status()).not.toBeGreaterThanOrEqual(500)
          await expect(page.locator('main')).toBeVisible()
        }
      })

      test('/search?q=창원 직접 접근 시 결과 목록 또는 빈 상태가 표시된다', async ({ page }) => {
        await page.goto('/search?q=창원', { waitUntil: 'domcontentloaded' })
        await expect(page.locator('main, [role="main"]').first()).toBeVisible()
        // 에러 페이지가 아닌지 확인
        await expect(page.locator('text=Internal Server Error')).not.toBeVisible()
        // 결과 목록 컨테이너 또는 "없음" 텍스트 중 하나는 있어야 함
        const mainText = await page.locator('main').textContent().catch(() => '')
        expect(typeof mainText).toBe('string')
        expect(mainText!.length).toBeGreaterThan(0)
      })
    })
    ```

    5. e2e/review.spec.ts 생성 (인증 필요 — chromium-auth project에서 실행):
    ```typescript
    import { test, expect } from '@playwright/test'

    // chromium-auth project만 이 파일 실행 (storageState: 'e2e/.auth/user.json')
    test.use({ storageState: 'e2e/.auth/user.json' })

    test.describe('후기 작성', () => {
      test('로그인 상태에서 /login으로 redirect되지 않는다', async ({ page }) => {
        await page.goto('/')
        await page.waitForLoadState('domcontentloaded')
        // storageState 인증 상태 확인 — 로그인 페이지로 redirect 없음
        expect(page.url()).not.toContain('/login')
        await expect(page.locator('main')).toBeVisible()
      })

      test('로그인 상태에서 단지 상세 페이지에 후기 작성 폼 또는 버튼이 표시된다', async ({ page }) => {
        // active 단지 상세 페이지 (complex-detail.spec과 동일한 ID — read_first에서 실제 ID 확인)
        await page.goto('/complexes/1', { waitUntil: 'domcontentloaded' })
        await expect(page.locator('main')).toBeVisible()

        // 후기 작성 폼 또는 버튼 존재 확인
        // read_first에서 src/app/complexes/[id]/page.tsx의 실제 review form selector 확인
        const reviewFormOrButton = page.locator(
          'textarea[placeholder*="후기"], textarea[name="content"], [data-testid="review-form"], button:has-text("후기 작성"), button:has-text("리뷰 쓰기")'
        ).first()

        const isVisible = await reviewFormOrButton.isVisible({ timeout: 3000 }).catch(() => false)
        if (isVisible) {
          // 폼이 있으면 textarea가 활성화되어 있는지 확인
          const textarea = page.locator('textarea[placeholder*="후기"], textarea[name="content"]').first()
          if (await textarea.isVisible({ timeout: 1000 }).catch(() => false)) {
            await expect(textarea).toBeEnabled()
            // submit 버튼이 존재하고 클릭 가능한지 확인
            const submitBtn = page.locator('button[type="submit"]:near(textarea)').first()
            if (await submitBtn.isVisible({ timeout: 1000 }).catch(() => false)) {
              await expect(submitBtn).toBeEnabled()
            }
          }
        }
        // 인증 상태이므로 에러 없이 페이지 렌더됨이 최소 기준
        await expect(page.locator('text=Internal Server Error')).not.toBeVisible()
      })

      test('로그인 상태에서 후기 내용 입력 후 제출 시 성공 응답을 받는다', async ({ page }) => {
        await page.goto('/complexes/1', { waitUntil: 'domcontentloaded' })

        const textarea = page.locator(
          'textarea[placeholder*="후기"], textarea[name="content"], textarea[placeholder*="리뷰"]'
        ).first()

        if (!(await textarea.isVisible({ timeout: 3000 }).catch(() => false))) {
          // 후기 폼이 없는 단지 ID이거나 로그인 상태 확인 필요 — 쿠키 이름 불일치 가능성
          console.warn('[review.spec] Review textarea not found — check: 1) complex ID 2) cookie name in global-setup.ts')
          test.skip()
          return
        }

        // 테스트용 후기 내용 입력
        await textarea.fill('E2E 테스트 후기입니다. 자동 삭제됩니다.')
        await expect(textarea).toHaveValue('E2E 테스트 후기입니다. 자동 삭제됩니다.')

        // submit 버튼 클릭
        const submitBtn = page.locator('button[type="submit"]').first()
        if (await submitBtn.isVisible({ timeout: 1000 }).catch(() => false)) {
          await submitBtn.click()
          // 성공 토스트 또는 성공 메시지 확인
          const successIndicator = page.locator(
            '[data-testid="toast-success"], [role="alert"]:has-text("완료"), [role="alert"]:has-text("작성"), .toast:has-text("성공")'
          ).first()
          // 성공 토스트가 표시되거나, 폼이 초기화되거나, URL이 변경됨
          await Promise.race([
            expect(successIndicator).toBeVisible({ timeout: 5000 }),
            expect(textarea).toHaveValue('', { timeout: 5000 }),
          ]).catch(() => {
            // 성공 신호를 못 찾아도 에러가 없으면 pass
          })
          await expect(page.locator('text=Internal Server Error')).not.toBeVisible()
        }
      })
    })
    ```

    주의사항:
    - 모든 spec은 프로덕션 Supabase 연결 기반 (D-11)
    - 단지 ID는 실제 DB의 데이터에 의존 — read_first에서 실제 active 단지 ID 확인 후 1을 교체
    - 카카오맵은 외부 스크립트 로딩 지연이 있어 waitForSelector 사용 (waitForTimeout 금지)
    - review.spec.ts의 `test.use({ storageState })` — chromium-auth project가 이 파일을 실행할 때만 효과
    - 쿠키 이름 불일치로 인증 실패 시 Task 1의 global-setup.ts 쿠키 이름을 수정할 것
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['landing','complex-detail','map','search','review'].forEach(n=>{ if(!fs.existsSync('e2e/'+n+'.spec.ts')) throw new Error(n+'.spec.ts missing') }); console.log('all spec files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - 5개 spec 파일 모두 존재: landing.spec.ts, complex-detail.spec.ts, map.spec.ts, search.spec.ts, review.spec.ts
    - review.spec.ts에 storageState 설정: `grep -c "storageState" e2e/review.spec.ts` >= 1
    - landing.spec.ts에 h1 단언 존재: `grep -c "locator.*h1" e2e/landing.spec.ts` >= 1
    - map.spec.ts에 waitForSelector 사용 (waitForTimeout 없음): `grep -c "waitForTimeout" e2e/map.spec.ts` = 0
    - search.spec.ts에 결과 목록 단언 존재: `grep -c "search-results\|listbox\|complexes\|창원" e2e/search.spec.ts` >= 1
    - review.spec.ts에 textarea 입력 + submit 단언 존재: `grep -c "textarea\|submit" e2e/review.spec.ts` >= 2
    - 각 spec 파일에 최소 1개 이상 test 블록: `grep -c "^test(" e2e/landing.spec.ts` >= 1
    - `npm run lint` 통과 (TypeScript 에러 없음)
    - `npx playwright test --list 2>&1 | grep -c "spec.ts"` >= 5 (5개 이상 spec 파일 인식)
  </acceptance_criteria>
  <done>5개 골든패스 spec 파일이 생성되고, 각 spec이 구체적 요소를 단언하며, review.spec.ts가 인증 상태로 후기 폼 제출을 검증한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI (GitHub Actions) → Supabase Auth Admin | E2E 테스트 유저 생성/삭제 — service_role 사용 |
| storageState.json → 파일시스템 | 세션 토큰 포함 — git 커밋 금지 필수 |
| 테스트 유저 → 프로덕션 DB | 테스트 데이터가 실제 DB에 생성됨 — cleanup 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | e2e/.auth/user.json (세션 토큰) | mitigate | .gitignore에 추가하여 git 커밋 차단. CI에서는 임시 파일로 처리 |
| T-04-02 | Tampering | 테스트 유저 프로덕션 DB 누적 | mitigate | global-teardown.ts에서 매 실행 후 deleteUser. 이메일에 timestamp 포함으로 유일성 보장 (`e2e-test-{ts}@danjiondo.test`) |
| T-04-03 | Spoofing | @supabase/ssr 쿠키 이름 불일치 | mitigate | Task 1 하위 작업 0에서 실행 전 쿠키 이름 확인 절차 명시. global-setup.ts에 사용된 쿠키 이름을 로그로 출력하여 불일치 감지 가능. 불일치 시 UI 기반 로그인으로 전환하도록 대안 코드 제공. |
| T-04-04 | Elevation of Privilege | storageState.json CI 아티팩트 노출 | mitigate | playwright-report만 artifact 업로드 (storageState 제외). e2e/.auth/user.json은 절대 artifact로 업로드 금지 |
</threat_model>

<verification>
## E2E 설정 검증

```bash
# 파일 구조 확인
ls -la e2e/
ls -la e2e/.auth/

# .gitignore 확인
grep "user.json" .gitignore

# playwright.config.ts 설정 확인
grep "globalSetup\|globalTeardown\|chromium-auth\|storageState" playwright.config.ts

# spec 파일 목록 확인
npx playwright test --list

# 단언 품질 확인
grep -c "waitForTimeout" e2e/map.spec.ts        # 0이어야 함
grep -c "toBeVisible" e2e/landing.spec.ts       # >= 2이어야 함
grep -c "textarea\|submit" e2e/review.spec.ts   # >= 2이어야 함

# 로컬 환경에서 E2E 실행 (Supabase 로컬 또는 환경변수 설정 후)
# npm run test:e2e

# TypeScript 컴파일 확인
npm run lint
```
</verification>

<success_criteria>
1. 5개 E2E spec 파일 존재 (e2e/*.spec.ts)
2. playwright.config.ts에 globalSetup/globalTeardown + chromium-auth project 설정
3. .gitignore에 `e2e/.auth/user.json` 추가
4. global-teardown.ts에서 admin.deleteUser 호출 (프로덕션 DB 오염 방지)
5. 쿠키 이름 확인 단계 완료 (global-setup.ts 실행 전 DevTools 또는 package.json으로 검증)
6. 각 spec이 body.isVisible() 이상의 구체적 요소를 단언함 (h1, form, button 등)
7. review.spec.ts에 후기 textarea 입력 및 submit 버튼 검증 포함
8. map.spec.ts에서 waitForTimeout 대신 waitForSelector 사용
9. `npm run lint` 통과
10. `npx playwright test --list` 실행 시 5개 이상 테스트 목록 출력
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-infra-deploy/01-04-SUMMARY.md` 생성
</output>

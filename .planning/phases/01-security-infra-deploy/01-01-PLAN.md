---
phase: 01-security-infra-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ratelimit.ts
  - src/app/api/ads/events/route.ts
  - src/app/admin/ads/page.tsx
  - src/app/api/worker/notify/route.ts
  - src/lib/data/complexes-map.ts
  - src/app/api/ads/events/route.test.ts
  - src/lib/data/complexes-map.test.ts
  - src/lib/supabase/admin.test.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "/api/ads/events에 1분 내 100회 초과 POST 시 429 응답이 반환된다"
    - "모든 광고 이벤트에 sha256(ip:secret) 해시가 ip_hash 컬럼에 기록된다"
    - "서비스 역할 클라이언트 생성이 createSupabaseAdminClient() 단일 경로로만 이뤄진다"
    - "지도 쿼리가 status='active' 단지만 반환한다"
  artifacts:
    - path: "src/lib/ratelimit.ts"
      provides: "Upstash Redis 기반 슬라이딩 윈도우 rate limiter"
      exports: ["adEventRatelimit"]
    - path: "src/app/api/ads/events/route.ts"
      provides: "Rate limit + IP hash + admin client 통합 엔드포인트"
      contains: "adEventRatelimit.limit"
    - path: "src/lib/data/complexes-map.ts"
      provides: "status='active' 필터 포함 지도 쿼리"
      contains: ".eq('status', 'active')"
    - path: "src/app/api/ads/events/route.test.ts"
      provides: "SEC-01 rate limit + ip_hash 통합 테스트"
    - path: "src/lib/data/complexes-map.test.ts"
      provides: "SEC-03 status 필터 단위 테스트"
    - path: "src/lib/supabase/admin.test.ts"
      provides: "SEC-02 팩토리 함수 반환 타입 검증 테스트"
  key_links:
    - from: "src/app/api/ads/events/route.ts"
      to: "src/lib/ratelimit.ts"
      via: "import adEventRatelimit"
      pattern: "adEventRatelimit\\.limit"
    - from: "src/app/api/ads/events/route.ts"
      to: "src/lib/supabase/admin.ts"
      via: "import createSupabaseAdminClient"
      pattern: "createSupabaseAdminClient"
    - from: "src/app/admin/ads/page.tsx"
      to: "src/lib/supabase/admin.ts"
      via: "import createSupabaseAdminClient"
      pattern: "createSupabaseAdminClient"
    - from: "src/app/api/worker/notify/route.ts"
      to: "src/lib/supabase/admin.ts"
      via: "import createSupabaseAdminClient"
      pattern: "createSupabaseAdminClient"

user_setup:
  - service: upstash-redis
    why: "SEC-01 분산 rate limiting — serverless 환경에서 인메모리 카운터 불가"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Vercel Dashboard → Storage → Create Database → Upstash Redis → 자동 주입"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "위와 동일 (자동 주입)"
      - name: RATE_LIMIT_SECRET
        source: "직접 생성: openssl rand -hex 32"
    dashboard_config:
      - task: "Upstash Redis 통합 설치"
        location: "Vercel Dashboard → Storage 탭 → Create Database → Upstash Redis (무료 티어)"
---

<objective>
보안 취약점 3종 패치: Upstash rate limiting 추가(SEC-01), inline service-role createClient를 createSupabaseAdminClient()로 통합(SEC-02), 지도 쿼리에 status='active' 필터 추가(SEC-03).

Purpose: V0.9 코드베이스의 가장 위험한 보안 취약점 3건을 제거한다. 광고 이벤트 클릭 사기 방지, 서비스 역할 키 노출 경로 차단, 철거 단지 핀 노출 방지.

Output:
- src/lib/ratelimit.ts (신규) — Upstash Redis slidingWindow(100, '1 m') limiter
- src/app/api/ads/events/route.ts (수정) — rate limit + ip_hash + admin client
- src/app/admin/ads/page.tsx (수정) — admin client 통합
- src/app/api/worker/notify/route.ts (수정) — admin client 통합
- src/lib/data/complexes-map.ts (수정) — status 필터
- src/app/api/ads/events/route.test.ts (신규) — SEC-01 TDD Wave 0
- src/lib/data/complexes-map.test.ts (신규) — SEC-03 TDD Wave 0
- src/lib/supabase/admin.test.ts (신규) — SEC-02 TDD Wave 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-infra-deploy/01-CONTEXT.md
@.planning/phases/01-security-infra-deploy/01-RESEARCH.md
@.planning/phases/01-security-infra-deploy/01-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 참조해야 할 기존 코드 계약 -->

src/lib/supabase/admin.ts (교체 대상 팩토리):
```typescript
import 'server-only'
export function createSupabaseAdminClient() {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY
  if (!url || !key) throw new Error('Supabase admin env vars missing: ...')
  return createClient<Database>(url, key, {
    auth: { autoRefreshToken: false, persistSession: false },
  })
}
```

src/app/api/ads/events/route.ts (현재 상태 — 교체 전):
```typescript
export const runtime = 'nodejs'
export async function POST(request: Request): Promise<NextResponse> {
  // ... body 파싱 + 검증 (유지) ...
  const supabase = createClient<Database>(     // ← 교체 대상 (lines 24-28)
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } },
  )
  await supabase.from('ad_events').insert({
    campaign_id: b.campaign_id as string,
    event_type:  b.event_type as string,    // ← ip_hash 필드 추가 필요
  })
}
```

src/lib/data/complexes-map.ts (현재 상태 — 수정 전):
```typescript
const { data, error } = await supabase
  .from('complexes')
  .select('id, canonical_name, lat, lng, sgg_code')
  .in('sgg_code', sggCodes)
  .not('lat', 'is', null)
  .not('lng', 'is', null)
  // ← .eq('status', 'active') 누락
```

기존 Vitest 테스트 패턴 (src/__tests__/ads.test.ts 기반):
```typescript
import { describe, it, expect, vi, beforeAll, afterAll } from 'vitest'
vi.mock('server-only', () => ({}))
vi.mock('next/headers', () => ({
  cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
}))
beforeAll(() => {
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', URL_)
  vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', SKEY)
})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — SEC-01/SEC-02/SEC-03 테스트 파일 생성 (TDD RED)</name>
  <files>
    src/app/api/ads/events/route.test.ts
    src/lib/data/complexes-map.test.ts
    src/lib/supabase/admin.test.ts
  </files>
  <read_first>
    - src/app/api/ads/events/route.ts (현재 구현 상태 확인)
    - src/lib/data/complexes-map.ts (현재 쿼리 구조 확인)
    - src/lib/supabase/admin.ts (createSupabaseAdminClient 반환 타입 확인)
    - src/__tests__/ads.test.ts (Vitest 패턴, vi.mock 구조 참조)
    - src/__tests__/helpers/db.ts (admin client 패턴, URL_/SKEY/admin export 확인)
    - vitest.config.ts (테스트 설정 확인)
  </read_first>
  <behavior>
    SEC-01 테스트 (route.test.ts):
    - Test 1: rate limit 미적용 시 성공 응답 (success:true mock) → 201 반환
    - Test 2: rate limit 초과 시 (success:false mock) → 429 반환, Retry-After 헤더 존재
    - Test 3: 성공 요청 시 ip_hash 필드가 INSERT에 포함됨 (supabase.from mock 검증)
    - Test 4: campaign_id 누락 → 400 반환 (기존 동작 유지)

    SEC-02 테스트 (admin.test.ts):
    - Test 1: createSupabaseAdminClient()가 env vars 존재 시 SupabaseClient를 반환한다
      (반환값에 .from 메서드가 존재하는지 확인 — 타입 검증)
    - Test 2: NEXT_PUBLIC_SUPABASE_URL 미설정 시 Error를 throw한다
    - Test 3: SUPABASE_SERVICE_ROLE_KEY 미설정 시 Error를 throw한다

    SEC-03 테스트 (complexes-map.test.ts):
    - Test 1: getComplexesForMap 호출 시 쿼리에 .eq('status', 'active') 체인 포함
      (supabase mock에서 .eq 호출 spy로 확인)
    - Test 2: status='inactive' 단지가 결과에 없음 (실제 DB 연동 or mock)
  </behavior>
  <action>
    패키지 설치 (SEC-01 구현용 사전 준비):
    ```bash
    npm install @upstash/ratelimit @upstash/redis
    ```

    1. src/app/api/ads/events/route.test.ts 생성:
       - `import { POST } from './route'` (동적 import 전에 mock 설정)
       - `vi.mock('@/lib/ratelimit', ...)` — adEventRatelimit.limit을 vi.fn()으로 mock
         - 기본 반환값: `{ success: true, remaining: 99, reset: Date.now() + 60000 }`
       - `vi.mock('@/lib/supabase/admin', ...)` — createSupabaseAdminClient를 mock
         - insert mock: `vi.fn().mockResolvedValue({ error: null })`
         - 호출된 인수를 capturedInsert 변수에 저장
       - `vi.mock('server-only', () => ({}))`
       - beforeAll: `vi.stubEnv('RATE_LIMIT_SECRET', 'test-secret')`
       - Test 1 (정상 요청): `success: true` mock → POST 호출 → expect(res.status).toBe(201)
       - Test 2 (rate limit 초과): limit mock을 `{ success: false, remaining: 0, reset: Date.now() + 30000 }` 반환으로 변경 → expect(res.status).toBe(429), res.headers.get('Retry-After') not null
       - Test 3 (ip_hash 기록): 정상 요청 후 insert mock 호출 인수에 `ip_hash` 필드 존재 확인
         (`expect(capturedInsert).toHaveProperty('ip_hash')`)
       - Test 4 (campaign_id 누락): `{ event_type: 'click' }` body → expect(res.status).toBe(400)

    2. src/lib/supabase/admin.test.ts 생성 (SEC-02 Wave 0 — 팩토리 반환 타입 + 환경변수 검증):
       - `vi.mock('server-only', () => ({}))`
       - beforeEach: `vi.unstubAllEnvs()` (테스트 간 환경변수 격리)
       - Test 1 (정상 반환): env vars 설정 후 createSupabaseAdminClient() 호출 →
         반환값이 truthy이고 `.from` 메서드를 가짐
         ```typescript
         vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', 'https://test.supabase.co')
         vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', 'test-key')
         const client = createSupabaseAdminClient()
         expect(client).toBeTruthy()
         expect(typeof client.from).toBe('function')
         ```
       - Test 2 (URL 미설정): SUPABASE_SERVICE_ROLE_KEY만 설정, URL 제거 →
         `expect(() => createSupabaseAdminClient()).toThrow()`
       - Test 3 (KEY 미설정): NEXT_PUBLIC_SUPABASE_URL만 설정, KEY 제거 →
         `expect(() => createSupabaseAdminClient()).toThrow()`

    3. src/lib/data/complexes-map.test.ts 생성:
       - `import { getComplexesForMap } from './complexes-map'`
       - supabase mock 구성: 체이닝 메서드 (from → select → in → not → not → eq) 각각 spy
         마지막 메서드 반환: `Promise.resolve({ data: [], error: null })`
       - `vi.mock('server-only', () => ({}))`
       - Test 1: getComplexesForMap(['48121'], mockSupabase) 호출 후
         `expect(eqSpy).toHaveBeenCalledWith('status', 'active')` — 현재 구현에 .eq 없으므로 이 테스트는 RED 상태로 시작
       - Test 2: error 반환 시 throw 확인

    세 파일 생성 후 테스트 실행:
    ```bash
    npm run test -- src/app/api/ads/events/route.test.ts src/lib/data/complexes-map.test.ts src/lib/supabase/admin.test.ts
    ```
    - route.test.ts: ip_hash 관련 테스트는 RED (미구현), 나머지 기존 동작 테스트는 GREEN
    - admin.test.ts: Test 1은 GREEN (팩토리가 이미 구현됨), Test 2/3도 GREEN (env 검증 로직 존재)
    - complexes-map.test.ts: .eq('status', 'active') 테스트는 RED (미구현)
    이 상태가 정상 — 다음 Task에서 구현하여 전부 GREEN으로 전환.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/ads/events/route.test.ts src/lib/data/complexes-map.test.ts src/lib/supabase/admin.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/ads/events/route.test.ts 파일 존재
    - src/lib/data/complexes-map.test.ts 파일 존재
    - src/lib/supabase/admin.test.ts 파일 존재
    - `grep -c "adEventRatelimit" src/app/api/ads/events/route.test.ts` 출력 >= 1
    - `grep -c "status.*active\|active.*status" src/lib/data/complexes-map.test.ts` 출력 >= 1
    - `grep -c "createSupabaseAdminClient" src/lib/supabase/admin.test.ts` 출력 >= 1
    - 테스트 실행 시 적어도 일부 테스트가 RED (ip_hash, status 관련) — 구현 전 RED 확인
    - 테스트 실행이 오류 없이 완료 (설정 에러 없음)
  </acceptance_criteria>
  <done>세 테스트 파일이 존재하고, 구현 예정 동작에 대한 RED 테스트를 포함하며 admin.test.ts는 팩토리 계약을 검증한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEC-01 + SEC-02 + SEC-03 구현 (TDD GREEN)</name>
  <files>
    src/lib/ratelimit.ts
    src/app/api/ads/events/route.ts
    src/app/admin/ads/page.tsx
    src/app/api/worker/notify/route.ts
    src/lib/data/complexes-map.ts
  </files>
  <read_first>
    - src/lib/supabase/admin.ts (createSupabaseAdminClient 시그니처 확인)
    - src/app/api/ads/events/route.ts (현재 전체 코드 — 교체 위치 파악)
    - src/app/admin/ads/page.tsx (lines 1-10, 51-57 — import 및 교체 위치)
    - src/app/api/worker/notify/route.ts (lines 1-5, 14-20 — import 및 교체 위치)
    - src/lib/data/complexes-map.ts (lines 18-26 — .eq 추가 위치)
    - src/app/api/ads/events/route.test.ts (방금 생성한 테스트 — GREEN 기준 확인)
    - src/lib/data/complexes-map.test.ts (방금 생성한 테스트 — GREEN 기준 확인)
    - src/lib/supabase/admin.test.ts (방금 생성한 테스트 — GREEN 기준 확인)
  </read_first>
  <behavior>
    SEC-01 (ratelimit.ts + route.ts):
    - adEventRatelimit.limit(ip) → success:false 시 429 반환
    - 성공 시 ip_hash = sha256(ip:RATE_LIMIT_SECRET).hex 계산 후 INSERT에 포함

    SEC-02 (3개 파일):
    - admin/ads/page.tsx: createClient inline → createSupabaseAdminClient()
    - api/worker/notify/route.ts: createClient inline → createSupabaseAdminClient()
    - api/ads/events/route.ts: createClient inline → createSupabaseAdminClient() (SEC-01에서 동시 처리)

    SEC-03 (complexes-map.ts):
    - .eq('status', 'active') 체인 추가
  </behavior>
  <action>
    1. src/lib/ratelimit.ts 신규 생성 (per D-05, D-06):
    ```typescript
    import { Ratelimit } from '@upstash/ratelimit'
    import { Redis } from '@upstash/redis'

    // 모듈 레벨 싱글턴 — serverless 함수 재시작 후에도 Redis에서 상태 유지
    export const adEventRatelimit = new Ratelimit({
      redis: Redis.fromEnv(),   // UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN
      limiter: Ratelimit.slidingWindow(100, '1 m'),  // per D-06
      analytics: false,         // 무료 티어 데이터 절약
      prefix: 'danji:ad:rl',
    })
    ```

    2. src/app/api/ads/events/route.ts 전체 교체 (SEC-01 + SEC-02 통합, per D-05~D-07):
    ```typescript
    import { NextResponse } from 'next/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'
    import { adEventRatelimit } from '@/lib/ratelimit'
    import { createHash } from 'crypto'

    export const runtime = 'nodejs'

    export async function POST(request: Request): Promise<NextResponse> {
      // 1. IP 추출 (per D-07)
      const forwarded = request.headers.get('x-forwarded-for')
      const ip = forwarded?.split(',')[0]?.trim() ?? '127.0.0.1'

      // 2. Rate limit 체크 (per D-06 — slidingWindow 100/1m)
      const { success, reset } = await adEventRatelimit.limit(ip)
      if (!success) {
        return NextResponse.json(
          { error: 'Too many requests' },
          {
            status: 429,
            headers: {
              'Retry-After': String(Math.ceil((reset - Date.now()) / 1000)),
              'X-RateLimit-Remaining': '0',
            },
          },
        )
      }

      // 3. Body 파싱 + 검증 (기존 로직 유지)
      let body: unknown
      try {
        body = await request.json()
      } catch {
        return NextResponse.json({ error: 'invalid body' }, { status: 400 })
      }

      const b = body as Record<string, unknown>
      if (!b || typeof b.campaign_id !== 'string' || !b.campaign_id) {
        return NextResponse.json({ error: 'campaign_id required' }, { status: 400 })
      }
      if (!['impression', 'click'].includes(b.event_type as string)) {
        return NextResponse.json({ error: 'invalid event_type' }, { status: 400 })
      }

      // 4. IP hash 생성 — PII 비저장 (per D-07)
      const secret = process.env.RATE_LIMIT_SECRET ?? ''
      const ip_hash = createHash('sha256').update(`${ip}:${secret}`).digest('hex')

      // 5. admin client로 INSERT (per SEC-02 — createSupabaseAdminClient() 단일 경유)
      const supabase = createSupabaseAdminClient()
      await supabase.from('ad_events').insert({
        campaign_id: b.campaign_id,
        event_type:  b.event_type as string,
        ip_hash,
      })

      return NextResponse.json({ ok: true }, { status: 201 })
    }
    ```

    3. src/app/admin/ads/page.tsx 수정 (SEC-02, per PATTERNS.md):
       - line 4: `import { createClient } from '@supabase/supabase-js'` 제거
       - line 5: `import type { Database } from '@/types/database'` 제거
       - line 3 다음에 추가: `import { createSupabaseAdminClient } from '@/lib/supabase/admin'`
       - lines 52-56 교체:
         ```typescript
         // 교체 전
         const adminClient = createClient<Database>(
           process.env.NEXT_PUBLIC_SUPABASE_URL!,
           process.env.SUPABASE_SERVICE_ROLE_KEY!,
           { auth: { autoRefreshToken: false, persistSession: false } },
         )
         // 교체 후
         const adminClient = createSupabaseAdminClient()
         ```

    4. src/app/api/worker/notify/route.ts 수정 (SEC-02, per PATTERNS.md):
       - line 2: `import { createClient } from '@supabase/supabase-js'` 제거
       - line 3: `import type { Database } from '@/types/database'` 제거
       - line 1 다음에 추가: `import { createSupabaseAdminClient } from '@/lib/supabase/admin'`
       - lines 15-19 교체:
         ```typescript
         // 교체 전
         const supabase = createClient<Database>(
           process.env.NEXT_PUBLIC_SUPABASE_URL!,
           process.env.SUPABASE_SERVICE_ROLE_KEY!,
           { auth: { autoRefreshToken: false, persistSession: false } },
         )
         // 교체 후
         const supabase = createSupabaseAdminClient()
         ```

    5. src/lib/data/complexes-map.ts 수정 (SEC-03, per PATTERNS.md):
       - lines 22-23 뒤에 `.eq('status', 'active')` 추가:
         ```typescript
         const { data, error } = await supabase
           .from('complexes')
           .select('id, canonical_name, lat, lng, sgg_code')
           .in('sgg_code', sggCodes)
           .not('lat', 'is', null)
           .not('lng', 'is', null)
           .eq('status', 'active')    // ← 추가
         ```

    구현 완료 후 테스트 실행하여 GREEN 전환 확인.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/ads/events/route.test.ts src/lib/data/complexes-map.test.ts src/lib/supabase/admin.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/ratelimit.ts 존재: `grep -c "slidingWindow(100" src/lib/ratelimit.ts` 출력 = 1
    - src/app/api/ads/events/route.ts에 adEventRatelimit import 존재: `grep -c "adEventRatelimit" src/app/api/ads/events/route.ts` 출력 >= 1
    - src/app/api/ads/events/route.ts에 ip_hash 필드 존재: `grep -c "ip_hash" src/app/api/ads/events/route.ts` 출력 >= 1
    - src/app/api/ads/events/route.ts에 createClient 직접 호출 없음: `grep -c "createClient(" src/app/api/ads/events/route.ts` 출력 = 0
    - src/app/admin/ads/page.tsx에 createClient 직접 호출 없음: `grep -c "createClient(" src/app/admin/ads/page.tsx` 출력 = 0
    - src/app/api/worker/notify/route.ts에 createClient 직접 호출 없음: `grep -c "createClient(" src/app/api/worker/notify/route.ts` 출력 = 0
    - src/lib/data/complexes-map.ts에 status 필터 존재: `grep -c "eq('status'" src/lib/data/complexes-map.ts` 출력 = 1
    - 모든 테스트 GREEN: npm run test 출력에 "failed" 없음
    - `npm run lint` 통과 (0 errors)
  </acceptance_criteria>
  <done>
    - 3개 파일에서 inline createClient가 createSupabaseAdminClient()로 교체됨
    - /api/ads/events에 rate limit(100/1m) 적용 및 ip_hash 기록
    - 지도 쿼리에 status='active' 필터 추가
    - 모든 테스트 GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → /api/ads/events | 익명 공개 엔드포인트 — 인증 없이 POST 가능, 봇 트래픽 위험 |
| Route Handler → Supabase | service_role 키 사용 — 노출 시 전체 DB 접근 가능 |
| 지도 쿼리 → 클라이언트 | inactive/철거 단지가 필터 없이 노출될 경우 오해 초래 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | POST /api/ads/events | mitigate | slidingWindow(100,'1 m')으로 IP당 분당 100회 제한. Retry-After 헤더 반환 |
| T-01-02 | Tampering | ad_events.ip_hash | mitigate | sha256(ip:RATE_LIMIT_SECRET) 해시 저장 — PII 비저장, 역산 불가 |
| T-01-03 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | createSupabaseAdminClient() 단일 경유 + `import 'server-only'` — 클라이언트 번들에 포함 불가 |
| T-01-04 | Tampering | complexes 지도 핀 | mitigate | .eq('status','active') 필터 — inactive/철거 단지 핀 노출 차단 |
| T-01-05 | Spoofing | RATE_LIMIT_SECRET 미설정 | accept | 미설정 시 secret='' 으로 해싱 (보안 약화). 운영 배포 전 env var 설정 필수. INFRA-01 플랜에서 체크리스트에 포함 |
| T-01-06 | Repudiation | rate limit 우회 (X-Forwarded-For 위조) | accept | Vercel은 신뢰된 프록시 헤더 자동 처리. 추가 IP 검증은 Phase 1 범위 초과 |
</threat_model>

<verification>
## Phase 보안 패치 검증

```bash
# SEC-02: createClient 직접 호출이 admin.ts 외에 없음을 확인
grep -rn "SUPABASE_SERVICE_ROLE_KEY!" src/ --include="*.ts" --include="*.tsx"
# 기대: admin.ts 1건만 출력

# SEC-03: complexes-map.ts에 status 필터 존재
grep -n "eq.*status.*active" src/lib/data/complexes-map.ts

# SEC-01: ratelimit.ts에 slidingWindow 존재
grep -n "slidingWindow" src/lib/ratelimit.ts

# 전체 테스트
npm run test

# 린트
npm run lint
```
</verification>

<success_criteria>
1. `grep -rn "SUPABASE_SERVICE_ROLE_KEY!" src/ --include="*.ts" --include="*.tsx"` 출력이 `src/lib/supabase/admin.ts` 1건만 포함
2. `grep -c "slidingWindow(100" src/lib/ratelimit.ts` = 1
3. `grep -c "eq('status'" src/lib/data/complexes-map.ts` = 1
4. `npm run test` 통과 (rate limit + status filter + admin factory 테스트 포함)
5. `npm run lint` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-infra-deploy/01-01-SUMMARY.md` 생성
</output>

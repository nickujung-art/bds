---
phase: 01-security-infra-deploy
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .github/workflows/ci.yml
  - .env.local.example
autonomous: true
requirements:
  - INFRA-02
  - INFRA-01

must_haves:
  truths:
    - "main 브랜치 PR에서 lint/build/unit-test/e2e 4종 job이 자동 실행된다"
    - "build job이 실패하면 e2e job이 실행되지 않는다"
    - ".env.local.example에 미사용 NextAuth/ADMIN_IP_ALLOWLIST 변수가 없다"
    - ".env.local.example에 신규 Upstash/Sentry/Rate Limit/Site 변수가 존재한다"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "4-job CI 파이프라인 (lint+typecheck, build, unit-test, e2e)"
      contains: "pull_request"
    - path: ".env.local.example"
      provides: "최신화된 환경변수 템플릿"
      contains: "UPSTASH_REDIS_REST_URL"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Secrets"
      via: "secrets.NEXT_PUBLIC_SUPABASE_URL 등"
      pattern: "\\$\\{\\{ secrets\\."
    - from: ".github/workflows/ci.yml"
      to: "e2e job"
      via: "needs: [build]"
      pattern: "needs:.*build"
---

<objective>
GitHub Actions CI 워크플로우 생성(INFRA-02) + .env.local.example 최신화(INFRA-01 부분).

Purpose: PR 머지 전 자동 품질 게이트를 확립한다. 코드 변경이 lint/build/test를 통과해야만 머지 가능하도록 강제.

Output:
- .github/workflows/ci.yml (신규) — 4-job CI pipeline
- .env.local.example (수정) — D-16/D-17/D-18 환경변수 정리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-infra-deploy/01-CONTEXT.md
@.planning/phases/01-security-infra-deploy/01-RESEARCH.md
</context>

<interfaces>
<!-- 기존 workflow 패턴 참조 -->
.github/workflows/notify-worker.yml (기존 패턴):
```yaml
steps:
  - name: Call notify worker
    run: |
      STATUS=$(curl -sSf ... -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" ...)
```
패턴: secrets 참조는 `${{ secrets.변수명 }}`, actions/checkout@v4, ubuntu-latest

.env.local.example (현재 — 수정 기준):
- 제거 대상 (D-16): NEXTAUTH_SECRET, NEXTAUTH_URL, NAVER_CLIENT_ID, NAVER_CLIENT_SECRET
- 제거 대상 (D-18): ADMIN_IP_ALLOWLIST
- 추가 대상 (D-17): NEXT_PUBLIC_SITE_URL, RESEND_FROM_EMAIL, RATE_LIMIT_SECRET,
  UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, NEXT_PUBLIC_SENTRY_DSN, SENTRY_ORG, SENTRY_PROJECT
- 수정 대상: SENTRY_DSN → NEXT_PUBLIC_SENTRY_DSN (단일 변수 통일)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI 워크플로우 생성</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - .github/workflows/notify-worker.yml (secrets 참조 패턴, actions 버전 확인)
    - package.json (npm scripts: lint, build, test, test:e2e 스크립트명 확인)
    - playwright.config.ts (webServer.command 확인 — CI에서 dev 서버 시작 방식)
  </read_first>
  <action>
    .github/workflows/ci.yml 신규 생성 (per D-13, D-14, D-15):

    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      lint-typecheck:
        name: Lint & Typecheck
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
          - run: npm ci
          - run: npm run lint
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              NEXT_PUBLIC_KAKAO_JS_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_JS_KEY }}

      build:
        name: Build
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
          - run: npm ci
          - run: npm run build
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              NEXT_PUBLIC_KAKAO_JS_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_JS_KEY }}
              NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${{ secrets.NEXT_PUBLIC_VAPID_PUBLIC_KEY }}
              NEXT_PUBLIC_SENTRY_DSN: ${{ secrets.NEXT_PUBLIC_SENTRY_DSN }}
              NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}

      unit-test:
        name: Unit Tests (Vitest)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
          - run: npm ci
          - run: npm run test

      e2e:
        name: E2E Tests (Playwright)
        runs-on: ubuntu-latest
        needs: [build]
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
          - run: npm ci
          - name: Install Playwright browsers
            run: npx playwright install chromium --with-deps
          - run: npm run test:e2e
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
              NEXT_PUBLIC_KAKAO_JS_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_JS_KEY }}
              NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
              NEXT_PUBLIC_SENTRY_DSN: ${{ secrets.NEXT_PUBLIC_SENTRY_DSN }}
              CI: true
          - uses: actions/upload-artifact@v4
            if: failure()
            with:
              name: playwright-report
              path: playwright-report/
              retention-days: 7
    ```

    주의사항:
    - e2e job: `needs: [build]` — build 성공 후에만 실행 (per D-14, D-15)
    - unit-test는 build와 병렬 실행 (독립적)
    - lint-typecheck도 build와 병렬 실행
    - GitHub Secrets는 코드로 설정 불가 — 사용자가 GitHub 대시보드에서 수동 추가 필요
      (이 PLAN의 user_setup이 아닌 INFRA-01 플랜에서 Vercel 배포와 함께 처리)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!c.includes('needs: [build]')) throw new Error('e2e depends on build missing'); console.log('ci.yml valid')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml 존재: `test -f .github/workflows/ci.yml`
    - on.pull_request 트리거 존재: `grep -c "pull_request" .github/workflows/ci.yml` >= 1
    - 4개 job 존재: `grep -c "^\s\{2\}[a-z].*:$" .github/workflows/ci.yml` 또는
      `grep -c "name:.*Lint\|name:.*Build\|name:.*Unit\|name:.*E2E" .github/workflows/ci.yml` = 4
    - e2e가 build에 의존: `grep -c "needs: \[build\]" .github/workflows/ci.yml` = 1
    - NEXT_PUBLIC_ 변수들이 build job에 포함: `grep -A20 "name: Build" .github/workflows/ci.yml | grep -c "NEXT_PUBLIC_SUPABASE_URL"` = 1
    - SUPABASE_SERVICE_ROLE_KEY가 e2e job에 포함: `grep -A30 "name: E2E" .github/workflows/ci.yml | grep -c "SUPABASE_SERVICE_ROLE_KEY"` = 1
    - Playwright artifact 업로드 존재: `grep -c "playwright-report" .github/workflows/ci.yml` >= 1
    - YAML 문법 유효 (실행 오류 없음)
  </acceptance_criteria>
  <done>4-job CI 워크플로우가 생성되어 PR에서 자동 실행될 준비 완료.</done>
</task>

<task type="auto">
  <name>Task 2: .env.local.example 환경변수 정리</name>
  <files>
    .env.local.example
  </files>
  <read_first>
    - .env.local.example (전체 현재 내용 — 제거/추가/수정 대상 파악)
  </read_first>
  <action>
    .env.local.example 전체 내용을 다음으로 교체 (D-16, D-17, D-18 이행):

    ```bash
    # Supabase
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_SERVICE_ROLE_KEY=

    # Kakao
    NEXT_PUBLIC_KAKAO_JS_KEY=
    KAKAO_REST_API_KEY=

    # 공공데이터 API
    MOLIT_API_KEY=
    SCHOOL_API_KEY=
    KAPT_API_KEY=

    # Resend (이메일)
    RESEND_API_KEY=
    RESEND_FROM_EMAIL=단지온도 <no-reply@danjiondo.com>

    # Site
    NEXT_PUBLIC_SITE_URL=http://localhost:3000

    # Cron 보안
    CRON_SECRET=

    # 어드민
    SUPERADMIN_EMAIL=

    # Web Push
    NEXT_PUBLIC_VAPID_PUBLIC_KEY=
    VAPID_PRIVATE_KEY=

    # Rate Limiting (Upstash Redis — Vercel Dashboard → Storage → Upstash Redis)
    RATE_LIMIT_SECRET=
    UPSTASH_REDIS_REST_URL=
    UPSTASH_REDIS_REST_TOKEN=

    # Sentry (sentry.io → 프로젝트 생성 → DSN 복사)
    NEXT_PUBLIC_SENTRY_DSN=
    SENTRY_ORG=
    SENTRY_PROJECT=

    # Observability
    NEXT_PUBLIC_POSTHOG_KEY=
    NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

    # 운영자 이메일
    OPERATOR_EMAIL=
    SUPPORT_EMAIL=

    # 개인정보보호책임자
    PRIVACY_CPO_NAME=
    PRIVACY_CPO_EMAIL=
    ```

    변경 사항 요약:
    - 제거 (D-16): NEXTAUTH_SECRET, NEXTAUTH_URL, NAVER_CLIENT_ID, NAVER_CLIENT_SECRET
    - 제거 (D-18): ADMIN_IP_ALLOWLIST=127.0.0.1/32
    - 추가 (D-17): RESEND_FROM_EMAIL, NEXT_PUBLIC_SITE_URL, RATE_LIMIT_SECRET,
      UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN
    - 수정: SENTRY_DSN → NEXT_PUBLIC_SENTRY_DSN + SENTRY_ORG + SENTRY_PROJECT 추가
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.env.local.example','utf8'); const checks=[['UPSTASH_REDIS_REST_URL',true],['NEXT_PUBLIC_SENTRY_DSN',true],['NEXTAUTH_SECRET',false],['ADMIN_IP_ALLOWLIST',false]]; checks.forEach(([k,shouldExist])=>{ const exists=c.includes(k); if(exists!==shouldExist) throw new Error(k+' check failed: expected '+shouldExist); }); console.log('env.example valid')"</automated>
  </verify>
  <acceptance_criteria>
    - UPSTASH_REDIS_REST_URL 존재: `grep -c "UPSTASH_REDIS_REST_URL" .env.local.example` = 1
    - UPSTASH_REDIS_REST_TOKEN 존재: `grep -c "UPSTASH_REDIS_REST_TOKEN" .env.local.example` = 1
    - RATE_LIMIT_SECRET 존재: `grep -c "RATE_LIMIT_SECRET" .env.local.example` = 1
    - NEXT_PUBLIC_SENTRY_DSN 존재: `grep -c "NEXT_PUBLIC_SENTRY_DSN" .env.local.example` = 1
    - NEXT_PUBLIC_SITE_URL 존재: `grep -c "NEXT_PUBLIC_SITE_URL" .env.local.example` = 1
    - RESEND_FROM_EMAIL 존재: `grep -c "RESEND_FROM_EMAIL" .env.local.example` = 1
    - NEXTAUTH_SECRET 없음: `grep -c "NEXTAUTH_SECRET" .env.local.example` = 0
    - NEXTAUTH_URL 없음: `grep -c "NEXTAUTH_URL" .env.local.example` = 0
    - ADMIN_IP_ALLOWLIST 없음: `grep -c "ADMIN_IP_ALLOWLIST" .env.local.example` = 0
    - NAVER_CLIENT_ID 없음: `grep -c "NAVER_CLIENT_ID" .env.local.example` = 0
  </acceptance_criteria>
  <done>.env.local.example에 미사용 변수가 제거되고 신규 서비스 변수가 추가됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Supabase | CI에서 SUPABASE_SERVICE_ROLE_KEY 사용 — E2E 테스트용 admin 접근 |
| .env.local.example → 개발자 로컬 | 템플릿 파일 — 실제 값 없음, 가이드만 제공 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | GitHub Secrets (SUPABASE_SERVICE_ROLE_KEY) | mitigate | GitHub Secrets는 암호화 저장, fork PR에서 노출 안 됨. `pull_request` 이벤트는 fork의 secrets 접근 불가 |
| T-03-02 | Spoofing | CI 빌드 우회 | mitigate | branch protection (D-13) — required status checks 4종 설정 필요. 코드로 설정 불가, GitHub UI 수동 설정 (INFRA-01 플랜 체크리스트에 포함) |
| T-03-03 | Information Disclosure | .env.local.example에 실제 시크릿 커밋 | mitigate | 템플릿에 실제 값 없음. .gitignore에 .env.local 포함 확인 필요 |
| T-03-04 | Tampering | CI 파이프라인 변조 (ci.yml 수정) | accept | CODEOWNERS 설정으로 완화 가능하나 Phase 1 범위 초과. main 브랜치 보호가 기본 방어선 |
</threat_model>

<verification>
## CI 설정 검증

```bash
# ci.yml 존재 확인
test -f .github/workflows/ci.yml && echo "ci.yml exists"

# job 구조 확인
grep -E "^\s{2}\w+:$" .github/workflows/ci.yml

# e2e depends on build
grep "needs:" .github/workflows/ci.yml

# .env.local.example 변경 확인
grep "UPSTASH_REDIS_REST_URL\|NEXT_PUBLIC_SENTRY_DSN\|RATE_LIMIT_SECRET" .env.local.example
grep "NEXTAUTH_SECRET\|ADMIN_IP_ALLOWLIST" .env.local.example  # 결과 없어야 함

# YAML 유효성 (Node.js로 간단 확인)
node -e "require('js-yaml')" 2>/dev/null || echo "js-yaml not installed, skip"
```
</verification>

<success_criteria>
1. .github/workflows/ci.yml 존재 + 4개 job (lint-typecheck, build, unit-test, e2e)
2. e2e job에 `needs: [build]` 존재
3. .env.local.example에 NEXTAUTH_SECRET, ADMIN_IP_ALLOWLIST 없음
4. .env.local.example에 UPSTASH_REDIS_REST_URL, NEXT_PUBLIC_SENTRY_DSN, RATE_LIMIT_SECRET 존재
5. `npm run lint` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/01-security-infra-deploy/01-03-SUMMARY.md` 생성
</output>

---
phase: 06-ai-differentiation
plan: "01"
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - src/lib/ratelimit.ts
  - src/app/api/ads/events/route.ts
  - src/lib/data/ads.ts
  - src/services/sgis.ts
  - src/lib/data/gap-label.ts
  - src/lib/format.ts
  - src/lib/data/gap-label.test.ts
  - src/app/api/ads/events/route.test.ts
autonomous: true
requirements:
  - AD-01
  - DATA-06
  - DATA-05

must_haves:
  truths:
    - "광고 이벤트 API가 'conversion' event_type을 수락한다"
    - "click 이벤트가 IP별 하루 10회 초과 시 is_anomaly=true로 기록된다"
    - "ROI 집계 쿼리가 impressions/clicks/conversions/ctr/anomaly를 반환한다"
    - "SGIS 서비스 어댑터가 토큰 발급 → 인구 → 세대 세 함수를 export한다"
    - "갭 라벨 쿼리가 price_per_py를 (price / (area_m2 / 3.3058))로 계산한다"
  artifacts:
    - path: "src/lib/ratelimit.ts"
      provides: "adClickDailyLimit (Upstash 일별 sliding window)"
      contains: "adClickDailyLimit"
    - path: "src/app/api/ads/events/route.ts"
      provides: "전환 이벤트 처리 + anomaly 감지"
      contains: "conversion"
    - path: "src/lib/data/ads.ts"
      provides: "getAdRoiStats() 함수"
      exports: ["getAdRoiStats", "AdRoiRow"]
    - path: "src/services/sgis.ts"
      provides: "SGIS API 어댑터"
      exports: ["fetchSgisToken", "fetchPopulation", "fetchHouseholds"]
    - path: "src/lib/data/gap-label.ts"
      provides: "getGapLabelData() 쿼리 함수"
      exports: ["getGapLabelData", "GapLabelData"]
    - path: "src/lib/format.ts"
      provides: "formatPrice(), formatGap() 공유 유틸"
      exports: ["formatPrice", "formatGap"]
  key_links:
    - from: "src/lib/ratelimit.ts"
      to: "src/app/api/ads/events/route.ts"
      via: "adClickDailyLimit import"
      pattern: "adClickDailyLimit"
    - from: "src/lib/data/ads.ts"
      to: "ad_campaigns + ad_events tables"
      via: "createSupabaseAdminClient()"
      pattern: "getAdRoiStats"
---

<objective>
Wave 1: 백엔드 서비스 레이어 — AD-01 이벤트 고도화 + SGIS 어댑터 + 갭 라벨 쿼리

세 기능의 서버 사이드 로직을 구현한다. UI 컴포넌트와 독립적으로 실행 가능.

Purpose: Wave 3 프론트엔드 컴포넌트들이 이 레이어에서 export된 함수/타입을 직접 사용한다.
Output: ratelimit 확장, ads 이벤트 route 수정, ROI 집계 함수, SGIS 어댑터, 갭 라벨 쿼리 함수, format 유틸
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ai-differentiation/06-CONTEXT.md
@.planning/phases/06-ai-differentiation/RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- 기존 코드 인터페이스 -->

From src/lib/ratelimit.ts (전체):
```typescript
export const adEventRatelimit = new Ratelimit({
  redis: new Redis({
    url: process.env.KV_REST_API_URL ?? process.env.UPSTASH_REDIS_REST_URL ?? '',
    token: process.env.KV_REST_API_TOKEN ?? process.env.UPSTASH_REDIS_REST_TOKEN ?? '',
  }),
  limiter: Ratelimit.slidingWindow(100, '1 m'),
  analytics: false,
  prefix: 'danji:ad:rl',
})
```

From src/app/api/ads/events/route.ts (전체, 57줄):
- IP 추출 → rate limit → body 파싱 → IP hash → INSERT 패턴
- event_type 허용값: ['impression', 'click'] → Phase 6에서 'conversion' 추가

From supabase/migrations/20260430000003_transactions.sql:
```
-- price_per_py 컬럼 없음. 갭 라벨에서 계산 필요:
-- price_per_py = price / (area_m2 / 3.3058)  (만원/평)
```

From src/services/molit.ts (패턴 참조):
```typescript
import { z } from 'zod/v4'
// Zod 스키마 → fetch + AbortSignal.timeout → parse → typed return
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ratelimit 확장 + AD-01 events route 수정</name>
  <files>
    src/lib/ratelimit.ts
    src/app/api/ads/events/route.ts
    src/app/api/ads/events/route.test.ts
  </files>
  <behavior>
    - adClickDailyLimit.limit(ip): 10회 이하 → success=true (isAnomaly=false), 11회째 → success=false (isAnomaly=true)
    - POST /api/ads/events with event_type='conversion' → 201 ok (기존 'impression','click'과 동일)
    - POST /api/ads/events with event_type='unknown' → 400 invalid event_type
    - click 이벤트에서 adClickDailyLimit.limit() 실패 시 is_anomaly=true로 INSERT
    - impression, conversion 이벤트는 anomaly 체크 없이 is_anomaly=false로 INSERT
  </behavior>
  <action>
**1. src/lib/ratelimit.ts 수정** — adClickDailyLimit 추가 (기존 adEventRatelimit 유지):
```typescript
import { Ratelimit } from '@upstash/ratelimit'
import { Redis } from '@upstash/redis'

const redis = new Redis({
  url:   process.env.KV_REST_API_URL ?? process.env.UPSTASH_REDIS_REST_URL ?? '',
  token: process.env.KV_REST_API_TOKEN ?? process.env.UPSTASH_REDIS_REST_TOKEN ?? '',
})

// 기존: IP당 분당 100회 rate limit
export const adEventRatelimit = new Ratelimit({
  redis,
  limiter:   Ratelimit.slidingWindow(100, '1 m'),
  analytics: false,
  prefix:    'danji:ad:rl',
})

// 신규 AD-03: IP별 일별 클릭 10회 초과 anomaly 감지
export const adClickDailyLimit = new Ratelimit({
  redis,
  limiter:   Ratelimit.slidingWindow(10, '24 h'),
  analytics: false,
  prefix:    'danji:ad:daily-click',
})
```

**2. src/app/api/ads/events/route.ts 수정** — 전환 이벤트 + anomaly 감지 추가:
- `['impression', 'click']` → `['impression', 'click', 'conversion']`으로 검증 확장
- import에 `adClickDailyLimit` 추가
- click 이벤트에서만 adClickDailyLimit.limit(ip_hash) 호출
- INSERT에 `is_anomaly: isAnomaly` 필드 추가
- `!withinDaily` → isAnomaly = true (10회 초과 시)
- impression, conversion은 `isAnomaly = false`로 고정

**3. src/app/api/ads/events/route.test.ts 생성** — Vitest 단위 테스트:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Upstash Ratelimit mock
vi.mock('@/lib/ratelimit', () => ({
  adEventRatelimit: { limit: vi.fn().mockResolvedValue({ success: true, reset: 0 }) },
  adClickDailyLimit: { limit: vi.fn().mockResolvedValue({ success: true, reset: 0 }) },
}))

// Supabase admin client mock
vi.mock('@/lib/supabase/admin', () => ({
  createSupabaseAdminClient: () => ({
    from: () => ({ insert: vi.fn().mockResolvedValue({ error: null }) }),
  }),
}))

describe('POST /api/ads/events', () => {
  it('accepts conversion event_type', async () => { ... })
  it('rejects unknown event_type with 400', async () => { ... })
  it('sets is_anomaly=true when daily limit exceeded for click', async () => { ... })
  it('does NOT check daily limit for impression events', async () => { ... })
})
```
테스트에서 라우트 핸들러를 직접 import하여 `Request` 객체로 호출.
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose route.test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - route.test.ts 4개 케이스 모두 GREEN
    - adClickDailyLimit이 ratelimit.ts에 export됨
    - events/route.ts가 'conversion' 허용 + is_anomaly 필드 INSERT
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ROI 집계 함수 + SGIS 어댑터 + 갭 라벨 쿼리</name>
  <files>
    src/lib/data/ads.ts
    src/services/sgis.ts
    src/lib/data/gap-label.ts
    src/lib/format.ts
    src/lib/data/gap-label.test.ts
  </files>
  <behavior>
    - getAdRoiStats(): 캠페인별 impressions/clicks/conversions/ctr/anomaly 집계 반환
    - ctr = null when clicks === 0 (divide-by-zero 방지)
    - anomaly = events?.some(e => e.is_anomaly) ?? false
    - getGapLabelData(): listingPricePerPy와 avgTransactionPricePerPy 반환
    - avgTransactionPricePerPy: price / (area_m2 / 3.3058) 평균 (최근 12개월, sale, cancel_date IS NULL AND superseded_by IS NULL)
    - 데이터 없으면 null 반환 (gap === null → 컴포넌트 미렌더)
    - formatGap(gap): gap >= 10000만원 → "1억 {n}만원" 형식, 그 외 "{n}만원"
  </behavior>
  <action>
**1. src/lib/format.ts 생성** (또는 기존 파일에 추가):
- `formatPrice(priceWan: number): string` — 기존 page.tsx의 formatPrice 로직 이동
- `formatGap(gapWan: number): string` — 차액 포맷 (만원 단위 정수)
  - `Math.abs(gapWan) >= 10000` → "N억 M만원" (M=0이면 "N억" 만)
  - 그 외 → "N만원"
  - 입력은 반올림된 정수 만원

**2. src/lib/data/gap-label.ts 생성**:
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export interface GapLabelData {
  listingPricePerPy:      number | null
  avgTransactionPricePerPy: number | null
}

export async function getGapLabelData(
  complexId: string,
  supabase: SupabaseClient<Database>,
): Promise<GapLabelData> {
  const twelveMonthsAgo = new Date(Date.now() - 365 * 24 * 60 * 60 * 1000)
    .toISOString().slice(0, 10)

  const [listingResult, transactionResult] = await Promise.all([
    supabase
      .from('listing_prices')
      .select('price_per_py')
      .eq('complex_id', complexId)
      .order('recorded_date', { ascending: false })
      .limit(1)
      .maybeSingle(),

    supabase
      .from('transactions')
      .select('price, area_m2')
      .eq('complex_id', complexId)
      .eq('deal_type', 'sale')
      .is('cancel_date', null)
      .is('superseded_by', null)
      .not('price', 'is', null)
      .not('area_m2', 'is', null)
      .gte('deal_date', twelveMonthsAgo),
  ])

  const listingPricePerPy = listingResult.data?.price_per_py ?? null

  const rows = transactionResult.data ?? []
  const avgTransactionPricePerPy =
    rows.length > 0
      ? Math.round(
          rows.reduce((sum, r) => {
            const py = Number(r.area_m2) / 3.3058
            return sum + (Number(r.price) / py)
          }, 0) / rows.length,
        )
      : null

  return { listingPricePerPy, avgTransactionPricePerPy }
}
```

**3. src/lib/data/gap-label.test.ts 생성** — Vitest:
```typescript
describe('getGapLabelData', () => {
  it('returns null for both when no data', ...)
  it('calculates avg price_per_py correctly from price + area_m2', ...)
  it('returns null avgTransactionPricePerPy when no transactions', ...)
})

describe('formatGap', () => {
  it('formats 500 → "500만원"', ...)
  it('formats 10000 → "1억"', ...)
  it('formats 10500 → "1억 500만원"', ...)
  it('formats 20000 → "2억"', ...)
})
```

**4. src/lib/data/ads.ts 생성** — ROI 집계:
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export interface AdRoiRow {
  campaignId:  string
  title:       string
  impressions: number
  clicks:      number
  conversions: number
  ctr:         number | null  // null when clicks === 0
  anomaly:     boolean
}

export async function getAdRoiStats(
  adminClient: SupabaseClient<Database>,
): Promise<AdRoiRow[]> {
  // 단일 SQL 집계 쿼리 사용 (N+1 방지)
  // Supabase는 raw SQL RPC를 사용하거나, 캠페인별 루프 (MVP 규모 허용)
  const { data: campaigns } = await adminClient
    .from('ad_campaigns')
    .select('id, title')
    .order('created_at', { ascending: false })

  if (!campaigns || campaigns.length === 0) return []

  const roiRows: AdRoiRow[] = []
  for (const c of campaigns) {
    const { data: events } = await adminClient
      .from('ad_events')
      .select('event_type, is_anomaly')
      .eq('campaign_id', c.id)

    const ev = events ?? []
    const impressions = ev.filter(e => e.event_type === 'impression').length
    const clicks      = ev.filter(e => e.event_type === 'click').length
    const conversions = ev.filter(e => e.event_type === 'conversion').length
    const anomaly     = ev.some(e => e.is_anomaly)
    const ctr         = clicks > 0 ? (conversions / clicks) * 100 : null

    roiRows.push({ campaignId: c.id, title: c.title, impressions, clicks, conversions, ctr, anomaly })
  }
  return roiRows
}
```

**5. src/services/sgis.ts 생성** — molit.ts 패턴 준수:
```typescript
import { z } from 'zod/v4'

const BASE = 'https://sgisapi.kostat.go.kr/OpenAPI3'

const TokenResponseSchema = z.object({
  result: z.object({
    accessToken:   z.string(),
    accessTimeout: z.number(),
  }),
  errMsg: z.string(),
  errCd:  z.number(),
})

export async function fetchSgisToken(): Promise<string> {
  const key    = process.env.SGIS_CONSUMER_KEY
  const secret = process.env.SGIS_CONSUMER_SECRET
  if (!key || !secret) throw new Error('SGIS credentials not set')
  const url = new URL(`${BASE}/auth/authentication.json`)
  url.searchParams.set('consumer_key',    key)
  url.searchParams.set('consumer_secret', secret)
  const res  = await fetch(url.toString(), { signal: AbortSignal.timeout(10_000) })
  const json = TokenResponseSchema.parse(await res.json())
  if (json.errCd !== 0) throw new Error(`SGIS auth error: ${json.errMsg}`)
  return json.result.accessToken
}

const PopulationItemSchema = z.object({
  adm_cd:     z.string(),
  adm_nm:     z.string(),
  population: z.coerce.number(),
})

export async function fetchPopulation(
  accessToken: string,
  adm_cd: string,
  year: number,
): Promise<{ population: number; adm_nm: string }> {
  const url = new URL(`${BASE}/stats/searchpopulation.json`)
  url.searchParams.set('accessToken', accessToken)
  url.searchParams.set('year',        String(year))
  url.searchParams.set('adm_cd',      adm_cd)
  const res  = await fetch(url.toString(), { signal: AbortSignal.timeout(10_000) })
  const json = await res.json() as { result?: unknown[]; errMsg?: string; errCd?: number }
  if ((json.errCd ?? -1) !== 0) throw new Error(`SGIS population error: ${json.errMsg}`)
  const item = PopulationItemSchema.parse((json.result ?? [])[0])
  return { population: item.population, adm_nm: item.adm_nm }
}

const HouseholdItemSchema = z.object({
  adm_cd:        z.string(),
  adm_nm:        z.string(),
  household_cnt: z.coerce.number(),
})

export async function fetchHouseholds(
  accessToken: string,
  adm_cd: string,
  year: number,
): Promise<{ households: number }> {
  const url = new URL(`${BASE}/stats/household.json`)
  url.searchParams.set('accessToken', accessToken)
  url.searchParams.set('year',        String(year))
  url.searchParams.set('adm_cd',      adm_cd)
  const res  = await fetch(url.toString(), { signal: AbortSignal.timeout(10_000) })
  const json = await res.json() as { result?: unknown[]; errMsg?: string; errCd?: number }
  if ((json.errCd ?? -1) !== 0) throw new Error(`SGIS household error: ${json.errMsg}`)
  const item = HouseholdItemSchema.parse((json.result ?? [])[0])
  return { households: item.household_cnt }
}

// 창원·김해 시군구 ADM_CD 코드 (SGIS stage API로 검증 필요 — ASSUMED)
// 검증 방법: GET ${BASE}/addr/stage.json?accessToken=...&cd=48&pg_yn=1
export const CHANGWON_GU_CODES: Record<string, string> = {
  '의창구': '48121',
  '성산구': '48123',
  '마산합포구': '48125',
  '마산회원구': '48127',
  '진해구':   '48129',
}
export const GIMHAE_CODE = '48250'
```
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose gap-label.test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - gap-label.test.ts 모든 케이스 GREEN
    - formatGap 포맷팅 테스트 GREEN
    - src/services/sgis.ts TypeScript 컴파일 오류 없음 (`npm run lint`)
    - src/lib/data/ads.ts TypeScript 컴파일 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → /api/ads/events | 광고 이벤트 POST. IP 위조 가능. 이벤트 타입 조작 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Spoofing | /api/ads/events IP 추출 | mitigate | x-forwarded-for 첫 번째 IP + sha256 hash. PII 비저장. Rate limit으로 대량 클릭 방어 |
| T-06-01-02 | Tampering | event_type 입력 | mitigate | `['impression','click','conversion'].includes()` 검증. 미허용 값 400 반환 |
| T-06-01-03 | Denial of Service | Upstash Redis 두 번째 limiter | accept | 무료 10,000 req/day. analytics=false로 절약. 두 limiter 합산 소진 위험 — 모니터링 |
| T-06-01-04 | Information Disclosure | SGIS API key 환경변수 | mitigate | 서버 사이드 전용 (SGIS_CONSUMER_KEY prefix 없음 → 클라이언트 번들 미포함). .env.local에만 저장 |
</threat_model>

<verification>
```bash
npm run test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|✓|✗)"
npm run lint 2>&1 | tail -5
```
</verification>

<success_criteria>
- gap-label.test.ts: 모든 테스트 GREEN
- route.test.ts: 모든 테스트 GREEN
- `npm run lint` 통과 (TypeScript 오류 없음)
- src/services/sgis.ts에 fetchSgisToken, fetchPopulation, fetchHouseholds export됨
- src/lib/data/ads.ts에 getAdRoiStats, AdRoiRow export됨
- src/lib/format.ts에 formatPrice, formatGap export됨
</success_criteria>

<output>
완료 후 `.planning/phases/06-ai-differentiation/06-01-SUMMARY.md` 생성:
- 수정/생성된 파일 목록
- 테스트 결과 요약
- SGIS adm_cd 코드 검증 상태 (ASSUMED 표시)
</output>

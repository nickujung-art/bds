---
phase: 06-ai-differentiation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260508000001_district_stats.sql
  - supabase/migrations/20260508000002_ad_events_conversion.sql
  - supabase/migrations/20260508000003_pgvector.sql
  - supabase/migrations/20260508000004_gps_auth.sql
  - package.json
  - .env.local.example
autonomous: false
requirements:
  - DIFF-03
  - DATA-06
  - AD-01
  - AUTH-01

must_haves:
  truths:
    - "모든 Phase 6 마이그레이션이 로컬 Supabase에 적용된다"
    - "pgvector extension이 활성화되고 complex_embeddings 테이블이 1024차원 벡터 컬럼을 가진다"
    - "@anthropic-ai/sdk 패키지가 package.json에 추가된다"
    - "gps_visits, gps_verification_requests 테이블과 profiles.gps_badge_level 컬럼이 생성된다"
    - "district_stats 테이블이 si+gu 인덱스와 RLS 정책을 갖는다"
  artifacts:
    - path: "supabase/migrations/20260508000001_district_stats.sql"
      provides: "district_stats 테이블 + RLS + 인덱스"
      contains: "district_stats_si_gu_idx"
    - path: "supabase/migrations/20260508000002_ad_events_conversion.sql"
      provides: "ad_events.event_type에 conversion 추가 + is_anomaly 컬럼"
      contains: "is_anomaly"
    - path: "supabase/migrations/20260508000003_pgvector.sql"
      provides: "complex_embeddings 테이블 + match_complex_embeddings RPC"
      contains: "vector(1024)"
    - path: "supabase/migrations/20260508000004_gps_auth.sql"
      provides: "gps_visits + gps_verification_requests + profiles.gps_badge_level"
      contains: "gps_badge_level"
  key_links:
    - from: "complex_embeddings.embedding"
      to: "match_complex_embeddings RPC"
      via: "extensions.vector(1024) <=> operator"
      pattern: "vector_cosine_ops"
    - from: "ad_events.is_anomaly"
      to: "AD-01 anomaly detection"
      via: "boolean flag set by route handler"
      pattern: "is_anomaly"
---

<objective>
Wave 0: DB 마이그레이션 + 패키지 설치 + 환경 변수 등록

Phase 6 전체 기능의 데이터베이스 기반을 구축한다. 4개 마이그레이션 파일 작성, `npm db:push` 적용, `@anthropic-ai/sdk` 패키지 설치, `.env.local.example` 환경 변수 등록.

Purpose: Wave 1–4 플랜이 모두 이 Wave 0 완료에 의존한다. 마이그레이션 실패 시 모든 후속 작업이 블로킹된다.
Output: 4개 SQL 마이그레이션 파일, 업데이트된 package.json, 업데이트된 .env.local.example
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-differentiation/06-CONTEXT.md
@.planning/phases/06-ai-differentiation/RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- 기존 테이블 참조 -->

From supabase/migrations/20260430000007_ads.sql:
```sql
-- ad_events 현재 CHECK constraint:
check (event_type in ('impression', 'click'))
-- Phase 6에서 'conversion' 추가 필요
```

From supabase/migrations/20260430000003_transactions.sql:
```sql
-- transactions 테이블에는 price_per_py 컬럼이 없음
-- price (bigint, 만원), area_m2 (numeric(6,2)) 만 존재
-- 갭 라벨 쿼리에서 (price / (area_m2 / 3.3058)) 계산 필요
```

From src/lib/ratelimit.ts:
```typescript
// 기존 Upstash Redis 싱글턴 패턴
export const adEventRatelimit = new Ratelimit({
  redis: new Redis({
    url:   process.env.KV_REST_API_URL ?? process.env.UPSTASH_REDIS_REST_URL ?? '',
    token: process.env.KV_REST_API_TOKEN ?? process.env.UPSTASH_REDIS_REST_TOKEN ?? '',
  }),
  limiter: Ratelimit.slidingWindow(100, '1 m'),
  analytics: false,
  prefix: 'danji:ad:rl',
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL 마이그레이션 4개 파일 작성</name>
  <files>
    supabase/migrations/20260508000001_district_stats.sql
    supabase/migrations/20260508000002_ad_events_conversion.sql
    supabase/migrations/20260508000003_pgvector.sql
    supabase/migrations/20260508000004_gps_auth.sql
  </files>
  <action>
아래 4개 파일을 정확히 작성한다.

**20260508000001_district_stats.sql** — SGIS 지역 통계 테이블:
```sql
-- Phase 6 DATA-06: SGIS 시군구 인구·세대 통계
create table public.district_stats (
  id           uuid primary key default gen_random_uuid(),
  adm_cd       text not null,
  adm_nm       text not null,
  si           text not null,
  gu           text not null,
  data_year    smallint not null,
  data_quarter smallint not null check (data_quarter between 1 and 4),
  population   integer,
  households   integer,
  fetched_at   timestamptz not null default now(),
  unique (adm_cd, data_year, data_quarter)
);

create index district_stats_si_gu_idx on public.district_stats(si, gu);
create index district_stats_year_quarter_idx
  on public.district_stats(data_year desc, data_quarter desc);

alter table public.district_stats enable row level security;

create policy "district_stats: public read"
  on public.district_stats for select using (true);

create policy "district_stats: service role write"
  on public.district_stats for all
  using (auth.role() = 'service_role')
  with check (auth.role() = 'service_role');
```

**20260508000002_ad_events_conversion.sql** — 전환 이벤트 + 이상 트래픽 플래그:
```sql
-- Phase 6 AD-01: 전환 이벤트 추가 + 이상 트래픽 플래그
alter table public.ad_events
  drop constraint if exists ad_events_event_type_check;

alter table public.ad_events
  add constraint ad_events_event_type_check
    check (event_type in ('impression', 'click', 'conversion'));

alter table public.ad_events
  add column if not exists is_anomaly boolean not null default false;

create index if not exists ad_events_anomaly_idx
  on public.ad_events(campaign_id, is_anomaly)
  where is_anomaly = true;
```

**20260508000003_pgvector.sql** — RAG 봇 벡터 테이블:
```sql
-- Phase 6 DIFF-03: pgvector RAG 임베딩 (voyage-4-lite 1024 dim)
create extension if not exists vector with schema extensions;

create table public.complex_embeddings (
  id          uuid primary key default gen_random_uuid(),
  complex_id  uuid not null references public.complexes(id) on delete cascade,
  content     text not null,
  chunk_type  text not null
    check (chunk_type in ('summary', 'transactions', 'reviews')),
  embedding   extensions.vector(1024),
  updated_at  timestamptz not null default now(),
  unique (complex_id, chunk_type)
);

create index complex_embeddings_hnsw_idx
  on public.complex_embeddings
  using hnsw (embedding extensions.vector_cosine_ops);

alter table public.complex_embeddings enable row level security;

create policy "complex_embeddings: public read"
  on public.complex_embeddings for select using (true);

create policy "complex_embeddings: service role write"
  on public.complex_embeddings for all
  using (auth.role() = 'service_role')
  with check (auth.role() = 'service_role');

create or replace function match_complex_embeddings(
  query_embedding extensions.vector(1024),
  target_complex_id uuid,
  match_count int default 3
)
returns table (
  chunk_type text,
  content    text,
  similarity float
)
language sql stable
as $$
  select
    chunk_type,
    content,
    1 - (embedding <=> query_embedding) as similarity
  from public.complex_embeddings
  where complex_id = target_complex_id
  order by embedding <=> query_embedding
  limit match_count;
$$;
```

**20260508000004_gps_auth.sql** — GPS L2/L3 인증 테이블:
```sql
-- Phase 6 AUTH-01: GPS L2 방문 기록 + L3 서류 인증 + 배지 레벨
create table public.gps_visits (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references public.profiles(id) on delete cascade,
  complex_id  uuid not null references public.complexes(id) on delete cascade,
  verified_at timestamptz not null default now(),
  lat         double precision,
  lng         double precision
);

create index gps_visits_user_complex_idx
  on public.gps_visits(user_id, complex_id, verified_at desc);

alter table public.gps_visits enable row level security;

create policy "gps_visits: owner read"
  on public.gps_visits for select
  using (user_id = auth.uid());

create policy "gps_visits: auth insert"
  on public.gps_visits for insert
  with check (user_id = auth.uid());

create table public.gps_verification_requests (
  id           uuid primary key default gen_random_uuid(),
  user_id      uuid not null references public.profiles(id) on delete cascade,
  complex_id   uuid not null references public.complexes(id) on delete cascade,
  doc_type     text not null check (doc_type in ('등본', '관리비')),
  storage_path text not null,
  status       text not null default 'pending'
    check (status in ('pending', 'approved', 'rejected')),
  reviewed_by  uuid references public.profiles(id),
  reviewed_at  timestamptz,
  created_at   timestamptz not null default now()
);

alter table public.gps_verification_requests enable row level security;

create policy "gps_req: owner read"
  on public.gps_verification_requests for select
  using (user_id = auth.uid());

create policy "gps_req: auth insert"
  on public.gps_verification_requests for insert
  with check (user_id = auth.uid());

create policy "gps_req: admin all"
  on public.gps_verification_requests for all
  using (
    exists (
      select 1 from public.profiles
      where id = auth.uid() and role in ('admin', 'superadmin')
    )
  );

alter table public.profiles
  add column if not exists gps_badge_level smallint not null default 0
    check (gps_badge_level between 0 and 3);
-- 0=없음, 1=방문인증, 2=거주인증, 3=소유자인증

-- Supabase Storage private 버킷 (gps-docs)
insert into storage.buckets (id, name, public)
values ('gps-docs', 'gps-docs', false)
on conflict (id) do nothing;

create policy "gps-docs: owner upload"
  on storage.objects for insert
  with check (
    bucket_id = 'gps-docs'
    and auth.uid()::text = (storage.foldername(name))[1]
  );

create policy "gps-docs: admin read"
  on storage.objects for select
  using (
    bucket_id = 'gps-docs'
    and exists (
      select 1 from public.profiles
      where id = auth.uid() and role in ('admin', 'superadmin')
    )
  );
```
  </action>
  <verify>
    <automated>ls supabase/migrations/20260508000001_district_stats.sql supabase/migrations/20260508000002_ad_events_conversion.sql supabase/migrations/20260508000003_pgvector.sql supabase/migrations/20260508000004_gps_auth.sql</automated>
  </verify>
  <done>4개 SQL 파일이 존재하고, 각각 RLS 정책과 인덱스를 포함한다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 마이그레이션 적용 + 패키지 설치 + 환경 변수 등록</name>
  <what-built>Task 1에서 4개 마이그레이션 파일 생성 완료</what-built>
  <how-to-verify>
순서대로 실행:

1. **로컬 Supabase에 마이그레이션 적용:**
   ```bash
   npm run db:push
   ```
   오류 없이 완료되는지 확인. pgvector extension이 이미 있으면 `create extension if not exists` 구문으로 안전하게 넘어감.

2. **@anthropic-ai/sdk 설치:**
   ```bash
   npm install @anthropic-ai/sdk
   ```
   package.json에 `"@anthropic-ai/sdk"` 항목이 추가되었는지 확인.

3. **.env.local에 아래 환경 변수 추가** (아직 없으면):
   ```
   ANTHROPIC_API_KEY=sk-ant-...          # Anthropic 콘솔에서 발급
   VOYAGE_API_KEY=pa-...                  # voyageai.com 가입 후 발급
   SGIS_CONSUMER_KEY=...                  # sgis.kostat.go.kr 개발자 센터
   SGIS_CONSUMER_SECRET=...               # sgis.kostat.go.kr 개발자 센터
   ```

4. **.env.local.example에 동일 키 (빈 값으로) 추가:**
   ```
   ANTHROPIC_API_KEY=
   VOYAGE_API_KEY=
   SGIS_CONSUMER_KEY=
   SGIS_CONSUMER_SECRET=
   ```

5. **마이그레이션 확인:**
   ```bash
   npm run db:start  # 이미 실행 중이면 생략
   ```
   Supabase Studio (localhost:54323) → Table Editor에서 다음 테이블 확인:
   - `district_stats` (si, gu 컬럼)
   - `complex_embeddings` (embedding 컬럼)
   - `gps_visits`, `gps_verification_requests`
   - `profiles` 테이블에 `gps_badge_level` 컬럼

6. **빌드 확인:**
   ```bash
   npm run lint && npm run build
   ```
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발생한 오류 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Supabase | SUPABASE_SERVICE_ROLE_KEY로 직접 write. 시크릿 노출 시 전체 DB write 권한 탈취 |
| Storage bucket gps-docs | private 버킷이지만 Storage policy 오류 시 파일 노출 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Elevation of Privilege | gps_verification_requests admin policy | mitigate | `exists (select 1 from profiles where role in ('admin','superadmin'))` — 직접 role 확인, auth.uid() 검증 포함 |
| T-06-00-02 | Tampering | storage.objects gps-docs upload policy | mitigate | `(storage.foldername(name))[1] = auth.uid()::text` — 본인 폴더에만 업로드 허용 |
| T-06-00-03 | Information Disclosure | complex_embeddings public read | accept | 임베딩 벡터는 역변환 불가. 원문 content 텍스트는 단지 공개 정보. PII 없음 |
| T-06-00-04 | Denial of Service | pgvector HNSW 인덱스 빌드 | accept | 9000 vectors 규모에서 수초 이내. 프로덕션 초기 규모에서 문제 없음 |
</threat_model>

<verification>
```bash
# 마이그레이션 파일 존재 확인
ls supabase/migrations/20260508*.sql | wc -l
# 출력: 4

# pgvector 확인 (db push 후)
# Supabase Studio → Database → Extensions → vector (enabled)

# 패키지 확인
node -e "require('@anthropic-ai/sdk'); console.log('ok')"
```
</verification>

<success_criteria>
- `supabase/migrations/` 에 20260508000001~4 파일 4개 존재
- `npm run db:push` 오류 없이 완료
- `node_modules/@anthropic-ai/sdk` 존재
- `.env.local.example`에 4개 신규 환경변수 키 포함
- `npm run lint && npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/06-ai-differentiation/06-00-SUMMARY.md` 생성:
- 적용된 마이그레이션 목록
- 설치된 패키지
- 추가된 환경변수 키 목록
- 발생한 이슈 (있으면)
</output>

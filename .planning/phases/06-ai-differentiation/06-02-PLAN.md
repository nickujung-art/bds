---
phase: 06-ai-differentiation
plan: "02"
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - src/app/api/chat/complex/route.ts
  - src/app/api/admin/ad-copy-review/route.ts
  - scripts/embed-complexes.ts
  - scripts/ingest-sgis.ts
  - .github/workflows/sgis-stats.yml
  - src/app/api/chat/complex/route.test.ts
  - src/app/api/admin/ad-copy-review/route.test.ts
autonomous: true
requirements:
  - DIFF-03
  - DATA-06
  - AD-02

must_haves:
  truths:
    - "POST /api/chat/complex가 SSE 스트리밍 응답을 반환한다"
    - "채팅 라우트가 Voyage AI로 쿼리 임베딩 후 match_complex_embeddings RPC로 검색한다"
    - "POST /api/admin/ad-copy-review가 관리자만 접근 가능하고 { violations, suggestions } JSON을 반환한다"
    - "Claude API 실패 시 광고 등록이 차단되지 않고 error:true 플래그를 반환한다"
    - "embed-complexes.ts 스크립트가 complexes + transactions + reviews를 청크로 임베딩한다"
    - "ingest-sgis.ts 스크립트가 6개 지역을 순회하며 district_stats에 upsert한다"
    - "GitHub Actions sgis-stats.yml이 분기 cron으로 실행된다"
  artifacts:
    - path: "src/app/api/chat/complex/route.ts"
      provides: "RAG 채팅 스트리밍 API"
      exports: ["POST"]
      contains: "voyage-4-lite"
    - path: "src/app/api/admin/ad-copy-review/route.ts"
      provides: "광고 카피 AI 검토 API (관리자 전용)"
      exports: ["POST"]
      contains: "claude-haiku-4-5-20251001"
    - path: "scripts/embed-complexes.ts"
      provides: "단지 임베딩 배치 스크립트"
      contains: "voyage-4-lite"
    - path: "scripts/ingest-sgis.ts"
      provides: "SGIS 통계 적재 배치 스크립트"
      contains: "district_stats"
    - path: ".github/workflows/sgis-stats.yml"
      provides: "분기 SGIS 적재 GitHub Actions"
      contains: "0 3 15 1,4,7,10"
  key_links:
    - from: "src/app/api/chat/complex/route.ts"
      to: "match_complex_embeddings RPC (Supabase)"
      via: "createSupabaseAdminClient().rpc()"
      pattern: "match_complex_embeddings"
    - from: "scripts/embed-complexes.ts"
      to: "complex_embeddings table"
      via: "Voyage AI → pgvector upsert"
      pattern: "complex_embeddings"
---

<objective>
Wave 1 (병렬): RAG 백엔드 + AD-02 AI 카피 검토 + 배치 스크립트

06-01과 같은 Wave 1에서 독립 실행 (파일 충돌 없음). Claude API + Voyage AI를 사용하는 서버 레이어 구현.

Purpose: Wave 3 프론트엔드 AI 채팅 패널이 /api/chat/complex를 호출한다. Wave 4 어드민 UI가 /api/admin/ad-copy-review를 호출한다.
Output: 채팅 API route, 카피 검토 API route, 임베딩 스크립트, SGIS 적재 스크립트, GitHub Actions workflow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ai-differentiation/06-CONTEXT.md
@.planning/phases/06-ai-differentiation/RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- 기존 패턴 참조 -->

From src/app/api/ads/events/route.ts (admin client pattern):
```typescript
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
export const runtime = 'nodejs'
```

From admin auth guard pattern (existing admin pages):
```typescript
const supabase = await createSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
const { data: profile } = await supabase
  .from('profiles').select('role').eq('id', user.id).single()
if (!['admin', 'superadmin'].includes((profile as { role: string })?.role ?? '')) {
  return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
}
```

Voyage AI HTTP API:
```
POST https://api.voyageai.com/v1/embeddings
Headers: Authorization: Bearer {VOYAGE_API_KEY}, Content-Type: application/json
Body: { input: string[], model: 'voyage-4-lite', input_type: 'document'|'query' }
Response: { data: Array<{ embedding: number[] }> }
```

Anthropic SDK (설치됨 after Wave 0):
```typescript
import Anthropic from '@anthropic-ai/sdk'
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
// 채팅 스트리밍:
const stream = client.messages.stream({ model, max_tokens, system, messages })
return new Response(stream.toReadableStream(), { headers: { 'Content-Type': 'text/event-stream' } })
// 단순 응답:
const msg = await client.messages.create({ model, max_tokens, messages })
```

match_complex_embeddings RPC (from migration 06-00):
```sql
-- 인자: query_embedding vector(1024), target_complex_id uuid, match_count int
-- 반환: chunk_type text, content text, similarity float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RAG 채팅 API + AD-02 카피 검토 API</name>
  <files>
    src/app/api/chat/complex/route.ts
    src/app/api/admin/ad-copy-review/route.ts
    src/app/api/chat/complex/route.test.ts
    src/app/api/admin/ad-copy-review/route.test.ts
  </files>
  <behavior>
    - POST /api/chat/complex: complexId 없으면 400. SSE 스트림 반환. Voyage AI → pgvector → Claude 파이프라인.
    - /api/chat/complex: 인증 불필요 (단지 정보는 공개). Rate limit으로 남용 방어.
    - POST /api/admin/ad-copy-review: 비관리자 → 403. copy 없으면 400. Claude API 성공 → { violations, suggestions }. Claude 실패 → { violations: [], suggestions: [], error: true } (status 200).
    - AD-02 응답: JSON 파싱 실패 시 error:true. 마크다운 코드 블록 제거 후 파싱 시도.
  </behavior>
  <action>
**1. src/app/api/chat/complex/route.ts 생성:**
```typescript
import Anthropic from '@anthropic-ai/sdk'
import { NextResponse } from 'next/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'

export const runtime = 'nodejs'

async function embedQuery(text: string): Promise<number[]> {
  const res = await fetch('https://api.voyageai.com/v1/embeddings', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.VOYAGE_API_KEY}`,
    },
    body: JSON.stringify({ input: [text], model: 'voyage-4-lite', input_type: 'query' }),
    signal: AbortSignal.timeout(8_000),
  })
  const json = await res.json() as { data?: Array<{ embedding: number[] }> }
  return json.data?.[0]?.embedding ?? []
}

export async function POST(request: Request): Promise<Response> {
  let body: unknown
  try { body = await request.json() } catch {
    return NextResponse.json({ error: 'invalid body' }, { status: 400 })
  }

  const b = body as Record<string, unknown>
  const complexId = typeof b.complexId === 'string' ? b.complexId : null
  const messages  = Array.isArray(b.messages) ? b.messages as Array<{ role: string; content: string }> : null

  if (!complexId || !messages) {
    return NextResponse.json({ error: 'complexId and messages required' }, { status: 400 })
  }

  // 마지막 user 메시지 추출
  const lastUserMsg = [...messages].reverse().find(m => m.role === 'user')?.content ?? ''
  if (!lastUserMsg) {
    return NextResponse.json({ error: 'no user message' }, { status: 400 })
  }

  // Voyage AI 임베딩
  const queryEmbedding = await embedQuery(lastUserMsg)

  // pgvector 유사도 검색
  const supabase = createSupabaseAdminClient()
  const { data: chunks } = await supabase.rpc('match_complex_embeddings', {
    query_embedding:   queryEmbedding,
    target_complex_id: complexId,
    match_count:       3,
  })

  const context = ((chunks ?? []) as Array<{ content: string }>)
    .map(c => c.content)
    .join('\n\n')

  // Claude API 스트리밍
  const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })
  const stream = client.messages.stream({
    model:      'claude-haiku-4-5-20251001',
    max_tokens: 512,
    system: `당신은 부동산 단지 정보 안내 도우미입니다. 반드시 아래 단지 데이터만 참조하여 답변하세요. 데이터에 없는 내용은 "해당 정보는 단지 데이터에 없습니다."라고 답하세요. 추측하거나 일반 지식으로 답하지 마세요.

[단지 데이터]
${context || '(데이터 없음)'}`,
    messages: messages.map(m => ({
      role:    m.role as 'user' | 'assistant',
      content: m.content,
    })),
  })

  return new Response(stream.toReadableStream(), {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection':   'keep-alive',
    },
  })
}
```

**2. src/app/api/admin/ad-copy-review/route.ts 생성:**
```typescript
import Anthropic from '@anthropic-ai/sdk'
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'

export const runtime = 'nodejs'

export async function POST(request: Request): Promise<NextResponse> {
  // Admin auth guard (기존 admin 페이지 패턴 동일)
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { data: profile } = await supabase
    .from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  let body: unknown
  try { body = await request.json() } catch {
    return NextResponse.json({ error: 'invalid body' }, { status: 400 })
  }

  const copy = typeof (body as Record<string, unknown>)?.copy === 'string'
    ? ((body as Record<string, unknown>).copy as string).trim()
    : ''
  if (!copy) return NextResponse.json({ error: 'copy required' }, { status: 400 })
  // D-10: 500자 제한
  if (copy.length > 500) return NextResponse.json({ error: 'copy too long' }, { status: 400 })

  const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

  try {
    const message = await client.messages.create({
      model:      'claude-haiku-4-5-20251001',
      max_tokens: 512,
      system: `당신은 한국 표시광고법 전문가입니다. 부동산 광고 카피를 검토하여 다음을 찾아냅니다:
1. 표시광고법 위반 가능 표현 (최저가 보장, 100% 확실, 투자 원금 보장 등 과장·허위 표현)
2. 과장 표현, 근거 없는 수익률 주장
응답은 반드시 JSON 형식: {"violations": [...], "suggestions": [...]}
위반·제안이 없으면 빈 배열 반환. 절대 JSON 외의 텍스트 포함 금지.`,
      messages: [{
        role: 'user',
        content: `다음 부동산 광고 카피를 검토해주세요:\n\n"${copy}"\n\n반드시 JSON만 반환하세요 (설명 금지):\n{"violations": ["위반 표현1", ...], "suggestions": ["개선 제안1", ...]}`,
      }],
    })

    const rawText = message.content[0]?.type === 'text' ? message.content[0].text : '{}'
    const jsonText = rawText.replace(/```json?\n?/g, '').replace(/```/g, '').trim()
    const result = JSON.parse(jsonText) as { violations?: string[]; suggestions?: string[] }

    return NextResponse.json({
      violations:  Array.isArray(result.violations)  ? result.violations  : [],
      suggestions: Array.isArray(result.suggestions) ? result.suggestions : [],
    })
  } catch (err) {
    // D-10: 실패 시 등록 차단 안 함 — error 플래그만
    console.error('Claude API error:', err)
    return NextResponse.json(
      { violations: [], suggestions: [], error: true },
      { status: 200 },
    )
  }
}
```

**3. 테스트 파일 2개 생성** (Vitest, mock 사용):

`src/app/api/chat/complex/route.test.ts`:
```typescript
vi.mock('@anthropic-ai/sdk', () => ({
  default: vi.fn().mockImplementation(() => ({
    messages: {
      stream: vi.fn().mockReturnValue({ toReadableStream: () => new ReadableStream() }),
    },
  })),
}))
vi.mock('@/lib/supabase/admin', () => ({
  createSupabaseAdminClient: () => ({
    rpc: vi.fn().mockResolvedValue({ data: [{ content: '테스트 단지 데이터', chunk_type: 'summary' }] }),
  }),
}))
// global.fetch mock for Voyage AI
// 테스트: 400 (complexId 없음), 400 (messages 없음), 200 streaming response
```

`src/app/api/admin/ad-copy-review/route.test.ts`:
```typescript
// 테스트: 401 (비인증), 403 (비관리자), 400 (copy 없음), 200 (violations 반환), 200 error:true (Claude 실패)
```
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose route.test 2>&1 | grep -E "(PASS|FAIL)" | tail -10</automated>
  </verify>
  <done>
    - /api/chat/complex/route.ts TypeScript 컴파일 오류 없음
    - /api/admin/ad-copy-review/route.ts TypeScript 컴파일 오류 없음
    - 두 테스트 파일의 모든 케이스 GREEN
    - Claude 실패 시 error:true + status 200 확인
  </done>
</task>

<task type="auto">
  <name>Task 2: 임베딩 스크립트 + SGIS 적재 스크립트 + GitHub Actions</name>
  <files>
    scripts/embed-complexes.ts
    scripts/ingest-sgis.ts
    .github/workflows/sgis-stats.yml
  </files>
  <action>
**1. scripts/embed-complexes.ts 생성** — 단지 임베딩 배치:
```typescript
/**
 * 단지 임베딩 배치 스크립트 (Voyage AI voyage-4-lite → pgvector)
 * 실행: npx tsx scripts/embed-complexes.ts
 * 환경변수: VOYAGE_API_KEY, NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
 */
import { createClient } from '@supabase/supabase-js'

const VOYAGE_API_KEY  = process.env.VOYAGE_API_KEY
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!,
)

if (!VOYAGE_API_KEY) {
  console.error('VOYAGE_API_KEY not set')
  process.exit(1)
}

async function embedTexts(texts: string[]): Promise<number[][]> {
  const res = await fetch('https://api.voyageai.com/v1/embeddings', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${VOYAGE_API_KEY}`,
    },
    body: JSON.stringify({ input: texts, model: 'voyage-4-lite', input_type: 'document' }),
  })
  if (!res.ok) throw new Error(`Voyage API error: ${res.status} ${await res.text()}`)
  const json = await res.json() as { data: Array<{ embedding: number[] }> }
  return json.data.map(d => d.embedding)
}

async function buildSummaryChunk(complex: {
  id: string; name: string; si: string; gu: string; dong?: string | null;
  build_year?: number | null; total_units?: number | null;
}): Promise<string> {
  const { data: lastTx } = await supabase
    .from('transactions')
    .select('price, deal_date')
    .eq('complex_id', complex.id)
    .eq('deal_type', 'sale')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .order('deal_date', { ascending: false })
    .limit(1)
    .maybeSingle()

  const latestDeal = lastTx
    ? `최근 실거래가 ${lastTx.price?.toLocaleString('ko-KR')}만원 (${lastTx.deal_date})`
    : '실거래 데이터 없음'

  return [
    `${complex.si} ${complex.gu} ${complex.dong ?? ''} ${complex.name}.`,
    complex.build_year ? `${complex.build_year}년 준공.` : '',
    complex.total_units ? `${complex.total_units}세대.` : '',
    latestDeal,
  ].filter(Boolean).join(' ')
}

async function buildTransactionChunk(complexId: string): Promise<string> {
  const twentyFourMonthsAgo = new Date(Date.now() - 730 * 24 * 60 * 60 * 1000)
    .toISOString().slice(0, 10)
  const { data: txs } = await supabase
    .from('transactions')
    .select('price, area_m2, deal_date, deal_type')
    .eq('complex_id', complexId)
    .is('cancel_date', null)
    .is('superseded_by', null)
    .gte('deal_date', twentyFourMonthsAgo)
    .order('deal_date', { ascending: false })
    .limit(50)

  if (!txs || txs.length === 0) return '최근 24개월 거래 데이터 없음'

  const summary = txs.slice(0, 5).map(t =>
    `${t.deal_date} ${t.deal_type} ${t.area_m2}㎡ ${t.price?.toLocaleString('ko-KR')}만원`
  ).join(', ')
  return `최근 거래 ${txs.length}건. 최근 5건: ${summary}`
}

async function buildReviewChunk(complexId: string): Promise<string> {
  const { data: reviews } = await supabase
    .from('complex_reviews')
    .select('content, rating')
    .eq('complex_id', complexId)
    .order('created_at', { ascending: false })
    .limit(20)

  if (!reviews || reviews.length === 0) return '주민 후기 없음'

  const combined = reviews.map(r => r.content).join(' ').slice(0, 500)
  const avgRating = reviews.reduce((s, r) => s + (r.rating ?? 0), 0) / reviews.length
  return `주민 후기 ${reviews.length}건. 평균 평점 ${avgRating.toFixed(1)}. 내용: ${combined}`
}

async function main() {
  console.log('단지 임베딩 시작...')

  const { data: complexes, error } = await supabase
    .from('complexes')
    .select('id, name, si, gu, dong, build_year, total_units')
    .eq('status', 'active')
    .order('id')

  if (error || !complexes) {
    console.error('complexes 조회 실패:', error)
    process.exit(1)
  }

  console.log(`총 ${complexes.length}개 단지 임베딩`)

  // 배치 처리 (Voyage AI rate limit 고려 — 20 단지씩)
  const BATCH_SIZE = 20
  for (let i = 0; i < complexes.length; i += BATCH_SIZE) {
    const batch = complexes.slice(i, i + BATCH_SIZE)
    console.log(`진행: ${i + 1}~${Math.min(i + BATCH_SIZE, complexes.length)}/${complexes.length}`)

    for (const complex of batch) {
      const [summaryText, txText, reviewText] = await Promise.all([
        buildSummaryChunk(complex),
        buildTransactionChunk(complex.id),
        buildReviewChunk(complex.id),
      ])

      const chunks = [
        { type: 'summary',      text: summaryText },
        { type: 'transactions', text: txText },
        { type: 'reviews',      text: reviewText },
      ]

      const embeddings = await embedTexts(chunks.map(c => c.text))

      for (let j = 0; j < chunks.length; j++) {
        const chunk = chunks[j]!
        const embedding = embeddings[j]!
        await supabase.from('complex_embeddings').upsert({
          complex_id: complex.id,
          chunk_type: chunk.type,
          content:    chunk.text,
          embedding:  `[${embedding.join(',')}]`,
          updated_at: new Date().toISOString(),
        }, { onConflict: 'complex_id,chunk_type' })
      }
    }

    // rate limit 방어
    if (i + BATCH_SIZE < complexes.length) {
      await new Promise(r => setTimeout(r, 1000))
    }
  }

  console.log('임베딩 완료')
}

main().catch(err => {
  console.error(err)
  process.exit(1)
})
```

**2. scripts/ingest-sgis.ts 생성** — SGIS 통계 적재:
```typescript
/**
 * SGIS 분기 통계 적재 스크립트
 * 실행: npx tsx scripts/ingest-sgis.ts
 * 환경변수: SGIS_CONSUMER_KEY, SGIS_CONSUMER_SECRET, NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
 *
 * 주의: adm_cd 코드는 ASSUMED (48121 등). 실행 전 SGIS stage API로 검증 필요:
 *   GET https://sgisapi.kostat.go.kr/OpenAPI3/addr/stage.json?accessToken=...&cd=48&pg_yn=1
 */
import { createClient } from '@supabase/supabase-js'
import { fetchSgisToken, fetchPopulation, fetchHouseholds } from '@/services/sgis'

// tsconfig paths를 인식 못하는 경우 상대 경로 사용:
// import { fetchSgisToken, fetchPopulation, fetchHouseholds } from '../src/services/sgis'

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!,
)

// 창원 5개 구 + 김해시 (ASSUMED adm_cd — stage API로 검증 필요)
const DISTRICTS = [
  { si: '창원시', gu: '의창구',    adm_cd: '48121' },
  { si: '창원시', gu: '성산구',    adm_cd: '48123' },
  { si: '창원시', gu: '마산합포구', adm_cd: '48125' },
  { si: '창원시', gu: '마산회원구', adm_cd: '48127' },
  { si: '창원시', gu: '진해구',    adm_cd: '48129' },
  { si: '김해시', gu: '김해시',    adm_cd: '48250' },
] as const

function currentQuarter(): { year: number; quarter: number } {
  const now = new Date()
  const year = now.getFullYear()
  const quarter = Math.ceil((now.getMonth() + 1) / 3)
  // SGIS 데이터는 전년도까지만 확정. 현재 분기 데이터는 전년도 동분기 사용
  return { year: year - 1, quarter }
}

async function main() {
  console.log('SGIS 분기 통계 적재 시작...')

  const token = await fetchSgisToken()
  console.log('SGIS 토큰 발급 완료')

  const { year, quarter } = currentQuarter()
  console.log(`대상 연도: ${year}년 ${quarter}분기`)

  for (const district of DISTRICTS) {
    try {
      const [popResult, hhResult] = await Promise.all([
        fetchPopulation(token, district.adm_cd, year),
        fetchHouseholds(token, district.adm_cd, year),
      ])

      const { error } = await supabase.from('district_stats').upsert({
        adm_cd:       district.adm_cd,
        adm_nm:       popResult.adm_nm,
        si:           district.si,
        gu:           district.gu,
        data_year:    year,
        data_quarter: quarter,
        population:   popResult.population,
        households:   hhResult.households,
        fetched_at:   new Date().toISOString(),
      }, { onConflict: 'adm_cd,data_year,data_quarter' })

      if (error) {
        console.error(`${district.gu} upsert 실패:`, error)
      } else {
        console.log(`${district.gu}: 인구 ${popResult.population.toLocaleString('ko-KR')}명, 세대 ${hhResult.households.toLocaleString('ko-KR')}세대`)
      }
    } catch (err) {
      console.error(`${district.gu} 처리 실패 (스킵):`, err)
    }
  }

  console.log('SGIS 적재 완료')
}

main().catch(err => {
  console.error(err)
  process.exit(1)
})
```

**3. .github/workflows/sgis-stats.yml 생성:**
```yaml
name: SGIS 분기 통계 적재

on:
  schedule:
    - cron: '0 3 15 1,4,7,10 *'  # 1·4·7·10월 15일 03:00 UTC (분기 첫 달)
  workflow_dispatch: {}            # 수동 실행 지원

jobs:
  ingest:
    name: SGIS 인구·세대 적재
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: SGIS adm_cd 코드 검증 로그
        run: echo "주의: adm_cd 코드(48121 등)는 ASSUMED입니다. 첫 실행 시 출력 확인 필요."

      - name: Run SGIS ingestion script
        env:
          SGIS_CONSUMER_KEY:         ${{ secrets.SGIS_CONSUMER_KEY }}
          SGIS_CONSUMER_SECRET:      ${{ secrets.SGIS_CONSUMER_SECRET }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          NEXT_PUBLIC_SUPABASE_URL:  ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
        run: npx tsx scripts/ingest-sgis.ts
```

**import 경로 주의:** `scripts/ingest-sgis.ts`에서 `@/services/sgis` 경로가 tsx 실행 시 인식되지 않을 수 있음. `tsconfig.json`의 paths 설정 확인 후 필요 시 상대 경로(`../src/services/sgis`)로 변경.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>
    - scripts/embed-complexes.ts, scripts/ingest-sgis.ts 파일 존재
    - .github/workflows/sgis-stats.yml 파일 존재 (분기 cron 포함)
    - TypeScript 컴파일 오류 없음 (API route 2개)
    - `npm run lint` 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → /api/chat/complex | 공개 엔드포인트. complexId + messages 조작 가능 |
| 어드민 → /api/admin/ad-copy-review | 관리자 인증 필요. Supabase auth 의존 |
| GitHub Actions → Voyage AI / SGIS | API key 시크릿 노출 시 과금 발생 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | /api/chat/complex — prompt injection | mitigate | 시스템 프롬프트로 역할 고정. 사용자 입력은 context 외부에 위치. Claude가 시스템 지시를 override하는 시도는 무시됨 |
| T-06-02-02 | Denial of Service | /api/chat/complex — 과도한 호출 | mitigate | Vercel Hobby 10초 함수 제한. 추가 rate limit은 Wave 4에서 IP 기반으로 추가 고려 |
| T-06-02-03 | Information Disclosure | Claude 응답에서 시스템 프롬프트 노출 | mitigate | 시스템 프롬프트에 민감 정보 없음. 단지 공개 데이터만 context로 사용 |
| T-06-02-04 | Elevation of Privilege | /api/admin/ad-copy-review 권한 우회 | mitigate | `createSupabaseServerClient()` auth.getUser() + profiles.role 체크. 두 단계 검증 |
| T-06-02-05 | Tampering | SGIS script — adm_cd 코드 오류 | accept | adm_cd ASSUMED 위험 문서화. 첫 실행 전 수동 검증 필요. 오류 시 특정 지역만 실패 (전체 스킵 아님) |
</threat_model>

<verification>
```bash
# TypeScript 컴파일 확인
npm run lint 2>&1 | tail -5

# 워크플로우 파일 확인
grep "1,4,7,10" .github/workflows/sgis-stats.yml

# 스크립트 파일 확인
ls scripts/embed-complexes.ts scripts/ingest-sgis.ts
```
</verification>

<success_criteria>
- /api/chat/complex/route.ts: Voyage AI + pgvector RPC + Anthropic stream 파이프라인 완성
- /api/admin/ad-copy-review/route.ts: 관리자 auth guard + Claude API + 실패 허용 패턴
- scripts/embed-complexes.ts: 3-chunk (summary/transactions/reviews) 임베딩 배치
- scripts/ingest-sgis.ts: 6개 지역 순회 upsert
- .github/workflows/sgis-stats.yml: 분기 cron (1/4/7/10월 15일)
- `npm run lint` 통과
- 두 테스트 파일 모든 케이스 GREEN
</success_criteria>

<output>
완료 후 `.planning/phases/06-ai-differentiation/06-02-SUMMARY.md` 생성:
- 생성된 파일 목록
- Voyage AI 모델 확인 (voyage-4-lite, 1024dim)
- adm_cd 코드 검증 상태 (ASSUMED)
- 테스트 결과 요약
</output>

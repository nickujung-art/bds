---
phase: 06-ai-differentiation
plan: "04"
type: execute
wave: 3
depends_on: ["06-00", "06-03"]
files_modified:
  - src/lib/auth/gps-badge.ts
  - src/components/reviews/GpsBadge.tsx
  - src/app/admin/gps-requests/page.tsx
  - src/app/api/admin/gps-approve/route.ts
  - src/components/community/GpsVerifyL3Upload.tsx
  - tests/e2e/phase6.spec.ts
  - tests/e2e/gap-label.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - DIFF-03
  - AD-01
  - DATA-05

must_haves:
  truths:
    - "GPS L1 諛⑸Ц 湲곕줉 ??30????3???댁긽?대㈃ profiles.gps_badge_level??2濡??낃렇?덉씠?쒕맂??
    - "L3 ?쒕쪟 ?낅줈?쒓? gps-docs Storage 踰꾪궥????λ릺怨?gps_verification_requests ?뚯씠釉붿뿉 pending ?됱씠 ?앹꽦?쒕떎"
    - "?대뱶誘?/admin/gps-requests ?섏씠吏?먯꽌 L3 ?붿껌 紐⑸줉???뺤씤?섍퀬 ?뱀씤/嫄곗젅?????덈떎"
    - "由щ럭 諛곗?媛 gps_badge_level???곕씪 L1=諛⑸Ц?몄쬆(neutral), L2=嫄곗＜?몄쬆(pos), L3=?뚯쑀?먯씤利?orange)???쒖떆?쒕떎"
    - "E2E ?뚯뒪?멸? 媛??쇰꺼 ?쒖떆, AI ?곷떞 踰꾪듉, SGIS ?듦퀎 移대뱶??DOM 議댁옱瑜?寃利앺븳??
  artifacts:
    - path: "src/lib/auth/gps-badge.ts"
      provides: "GPS L1 諛⑸Ц 湲곕줉 + L2 諛곗? ?낃렇?덉씠??Server Action"
      exports: ["recordGpsVisitAndCheckL2"]
    - path: "src/components/reviews/GpsBadge.tsx"
      provides: "諛곗? ?덈꺼蹂?諛곗? 而댄룷?뚰듃 (L1/L2/L3)"
      contains: "gps_badge_level"
    - path: "src/app/admin/gps-requests/page.tsx"
      provides: "L3 ?몄쬆 ?붿껌 ?대뱶誘?寃???섏씠吏"
      contains: "gps_verification_requests"
    - path: "src/app/api/admin/gps-approve/route.ts"
      provides: "L3 ?뱀씤/嫄곗젅 API (?대뱶誘??꾩슜)"
      exports: ["POST"]
    - path: "tests/e2e/phase6.spec.ts"
      provides: "Phase 6 湲곕뒫 E2E ?뚯뒪??
      contains: "AI ?곷떞"
  key_links:
    - from: "src/lib/auth/gps-badge.ts"
      to: "gps_visits table"
      via: "createSupabaseAdminClient().from('gps_visits').insert()"
      pattern: "gps_visits"
    - from: "src/app/api/admin/gps-approve/route.ts"
      to: "profiles.gps_badge_level"
      via: "adminClient.from('profiles').update({ gps_badge_level: 3 })"
      pattern: "gps_badge_level"
---

<objective>
Wave 3: GPS L2+L3 ?몄쬆 + ?대뱶誘??뱀씤 UI + E2E ?뚯뒪??
GPS 諛⑸Ц 湲곕줉 Server Action, 諛곗? ?덈꺼 而댄룷?뚰듃, L3 ?쒕쪟 ?낅줈??UI, ?대뱶誘??뱀씤 ?섏씠吏, Phase 6 E2E ?뚯뒪?몃? 援ы쁽?쒕떎.

Purpose: AUTH-01???꾩꽦?섍퀬 Phase 6 ?꾩껜 湲곕뒫??E2E ?뚯뒪?몃줈 寃利앺븳??
Output: GPS 諛곗? Server Action, L3 ?대뱶誘?UI, E2E ?뚯뒪??2媛?</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ai-differentiation/06-CONTEXT.md
@.planning/phases/06-ai-differentiation/RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Wave 0 留덉씠洹몃젅?댁뀡?먯꽌 ?앹꽦???뚯씠釉?-->

From supabase/migrations/20260508000004_gps_auth.sql:
```sql
-- gps_visits: (user_id uuid, complex_id uuid, verified_at timestamptz, lat double precision, lng double precision)
-- gps_verification_requests: (user_id, complex_id, doc_type text('?깅낯'|'愿由щ퉬'), storage_path text, status text('pending'|'approved'|'rejected'), reviewed_by uuid, reviewed_at timestamptz)
-- profiles.gps_badge_level smallint (0=?놁쓬, 1=諛⑸Ц?몄쬆, 2=嫄곗＜?몄쬆, 3=?뚯쑀?먯씤利?
-- Storage bucket: 'gps-docs' (private)
```

From CLAUDE.md (?꾪궎?띿쿂 洹쒖튃):
```
Server Action ?곗꽑. ??submit쨌mutation? Server Action. REST API Route???몃? ?몄텧???꾩슂??寃쎌슦留?createSupabaseAdminClient() ?⑥씪 寃쎌쑀
紐⑤뱺 ?ъ슜???곗씠???뚯씠釉붿? RLS ?뺤콉 ?꾩닔
```

From 湲곗〈 review badge ?⑦꽩 (src/components/reviews/ReviewList.tsx):
```typescript
// gps_verified boolean ??Phase 6?먯꽌 gps_badge_level濡??뺤옣
// 湲곗〈: { gps_verified: true } ??"諛⑸Ц?몄쬆" badge.neutral
// ?좉퇋: gps_badge_level 0/1/2/3 湲곕컲 諛곗? ?④퀎
```

Admin auth guard pattern (湲곗〈 /admin/* ?섏씠吏):
```typescript
const supabase = await createSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect('/login?next=/admin/...')
const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
if (!['admin', 'superadmin'].includes(profile?.role ?? '')) redirect('/')
const adminClient = createSupabaseAdminClient()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GPS L2 Server Action + L3 ?낅줈??而댄룷?뚰듃 + 諛곗? 而댄룷?뚰듃 + ?대뱶誘??뱀씤 UI</name>
  <files>
    src/lib/auth/gps-badge.ts
    src/components/reviews/GpsBadge.tsx
    src/components/community/GpsVerifyL3Upload.tsx
    src/app/admin/gps-requests/page.tsx
    src/app/api/admin/gps-approve/route.ts
  </files>
  <action>
**1. src/lib/auth/gps-badge.ts ?앹꽦** ??GPS L1/L2 Server Action:

```typescript
'use server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { createSupabaseServerClient } from '@/lib/supabase/server'

interface GpsVisitResult {
  success: boolean
  newBadgeLevel: number
  error?: string
}

/**
 * GPS L1 諛⑸Ц 湲곕줉 + L2 諛곗? ?낃렇?덉씠??泥댄겕
 * ?몄텧 議곌굔: complex_reviews ?묒꽦 ??GPS ?몄쬆 踰꾪듉 ?대┃ (湲곗〈 L1 ?몄쬆 ?먮쫫)
 * ?쒕쾭?먯꽌 ?⑥? 醫뚰몴? 짹100m 嫄곕━ 寃利??꾩슂 (PostGIS)
 */
export async function recordGpsVisitAndCheckL2(
  complexId: string,
  lat: number,
  lng: number,
): Promise<GpsVisitResult> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, newBadgeLevel: 0, error: 'Unauthenticated' }

  const adminClient = createSupabaseAdminClient()

  // 1. ?⑥? 醫뚰몴 寃利?(PostGIS ST_DWithin 짹100m)
  const { data: complex } = await adminClient
    .from('complexes')
    .select('location')
    .eq('id', complexId)
    .single()

  if (!complex) return { success: false, newBadgeLevel: 0, error: 'Complex not found' }

  // PostGIS ?⑥닔 RPC濡?嫄곕━ 寃利?(湲곗〈 GPS L1 ?⑦꽩 以??
  // complex.location? geography ??? ST_DWithin?쇰줈 100m ?대궡 ?뺤씤
  // 湲곗〈 援ы쁽??PostGIS RPC媛 ?놁쑝硫?媛꾨떒???꾨룄/寃쎈룄 踰붿쐞 泥댄겕濡??泥?  // ?꾨룄 0.001????111m ??짹0.0009???ъ슜
  const complexLat = typeof complex.location === 'object' && complex.location !== null
    ? (complex.location as { lat?: number }).lat
    : null
  const complexLng = typeof complex.location === 'object' && complex.location !== null
    ? (complex.location as { lng?: number }).lng
    : null

  if (complexLat !== null && complexLng !== null) {
    const latDiff = Math.abs(lat - complexLat)
    const lngDiff = Math.abs(lng - complexLng)
    if (latDiff > 0.0009 || lngDiff > 0.0013) {  // ???100m
      return { success: false, newBadgeLevel: 0, error: '?⑥? 諛섍꼍 100m ?대궡???덉? ?딆뒿?덈떎.' }
    }
  }

  // 2. gps_visits INSERT
  const { error: insertErr } = await adminClient
    .from('gps_visits')
    .insert({ user_id: user.id, complex_id: complexId, lat, lng })

  if (insertErr) {
    console.error('[gps-badge] insert error:', insertErr)
    return { success: false, newBadgeLevel: 0, error: 'DB error' }
  }

  // 3. 30?????숈씪 ?⑥? 諛⑸Ц ?잛닔 吏묎퀎
  const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString()
  const { count } = await adminClient
    .from('gps_visits')
    .select('id', { count: 'exact', head: true })
    .eq('user_id', user.id)
    .eq('complex_id', complexId)
    .gte('verified_at', thirtyDaysAgo)

  // 4. ?꾩옱 諛곗? ?덈꺼 議고쉶
  const { data: profileData } = await adminClient
    .from('profiles')
    .select('gps_badge_level')
    .eq('id', user.id)
    .single()

  const currentLevel = (profileData as { gps_badge_level?: number } | null)?.gps_badge_level ?? 0

  // 5. L2 ?낃렇?덉씠?? 30??3???댁긽 + ?꾩옱 L1
  if (currentLevel < 2 && (count ?? 0) >= 3) {
    await adminClient
      .from('profiles')
      .update({ gps_badge_level: 2 })
      .eq('id', user.id)
    return { success: true, newBadgeLevel: 2 }
  }

  return { success: true, newBadgeLevel: currentLevel }
}

/**
 * L3 ?쒕쪟 ?몄쬆 ?붿껌 ?앹꽦
 * ?뚯씪? ?대씪?댁뼵?몄뿉??Supabase Storage???낅줈????寃쎈줈留??꾨떖
 */
export async function submitL3VerificationRequest(
  complexId: string,
  docType: '?깅낯' | '愿由щ퉬',
  storagePath: string,
): Promise<{ success: boolean; error?: string }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Unauthenticated' }

  const adminClient = createSupabaseAdminClient()
  const { error } = await adminClient
    .from('gps_verification_requests')
    .insert({
      user_id:      user.id,
      complex_id:   complexId,
      doc_type:     docType,
      storage_path: storagePath,
      status:       'pending',
    })

  if (error) {
    console.error('[gps-badge] L3 request error:', error)
    return { success: false, error: 'DB error' }
  }

  return { success: true }
}
```

**2. src/components/reviews/GpsBadge.tsx ?앹꽦** ??諛곗? ?덈꺼蹂??쒖떆:

```tsx
interface GpsBadgeProps {
  level: 0 | 1 | 2 | 3
}

const BADGE_CONFIG = {
  0: null,
  1: { label: '諛⑸Ц?몄쬆', className: 'badge neutral' },
  2: { label: '嫄곗＜?몄쬆', className: 'badge pos' },
  3: { label: '?뚯쑀?먯씤利?, className: 'badge orange' },
} as const

export function GpsBadge({ level }: GpsBadgeProps) {
  const config = BADGE_CONFIG[level]
  if (!config) return null

  return (
    <span className={config.className} aria-label={`GPS ?몄쬆 ?덈꺼: ${config.label}`}>
      {config.label}
    </span>
  )
}
```

**3. src/components/community/GpsVerifyL3Upload.tsx ?앹꽦** ??L3 ?쒕쪟 ?낅줈???대씪?댁뼵??而댄룷?뚰듃:

```tsx
'use client'
import { useState, useRef } from 'react'
import { createClientComponentClient } from '@supabase/auth-helpers-nextjs'
import { submitL3VerificationRequest } from '@/lib/auth/gps-badge'

interface GpsVerifyL3UploadProps {
  complexId: string
  onSuccess?: () => void
}

type DocType = '?깅낯' | '愿由щ퉬'
type UploadState = 'idle' | 'uploading' | 'success' | 'error'

const ALLOWED_TYPES = ['image/jpeg', 'image/png', 'application/pdf']
const MAX_SIZE = 5 * 1024 * 1024  // 5MB

export function GpsVerifyL3Upload({ complexId, onSuccess }: GpsVerifyL3UploadProps) {
  const [docType, setDocType] = useState<DocType>('?깅낯')
  const [state, setState] = useState<UploadState>('idle')
  const [errorMsg, setErrorMsg] = useState<string | null>(null)
  const fileRef = useRef<HTMLInputElement>(null)
  const supabase = createClientComponentClient()

  async function handleUpload() {
    const file = fileRef.current?.files?.[0]
    if (!file) return

    // ?대씪?댁뼵??寃利?    if (!ALLOWED_TYPES.includes(file.type)) {
      setErrorMsg('JPG, PNG, PDF ?뚯씪留??낅줈??媛?ν빀?덈떎.')
      return
    }
    if (file.size > MAX_SIZE) {
      setErrorMsg('?뚯씪 ?ш린??5MB ?댄븯?ъ빞 ?⑸땲??')
      return
    }

    setState('uploading')
    setErrorMsg(null)

    try {
      // Supabase Storage ?낅줈??(gps-docs 踰꾪궥, 蹂몄씤 ?대뜑)
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) throw new Error('濡쒓렇?몄씠 ?꾩슂?⑸땲??')

      const filePath = `${user.id}/${complexId}/${docType}-${Date.now()}.${file.name.split('.').pop() ?? 'bin'}`
      const { error: uploadErr } = await supabase.storage
        .from('gps-docs')
        .upload(filePath, file, { upsert: false })

      if (uploadErr) throw new Error(uploadErr.message)

      // Server Action?쇰줈 DB ?깅줉
      const result = await submitL3VerificationRequest(complexId, docType, filePath)
      if (!result.success) throw new Error(result.error)

      setState('success')
      onSuccess?.()
    } catch (err) {
      console.error('[GpsVerifyL3Upload]', err)
      setErrorMsg(err instanceof Error ? err.message : '?낅줈???ㅽ뙣')
      setState('error')
    }
  }

  if (state === 'success') {
    return (
      <div style={{ font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-positive)', padding: '12px 0' }}>
        ?쒕쪟媛 ?쒖텧?섏뿀?듬땲?? 寃????諛곗?媛 ?낃렇?덉씠?쒕맗?덈떎. (1~3 ?곸뾽??
      </div>
    )
  }

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
      <div style={{ display: 'flex', gap: '8px' }}>
        {(['?깅낯', '愿由щ퉬'] as DocType[]).map(type => (
          <button
            key={type}
            className={`chip ${docType === type ? 'selected' : ''}`}
            onClick={() => setDocType(type)}
          >
            {type === '?깅낯' ? '二쇰??깅줉?깅낯' : '愿由щ퉬 ?⑸? ?뺤씤??}
          </button>
        ))}
      </div>

      <input
        ref={fileRef}
        type="file"
        accept=".jpg,.jpeg,.png,.pdf"
        style={{ display: 'none' }}
        aria-label="?쒕쪟 ?뚯씪 ?좏깮"
        onChange={() => setErrorMsg(null)}
      />

      <div style={{ display: 'flex', gap: '8px', alignItems: 'center' }}>
        <button
          className="btn btn-md btn-secondary"
          onClick={() => fileRef.current?.click()}
          disabled={state === 'uploading'}
        >
          ?뚯씪 ?좏깮
        </button>
        {fileRef.current?.files?.[0] && (
          <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
            {fileRef.current.files[0].name}
          </span>
        )}
        <button
          className="btn btn-md btn-orange"
          onClick={() => void handleUpload()}
          disabled={state === 'uploading'}
        >
          {state === 'uploading' ? '?낅줈??以?..' : '?쒖텧'}
        </button>
      </div>

      {errorMsg && (
        <p style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-negative)', margin: 0 }}>
          {errorMsg}
        </p>
      )}
    </div>
  )
}
```

**4. src/app/admin/gps-requests/page.tsx ?앹꽦** ???대뱶誘?L3 寃???섏씠吏:

```tsx
import { redirect } from 'next/navigation'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'

export const revalidate = 0

export default async function GpsRequestsPage() {
  // Admin guard
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login?next=/admin/gps-requests')

  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) redirect('/')

  const adminClient = createSupabaseAdminClient()

  // pending ?붿껌 紐⑸줉 議고쉶
  const { data: requests } = await adminClient
    .from('gps_verification_requests')
    .select(`
      id, doc_type, storage_path, status, created_at,
      user_id,
      profiles!gps_verification_requests_user_id_fkey(nickname),
      complexes!gps_verification_requests_complex_id_fkey(canonical_name)
    `)
    .eq('status', 'pending')
    .order('created_at', { ascending: true })

  return (
    <main style={{ padding: '32px', maxWidth: '960px', margin: '0 auto' }}>
      <h1 style={{ font: '700 24px/1.25 var(--font-sans)', letterSpacing: '-0.024em', color: 'var(--fg-pri)', marginBottom: '24px' }}>
        GPS L3 ?몄쬆 ?붿껌 寃??      </h1>

      {!requests || requests.length === 0 ? (
        <p style={{ font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
          寃???湲?以묒씤 ?붿껌???놁뒿?덈떎.
        </p>
      ) : (
        <div className="card" style={{ overflow: 'hidden' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse' }}>
            <thead>
              <tr style={{ background: 'var(--bg-surface-2)', borderBottom: '1px solid var(--line-default)' }}>
                <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left' }}>?뚯썝</th>
                <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left' }}>?⑥?</th>
                <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left' }}>?쒕쪟 醫낅쪟</th>
                <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left' }}>?뚯씪</th>
                <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left' }}>?쒖텧??/th>
                <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'center' }}>泥섎━</th>
              </tr>
            </thead>
            <tbody>
              {requests.map((req, idx) => {
                const nickname = (req.profiles as { nickname?: string } | null)?.nickname ?? '??
                const complexName = (req.complexes as { canonical_name?: string } | null)?.canonical_name ?? '??
                return (
                  <tr key={req.id} style={{ borderBottom: idx < requests.length - 1 ? '1px solid var(--line-subtle)' : 'none' }}>
                    <td style={{ padding: '12px 16px', font: '500 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)' }}>{nickname}</td>
                    <td style={{ padding: '12px 16px', font: '500 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)' }}>{complexName}</td>
                    <td style={{ padding: '12px 16px', font: '500 13px/1.3 var(--font-sans)', color: 'var(--fg-sec)' }}>{req.doc_type}</td>
                    <td style={{ padding: '12px 16px' }}>
                      <a
                        href={`/api/admin/gps-file?path=${encodeURIComponent(req.storage_path)}`}
                        target="_blank"
                        rel="noopener noreferrer"
                        style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--dj-orange)' }}
                      >
                        ?뚯씪 蹂닿린
                      </a>
                    </td>
                    <td style={{ padding: '12px 16px', font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                      {new Date(req.created_at).toLocaleDateString('ko-KR')}
                    </td>
                    <td style={{ padding: '12px 16px', textAlign: 'center' }}>
                      <div style={{ display: 'flex', gap: '8px', justifyContent: 'center' }}>
                        <form action={`/api/admin/gps-approve`} method="POST">
                          <input type="hidden" name="requestId" value={req.id} />
                          <input type="hidden" name="userId" value={req.user_id} />
                          <input type="hidden" name="action" value="approve" />
                          <button type="submit" className="btn btn-sm btn-orange">?뱀씤</button>
                        </form>
                        <form action={`/api/admin/gps-approve`} method="POST">
                          <input type="hidden" name="requestId" value={req.id} />
                          <input type="hidden" name="userId" value={req.user_id} />
                          <input type="hidden" name="action" value="reject" />
                          <button type="submit" className="btn btn-sm btn-secondary">嫄곗젅</button>
                        </form>
                      </div>
                    </td>
                  </tr>
                )
              })}
            </tbody>
          </table>
        </div>
      )}
    </main>
  )
}
```

**5. src/app/api/admin/gps-approve/route.ts ?앹꽦** ??L3 ?뱀씤/嫄곗젅 API:

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'

export const runtime = 'nodejs'

export async function POST(request: Request): Promise<NextResponse> {
  // Admin guard
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
  if (!['admin', 'superadmin'].includes((profile as { role: string } | null)?.role ?? '')) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  // formData ?먮뒗 JSON ?뚯떛
  let requestId: string, userId: string, action: 'approve' | 'reject'
  const contentType = request.headers.get('content-type') ?? ''

  if (contentType.includes('application/x-www-form-urlencoded')) {
    const formData = await request.formData()
    requestId = formData.get('requestId') as string
    userId    = formData.get('userId') as string
    action    = formData.get('action') as 'approve' | 'reject'
  } else {
    const body = await request.json().catch(() => null) as { requestId?: string; userId?: string; action?: string } | null
    requestId = body?.requestId ?? ''
    userId    = body?.userId ?? ''
    action    = (body?.action as 'approve' | 'reject') ?? 'reject'
  }

  if (!requestId || !userId || !['approve', 'reject'].includes(action)) {
    return NextResponse.json({ error: 'invalid params' }, { status: 400 })
  }

  const adminClient = createSupabaseAdminClient()
  const newStatus = action === 'approve' ? 'approved' : 'rejected'

  // gps_verification_requests ?곹깭 ?낅뜲?댄듃
  const { error: updateErr } = await adminClient
    .from('gps_verification_requests')
    .update({
      status:      newStatus,
      reviewed_by: user.id,
      reviewed_at: new Date().toISOString(),
    })
    .eq('id', requestId)

  if (updateErr) {
    console.error('[gps-approve] update error:', updateErr)
    return NextResponse.json({ error: 'DB error' }, { status: 500 })
  }

  // ?뱀씤 ?? profiles.gps_badge_level = 3
  if (action === 'approve') {
    const { error: profileErr } = await adminClient
      .from('profiles')
      .update({ gps_badge_level: 3 })
      .eq('id', userId)

    if (profileErr) {
      console.error('[gps-approve] profile update error:', profileErr)
      return NextResponse.json({ error: 'profile update failed' }, { status: 500 })
    }
  }

  // form submit? redirect濡??묐떟
  if (contentType.includes('application/x-www-form-urlencoded')) {
    return NextResponse.redirect(new URL('/admin/gps-requests', request.url))
  }

  return NextResponse.json({ ok: true, status: newStatus })
}
```
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -10 && npm run build 2>&1 | grep -E "^.*(error|Error)" | grep -v node_modules | head -10</automated>
  </verify>
  <done>
    - src/lib/auth/gps-badge.ts: recordGpsVisitAndCheckL2 + submitL3VerificationRequest export??    - src/components/reviews/GpsBadge.tsx: level 0 ??null, 1 ??neutral, 2 ??pos, 3 ??orange
    - src/app/admin/gps-requests/page.tsx: pending ?붿껌 紐⑸줉 + ?뱀씤/嫄곗젅 form
    - src/app/api/admin/gps-approve/route.ts: approve ??gps_badge_level=3, reject ??status=rejected
    - `npm run lint && npm run build` ?ㅻ쪟 ?놁쓬
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase 6 E2E ?뚯뒪??/name>
  <files>
    tests/e2e/phase6.spec.ts
    tests/e2e/gap-label.spec.ts
  </files>
  <action>
**1. tests/e2e/phase6.spec.ts ?앹꽦** ??Phase 6 ?듭떖 湲곕뒫 E2E:

```typescript
import { test, expect } from '@playwright/test'

/**
 * Phase 6 E2E ?뚯뒪?? * ?꾩젣: 濡쒖뺄 Supabase + Next.js dev server (localhost:3000)
 * ?꾩젣: complexes ?뚯씠釉붿뿉 理쒖냼 1媛?active ?⑥? 議댁옱
 * ?꾩젣: district_stats ?뚯씠釉붿뿉 李쎌썝???먮뒗 源?댁떆 ?곗씠??議댁옱 (SGIS ?곸옱 ??
 */

// ?뚯뒪?몄뿉 ?ъ슜???⑥? ID (?ㅼ젣 DB??議댁옱?섎뒗 媛믪쑝濡?援먯껜 ?꾩슂)
// CI ?섍꼍?먯꽌??SKIP (Supabase 誘몄뿰寃?
const SKIP_IN_CI = !!process.env.CI

test.describe('Phase 6: AI ?곷떞 踰꾪듉', () => {
  test.skip(SKIP_IN_CI, 'Requires local Supabase')

  test('?⑥? ?곸꽭 ?섏씠吏??AI ?곷떞 踰꾪듉???쒖떆?쒕떎', async ({ page }) => {
    // 媛??泥?踰덉㎏ ?쒖꽦 ?⑥? URL濡??대룞
    await page.goto('/')
    // 泥?踰덉㎏ ?⑥? 移대뱶 ?대┃ (?ㅺ굅??異붿씠 留곹겕)
    const complexLink = page.locator('a[href*="/complexes/"]').first()
    await complexLink.click()
    await page.waitForURL(/\/complexes\//)

    // AI ?곷떞 踰꾪듉 ?뺤씤
    const aiBtn = page.getByRole('button', { name: 'AI ?곷떞' })
    await expect(aiBtn).toBeVisible()

    // ?대┃ ???⑤꼸 ?대┝
    await aiBtn.click()
    await expect(page.getByRole('dialog', { name: /?곷떞/ })).toBeVisible()

    // ?섏쁺 硫붿떆吏 ?뺤씤
    const welcomeMsg = page.locator('[role="log"]').locator('text=沅곴툑???먯쓣 臾쇱뼱蹂댁꽭??)
    await expect(welcomeMsg).toBeVisible()

    // 硫댁콉 怨좎? ?뺤씤
    await expect(page.locator('text=?ъ옄 議곗뼵???꾨떃?덈떎')).toBeVisible()

    // Escape濡??リ린
    await page.keyboard.press('Escape')
    await expect(page.getByRole('dialog')).not.toBeVisible()
  })

  test('AI ?곷떞 ?⑤꼸?먯꽌 "?リ린" 踰꾪듉?쇰줈 ?⑤꼸 ?ロ엺??, async ({ page }) => {
    await page.goto('/')
    const complexLink = page.locator('a[href*="/complexes/"]').first()
    await complexLink.click()
    await page.waitForURL(/\/complexes\//)

    await page.getByRole('button', { name: 'AI ?곷떞' }).click()
    await expect(page.getByRole('dialog')).toBeVisible()

    await page.getByRole('button', { name: '?リ린' }).click()
    await expect(page.getByRole('dialog')).not.toBeVisible()
  })
})

test.describe('Phase 6: ?대뱶誘?ROI ??쒕낫??, () => {
  test.skip(SKIP_IN_CI, 'Requires admin auth + local Supabase')

  test('/admin/ads ?섏씠吏??ROI ?꾪솴 ?뱀뀡???쒖떆?쒕떎', async ({ page }) => {
    // ?대뱶誘?濡쒓렇??(湲곗〈 admin ?뚯뒪???⑦꽩 李몄“)
    // 濡쒓렇???놁씠 ?묎렐 ??redirect ???뚯뒪?몃뒗 濡쒓렇???곹깭 媛??    await page.goto('/admin/ads')
    // 鍮꾨줈洹몄씤 ??login redirect
    if (page.url().includes('/login')) {
      test.skip()
      return
    }
    await expect(page.locator('text=愿묎퀬 ROI ?꾪솴')).toBeVisible()
  })
})

test.describe('Phase 6: 吏???듦퀎 移대뱶 (SGIS ?곗씠???곸옱 ??', () => {
  test.skip(SKIP_IN_CI, 'Requires local Supabase with SGIS data')

  test('?⑥? ?곸꽭 ?섏씠吏??吏???듦퀎 移대뱶媛 ?쒖떆?????덈떎 (?곗씠???덉쓣 ??', async ({ page }) => {
    await page.goto('/')
    const complexLink = page.locator('a[href*="/complexes/"]').first()
    await complexLink.click()
    await page.waitForURL(/\/complexes\//)

    // district_stats ?곗씠?곌? ?덉쑝硫??쒖떆, ?놁쑝硫?移대뱶 ?놁쓬 (D-12)
    const statsCard = page.locator('[role="region"][aria-labelledby="district-stats-heading"]')
    // 移대뱶 議댁옱 ?щ?留??뺤씤 (?곗씠???щ????곕씪 ?щ씪吏?
    const count = await statsCard.count()
    // 移대뱶媛 ?덉쑝硫?heading ?뺤씤
    if (count > 0) {
      await expect(page.locator('#district-stats-heading')).toHaveText('吏???듦퀎')
    }
    // 移대뱶媛 ?놁뼱???뚯뒪???듦낵 (?곗씠???녿뒗 寃쎌슦)
    expect(count).toBeGreaterThanOrEqual(0)
  })
})
```

**2. tests/e2e/gap-label.spec.ts ?앹꽦** ??媛??쇰꺼 E2E:

```typescript
import { test, expect } from '@playwright/test'

const SKIP_IN_CI = !!process.env.CI

test.describe('Phase 6: 媛??쇰꺼', () => {
  test.skip(SKIP_IN_CI, 'Requires local Supabase with listing_prices data')

  test('?⑥? ?곸꽭 ?섏씠吏 ?ㅺ굅?섍? 異붿씠 ?ㅻ뜑??媛??쇰꺼??議곌굔遺 ?쒖떆?쒕떎', async ({ page }) => {
    await page.goto('/')
    const complexLink = page.locator('a[href*="/complexes/"]').first()
    await complexLink.click()
    await page.waitForURL(/\/complexes\//)

    // 媛??쇰꺼 諛곗? (listing_prices ?곗씠???덉쓣 ?뚮쭔 ?쒖떆)
    const gapBadge = page.locator('.badge.neg, .badge.pos').filter({ hasText: /?쒖꽭蹂대떎/ })
    const count = await gapBadge.count()

    if (count > 0) {
      // 諛곗? 臾멸뎄 ?뺤떇 ?뺤씤
      await expect(gapBadge.first()).toContainText('?쒖꽭蹂대떎')
      await expect(gapBadge.first()).toContainText(/?믪쓬|??쓬/)
      // aria-label ?뺤씤
      await expect(gapBadge.first()).toHaveAttribute('aria-label', /留ㅻЪ ?쒖꽭 鍮꾧탳/)
    }
    // ?곗씠???놁쑝硫?諛곗? ?놁쓬 ???뺤긽 (D-12)
    expect(count).toBeGreaterThanOrEqual(0)
  })

  test('媛??쇰꺼 諛곗?媛 諛⑺뼢???곕씪 neg(?믪쓬) ?먮뒗 pos(??쓬) ?대옒?ㅻ? 媛뽯뒗??, async ({ page }) => {
    await page.goto('/')
    const complexLink = page.locator('a[href*="/complexes/"]').first()
    await complexLink.click()
    await page.waitForURL(/\/complexes\//)

    const negBadge = page.locator('.badge.neg').filter({ hasText: '?믪쓬' })
    const posBadge = page.locator('.badge.pos').filter({ hasText: '??쓬' })

    const negCount = await negBadge.count()
    const posCount = await posBadge.count()

    // neg 諛곗?媛 ?덉쑝硫?"?믪쓬" ?ы븿, pos 諛곗?媛 ?덉쑝硫?"??쓬" ?ы븿
    if (negCount > 0) {
      await expect(negBadge.first()).toContainText('?믪쓬')
    }
    if (posCount > 0) {
      await expect(posBadge.first()).toContainText('??쓬')
    }
    // ?????놁뼱???듦낵 (?곗씠???놁쓬)
  })
})
```
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 && ls tests/e2e/phase6.spec.ts tests/e2e/gap-label.spec.ts</automated>
  </verify>
  <done>
    - tests/e2e/phase6.spec.ts: AI ?곷떞 踰꾪듉, ?⑤꼸 ?닿린/?リ린, Escape ?リ린 ?뚯뒪???ы븿
    - tests/e2e/gap-label.spec.ts: 媛??쇰꺼 議곌굔遺 ?쒖떆 ?뚯뒪???ы븿
    - 紐⑤뱺 ?뚯뒪?몄뿉 `SKIP_IN_CI` 媛???곸슜 (湲곗〈 CI ?⑦꽩 以??
    - `npm run lint` ?듦낵
    - `npm run test:e2e` CI ?섍꼍?먯꽌 SKIP (濡쒖뺄?먯꽌留??ㅽ뻾)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?몄쬆???좎? ??recordGpsVisitAndCheckL2 | ?쒕쾭?먯꽌 醫뚰몴 寃利? GPS ?꾩“ ?쒕룄 媛??|
| ?몄쬆???좎? ??Supabase Storage (gps-docs) | 蹂몄씤 ?대뜑留??낅줈???덉슜 (Storage policy) |
| ?대뱶誘???/api/admin/gps-approve | 愿由ъ옄 auth guard. L3 ?뱀씤?쇰줈 badge_level=3 遺??|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Spoofing | GPS 醫뚰몴 ?꾩“ | mitigate | recordGpsVisitAndCheckL2?먯꽌 complexes.location怨?짹100m 踰붿쐞 泥댄겕. PostGIS 誘몄궗???섍꼍?먯꽌???꾨룄/寃쎈룄 洹쇱궗 寃利?|
| T-06-04-02 | Tampering | gps_visits 吏곸젒 INSERT | mitigate | RLS policy "gps_visits: auth insert" ??user_id = auth.uid() 媛뺤젣. 蹂몄씤 ?덉퐫?쒕쭔 ?쎌엯 媛??|
| T-06-04-03 | Tampering | Storage ?ㅻⅨ ?좎? ?대뜑 ?낅줈???쒕룄 | mitigate | Storage policy: `(storage.foldername(name))[1] = auth.uid()::text` ???대뜑紐낆씠 uid? ?쇱튂?댁빞 ?낅줈???덉슜 |
| T-06-04-04 | Elevation of Privilege | /api/admin/gps-approve ???대뱶誘?沅뚰븳 ?놁씠 badge_level 蹂寃??쒕룄 | mitigate | createSupabaseServerClient() auth.getUser() + profiles.role in ('admin','superadmin') ?댁쨷 ?뺤씤 |
| T-06-04-05 | Tampering | L3 ?뚯씪 ?낆꽦肄붾뱶 ?낅줈??| mitigate | ?대씪?댁뼵?몄뿉??ALLOWED_TYPES 泥댄겕 (JPG/PNG/PDF). ?뚯씪? private 踰꾪궥????? ?대뱶誘쇰쭔 ?묎렐. ?뚯씪 ?ㅽ뻾 遺덇? (Storage????λ쭔) |
| T-06-04-06 | Information Disclosure | gps_verification_requests ???ㅻⅨ ?좎? ?쒕쪟 議고쉶 | mitigate | RLS "gps_req: owner read" ??user_id = auth.uid(). ?대뱶誘쇱? 蹂꾨룄 policy濡??꾩껜 議고쉶 |
</threat_model>

<verification>
```bash
# TypeScript + 鍮뚮뱶
npm run lint 2>&1 | tail -5
npm run build 2>&1 | grep -E "^.*(error|Error)" | grep -v node_modules | head -10

# E2E ?뚯씪 ?뺤씤
ls tests/e2e/phase6.spec.ts tests/e2e/gap-label.spec.ts

# GPS badge ?뚯씪 ?뺤씤
ls src/lib/auth/gps-badge.ts \
   src/components/reviews/GpsBadge.tsx \
   src/components/community/GpsVerifyL3Upload.tsx \
   src/app/admin/gps-requests/page.tsx \
   src/app/api/admin/gps-approve/route.ts
```
</verification>

<success_criteria>
- recordGpsVisitAndCheckL2: 30??3???댁긽 ??gps_badge_level 2 ?낃렇?덉씠??- submitL3VerificationRequest: gps_verification_requests INSERT, status='pending'
- GpsBadge: level 1 ??neutral, 2 ??pos, 3 ??orange, 0 ??null
- /admin/gps-requests: pending ?붿껌 紐⑸줉 ?쒖떆, ?뱀씤/嫄곗젅 form
- /api/admin/gps-approve: approve ??badge_level=3, reject ??status=rejected
- E2E: AI ?곷떞 踰꾪듉 ?쒖떆/?대┃/Escape ?リ린 ?뚯뒪??- E2E: 媛??쇰꺼 議곌굔遺 ?쒖떆 ?뚯뒪??(CI SKIP 媛???ы븿)
- `npm run lint && npm run build` ?듦낵
</success_criteria>

<output>
?꾨즺 ??`.planning/phases/06-ai-differentiation/06-04-SUMMARY.md` ?앹꽦:
- GPS L2/L3 援ы쁽 ?꾨즺 ??ぉ
- E2E ?뚯뒪??而ㅻ쾭由ъ? 紐⑸줉
- Phase 6 ?꾩껜 ?꾨즺 湲곗? 泥댄겕由ъ뒪??
  - [ ] 媛??쇰꺼 UI ?쒖떆 (listing_prices ?곗씠???덉쓣 ??
  - [ ] SGIS ?듦퀎 移대뱶 ?쒖떆 (ingest-sgis.ts ?ㅽ뻾 ??
  - [ ] AI ?곷떞 踰꾪듉 + ?⑤꼸 (local test 媛??
  - [ ] 愿묎퀬 ROI ??쒕낫??(/admin/ads)
  - [ ] 愿묎퀬 移댄뵾 AI 寃??(?대뱶誘???
  - [ ] GPS L2 諛곗? (諛⑸Ц 3????
  - [ ] GPS L3 ?붿껌 + ?대뱶誘??뱀씤
  - [ ] embed-complexes.ts ?ㅽ뻾 (珥덇린 ?꾨쿋??
  - [ ] ingest-sgis.ts ?ㅽ뻾 (SGIS ?곗씠??
  - [ ] GitHub Actions sgis-stats.yml ?깅줉 (遺꾧린 ?곸옱)
</output>

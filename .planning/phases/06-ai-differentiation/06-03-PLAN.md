---
phase: 06-ai-differentiation
plan: "03"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/app/complexes/[id]/page.tsx
  - src/components/complex/GapLabel.tsx
  - src/components/complex/DistrictStatsCard.tsx
  - src/components/complex/AiChatPanel.tsx
  - src/app/admin/ads/page.tsx
  - src/components/admin/AdRoiTable.tsx
  - src/components/admin/AdCopyReviewer.tsx
autonomous: true
requirements:
  - DATA-05
  - DATA-06
  - DIFF-03
  - AD-01
  - AD-02

must_haves:
  truths:
    - "?⑥? ?곸꽭 ?섏씠吏 '?ㅺ굅?섍? 異붿씠' 移대뱶 ?ㅻ뜑??媛??쇰꺼 諛곗?媛 ?쒖떆?쒕떎 (?곗씠???덉쓣 ?뚮쭔)"
    - "?⑥? ?곸꽭 ?섏씠吏??'吏???듦퀎' 移대뱶媛 ?쒖떆?쒕떎 (si+gu濡?district_stats 議고쉶)"
    - "?⑥? ?곸꽭 ?섏씠吏 ?고븯?⑥뿉 'AI ?곷떞' 踰꾪듉??怨좎젙 ?쒖떆?섍퀬, ?대┃ ??梨꾪똿 ?ъ씠???⑤꼸???щ씪?대뱶?몃맂??
    - "/admin/ads ?섏씠吏 ?곷떒??ROI ??쒕낫???뚯씠釉붿씠 ?쒖떆?쒕떎"
    - "?대뱶誘?愿묎퀬 ?깅줉 ?쇱뿉 'AI 寃?? 踰꾪듉???쒖떆?섍퀬, ?대┃ ??violations/suggestions ?⑤꼸???섑??쒕떎"
  artifacts:
    - path: "src/components/complex/GapLabel.tsx"
      provides: "媛??쇰꺼 諛곗? 而댄룷?뚰듃"
      contains: "badge neg"
    - path: "src/components/complex/DistrictStatsCard.tsx"
      provides: "吏???듦퀎 移대뱶 而댄룷?뚰듃"
      contains: "district-stats"
    - path: "src/components/complex/AiChatPanel.tsx"
      provides: "AI ?곷떞 ?뚮줈??踰꾪듉 + ?ъ씠??梨꾪똿 ?⑤꼸"
      contains: "position: fixed"
    - path: "src/components/admin/AdRoiTable.tsx"
      provides: "愿묎퀬 ROI ??쒕낫???뚯씠釉?
      contains: "AdRoiRow"
    - path: "src/components/admin/AdCopyReviewer.tsx"
      provides: "愿묎퀬 移댄뵾 AI 寃???대씪?댁뼵??而댄룷?뚰듃"
      contains: "AI 寃??
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/components/complex/GapLabel.tsx"
      via: "getGapLabelData() Promise.all() 寃곌낵瑜?prop?쇰줈 ?꾨떖"
      pattern: "GapLabel"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/components/complex/DistrictStatsCard.tsx"
      via: "district_stats 議고쉶 寃곌낵瑜?prop?쇰줈 ?꾨떖"
      pattern: "DistrictStatsCard"
    - from: "src/components/complex/AiChatPanel.tsx"
      to: "/api/chat/complex"
      via: "fetch POST with complexId + messages"
      pattern: "fetch.*api/chat/complex"
    - from: "src/app/admin/ads/page.tsx"
      to: "src/components/admin/AdRoiTable.tsx"
      via: "getAdRoiStats(adminClient) 寃곌낵瑜?prop?쇰줈 ?꾨떖"
      pattern: "AdRoiTable"
---

<objective>
Wave 3: ?꾨줎?몄뿏??而댄룷?뚰듃 ??媛??쇰꺼 + 吏???듦퀎 ??+ AI ?곷떞 ?⑤꼸 + ?대뱶誘?ROI + 移댄뵾 寃??UI

Wave 1/2?먯꽌 援ы쁽??諛깆뿏???⑥닔? API瑜??ㅼ젣 UI???곌껐?쒕떎. ?⑥? ?곸꽭 ?섏씠吏 3媛吏 UI 異붽?, ?대뱶誘?2媛吏 UI 異붽?.

Purpose: ?ъ슜?먯? ?대뱶誘쇱씠 Phase 6 湲곕뒫???ㅼ젣濡??ъ슜?????덈뒗 ?붾㈃???꾩꽦?쒕떎.
Output: 5媛?UI 而댄룷?뚰듃 + page.tsx ?섏젙 2嫄?</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ai-differentiation/06-CONTEXT.md
@.planning/phases/06-ai-differentiation/06-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Wave 1?먯꽌 ?앹꽦???곗씠???덉씠???명꽣?섏씠??-->

From src/lib/data/gap-label.ts (Wave 1):
```typescript
export interface GapLabelData {
  listingPricePerPy:      number | null
  avgTransactionPricePerPy: number | null
}
export async function getGapLabelData(complexId: string, supabase: SupabaseClient<Database>): Promise<GapLabelData>
```

From src/lib/format.ts (Wave 1):
```typescript
export function formatGap(gapWan: number): string
// gap >= 10000 ??"1??{n}留뚯썝" | 洹?????"{n}留뚯썝"
```

From src/lib/data/ads.ts (Wave 1):
```typescript
export interface AdRoiRow {
  campaignId:  string
  title:       string
  impressions: number
  clicks:      number
  conversions: number
  ctr:         number | null
  anomaly:     boolean
}
export async function getAdRoiStats(adminClient: SupabaseClient<Database>): Promise<AdRoiRow[]>
```

From src/app/complexes/[id]/page.tsx (湲곗〈):
```typescript
// revalidate = 86400 (ISR 24?쒓컙)
// Promise.all([
//   getComplexById(id, supabase),
//   getComplexTransactionSummary(id, supabase),
//   getActiveAds(supabase),
//   getComplexReviewStats(id, supabase),
//   getReviewsWithComments(id, supabase),
//   getRedevelopmentProject(id, supabase),
//   getQuadrantData(id, supabase),
// ])
// complex.si, complex.gu ?ъ슜 媛??```

From district_stats ?뚯씠釉?(Wave 0 留덉씠洹몃젅?댁뀡):
```sql
-- si text, gu text, population integer, households integer, data_year smallint, data_quarter smallint
-- 荑쇰━: .eq('si', complex.si).eq('gu', complex.gu).order('data_year', {asc:false}).limit(1).maybeSingle()
```

From 06-UI-SPEC.md (?듭떖 ?좏겙):
```css
/* 媛??쇰꺼 */
--fg-negative: #dc2626;  /* ?믪쓬 (badge.neg) */
--fg-positive: #16a34a;  /* ??쓬 (badge.pos) */
--bg-negative-tint: #fef2f2;
--bg-positive-tint: #ecfdf5;

/* AI ?⑤꼸 */
--color-cool-10: #171719;  /* ?좎? 硫붿떆吏 bubble background */
--bg-surface-2: #f4f4f5;   /* ?댁떆?ㅽ꽩??硫붿떆吏 bubble background */
--bg-cautionary-tint: #fffbeb; /* disclaimer strip */
--dj-orange: #ea580c;

/* component classes */
/* .badge.neg, .badge.pos, .badge.caut, .btn.btn-md.btn-orange, .tab, .tnum */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 媛??쇰꺼 而댄룷?뚰듃 + 吏???듦퀎 移대뱶 + ?⑥? ?곸꽭 ?섏씠吏 ?곌껐</name>
  <files>
    src/components/complex/GapLabel.tsx
    src/components/complex/DistrictStatsCard.tsx
    src/app/complexes/[id]/page.tsx
  </files>
  <action>
**1. src/components/complex/GapLabel.tsx ?앹꽦** ???쒖닔 presentational:

UI-SPEC.md 媛??쇰꺼 ?ъ뼇 ?뺥솗??援ы쁽:
- prop: `gap: number | null` (listingPricePerPy - avgTransactionPricePerPy, 留뚯썝 ?⑥쐞)
- `gap === null || gap === 0` ??null return (而댄룷?뚰듃 ?뚮뜑留?????
- `gap > 0` ??`.badge.neg` + ?곹뼢 ?붿궡??+ "?쒖꽭蹂대떎 {N}留뚯썝 ?믪쓬"
- `gap < 0` ??`.badge.pos` + ?섑뼢 ?붿궡??+ "?쒖꽭蹂대떎 {N}留뚯썝 ??쓬"
- formatGap??`src/lib/format.ts`?먯꽌 import?섏뿬 ?ъ슜
- aria-label: `"留ㅻЪ ?쒖꽭 鍮꾧탳: ?쒖꽭蹂대떎 {N}留뚯썝 {?믪쓬|??쓬}"`
- 諛곗? ?ㅽ??? height 22px, padding 0 8px, border-radius 6px, font 600 11px, letter-spacing 0.02em
- ?붿궡??SVG: 10횞10, stroke="currentColor", strokeWidth="2.5"

```tsx
import { formatGap } from '@/lib/format'

interface GapLabelProps {
  gap: number | null
}

function ArrowUp() {
  return (
    <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" aria-hidden="true">
      <path d="m6 14 6-6 6 6" />
    </svg>
  )
}

function ArrowDown() {
  return (
    <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" aria-hidden="true">
      <path d="m6 10 6 6 6-6" />
    </svg>
  )
}

export function GapLabel({ gap }: GapLabelProps) {
  if (gap === null || gap === 0) return null

  const absGap = Math.abs(gap)
  const isAbove = gap > 0
  const label = isAbove ? '?믪쓬' : '??쓬'
  const formattedGap = formatGap(absGap)
  const ariaLabel = `留ㅻЪ ?쒖꽭 鍮꾧탳: ?쒖꽭蹂대떎 ${formattedGap} ${label}`

  return (
    <span
      className={`badge ${isAbove ? 'neg' : 'pos'}`}
      aria-label={ariaLabel}
      style={{
        height: '22px',
        padding: '0 8px',
        borderRadius: '6px',
        font: '600 11px/1 var(--font-sans)',
        letterSpacing: '0.02em',
        display: 'inline-flex',
        alignItems: 'center',
        gap: '4px',
      }}
    >
      {isAbove ? <ArrowUp /> : <ArrowDown />}
      ?쒖꽭蹂대떎 {formattedGap} {label}
    </span>
  )
}
```

**2. src/components/complex/DistrictStatsCard.tsx ?앹꽦** ???쒕쾭 而댄룷?뚰듃:

UI-SPEC.md 吏???듦퀎 移대뱶 ?ъ뼇 援ы쁽:
```tsx
interface DistrictStatsCardProps {
  districtName: string
  population: number | null
  households: number | null
  dataYear: number | null
  dataQuarter: number | null  // 1??
}

export function DistrictStatsCard({ districtName, population, households, dataYear, dataQuarter }: DistrictStatsCardProps) {
  const hasData = population !== null || households !== null

  return (
    <div
      className="card"
      role="region"
      aria-labelledby="district-stats-heading"
      style={{ padding: '24px' }}
    >
      {/* 移대뱶 ?ㅻ뜑 */}
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '12px' }}>
        <h3 id="district-stats-heading" style={{ font: '700 15px/1.4 var(--font-sans)', color: 'var(--fg-pri)', margin: 0 }}>
          吏???듦퀎
        </h3>
        <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
          {districtName} 湲곗?
        </span>
      </div>

      {!hasData ? (
        <p style={{ font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)', textAlign: 'center', padding: '32px 0', margin: 0 }}>
          ?대떦 吏???듦퀎 ?곗씠?곌? ?꾩쭅 ?섏쭛?섏? ?딆븯?듬땲??
        </p>
      ) : (
        <>
          {dataYear !== null && dataQuarter !== null && (
            <p style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: '16px' }}>
              SGIS ?듦퀎泥?湲곗? 쨌 {dataYear}??{dataQuarter}遺꾧린
            </p>
          )}
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px' }}>
            <div>
              <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: '4px' }}>
                ?멸뎄??              </div>
              <div className="tnum" style={{ font: '700 24px/1 var(--font-sans)', color: 'var(--fg-pri)' }}>
                {population !== null ? `${population.toLocaleString('ko-KR')}紐? : '??}
              </div>
            </div>
            <div>
              <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: '4px' }}>
                ?몃???              </div>
              <div className="tnum" style={{ font: '700 24px/1 var(--font-sans)', color: 'var(--fg-pri)' }}>
                {households !== null ? `${households.toLocaleString('ko-KR')}?몃?` : '??}
              </div>
            </div>
          </div>
        </>
      )}
    </div>
  )
}
```

**3. src/app/complexes/[id]/page.tsx ?섏젙:**

湲곗〈 Promise.all()??2媛?荑쇰━ 異붽? (湲곗〈 revalidate = 86400 ?좎?):

```typescript
// import 異붽?
import { getGapLabelData } from '@/lib/data/gap-label'
import { GapLabel } from '@/components/complex/GapLabel'
import { DistrictStatsCard } from '@/components/complex/DistrictStatsCard'
import { AiChatPanel } from '@/components/complex/AiChatPanel'

// Promise.all()??2媛?異붽?:
const [
  complex, txSummary, activeAds, reviewStats, reviewsWithComments,
  redevelopment, quadrantData,
  gapLabelData,      // 異붽?
  districtStatsData, // 異붽?
] = await Promise.all([
  getComplexById(id, supabase),
  getComplexTransactionSummary(id, supabase),
  getActiveAds(supabase),
  getComplexReviewStats(id, supabase),
  getReviewsWithComments(id, supabase),
  getRedevelopmentProject(id, supabase),
  getQuadrantData(id, supabase),
  // 媛??쇰꺼 (?ㅽ뙣 ??null濡?fallback)
  getGapLabelData(id, supabase).catch(() => ({ listingPricePerPy: null, avgTransactionPricePerPy: null })),
  // 吏???듦퀎 (si+gu濡?議고쉶, ?놁쑝硫?null)
  (complex?.si && complex?.gu)
    ? supabase
        .from('district_stats')
        .select('adm_nm, population, households, data_year, data_quarter')
        .eq('si', complex.si)
        .eq('gu', complex.gu)
        .order('data_year', { ascending: false })
        .order('data_quarter', { ascending: false })
        .limit(1)
        .maybeSingle()
        .then(r => r.data)
        .catch(() => null)
    : Promise.resolve(null),
])
```

**二쇱쓽:** complex媛 癒쇱? ?뺤젙?섏뼱??si/gu瑜?李몄“?????덉쑝誘濡? Promise.all?먯꽌 complex瑜??낅┰?곸쑝濡?癒쇱? 議고쉶?섍굅?? district_stats 議고쉶瑜?`complex` 議고쉶 ??蹂꾨룄 await濡?泥섎━:

```typescript
// 諛⑸쾿: complex 癒쇱? 議고쉶 ???섎㉧吏 蹂묐젹
const complex = await getComplexById(id, supabase)
if (!complex) notFound()

const [txSummary, activeAds, reviewStats, reviewsWithComments, redevelopment, quadrantData, gapLabelData, districtStats] = await Promise.all([
  getComplexTransactionSummary(id, supabase),
  getActiveAds(supabase),
  getComplexReviewStats(id, supabase),
  getReviewsWithComments(id, supabase),
  getRedevelopmentProject(id, supabase),
  getQuadrantData(id, supabase),
  getGapLabelData(id, supabase).catch(() => ({ listingPricePerPy: null, avgTransactionPricePerPy: null })),
  complex.si && complex.gu
    ? supabase
        .from('district_stats')
        .select('adm_nm, population, households, data_year, data_quarter')
        .eq('si', complex.si)
        .eq('gu', complex.gu)
        .order('data_year', { ascending: false })
        .order('data_quarter', { ascending: false })
        .limit(1)
        .maybeSingle()
        .then(r => r.data)
        .catch(() => null)
    : Promise.resolve(null),
])
```

**媛??쇰꺼 ?쎌엯 ?꾩튂:** `<DealTypeTabs>` ?곷떒, "?ㅺ굅?섍? 異붿씠" `<h3>` ?ㅻ뜑 ?곗륫:
```tsx
// gap 怨꾩궛
const gap = gapLabelData.listingPricePerPy !== null && gapLabelData.avgTransactionPricePerPy !== null
  ? gapLabelData.listingPricePerPy - gapLabelData.avgTransactionPricePerPy
  : null

// ?ㅻ뜑 row???쎌엯
<div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: '12px' }}>
  <h3>?ㅺ굅?섍? 異붿씠</h3>
  <GapLabel gap={gap} />
</div>
```

**吏???듦퀎 移대뱶 ?쎌엯 ?꾩튂:** `<ValueQuadrantChart>` 諛붾줈 ?ㅼ쓬 (硫붿씤 而щ읆 flex stack, gap: 16px):
```tsx
{districtStats && (
  <DistrictStatsCard
    districtName={districtStats.adm_nm}
    population={districtStats.population}
    households={districtStats.households}
    dataYear={districtStats.data_year}
    dataQuarter={districtStats.data_quarter}
  />
)}
{/* complex.si && complex.gu ?덉?留?districtStats??null?대㈃ 移대뱶 ?놁쓬 (D-12) */}
```

**AI 梨꾪똿 ?⑤꼸 ?쎌엯 ?꾩튂:** ?섏씠吏 而댄룷?뚰듃 ?몃━ 理쒗븯??(position:fixed??stacking context 怨좊젮):
```tsx
{/* ?섏씠吏 理쒗븯????AiChatPanel? 'use client', position:fixed */}
<AiChatPanel complexId={id} complexName={complex.canonical_name} />
```
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -10 && npm run build 2>&1 | grep -E "(error|Error)" | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>
    - GapLabel.tsx: gap=500 ??badge.neg "?쒖꽭蹂대떎 500留뚯썝 ?믪쓬", gap=-1000 ??badge.pos "?쒖꽭蹂대떎 1000留뚯썝 ??쓬", gap=null ??null ?뚮뜑
    - DistrictStatsCard.tsx: ?곗씠???덉쑝硫??멸뎄???몃????쒖떆, ?놁쑝硫?"?꾩쭅 ?섏쭛?섏? ?딆븯?듬땲??" 臾멸뎄
    - complexes/[id]/page.tsx: TypeScript ?ㅻ쪟 ?놁쓬, getGapLabelData + district_stats 荑쇰━ 異붽???    - `npm run build` ?ㅻ쪟 ?놁쓬
  </done>
</task>

<task type="auto">
  <name>Task 2: AI ?곷떞 ?⑤꼸 + ?대뱶誘?ROI ?뚯씠釉?+ 移댄뵾 寃??而댄룷?뚰듃</name>
  <files>
    src/components/complex/AiChatPanel.tsx
    src/app/admin/ads/page.tsx
    src/components/admin/AdRoiTable.tsx
    src/components/admin/AdCopyReviewer.tsx
  </files>
  <action>
**1. src/components/complex/AiChatPanel.tsx ?앹꽦** ??'use client':

UI-SPEC.md Component 3 ?ъ뼇 ?뺥솗??援ы쁽:

```tsx
'use client'
import { useState, useRef, useEffect } from 'react'

interface AiPanelProps {
  complexId: string
  complexName: string
}

interface ChatMessage {
  role: 'user' | 'assistant'
  content: string
  isError?: boolean
  isPending?: boolean
}

function ChatIcon() {
  return (
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true">
      <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
    </svg>
  )
}

function XIcon() {
  return (
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true">
      <path d="M18 6 6 18M6 6l12 12" />
    </svg>
  )
}

function SendIcon() {
  return (
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true">
      <path d="m22 2-7 20-4-9-9-4 20-7z" />
    </svg>
  )
}

export function AiChatPanel({ complexId, complexName }: AiPanelProps) {
  const [isOpen, setIsOpen] = useState(false)
  const [messages, setMessages] = useState<ChatMessage[]>([])
  const [inputValue, setInputValue] = useState('')
  const [isPending, setIsPending] = useState(false)
  const triggerRef = useRef<HTMLButtonElement>(null)
  const closeRef = useRef<HTMLButtonElement>(null)
  const inputRef = useRef<HTMLInputElement>(null)
  const messageListRef = useRef<HTMLDivElement>(null)

  // ?⑤꼸 ?닿린 ??close 踰꾪듉?쇰줈 ?ъ빱???대룞 (ARIA)
  useEffect(() => {
    if (isOpen) {
      closeRef.current?.focus()
      document.body.style.overflow = 'hidden'
    } else {
      triggerRef.current?.focus()
      document.body.style.overflow = ''
    }
    return () => { document.body.style.overflow = '' }
  }, [isOpen])

  // ??硫붿떆吏 ?꾩갑 ???ㅽ겕濡??섎떒
  useEffect(() => {
    if (messageListRef.current) {
      messageListRef.current.scrollTop = messageListRef.current.scrollHeight
    }
  }, [messages])

  // Escape ?ㅻ줈 ?⑤꼸 ?リ린
  useEffect(() => {
    if (!isOpen) return
    const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') setIsOpen(false) }
    document.addEventListener('keydown', handler)
    return () => document.removeEventListener('keydown', handler)
  }, [isOpen])

  async function sendMessage() {
    const content = inputValue.trim()
    if (!content || isPending) return

    const userMsg: ChatMessage = { role: 'user', content }
    const newMessages = [...messages, userMsg]
    setMessages(newMessages)
    setInputValue('')
    setIsPending(true)

    // ?묐떟 以?placeholder
    setMessages(prev => [...prev, { role: 'assistant', content: '?묐떟 以?..', isPending: true }])

    try {
      const res = await fetch('/api/chat/complex', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          complexId,
          messages: newMessages.map(m => ({ role: m.role, content: m.content })),
        }),
      })

      if (!res.ok || !res.body) throw new Error('stream error')

      // SSE ?ㅽ듃由щ컢 ?섏떊
      const reader = res.body.getReader()
      const decoder = new TextDecoder()
      let assistantText = ''

      while (true) {
        const { done, value } = await reader.read()
        if (done) break
        const chunk = decoder.decode(value)
        for (const line of chunk.split('\n')) {
          if (line.startsWith('data: ')) {
            try {
              const data = JSON.parse(line.slice(6)) as {
                type?: string
                delta?: { type?: string; text?: string }
              }
              if (data.type === 'content_block_delta' && data.delta?.type === 'text_delta' && data.delta.text) {
                assistantText += data.delta.text
                // ?ㅼ떆媛??ㅽ듃由щ컢 ?낅뜲?댄듃
                setMessages(prev => {
                  const updated = [...prev]
                  const lastIdx = updated.length - 1
                  if (updated[lastIdx]?.isPending) {
                    updated[lastIdx] = { role: 'assistant', content: assistantText }
                  }
                  return updated
                })
              }
            } catch { /* SSE ?뚯떛 ?ㅻ쪟 臾댁떆 */ }
          }
        }
      }

      // ?ㅽ듃由??꾨즺: pending ?쒓굅
      setMessages(prev => {
        const updated = [...prev]
        const lastIdx = updated.length - 1
        if (updated[lastIdx]?.isPending) {
          updated[lastIdx] = { role: 'assistant', content: assistantText || '?묐떟??諛쏆? 紐삵뻽?듬땲??' }
        }
        return updated
      })
    } catch {
      setMessages(prev => {
        const updated = [...prev]
        const lastIdx = updated.length - 1
        if (updated[lastIdx]?.isPending) {
          updated[lastIdx] = {
            role: 'assistant',
            content: '?붿껌 泥섎━ 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎. ?좎떆 ???ㅼ떆 ?쒕룄?댁＜?몄슂.',
            isError: true,
          }
        }
        return updated
      })
    } finally {
      setIsPending(false)
    }
  }

  const welcomeMessage: ChatMessage = {
    role: 'assistant',
    content: `?덈뀞?섏꽭?? ${complexName}?????沅곴툑???먯쓣 臾쇱뼱蹂댁꽭??`,
  }
  const displayMessages = messages.length === 0 ? [welcomeMessage] : messages

  return (
    <>
      {/* ?뚮줈??踰꾪듉 */}
      <button
        ref={triggerRef}
        className={`btn btn-md ${isOpen ? 'btn-secondary' : 'btn-orange'}`}
        style={{ position: 'fixed', bottom: '24px', right: '24px', zIndex: 100, height: '44px', padding: '0 18px', display: 'flex', alignItems: 'center', gap: '8px', borderRadius: 'var(--radius-lg)' }}
        onClick={() => setIsOpen(prev => !prev)}
        aria-label={isOpen ? '?곷떞 ?⑤꼸 ?リ린' : 'AI ?곷떞 ?닿린'}
      >
        {isOpen ? <><XIcon /> ?リ린</> : <><ChatIcon /> AI ?곷떞</>}
      </button>

      {/* ?ъ씠???⑤꼸 */}
      {isOpen && (
        <div
          role="dialog"
          aria-label={`${complexName} AI ?곷떞`}
          aria-modal="true"
          style={{
            position: 'fixed', top: 0, right: 0,
            width: 'min(400px, 100vw)', height: '100vh',
            zIndex: 200,
            background: 'var(--bg-surface)',
            borderLeft: '1px solid var(--line-default)',
            boxShadow: '-4px 0 24px rgba(0,0,0,0.08)',
            display: 'flex', flexDirection: 'column',
            transform: 'translateX(0)',
            transition: 'transform 200ms cubic-bezier(0.16,1,0.3,1)',
          }}
        >
          {/* ?ㅻ뜑 */}
          <div style={{ height: '60px', padding: '0 20px', borderBottom: '1px solid var(--line-default)', display: 'flex', alignItems: 'center', justifyContent: 'space-between', flexShrink: 0 }}>
            <span style={{ font: '700 15px/1.4 var(--font-sans)', color: 'var(--fg-pri)' }}>
              {complexName} ?곷떞
            </span>
            <button
              ref={closeRef}
              className="btn btn-ghost btn-icon"
              onClick={() => setIsOpen(false)}
              aria-label="?곷떞 ?⑤꼸 ?リ린"
              style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', width: '32px', height: '32px' }}
            >
              <XIcon />
            </button>
          </div>

          {/* 硫댁콉 怨좎? */}
          <div style={{ background: 'var(--bg-cautionary-tint)', borderBottom: '1px solid rgba(180,120,0,0.15)', padding: '8px 16px', flexShrink: 0 }}>
            <p style={{ font: '500 11px/1.4 var(--font-sans)', color: 'rgba(120,80,0,0.75)', margin: 0 }}>
              ?⑥? DB ?곗씠??湲곕컲 ?묐떟?낅땲?? ?ъ옄 議곗뼵???꾨떃?덈떎.
            </p>
          </div>

          {/* 硫붿떆吏 ?곸뿭 */}
          <div
            ref={messageListRef}
            role="log"
            aria-live="polite"
            aria-label="?곷떞 ?댁슜"
            style={{ flex: 1, overflowY: 'auto', padding: '16px', display: 'flex', flexDirection: 'column', gap: '12px' }}
          >
            {displayMessages.map((msg, idx) => (
              <div
                key={idx}
                style={{
                  alignSelf: msg.role === 'user' ? 'flex-end' : 'flex-start',
                  maxWidth: msg.role === 'user' ? '80%' : '85%',
                  background: msg.role === 'user' ? 'var(--color-cool-10)' : 'var(--bg-surface-2)',
                  color: msg.isError ? 'var(--fg-negative)' : (msg.role === 'user' ? '#fff' : 'var(--fg-pri)'),
                  borderRadius: msg.role === 'user' ? '12px 12px 2px 12px' : '12px 12px 12px 2px',
                  padding: '10px 14px',
                  font: '500 13px/1.5 var(--font-sans)',
                  border: msg.role === 'assistant' ? '1px solid var(--line-subtle)' : 'none',
                  opacity: msg.isPending ? 0.7 : 1,
                }}
              >
                {msg.content}
              </div>
            ))}
          </div>

          {/* ?낅젰 ?곸뿭 */}
          <div style={{ borderTop: '1px solid var(--line-default)', padding: '12px 16px', display: 'flex', gap: '8px', flexShrink: 0 }}>
            <input
              ref={inputRef}
              className="input"
              value={inputValue}
              onChange={e => setInputValue(e.target.value)}
              onKeyDown={e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); void sendMessage() } }}
              placeholder="吏덈Ц???낅젰?섏꽭??
              aria-label="吏덈Ц ?낅젰"
              disabled={isPending}
              style={{ flex: 1, height: '40px', borderRadius: 'var(--radius-lg)', fontSize: '14px' }}
            />
            <button
              className="btn btn-md btn-orange"
              onClick={() => void sendMessage()}
              disabled={isPending || !inputValue.trim()}
              aria-label="?꾩넚"
              style={{ width: '40px', padding: 0, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}
            >
              <SendIcon />
            </button>
          </div>
        </div>
      )}
    </>
  )
}
```

**2. src/components/admin/AdRoiTable.tsx ?앹꽦** ??UI-SPEC.md Component 5 援ы쁽:

```tsx
import type { AdRoiRow } from '@/lib/data/ads'

interface AdRoiTableProps {
  rows: AdRoiRow[]
}

export function AdRoiTable({ rows }: AdRoiTableProps) {
  if (rows.length === 0) return null

  return (
    <div style={{ marginBottom: '32px' }}>
      <h2 style={{ font: '700 16px/1.4 var(--font-sans)', letterSpacing: '-0.02em', color: 'var(--fg-pri)', margin: '0 0 12px' }}>
        愿묎퀬 ROI ?꾪솴
      </h2>
      <div className="card" style={{ overflow: 'hidden' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <caption className="sr-only">愿묎퀬 ROI ?꾪솴</caption>
          <thead>
            <tr style={{ background: 'var(--bg-surface-2)', borderBottom: '1px solid var(--line-default)' }}>
              <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'left' }}>愿묎퀬紐?/th>
              <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right', width: '80px' }}>?몄텧</th>
              <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right', width: '80px' }}>?대┃</th>
              <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right', width: '80px' }}>?꾪솚</th>
              <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right', width: '80px' }}>CTR%</th>
              <th scope="col" style={{ padding: '10px 16px', font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'center', width: '100px' }}>?댁긽?몃옒??/th>
            </tr>
          </thead>
          <tbody>
            {rows.map((row, idx) => (
              <tr key={row.campaignId} style={{ borderBottom: idx < rows.length - 1 ? '1px solid var(--line-subtle)' : 'none' }}>
                <td style={{ padding: '12px 16px', font: '600 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)' }}>
                  {row.title}
                </td>
                <td className="tnum" style={{ padding: '12px 16px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right' }}>
                  {row.impressions.toLocaleString('ko-KR')}
                </td>
                <td className="tnum" style={{ padding: '12px 16px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right' }}>
                  {row.clicks.toLocaleString('ko-KR')}
                </td>
                <td className="tnum" style={{ padding: '12px 16px', font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)', textAlign: 'right' }}>
                  {row.conversions.toLocaleString('ko-KR')}
                </td>
                <td className="tnum" style={{ padding: '12px 16px', font: '600 13px/1 var(--font-sans)', color: 'var(--fg-pri)', textAlign: 'right' }}>
                  {row.ctr !== null ? `${row.ctr.toFixed(1)}%` : '??}
                </td>
                <td style={{ padding: '12px 16px', textAlign: 'center' }}>
                  {row.anomaly
                    ? <span className="badge caut" aria-label="?댁긽 ?몃옒??媛먯???>?댁긽媛먯?</span>
                    : <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>??/span>
                  }
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

**3. src/components/admin/AdCopyReviewer.tsx ?앹꽦** ??'use client', UI-SPEC.md Component 4:

```tsx
'use client'
import { useState } from 'react'

type ReviewState = 'idle' | 'loading' | 'result' | 'error'

interface ReviewResult {
  violations: string[]
  suggestions: string[]
}

interface AdCopyReviewerProps {
  copyText: string  // ?꾩옱 ?낅젰??愿묎퀬 移댄뵾 ?띿뒪??(遺紐⑥뿉??愿由?
}

function SparkleIcon() {
  return (
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true">
      <path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3z" />
    </svg>
  )
}

export function AdCopyReviewer({ copyText }: AdCopyReviewerProps) {
  const [state, setState] = useState<ReviewState>('idle')
  const [result, setResult] = useState<ReviewResult | null>(null)

  async function handleReview() {
    if (!copyText.trim() || state === 'loading') return
    setState('loading')
    try {
      const res = await fetch('/api/admin/ad-copy-review', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ copy: copyText }),
      })
      const data = await res.json() as ReviewResult & { error?: boolean }
      if (data.error) {
        setState('error')
      } else {
        setResult(data)
        setState('result')
      }
    } catch {
      setState('error')
    }
  }

  return (
    <div style={{ marginTop: '8px' }}>
      <button
        className="btn btn-sm btn-secondary"
        onClick={() => void handleReview()}
        disabled={state === 'loading' || !copyText.trim()}
        aria-label="愿묎퀬 移댄뵾 AI 寃??
        style={{ display: 'inline-flex', alignItems: 'center', gap: '6px', opacity: state === 'loading' ? 0.5 : 1, cursor: state === 'loading' ? 'not-allowed' : 'pointer' }}
      >
        <SparkleIcon />
        AI 寃??      </button>

      {state !== 'idle' && (
        <div
          role="status"
          aria-live="polite"
          aria-busy={state === 'loading'}
          style={{ marginTop: '8px', border: '1px solid var(--line-default)', borderRadius: 'var(--radius-md)', padding: '12px 16px', background: 'var(--bg-surface)' }}
        >
          {state === 'loading' && (
            <p style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>寃??以?..</p>
          )}

          {state === 'error' && (
            <p style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-negative)', margin: 0 }}>
              AI 寃???붿껌???ㅽ뙣?덉뒿?덈떎. 吏곸젒 寃?????깅줉?섏꽭??
            </p>
          )}

          {state === 'result' && result && (
            <>
              <p style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: '8px' }}>寃??寃곌낵</p>

              {result.violations.length === 0 && result.suggestions.length === 0 && (
                <p style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-positive)', margin: 0 }}>
                  寃??寃곌낵 ?뱀씠 ?ы빆???놁뒿?덈떎.
                </p>
              )}

              {result.violations.length > 0 && (
                <div style={{ marginBottom: '8px' }}>
                  <p style={{ font: '600 12px/1.4 var(--font-sans)', color: 'var(--fg-negative)', marginBottom: '6px' }}>?꾨컲 媛???쒗쁽</p>
                  <ul style={{ listStyle: 'none', padding: 0, margin: 0 }}>
                    {result.violations.map((v, i) => (
                      <li key={i} style={{ font: '500 12px/1.6 var(--font-sans)', color: 'var(--fg-pri)', paddingLeft: '8px' }}>쨌 {v}</li>
                    ))}
                  </ul>
                </div>
              )}

              {result.suggestions.length > 0 && (
                <div>
                  <p style={{ font: '600 12px/1.4 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: '6px' }}>媛쒖꽑 ?쒖븞</p>
                  <ul style={{ listStyle: 'none', padding: 0, margin: 0 }}>
                    {result.suggestions.map((s, i) => (
                      <li key={i} style={{ font: '500 12px/1.6 var(--font-sans)', color: 'var(--fg-pri)', paddingLeft: '8px' }}>쨌 {s}</li>
                    ))}
                  </ul>
                </div>
              )}
            </>
          )}
        </div>
      )}
    </div>
  )
}
```

**4. src/app/admin/ads/page.tsx ?섏젙:**

- import 異붽?: `AdRoiTable`, `getAdRoiStats`, `createSupabaseAdminClient`
- 湲곗〈 ?곗씠??fetch??`getAdRoiStats(adminClient)` 異붽?
- ROI ??쒕낫??移대뱶瑜?湲곗〈 罹좏럹??紐⑸줉 移대뱶 ?꾩뿉 ?쎌엯
- `AdCopyReviewer`???대뱶誘?愿묎퀬 ?깅줉/?섏젙 ????`<textarea>` ?꾨옒???쎌엯 (?쇱씠 蹂꾨룄 而댄룷?뚰듃濡?遺꾨━?섏뼱 ?덉쑝硫?洹?而댄룷?뚰듃 ?섏젙)
- 湲곗〈 ?섏씠吏 `revalidate = 0` ?좎?

```typescript
// import 異붽?
import { getAdRoiStats } from '@/lib/data/ads'
import { AdRoiTable } from '@/components/admin/AdRoiTable'
// AdCopyReviewer??'use client' ???몃씪??import ?먮뒗 dynamic import

// ?곗씠??fetch??異붽?
const roiStats = await getAdRoiStats(adminClient)

// JSX??異붽? (湲곗〈 罹좏럹??移대뱶 ??
<AdRoiTable rows={roiStats} />
```
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -10 && npm run build 2>&1 | grep -E "error" | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>
    - AiChatPanel.tsx: 'use client', position:fixed 踰꾪듉, ?щ씪?대뱶???⑤꼸, SSE ?ㅽ듃由щ컢 ?뚮퉬, Escape ?リ린
    - AdRoiTable.tsx: AdRoiRow[] prop, table with caption sr-only, anomaly badge.caut
    - AdCopyReviewer.tsx: 'use client', idle/loading/result/error ?곹깭, AI 寃??踰꾪듉, violations/suggestions ?뚮뜑
    - admin/ads/page.tsx: AdRoiTable ?쎌엯?? TypeScript ?ㅻ쪟 ?놁쓬
    - `npm run build` ?ㅻ쪟 ?놁쓬
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 釉뚮씪?곗? ??AiChatPanel ??/api/chat/complex | 怨듦컻 ?붾뱶?ъ씤?? complexId? 硫붿떆吏 議곗옉 媛??|
| ?대뱶誘?釉뚮씪?곗? ??AdCopyReviewer ??/api/admin/ad-copy-review | 愿由ъ옄留??묎렐 媛?ν븳 ?붾뱶?ъ씤??|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | AiChatPanel ??messages 諛곗뿴 議곗옉 | mitigate | ?쒕쾭?먯꽌 role ?꾪꽣留?(user/assistant留??덉슜). ?쒖뒪???꾨＼?꾪듃 遺꾨━. ?쒕쾭?먯꽌 context 二쇱엯 |
| T-06-03-02 | Information Disclosure | AI ?⑤꼸 disclaimer 誘명몴??| mitigate | disclaimer strip ??긽 ?쒖떆 (?⑤꼸 ?대┫ ??. "?ъ옄 議곗뼵 ?꾨떂" 怨좎? |
| T-06-03-03 | Denial of Service | AiChatPanel ?곗냽 ?꾩넚 | mitigate | isPending ?곹깭濡?UI ?덈꺼 以묐났 ?꾩넚 諛⑹?. ?쒕쾭 rate limit? Wave 2?먯꽌 援ы쁽 |
| T-06-03-04 | Tampering | AdCopyReviewer form submit 李⑤떒 ?놁쓬 | accept | D-10 ?ㅺ퀎 ?섎룄: Claude API ?ㅽ뙣媛 愿묎퀬 ?깅줉 李⑤떒 ?딆쓬. advisory only. |
</threat_model>

<verification>
```bash
# TypeScript + 鍮뚮뱶
npm run lint 2>&1 | tail -5
npm run build 2>&1 | grep "error" | grep -v node_modules | head -10

# 而댄룷?뚰듃 ?뚯씪 議댁옱 ?뺤씤
ls src/components/complex/GapLabel.tsx \
   src/components/complex/DistrictStatsCard.tsx \
   src/components/complex/AiChatPanel.tsx \
   src/components/admin/AdRoiTable.tsx \
   src/components/admin/AdCopyReviewer.tsx
```
</verification>

<success_criteria>
- GapLabel.tsx: null ?덉쟾, gap>0 badge.neg, gap<0 badge.pos, aria-label ?ы븿
- DistrictStatsCard.tsx: role="region", ?곗씠???놁쑝硫?"?꾩쭅 ?섏쭛?섏? ?딆븯?듬땲??" ?쒖떆
- AiChatPanel.tsx: SSE ?ㅽ듃由щ컢 ?뚮퉬, Escape ?リ린, ARIA role="dialog" aria-modal="true"
- AdRoiTable.tsx: caption sr-only, anomaly badge.caut with aria-label
- AdCopyReviewer.tsx: 4媛??곹깭 援ы쁽, form submit 李⑤떒 ?놁쓬
- complexes/[id]/page.tsx: getGapLabelData + district_stats + AiChatPanel 異붽???- admin/ads/page.tsx: AdRoiTable ?쎌엯??- `npm run lint && npm run build` ?듦낵
</success_criteria>

<output>
?꾨즺 ??`.planning/phases/06-ai-differentiation/06-03-SUMMARY.md` ?앹꽦:
- ?앹꽦/?섏젙??而댄룷?뚰듃 紐⑸줉
- ?⑥? ?곸꽭 ?섏씠吏 蹂寃??ы빆 ?붿빟
- ?대뱶誘??섏씠吏 蹂寃??ы빆 ?붿빟
- `npm run build` 寃곌낵
</output>

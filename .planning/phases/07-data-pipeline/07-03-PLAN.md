---
phase: 07-data-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/lib/data/realprice.ts
  - src/__tests__/molit-ingest.test.ts
autonomous: true
requirements:
  - DATA-10
must_haves:
  truths:
    - "ingestMonth 실행 시 각 거래에 complex_id가 자동으로 채워진다 (confidence >= 0.9일 때)"
    - "aptSeq가 있고 신뢰도 >= 0.9 매칭 성공 시 complexes.molit_complex_code가 설정된다"
    - "같은 sgg_code + name_normalized 조합은 ingestMonth 내에서 캐시되어 중복 RPC 호출이 없다"
    - "molit_complex_code는 이미 설정된 경우 덮어쓰지 않는다 (.is('molit_complex_code', null) 가드)"
    - "Zod 스키마 변경 없음 — aptSeq는 MolitSaleItemSchema에 이미 파싱됨"
  artifacts:
    - path: "src/lib/data/realprice.ts"
      provides: "ingestMonth with complex_id lookup + molit_complex_code update"
      contains: "lookupComplexIdCached\|complexIdCache"
    - path: "src/__tests__/molit-ingest.test.ts"
      provides: "DATA-10 검증 테스트 (complex_id 자동 채움 + molit_complex_code 설정)"
      contains: "complex_id\|molit_complex_code"
  key_links:
    - from: "src/lib/data/realprice.ts"
      to: "match_complex_by_admin RPC"
      via: "lookupComplexIdCached() → supabase.rpc('match_complex_by_admin')"
      pattern: "match_complex_by_admin"
    - from: "src/lib/data/realprice.ts"
      to: "complexes.molit_complex_code"
      via: "supabase.from('complexes').update({molit_complex_code}).eq('id', complexId).is('molit_complex_code', null)"
      pattern: "molit_complex_code"
    - from: "src/lib/data/realprice.ts"
      to: "upsertTransaction"
      via: "complex_id: complexId ?? null passed to upsertTransaction row"
      pattern: "complex_id.*complexId\|complexId.*complex_id"
---

<objective>
DATA-10: ingestMonth 수정 — 향후 거래 수집 시 complex_id와 molit_complex_code를 자동으로 채운다.

Purpose: Wave 1 스크립트(07-01, 07-02)가 과거 데이터를 처리하지만, 향후 일배치 cron이 신규 거래를 수집할 때도 complex_id가 자동 연결되어야 한다. 이 plan이 데이터 파이프라인의 지속적 품질을 보장한다.

Output:
- src/lib/data/realprice.ts — lookupComplexIdCached() 추가, ingestMonth에서 complex_id 자동 채움
- src/__tests__/molit-ingest.test.ts — DATA-10 검증 테스트 추가 (complex_id, molit_complex_code)

Note: Wave 2 — Wave 1(07-01) 완료 후 실행. kapt.ts 스키마 확장이 완료된 이후에 시작한다.
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-pipeline/07-RESEARCH.md

<interfaces>
<!-- 아래 인터페이스/타입을 executor가 직접 사용한다. 코드베이스 탐색 불필요. -->

From src/lib/data/realprice.ts (현재 ingestMonth 구조 — 수정 전):
```typescript
// ingestMonth 함수 시그니처:
export async function ingestMonth(
  sggCode: string,
  yearMonth: string,
  supabase: SupabaseClient,
): Promise<IngestResult>

// processSaleItem 내부 현재 흐름:
// 1. MolitSaleItemSchema.safeParse(raw) → item
// 2. item.aptSeq 파싱됨 (dedupe_key에 이미 사용됨)
// 3. upsertTransaction({...}) — complex_id 없이 호출

// TransactionInsert 인터페이스:
interface TransactionInsert {
  deal_type, deal_date, price, monthly_rent, area_m2, floor,
  sgg_code, raw_complex_name, raw_region_code, cancel_date,
  source_run_id, dedupe_key,
  complex_id?: string | null,   // 이미 선택적 필드로 존재
  building_name?: string | null,
}
```

From src/lib/data/name-normalize.ts:
```typescript
export function nameNormalize(raw: string): string
// import 경로: './name-normalize'
```

From src/lib/data/complex-matching.ts:
```typescript
// Axis 3 직접 호출 또는 직접 RPC 호출:
// supabase.rpc('match_complex_by_admin', {
//   p_sgg_code: sggCode,
//   p_name_normalized: nameNormalized,
//   p_min_similarity: 0.9,  // 높은 임계값으로 자동 연결만
// })
// 반환: { id: string, trgm_sim: number }[] 또는 []
```

MolitSaleItemSchema에서 aptSeq 위치 (src/services/molit.ts):
```typescript
// MolitSaleItem 타입:
// item.aptSeq: string | undefined  (예: "48121-792")
// item.aptNm: string               (단지명)
// item.sggCd: string | number      (sgg_code)
```

complexes 테이블 관련:
```
molit_complex_code: text UNIQUE  (migrations/20260430000002_complexes.sql 검증)
UPDATE guard: .is('molit_complex_code', null)  — 이미 설정된 경우 덮어쓰기 방지
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: molit-ingest.test.ts에 DATA-10 테스트 추가</name>
  <files>src/__tests__/molit-ingest.test.ts</files>

  <read_first>
    - src/__tests__/molit-ingest.test.ts — 기존 테스트 구조 파악 (mocking 패턴, describe 블록, supabase mock 방식)
    - src/lib/data/realprice.ts — ingestMonth, upsertTransaction, makeDedupeKey 함수 (수정 전 현재 상태)
    - src/lib/data/name-normalize.ts — nameNormalize import
  </read_first>

  <behavior>
    기존 테스트 파일의 mock 패턴을 그대로 따라 다음 테스트를 추가한다.

    - Test 1 (RED): ingestMonth가 처리한 SaleItem에 complex_id가 포함되어야 한다
      - match_complex_by_admin RPC mock → {id: 'complex-uuid-1', trgm_sim: 0.95} 반환
      - upsertTransaction 호출 시 complex_id: 'complex-uuid-1' 이 포함되어야 함
      - expect(upsertTransactionMock).toHaveBeenCalledWith(expect.objectContaining({complex_id: 'complex-uuid-1'}), supabase)

    - Test 2 (RED): confidence < 0.9 매칭 시 complex_id는 null이어야 한다
      - match_complex_by_admin RPC mock → {id: 'complex-uuid-2', trgm_sim: 0.75} 반환
      - upsertTransaction 호출 시 complex_id: null 이어야 함

    - Test 3 (RED): aptSeq가 있고 매칭 성공(>=0.9)이면 complexes.molit_complex_code 업데이트가 호출된다
      - SaleItem에 aptSeq: "48121-792" 포함
      - supabase.from('complexes').update() 호출 시 molit_complex_code: "48121-792" 포함 검증
      - .is('molit_complex_code', null) guard 검증

    - Test 4 (RED): 같은 sgg_code + name_normalized 조합은 match RPC가 1회만 호출된다 (캐시 검증)
      - 동일 단지명의 SaleItem 2건 처리
      - supabase.rpc mock 호출 횟수가 1회임을 검증 (expect(rpcMock).toHaveBeenCalledTimes(1))
  </behavior>

  <action>
기존 molit-ingest.test.ts 파일을 읽어 mocking 패턴을 파악한다. 기존 테스트 describe 블록 아래에 새로운 describe('DATA-10: complex_id 자동 연결', () => {...}) 블록을 추가한다.

supabase mock 설정:
- supabase.rpc.mockImplementation 또는 vi.fn()으로 match_complex_by_admin 응답 제어
- supabase.from('complexes').update().eq().is() 체인 mock
- upsertTransaction은 기존 파일의 mock 방식을 따름

테스트 파일 수정 시 기존 테스트는 변경하지 않는다. 새 describe 블록만 추가한다.

중요: 이 Task는 RED 단계 — 테스트가 실패하는 상태로 커밋한다. Task 2에서 구현 후 GREEN이 된다.
  </action>

  <verify>
    <automated>npm run test -- molit-ingest 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - 새로 추가된 describe('DATA-10') 블록의 테스트 4개가 존재한다
    - npm run test -- molit-ingest → 기존 테스트 통과, 새 DATA-10 테스트 FAIL (RED 상태)
    - grep "complex_id\|molit_complex_code" src/__tests__/molit-ingest.test.ts → 2개 이상 매칭
    - grep "toHaveBeenCalledTimes(1)" src/__tests__/molit-ingest.test.ts → 캐시 검증 테스트 존재
  </acceptance_criteria>

  <done>DATA-10 검증 테스트 4개가 RED 상태로 추가된다. 기존 테스트는 여전히 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingestMonth에 complex_id 자동 연결 + molit_complex_code 저장 구현</name>
  <files>src/lib/data/realprice.ts</files>

  <read_first>
    - src/lib/data/realprice.ts — 전체 파일 (ingestMonth, processSaleItem, processRentItem, upsertTransaction 현재 구조)
    - src/lib/data/name-normalize.ts — nameNormalize import 경로 확인 ('./name-normalize')
    - src/__tests__/molit-ingest.test.ts — Task 1에서 추가된 테스트 (GREEN 목표)
    - .planning/phases/07-data-pipeline/07-RESEARCH.md — Pattern 4 (ingestMonth DATA-10 수정), Pitfall 5 (캐시 전략)
  </read_first>

  <behavior>
    - lookupComplexIdCached() 호출 시 캐시 miss → RPC 호출, cache hit → Map에서 반환
    - processSaleItem: complex_id = await lookupComplexIdCached(sggCode, nameNormalize(item.aptNm))
    - processRentItem: 동일하게 complex_id 연결
    - aptSeq 있고 complexId 있으면: complexes.molit_complex_code UPDATE (.is('molit_complex_code', null) guard)
    - RPC 응답 trgm_sim < 0.9 → null 반환 (자동 연결 안 함)
  </behavior>

  <action>
src/lib/data/realprice.ts를 다음과 같이 수정한다.

**1. import 추가:**
```typescript
import { nameNormalize } from './name-normalize'
```

**2. ingestMonth 함수 내부 상단에 캐시 Map 추가:**
```typescript
// complex_id 캐시 (ingestMonth 호출 당 — 중복 RPC 방지, Pitfall 5)
const complexIdCache = new Map<string, string | null>()

async function lookupComplexIdCached(
  sggCode: string,
  nameNormalized: string,
): Promise<string | null> {
  const key = `${sggCode}:${nameNormalized}`
  if (complexIdCache.has(key)) return complexIdCache.get(key)!
  const { data, error } = await supabase.rpc('match_complex_by_admin', {
    p_sgg_code:        sggCode,
    p_name_normalized: nameNormalized,
    p_min_similarity:  0.9,   // 자동 연결은 고신뢰만
  })
  if (error || !data || (data as unknown[]).length === 0) {
    complexIdCache.set(key, null)
    return null
  }
  const row = (data as { id: string; trgm_sim: number }[])[0]!
  const complexId = Number(row.trgm_sim) >= 0.9 ? row.id : null
  complexIdCache.set(key, complexId)
  return complexId
}
```

**3. processSaleItem 수정:**

현재 `upsertTransaction({...}, supabase)` 호출 바로 전에 다음을 추가:

```typescript
// DATA-10: complex_id 자동 연결
const nameNorm = nameNormalize(item.aptNm)
const complexId = await lookupComplexIdCached(String(item.sggCd), nameNorm)

// molit_complex_code 저장 (aptSeq 있고 매칭 성공 시 1회만)
if (item.aptSeq && complexId) {
  await supabase
    .from('complexes')
    .update({ molit_complex_code: item.aptSeq })
    .eq('id', complexId)
    .is('molit_complex_code', null)  // 이미 설정된 경우 덮어쓰기 금지
}
```

upsertTransaction 호출을 다음으로 교체:
```typescript
const outcome = await upsertTransaction({
  // ... 기존 모든 필드 유지 ...
  complex_id: complexId ?? null,  // DATA-10 추가
}, supabase)
```

**4. processRentItem 수정:**

processSaleItem과 동일한 패턴으로 complex_id 추가:
- `const nameNorm = nameNormalize(item.aptNm)`
- `const complexId = await lookupComplexIdCached(String(item.sggCd), nameNorm)`
- upsertTransaction에 `complex_id: complexId ?? null` 추가
- 전월세는 aptSeq가 없으므로 molit_complex_code 업데이트 블록 불필요 (MolitRentItem에 aptSeq 없음)

**중요 주의사항:**
- ingestMonth 함수 내부의 closures(processSaleItem, processRentItem)가 lookupComplexIdCached를 참조하므로, complexIdCache와 lookupComplexIdCached는 ingestMonth 함수 스코프 내에 정의한다 (모듈 스코프 금지 — 호출 간 캐시 오염 방지)
- lookupComplexIdCached에서 error가 발생해도 throw하지 않음 — null 반환 후 계속 진행 (ingest 실패 방지)
- 기존 TransactionInsert 인터페이스는 이미 `complex_id?: string | null` 필드가 있으므로 변경 불필요
  </action>

  <verify>
    <automated>npm run test -- molit-ingest</automated>
  </verify>

  <acceptance_criteria>
    - npm run test -- molit-ingest → 모든 테스트 PASS (Task 1 RED → GREEN)
    - grep "lookupComplexIdCached\|complexIdCache" src/lib/data/realprice.ts → 캐시 구현 존재
    - grep "match_complex_by_admin" src/lib/data/realprice.ts → RPC 호출 존재
    - grep "molit_complex_code" src/lib/data/realprice.ts → complexes 업데이트 코드 존재
    - grep "molit_complex_code.*null\|\.is('molit_complex_code'" src/lib/data/realprice.ts → 덮어쓰기 방지 가드 존재
    - grep "complex_id.*complexId\|complexId.*null" src/lib/data/realprice.ts → upsertTransaction에 complex_id 전달 존재
    - npm run lint → 에러 없음
    - npm run build → 에러 없음
  </acceptance_criteria>

  <done>
    ingestMonth가 각 거래 처리 시 complex_id를 자동으로 채운다. molit_complex_code는 매칭 성공 시 1회만 설정된다. 캐시로 중복 RPC 호출이 차단된다. 모든 테스트 GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestMonth → match_complex_by_admin RPC | p_min_similarity=0.9 — 고신뢰만 자동 연결 |
| ingestMonth → complexes UPDATE | .is('molit_complex_code', null) guard — UNIQUE 제약 보호 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | molit_complex_code overwrite | mitigate | `.is('molit_complex_code', null)` guard 필수 — UNIQUE 인덱스 충돌 및 잘못된 aptSeq 덮어쓰기 방지 |
| T-07-03-02 | Tampering | Wrong complex_id from low-confidence match | mitigate | p_min_similarity=0.9 (자동 연결 임계값) — 낮은 신뢰도 매칭은 null로 처리 |
| T-07-03-03 | Denial of Service | ingestMonth performance with extra RPC calls | mitigate | complexIdCache Map으로 동일 단지 중복 RPC 차단 — 실질적 추가 호출 = 고유 단지 수만큼만 |
  </threat_model>

<verification>
```bash
# TDD: 테스트 통과 확인
npm run test -- molit-ingest

# 캐시 및 RPC 코드 확인
grep "complexIdCache\|match_complex_by_admin\|molit_complex_code" src/lib/data/realprice.ts

# TypeScript strict 컴파일
npm run lint

# 빌드
npm run build
```
</verification>

<success_criteria>
1. npm run test -- molit-ingest → 모든 테스트 PASS (기존 + DATA-10 신규 4개)
2. ingestMonth 내부에 complexIdCache Map + lookupComplexIdCached 함수가 존재한다
3. processSaleItem이 complex_id를 upsertTransaction에 전달한다
4. molit_complex_code 업데이트 시 .is('molit_complex_code', null) guard가 있다
5. npm run lint + npm run build 통과
</success_criteria>

<output>
완료 후 `.planning/phases/07-data-pipeline/07-03-SUMMARY.md` 생성
</output>

---
phase: 07-data-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/link-transactions.test.ts
  - src/lib/data/name-aliases.json
  - scripts/link-transactions.ts
  - .github/workflows/link-transactions-once.yml
autonomous: true
requirements:
  - DATA-09
must_haves:
  truths:
    - "transactions 186,765건 중 80% 이상 complex_id가 연결된다"
    - "confidence < 0.5 미매칭 건은 complex_match_queue에 no_match로 기록된다"
    - "confidence 0.5~0.9 저신뢰 건은 complex_match_queue에 low_confidence로 기록된다"
    - "스크립트는 WHERE complex_id IS NULL 조건으로 idempotent하게 동작한다"
    - "단지명 단독 매칭 절대 금지 — 항상 sgg_code + pg_trgm 복합 매칭(Axis 3)"
    - "unmatched 로그가 .planning/phases/07-data-pipeline/unmatched-log.jsonl에 기록된다"
    - "재실행 시 complex_match_queue에 동일 transaction_id 중복 행이 생성되지 않는다"
  artifacts:
    - path: "src/__tests__/link-transactions.test.ts"
      provides: "이름 정규화, 신뢰도 임계값, 큐 적재 단위 테스트"
      contains: "nameNormalize\|confidence\|complex_match_queue"
    - path: "src/lib/data/name-aliases.json"
      provides: "한국 아파트 브랜드 별칭 사전"
      contains: "힐스테이트\|래미안\|e편한세상\|이편한세상"
    - path: "scripts/link-transactions.ts"
      provides: "transactions.complex_id 일괄 연결 스크립트"
      min_lines: 100
    - path: ".github/workflows/link-transactions-once.yml"
      provides: "workflow_dispatch GitHub Actions 워크플로우"
      contains: "workflow_dispatch"
  key_links:
    - from: "scripts/link-transactions.ts"
      to: "src/lib/data/name-normalize.ts"
      via: "nameNormalize() import"
      pattern: "nameNormalize"
    - from: "scripts/link-transactions.ts"
      to: "src/lib/data/complex-matching.ts"
      via: "matchByAdminCode() wrapper (applies ADMIN_CONFIDENCE_CAP internally)"
      pattern: "matchByAdminCode"
    - from: "scripts/link-transactions.ts"
      to: "complex_match_queue table"
      via: "supabase.from('complex_match_queue').insert()"
      pattern: "complex_match_queue"
---

<objective>
DATA-09: transactions.complex_id 일괄 연결 — 186,765건 거래를 complexes 테이블과 연결한다.

Purpose: complex_id가 NULL이면 단지 상세 페이지, AI 채팅, 랭킹 기능이 모두 빈 데이터를 보인다. 이 스크립트가 실행되면 전체 서비스 데이터 기반이 완성된다.

Output:
- src/__tests__/link-transactions.test.ts — 이름 정규화, 신뢰도 임계값, 큐 적재 테스트 (TDD Wave 0)
- src/lib/data/name-aliases.json — 매칭율 향상을 위한 브랜드 별칭 사전
- scripts/link-transactions.ts — batched 매칭 + complex_match_queue 적재 + 중복 방지 스크립트
- .planning/phases/07-data-pipeline/unmatched-log.jsonl — 매칭 실패 로그
- .github/workflows/link-transactions-once.yml — workflow_dispatch 워크플로우
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-pipeline/07-RESEARCH.md

<interfaces>
<!-- 아래 인터페이스/타입을 executor가 직접 사용한다. 코드베이스 탐색 불필요. -->

From src/lib/data/name-normalize.ts:
```typescript
// 정규화 결과 예시: "힐스테이트창원" → "힐스테이트창원"
// "e편한세상창원" → 별칭 JSON 없으면 "e편한세상창원"으로 유지
export function nameNormalize(raw: string): string
// 처리: NFC 정규화 → 별칭 치환(name-aliases.json) → 한자수→숫자 변환 → 아파트 접미사 제거 → 공백/특수문자 제거 → lowercase
```

From src/lib/data/complex-matching.ts:
```typescript
// Axis 3: sgg_code + pg_trgm 매칭 (CLAUDE.md 필수 — 이름 단독 매칭 절대 금지)
// matchByAdminCode wrapper를 사용한다 — ADMIN_CONFIDENCE_CAP(0.85)이 내부에서 적용됨
export async function matchByAdminCode(
  params: { sggCode: string; nameNormalized: string },
  supabase: SupabaseClient,
): Promise<MatchResult | null>

// MatchResult:
// { complexId: string, confidence: number, axis: 'admin_code' }
// confidence는 Math.min(trgm_sim, 0.85) 로 캡됨 (ADMIN_CONFIDENCE_CAP) — 내부 처리
// 주의: supabase.rpc('match_complex_by_admin') 직접 호출 금지 —
//       matchByAdminCode wrapper를 사용해야 ADMIN_CONFIDENCE_CAP이 일관되게 적용됨

// 큐 적재는 complex_match_queue에 직접 insert (enqueueMatch는 private — 직접 사용 불가)
// complex_match_queue 컬럼: source, raw_payload (jsonb), candidate_ids (uuid[]), reason, status
```

From scripts/backfill-realprice.ts (배치 패턴):
```typescript
import { loadEnvConfig } from '@next/env'
import { createClient } from '@supabase/supabase-js'
loadEnvConfig(process.cwd())
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!,
  { auth: { autoRefreshToken: false, persistSession: false } },
)
// 진행 출력: process.stdout.write(`\r[${done}/${total}] ...`)
```

RPC 시그니처 (supabase/migrations/20260430000012_match_functions.sql 검증):
```sql
-- match_complex_by_admin(p_sgg_code text, p_name_normalized text, p_min_similarity float DEFAULT 0.5)
-- 반환: TABLE(id uuid, canonical_name text, trgm_sim float)
```

transactions 테이블 주요 컬럼:
- id, sgg_code, raw_complex_name, complex_id (NULL = 미연결), cancel_date, superseded_by

complex_match_queue 테이블 컬럼:
- source TEXT, raw_payload JSONB, candidate_ids UUID[], reason TEXT ('no_match'|'low_confidence'), status TEXT DEFAULT 'pending'
- 주의: transaction_id에 대한 UNIQUE 제약 없음 → 재실행 시 중복 방지를 스크립트에서 처리
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/__tests__/link-transactions.test.ts 생성 (TDD Wave 0)</name>
  <files>src/__tests__/link-transactions.test.ts</files>

  <read_first>
    - src/lib/data/name-normalize.ts — nameNormalize 함수 시그니처 + 동작
    - src/lib/data/complex-matching.ts — matchByAdminCode, MatchResult 타입, ADMIN_CONFIDENCE_CAP
    - src/__tests__/ 내 기존 테스트 파일 하나 — vi.mock 패턴 파악
  </read_first>

  <behavior>
    - Test 1 (unit): nameNormalize가 e편한세상을 이편한세상으로 변환한다
      - nameNormalize('e편한세상창원마세')에서 별칭 JSON 적용 결과 검증
      - (name-aliases.json 업데이트 후 GREEN)

    - Test 2 (unit): confidence >= 0.9 → complex_id 자동 설정 대상이다
      - matchByAdminCode mock → { complexId: 'uuid-1', confidence: 0.92, axis: 'admin_code' }
      - 스크립트 로직: confidence >= AUTO_THRESHOLD(0.9) → linked 대상
      - expect: transaction이 linked_pairs에 포함됨

    - Test 3 (unit): confidence 0.5~0.9 → complex_match_queue low_confidence 적재
      - matchByAdminCode mock → { complexId: 'uuid-2', confidence: 0.72, axis: 'admin_code' }
      - expect: complex_match_queue.insert 호출, reason: 'low_confidence'

    - Test 4 (unit): matchByAdminCode null 반환 → complex_match_queue no_match 적재
      - matchByAdminCode mock → null
      - expect: complex_match_queue.insert 호출, reason: 'no_match'

    - Test 5 (unit): 재실행 중복 방지 — 기존 큐 항목 있으면 insert 건너뜀
      - complex_match_queue.select mock → 기존 항목 존재 반환
      - expect: complex_match_queue.insert 호출 안 됨 (중복 방지 가드 검증)
  </behavior>

  <action>
src/__tests__/link-transactions.test.ts를 새로 생성한다. 기존 테스트의 vi.mock 패턴을 따른다.

Tests 1은 nameNormalize를 직접 import하여 순수 단위 테스트로 작성한다.
Tests 2~5는 matchByAdminCode와 supabase를 vi.mock하여 스크립트의 분기 로직을 검증한다.

Test 5 (중복 방지)가 핵심: link-transactions.ts가 큐 insert 전 SELECT로 기존 항목을 확인하는 로직을 검증한다.

Task 1은 RED 단계 — link-transactions.ts가 아직 없으므로 Tests 2~5는 실패한다. Test 1은 nameNormalize + name-aliases.json 별칭이 채워진 후 GREEN이 된다.
  </action>

  <verify>
    <automated>npm run test -- link-transactions 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep "nameNormalize\|confidence\|complex_match_queue\|중복\|dedup" src/__tests__/link-transactions.test.ts → 3개 이상 매칭
    - npm run test -- link-transactions → 테스트 파일 실행됨 (일부 FAIL은 RED 상태로 정상)
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>

  <done>link-transactions.test.ts가 생성되고 이름 정규화, 신뢰도 임계값, 큐 적재, 중복 방지를 검증하는 테스트 5개가 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: name-aliases.json 한국 아파트 브랜드 별칭 사전 작성</name>
  <files>src/lib/data/name-aliases.json</files>

  <read_first>
    - src/lib/data/name-aliases.json — 현재 내용 확인 (현재 {} 빈 객체)
    - src/lib/data/name-normalize.ts — 별칭 치환 로직 (Object.entries(aliases)로 RegExp 생성)
  </read_first>

  <action>
현재 {} 인 name-aliases.json을 다음 내용으로 교체한다. 키는 정규식 패턴, 값은 치환 문자열이다.

nameNormalize 함수가 `new RegExp(pattern, 'g')` 로 치환하므로 키는 정규식 패턴을 사용한다.

최종 파일 내용 (구체적 패턴이 앞에 위치, 짧은 패턴이 덮어쓰는 문제 방지):
```json
{
  "한화[\\s]?포레나": "한화포레나",
  "e편한세상": "이편한세상",
  "e-편한세상": "이편한세상",
  "힐스테잇": "힐스테이트",
  "아이 파크": "아이파크",
  "더 샵": "더샵",
  "SK[\\s]?뷰": "sk뷰",
  "롯데 캐슬": "롯데캐슬",
  "LH[\\s]?아파트": "lh아파트",
  "포레나": "한화포레나",
  "두산[\\s]?위브": "두산위브",
  "위브": "두산위브",
  "서희[\\s]?스타힐스": "서희스타힐스",
  "KCC[\\s]?스위첸": "kcc스위첸",
  "스위첸": "kcc스위첸",
  "자이Xi": "자이",
  "Xi": "자이",
  "xi": "자이"
}
```

주의: "한화포레나"를 "포레나"보다 먼저 배치하여 긴 패턴이 먼저 치환되도록 한다.
  </action>

  <verify>
    <automated>node -e "const a = require('./src/lib/data/name-aliases.json'); console.log('aliases count:', Object.keys(a).length)"</automated>
  </verify>

  <acceptance_criteria>
    - node -e "require('./src/lib/data/name-aliases.json')" → JSON 파싱 에러 없음
    - Object.keys(aliases).length >= 10 (최소 10개 별칭)
    - grep "이편한세상" src/lib/data/name-aliases.json → 값으로 존재
    - grep "한화포레나\|스위첸" src/lib/data/name-aliases.json → 존재
    - npm run test -- link-transactions → Test 1 (nameNormalize 별칭 변환) PASS (GREEN)
    - npm run lint → 에러 없음
  </acceptance_criteria>

  <done>name-aliases.json에 한국 아파트 브랜드 별칭 10개 이상이 등록되고, JSON 파싱이 정상 동작한다. Task 1 Test 1이 GREEN이 된다.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/link-transactions.ts + .github/workflows/link-transactions-once.yml 작성</name>
  <files>scripts/link-transactions.ts, .github/workflows/link-transactions-once.yml</files>

  <read_first>
    - scripts/backfill-realprice.ts — loadEnvConfig, supabase client, 진행 출력 패턴
    - src/lib/data/name-normalize.ts — nameNormalize 함수 (import 경로: '../src/lib/data/name-normalize')
    - src/lib/data/complex-matching.ts — matchByAdminCode 함수 시그니처 (import 가능)
    - .github/workflows/embed-complexes-once.yml — workflow_dispatch 패턴
    - .planning/phases/07-data-pipeline/07-RESEARCH.md — Pattern 3 (배치 매칭 전략), Pitfall 3 (중복 큐 방지)
    - src/__tests__/link-transactions.test.ts — Task 1에서 추가된 테스트 (GREEN 목표)
  </read_first>

  <action>
scripts/link-transactions.ts를 다음 설계로 작성한다.

**핵심 제약 (CLAUDE.md):** 단지명 단독 매칭 절대 금지. 항상 sgg_code(행정구역) + trigram 복합 매칭.

**매칭 함수 선택 (일관성):**
- `matchByAdminCode()` wrapper만 사용한다.
- `supabase.rpc('match_complex_by_admin')` 직접 호출 금지 — wrapper를 우회하면 ADMIN_CONFIDENCE_CAP(0.85)이 적용 안 됨.
- import: `import { matchByAdminCode } from '../src/lib/data/complex-matching'`

**임계값:**
- AUTO_THRESHOLD = 0.9 → complex_id 자동 설정 (matchByAdminCode.confidence >= 0.9)
- QUEUE_LOW_CONFIDENCE = 0.5 → complex_match_queue low_confidence 적재
- ADMIN_CONFIDENCE_CAP = 0.85 는 matchByAdminCode 내부에서 적용됨 — 별도 Math.min 불필요

**배치 처리 전략:**
- BATCH_SIZE = 500 rows per Supabase query
- 매칭 결과를 (tx_id, complex_id) 쌍으로 누적 후 bulk UPDATE
- `unmatched-log.jsonl`은 fs.appendFileSync로 실시간 기록

**스크립트 구조:**

```typescript
/**
 * transactions.complex_id 일괄 연결 스크립트 (DATA-09)
 *
 * 실행: npx tsx scripts/link-transactions.ts
 * 환경변수: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
 *
 * 중요: 단지명 단독 매칭 절대 금지 (CLAUDE.md)
 * 항상 sgg_code + pg_trgm 복합 매칭 — matchByAdminCode() wrapper 사용
 */
import { loadEnvConfig } from '@next/env'
import { createClient } from '@supabase/supabase-js'
import { nameNormalize } from '../src/lib/data/name-normalize'
import { matchByAdminCode } from '../src/lib/data/complex-matching'
import * as fs from 'fs'
import * as path from 'path'

loadEnvConfig(process.cwd())
```

**중복 방지 가드 (Pitfall 3 — 재실행 안전):**

큐에 insert하기 전 기존 항목 확인:
```typescript
async function isAlreadyQueued(txId: string): Promise<boolean> {
  const { data } = await supabase
    .from('complex_match_queue')
    .select('source')
    .eq('source', 'link-transactions')
    .contains('raw_payload', { tx_id: txId })
    .limit(1)
  return (data?.length ?? 0) > 0
}
```
큐 insert 전 `if (await isAlreadyQueued(tx.id)) continue` 로 중복 방지.

**main 로직 단계:**

1. 환경변수 유효성 검사
2. unmatched-log.jsonl 경로 = `.planning/phases/07-data-pipeline/unmatched-log.jsonl`
3. 총 COUNT 조회: WHERE complex_id IS NULL
4. offset/limit = 0/BATCH_SIZE로 페이지네이션
5. 각 배치에서:
   - `matchByAdminCode({ sggCode: tx.sgg_code, nameNormalized: nameNormalize(tx.raw_complex_name) }, supabase)` 호출
   - confidence >= AUTO_THRESHOLD(0.9) → linked_pairs 배열에 {id, complexId} 추가
   - confidence >= QUEUE_LOW_CONFIDENCE(0.5) 이고 < 0.9 → 중복 확인 후 큐 insert (low_confidence)
   - null 또는 confidence < 0.5 → 중복 확인 후 큐 insert (no_match) + appendFileSync(logPath, ...)
6. 배치 후 bulk UPDATE (linked_pairs → transactions.complex_id)
7. 진행 출력: `[batch N/M] linked: X, queued: Y, unmatched: Z`
8. 최종 요약 (80% 달성 여부 포함)

**Pitfall 방지:**
- WHERE complex_id IS NULL 조건 항상 포함 (재실행 안전)
- matchByAdminCode wrapper 사용 — sgg_code 필터는 wrapper 내부에서 RPC 파라미터로 전달됨
- complex_match_queue insert 전 중복 확인 (isAlreadyQueued)
- complex_match_queue insert 실패는 console.warn으로만 기록 후 계속 진행

---

**.github/workflows/link-transactions-once.yml** 을 embed-complexes-once.yml 패턴으로 생성한다:

```yaml
name: transactions complex_id 연결 (1회성)

on:
  workflow_dispatch: {}

jobs:
  link:
    name: transactions↔complexes 일괄 연결
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Run link-transactions
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/link-transactions.ts

      - name: Upload unmatched log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unmatched-log
          path: .planning/phases/07-data-pipeline/unmatched-log.jsonl
          if-no-files-found: warn

      - name: 완료
        run: echo "연결 완료 — transactions 테이블에서 complex_id 비율 확인"
```

`upload-artifact` 스텝은 unmatched-log.jsonl을 GitHub Actions artifact로 보존한다. `if: always()`로 스크립트 실패 시에도 업로드된다.

timeout-minutes: 120 — 186K rows 처리 예상. Open Questions Q1(RESOLVED) 참고.
  </action>

  <verify>
    <automated>npm run lint 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - npm run lint → TypeScript 컴파일 에러 없음 (strict 모드 포함)
    - grep "matchByAdminCode" scripts/link-transactions.ts → wrapper 사용 확인
    - grep -v "matchByAdminCode" scripts/link-transactions.ts | grep "match_complex_by_admin" → 결과 없음 (직접 RPC 호출 금지 확인)
    - grep "sgg_code" scripts/link-transactions.ts → sgg_code 포함 매칭 코드 존재 (이름 단독 매칭 금지 검증)
    - grep "complex_id.*IS NULL\|\.is('complex_id', null)" scripts/link-transactions.ts → idempotent 조건 존재
    - grep "complex_match_queue" scripts/link-transactions.ts → 미매칭 큐 적재 코드 존재
    - grep "isAlreadyQueued\|already.*queue\|dedup\|중복" scripts/link-transactions.ts → 중복 방지 가드 존재
    - grep "unmatched-log.jsonl" scripts/link-transactions.ts → 언매칭 로그 경로 존재
    - grep "0\.9\|AUTO_THRESHOLD" scripts/link-transactions.ts → 자동 연결 임계값 0.9 존재
    - npm run test -- link-transactions → Tasks 1 Tests 2~5 PASS (GREEN)
    - npm run lint → 에러 없음
  </acceptance_criteria>

  <done>
    scripts/link-transactions.ts가 TypeScript 에러 없이 컴파일된다. matchByAdminCode() wrapper만 사용하여 sgg_code + trigram 복합 매칭한다. 재실행 시 complex_match_queue 중복 방지 가드가 동작한다. 모든 테스트 GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts → matchByAdminCode wrapper | sgg_code 필터 + ADMIN_CONFIDENCE_CAP이 wrapper 내부에서 일관되게 적용됨 |
| scripts → complex_match_queue | service_role INSERT — 악의적 데이터 없음 (내부 배치) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | transactions UPDATE without WHERE guard | mitigate | `.is('complex_id', null)` guard 필수 — 이미 연결된 행 덮어쓰기 방지 |
| T-07-02-02 | Tampering | name-only matching (CLAUDE.md violation) | mitigate | matchByAdminCode wrapper는 sgg_code를 RPC 파라미터로 필수 전달 — 단독 이름 매칭 구조적으로 불가 |
| T-07-02-03 | Denial of Service | 186K RPC calls in sequence | accept | 배치 처리로 부하 분산; GitHub Actions 120분 제한 내 처리 예상; 초과 시 idempotent 재실행 가능 (Q1 RESOLVED) |
| T-07-02-04 | Information Disclosure | unmatched-log.jsonl in repo | accept | 내부 단지명/sgg_code 데이터만 포함 — 개인정보 없음; .gitignore 대상 고려 |
| T-07-02-05 | Tampering | complex_match_queue duplicate entries on re-run | mitigate | isAlreadyQueued() 중복 확인 후 insert — 동일 transaction_id 중복 행 방지 |
</threat_model>

<verification>
```bash
# TDD: 모든 테스트 통과 확인
npm run test -- link-transactions

# matchByAdminCode wrapper 사용 확인 (직접 RPC 호출 금지)
grep "matchByAdminCode" scripts/link-transactions.ts
grep -v "matchByAdminCode" scripts/link-transactions.ts | grep "match_complex_by_admin"  # 결과 없어야 함

# 중복 방지 가드 확인
grep "isAlreadyQueued\|dedup" scripts/link-transactions.ts

# sgg_code 기반 매칭 확인 (이름 단독 매칭 금지)
grep "sgg_code\|matchByAdminCode" scripts/link-transactions.ts

# 별칭 사전 JSON 파싱 확인
node -e "const a = require('./src/lib/data/name-aliases.json'); console.log(Object.keys(a).length)"

# 워크플로우 확인
grep "workflow_dispatch" .github/workflows/link-transactions-once.yml

# 전체 lint
npm run lint
```
</verification>

<success_criteria>
1. src/__tests__/link-transactions.test.ts에 이름 정규화, 신뢰도 임계값, 큐 적재, 중복 방지 테스트 5개가 있다
2. name-aliases.json에 10개 이상의 한국 아파트 브랜드 별칭이 등록된다
3. scripts/link-transactions.ts가 TypeScript strict 에러 없이 컴파일된다
4. matchByAdminCode() wrapper만 사용한다 — supabase.rpc('match_complex_by_admin') 직접 호출 없음
5. 스크립트는 sgg_code + pg_trgm 복합 매칭만 사용한다 (이름 단독 매칭 없음)
6. WHERE complex_id IS NULL 조건으로 idempotent하게 동작한다
7. complex_match_queue insert 전 isAlreadyQueued() 중복 확인 가드가 있다
8. 미매칭 건이 complex_match_queue와 unmatched-log.jsonl 양쪽에 기록된다
9. npm run test -- link-transactions → 모든 테스트 PASS
10. .github/workflows/link-transactions-once.yml이 workflow_dispatch로 트리거된다
11. npm run lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/07-data-pipeline/07-02-SUMMARY.md` 생성
</output>

---
phase: 07-data-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/name-aliases.json
  - scripts/link-transactions.ts
  - .github/workflows/link-transactions-once.yml
autonomous: true
requirements:
  - DATA-09
must_haves:
  truths:
    - "transactions 186,765건 중 80% 이상 complex_id가 연결된다"
    - "confidence < 0.5 미매칭 건은 complex_match_queue에 no_match로 기록된다"
    - "confidence 0.5~0.9 저신뢰 건은 complex_match_queue에 low_confidence로 기록된다"
    - "스크립트는 WHERE complex_id IS NULL 조건으로 idempotent하게 동작한다"
    - "단지명 단독 매칭 절대 금지 — 항상 sgg_code + pg_trgm 복합 매칭(Axis 3)"
    - "unmatched 로그가 .planning/phases/07-data-pipeline/unmatched-log.jsonl에 기록된다"
  artifacts:
    - path: "src/lib/data/name-aliases.json"
      provides: "한국 아파트 브랜드 별칭 사전"
      contains: "힐스테이트\|래미안\|e편한세상\|이편한세상"
    - path: "scripts/link-transactions.ts"
      provides: "transactions.complex_id 일괄 연결 스크립트"
      min_lines: 100
    - path: ".github/workflows/link-transactions-once.yml"
      provides: "workflow_dispatch GitHub Actions 워크플로우"
      contains: "workflow_dispatch"
  key_links:
    - from: "scripts/link-transactions.ts"
      to: "src/lib/data/name-normalize.ts"
      via: "nameNormalize() import"
      pattern: "nameNormalize"
    - from: "scripts/link-transactions.ts"
      to: "match_complex_by_admin RPC"
      via: "supabase.rpc('match_complex_by_admin')"
      pattern: "match_complex_by_admin"
    - from: "scripts/link-transactions.ts"
      to: "complex_match_queue table"
      via: "supabase.from('complex_match_queue').insert()"
      pattern: "complex_match_queue"
---

<objective>
DATA-09: transactions.complex_id 일괄 연결 — 186,765건 거래를 complexes 테이블과 연결한다.

Purpose: complex_id가 NULL이면 단지 상세 페이지, AI 채팅, 랭킹 기능이 모두 빈 데이터를 보인다. 이 스크립트가 실행되면 전체 서비스 데이터 기반이 완성된다.

Output:
- src/lib/data/name-aliases.json — 매칭율 향상을 위한 브랜드 별칭 사전
- scripts/link-transactions.ts — batched 매칭 + complex_match_queue 적재 스크립트
- .planning/phases/07-data-pipeline/unmatched-log.jsonl — 매칭 실패 로그
- .github/workflows/link-transactions-once.yml — workflow_dispatch 워크플로우
</objective>

<execution_context>
@C:\Users\jung\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jung\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-pipeline/07-RESEARCH.md

<interfaces>
<!-- 아래 인터페이스/타입을 executor가 직접 사용한다. 코드베이스 탐색 불필요. -->

From src/lib/data/name-normalize.ts:
```typescript
// 정규화 결과 예시: "힐스테이트창원" → "힐스테이트창원"
// "e편한세상창원" → 별칭 JSON 없으면 "e편한세상창원"으로 유지
export function nameNormalize(raw: string): string
// 처리: NFC 정규화 → 별칭 치환(name-aliases.json) → 한자수→숫자 변환 → 아파트 접미사 제거 → 공백/특수문자 제거 → lowercase
```

From src/lib/data/complex-matching.ts:
```typescript
// Axis 3: sgg_code + pg_trgm 매칭 (CLAUDE.md 필수 — 이름 단독 매칭 절대 금지)
export async function matchByAdminCode(
  params: { sggCode: string; nameNormalized: string },
  supabase: SupabaseClient,
): Promise<MatchResult | null>

// MatchResult:
// { complexId: string, confidence: number, axis: 'admin_code' }
// confidence는 Math.min(trgm_sim, 0.85) 로 캡됨 (ADMIN_CONFIDENCE_CAP)

// 큐 적재는 complex_match_queue에 직접 insert (enqueueMatch는 private — 직접 사용 불가)
// complex_match_queue 컬럼: source, raw_payload (jsonb), candidate_ids (uuid[]), reason, status
```

From scripts/backfill-realprice.ts (배치 패턴):
```typescript
import { loadEnvConfig } from '@next/env'
import { createClient } from '@supabase/supabase-js'
loadEnvConfig(process.cwd())
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!,
  { auth: { autoRefreshToken: false, persistSession: false } },
)
// 진행 출력: process.stdout.write(`\r[${done}/${total}] ...`)
```

RPC 시그니처 (supabase/migrations/20260430000012_match_functions.sql 검증):
```sql
-- match_complex_by_admin(p_sgg_code text, p_name_normalized text, p_min_similarity float DEFAULT 0.5)
-- 반환: TABLE(id uuid, canonical_name text, trgm_sim float)
```

transactions 테이블 주요 컬럼:
- id, sgg_code, raw_complex_name, complex_id (NULL = 미연결), cancel_date, superseded_by

complex_match_queue 테이블 컬럼:
- source TEXT, raw_payload JSONB, candidate_ids UUID[], reason TEXT ('no_match'|'low_confidence'), status TEXT DEFAULT 'pending'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: name-aliases.json 한국 아파트 브랜드 별칭 사전 작성</name>
  <files>src/lib/data/name-aliases.json</files>

  <read_first>
    - src/lib/data/name-aliases.json — 현재 내용 확인 (현재 {} 빈 객체)
    - src/lib/data/name-normalize.ts — 별칭 치환 로직 (Object.entries(aliases)로 RegExp 생성)
  </read_first>

  <action>
현재 {} 인 name-aliases.json을 다음 내용으로 교체한다. 키는 정규식 패턴, 값은 치환 문자열이다.

nameNormalize 함수가 `new RegExp(pattern, 'g')` 로 치환하므로 키는 정규식 패턴을 사용한다.

```json
{
  "e편한세상": "이편한세상",
  "이-편한세상": "이편한세상",
  "e-편한세상": "이편한세상",
  "힐스테잇": "힐스테이트",
  "힐스테이트": "힐스테이트",
  "래미안": "래미안",
  "자이": "자이",
  "아이파크": "아이파크",
  "아이 파크": "아이파크",
  "아이파크": "아이파크",
  "더샵": "더샵",
  "더 샵": "더샵",
  "푸르지오": "푸르지오",
  "Xi": "자이",
  "xi": "자이",
  "SK[\\s]?뷰": "sk뷰",
  "롯데캐슬": "롯데캐슬",
  "롯데 캐슬": "롯데캐슬",
  "LH[\\s]?아파트": "lh아파트",
  "한화[\\s]?포레나": "한화포레나",
  "포레나": "한화포레나",
  "위브": "두산위브",
  "두산[\\s]?위브": "두산위브",
  "서희[\\s]?스타힐스": "서희스타힐스",
  "비스타동원": "동원비스타",
  "KCC[\\s]?스위첸": "kcc스위첸",
  "스위첸": "kcc스위첸"
}
```

실제 창원/김해 거래 데이터에서 나타나는 브랜드 위주로 구성했다. `"힐스테이트": "힐스테이트"` 처럼 동일 값인 항목은 불필요하므로 제거하고, 실제로 variant가 존재하는 것만 유지한다.

최종 파일 내용 (불필요 항목 제거 후):
```json
{
  "e편한세상": "이편한세상",
  "e-편한세상": "이편한세상",
  "힐스테잇": "힐스테이트",
  "아이 파크": "아이파크",
  "더 샵": "더샵",
  "SK[\\s]?뷰": "sk뷰",
  "롯데 캐슬": "롯데캐슬",
  "LH[\\s]?아파트": "lh아파트",
  "한화[\\s]?포레나": "한화포레나",
  "포레나": "한화포레나",
  "두산[\\s]?위브": "두산위브",
  "위브": "두산위브",
  "서희[\\s]?스타힐스": "서희스타힐스",
  "KCC[\\s]?스위첸": "kcc스위첸",
  "스위첸": "kcc스위첸",
  "자이Xi": "자이",
  "xi": "자이",
  "Xi": "자이"
}
```

주의: "포레나" → "한화포레나" 처럼 짧은 패턴이 긴 패턴보다 먼저 치환되면 문제가 될 수 있다. nameNormalize는 Object.entries() 순서로 치환하므로 더 구체적인 패턴을 앞에 배치한다 (JSON 객체는 삽입 순서 유지). "한화포레나" 항목을 "포레나" 앞에 배치한다.
  </action>

  <verify>
    <automated>node -e "const a = require('./src/lib/data/name-aliases.json'); console.log('aliases count:', Object.keys(a).length)"</automated>
  </verify>

  <acceptance_criteria>
    - node -e "require('./src/lib/data/name-aliases.json')" → JSON 파싱 에러 없음
    - Object.keys(aliases).length >= 10 (최소 10개 별칭)
    - grep "이편한세상" src/lib/data/name-aliases.json → 값으로 존재
    - grep "한화포레나\|스위첸" src/lib/data/name-aliases.json → 존재
    - npm run lint → 에러 없음
  </acceptance_criteria>

  <done>name-aliases.json에 한국 아파트 브랜드 별칭 10개 이상이 등록되고, JSON 파싱이 정상 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/link-transactions.ts + unmatched-log 작성</name>
  <files>scripts/link-transactions.ts</files>

  <read_first>
    - scripts/backfill-realprice.ts — loadEnvConfig, supabase client, 진행 출력 패턴
    - src/lib/data/name-normalize.ts — nameNormalize 함수 (import 경로: '../src/lib/data/name-normalize')
    - src/lib/data/complex-matching.ts — matchByAdminCode 함수 (import 가능), ADMIN_CONFIDENCE_CAP = 0.85
    - .planning/phases/07-data-pipeline/07-RESEARCH.md — Pattern 3 (배치 매칭 전략), Pitfall 3 (중복 큐 방지)
  </read_first>

  <action>
scripts/link-transactions.ts를 다음 설계로 작성한다.

**핵심 제약 (CLAUDE.md):** 단지명 단독 매칭 절대 금지. 항상 sgg_code(행정구역) + trigram 복합 매칭.

**임계값:**
- AUTO_THRESHOLD = 0.9 → complex_id 자동 설정
- QUEUE_THRESHOLD = 0.5 → complex_match_queue low_confidence 적재
- ADMIN_CONFIDENCE_CAP = 0.85 (matchByAdminCode에서 적용됨)

**배치 처리 전략 (성능):**
- BATCH_SIZE = 500 rows per Supabase query
- 매칭 결과를 (tx_id, complex_id) 쌍으로 누적 후 bulk UPDATE (개별 UPDATE가 아닌 배치)
- `unmatched-log.jsonl` 은 fs.appendFileSync로 실시간 기록

**스크립트 구조:**

```typescript
/**
 * transactions.complex_id 일괄 연결 스크립트 (DATA-09)
 *
 * 실행: npx tsx scripts/link-transactions.ts
 * 환경변수: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
 *
 * 중요: 단지명 단독 매칭 절대 금지 (CLAUDE.md)
 * 항상 sgg_code + pg_trgm 복합 매칭 (Axis 3: match_complex_by_admin RPC)
 */
import { loadEnvConfig } from '@next/env'
import { createClient } from '@supabase/supabase-js'
import { nameNormalize } from '../src/lib/data/name-normalize'
import { matchByAdminCode } from '../src/lib/data/complex-matching'
import * as fs from 'fs'
import * as path from 'path'

loadEnvConfig(process.cwd())
```

**main 로직 단계:**

1. 환경변수 유효성 검사 (SUPABASE_SERVICE_ROLE_KEY 미설정 시 exit(1))

2. unmatched-log.jsonl 경로 = `.planning/phases/07-data-pipeline/unmatched-log.jsonl`
   - 실행 전 파일 초기화 (신규 작성 모드)

3. 총 복수 COUNT 조회: `SELECT count(*) FROM transactions WHERE complex_id IS NULL`

4. offset/limit = 0/BATCH_SIZE(500)로 페이지네이션:
   ```
   .from('transactions')
   .select('id, sgg_code, raw_complex_name')
   .is('complex_id', null)
   .range(offset, offset + BATCH_SIZE - 1)
   ```

5. 각 배치에서:
   - 각 트랜잭션: nameNormalize(raw_complex_name) → matchByAdminCode({sggCode, nameNormalized}, supabase)
   - confidence >= 0.9 → linked_pairs 배열에 {id, complexId} 추가
   - confidence 0.5~0.9 → supabase.from('complex_match_queue').insert({source:'link-transactions', raw_payload:{tx_id, raw_complex_name, sgg_code}, candidate_ids:[matchResult.complexId], reason:'low_confidence', status:'pending'})
   - null 또는 confidence < 0.5 → complex_match_queue insert(reason: 'no_match') + appendFileSync(logPath, JSON.stringify({id, sgg_code, raw_complex_name})+'\n')

6. 배치 후 bulk UPDATE: linked_pairs가 있으면
   ```
   for (const {id, complexId} of linked_pairs) {
     await supabase.from('transactions').update({complex_id: complexId}).eq('id', id).is('complex_id', null)
   }
   ```
   (개별 UPDATE지만 배치 내 결과 모아서 수행 — 500개 단위)

7. 진행 출력: `[batch N/M] linked: X, queued: Y, unmatched: Z`

8. 최종 요약:
   ```
   총 처리: X건
   자동 연결: Y건 (Z.Z%)
   수동 검수 필요: A건
   미매칭: B건
   목표 80% → 달성 여부 표시
   ```

**Pitfall 방지:**
- WHERE complex_id IS NULL 조건 항상 포함 (재실행 안전)
- matchByAdminCode 내부에서 이미 sgg_code 필터 적용됨 — 추가 필터 불필요
- complex_match_queue insert 실패는 console.warn으로만 기록 후 계속 진행
  </action>

  <verify>
    <automated>npx tsx --noEmit scripts/link-transactions.ts 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - npx tsx --noEmit scripts/link-transactions.ts → TypeScript 컴파일 에러 없음
    - grep "match_complex_by_admin\|matchByAdminCode" scripts/link-transactions.ts → sgg_code 기반 RPC 호출 존재
    - grep "sgg_code" scripts/link-transactions.ts → sgg_code 포함 쿼리/매칭 코드 존재 (이름 단독 매칭 금지 검증)
    - grep "complex_id.*IS NULL\|\.is('complex_id', null)" scripts/link-transactions.ts → idempotent 조건 존재
    - grep "complex_match_queue" scripts/link-transactions.ts → 미매칭 큐 적재 코드 존재
    - grep "unmatched-log.jsonl" scripts/link-transactions.ts → 언매칭 로그 경로 존재
    - grep "0\.9\|AUTO_THRESHOLD" scripts/link-transactions.ts → 자동 연결 임계값 0.9 존재
    - npm run lint → 에러 없음
  </acceptance_criteria>

  <done>
    scripts/link-transactions.ts가 TypeScript 에러 없이 컴파일된다. sgg_code + trigram 복합 매칭만 사용하고, 미매칭 건은 complex_match_queue와 unmatched-log.jsonl에 기록한다.
  </done>
</task>

<task type="auto">
  <name>Task 3: .github/workflows/link-transactions-once.yml 생성</name>
  <files>.github/workflows/link-transactions-once.yml</files>

  <read_first>
    - .github/workflows/embed-complexes-once.yml — workflow_dispatch 패턴
  </read_first>

  <action>
embed-complexes-once.yml 패턴으로 link-transactions-once.yml을 생성한다.

DATA-09 스크립트는 KAPT API 키 불필요 (Supabase RPC만 사용).
186,765건 처리 예상 시간: 374 배치 × (500 RPC + bulk UPDATE) = 약 20~40분. timeout-minutes: 120으로 설정.

```yaml
name: transactions complex_id 연결 (1회성)

on:
  workflow_dispatch: {}

jobs:
  link:
    name: transactions↔complexes 일괄 연결
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Run link-transactions
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/link-transactions.ts

      - name: Upload unmatched log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unmatched-log
          path: .planning/phases/07-data-pipeline/unmatched-log.jsonl
          if-no-files-found: warn

      - name: 완료
        run: echo "연결 완료 — transactions 테이블에서 complex_id 비율 확인"
```

`upload-artifact` 스텝은 unmatched-log.jsonl을 GitHub Actions artifact로 보존한다. `if: always()` 로 스크립트 실패 시에도 업로드된다.
  </action>

  <verify>
    <automated>grep -n "workflow_dispatch\|SUPABASE_SERVICE_ROLE_KEY\|link-transactions" .github/workflows/link-transactions-once.yml</automated>
  </verify>

  <acceptance_criteria>
    - grep "workflow_dispatch" .github/workflows/link-transactions-once.yml → 존재
    - grep "SUPABASE_SERVICE_ROLE_KEY.*secrets" .github/workflows/link-transactions-once.yml → secrets 참조 존재
    - grep "npx tsx scripts/link-transactions.ts" .github/workflows/link-transactions-once.yml → 실행 명령 존재
    - grep "upload-artifact\|unmatched-log" .github/workflows/link-transactions-once.yml → artifact 업로드 스텝 존재
    - grep "timeout-minutes" .github/workflows/link-transactions-once.yml → 타임아웃 설정 존재
  </acceptance_criteria>

  <done>
    workflow_dispatch 트리거 워크플로우가 생성된다. unmatched-log.jsonl이 GitHub Actions artifact로 보존된다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts → match_complex_by_admin RPC | DB 내부 함수 — sgg_code 필터가 RPC에 포함됨 |
| scripts → complex_match_queue | service_role INSERT — 악의적 데이터 없음 (내부 배치) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | transactions UPDATE without WHERE guard | mitigate | `.is('complex_id', null)` guard 필수 — 이미 연결된 행 덮어쓰기 방지 |
| T-07-02-02 | Tampering | name-only matching (CLAUDE.md violation) | mitigate | matchByAdminCode는 sgg_code를 RPC 파라미터로 필수 전달 — 단독 이름 매칭 구조적으로 불가 |
| T-07-02-03 | Denial of Service | 186K RPC calls in sequence | accept | 배치 처리로 부하 분산; GitHub Actions 120분 제한 내 처리 예상; 초과 시 --resume 패턴으로 재실행 |
| T-07-02-04 | Information Disclosure | unmatched-log.jsonl in repo | accept | 내부 단지명/sgg_code 데이터만 포함 — 개인정보 없음; .gitignore 대상 고려 |
</threat_model>

<verification>
```bash
# TypeScript 컴파일 확인
npx tsx --noEmit scripts/link-transactions.ts

# sgg_code 기반 매칭 확인 (이름 단독 매칭 금지)
grep "sgg_code\|matchByAdminCode" scripts/link-transactions.ts

# 별칭 사전 JSON 파싱 확인
node -e "const a = require('./src/lib/data/name-aliases.json'); console.log(Object.keys(a).length)"

# 워크플로우 확인
grep "workflow_dispatch" .github/workflows/link-transactions-once.yml

# 전체 lint
npm run lint
```
</verification>

<success_criteria>
1. name-aliases.json에 10개 이상의 한국 아파트 브랜드 별칭이 등록된다
2. scripts/link-transactions.ts가 TypeScript strict 에러 없이 컴파일된다
3. 스크립트는 sgg_code + pg_trgm 복합 매칭만 사용한다 (이름 단독 매칭 없음)
4. WHERE complex_id IS NULL 조건으로 idempotent하게 동작한다
5. 미매칭 건이 complex_match_queue와 unmatched-log.jsonl 양쪽에 기록된다
6. .github/workflows/link-transactions-once.yml이 workflow_dispatch로 트리거된다
7. npm run lint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/07-data-pipeline/07-02-SUMMARY.md` 생성
</output>

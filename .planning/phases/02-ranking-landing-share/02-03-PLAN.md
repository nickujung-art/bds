---
phase: 02-ranking-landing-share
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
autonomous: true
files_modified:
  - src/app/page.tsx
  - src/components/home/RankingTabs.tsx
  - src/components/home/HighRecordCard.tsx
  - src/lib/format.ts
requirements:
  - RANK-03

must_haves:
  truths:
    - "랜딩 페이지에 오늘 신고가 카드가 최소 3개 (데이터 있을 때) 표시된다"
    - "랭킹 탭 4종(신고가·거래량·평당가·관심도)이 데이터와 함께 렌더된다"
    - "탭 전환 시 각 랭킹 목록이 즉시 표시된다 (클라이언트 상태 전환)"
    - "page.tsx의 revalidate가 60으로 설정되어 ISR이 활성화된다"
    - "createReadonlyClient()가 유지되어 ISR을 깨뜨리지 않는다"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "랜딩 페이지 ISR 60s"
      contains: "revalidate = 60"
    - path: "src/components/home/RankingTabs.tsx"
      provides: "4종 랭킹 탭 클라이언트 컴포넌트"
      exports: ["RankingTabs"]
    - path: "src/components/home/HighRecordCard.tsx"
      provides: "신고가 카드 RSC"
      exports: ["HighRecordCard"]
    - path: "src/lib/format.ts"
      provides: "formatPrice, formatPyeong, formatDealDate 유틸"
      exports: ["formatPrice", "formatPyeong", "formatDealDate"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/data/rankings.ts"
      via: "getRankingsByType() Promise.all"
      pattern: "getRankingsByType"
    - from: "src/app/page.tsx"
      to: "src/components/home/RankingTabs.tsx"
      via: "initialData props"
      pattern: "RankingTabs"
    - from: "src/components/home/RankingTabs.tsx"
      to: "src/lib/data/rankings.ts"
      via: "RankType, RankingRow 타입 import"
      pattern: "RankType"
---

<objective>
Wave 2 — 랜딩 페이지 ISR 완성 (RANK-03)

이 플랜은 랜딩 페이지(`src/app/page.tsx`)를 4종 랭킹 탭이 동작하는 완성 상태로 업그레이드한다.

변경 범위:
1. `revalidate = 0` → `revalidate = 60` (ISR 60s 활성화)
2. 기존 정적 탭 HTML을 `<RankingTabs>` 클라이언트 컴포넌트로 교체
3. 기존 신고가 카드 JSX를 `<HighRecordCard>` RSC로 추출
4. `formatPrice`, `formatPyeong`, `formatDealDate` 유틸을 `src/lib/format.ts`로 분리
5. `Promise.all`로 4종 랭킹 데이터 병렬 페칭

Purpose: RANK-03 성공 기준 — "랜딩 페이지에 오늘 신고가 카드 ≥ 3개 + 랭킹 탭 4종이 데이터와 함께 렌더"

Output:
- `src/app/page.tsx` (수정)
- `src/components/home/RankingTabs.tsx` (신규)
- `src/components/home/HighRecordCard.tsx` (신규)
- `src/lib/format.ts` (신규 또는 기존 파일에 추가)
</objective>

<execution_context>
@C:\Users\jung\coding\bds\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-PATTERNS.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-02-SUMMARY.md
@C:\Users\jung\coding\bds\src\app\page.tsx
@C:\Users\jung\coding\bds\src\lib\data\homepage.ts

<interfaces>
<!-- homepage.ts에서 추출한 RecentHighRecord 타입 -->
```typescript
export interface RecentHighRecord {
  price: number
  area_m2: number
  floor: number | null
  deal_date: string
  complex: {
    id: string
    canonical_name: string
    si: string | null
    gu: string | null
    dong: string | null
  }
}
```

<!-- rankings.ts에서 추출한 타입 (Wave 1에서 생성) -->
```typescript
export type RankType = 'high_price' | 'volume' | 'price_per_pyeong' | 'interest'

export interface RankingRow {
  id: string
  canonical_name: string
  si: string | null
  gu: string | null
  score: number
  rank: number
}

export async function getRankingsByType(
  supabase: SupabaseClient<Database>,
  rankType: RankType,
  limit?: number,
): Promise<RankingRow[]>
```

<!-- 현재 page.tsx에서 사용 중인 CSS 클래스 -->
```
.tabs, .tab — globals.css에 정의
.card — globals.css에 정의
.badge.orange — globals.css에 정의
data-orange-active="true" — 탭 활성 상태 어트리뷰트
var(--dj-orange), var(--fg-sec), var(--fg-tertiary) — CSS 변수
var(--font-sans) — CSS 폰트 변수
.tnum — tabular numerals
```

<!-- 현재 page.tsx 핵심 구조 (line 111-117) -->
```typescript
export default async function HomePage() {
  const supabase = createReadonlyClient()
  const [highRecords, rankings] = await Promise.all([
    getRecentHighRecords(supabase, 4),
    getTopComplexRankings(supabase, 8),   // ← Wave 2에서 교체
  ])
```

<!-- FavoriteButton.tsx에서 추출한 useState 패턴 -->
```typescript
'use client'
import { useState } from 'react'
// 상태 전환 패턴
const [activeTab, setActiveTab] = useState<RankType>('high_price')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: format.ts 유틸 분리 + 컴포넌트 2종 생성</name>
  <files>
    src/lib/format.ts
    src/components/home/HighRecordCard.tsx
    src/components/home/RankingTabs.tsx
  </files>
  <action>
**단계 1: `src/lib/format.ts` 생성**

`page.tsx`의 인라인 함수 3개를 모듈로 분리한다. (`page.tsx`에서는 삭제 또는 re-export 처리)

```typescript
// src/lib/format.ts
// 가격·평수·날짜 포맷 유틸 — page.tsx에서 추출, 프로젝트 전반 공유

export function formatPrice(price: number): string {
  const uk = Math.floor(price / 10000)
  const man = price % 10000
  if (uk > 0 && man > 0) return `${uk}억 ${man.toLocaleString()}`
  if (uk > 0) return `${uk}억`
  return `${price.toLocaleString()}만`
}

export function formatPyeong(area_m2: number): string {
  return `${Math.round(area_m2 / 3.3058)}평`
}

export function formatDealDate(dealDate: string): string {
  const today = new Date().toISOString().split('T')[0]!
  const yesterday = new Date(Date.now() - 86_400_000).toISOString().split('T')[0]!
  if (dealDate === today) return '오늘'
  if (dealDate === yesterday) return '어제'
  const d = new Date(dealDate)
  return `${d.getMonth() + 1}.${d.getDate()}`
}
```

---

**단계 2: `src/components/home/HighRecordCard.tsx` 생성**

`page.tsx`의 신고가 카드 JSX를 RSC 컴포넌트로 추출한다. `'use client'` 없음.

```typescript
// src/components/home/HighRecordCard.tsx
import Link from 'next/link'
import type { RecentHighRecord } from '@/lib/data/homepage'
import { formatPrice, formatPyeong, formatDealDate } from '@/lib/format'

interface Props {
  record: RecentHighRecord
}

function FireIcon() {
  return (
    <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
      <path d="M12 3c1 4 5 5 5 10a5 5 0 0 1-10 0c0-2 1-3 2-4 0 1 1 2 2 2 0-3-1-5 1-8z" />
    </svg>
  )
}

export function HighRecordCard({ record }: Props) {
  const loc = [record.complex.si, record.complex.gu, record.complex.dong]
    .filter(Boolean)
    .join(' ')

  return (
    <Link
      href={`/complexes/${record.complex.id}`}
      style={{ textDecoration: 'none', color: 'inherit' }}
    >
      <div className="card" style={{ padding: 20, cursor: 'pointer', height: '100%' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 10 }}>
          <span className="badge orange">
            <FireIcon />
            신고가
          </span>
          <span style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginLeft: 'auto' }}>
            {formatDealDate(record.deal_date)}
          </span>
        </div>
        <div style={{ font: '700 16px/1.35 var(--font-sans)', letterSpacing: '-0.012em', marginBottom: 2 }}>
          {record.complex.canonical_name}
        </div>
        <div style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: 14 }}>
          {loc} · {formatPyeong(record.area_m2)}
        </div>
        <div className="tnum" style={{ font: '700 22px/1 var(--font-sans)', letterSpacing: '-0.02em' }}>
          {formatPrice(record.price)}
          <span style={{ fontSize: 14, fontWeight: 600, color: 'var(--fg-sec)', marginLeft: 2 }}>만원</span>
        </div>
      </div>
    </Link>
  )
}
```

---

**단계 3: `src/components/home/RankingTabs.tsx` 생성**

탭 전환 로컬 상태를 관리하는 클라이언트 컴포넌트. 데이터는 `initialData` props로 서버에서 전달받는다 (CLAUDE.md: 'use client'에서 DB 직접 쿼리 금지).

```typescript
// src/components/home/RankingTabs.tsx
'use client'

import { useState } from 'react'
import Link from 'next/link'
import type { RankType, RankingRow } from '@/lib/data/rankings'
import { formatPrice } from '@/lib/format'

interface Props {
  initialData: Record<RankType, RankingRow[]>
}

const TAB_LABELS: Record<RankType, string> = {
  high_price: '신고가 TOP',
  volume: '거래량',
  price_per_pyeong: '평당가',
  interest: '관심도',
}

function ArrUpIcon() {
  return (
    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
      <path d="m6 14 6-6 6 6" />
    </svg>
  )
}

function formatScore(rankType: RankType, score: number): string {
  if (rankType === 'high_price') return formatPrice(score)
  if (rankType === 'price_per_pyeong') return `${Math.round(score / 10000).toLocaleString()}만/평`
  if (rankType === 'volume') return `${score}건`
  return `${score}명` // interest
}

export function RankingTabs({ initialData }: Props) {
  const [activeTab, setActiveTab] = useState<RankType>('high_price')
  const rows = initialData[activeTab] ?? []

  return (
    <div>
      {/* 탭 헤더 */}
      <div
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          marginBottom: 12,
        }}
      >
        <div className="tabs" style={{ border: 'none' }}>
          {(Object.keys(TAB_LABELS) as RankType[]).map((tab) => (
            <button
              key={tab}
              className="tab"
              data-orange-active={activeTab === tab ? 'true' : undefined}
              style={{ background: 'none' }}
              onClick={() => setActiveTab(tab)}
            >
              {TAB_LABELS[tab]}
            </button>
          ))}
        </div>
        <span style={{ font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
          매매 기준 · 최근 30일
        </span>
      </div>

      {/* 랭킹 목록 */}
      <div
        style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(2, 1fr)',
          gap: '0 32px',
          borderTop: '1px solid var(--line-default)',
        }}
      >
        {rows.length > 0 ? (
          rows.map((r) => (
            <Link
              key={r.id}
              href={`/complexes/${r.id}`}
              style={{ textDecoration: 'none', color: 'inherit' }}
            >
              <div
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  padding: '14px 0',
                  borderBottom: '1px solid var(--line-subtle)',
                  gap: 4,
                }}
              >
                <span
                  style={{
                    width: 24,
                    font: '700 14px/1 var(--font-sans)',
                    color: r.rank <= 3 ? 'var(--dj-orange)' : 'var(--fg-tertiary)',
                    flexShrink: 0,
                  }}
                >
                  {r.rank}
                </span>
                <span style={{ flex: 1, font: '600 14px/1 var(--font-sans)' }}>
                  {r.canonical_name}
                </span>
                {r.rank <= 3 && (
                  <span style={{ display: 'flex', alignItems: 'center', color: 'var(--dj-orange)' }}>
                    <ArrUpIcon />
                  </span>
                )}
                <span
                  className="tnum"
                  style={{
                    font: '500 13px/1 var(--font-sans)',
                    color: 'var(--fg-sec)',
                    width: 90,
                    textAlign: 'right',
                  }}
                >
                  {formatScore(activeTab, r.score)}
                </span>
              </div>
            </Link>
          ))
        ) : (
          <div
            style={{
              gridColumn: '1 / -1',
              padding: '32px 0',
              textAlign: 'center',
              color: 'var(--fg-tertiary)',
              font: '500 14px/1 var(--font-sans)',
            }}
          >
            랭킹 데이터를 불러오는 중입니다.
          </div>
        )}
      </div>
    </div>
  )
}
```

**UI 규칙 준수 (CLAUDE.md):**
- backdrop-blur, gradient-text, glow 애니메이션, 보라/인디고 색상, gradient orb 사용 금지
- 기존 CSS 클래스(`.tabs`, `.tab`, `.card`, `.badge.orange`) 그대로 활용
- `data-orange-active="true"` 어트리뷰트로 활성 탭 스타일링 (기존 globals.css 패턴)
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `src/lib/format.ts`가 `formatPrice`, `formatPyeong`, `formatDealDate`를 export한다
    - `src/components/home/HighRecordCard.tsx`가 `'use client'` 없이 RSC로 존재한다
    - `src/components/home/RankingTabs.tsx`가 `'use client'`로 선언되고 `RankingTabs`를 export한다
    - `npm run lint`가 세 파일에서 오류를 보고하지 않는다
  </done>
</task>

<task type="auto">
  <name>Task 2: page.tsx — ISR 60s + 4종 랭킹 데이터 연결</name>
  <files>src/app/page.tsx</files>
  <action>
`src/app/page.tsx`를 수정한다. 기존 파일 전체를 읽고 다음 변경 사항을 적용한다.

**변경 1: revalidate 수정 (line 8)**
```typescript
// 변경 전
export const revalidate = 0
// 변경 후
export const revalidate = 60  // ISR 60s — createReadonlyClient()가 cookies() 미호출로 ISR 안전
```

**변경 2: import 추가**
```typescript
// 기존 import 유지하고 추가
import { getRankingsByType } from '@/lib/data/rankings'
import { HighRecordCard } from '@/components/home/HighRecordCard'
import { RankingTabs } from '@/components/home/RankingTabs'
// formatPrice, formatPyeong, formatDealDate는 page.tsx 인라인에서 제거하고 format.ts에서 import
import { formatPrice, formatPyeong, formatDealDate } from '@/lib/format'
```

**변경 3: HomePage 함수 — Promise.all 확장**
```typescript
export default async function HomePage() {
  const supabase = createReadonlyClient()

  const [highRecords, rankHighPrice, rankVolume, rankPricePerPyeong, rankInterest] =
    await Promise.all([
      getRecentHighRecords(supabase, 4),
      getRankingsByType(supabase, 'high_price', 10),
      getRankingsByType(supabase, 'volume', 10),
      getRankingsByType(supabase, 'price_per_pyeong', 10),
      getRankingsByType(supabase, 'interest', 10),
    ])

  const rankingData = {
    high_price: rankHighPrice,
    volume: rankVolume,
    price_per_pyeong: rankPricePerPyeong,
    interest: rankInterest,
  }
  // ...
```

**변경 4: 신고가 카드 섹션 교체**

기존:
```tsx
{highRecords.map((rec) => {
  const loc = [...]
  return (
    <Link key={...}>
      <div className="card" ...>
        // 인라인 JSX
      </div>
    </Link>
  )
})}
```

교체 후:
```tsx
{highRecords.map((rec) => (
  <HighRecordCard key={rec.complex.id + rec.deal_date} record={rec} />
))}
```

**변경 5: 정적 탭 섹션 교체**

기존 (`div.tabs` + 정적 버튼 + `rankings.map` 인라인 JSX):
```tsx
<div className="tabs" style={{ border: 'none' }}>
  <button className="tab" data-orange-active="true" ...>가격 TOP</button>
  // ...
</div>
// rankings.map 인라인 ...
```

교체 후:
```tsx
<RankingTabs initialData={rankingData} />
```

**변경 6: page.tsx 내 인라인 함수 삭제**

`formatPrice`, `formatPyeong`, `formatDealDate`, `FireIcon`, `ArrUpIcon` 함수를 page.tsx에서 삭제한다. 각각 `src/lib/format.ts`와 컴포넌트로 이전됨. `SearchIcon`, `BellIcon`은 page.tsx에 유지 (다른 곳에서 import하지 않음).

**변경 7: getTopComplexRankings import 제거**

`getTopComplexRankings`는 Wave 2에서 `getRankingsByType`으로 대체되므로 import와 사용을 제거한다. `homepage.ts`의 `getTopComplexRankings`는 파일에서 삭제하지 않는다 (다른 코드가 참조할 수 있음).

**최종 확인:**
- `createReadonlyClient()` 호출이 유지되는지 확인 (ISR 조건)
- `cookies()` 또는 `headers()` 직접 호출이 없는지 확인
- `Suspense`로 감싼 `UserMenu`가 그대로 유지되는지 확인
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `page.tsx`의 `revalidate`가 `60`이다
    - `RankingTabs`와 `HighRecordCard`가 page.tsx에서 렌더된다
    - `getRankingsByType`이 4종으로 `Promise.all` 내에서 병렬 호출된다
    - `npm run build`가 성공한다
    - `npm run lint`가 오류를 보고하지 않는다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel CDN → page.tsx | ISR 캐시 — 60s stale 허용 |
| RankingTabs → DB | 클라이언트 컴포넌트이므로 DB 직접 접근 없음 (props 수신) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | page.tsx ISR | accept | 랜딩 데이터는 공개 정보 — 캐시 공유 무방 |
| T-02-09 | Tampering | RankingTabs props | accept | 서버에서 검증된 데이터를 props로 전달. 클라이언트에서 변조 불가 |
| T-02-10 | Denial of Service | ISR 과부하 | mitigate | revalidate=60으로 DB 쿼리 빈도 제한. Vercel Edge CDN이 캐시 히트 처리 |
</threat_model>

<verification>
```bash
# 빌드 성공 확인
npm run build

# ISR revalidate 설정 확인
grep -n "revalidate" src/app/page.tsx
# → export const revalidate = 60

# getRankingsByType 4종 호출 확인
grep -c "getRankingsByType" src/app/page.tsx
# → 4

# RankingTabs import 확인
grep "RankingTabs" src/app/page.tsx

# 린트
npm run lint
```
</verification>

<success_criteria>
1. `src/app/page.tsx`에 `export const revalidate = 60`이 설정된다
2. `getRankingsByType`이 4종 타입으로 `Promise.all` 내에서 병렬 호출된다
3. `<RankingTabs initialData={...} />`가 page.tsx에서 렌더된다
4. `<HighRecordCard record={...} />`가 신고가 카드 영역에서 렌더된다
5. `createReadonlyClient()`가 유지되어 ISR이 깨지지 않는다
6. `npm run build && npm run lint` 통과
7. 로컬 서버에서 `http://localhost:3000` 접속 시 랭킹 탭 4개와 신고가 카드가 표시된다 (데이터 있을 때)
</success_criteria>

<output>
완료 후 `.planning/phases/02-ranking-landing-share/02-03-SUMMARY.md`를 생성한다.
</output>

---
phase: 02-ranking-landing-share
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - supabase/migrations/20260507000001_complex_rankings.sql
  - supabase/migrations/20260507000002_complex_rankings_rls.sql
  - public/fonts/PretendardSubset.ttf
  - src/__tests__/rankings.test.ts
  - src/__tests__/share-button.test.ts
requirements:
  - RANK-01

must_haves:
  truths:
    - "complex_rankings 테이블이 Supabase DB에 존재하고 4종 rank_type CHECK 제약이 적용된다"
    - "complex_rankings 테이블에 public read RLS 정책이 적용되고 anon key로 SELECT가 된다"
    - "public/fonts/PretendardSubset.ttf 파일이 존재하고 OG 이미지 렌더링에 사용 가능하다"
    - "rankings.test.ts 스캐폴드가 존재하고 Vitest가 파일을 파싱한다 (테스트는 SKIP 상태 허용)"
    - "share-button.test.ts 스캐폴드가 존재하고 Vitest가 파일을 파싱한다"
  artifacts:
    - path: "supabase/migrations/20260507000001_complex_rankings.sql"
      provides: "complex_rankings 테이블 DDL + 인덱스"
      contains: "create table public.complex_rankings"
    - path: "supabase/migrations/20260507000002_complex_rankings_rls.sql"
      provides: "complex_rankings RLS 정책"
      contains: "complex_rankings: public read"
    - path: "public/fonts/PretendardSubset.ttf"
      provides: "OG 이미지용 한글 TTF 폰트"
      min_lines: 1
    - path: "src/__tests__/rankings.test.ts"
      provides: "랭킹 기능 테스트 스캐폴드"
      contains: "computeRankings"
    - path: "src/__tests__/share-button.test.ts"
      provides: "ShareButton 폴백 테스트 스캐폴드"
      contains: "ShareButton"
  key_links:
    - from: "supabase/migrations/20260507000001_complex_rankings.sql"
      to: "public.complexes"
      via: "FOREIGN KEY complex_id"
      pattern: "references public\\.complexes"
    - from: "supabase/migrations/20260507000002_complex_rankings_rls.sql"
      to: "public.complex_rankings"
      via: "RLS 정책"
      pattern: "on public\\.complex_rankings"
---

<objective>
Wave 0 — DB 기반 + 사전 준비

이 플랜은 Phase 2의 모든 후속 플랜이 의존하는 두 가지 사전 조건을 확보한다:
1. `complex_rankings` 테이블 마이그레이션 (Wave 1 데이터 레이어가 UPSERT할 대상)
2. Pretendard TTF 서브셋 폰트 파일 (Wave 2 OG 이미지가 로드할 폰트)
3. 테스트 스캐폴드 2종 (TDD 원칙에 따라 구현 전 먼저 생성)

Purpose: 다운스트림 플랜이 "파일/테이블이 없어서 중단"되는 사태를 예방. Wave 1, Wave 2는 이 플랜 완료 후 병렬로 실행된다.

Output:
- `supabase/migrations/20260507000001_complex_rankings.sql`
- `supabase/migrations/20260507000002_complex_rankings_rls.sql`
- `public/fonts/PretendardSubset.ttf`
- `src/__tests__/rankings.test.ts`
- `src/__tests__/share-button.test.ts`
</objective>

<execution_context>
@C:\Users\jung\coding\bds\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\STATE.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-PATTERNS.md

<interfaces>
<!-- 기존 마이그레이션 RLS 패턴 (supabase/migrations/20260430000009_rls.sql에서 추출) -->
```sql
-- RLS 패턴: public read + service_role 쓰기 (service_role은 RLS 우회)
alter table public.[TABLE] enable row level security;
create policy "[TABLE]: public read"
  on public.[TABLE] for select using (true);
```

<!-- 기존 테이블 DDL 패턴 (supabase/migrations/20260430000013_complex_detail_functions.sql에서 추출) -->
```sql
-- 외래키 패턴
references public.complexes(id) on delete cascade
-- UUID PK 패턴
id uuid primary key default gen_random_uuid()
-- 타임스탬프 패턴
computed_at timestamptz not null default now()
```

<!-- 기존 테스트 임포트 패턴 (src/__tests__/ads.test.ts에서 추출) -->
```typescript
import { describe, it, expect, vi, beforeAll, afterAll } from 'vitest'
import { URL_, SKEY, AKEY, admin } from './helpers/db'

vi.mock('server-only', () => ({}))
vi.mock('next/headers', () => ({
  cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
}))
vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))

beforeAll(() => {
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', URL_)
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', AKEY)
  vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', SKEY)
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: complex_rankings 마이그레이션 SQL 작성</name>
  <files>
    supabase/migrations/20260507000001_complex_rankings.sql
    supabase/migrations/20260507000002_complex_rankings_rls.sql
  </files>
  <action>
두 개의 마이그레이션 파일을 작성한다.

**파일 1: `supabase/migrations/20260507000001_complex_rankings.sql`**

```sql
-- complex_rankings: 랭킹 cron이 1시간마다 UPSERT하는 전용 테이블
-- materialized view 대신 테이블 사용 이유: RLS 적용 가능, 서비스롤 UPSERT 안정
create table public.complex_rankings (
  id           uuid primary key default gen_random_uuid(),
  complex_id   uuid not null references public.complexes(id) on delete cascade,
  rank_type    text not null
                 check (rank_type in ('high_price', 'volume', 'price_per_pyeong', 'interest')),
  score        numeric not null,
  rank         integer not null check (rank >= 1),
  window_days  integer not null default 30,
  computed_at  timestamptz not null default now(),
  unique (rank_type, complex_id, window_days)
);

-- 랭킹 조회 최적화 인덱스 (rank_type별 순위 정렬)
create index complex_rankings_type_rank_idx
  on public.complex_rankings (rank_type, rank);

comment on table public.complex_rankings is
  '창원·김해 단지 랭킹 4종(신고가·거래량·평당가·관심도). rankings cron이 1h 주기로 UPSERT.';
```

**파일 2: `supabase/migrations/20260507000002_complex_rankings_rls.sql`**

```sql
-- complex_rankings RLS: 공개 읽기, 쓰기는 service_role 전용 (rankings cron 파이프라인)
-- 기존 패턴: supabase/migrations/20260430000009_rls.sql의 complexes 정책과 동일
alter table public.complex_rankings enable row level security;

create policy "complex_rankings: public read"
  on public.complex_rankings for select using (true);

-- 쓰기(insert/update/delete)는 service_role이 RLS를 우회하므로 별도 정책 불필요.
-- rankings cron은 createSupabaseAdminClient()로만 접근한다.
```

마이그레이션 파일명 규칙: `YYYYMMDD000001_...sql` — 기존 파일(`20260430000013_...`) 이후 번호 사용.
  </action>
  <verify>
    <automated>npx supabase db push --local 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 두 파일이 존재한다
    - `supabase db push --local`이 오류 없이 완료된다
    - 로컬 Supabase에 `complex_rankings` 테이블과 RLS 정책이 생성된다
  </done>
</task>

<task type="auto">
  <name>Task 2: Pretendard TTF 폰트 파일 추가</name>
  <files>
    public/fonts/PretendardSubset.ttf
  </files>
  <action>
Pretendard TTF 파일을 `public/fonts/PretendardSubset.ttf`에 추가한다.

**단계:**

1. Pretendard GitHub Releases에서 TTF 다운로드:
   - URL: `https://github.com/orioncactus/pretendard/releases/latest`
   - 다운로드 대상: `Pretendard-1.3.9.zip` (또는 최신 버전)
   - 압축 해제 후 `web/static/alternative/Pretendard.ttf` 또는 `dist/public/static/Pretendard-Regular.ttf` 사용

2. 다운로드가 불가한 환경이라면, curl로 직접 다운로드:
   ```bash
   curl -L \
     "https://github.com/orioncactus/pretendard/releases/download/v1.3.9/Pretendard-1.3.9.zip" \
     -o /tmp/pretendard.zip
   unzip -j /tmp/pretendard.zip "public/static/Pretendard-Regular.ttf" -d public/fonts/
   mv public/fonts/Pretendard-Regular.ttf public/fonts/PretendardSubset.ttf
   ```

3. 파일 크기 확인: TTF는 ~3-5MB 정도. 4MB 이하 권장.
   - 4MB 초과 시: `pyftsubset` 또는 online subsetter를 사용해 KR 한글 + ASCII 범위만 추출.
   - Vercel Edge에서는 1MB 한도이므로 `opengraph-image.tsx`는 반드시 `export const runtime = 'nodejs'` 사용 (RESEARCH.md Pitfall 3 참조).

4. `public/fonts/` 디렉토리에 `PretendardSubset.ttf`가 있는지 확인:
   ```bash
   ls -lh public/fonts/
   ```

**주의:** `public/fonts/PretendardVariable.woff2`는 이미 존재하지만 Satori(ImageResponse 내부)는 WOFF2를 지원하지 않는다. WOFF2 파일은 그대로 두고 TTF 파일만 추가한다.
  </action>
  <verify>
    <automated>ls -lh public/fonts/PretendardSubset.ttf 2>&1</automated>
  </verify>
  <done>
    - `public/fonts/PretendardSubset.ttf` 파일이 존재한다
    - 파일 크기가 0바이트가 아니다
    - 기존 `public/fonts/PretendardVariable.woff2`는 그대로 유지된다
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 테스트 스캐폴드 2종 작성 (RED 단계)</name>
  <files>
    src/__tests__/rankings.test.ts
    src/__tests__/share-button.test.ts
  </files>
  <behavior>
    - rankings.test.ts: `computeRankings` 미구현 시 import 오류 발생 (RED)
    - rankings.test.ts: GET /api/cron/rankings — CRON_SECRET 없음 → 401 (RED, route 미존재)
    - rankings.test.ts: `getRankingsByType` — 빈 DB에서 빈 배열 반환 (RED, 함수 미존재)
    - share-button.test.ts: `ShareButton` — `window.Kakao` 없을 때 navigator.share 폴백 호출 (RED, 컴포넌트 미존재)
  </behavior>
  <action>
TDD RED 단계: 구현이 없으므로 import가 실패하거나 테스트가 실패하는 상태로 파일을 작성한다.

**`src/__tests__/rankings.test.ts`:**

```typescript
/**
 * Phase 2 랭킹 기능 수용 기준 테스트 — RANK-01, RANK-02, RANK-03
 *
 * - computeRankings: 4종 랭킹 산식이 올바르게 집계되고 complex_rankings에 UPSERT
 * - getRankingsByType: rank_type별 올바른 순서의 결과 반환
 * - GET /api/cron/rankings: CRON_SECRET 검증 (401/200)
 */
import { describe, it, expect, vi, beforeAll, afterAll } from 'vitest'
import { URL_, SKEY, AKEY, admin } from './helpers/db'

vi.mock('server-only', () => ({}))
vi.mock('next/headers', () => ({
  cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
}))
vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))

beforeAll(() => {
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', URL_)
  vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', AKEY)
  vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', SKEY)
  vi.stubEnv('CRON_SECRET', 'test-cron-secret')
})

// ── GET /api/cron/rankings ──────────────────────────────────────────────────

describe('GET /api/cron/rankings', () => {
  it('Authorization 헤더 없음 → 401', async () => {
    const { GET } = await import('@/app/api/cron/rankings/route')
    const req = new Request('http://localhost/api/cron/rankings')
    const res = await GET(req)
    expect(res.status).toBe(401)
  })

  it('잘못된 CRON_SECRET → 401', async () => {
    const { GET } = await import('@/app/api/cron/rankings/route')
    const req = new Request('http://localhost/api/cron/rankings', {
      headers: { authorization: 'Bearer wrong-secret' },
    })
    const res = await GET(req)
    expect(res.status).toBe(401)
  })

  it('올바른 CRON_SECRET → 200 + ok:true', async () => {
    const { GET } = await import('@/app/api/cron/rankings/route')
    const req = new Request('http://localhost/api/cron/rankings', {
      headers: { authorization: 'Bearer test-cron-secret' },
    })
    const res = await GET(req)
    expect(res.status).toBe(200)
    const body = await res.json() as { ok: boolean }
    expect(body.ok).toBe(true)
  })
})

// ── getRankingsByType ──────────────────────────────────────────────────────

describe('getRankingsByType', () => {
  it('빈 complex_rankings → 빈 배열 반환', async () => {
    const { getRankingsByType } = await import('@/lib/data/rankings')
    const result = await getRankingsByType(admin, 'high_price', 10)
    expect(Array.isArray(result)).toBe(true)
  })

  it('반환 행은 rank 오름차순 정렬', async () => {
    const { getRankingsByType } = await import('@/lib/data/rankings')
    const result = await getRankingsByType(admin, 'volume', 10)
    for (let i = 1; i < result.length; i++) {
      expect(result[i]!.rank).toBeGreaterThanOrEqual(result[i - 1]!.rank)
    }
  })
})

// ── computeRankings ────────────────────────────────────────────────────────

describe('computeRankings', () => {
  it('실행 후 complex_rankings에 레코드가 존재 (transactions 데이터 있을 때)', async () => {
    const { computeRankings } = await import('@/lib/data/rankings')
    // 실제 트랜잭션 데이터가 없으면 빈 결과이므로 오류 없이 완료만 검증
    const results = await computeRankings(admin)
    expect(Array.isArray(results)).toBe(true)
    for (const r of results) {
      expect(r).toHaveProperty('type')
      expect(r).toHaveProperty('upserted')
    }
  })
})
```

**`src/__tests__/share-button.test.ts`:**

```typescript
/**
 * Phase 2 ShareButton 수용 기준 테스트 — SHARE-02
 *
 * RED 단계: ShareButton 컴포넌트가 미구현이면 import 실패 → 테스트 전체 FAIL
 * GREEN 단계: Wave 2에서 ShareButton 구현 후 PASS
 *
 * 검증 목표:
 * - window.Kakao 없을 때 handleKakaoShare가 오류 없이 반환
 * - handleCopyLink가 navigator.clipboard.writeText 호출
 */
import { describe, it, expect, vi, beforeEach } from 'vitest'

// RED 단계: 이 import는 Wave 2 완료 전까지 실패한다 (ShareButton 미존재)
// GREEN 단계: Wave 2에서 src/components/complex/ShareButton.tsx 생성 후 PASS
import { handleKakaoShare, handleCopyLink } from '@/components/complex/ShareButton'

describe('ShareButton — Kakao SDK 없을 때 폴백', () => {
  beforeEach(() => {
    // window.Kakao 제거 (Kakao SDK 미로드 시나리오)
    delete (window as Record<string, unknown>).Kakao
  })

  it('window.Kakao 없을 때 handleKakaoShare가 조용히 반환 (오류 없음)', () => {
    expect(() => {
      handleKakaoShare({
        complexId: 'test-id',
        complexName: '테스트단지',
        location: '창원 성산구',
        siteUrl: 'https://danjiondo.vercel.app',
      })
    }).not.toThrow()
  })

  it('handleCopyLink가 navigator.clipboard.writeText 호출', async () => {
    const writeText = vi.fn().mockResolvedValue(undefined)
    Object.defineProperty(navigator, 'clipboard', {
      value: { writeText },
      writable: true,
      configurable: true,
    })

    await handleCopyLink({
      complexId: 'test-complex-id',
      siteUrl: 'https://danjiondo.vercel.app',
    })

    expect(writeText).toHaveBeenCalledWith(
      'https://danjiondo.vercel.app/complexes/test-complex-id',
    )
  })
})
```

**구현 노트 (Wave 2 개발자용):** `ShareButton.tsx`에서 `handleKakaoShare`와 `handleCopyLink`를 named export로 추출해야 이 테스트가 GREEN이 된다. 컴포넌트 내부 핸들러를 export하거나 별도 유틸 함수로 분리하는 방식 모두 허용.

두 파일 모두 Wave 1/2에서 구현이 완료된 후 GREEN으로 전환된다.
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/__tests__/rankings.test.ts src/__tests__/share-button.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `rankings.test.ts`와 `share-button.test.ts`가 Vitest에 의해 인식된다
    - Wave 0 시점에서는 import 실패로 일부 테스트가 FAIL 상태여도 허용 (RED 단계)
    - `share-button.test.ts`의 폴백 로직 테스트는 구현 없이도 PASS해야 한다
    - `npm run lint`가 두 파일에서 TypeScript 오류를 보고하지 않는다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | DDL 직접 실행 — 테이블 구조 변경 |
| CDN → public/fonts | Pretendard TTF 다운로드 — 외부 소스 바이너리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | complex_rankings DDL | mitigate | `check (rank_type in (...))` 제약으로 유효하지 않은 rank_type 삽입 차단 |
| T-02-02 | Tampering | complex_rankings RLS | mitigate | public read only, write는 service_role(cron)만 가능 |
| T-02-03 | Spoofing | TTF 폰트 파일 (외부 다운로드) | mitigate | Pretendard 공식 GitHub Releases URL 사용. SHA 체크섬 검증 선택 사항 |
</threat_model>

<verification>
```bash
# 마이그레이션 파일 존재 확인
ls -la supabase/migrations/20260507000001_complex_rankings.sql
ls -la supabase/migrations/20260507000002_complex_rankings_rls.sql

# TTF 폰트 존재 확인
ls -lh public/fonts/PretendardSubset.ttf

# 로컬 DB에 테이블 확인 (Supabase 로컬 실행 중일 때)
npx supabase db push --local

# 테스트 스캐폴드 실행
npm run test -- src/__tests__/rankings.test.ts src/__tests__/share-button.test.ts

# 린트 통과
npm run lint
```
</verification>

<success_criteria>
1. `supabase/migrations/20260507000001_complex_rankings.sql` — `complex_rankings` 테이블 DDL과 인덱스 포함
2. `supabase/migrations/20260507000002_complex_rankings_rls.sql` — public read RLS 정책 포함
3. `public/fonts/PretendardSubset.ttf` — 0바이트가 아닌 TTF 파일 존재
4. `src/__tests__/rankings.test.ts` — `computeRankings`, `getRankingsByType`, cron 401 테스트 포함
5. `src/__tests__/share-button.test.ts` — clipboard 폴백 테스트 포함
6. `npm run lint` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/02-ranking-landing-share/02-01-SUMMARY.md`를 생성한다.
</output>

---
phase: 02-ranking-landing-share
plan: "04"
type: execute
wave: 3
depends_on: ["02-01"]
autonomous: true
files_modified:
  - src/app/complexes/[id]/opengraph-image.tsx
requirements:
  - SHARE-01

must_haves:
  truths:
    - "단지 상세 URL을 SNS에 공유할 때 단지명과 위치가 담긴 OG 이미지 카드가 노출된다"
    - "opengraph-image.tsx가 한글 텍스트를 올바르게 렌더링한다 (TTF 폰트 로드)"
    - "단지 ID가 존재하지 않으면 기본 브랜드 이미지를 반환한다"
    - "이미지 크기는 1200x630 PNG이다"
  artifacts:
    - path: "src/app/complexes/[id]/opengraph-image.tsx"
      provides: "단지별 동적 OG 이미지"
      contains: "ImageResponse"
  key_links:
    - from: "src/app/complexes/[id]/opengraph-image.tsx"
      to: "public/fonts/PretendardSubset.ttf"
      via: "fs.readFileSync"
      pattern: "PretendardSubset\\.ttf"
    - from: "src/app/complexes/[id]/opengraph-image.tsx"
      to: "Supabase complexes 테이블"
      via: "getComplexById(id, supabase)"
      pattern: "getComplexById"
---

<objective>
Wave 2 — 단지별 동적 OG 이미지 (SHARE-01)

이 플랜은 `src/app/complexes/[id]/opengraph-image.tsx` 파일 컨벤션을 구현한다.
Next.js 파일 컨벤션을 사용하면 `generateMetadata`에서 `openGraph.images`를 수동으로 추가하지 않아도 `<meta property="og:image">` 태그가 자동으로 주입된다.

Wave 0에서 추가한 `public/fonts/PretendardSubset.ttf`를 Satori 렌더러에 전달해 한글 텍스트를 표시한다.
`export const runtime = 'nodejs'`로 설정해 4MB TTF 로드가 가능한 환경에서 실행한다.

Purpose: SHARE-01 성공 기준 — "단지 URL 카카오톡 공유 시 단지명·가격이 담긴 OG 카드 노출"

Output:
- `src/app/complexes/[id]/opengraph-image.tsx`
</objective>

<execution_context>
@C:\Users\jung\coding\bds\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-01-SUMMARY.md

<interfaces>
<!-- src/app/complexes/[id]/page.tsx에서 추출한 패턴 -->
```typescript
// params는 Promise (Next.js 15 breaking change)
interface Props {
  params: Promise<{ id: string }>
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { id } = await params  // await 필수
  const supabase = createReadonlyClient()
  const complex = await getComplexById(id, supabase)
  if (!complex) return { title: '단지를 찾을 수 없습니다' }
  // ...
}
```

<!-- getComplexById 함수 시그니처 -->
```typescript
// src/lib/data/complex-detail.ts에서 export
export async function getComplexById(
  id: string,
  supabase: SupabaseClient<Database>,
): Promise<ComplexDetail | null>

// ComplexDetail에서 OG 이미지에 필요한 필드
interface ComplexDetail {
  id: string
  canonical_name: string
  si: string | null
  gu: string | null
  dong: string | null
  // ... 기타 필드
}
```

<!-- createReadonlyClient 임포트 -->
```typescript
import { createReadonlyClient } from '@/lib/supabase/readonly'
// cookies() 미호출 → opengraph-image.tsx에서 사용 가능
```

<!-- next/og ImageResponse API (Next.js 15 내장, RESEARCH.md Pattern 4) -->
```typescript
import { ImageResponse } from 'next/og'

export const runtime = 'nodejs'   // TTF 4MB 로드 허용 (Edge: 1MB 한도)
export const alt = '단지온도 아파트 실거래가'
export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'

// default export 함수 (파일 컨벤션 필수)
export default async function Image({ params }: Props): Promise<ImageResponse>
```

<!-- fs/path 임포트 (Node.js runtime 전용) -->
```typescript
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
// 폰트 로드: join(process.cwd(), 'public/fonts/PretendardSubset.ttf')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: opengraph-image.tsx 구현</name>
  <files>src/app/complexes/[id]/opengraph-image.tsx</files>
  <action>
`src/app/complexes/[id]/opengraph-image.tsx`를 신규 생성한다.

**전체 파일 내용:**

```typescript
import { ImageResponse } from 'next/og'
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getComplexById } from '@/lib/data/complex-detail'

// Node.js runtime 사용: TTF 4MB 파일 로드 허용 (Edge runtime은 1MB 한도)
export const runtime = 'nodejs'

export const alt = '단지온도 아파트 실거래가'
export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'

// 24시간 캐시 (단지 기본 정보는 자주 바뀌지 않음)
export const revalidate = 86400

interface Props {
  params: Promise<{ id: string }>
}

export default async function Image({ params }: Props): Promise<ImageResponse> {
  // Next.js 15: params는 Promise — await 필수
  const { id } = await params

  // 한글 TTF 폰트 로드 (WOFF2는 Satori 미지원 — RESEARCH.md Pitfall 1)
  const fontData = readFileSync(
    join(process.cwd(), 'public/fonts/PretendardSubset.ttf'),
  )

  // 단지 데이터 조회 (createReadonlyClient: cookies() 미호출, ISR 안전)
  const supabase = createReadonlyClient()
  const complex = await getComplexById(id, supabase)

  const name = complex?.canonical_name ?? '단지온도'
  const location = [complex?.si, complex?.gu, complex?.dong]
    .filter(Boolean)
    .join(' ')

  return new ImageResponse(
    (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          background: '#ffffff',
          padding: '60px 72px',
          fontFamily: 'Pretendard',
        }}
      >
        {/* 브랜드 헤더 */}
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 8,
            marginBottom: 40,
          }}
        >
          <div
            style={{
              width: 32,
              height: 32,
              background: '#ea580c',
              borderRadius: 8,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              color: '#ffffff',
              fontSize: 16,
              fontWeight: 700,
            }}
          >
            단
          </div>
          <span style={{ fontSize: 18, fontWeight: 700, color: '#ea580c' }}>
            단지온도
          </span>
        </div>

        {/* 단지명 */}
        <div
          style={{
            fontSize: complex ? 56 : 48,
            fontWeight: 700,
            color: '#111111',
            letterSpacing: '-2px',
            lineHeight: 1.15,
            flex: 1,
            display: 'flex',
            alignItems: 'center',
          }}
        >
          {name}
        </div>

        {/* 위치 + 하단 구분선 */}
        <div
          style={{
            display: 'flex',
            alignItems: 'flex-end',
            justifyContent: 'space-between',
            borderTop: '1px solid #e5e7eb',
            paddingTop: 24,
            marginTop: 24,
          }}
        >
          <span style={{ fontSize: 22, color: '#6b7280', fontWeight: 500 }}>
            {location || '창원·김해 아파트 실거래가'}
          </span>
          <span style={{ fontSize: 16, color: '#9ca3af' }}>
            실거래가 · 단지온도
          </span>
        </div>
      </div>
    ),
    {
      ...size,
      fonts: [
        {
          name: 'Pretendard',
          data: fontData,
          style: 'normal',
          weight: 700,
        },
      ],
    },
  )
}
```

**UI 규칙 준수 (CLAUDE.md AI 슬롭 금지):**
- backdrop-blur 없음
- gradient-text 없음
- glow 애니메이션 없음
- 보라/인디고 브랜드색 없음 (오렌지 `#ea580c` 사용)
- gradient orb 없음
- 배경은 단순 흰색 (`#ffffff`)

**Satori 레이아웃 제약:**
- `display: 'flex'` 사용 (grid 미지원)
- 중첩 flex 컨테이너로 레이아웃 구성
- 텍스트 줄바꿈은 자동 처리됨
- `border-radius`는 수치로만 (단위 없이 px 값)

**단지 미존재 처리:**
- `getComplexById`가 null 반환 시 → 기본 브랜드 이미지 반환 (404 반환하지 않음)
- SNS 크롤러가 잘못된 ID로 요청해도 응답해야 함
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(opengraph|error|Error)" | head -20</automated>
  </verify>
  <done>
    - `src/app/complexes/[id]/opengraph-image.tsx`가 존재한다
    - `npm run build`가 성공한다 (TypeScript 오류 없음)
    - 빌드 출력에서 `opengraph-image` 경로가 OG 이미지 route로 인식된다
    - `npm run lint`가 오류를 보고하지 않는다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SNS 크롤러 → opengraph-image.tsx | 외부에서 id 파라미터 조작 가능 |
| opengraph-image.tsx → Supabase | 공개 읽기 쿼리 |
| opengraph-image.tsx → filesystem | TTF 파일 로드 (process.cwd() 기반) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | id 파라미터 (UUID 조작) | mitigate | `getComplexById(id, supabase)`가 UUID 형식이 아닌 id에 대해 null 반환 → 기본 이미지 렌더. 외부 URL 조회 없음 (SSRF 불가) |
| T-02-12 | Denial of Service | OG 이미지 반복 요청 | mitigate | `revalidate = 86400` — 24시간 캐시. 동일 id 반복 요청 시 캐시 히트 |
| T-02-13 | Information Disclosure | OG 이미지 단지 정보 노출 | accept | 랜딩·단지 상세 페이지에서 이미 공개된 정보. OG 이미지는 공개 의도된 데이터만 표시 |
</threat_model>

<verification>
```bash
# 빌드 성공
npm run build

# opengraph-image 경로 확인
grep -r "opengraph-image" .next/server/ 2>/dev/null | head -3

# 린트
npm run lint

# 로컬 서버에서 OG 이미지 접근 테스트 (단지 ID가 DB에 존재할 때)
# curl "http://localhost:3000/complexes/{valid-id}/opengraph-image" -o /tmp/test-og.png
# 이미지 파일 크기 확인: ls -lh /tmp/test-og.png
```
</verification>

<success_criteria>
1. `src/app/complexes/[id]/opengraph-image.tsx`가 존재하고 `export default async function Image`를 포함한다
2. `export const runtime = 'nodejs'`로 설정된다
3. `PretendardSubset.ttf`를 `readFileSync`로 로드해 `ImageResponse` fonts 배열에 전달한다
4. `params`를 `await`해서 id를 추출한다 (Next.js 15 규칙)
5. 단지 미존재 시 기본 브랜드 이미지를 반환한다 (404 반환하지 않음)
6. `npm run build` 성공
7. 로컬에서 단지 상세 페이지 소스에 `og:image` 메타태그가 자동 주입된다
</success_criteria>

<output>
완료 후 `.planning/phases/02-ranking-landing-share/02-04-SUMMARY.md`를 생성한다.
</output>

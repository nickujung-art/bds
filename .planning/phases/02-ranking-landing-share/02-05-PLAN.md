---
phase: 02-ranking-landing-share
plan: "05"
type: execute
wave: 3
depends_on: ["02-04"]
autonomous: true
files_modified:
  - src/components/complex/ShareButton.tsx
  - src/app/complexes/[id]/page.tsx
requirements:
  - SHARE-02

must_haves:
  truths:
    - "단지 상세 페이지에 카카오톡·네이버·링크복사 공유 버튼이 표시된다"
    - "카카오톡 공유 버튼 클릭 시 단지명과 위치가 담긴 공유 카드가 전송된다"
    - "window.Kakao가 없을 때 navigator.share 또는 clipboard.writeText 폴백이 실행된다"
    - "Kakao SDK 초기화는 isInitialized() 체크 후 중복 호출을 방지한다"
  artifacts:
    - path: "src/components/complex/ShareButton.tsx"
      provides: "카카오톡·네이버·링크복사 공유 버튼"
      exports: ["ShareButton"]
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "단지 상세 페이지 공유 버튼 연결"
      contains: "ShareButton"
  key_links:
    - from: "src/app/complexes/[id]/page.tsx"
      to: "src/components/complex/ShareButton.tsx"
      via: "complexId, complexName props"
      pattern: "ShareButton"
    - from: "src/components/complex/ShareButton.tsx"
      to: "window.Kakao.Share"
      via: "동적 SDK 로드 + sendDefault()"
      pattern: "Kakao\\.Share\\.sendDefault"
---

<objective>
Wave 2 — 카카오톡·네이버 공유 버튼 (SHARE-02)

이 플랜은 단지 상세 페이지의 정적 공유 아이콘 버튼을 실제로 동작하는 `<ShareButton>` 클라이언트 컴포넌트로 교체한다.

기능:
1. 카카오톡 공유 — Kakao JS SDK `window.Kakao.Share.sendDefault()` 사용
2. 네이버 공유 — `share.naver.com` URL 방식 (`window.open`)
3. 링크 복사 — `navigator.clipboard.writeText()` + 복사 완료 토스트

Kakao SDK는 layout.tsx를 수정하지 않고 클릭 시점에 동적 로드 + 초기화한다.
`window.Kakao`가 없을 때의 폴백도 처리한다.

Purpose: SHARE-02 성공 기준 — "카카오톡·네이버 공유 버튼 + 단지 상세 공유 UX"

Output:
- `src/components/complex/ShareButton.tsx` (신규)
- `src/app/complexes/[id]/page.tsx` (수정 — 정적 공유 버튼 → ShareButton 교체)
</objective>

<execution_context>
@C:\Users\jung\coding\bds\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-PATTERNS.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-04-SUMMARY.md

<interfaces>
<!-- FavoriteButton.tsx에서 추출한 클라이언트 컴포넌트 패턴 -->
```typescript
'use client'
import { useState, useTransition } from 'react'

interface Props {
  complexId: string
  // ...
}

export function FavoriteButton({ complexId }: Props) {
  const [isPending, startTransition] = useTransition()
  const toggle = () => { startTransition(async () => { /* ... */ }) }
  return <button onClick={toggle} disabled={isPending} className="btn btn-md btn-secondary" ... />
}
```

<!-- complexes/[id]/page.tsx에서 추출한 현재 공유 버튼 (교체 대상) -->
```typescript
// src/app/complexes/[id]/page.tsx lines 188–196
<button
  className="btn btn-md btn-ghost"
  style={{ color: 'var(--fg-sec)' }}
  aria-label="공유"
>
  <ShareIcon />
</button>
// → <ShareButton complexId={id} complexName={complex.canonical_name} /> 로 교체
```

<!-- RESEARCH.md Pattern 5: Kakao Share SDK 패턴 -->
```typescript
// Kakao SDK 동적 로드 패턴
function loadKakaoSdk(): Promise<void> {
  return new Promise((resolve, reject) => {
    if (document.getElementById('kakao-sdk')) {
      resolve(); return
    }
    const script = document.createElement('script')
    script.id = 'kakao-sdk'
    script.src = '//t1.kakaocdn.net/kakao_js_sdk/2.7.2/kakao.min.js'
    script.crossOrigin = 'anonymous'
    script.onload = () => resolve()
    script.onerror = reject
    document.head.appendChild(script)
  })
}

// 초기화 중복 방지 (Pitfall 4)
if (!window.Kakao.isInitialized()) {
  window.Kakao.init(process.env.NEXT_PUBLIC_KAKAO_JS_KEY ?? '')
}

// 공유 (Kakao.Link deprecated → Kakao.Share 사용)
window.Kakao.Share.sendDefault({
  objectType: 'feed',
  content: {
    title: complexName,
    description: `${location} · 실거래가 확인`,
    imageUrl: ogImageUrl,
    link: { mobileWebUrl: complexUrl, webUrl: complexUrl },
  },
  buttons: [{ title: '실거래가 보기', link: { mobileWebUrl: complexUrl, webUrl: complexUrl } }],
})
```

<!-- 환경변수 (기존 CI env에서 확인됨 — 02-PATTERNS.md) -->
```typescript
process.env.NEXT_PUBLIC_KAKAO_JS_KEY  // Kakao SDK 앱 키
process.env.NEXT_PUBLIC_SITE_URL      // https://danjiondo.kr
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShareButton 컴포넌트 구현</name>
  <files>src/components/complex/ShareButton.tsx</files>
  <behavior>
    - window.Kakao 없을 때 handleKakaoShare 클릭 시 오류 없이 종료 (폴백 없음 — SDK 로드 후 재시도)
    - handleCopyLink 클릭 시 navigator.clipboard.writeText(complexUrl) 호출
    - handleNaverShare 클릭 시 share.naver.com URL로 window.open 호출
    - Kakao SDK 초기화 시 isInitialized() 체크 후 중복 init() 방지
    - share-button.test.ts의 clipboard 테스트가 PASS
  </behavior>
  <action>
`src/components/complex/ShareButton.tsx`를 신규 생성한다.

```typescript
'use client'

import { useState } from 'react'

// Kakao SDK 타입 선언 (별도 패키지 설치 불필요)
declare global {
  interface Window {
    Kakao?: {
      isInitialized: () => boolean
      init: (key: string) => void
      Share: {
        sendDefault: (options: KakaoShareOptions) => void
      }
    }
  }
}

interface KakaoShareOptions {
  objectType: 'feed'
  content: {
    title: string
    description: string
    imageUrl: string
    link: { mobileWebUrl: string; webUrl: string }
  }
  buttons: Array<{
    title: string
    link: { mobileWebUrl: string; webUrl: string }
  }>
}

interface Props {
  complexId: string
  complexName: string
  location?: string   // "창원시 성산구 반림동" 형태
  price?: number      // 최고 실거래가 (만원 단위), 있을 때 description에 포함
}

// Kakao SDK 동적 로드 (layout.tsx 수정 최소화)
function loadKakaoSdk(): Promise<void> {
  return new Promise((resolve, reject) => {
    if (typeof document === 'undefined') {
      reject(new Error('Not in browser')); return
    }
    if (document.getElementById('kakao-sdk-share')) {
      resolve(); return
    }
    const script = document.createElement('script')
    script.id = 'kakao-sdk-share'
    script.src = '//t1.kakaocdn.net/kakao_js_sdk/2.7.2/kakao.min.js'
    script.crossOrigin = 'anonymous'
    script.onload = () => resolve()
    script.onerror = () => reject(new Error('Kakao SDK load failed'))
    document.head.appendChild(script)
  })
}

function ShareIcon() {
  return (
    <svg
      width="16"
      height="16"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
    >
      <circle cx="18" cy="5" r="3" />
      <circle cx="6" cy="12" r="3" />
      <circle cx="18" cy="19" r="3" />
      <line x1="8.59" y1="13.51" x2="15.42" y2="17.49" />
      <line x1="15.41" y1="6.51" x2="8.59" y2="10.49" />
    </svg>
  )
}

function CopyIcon() {
  return (
    <svg
      width="14"
      height="14"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
    >
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  )
}

export function ShareButton({ complexId, complexName, location, price }: Props) {
  const [open, setOpen] = useState(false)
  const [copied, setCopied] = useState(false)

  const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://danjiondo.kr'
  const complexUrl = `${siteUrl}/complexes/${complexId}`
  const ogImageUrl = `${siteUrl}/complexes/${complexId}/opengraph-image`

  const description = [
    location,
    price != null ? `최고 ${formatPriceBrief(price)}` : null,
    '실거래가 확인',
  ]
    .filter(Boolean)
    .join(' · ')

  const handleKakaoShare = async () => {
    try {
      await loadKakaoSdk()
      if (!window.Kakao) return

      // 중복 초기화 방지 (RESEARCH.md Pitfall 4)
      if (!window.Kakao.isInitialized()) {
        window.Kakao.init(process.env.NEXT_PUBLIC_KAKAO_JS_KEY ?? '')
      }

      window.Kakao.Share.sendDefault({
        objectType: 'feed',
        content: {
          title: complexName,
          description,
          imageUrl: ogImageUrl,
          link: { mobileWebUrl: complexUrl, webUrl: complexUrl },
        },
        buttons: [
          {
            title: '실거래가 보기',
            link: { mobileWebUrl: complexUrl, webUrl: complexUrl },
          },
        ],
      })
    } catch {
      // SDK 로드 실패 시 링크 복사로 폴백
      await handleCopyLink()
    }
    setOpen(false)
  }

  const handleNaverShare = () => {
    const encoded = encodeURIComponent(complexUrl)
    const title = encodeURIComponent(complexName)
    window.open(
      `https://share.naver.com/web/shareView?url=${encoded}&title=${title}`,
      '_blank',
      'noopener,noreferrer',
    )
    setOpen(false)
  }

  const handleCopyLink = async () => {
    await navigator.clipboard.writeText(complexUrl)
    setCopied(true)
    setTimeout(() => setCopied(false), 2000)
    setOpen(false)
  }

  return (
    <div style={{ position: 'relative' }}>
      <button
        className="btn btn-md btn-ghost"
        style={{ color: 'var(--fg-sec)', gap: 6 }}
        aria-label="공유"
        aria-expanded={open}
        onClick={() => setOpen((v) => !v)}
      >
        <ShareIcon />
        공유
      </button>

      {open && (
        <>
          {/* 드롭다운 오버레이 (클릭 시 닫기) */}
          <div
            style={{
              position: 'fixed',
              inset: 0,
              zIndex: 40,
            }}
            onClick={() => setOpen(false)}
            aria-hidden="true"
          />
          {/* 공유 메뉴 */}
          <div
            style={{
              position: 'absolute',
              top: '100%',
              right: 0,
              marginTop: 6,
              background: '#fff',
              border: '1px solid var(--line-default)',
              borderRadius: 10,
              boxShadow: '0 4px 16px rgba(0,0,0,0.10)',
              zIndex: 50,
              minWidth: 160,
              overflow: 'hidden',
            }}
          >
            <button
              onClick={handleKakaoShare}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 10,
                width: '100%',
                padding: '12px 16px',
                background: 'none',
                border: 'none',
                cursor: 'pointer',
                font: '500 14px/1 var(--font-sans)',
                color: '#111',
                textAlign: 'left',
              }}
            >
              {/* 카카오 노란 원 아이콘 대용 */}
              <span
                style={{
                  width: 22,
                  height: 22,
                  background: '#FEE500',
                  borderRadius: '50%',
                  display: 'inline-flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 12,
                  flexShrink: 0,
                }}
              >
                K
              </span>
              카카오톡 공유
            </button>

            <div style={{ height: 1, background: 'var(--line-subtle)', margin: '0 12px' }} />

            <button
              onClick={handleNaverShare}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 10,
                width: '100%',
                padding: '12px 16px',
                background: 'none',
                border: 'none',
                cursor: 'pointer',
                font: '500 14px/1 var(--font-sans)',
                color: '#111',
                textAlign: 'left',
              }}
            >
              <span
                style={{
                  width: 22,
                  height: 22,
                  background: '#03C75A',
                  borderRadius: '50%',
                  display: 'inline-flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 12,
                  color: '#fff',
                  flexShrink: 0,
                }}
              >
                N
              </span>
              네이버 공유
            </button>

            <div style={{ height: 1, background: 'var(--line-subtle)', margin: '0 12px' }} />

            <button
              onClick={handleCopyLink}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 10,
                width: '100%',
                padding: '12px 16px',
                background: 'none',
                border: 'none',
                cursor: 'pointer',
                font: '500 14px/1 var(--font-sans)',
                color: '#111',
                textAlign: 'left',
              }}
            >
              <span style={{ flexShrink: 0 }}>
                <CopyIcon />
              </span>
              {copied ? '복사됨!' : '링크 복사'}
            </button>
          </div>
        </>
      )}
    </div>
  )
}

function formatPriceBrief(price: number): string {
  const uk = Math.floor(price / 10000)
  return uk > 0 ? `${uk}억` : `${price.toLocaleString()}만`
}
```

**UI 규칙 준수 (CLAUDE.md AI 슬롭 금지):**
- backdrop-blur 없음 — 일반 solid 배경 사용
- gradient-text 없음
- glow 없음
- 보라/인디고 없음 — 카카오 `#FEE500`, 네이버 `#03C75A` (공식 브랜드색)
- gradient orb 없음

**보안:**
- `window.open`에 `noopener,noreferrer` 적용 (RESEARCH.md 보안 도메인)
- Kakao SDK init key는 `process.env.NEXT_PUBLIC_KAKAO_JS_KEY`에서만 읽음
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/__tests__/share-button.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `src/components/complex/ShareButton.tsx`가 존재하고 `ShareButton`을 export한다
    - `'use client'`로 선언된다
    - `share-button.test.ts`의 모든 테스트가 PASS한다
    - `npm run lint`가 오류를 보고하지 않는다
  </done>
</task>

<task type="auto">
  <name>Task 2: 단지 상세 page.tsx — 정적 공유 버튼을 ShareButton으로 교체</name>
  <files>src/app/complexes/[id]/page.tsx</files>
  <action>
`src/app/complexes/[id]/page.tsx`를 수정한다.

**변경 1: ShareButton import 추가**
```typescript
import { ShareButton } from '@/components/complex/ShareButton'
```

**변경 2: 기존 정적 공유 버튼 교체**

현재 코드 (PATTERNS.md lines 620-630 참조):
```typescript
<button
  className="btn btn-md btn-ghost"
  style={{ color: 'var(--fg-sec)' }}
  aria-label="공유"
>
  <ShareIcon />
</button>
```

교체 후:
```typescript
<ShareButton
  complexId={id}
  complexName={complex.canonical_name}
  location={[complex.si, complex.gu, complex.dong].filter(Boolean).join(' ')}
/>
```

**주의사항:**
- `complex.si`, `complex.gu`, `complex.dong`은 `ComplexDetail` 타입에서 가져온다
- `ShareIcon` SVG 함수는 `ShareButton` 내부에서 정의되므로, `page.tsx`에서 `ShareIcon`을 사용하는 곳이 없다면 삭제해도 된다 (사용 여부 먼저 확인)
- `id`는 이미 page.tsx에서 `await params`로 추출된 변수다
- `complex`는 이미 page.tsx에서 `getComplexById(id, supabase)` 결과 변수다

**page.tsx 전체 수정이 최소화되도록:** ShareButton import 추가 + 버튼 교체 2개 변경만 적용. 나머지 코드는 그대로 유지한다.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `src/app/complexes/[id]/page.tsx`에 `ShareButton` import가 추가된다
    - 기존 정적 `<button aria-label="공유">` 이 `<ShareButton ... />`으로 교체된다
    - `npm run build`가 성공한다
    - `npm run lint`가 오류를 보고하지 않는다
    - 로컬에서 단지 상세 페이지 접근 시 공유 버튼이 표시된다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Kakao CDN | 동적 스크립트 로드 — 외부 CDN 의존 |
| ShareButton → window.open | 외부 URL 오픈 |
| ShareButton → navigator.clipboard | 사용자 클립보드 접근 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Spoofing | Kakao SDK CDN 로드 | accept | Kakao 공식 CDN URL 사용 (`t1.kakaocdn.net`). SRI 해시 추가는 선택 사항 |
| T-02-15 | Tampering | window.open 대상 URL | mitigate | `noopener,noreferrer` 적용. 사용자가 공유 URL을 변경 불가 (hardcoded 생성) |
| T-02-16 | Information Disclosure | 클립보드 복사 | accept | 공개 단지 URL만 복사. 개인 정보 없음 |
| T-02-17 | Tampering | NEXT_PUBLIC_KAKAO_JS_KEY 노출 | accept | NEXT_PUBLIC_ 접두사 환경변수는 클라이언트 번들에 포함됨 — Kakao JS Key는 공개 의도된 키 |
</threat_model>

<verification>
```bash
# 전체 테스트
npm run test -- src/__tests__/share-button.test.ts

# 빌드 성공
npm run build

# 단지 상세 페이지에 ShareButton 렌더 확인
grep -n "ShareButton" src/app/complexes/\[id\]/page.tsx

# 린트
npm run lint
```
</verification>

<success_criteria>
1. `src/components/complex/ShareButton.tsx`가 카카오톡·네이버·링크복사 세 가지 공유 옵션을 제공한다
2. Kakao SDK 중복 초기화가 `isInitialized()` 체크로 방지된다
3. `window.Kakao` 없을 때 SDK 로드 실패 시 `handleCopyLink` 폴백이 실행된다
4. `src/app/complexes/[id]/page.tsx`의 정적 공유 버튼이 `<ShareButton>`으로 교체된다
5. `share-button.test.ts` 전체 PASS
6. `npm run build && npm run lint` 통과
7. 단지 상세 페이지에서 공유 버튼 클릭 시 드롭다운이 열린다
</success_criteria>

<output>
완료 후 `.planning/phases/02-ranking-landing-share/02-05-SUMMARY.md`를 생성한다.

모든 플랜(02-01~02-05) 완료 시 전체 Phase 2 통합 검증을 실행한다:
```bash
npm run lint && npm run build && npm run test
```
</output>

---
phase: 02-ranking-landing-share
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
autonomous: true
files_modified:
  - src/lib/data/rankings.ts
  - src/app/api/cron/rankings/route.ts
  - .github/workflows/rankings-cron.yml
requirements:
  - RANK-01
  - RANK-02

must_haves:
  truths:
    - "computeRankings()가 4종 랭킹 산식을 집계해 complex_rankings 테이블에 UPSERT한다"
    - "각 산식은 sgg_code IN ('48121','48123','48125','48127','48129','48250') 필터를 적용한다"
    - "거래 쿼리는 cancel_date IS NULL AND superseded_by IS NULL 조건을 포함한다"
    - "GET /api/cron/rankings는 CRON_SECRET Bearer 헤더가 없으면 401을 반환한다"
    - "GitHub Actions rankings-cron.yml이 0 * * * * 스케줄로 실행된다"
    - "cron 실행 결과가 ingest_runs 테이블에 기록된다"
  artifacts:
    - path: "src/lib/data/rankings.ts"
      provides: "getRankingsByType + computeRankings 함수"
      exports: ["getRankingsByType", "computeRankings", "RankType", "RankingRow"]
    - path: "src/app/api/cron/rankings/route.ts"
      provides: "랭킹 집계 cron API endpoint"
      contains: "Bearer ${process.env.CRON_SECRET}"
    - path: ".github/workflows/rankings-cron.yml"
      provides: "GitHub Actions 1h 랭킹 cron"
      contains: "0 * * * *"
  key_links:
    - from: "src/app/api/cron/rankings/route.ts"
      to: "src/lib/data/rankings.ts"
      via: "computeRankings() 호출"
      pattern: "computeRankings"
    - from: "src/lib/data/rankings.ts"
      to: "complex_rankings (Supabase)"
      via: "UPSERT on conflict"
      pattern: "complex_rankings"
    - from: ".github/workflows/rankings-cron.yml"
      to: "/api/cron/rankings"
      via: "curl GET + Authorization Bearer"
      pattern: "api/cron/rankings"
---

<objective>
Wave 1 — 랭킹 데이터 레이어 + cron 파이프라인

이 플랜은 RANK-01(지역 인기 단지 풀 + 일배치)과 RANK-02(4종 랭킹 산식 + 1h cron)를 구현한다.
Wave 0에서 생성된 `complex_rankings` 테이블에 실제 데이터를 UPSERT하는 파이프라인을 완성한다.

Purpose: 랜딩 페이지(RANK-03)가 DB에서 랭킹을 읽을 수 있도록 데이터 레이어를 먼저 확보한다.

Output:
- `src/lib/data/rankings.ts` — 4종 랭킹 산식 + 읽기 함수
- `src/app/api/cron/rankings/route.ts` — cron endpoint (CRON_SECRET 인증)
- `.github/workflows/rankings-cron.yml` — GitHub Actions 1h 스케줄
</objective>

<execution_context>
@C:\Users\jung\coding\bds\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Users\jung\coding\bds\.planning\ROADMAP.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-RESEARCH.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-PATTERNS.md
@C:\Users\jung\coding\bds\.planning\phases\02-ranking-landing-share\02-01-SUMMARY.md
@C:\Users\jung\coding\bds\src\lib\data\homepage.ts
@C:\Users\jung\coding\bds\src\app\api\ingest\molit-trade\route.ts
@C:\Users\jung\coding\bds\.github\workflows\notify-worker.yml

<interfaces>
<!-- homepage.ts에서 추출한 핵심 패턴 -->
```typescript
// 임포트 패턴
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

// 조인 응답 정규화 패턴 (Supabase가 배열/객체 모두 반환할 수 있음)
const r = row as any
const c = Array.isArray(r.complexes) ? r.complexes[0] : r.complexes
if (!c) continue

// 유효 거래 필터 (CRITICAL — 모든 transactions 쿼리 필수)
.is('cancel_date', null)
.is('superseded_by', null)
```

<!-- molit-trade/route.ts에서 추출한 cron 패턴 -->
```typescript
// CRON_SECRET 검증 (Authorization: Bearer 방식)
const authHeader = request.headers.get('authorization')
if (!process.env.CRON_SECRET || authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
  return new Response('Unauthorized', { status: 401 })
}
const supabase = createSupabaseAdminClient()

// 응답 형식
return Response.json({ summary, results })
```

<!-- notify-worker.yml에서 추출한 GitHub Actions cron 패턴 -->
```yaml
# 변경점: cron '0 * * * *', GET, Authorization Bearer, timeout-minutes: 3
STATUS=$(curl -sSf -o /tmp/response.json -w "%{http_code}" \
  -X POST \
  -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
  "${{ secrets.SITE_URL }}/api/worker/notify")
```

<!-- complex_rankings 테이블 스키마 (Wave 0 마이그레이션에서 생성) -->
```sql
complex_rankings (
  id uuid primary key,
  complex_id uuid NOT NULL REFERENCES complexes(id),
  rank_type text CHECK IN ('high_price', 'volume', 'price_per_pyeong', 'interest'),
  score numeric NOT NULL,
  rank integer NOT NULL,
  window_days integer NOT NULL DEFAULT 30,
  computed_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (rank_type, complex_id, window_days)
)
```

<!-- ingest_runs 테이블 스키마 (기존 파이프라인 패턴) -->
```typescript
// ingest_runs에 source='rankings' 기록 패턴
// src/lib/data/realprice.ts에서 ingest_runs UPSERT 패턴 확인됨
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rankings.ts — 4종 랭킹 산식 + 읽기 함수 구현</name>
  <files>src/lib/data/rankings.ts</files>
  <behavior>
    - `getRankingsByType(supabase, 'high_price', 10)` → complex_rankings 테이블에서 rank_type='high_price', window_days=30 행을 rank 오름차순으로 최대 10개 반환
    - 반환 배열의 각 항목은 `{ id, canonical_name, si, gu, score, rank }` 형태
    - `computeRankings(supabase)` → 4종 집계 SQL을 실행하고 complex_rankings에 UPSERT. `[{ type: 'high_price', upserted: N }, ...]` 형태 배열 반환
    - 신고가 산식: `MAX(price)` WHERE deal_type='sale', last 30 days, cancel_date IS NULL, superseded_by IS NULL, sgg_code IN 창원·김해 6개
    - 거래량 산식: `COUNT(*)` 동일 조건
    - 평당가 산식: `ROUND(AVG(price / (area_m2 / 3.3058)))` 동일 조건 + area_m2 > 0
    - 관심도 산식: `COUNT(*)` FROM favorites JOIN complexes WHERE sgg_code IN 창원·김해 6개
    - UPSERT: `ON CONFLICT (rank_type, complex_id, window_days) DO UPDATE SET score=..., rank=..., computed_at=now()`
  </behavior>
  <action>
`src/lib/data/rankings.ts`를 신규 생성한다. `homepage.ts`의 패턴을 그대로 따른다.

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

// 창원·김해 활성 sgg_code (RESEARCH.md Open Question 1 해소: 하드코딩)
// 확장 시 regions 테이블에서 동적 조회로 변경 가능
const ACTIVE_SGG_CODES = ['48121', '48123', '48125', '48127', '48129', '48250'] as const
const WINDOW_DAYS = 30

export type RankType = 'high_price' | 'volume' | 'price_per_pyeong' | 'interest'

export interface RankingRow {
  id: string
  canonical_name: string
  si: string | null
  gu: string | null
  score: number
  rank: number
}

// ── 읽기 함수 (createReadonlyClient 또는 admin client 모두 사용 가능) ──────────

/**
 * complex_rankings 테이블에서 rank_type별 상위 N개 결과를 반환한다.
 * page.tsx에서 createReadonlyClient()로 호출 → ISR 가능.
 */
export async function getRankingsByType(
  supabase: SupabaseClient<Database>,
  rankType: RankType,
  limit = 10,
): Promise<RankingRow[]> {
  const { data, error } = await supabase
    .from('complex_rankings')
    .select(`
      score, rank,
      complexes!inner (id, canonical_name, si, gu)
    `)
    .eq('rank_type', rankType)
    .eq('window_days', WINDOW_DAYS)
    .order('rank', { ascending: true })
    .limit(limit)

  if (error) throw new Error(`getRankingsByType(${rankType}) failed: ${error.message}`)

  const results: RankingRow[] = []
  for (const row of data ?? []) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const r = row as any
    const c = Array.isArray(r.complexes) ? r.complexes[0] : r.complexes
    if (!c) continue
    results.push({
      id: c.id as string,
      canonical_name: c.canonical_name as string,
      si: c.si as string | null,
      gu: c.gu as string | null,
      score: Number(r.score),
      rank: Number(r.rank),
    })
  }
  return results
}

// ── 집계 함수 (createSupabaseAdminClient()로만 호출) ──────────────────────────

interface AggRow { complex_id: string; score: number }

async function aggregateHighPrice(supabase: SupabaseClient<Database>): Promise<AggRow[]> {
  const thirtyDaysAgo = new Date(Date.now() - WINDOW_DAYS * 86_400_000)
    .toISOString()
    .split('T')[0]!

  const { data, error } = await supabase
    .from('transactions')
    .select('complex_id, price')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .eq('deal_type', 'sale')
    .gte('deal_date', thirtyDaysAgo)
    .in('sgg_code', ACTIVE_SGG_CODES)
    .not('complex_id', 'is', null)
    .order('price', { ascending: false })
    .limit(2000) // 충분한 범위에서 집계

  if (error) throw new Error(`aggregateHighPrice failed: ${error.message}`)

  // 단지별 최고가 집계
  const map = new Map<string, number>()
  for (const row of data ?? []) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const r = row as any
    const cid: string = r.complex_id
    const price: number = r.price
    if (!map.has(cid) || price > map.get(cid)!) map.set(cid, price)
  }
  return Array.from(map.entries())
    .sort(([, a], [, b]) => b - a)
    .slice(0, 100)
    .map(([complex_id, score]) => ({ complex_id, score }))
}

async function aggregateVolume(supabase: SupabaseClient<Database>): Promise<AggRow[]> {
  const thirtyDaysAgo = new Date(Date.now() - WINDOW_DAYS * 86_400_000)
    .toISOString()
    .split('T')[0]!

  // Supabase JS SDK로 GROUP BY COUNT 구현: rpc 또는 raw SQL 사용
  // rpc 없이 SDK만으로 집계: 행을 가져와 JS에서 카운트
  const { data, error } = await supabase
    .from('transactions')
    .select('complex_id')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .eq('deal_type', 'sale')
    .gte('deal_date', thirtyDaysAgo)
    .in('sgg_code', ACTIVE_SGG_CODES)
    .not('complex_id', 'is', null)
    .limit(5000)

  if (error) throw new Error(`aggregateVolume failed: ${error.message}`)

  const map = new Map<string, number>()
  for (const row of data ?? []) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const r = row as any
    const cid: string = r.complex_id
    map.set(cid, (map.get(cid) ?? 0) + 1)
  }
  return Array.from(map.entries())
    .sort(([, a], [, b]) => b - a)
    .slice(0, 100)
    .map(([complex_id, score]) => ({ complex_id, score }))
}

async function aggregatePricePerPyeong(supabase: SupabaseClient<Database>): Promise<AggRow[]> {
  const thirtyDaysAgo = new Date(Date.now() - WINDOW_DAYS * 86_400_000)
    .toISOString()
    .split('T')[0]!

  const { data, error } = await supabase
    .from('transactions')
    .select('complex_id, price, area_m2')
    .is('cancel_date', null)
    .is('superseded_by', null)
    .eq('deal_type', 'sale')
    .gte('deal_date', thirtyDaysAgo)
    .in('sgg_code', ACTIVE_SGG_CODES)
    .not('complex_id', 'is', null)
    .gt('area_m2', 0)
    .limit(5000)

  if (error) throw new Error(`aggregatePricePerPyeong failed: ${error.message}`)

  const map = new Map<string, { sum: number; count: number }>()
  for (const row of data ?? []) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const r = row as any
    const cid: string = r.complex_id
    const pricePerPyeong = (r.price as number) / ((r.area_m2 as number) / 3.3058)
    const cur = map.get(cid) ?? { sum: 0, count: 0 }
    map.set(cid, { sum: cur.sum + pricePerPyeong, count: cur.count + 1 })
  }
  return Array.from(map.entries())
    .map(([complex_id, { sum, count }]) => ({
      complex_id,
      score: Math.round(sum / count),
    }))
    .sort((a, b) => b.score - a.score)
    .slice(0, 100)
}

async function aggregateInterest(supabase: SupabaseClient<Database>): Promise<AggRow[]> {
  // favorites JOIN complexes → sgg_code 필터 (favorites 테이블에 sgg_code 없음)
  const { data, error } = await supabase
    .from('favorites')
    .select(`
      complex_id,
      complexes!inner (sgg_code)
    `)
    .in('complexes.sgg_code', ACTIVE_SGG_CODES)
    .limit(5000)

  if (error) throw new Error(`aggregateInterest failed: ${error.message}`)

  const map = new Map<string, number>()
  for (const row of data ?? []) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const r = row as any
    const cid: string = r.complex_id
    map.set(cid, (map.get(cid) ?? 0) + 1)
  }
  return Array.from(map.entries())
    .sort(([, a], [, b]) => b - a)
    .slice(0, 100)
    .map(([complex_id, score]) => ({ complex_id, score }))
}

/**
 * 4종 랭킹을 집계해 complex_rankings 테이블에 UPSERT한다.
 * cron endpoint에서 createSupabaseAdminClient()를 전달받아 호출한다.
 */
export async function computeRankings(
  supabase: SupabaseClient<Database>,
): Promise<{ type: RankType; upserted: number }[]> {
  const computedAt = new Date().toISOString()

  const aggregators: Array<{ type: RankType; fn: (s: SupabaseClient<Database>) => Promise<AggRow[]> }> = [
    { type: 'high_price', fn: aggregateHighPrice },
    { type: 'volume', fn: aggregateVolume },
    { type: 'price_per_pyeong', fn: aggregatePricePerPyeong },
    { type: 'interest', fn: aggregateInterest },
  ]

  const results: { type: RankType; upserted: number }[] = []

  for (const { type, fn } of aggregators) {
    const rows = await fn(supabase)

    if (rows.length === 0) {
      results.push({ type, upserted: 0 })
      continue
    }

    const upsertRows = rows.map((row, idx) => ({
      complex_id: row.complex_id,
      rank_type: type,
      score: row.score,
      rank: idx + 1,
      window_days: WINDOW_DAYS,
      computed_at: computedAt,
    }))

    const { error } = await supabase
      .from('complex_rankings')
      .upsert(upsertRows, {
        onConflict: 'rank_type,complex_id,window_days',
        ignoreDuplicates: false,
      })

    if (error) throw new Error(`computeRankings UPSERT(${type}) failed: ${error.message}`)
    results.push({ type, upserted: upsertRows.length })
  }

  return results
}
```

**중요 준수 사항:**
- `computeRankings`는 `createSupabaseAdminClient()`를 직접 호출하지 않는다. 호출부(route.ts)에서 admin client를 전달한다.
- 모든 `transactions` 쿼리에 `.is('cancel_date', null).is('superseded_by', null)` 포함 (CRITICAL)
- `favorites` 집계 시 `complexes!inner`로 JOIN해 sgg_code 필터 적용 (RESEARCH.md Pitfall 5 참조)
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/__tests__/rankings.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `src/lib/data/rankings.ts`가 존재하고 `getRankingsByType`, `computeRankings`, `RankType`, `RankingRow`를 export한다
    - `npm run test -- src/__tests__/rankings.test.ts`에서 `getRankingsByType` 테스트 2종이 PASS한다
    - `computeRankings` 테스트가 오류 없이 완료된다
    - `npm run lint`가 오류를 보고하지 않는다
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cron endpoint + GitHub Actions 워크플로우</name>
  <files>
    src/app/api/cron/rankings/route.ts
    .github/workflows/rankings-cron.yml
  </files>
  <behavior>
    - GET /api/cron/rankings — Authorization 헤더 없음 → 401
    - GET /api/cron/rankings — 잘못된 secret → 401
    - GET /api/cron/rankings — 올바른 CRON_SECRET → 200 + `{ ok: true, results: [...], computedAt: "..." }`
    - rankings-cron.yml — schedule: `0 * * * *`, GET, Authorization Bearer, timeout-minutes: 3
  </behavior>
  <action>
**파일 1: `src/app/api/cron/rankings/route.ts`**

`molit-trade/route.ts` 패턴을 그대로 따른다. `computeRankings()` 완료 후 ingest_runs에 기록한다.

```typescript
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { computeRankings } from '@/lib/data/rankings'

export const runtime = 'nodejs'

export async function GET(request: Request): Promise<Response> {
  // CRON_SECRET 검증 (ADR: 모든 /api/cron/* 필수)
  const authHeader = request.headers.get('authorization')
  if (!process.env.CRON_SECRET || authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return new Response('Unauthorized', { status: 401 })
  }

  const supabase = createSupabaseAdminClient()

  // ingest_runs 실행 시작 기록
  const startedAt = new Date().toISOString()
  const { data: runData } = await supabase
    .from('ingest_runs')
    .insert({
      source: 'rankings',
      status: 'running',
      started_at: startedAt,
    })
    .select('id')
    .single()

  const runId: string | null = (runData as { id: string } | null)?.id ?? null

  try {
    const results = await computeRankings(supabase)
    const totalUpserted = results.reduce((s, r) => s + r.upserted, 0)

    // ingest_runs 완료 기록
    if (runId) {
      await supabase
        .from('ingest_runs')
        .update({
          status: 'success',
          finished_at: new Date().toISOString(),
          rows_upserted: totalUpserted,
        })
        .eq('id', runId)
    }

    return Response.json({
      ok: true,
      results,
      totalUpserted,
      runId,
      computedAt: new Date().toISOString(),
    })
  } catch (err) {
    // ingest_runs 실패 기록
    if (runId) {
      await supabase
        .from('ingest_runs')
        .update({
          status: 'failed',
          finished_at: new Date().toISOString(),
          error_message: err instanceof Error ? err.message : String(err),
        })
        .eq('id', runId)
    }

    console.error('computeRankings failed:', err)
    return Response.json(
      { ok: false, error: err instanceof Error ? err.message : 'Unknown error' },
      { status: 500 },
    )
  }
}
```

**사전 확인 필수 (구현 전):** 기존 `ingest_runs` 마이그레이션 파일에서 실제 컬럼명을 확인한다:
```bash
grep -h "column\|^\s*[a-z_]* " supabase/migrations/*ingest_runs* 2>/dev/null \
  || grep -rn "ingest_runs" supabase/migrations/ | head -20
```
확인된 컬럼명이 `source`, `status`, `started_at`, `finished_at`, `rows_upserted`, `error_message`와 다르면 위 route.ts 코드의 insert/update 필드를 실제 컬럼명에 맞춰 수정한다.

---

**파일 2: `.github/workflows/rankings-cron.yml`**

`notify-worker.yml`의 exact analog. 변경점: schedule, HTTP 메서드, 헤더, 엔드포인트.

```yaml
name: Rankings Cron

on:
  schedule:
    # 매 시간 정각 실행 (UTC 기준)
    - cron: '0 * * * *'
  workflow_dispatch: {}

jobs:
  compute-rankings:
    name: Compute & store rankings
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Call rankings cron endpoint
        run: |
          STATUS=$(curl -sSf -o /tmp/rankings_response.json -w "%{http_code}" \
            -X GET \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.SITE_URL }}/api/cron/rankings")

          echo "HTTP status: $STATUS"
          cat /tmp/rankings_response.json

          if [ "$STATUS" != "200" ]; then
            echo "Rankings cron returned $STATUS"
            exit 1
          fi
```

**GitHub Actions 사용량 주의 (RESEARCH.md Pattern 6):**
- rankings-cron: 24회/일 × ~30초 = 720회 × 30초 ≈ 360분/월
- notify-worker: ~1,440분/월
- 합산 ≈ 1,800분/월 (GitHub 무료 2,000분 한도)
- `timeout-minutes: 3` 명시 필수
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/__tests__/rankings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `src/app/api/cron/rankings/route.ts`가 존재하고 GET handler를 export한다
    - rankings.test.ts의 401/200 테스트가 모두 PASS한다
    - `.github/workflows/rankings-cron.yml`이 존재하고 `0 * * * *` 스케줄을 포함한다
    - `npm run lint`가 오류를 보고하지 않는다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → /api/cron/rankings | 외부 HTTP 요청 — CRON_SECRET 검증 필요 |
| rankings.ts → complex_rankings | DB UPSERT — admin client 전용 |
| /api/cron/rankings → 클라이언트 | 공개 endpoint — 인증 없이 401 반환 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | GET /api/cron/rankings | mitigate | `Authorization: Bearer {CRON_SECRET}` 검증. 헤더 없음 또는 오답 → 401 |
| T-02-05 | Tampering | complex_rankings UPSERT | mitigate | `createSupabaseAdminClient()` 단일 경유. RLS에 의해 anon/user는 write 불가 |
| T-02-06 | Denial of Service | rankings cron (GitHub Actions 한도) | accept | `timeout-minutes: 3` 명시. 360분/월 예상 — 무료 한도(2,000분) 내 |
| T-02-07 | Information Disclosure | 500 오류 응답 | mitigate | error.message만 반환. 스택 트레이스 미노출 |
</threat_model>

<verification>
```bash
# 랭킹 함수 단위 테스트
npm run test -- --reporter=verbose src/__tests__/rankings.test.ts

# cron endpoint 401 검증 (로컬 서버 실행 중일 때)
curl -X GET http://localhost:3000/api/cron/rankings
# → 401 Unauthorized

# 올바른 시크릿으로 호출 (CRON_SECRET=test-secret 가정)
curl -X GET http://localhost:3000/api/cron/rankings \
  -H "Authorization: Bearer test-secret"
# → 200 {"ok":true,...}

# GitHub Actions 워크플로우 파일 확인
grep -n "0 \* \* \* \*" .github/workflows/rankings-cron.yml

# 린트 + 빌드
npm run lint && npm run build
```
</verification>

<success_criteria>
1. `src/lib/data/rankings.ts`가 `getRankingsByType`, `computeRankings`, `RankType`, `RankingRow`를 export한다
2. 모든 transactions 쿼리에 `cancel_date IS NULL AND superseded_by IS NULL` 조건이 포함된다
3. `GET /api/cron/rankings` — 올바른 CRON_SECRET → 200, 없거나 틀리면 → 401
4. `complex_rankings`에 UPSERT가 성공하고 ingest_runs에 기록이 남는다
5. `.github/workflows/rankings-cron.yml`이 `0 * * * *` 스케줄로 정의된다
6. `npm run test -- src/__tests__/rankings.test.ts` 전체 PASS
7. `npm run lint && npm run build` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/02-ranking-landing-share/02-02-SUMMARY.md`를 생성한다.
</output>

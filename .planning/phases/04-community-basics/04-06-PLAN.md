---
phase: 04-community-basics
plan: "06"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - src/app/api/worker/cafe-code/route.ts
  - .github/workflows/cafe-code-weekly.yml
  - src/app/admin/status/page.tsx
autonomous: true
requirements:
  - COMM-05

must_haves:
  truths:
    - "POST /api/worker/cafe-code가 x-cron-secret 검증 후 cafe_join_codes에 8자 코드를 INSERT한다"
    - "week_start UNIQUE 제약으로 같은 주에 코드 중복 생성이 방지된다"
    - "GitHub Actions 주간 cron이 매주 월요일 09:05 KST에 실행된다"
    - "어드민 /admin/status 페이지에서 현재 주의 카페 가입 코드를 확인할 수 있다"
  artifacts:
    - path: "src/app/api/worker/cafe-code/route.ts"
      provides: "카페 코드 생성 worker endpoint"
      contains: "cafe_join_codes"
    - path: ".github/workflows/cafe-code-weekly.yml"
      provides: "주간 카페 코드 GitHub Actions cron"
      contains: "cron: '5 0 * * 1'"
    - path: "src/app/admin/status/page.tsx"
      provides: "현재 주 카페 가입 코드 표시"
      contains: "cafe_join_codes"
  key_links:
    - from: ".github/workflows/cafe-code-weekly.yml"
      to: "src/app/api/worker/cafe-code/route.ts"
      via: "curl POST x-cron-secret"
      pattern: "cafe-code"
    - from: "src/app/api/worker/cafe-code/route.ts"
      to: "cafe_join_codes 테이블"
      via: "INSERT week_start UNIQUE"
      pattern: "cafe_join_codes"
---

<objective>
주간 회전 카페 가입 코드 시스템을 구현한다. GitHub Actions 주간 cron이 새 코드를 생성하고 어드민에서 확인할 수 있다.

Purpose: 운영자가 매주 카페 가입 코드를 어드민에서 확인하고 멤버에게 공유할 수 있어야 한다. Vercel Hobby 1일 cron 한도를 피해 GitHub Actions 주간 cron을 사용한다.
Output: cafe-code worker route + GitHub Actions yml + admin/status 코드 표시
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- src/app/api/worker/notify/route.ts 기존 패턴 (analog) -->
```typescript
import { NextResponse } from 'next/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'

export const runtime = 'nodejs'

export async function POST(request: Request): Promise<NextResponse> {
  const secret = request.headers.get('x-cron-secret')
  if (!secret || secret !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }
  const supabase = createSupabaseAdminClient()
  // ...
}
```

<!-- cafe_join_codes 스키마 (Wave 0 마이그레이션) -->
```sql
CREATE TABLE public.cafe_join_codes (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  code       text NOT NULL UNIQUE,
  week_start date NOT NULL UNIQUE,  -- 주 중복 방지
  created_at timestamptz NOT NULL DEFAULT now()
);
-- RLS: admin/superadmin만 SELECT
```

<!-- .github/workflows/notify-worker.yml 패턴 -->
```yaml
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}
jobs:
  notify:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Run notify worker
        run: |
          STATUS=$(curl -sSf -o /tmp/response.json -w "%{http_code}" \
            -X POST \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.SITE_URL }}/api/worker/notify")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: cafe-code worker route + GitHub Actions yml 구현</name>
  <files>src/app/api/worker/cafe-code/route.ts, .github/workflows/cafe-code-weekly.yml</files>
  <read_first>
    - src/app/api/worker/notify/route.ts (worker endpoint 패턴 참조)
    - .github/workflows/notify-worker.yml (GitHub Actions 패턴 참조)
    - .planning/phases/04-community-basics/04-RESEARCH.md §6 카페 가입 코드
    - .planning/phases/04-community-basics/04-PATTERNS.md §cafe-code/route.ts 패턴
  </read_first>
  <action>
**`src/app/api/worker/cafe-code/route.ts`** (신규 파일):

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'

export const runtime = 'nodejs'

// 이번 주 월요일 날짜 반환 (YYYY-MM-DD)
function getMonday(date: Date): Date {
  const d = new Date(date)
  const day = d.getUTCDay()
  const diff = d.getUTCDate() - day + (day === 0 ? -6 : 1)  // 월요일 기준
  d.setUTCDate(diff)
  return d
}

// 8자 알파뉴메릭 랜덤 코드 (PostgreSQL md5 없이 JS에서 생성)
function generateCode(): string {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'  // 혼동 문자(0/O, 1/I) 제외
  let code = ''
  for (let i = 0; i < 8; i++) {
    code += chars[Math.floor(Math.random() * chars.length)]
  }
  return code
}

export async function POST(request: Request): Promise<NextResponse> {
  const secret = request.headers.get('x-cron-secret')
  if (!secret || secret !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const supabase = createSupabaseAdminClient()
  const weekStart = getMonday(new Date()).toISOString().slice(0, 10)

  // 이번 주 코드가 이미 존재하면 스킵 (week_start UNIQUE 제약)
  const { data: existing } = await supabase
    .from('cafe_join_codes')
    .select('code')
    .eq('week_start', weekStart)
    .single()

  if (existing) {
    return NextResponse.json({ code: existing.code, skipped: true })
  }

  const code = generateCode()
  const { data, error } = await supabase
    .from('cafe_join_codes')
    .insert({ week_start: weekStart, code })
    .select('code')
    .single()

  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 })
  }

  return NextResponse.json({ code: data.code, skipped: false })
}
```

**`.github/workflows/cafe-code-weekly.yml`** (신규 파일):
```yaml
name: Cafe Code Weekly

on:
  schedule:
    - cron: '5 0 * * 1'  # 매주 월요일 09:05 KST (UTC 00:05)
  workflow_dispatch: {}

jobs:
  generate-code:
    name: Generate weekly cafe join code
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Generate cafe join code
        run: |
          STATUS=$(curl -sSf -o /tmp/cafe_code_response.json -w "%{http_code}" \
            -X POST \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.SITE_URL }}/api/worker/cafe-code")

          echo "HTTP status: $STATUS"
          cat /tmp/cafe_code_response.json

          if [ "$STATUS" != "200" ]; then
            echo "Cafe code worker returned $STATUS"
            exit 1
          fi
```
  </action>
  <verify>
    <automated>grep -n "cafe_join_codes" src/app/api/worker/cafe-code/route.ts</automated>
  </verify>
  <done>
    1. cafe-code/route.ts가 x-cron-secret 검증 → 중복 체크 → INSERT 흐름으로 구현됨
    2. cafe-code-weekly.yml이 `cron: '5 0 * * 1'` 스케줄로 설정됨
    3. `npm run lint`가 에러 없이 통과한다
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: admin/status 페이지에 카페 가입 코드 표시</name>
  <files>src/app/admin/status/page.tsx</files>
  <read_first>
    - src/app/admin/status/page.tsx (기존 파일 전체 — 현재 구조 파악)
    - src/lib/auth/admin-actions.ts (requireAdmin 패턴 확인)
  </read_first>
  <action>
`src/app/admin/status/page.tsx`를 읽은 후 현재 주의 카페 가입 코드를 표시하는 섹션을 추가한다.

**추가할 데이터 fetch (기존 Promise.all에 통합 또는 개별 fetch):**
```typescript
// 현재 주 월요일 계산
function getMonday(date: Date): string {
  const d = new Date(date)
  const day = d.getDay()
  const diff = d.getDate() - day + (day === 0 ? -6 : 1)
  d.setDate(diff)
  return d.toISOString().slice(0, 10)
}

const weekStart = getMonday(new Date())
const { data: cafeCode } = await supabase
  .from('cafe_join_codes')
  .select('code, week_start, created_at')
  .eq('week_start', weekStart)
  .single()
```

**추가할 UI 섹션 (기존 카드 패턴 준수):**
```typescript
{/* 카페 가입 코드 섹션 */}
<div className="card" style={{ padding: 20, marginBottom: 20 }}>
  <h3 style={{ font: '700 14px/1.4 var(--font-sans)', margin: '0 0 14px' }}>
    이번 주 카페 가입 코드
  </h3>
  {cafeCode ? (
    <div>
      <div
        className="tnum"
        style={{
          font:          '700 28px/1 var(--font-mono, monospace)',
          color:         'var(--dj-orange)',
          letterSpacing: '0.1em',
          marginBottom:  8,
        }}
      >
        {cafeCode.code}
      </div>
      <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
        {weekStart} 주 발행 · {new Date(cafeCode.created_at).toLocaleDateString('ko-KR')} 생성
      </div>
    </div>
  ) : (
    <div>
      <p style={{ font: '500 13px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 12px' }}>
        이번 주 코드가 아직 생성되지 않았습니다.
      </p>
      <p style={{ font: '500 11px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
        매주 월요일 09:05 KST에 자동 생성됩니다.
      </p>
    </div>
  )}
</div>
```

`supabase` 클라이언트는 기존 admin/status/page.tsx의 패턴을 따른다 (requireAdmin + createSupabaseAdminClient 또는 서버 컴포넌트).
  </action>
  <verify>
    <automated>grep -n "cafe_join_codes" src/app/admin/status/page.tsx</automated>
  </verify>
  <done>
    1. admin/status/page.tsx에 카페 가입 코드 섹션이 추가된다
    2. 현재 주의 코드를 큰 폰트(tnum, dj-orange)로 표시한다
    3. 코드가 없을 때 "매주 월요일 09:05 KST에 자동 생성됩니다" 안내가 표시된다
    4. `npm run lint && npm run build`가 에러 없이 통과한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → worker endpoint | x-cron-secret 헤더로 인증 |
| admin/status → cafe_join_codes | admin RLS 정책으로 보호됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Elevation of Privilege | /api/worker/cafe-code | mitigate | x-cron-secret 헤더 검증. 미인증 요청 401 반환 |
| T-04-06-02 | Spoofing | cafe_join_codes 코드 예측 | accept | 8자 alphanumeric (혼동 문자 제외) — 충분한 엔트로피. 추측 공격 실용적 불가 |
| T-04-06-03 | Information Disclosure | cafe_join_codes | mitigate | admin/superadmin만 SELECT 가능 (RLS Wave 0에서 설정) |
</threat_model>

<verification>
```bash
# 파일 존재 확인
ls src/app/api/worker/cafe-code/route.ts
ls .github/workflows/cafe-code-weekly.yml

# 핵심 로직 확인
grep "cafe_join_codes" src/app/api/worker/cafe-code/route.ts
grep "cron: '5 0 \* \* 1'" .github/workflows/cafe-code-weekly.yml
grep "cafe_join_codes" src/app/admin/status/page.tsx

# 빌드 에러 없음
npm run lint
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. POST /api/worker/cafe-code가 x-cron-secret 없을 때 401을 반환한다
2. 같은 주에 두 번 호출하면 두 번째는 skipped: true를 반환한다
3. GitHub Actions cron이 `5 0 * * 1` (매주 월요일 09:05 KST)로 설정된다
4. admin/status 페이지에서 현재 주의 카페 가입 코드를 확인할 수 있다
5. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-06-SUMMARY.md` 생성
</output>

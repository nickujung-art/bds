---
phase: 04-community-basics
plan: "07"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - src/lib/notifications/digest.ts
  - src/app/api/worker/digest/route.ts
  - .github/workflows/weekly-digest.yml
autonomous: true
requirements:
  - NOTIF-01

must_haves:
  truths:
    - "buildWeeklyDigest가 favorites 테이블에서 관심 단지 구독자를 조회한다"
    - "각 구독자에게 type='digest' notifications 행이 INSERT된다 (dedupe_key로 중복 방지)"
    - "기존 5분 notify-worker가 type='digest' 알림을 Resend로 전송한다"
    - "GitHub Actions 주간 cron이 매주 월요일 09:00 KST에 실행된다"
    - "digest.test.ts의 모든 테스트가 GREEN이다"
  artifacts:
    - path: "src/lib/notifications/digest.ts"
      provides: "buildWeeklyDigest (favorites→notifications INSERT)"
      contains: "buildWeeklyDigest"
      exports: ["buildWeeklyDigest"]
    - path: "src/app/api/worker/digest/route.ts"
      provides: "주간 다이제스트 worker endpoint"
      contains: "buildWeeklyDigest"
    - path: ".github/workflows/weekly-digest.yml"
      provides: "주간 다이제스트 GitHub Actions cron"
      contains: "cron: '0 0 * * 1'"
  key_links:
    - from: ".github/workflows/weekly-digest.yml"
      to: "src/app/api/worker/digest/route.ts"
      via: "curl POST x-cron-secret"
      pattern: "digest"
    - from: "src/lib/notifications/digest.ts"
      to: "notifications 테이블"
      via: "INSERT type='digest' + dedupe_key=YYYY-WW"
      pattern: "type.*digest"
---

<objective>
주간 다이제스트 이메일 시스템을 구현한다. 관심 단지(favorites)가 있는 사용자에게 매주 관심 단지 거래 요약을 이메일로 발송한다.

Purpose: 구독 회원이 매주 관심 단지의 최신 거래 정보를 이메일로 받아볼 수 있어야 한다. 기존 deliver.ts + 5분 notify-worker 인프라를 재활용한다.
Output: digest.ts + digest worker route + GitHub Actions yml
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- src/lib/notifications/deliver.ts 기존 패턴 -->
```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

const BATCH_SIZE = 50

export async function deliverPendingNotifications(
  supabase: SupabaseClient<Database>,
): Promise<{ sent: number; failed: number }> {
  // notifications WHERE status='pending' 폴링
  // Resend + web-push 이중 전달
}
```

<!-- notifications 스키마 (Wave 0에서 'digest' 추가됨) -->
```sql
-- type CHECK: ('price_alert', 'comment', 'reply', 'system', 'digest')
-- UNIQUE(user_id, event_type, target_id, dedupe_key) — 중복 방지
-- dedupe_key 활용: YYYY-WW 형식으로 주 단위 중복 방지
```

<!-- favorites 테이블 (기존) -->
```typescript
// favorites.user_id, favorites.complex_id
// N+1 방지: SELECT user_id, complex_id FROM favorites (한번에)
```

<!-- worker endpoint 패턴 -->
```typescript
export const runtime = 'nodejs'
const secret = request.headers.get('x-cron-secret')
if (!secret || secret !== process.env.CRON_SECRET) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
}
const supabase = createSupabaseAdminClient()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: digest.ts buildWeeklyDigest 구현 (GREEN)</name>
  <files>src/lib/notifications/digest.ts</files>
  <read_first>
    - src/lib/notifications/digest.test.ts (Wave 0 RED 테스트 — GREEN 목표)
    - src/lib/notifications/deliver.ts (기존 패턴 참조 — BATCH_SIZE, 에러 처리)
    - .planning/phases/04-community-basics/04-RESEARCH.md §7 Resend 주간 다이제스트
    - .planning/phases/04-community-basics/04-PATTERNS.md §digest.ts 패턴
  </read_first>
  <behavior>
    - favorites가 비어있으면 { inserted: 0 } 반환
    - favorites가 있으면 user별 관심 단지를 묶어 digest notifications INSERT
    - dedupe_key = 'digest-YYYY-WW' (ISO 주번호) — 주 단위 중복 방지
    - N+1 방지: complex_id 배치 쿼리로 거래 데이터 한 번에 조회
    - Resend 3,000건/월 한도: 배치 크기 제한 (BATCH_SIZE=50)
  </behavior>
  <action>
**`src/lib/notifications/digest.ts`** (신규 파일):

```typescript
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

const BATCH_SIZE = 50  // deliver.ts와 동일한 배치 크기

// ISO 주번호 (YYYY-WW 형식)
function getWeekKey(date: Date): string {
  const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()))
  const dayNum = d.getUTCDay() || 7
  d.setUTCDate(d.getUTCDate() + 4 - dayNum)
  const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1))
  const weekNo = Math.ceil(((d.getTime() - yearStart.getTime()) / 86400000 + 1) / 7)
  return `digest-${d.getUTCFullYear()}-W${String(weekNo).padStart(2, '0')}`
}

export async function buildWeeklyDigest(
  supabase: SupabaseClient<Database>,
): Promise<{ inserted: number }> {
  // 1. 관심 단지가 있는 사용자 전체 조회 (N+1 방지)
  const { data: favorites } = await supabase
    .from('favorites')
    .select('user_id, complex_id')

  if (!favorites?.length) return { inserted: 0 }

  // 2. user별 관심 단지 묶기
  const userComplexMap = new Map<string, string[]>()
  for (const fav of favorites) {
    const list = userComplexMap.get(fav.user_id) ?? []
    list.push(fav.complex_id)
    userComplexMap.set(fav.user_id, list)
  }

  // 3. unique complex_id 배치로 최근 거래 조회
  const allComplexIds = [...new Set(favorites.map(f => f.complex_id))]
  const { data: recentTrades } = await supabase
    .from('transactions')
    .select('complex_id, price_per_area, deal_date, area')
    .in('complex_id', allComplexIds)
    .is('cancel_date', null)       // transactions 대원칙
    .is('superseded_by', null)     // transactions 대원칙
    .order('deal_date', { ascending: false })
    .limit(allComplexIds.length * 3)  // 단지당 최근 3건

  // complex_id별 최근 거래 인덱싱
  const tradesByComplex = new Map<string, typeof recentTrades>()
  for (const trade of recentTrades ?? []) {
    const list = tradesByComplex.get(trade.complex_id) ?? []
    list.push(trade)
    tradesByComplex.set(trade.complex_id, list)
  }

  // 4. 사용자별 digest content 생성 + notifications INSERT
  const weekKey = getWeekKey(new Date())
  const notifications: Array<{
    user_id: string
    type: string
    event_type: string
    target_id: string | null
    dedupe_key: string
    title: string
    body: string
  }> = []

  for (const [userId, complexIds] of userComplexMap) {
    const tradeLines = complexIds
      .flatMap(cid => (tradesByComplex.get(cid) ?? []).slice(0, 1))
      .map(t => `· ${t.complex_id} ${t.area}㎡ ${t.price_per_area ? `${t.price_per_area.toLocaleString('ko-KR')}만원/㎡` : ''}`)

    const body = tradeLines.length > 0
      ? tradeLines.join('\n')
      : '이번 주 관심 단지에 새 거래가 없습니다.'

    notifications.push({
      user_id:    userId,
      type:       'digest',
      event_type: 'weekly_digest',
      target_id:  null,
      dedupe_key: weekKey,
      title:      '이번 주 관심 단지 요약',
      body,
    })
  }

  // 5. 배치 INSERT (UNIQUE 충돌 시 skip — 이미 발송된 주)
  let inserted = 0
  for (let i = 0; i < notifications.length; i += BATCH_SIZE) {
    const batch = notifications.slice(i, i + BATCH_SIZE)
    const { error } = await supabase
      .from('notifications')
      .upsert(batch, { onConflict: 'user_id,event_type,target_id,dedupe_key', ignoreDuplicates: true })
    if (!error) inserted += batch.length
  }

  return { inserted }
}
```

**주의:** `transactions` 테이블의 실제 컬럼명을 확인 후 조정. `price_per_area` 또는 다른 컬럼명이면 맞게 수정. tradeLines에서 complex_id 대신 complexes.name을 표시하려면 JOIN이 필요하나, V1.5 수준에서는 complex_id 표시로 충분.
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/notifications/digest.test.ts</automated>
  </verify>
  <done>digest.test.ts의 모든 테스트가 GREEN. buildWeeklyDigest가 export되고 favorites가 없을 때 { inserted: 0 }을 반환한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: digest worker route + GitHub Actions yml 생성</name>
  <files>src/app/api/worker/digest/route.ts, .github/workflows/weekly-digest.yml</files>
  <read_first>
    - src/app/api/worker/notify/route.ts (worker endpoint 패턴 참조)
    - .github/workflows/notify-worker.yml (GitHub Actions 패턴 참조)
    - .planning/phases/04-community-basics/04-PATTERNS.md §digest/route.ts 패턴, §weekly-digest.yml 패턴
  </read_first>
  <action>
**`src/app/api/worker/digest/route.ts`** (신규 파일):

```typescript
import { NextResponse } from 'next/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { buildWeeklyDigest } from '@/lib/notifications/digest'

export const runtime = 'nodejs'

export async function POST(request: Request): Promise<NextResponse> {
  const secret = request.headers.get('x-cron-secret')
  if (!secret || secret !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const supabase = createSupabaseAdminClient()

  try {
    const { inserted } = await buildWeeklyDigest(supabase)
    return NextResponse.json({ inserted })
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err)
    console.error('digest worker error:', message)
    return NextResponse.json({ error: message }, { status: 500 })
  }
}
```

**`.github/workflows/weekly-digest.yml`** (신규 파일):
```yaml
name: Weekly Digest

on:
  schedule:
    - cron: '0 0 * * 1'  # 매주 월요일 09:00 KST (UTC 00:00)
  workflow_dispatch: {}

jobs:
  send-digest:
    name: Build & enqueue weekly digest
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Trigger digest worker
        run: |
          STATUS=$(curl -sSf -o /tmp/digest_response.json -w "%{http_code}" \
            -X POST \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.SITE_URL }}/api/worker/digest")

          echo "HTTP status: $STATUS"
          cat /tmp/digest_response.json

          if [ "$STATUS" != "200" ]; then
            echo "Digest worker returned $STATUS"
            exit 1
          fi
```

**기존 notify-worker 호환성 확인 (필수):**
1. `src/lib/notifications/deliver.ts`를 읽어 `deliverPendingNotifications` 함수가 type별 분기를 사용하는지 확인한다.
2. type 조건 필터링이 있다면: type='digest' case를 추가한다 (이메일 전송 분기에 포함).
3. type 필터링이 없다면: 기존 로직이 모든 pending 알림을 처리하므로 digest도 자동 처리됨 — 별도 수정 불필요.
4. notifications.type CHECK에 'digest'는 Wave 0에서 이미 추가됨 (DB 허용).

`grep -n "type" src/lib/notifications/deliver.ts` 실행으로 현재 구조 확인.
  </action>
  <verify>
    <automated>grep -n "buildWeeklyDigest" src/app/api/worker/digest/route.ts && grep -n "digest" src/lib/notifications/deliver.ts</automated>
  </verify>
  <done>
    1. digest/route.ts가 x-cron-secret 검증 후 buildWeeklyDigest를 호출한다
    2. weekly-digest.yml이 `cron: '0 0 * * 1'` 스케줄로 설정된다
    3. deliver.ts가 type='digest' 알림을 처리하는 분기를 포함한다 — grep으로 'digest' 문자열이 deliver.ts에 존재함을 확인
    4. deliver.ts에 'digest' 처리 분기가 없다면: deliverPendingNotifications 함수에서 type 필터를 제거하거나 'digest' case를 추가한다
    5. `npm run lint && npm run build`가 에러 없이 통과한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → digest worker | x-cron-secret 헤더 인증 |
| buildWeeklyDigest → notifications INSERT | admin client (service role) — RLS 우회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Elevation of Privilege | /api/worker/digest | mitigate | x-cron-secret 검증. 미인증 401 반환 |
| T-04-07-02 | DoS | Resend 월 한도 초과 | accept | 초기 단계(창원·김해 실수요자)에서 750명/주 초과 가능성 낮음. 초과 시 Resend API 에러로 failed 처리 |
| T-04-07-03 | Information Disclosure | 이메일 다이제스트 내용 | accept | 관심 단지 거래 요약만 포함. 개인정보 최소화 |
| T-04-07-04 | Tampering | dedupe_key 중복 | mitigate | UNIQUE(user_id, event_type, target_id, dedupe_key) + ignoreDuplicates — 동일 주 중복 발송 불가 |
</threat_model>

<verification>
```bash
# GREEN 테스트
npm run test -- --run src/lib/notifications/digest.test.ts

# 파일 존재 확인
ls src/lib/notifications/digest.ts
ls src/app/api/worker/digest/route.ts
ls .github/workflows/weekly-digest.yml

# 핵심 로직 확인
grep -n "buildWeeklyDigest" src/app/api/worker/digest/route.ts
grep "cron: '0 0" .github/workflows/weekly-digest.yml
grep "dedupe_key" src/lib/notifications/digest.ts

# 빌드 에러 없음
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `digest.test.ts`의 모든 테스트가 GREEN이다
2. `buildWeeklyDigest`가 dedupe_key='digest-YYYY-WW' 형식으로 중복 방지한다
3. digest worker가 x-cron-secret 없을 때 401을 반환한다
4. GitHub Actions cron이 `0 0 * * 1` (매주 월요일 09:00 KST)로 설정된다
5. 기존 5분 notify-worker가 'digest' type 알림을 Resend로 전송한다
6. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-07-SUMMARY.md` 생성
</output>

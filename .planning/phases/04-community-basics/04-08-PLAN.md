---
phase: 04-community-basics
plan: "08"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - src/lib/auth/topic-actions.ts
  - src/lib/data/topics.ts
  - src/components/profile/TopicToggle.tsx
  - src/app/profile/page.tsx
autonomous: true
requirements:
  - NOTIF-02

must_haves:
  truths:
    - "upsertNotificationTopic이 notification_topics에 (user_id, topic) UNIQUE 기준으로 UPSERT한다"
    - "deleteNotificationTopic이 해당 토픽 행을 DELETE한다"
    - "프로필 페이지에 신고가/신축분양/단지업데이트 3개 토픽 토글이 표시된다"
    - "토픽 토글이 낙관적 업데이트(즉시 UI 전환) + Server Action 실패 시 rollback을 구현한다"
    - "topic-actions.test.ts의 모든 테스트가 GREEN이다"
  artifacts:
    - path: "src/lib/auth/topic-actions.ts"
      provides: "upsertNotificationTopic + deleteNotificationTopic Server Action"
      contains: "upsertNotificationTopic"
      exports: ["upsertNotificationTopic", "deleteNotificationTopic"]
    - path: "src/lib/data/topics.ts"
      provides: "getNotificationTopics 데이터 쿼리"
      contains: "getNotificationTopics"
    - path: "src/components/profile/TopicToggle.tsx"
      provides: "pill 토글 + 낙관적 업데이트 Client Component"
      contains: "TopicToggle"
    - path: "src/app/profile/page.tsx"
      provides: "알림 토픽 설정 UI 추가"
      contains: "TopicToggle"
  key_links:
    - from: "src/components/profile/TopicToggle.tsx"
      to: "src/lib/auth/topic-actions.ts"
      via: "upsertNotificationTopic / deleteNotificationTopic Server Action"
      pattern: "upsertNotificationTopic"
    - from: "src/app/profile/page.tsx"
      to: "src/lib/data/topics.ts"
      via: "getNotificationTopics 초기 상태 fetch"
      pattern: "getNotificationTopics"
---

<objective>
알림 토픽 채널 구독 UI를 구현한다. 사용자가 프로필 설정에서 받고 싶은 알림 카테고리를 선택하고 notification_topics 테이블에 저장한다.

Purpose: 사용자가 신고가 알림, 신축 분양 알림, 단지 업데이트 중 원하는 항목만 선택해 받을 수 있어야 한다. 낙관적 업데이트로 즉각적인 UI 반응을 제공한다.
Output: topic-actions.ts + topics.ts + TopicToggle.tsx + profile/page.tsx 수정
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-UI-SPEC.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- notification_topics 스키마 (Wave 0 마이그레이션) -->
```sql
CREATE TABLE public.notification_topics (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id    uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  topic      text NOT NULL CHECK (topic IN ('high_price', 'presale', 'complex_update')),
  created_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE(user_id, topic)
);
-- RLS: owner all (user_id = auth.uid())
```

<!-- Server Action 패턴 -->
```typescript
'use server'
import { createSupabaseServerClient } from '@/lib/supabase/server'

export async function upsertNotificationTopic(
  topic: 'high_price' | 'presale' | 'complex_update',
): Promise<{ error: string | null }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다.' }
  // INSERT ... ON CONFLICT DO NOTHING
}
```

<!-- UI-SPEC §3-10 Notification Topic Settings -->
```typescript
// 토픽 정의:
// high_price: "신고가 알림" — 관심 단지 신고가 갱신 시
// presale: "신축 분양 알림" — 창원·김해 신규 분양 등록 시
// complex_update: "단지 업데이트" — 관심 단지 정보 변경 시

// 토글 버튼: role="switch" aria-checked
// 선택됨: background: var(--dj-orange), 미선택: background: var(--line-default)
// 낙관적 업데이트: UI 즉시 전환 → 실패 시 rollback
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: topic-actions.ts + topics.ts 구현 (GREEN)</name>
  <files>src/lib/auth/topic-actions.ts, src/lib/data/topics.ts</files>
  <read_first>
    - src/lib/auth/topic-actions.test.ts (Wave 0 RED 테스트 — GREEN 목표)
    - src/lib/auth/comment-actions.ts (Server Action 패턴 참조)
    - src/lib/data/comments.ts (데이터 레이어 패턴 참조)
  </read_first>
  <behavior>
    - upsertNotificationTopic: 미로그인 에러 → UPSERT (onConflict='user_id,topic', ignoreDuplicates=true)
    - deleteNotificationTopic: 미로그인 에러 → DELETE where user_id=user.id and topic=topic
    - getNotificationTopics: user_id로 현재 구독 토픽 배열 반환
  </behavior>
  <action>
**`src/lib/auth/topic-actions.ts`** (신규 파일):
```typescript
'use server'

import { createSupabaseServerClient } from '@/lib/supabase/server'

export type TopicType = 'high_price' | 'presale' | 'complex_update'

export async function upsertNotificationTopic(
  topic: TopicType,
): Promise<{ error: string | null }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다.' }

  const { error } = await supabase
    .from('notification_topics')
    .upsert(
      { user_id: user.id, topic },
      { onConflict: 'user_id,topic', ignoreDuplicates: true },
    )

  if (error) return { error: error.message }
  return { error: null }
}

export async function deleteNotificationTopic(
  topic: TopicType,
): Promise<{ error: string | null }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다.' }

  const { error } = await supabase
    .from('notification_topics')
    .delete()
    .eq('user_id', user.id)
    .eq('topic', topic)

  if (error) return { error: error.message }
  return { error: null }
}
```

**`src/lib/data/topics.ts`** (신규 파일):
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'
import type { TopicType } from '@/lib/auth/topic-actions'

export async function getNotificationTopics(
  userId: string,
  supabase: SupabaseClient<Database>,
): Promise<TopicType[]> {
  const { data } = await supabase
    .from('notification_topics')
    .select('topic')
    .eq('user_id', userId)
  return (data ?? []).map(row => row.topic as TopicType)
}
```
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/auth/topic-actions.test.ts</automated>
  </verify>
  <done>topic-actions.test.ts의 모든 테스트가 GREEN. upsertNotificationTopic, deleteNotificationTopic이 export된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TopicToggle.tsx + profile/page.tsx 수정</name>
  <files>src/components/profile/TopicToggle.tsx, src/app/profile/page.tsx</files>
  <read_first>
    - src/app/profile/page.tsx (기존 파일 전체 — 알림 설정 카드 위치 파악)
    - .planning/phases/04-community-basics/04-UI-SPEC.md §3-10 Notification Topic Settings, §4-4 Notification Topic Toggle
    - src/lib/auth/topic-actions.ts (방금 작성한 파일 — import 경로)
  </read_first>
  <action>
**`src/components/profile/TopicToggle.tsx`** (신규 파일, UI-SPEC §3-10 준수):

```typescript
'use client'

import { useState, useTransition } from 'react'
import {
  upsertNotificationTopic,
  deleteNotificationTopic,
  type TopicType,
} from '@/lib/auth/topic-actions'

const TOPICS: Array<{ key: TopicType; label: string; description: string }> = [
  { key: 'high_price',     label: '신고가 알림',    description: '관심 단지 신고가 갱신 시' },
  { key: 'presale',        label: '신축 분양 알림', description: '창원·김해 신규 분양 등록 시' },
  { key: 'complex_update', label: '단지 업데이트',  description: '관심 단지 정보 변경 시' },
]

interface Props {
  initialTopics: TopicType[]
}

export function TopicToggle({ initialTopics }: Props) {
  const [activeTopics, setActiveTopics] = useState<Set<TopicType>>(
    new Set(initialTopics),
  )
  const [error, setError] = useState<string | null>(null)
  const [, startTransition] = useTransition()

  function handleToggle(topic: TopicType) {
    const isActive = activeTopics.has(topic)
    // 낙관적 업데이트: 즉시 UI 전환
    const next = new Set(activeTopics)
    if (isActive) next.delete(topic)
    else next.add(topic)
    setActiveTopics(next)
    setError(null)

    startTransition(async () => {
      const result = isActive
        ? await deleteNotificationTopic(topic)
        : await upsertNotificationTopic(topic)

      if (result.error) {
        // rollback
        setActiveTopics(new Set(activeTopics))
        setError('저장에 실패했습니다. 다시 시도해 주세요.')
      }
    })
  }

  return (
    <div
      role="group"
      aria-labelledby="topic-section-title"
      style={{ marginTop: 16 }}
    >
      <div
        style={{
          borderTop: '1px solid var(--line-subtle)',
          marginBottom: 12,
          paddingTop: 16,
        }}
      >
        <span
          id="topic-section-title"
          style={{ font: '700 13px/1 var(--font-sans)', color: 'var(--fg-pri)' }}
        >
          알림 받을 항목
        </span>
      </div>

      {TOPICS.map(({ key, label, description }) => {
        const isActive = activeTopics.has(key)
        return (
          <div
            key={key}
            style={{
              display:        'flex',
              alignItems:     'center',
              justifyContent: 'space-between',
              gap:            12,
              padding:        '10px 0',
              minHeight:      44,
            }}
          >
            <div>
              <div style={{ font: '700 13px/1 var(--font-sans)', color: 'var(--fg-pri)' }}>
                {label}
              </div>
              <div
                className="sm:block hidden"
                style={{ font: '500 11px/1.3 var(--font-sans)', color: 'var(--fg-tertiary)', marginTop: 2 }}
              >
                {description}
              </div>
            </div>

            {/* pill 토글 */}
            <button
              type="button"
              role="switch"
              aria-checked={isActive}
              aria-label={`${label} ${isActive ? '끄기' : '켜기'}`}
              onClick={() => handleToggle(key)}
              style={{
                width:        40,
                height:       22,
                borderRadius: 11,
                border:       'none',
                cursor:       'pointer',
                background:   isActive ? 'var(--dj-orange)' : 'var(--line-default)',
                position:     'relative',
                flexShrink:   0,
                transition:   'background 150ms ease',
              }}
            >
              <span
                style={{
                  position:    'absolute',
                  top:         2,
                  left:        isActive ? 20 : 2,
                  width:       18,
                  height:      18,
                  borderRadius: '50%',
                  background:  '#fff',
                  transition:  'left 150ms ease',
                }}
              />
            </button>
          </div>
        )
      })}

      {error && (
        <p
          aria-live="polite"
          style={{ font: '500 11px/1.4 var(--font-sans)', color: '#dc2626', marginTop: 8 }}
        >
          {error}
        </p>
      )}
    </div>
  )
}
```

**`src/app/profile/page.tsx`** 수정:

기존 파일을 읽은 후 다음을 추가한다.

1. import 추가:
```typescript
import { TopicToggle } from '@/components/profile/TopicToggle'
import { getNotificationTopics } from '@/lib/data/topics'
```

2. 기존 데이터 fetch (Promise.all)에 topics 추가:
```typescript
const [favCount, reviewCount, isPushSubscribed, topics] = await Promise.all([
  getFavoritesCount(user.id, supabase),
  getReviewsCount(user.id, supabase),
  hasPushSubscription(user.id, supabase),
  getNotificationTopics(user.id, supabase),  // 신규
])
```
기존 Promise.all 구조가 다를 경우 개별 await로 추가.

3. 알림 설정 카드 내부 또는 하단에 TopicToggle 추가:
```typescript
{/* 알림 설정 카드 */}
<div className="card" style={{ padding: 20, marginBottom: 16 }}>
  <h3 style={{ font: '700 14px/1.4 var(--font-sans)', margin: '0 0 14px' }}>
    알림 설정
  </h3>
  {/* 기존 PushToggle 컴포넌트 유지 */}
  {/* ... */}

  {/* 알림 토픽 설정 추가 */}
  <TopicToggle initialTopics={topics} />
</div>
```

기존 카드 구조가 다를 경우 UI-SPEC §3-10 구조를 참고해 알림 설정 카드 하단에 자연스럽게 추가한다.
  </action>
  <verify>
    <automated>grep -n "TopicToggle" src/app/profile/page.tsx</automated>
  </verify>
  <done>
    1. TopicToggle.tsx가 존재하고 3개 토픽 토글을 렌더한다
    2. 낙관적 업데이트 패턴이 구현됨 (즉시 UI 전환 + 실패 시 rollback)
    3. profile/page.tsx에 TopicToggle이 추가된다
    4. `npm run lint && npm run build`가 에러 없이 통과한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client TopicToggle → Server Action | 사용자 인터랙션이 서버 액션으로 전달됨 |
| Server Action → notification_topics RLS | user_id = auth.uid() — 본인 토픽만 수정 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | 타인 토픽 조작 | mitigate | RLS: owner all (user_id = auth.uid()). Server Action에서도 auth.getUser()로 user_id 강제 |
| T-04-08-02 | Spoofing | 유효하지 않은 topic 값 | mitigate | DB CHECK(topic IN ('high_price', 'presale', 'complex_update')). 유효하지 않은 값은 DB에서 거부 |
| T-04-08-03 | DoS | 토픽 토글 스팸 | accept | Server Action 레이어에서 추가 rate limit 없음. RLS + DB 제약으로 충분. V1.5 수준 수용 |
</threat_model>

<verification>
```bash
# GREEN 테스트
npm run test -- --run src/lib/auth/topic-actions.test.ts

# 파일 존재 확인
ls src/lib/auth/topic-actions.ts
ls src/lib/data/topics.ts
ls src/components/profile/TopicToggle.tsx

# 핵심 로직 확인
grep "upsertNotificationTopic" src/lib/auth/topic-actions.ts
grep "TopicToggle" src/app/profile/page.tsx
grep "role=\"switch\"" src/components/profile/TopicToggle.tsx

# 빌드 에러 없음
npm run lint
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `topic-actions.test.ts`의 모든 테스트가 GREEN이다
2. TopicToggle이 3개 토픽 pill 토글을 렌더한다 (role="switch" aria-checked)
3. 낙관적 업데이트: 클릭 즉시 UI 전환, Server Action 실패 시 rollback
4. profile/page.tsx에서 getNotificationTopics로 초기 상태를 fetch한다
5. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-08-SUMMARY.md` 생성
</output>

---
phase: 04-community-basics
plan: "01"
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/lib/auth/comment-actions.ts
  - src/lib/data/comments.ts
  - src/components/reviews/CommentSection.tsx
  - src/components/reviews/ReviewList.tsx
autonomous: true
requirements:
  - COMM-01

must_haves:
  truths:
    - "submitComment를 호출하면 comments 테이블에 행이 INSERT된다"
    - "미로그인 사용자가 댓글 폼을 보면 '로그인하면 댓글을 달 수 있어요' 텍스트가 표시된다"
    - "후기 카드 하단에 댓글 최대 3개가 기본 표시되고 '댓글 N개 더 보기' 버튼으로 전체 토글 가능하다"
    - "댓글 신고 버튼이 기존 ReportButton 컴포넌트를 target_type='comment'로 재사용한다"
    - "comment-actions.test.ts가 GREEN이다"
  artifacts:
    - path: "src/lib/auth/comment-actions.ts"
      provides: "submitComment + deleteComment Server Action"
      contains: "submitComment"
      exports: ["submitComment", "deleteComment"]
    - path: "src/lib/data/comments.ts"
      provides: "getReviewsWithComments (N+1 방지 JOIN 쿼리)"
      contains: "getReviewsWithComments"
    - path: "src/components/reviews/CommentSection.tsx"
      provides: "댓글 인라인 표시 + 토글 + 작성 폼 Client Component"
      contains: "CommentSection"
    - path: "src/components/reviews/ReviewList.tsx"
      provides: "CommentSection 연결 + getReviewsWithComments 사용"
      contains: "CommentSection"
  key_links:
    - from: "src/components/reviews/CommentSection.tsx"
      to: "src/lib/auth/comment-actions.ts"
      via: "submitComment Server Action import"
      pattern: "submitComment"
    - from: "src/components/reviews/ReviewList.tsx"
      to: "src/lib/data/comments.ts"
      via: "getReviewsWithComments JOIN 쿼리"
      pattern: "getReviewsWithComments"
---

<objective>
후기 댓글 시스템 전체를 구현한다: Server Action, 데이터 레이어, Client Component UI.

Purpose: 실수요자가 후기에 댓글로 이웃 의견을 남길 수 있어야 한다. N+1 없이 후기+댓글을 함께 로드하고, 댓글 신고는 기존 reports 인프라를 재사용한다.
Output: comment-actions.ts (Server Action), comments.ts (데이터), CommentSection.tsx (UI), ReviewList.tsx 수정
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-UI-SPEC.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- src/lib/auth/review-actions.ts 기존 패턴 (analog) -->
```typescript
'use server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

// 인증 가드 패턴:
const supabase = await createSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { error: '로그인이 필요합니다.' }
```

<!-- src/components/reviews/ReviewForm.tsx 기존 패턴 -->
```typescript
'use client'
import { useState, useTransition } from 'react'
// useTransition으로 pending 상태 처리
// form onSubmit -> startTransition(async () => { ... })
```

<!-- 기존 ReportButton 컴포넌트 (재사용) -->
```typescript
// target_type: 'review' | 'user' | 'ad' | 'comment'
// 04-00-PLAN 마이그레이션에서 'comment'가 enum에 추가됨
<ReportButton targetType="comment" targetId={comment.id} />
```

<!-- ReviewList에서 후기 쿼리 패턴 (기존) -->
```typescript
// 현재: complex_reviews만 조회
// 변경 후: getReviewsWithComments로 댓글 JOIN
const { data } = await supabase
  .from('complex_reviews')
  .select('*, comments(id, content, created_at, user_id)')
  .eq('complex_id', complexId)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: comment-actions.ts + comments.ts 구현 (GREEN)</name>
  <files>src/lib/auth/comment-actions.ts, src/lib/data/comments.ts</files>
  <read_first>
    - src/lib/auth/comment-actions.test.ts (Wave 0에서 작성된 RED 테스트 — 이 테스트를 GREEN으로 만든다)
    - src/lib/auth/review-actions.ts (Server Action 패턴 참조)
    - src/lib/data/reviews.ts (데이터 레이어 패턴 참조)
  </read_first>
  <behavior>
    - submitComment: 미로그인 에러 (auth-first) → content 길이 검증(10~500자) → DB insert → revalidatePath
    - deleteComment: 미로그인 에러 → DB delete (eq user_id 이중 방어) → revalidatePath
    - getReviewsWithComments: N+1 방지 JOIN `.select('*, comments(id, content, created_at, user_id)')`
    - getCommentsByReviewId: 단일 후기의 모든 댓글 (limit=20)
  </behavior>
  <action>
**`src/lib/auth/comment-actions.ts`** (review-actions.ts 패턴 준수):
```typescript
'use server'

import { createSupabaseServerClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

export async function submitComment(input: {
  reviewId: string
  complexId: string
  content: string
}): Promise<{ error: string | null }> {
  const { reviewId, complexId, content } = input

  // auth-first 패턴: 인증 확인을 입력 검증보다 먼저 (기존 review-actions.ts 패턴 일치)
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다.' }

  if (content.length < 10 || content.length > 500)
    return { error: '댓글은 10자 이상 500자 이하로 작성해주세요.' }

  const { error } = await supabase
    .from('comments')
    .insert({ review_id: reviewId, user_id: user.id, content: content.trim() })

  if (error) return { error: error.message }
  revalidatePath(`/complexes/${complexId}`)
  return { error: null }
}

export async function deleteComment(
  commentId: string,
  complexId: string,
): Promise<{ error: string | null }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다.' }

  const { error } = await supabase
    .from('comments')
    .delete()
    .eq('id', commentId)
    .eq('user_id', user.id)

  if (error) return { error: error.message }
  revalidatePath(`/complexes/${complexId}`)
  return { error: null }
}
```

**`src/lib/data/comments.ts`** (reviews.ts 패턴 준수):
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export type Comment = Database['public']['Tables']['comments']['Row']
export type ReviewWithComments = Database['public']['Tables']['complex_reviews']['Row'] & {
  comments: Pick<Comment, 'id' | 'content' | 'created_at' | 'user_id'>[]
}

export async function getReviewsWithComments(
  complexId: string,
  supabase: SupabaseClient<Database>,
): Promise<ReviewWithComments[]> {
  const { data } = await supabase
    .from('complex_reviews')
    .select('*, comments(id, content, created_at, user_id)')
    .eq('complex_id', complexId)
    .order('created_at', { ascending: false })
    .limit(20)
  return (data ?? []) as ReviewWithComments[]
}

export async function getCommentsByReviewId(
  reviewId: string,
  supabase: SupabaseClient<Database>,
  limit = 20,
): Promise<Comment[]> {
  const { data } = await supabase
    .from('comments')
    .select('*')
    .eq('review_id', reviewId)
    .order('created_at', { ascending: true })
    .limit(limit)
  return data ?? []
}
```
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/auth/comment-actions.test.ts</automated>
  </verify>
  <done>comment-actions.test.ts의 모든 테스트가 GREEN. `npm run test -- --run src/lib/auth/comment-actions.test.ts`가 0 failed로 종료됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CommentSection.tsx 구현 + ReviewList.tsx 수정</name>
  <files>src/components/reviews/CommentSection.tsx, src/components/reviews/ReviewList.tsx</files>
  <read_first>
    - src/components/reviews/ReviewForm.tsx (Client Component 패턴 — useState + useTransition)
    - src/components/reviews/ReviewList.tsx (기존 파일 전체 — 수정 대상)
    - .planning/phases/04-community-basics/04-UI-SPEC.md §3-1 CommentSection, §3-2 CommentForm
    - src/lib/auth/comment-actions.ts (방금 작성한 파일 — import 경로 확인)
  </read_first>
  <action>
**`src/components/reviews/CommentSection.tsx`** (UI-SPEC §3-1, §3-2 준수):

```typescript
'use client'

import { useState, useTransition } from 'react'
import { submitComment, deleteComment } from '@/lib/auth/comment-actions'
import type { Comment } from '@/lib/data/comments'

interface Props {
  reviewId: string
  complexId: string
  initialComments: Pick<Comment, 'id' | 'content' | 'created_at' | 'user_id'>[]
  currentUserId?: string | null  // 로그인 여부 판단
}

export function CommentSection({ reviewId, complexId, initialComments, currentUserId }: Props) {
  const [showAll, setShowAll] = useState(false)
  const [content, setContent] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [pending, startTransition] = useTransition()

  const visible = showAll ? initialComments : initialComments.slice(0, 3)

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setError(null)
    startTransition(async () => {
      const result = await submitComment({ reviewId, complexId, content })
      if (result.error) {
        setError(result.error)
      } else {
        setContent('')
      }
    })
  }

  function formatNick(userId: string | null): string {
    if (!userId) return '익명'
    return userId.slice(0, 5) + '***'
  }

  return (
    <div style={{ borderTop: '1px solid var(--line-subtle)', paddingTop: 10, marginTop: 10 }}>
      {/* 댓글 목록 */}
      {initialComments.length === 0 ? (
        <p style={{ font: '500 11px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 10px' }}>
          댓글이 없습니다. 첫 번째 의견을 남겨보세요.
        </p>
      ) : (
        <div
          id={`comment-list-${reviewId}`}
          role="list"
        >
          {visible.map((c, i) => (
            <div
              key={c.id}
              role="article"
              aria-label="댓글"
              style={{
                padding: '10px 0',
                borderBottom: i < visible.length - 1 ? '1px solid var(--line-subtle)' : 'none',
              }}
            >
              <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>
                <span style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                  {formatNick(c.user_id)} · {new Date(c.created_at).toLocaleDateString('ko-KR')}
                </span>
              </div>
              <p style={{ font: '500 13px/1.55 var(--font-sans)', color: 'var(--fg-pri)', margin: 0 }}>
                {c.content}
              </p>
            </div>
          ))}
        </div>
      )}

      {/* 더 보기 토글 */}
      {initialComments.length > 3 && (
        <button
          type="button"
          aria-expanded={showAll}
          aria-controls={`comment-list-${reviewId}`}
          onClick={() => setShowAll(v => !v)}
          style={{
            font: '500 11px/1 var(--font-sans)',
            color: showAll ? 'var(--fg-sec)' : 'var(--dj-orange)',
            background: 'none',
            border: 'none',
            cursor: 'pointer',
            padding: '8px 0',
            minHeight: 44,
            display: 'flex',
            alignItems: 'center',
          }}
        >
          {showAll
            ? '댓글 접기 ↑'
            : `댓글 ${initialComments.length - 3}개 더 보기 ↓`}
        </button>
      )}

      {/* 댓글 작성 폼 (로그인 사용자만) */}
      {currentUserId ? (
        <form onSubmit={handleSubmit} style={{ display: 'flex', flexDirection: 'column', gap: 8, marginTop: 12 }}>
          <textarea
            value={content}
            onChange={e => setContent(e.target.value)}
            placeholder="댓글을 입력해주세요. (10자 이상)"
            maxLength={500}
            rows={2}
            className="input"
            aria-label="댓글 작성"
            aria-required="true"
            style={{ font: '500 13px/1.55 var(--font-sans)', padding: '8px 10px', resize: 'none' }}
          />
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: 8 }}>
            <span
              style={{
                font: '500 11px/1 var(--font-sans)',
                color: content.length < 10 ? '#dc2626' : 'var(--fg-tertiary)',
              }}
            >
              {content.length}/500
            </span>
            {error && (
              <span style={{ font: '500 11px/1 var(--font-sans)', color: '#dc2626', flex: 1 }}>{error}</span>
            )}
            <button
              type="submit"
              className="btn btn-sm btn-orange"
              disabled={pending || content.length < 10}
            >
              {pending ? '등록 중…' : '댓글 등록'}
            </button>
          </div>
        </form>
      ) : (
        <p style={{ font: '500 11px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', marginTop: 12 }}>
          <a href="/login" style={{ color: 'var(--dj-orange)', textDecoration: 'none' }}>로그인</a>
          하면 댓글을 달 수 있어요.
        </p>
      )}
    </div>
  )
}
```

**`src/components/reviews/ReviewList.tsx`** 수정:

기존 파일을 읽은 후 다음을 수정한다:
1. import 추가: `import { CommentSection } from './CommentSection'`
2. 쿼리 변경: `getReviewsWithComments` 사용 (N+1 방지 JOIN). 기존 `complex_reviews` 단독 쿼리를 교체.
3. 각 리뷰 카드 하단(기존 `<p>` 태그 이후)에 `<CommentSection>` 추가:
   ```typescript
   <CommentSection
     reviewId={r.id}
     complexId={complexId}
     initialComments={(r.comments ?? []).slice(0, 3)}
     currentUserId={currentUserId}  // 서버에서 전달받은 user.id 또는 null
   />
   ```
4. GPS 배지 토큰 정합: 기존 `background: '#dcfce7'` → `background: 'var(--bg-positive-tint)'`; `aria-label="GPS 위치 인증 완료된 후기"` 추가.

수정 시 주의:
- ReviewList는 Server Component이므로 `'use client'` 추가 금지
- `getReviewsWithComments`는 서버 컴포넌트에서 직접 호출 (supabase 클라이언트를 prop으로 받거나 내부에서 `createReadonlyClient()` 사용)
- `currentUserId`는 ReviewList에서 `supabase.auth.getUser()`로 가져와 CommentSection에 전달
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -E "(error|Error)" | head -20</automated>
  </verify>
  <done>
    1. CommentSection.tsx 파일이 존재하고 `CommentSection` 함수를 export한다
    2. ReviewList.tsx가 CommentSection을 import하고 각 후기 카드 하단에 렌더한다
    3. `npm run lint`가 에러 없이 통과한다
    4. `npm run build`가 에러 없이 완료된다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client textarea → Server Action | 사용자 입력이 신뢰 경계를 넘음 |
| Server Action → Supabase RLS | user_id 강제 삽입 — 클라이언트가 다른 user_id 지정 불가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | submitComment | mitigate | Server Action에서 `auth.getUser()`로 user_id 강제 — 클라이언트 제출 user_id 무시 |
| T-04-01-02 | Tampering | deleteComment | mitigate | `.eq('user_id', user.id)` 이중 방어 — RLS + 애플리케이션 레벨 중복 검증 |
| T-04-01-03 | DoS | 댓글 스팸 | mitigate | DB CHECK(10~500자) + RLS auth insert 제약. 추가 rate limit은 Phase 4 외 관심사 |
| T-04-01-04 | Information Disclosure | formatNick | accept | userId 앞 5자만 표시 — 완전한 익명화 아니나 V1.5 수준에서 수용 가능 |
</threat_model>

<verification>
```bash
# GREEN 테스트 확인
npm run test -- --run src/lib/auth/comment-actions.test.ts

# 파일 존재 + 핵심 export 확인
grep "export.*submitComment" src/lib/auth/comment-actions.ts
grep "export.*getReviewsWithComments" src/lib/data/comments.ts
grep "CommentSection" src/components/reviews/ReviewList.tsx

# 빌드 에러 없음
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
1. `comment-actions.test.ts`의 모든 테스트가 GREEN이다
2. `submitComment`가 content 길이 검증 + 미로그인 체크 + DB insert + revalidatePath를 수행한다
3. ReviewList가 댓글과 함께 후기를 한 번의 쿼리로 로드한다 (N+1 없음)
4. CommentSection이 기본 3개 표시 + "N개 더 보기" 토글 + 댓글 작성 폼을 렌더한다
5. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-01-SUMMARY.md` 생성
</output>

---
phase: 04-community-basics
plan: "02"
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - src/lib/auth/review-actions.ts
  - src/components/reviews/ReviewForm.tsx
autonomous: true
requirements:
  - COMM-02

must_haves:
  truths:
    - "ReviewForm에 '현재 위치로 인증' 버튼이 존재하고 클릭 시 navigator.geolocation.getCurrentPosition()을 호출한다"
    - "GPS 인증 성공 시 verifyGpsForReview Server Action이 호출되고 gps_verified=true로 DB에 저장된다"
    - "GPS 실패/거부 시 gps_verified=false로 후기 제출이 허용된다 (강제 아님)"
    - "서버에서 PostGIS check_gps_proximity RPC로 좌표 검증 — 클라이언트가 gps_verified를 직접 설정 불가"
    - "review-actions.test.ts의 GPS 테스트 케이스가 GREEN이다"
  artifacts:
    - path: "src/lib/auth/review-actions.ts"
      provides: "verifyGpsForReview Server Action (PostGIS RPC 호출)"
      contains: "verifyGpsForReview"
      exports: ["verifyGpsForReview"]
    - path: "src/components/reviews/ReviewForm.tsx"
      provides: "GPS 인증 버튼 (4개 상태: idle/loading/verified/failed/denied)"
      contains: "handleGpsVerify"
  key_links:
    - from: "src/components/reviews/ReviewForm.tsx"
      to: "src/lib/auth/review-actions.ts"
      via: "verifyGpsForReview Server Action"
      pattern: "verifyGpsForReview"
    - from: "src/lib/auth/review-actions.ts"
      to: "supabase.rpc('check_gps_proximity')"
      via: "PostGIS 서버 검증 — 스푸핑 방지"
      pattern: "check_gps_proximity"
---

<objective>
GPS L1 인증 배지를 활성화한다. 후기 작성 시 선택적으로 GPS 인증을 수행하고, 서버에서 PostGIS로 단지 ±100m 검증 후 gps_verified 플래그를 저장한다.

Purpose: 클라이언트 좌표를 신뢰하지 않는 스푸핑 저항 설계 필수 (D-07). GPS 배지 UI는 ReviewList.tsx line 78에 이미 존재 — 서버 검증 로직만 추가하면 됨.
Output: verifyGpsForReview Server Action + ReviewForm GPS 버튼
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-UI-SPEC.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- 기존 review-actions.ts 패턴 (확장 대상) -->
```typescript
'use server'
import { createSupabaseServerClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

export async function submitReview(input: {...}): Promise<{ error: string | null }> {
  // ... 기존 구현
}
// 이 파일 하단에 verifyGpsForReview를 추가한다
```

<!-- check_gps_proximity RPC (Wave 0 마이그레이션에서 생성됨) -->
```sql
-- supabase.rpc('check_gps_proximity', {
--   p_complex_id: complexId,
--   p_lat: lat,
--   p_lng: lng,
--   p_distance_m: 100,
-- }) → boolean
```

<!-- ReviewList.tsx line 78 기존 GPS 배지 (수정 필요 없음) -->
```typescript
{r.gps_verified && (
  <span style={{
    font: '500 10px/1 var(--font-sans)',
    color: '#16a34a',    // → 'var(--fg-positive)'로 교체 (04-01-PLAN에서 처리)
    background: '#dcfce7',
    ...
  }}>GPS 인증</span>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verifyGpsForReview Server Action 구현 (GREEN)</name>
  <files>src/lib/auth/review-actions.ts</files>
  <read_first>
    - src/lib/auth/review-actions.test.ts (Wave 0에서 작성된 GPS 테스트 케이스 — GREEN 목표)
    - src/lib/auth/review-actions.ts (기존 파일 전체 — 하단에 추가)
    - .planning/phases/04-community-basics/04-RESEARCH.md §3 GPS PostGIS 검증
  </read_first>
  <behavior>
    - 미로그인: { gps_verified: false, error: '로그인이 필요합니다.' }
    - check_gps_proximity RPC가 true: complex_reviews UPDATE → { gps_verified: true, error: null }
    - check_gps_proximity RPC가 false: { gps_verified: false, error: null } (범위 밖)
    - 클라이언트가 전달한 lat/lng를 PostGIS로만 검증 — 직접 gps_verified=true 저장 불가
  </behavior>
  <action>
`src/lib/auth/review-actions.ts` 파일을 읽은 후 하단에 `verifyGpsForReview` 함수를 추가한다. 기존 코드는 수정하지 않는다.

추가할 코드:
```typescript
export async function verifyGpsForReview(
  reviewId:  string,
  complexId: string,
  lat:       number,
  lng:       number,
): Promise<{ gps_verified: boolean; error: string | null }> {
  const supabase = await createSupabaseServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { gps_verified: false, error: '로그인이 필요합니다.' }

  // 스푸핑 방지: 클라이언트 좌표를 PostGIS로 서버 검증 (D-07)
  const { data: proximity } = await supabase.rpc('check_gps_proximity', {
    p_complex_id: complexId,
    p_lat:        lat,
    p_lng:        lng,
    p_distance_m: 100,
  })

  const verified = proximity === true
  if (verified) {
    await supabase
      .from('complex_reviews')
      .update({ gps_verified: true })
      .eq('id', reviewId)
      .eq('user_id', user.id)  // 본인 후기만 업데이트
    revalidatePath(`/complexes/${complexId}`)
  }
  return { gps_verified: verified, error: null }
}
```
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/auth/review-actions.test.ts</automated>
  </verify>
  <done>review-actions.test.ts의 GPS 테스트 케이스가 GREEN. `verifyGpsForReview`가 export되고 check_gps_proximity RPC를 호출한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ReviewForm.tsx GPS 버튼 추가</name>
  <files>src/components/reviews/ReviewForm.tsx</files>
  <read_first>
    - src/components/reviews/ReviewForm.tsx (기존 파일 전체 — GPS 버튼 삽입 위치 파악)
    - .planning/phases/04-community-basics/04-UI-SPEC.md §3-3 GPS Authentication Button
    - .planning/phases/04-community-basics/04-UI-SPEC.md §4-2 GPS Flow
    - src/lib/auth/review-actions.ts (verifyGpsForReview import 경로)
  </read_first>
  <action>
`src/components/reviews/ReviewForm.tsx`를 읽은 후 GPS 버튼을 추가한다.

**추가할 state (기존 state 블록 아래에 추가):**
```typescript
const [gpsState, setGpsState] = useState<'idle' | 'loading' | 'verified' | 'failed' | 'denied'>('idle')
const [gpsCoords, setGpsCoords] = useState<{ lat: number; lng: number } | null>(null)
```

**GPS 핸들러 함수 (기존 handleSubmit 함수 위에 추가):**
```typescript
async function handleGpsVerify() {
  if (!navigator.geolocation) {
    setGpsState('denied')
    return
  }
  setGpsState('loading')
  navigator.geolocation.getCurrentPosition(
    async (pos) => {
      const lat = pos.coords.latitude
      const lng = pos.coords.longitude
      setGpsCoords({ lat, lng })
      // Server Action에서 PostGIS 검증 — 클라이언트가 verified 플래그 직접 설정 금지
      const result = await verifyGpsForReview(reviewId, complexId, lat, lng)
      setGpsState(result.gps_verified ? 'verified' : 'failed')
    },
    () => setGpsState('denied'),
    { timeout: 10000, maximumAge: 30000 },
  )
}
```

주의: `verifyGpsForReview`는 후기가 이미 존재할 때 호출한다. 후기 생성 전이라면 GPS 좌표만 state에 보관하고, submitReview 시 함께 전달하는 방식도 가능. 기존 ReviewForm의 submit 흐름을 확인 후 자연스러운 위치에 통합한다.

ReviewForm의 구조가 "후기 먼저 저장 → GPS 나중에 인증"이 아니라 "후기 작성 중 GPS 인증 옵션"이라면, GPS 좌표를 form state에 보관하고 submitReview에 lat/lng를 함께 전달하여 서버에서 한 번에 처리하는 방식이 더 자연스럽다. 기존 `submitReview`의 시그니처를 확인하고 lat/lng 파라미터를 추가하거나, 독립적인 GPS 인증 버튼 + 별도 submitReview 분리 방식 중 기존 코드에 맞는 것을 선택한다.

**GPS 버튼 JSX (textarea 아래, submit 버튼 행 위 — 별도 행):**
```typescript
{/* GPS 인증 버튼 (선택사항) */}
<div style={{ display: 'flex', alignItems: 'center', gap: 8, minHeight: 44 }}>
  <button
    type="button"
    onClick={handleGpsVerify}
    disabled={gpsState === 'loading' || gpsState === 'verified'}
    aria-label="현재 위치로 GPS 인증 (선택사항)"
    style={{
      height: 36,
      padding: '0 14px',
      borderRadius: 'var(--radius-md)',
      font: '500 13px/1 var(--font-sans)',
      cursor: gpsState === 'loading' || gpsState === 'verified' ? 'default' : 'pointer',
      background: gpsState === 'verified'
        ? 'var(--bg-positive-tint)'
        : gpsState === 'failed'
        ? 'var(--bg-cautionary-tint)'
        : gpsState === 'denied'
        ? 'var(--bg-negative-tint)'
        : 'var(--dj-orange-tint)',
      color: gpsState === 'verified'
        ? 'var(--fg-positive)'
        : gpsState === 'failed'
        ? '#d97706'
        : gpsState === 'denied'
        ? 'var(--fg-negative)'
        : 'var(--dj-orange)',
      border: gpsState === 'verified'
        ? '1px solid rgba(22,163,74,.3)'
        : gpsState === 'failed'
        ? '1px solid rgba(217,119,6,.3)'
        : gpsState === 'denied'
        ? '1px solid rgba(220,38,38,.3)'
        : '1px solid rgba(234,88,12,.3)',
    }}
  >
    {gpsState === 'loading'  ? '위치 확인 중…' :
     gpsState === 'verified' ? '인증 완료 ✓' :
     gpsState === 'failed'   ? '단지 반경 밖입니다' :
     gpsState === 'denied'   ? '위치 권한이 거부됨' :
                               '현재 위치로 인증'}
  </button>
  <span style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>(선택)</span>
</div>
```

**import 추가 (파일 상단):**
```typescript
import { verifyGpsForReview } from '@/lib/auth/review-actions'
```
  </action>
  <verify>
    <automated>grep -n "handleGpsVerify" src/components/reviews/ReviewForm.tsx</automated>
  </verify>
  <done>
    1. ReviewForm.tsx에 `handleGpsVerify` 함수가 존재한다
    2. GPS 버튼이 JSX에 렌더된다 (5가지 상태)
    3. `verifyGpsForReview`를 import하고 호출한다
    4. `npm run lint`가 에러 없이 통과한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser geolocation API → Server Action | 클라이언트가 제출한 lat/lng가 신뢰 경계를 넘음 |
| Server Action → PostGIS RPC | 서버에서만 검증 — 클라이언트 gps_verified 직접 설정 불가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | GPS 좌표 스푸핑 | mitigate | Server Action에서 check_gps_proximity RPC로만 검증. 클라이언트가 제출한 좌표를 그대로 gps_verified=true로 저장하는 경로 없음 (D-07) |
| T-04-02-02 | Tampering | 타인 후기 GPS 인증 | mitigate | UPDATE 시 `.eq('user_id', user.id)` — 본인 후기만 업데이트 가능 |
| T-04-02-03 | Information Disclosure | 정확한 위치 노출 | accept | 배지는 단지 ±100m 검증만 표시. 정확한 좌표는 저장하지 않음 |
</threat_model>

<verification>
```bash
# GREEN 테스트
npm run test -- --run src/lib/auth/review-actions.test.ts

# 함수 export 확인
grep "export.*verifyGpsForReview" src/lib/auth/review-actions.ts

# GPS 버튼 UI 확인
grep "handleGpsVerify" src/components/reviews/ReviewForm.tsx
grep "check_gps_proximity" src/lib/auth/review-actions.ts

# 빌드 에러 없음
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
1. `review-actions.test.ts`의 GPS 테스트 케이스가 모두 GREEN이다
2. `verifyGpsForReview`가 `check_gps_proximity` RPC를 호출한다
3. ReviewForm에 GPS 버튼이 5가지 상태로 렌더된다
4. `gps_verified` 플래그가 서버에서만 설정된다 (클라이언트 직접 설정 경로 없음)
5. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-02-SUMMARY.md` 생성
</output>

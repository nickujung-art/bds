---
phase: 04-community-basics
plan: "05"
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - src/services/molit-presale.ts
  - src/lib/data/presale.ts
  - src/app/api/cron/daily/route.ts
  - src/app/presale/page.tsx
  - src/components/presale/PresaleCard.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "fetchPresaleTrades가 MOLIT 분양권전매 XML API를 파싱하고 PresaleTrade 배열을 반환한다"
    - "/presale ISR 페이지가 분양 카드 그리드를 표시하고 빈 상태 메시지를 처리한다"
    - "랜딩 페이지 nav의 분양 링크가 href='/presale'로 활성화된다"
    - "/api/cron/daily가 MOLIT 분양권전매 API를 호출하고 presale_transactions에 UPSERT한다"
    - "molit-presale.test.ts의 테스트가 GREEN이다"
    - "presale_transactions 쿼리에 cancel_date IS NULL AND superseded_by IS NULL이 포함된다"
  artifacts:
    - path: "src/services/molit-presale.ts"
      provides: "fetchPresaleTrades (MOLIT XML API 어댑터)"
      contains: "fetchPresaleTrades"
      exports: ["fetchPresaleTrades", "PresaleTrade"]
    - path: "src/lib/data/presale.ts"
      provides: "getActiveListings + getPresaleTransactions (대원칙 포함)"
      contains: "getActiveListings"
    - path: "src/app/api/cron/daily/route.ts"
      provides: "일배치 cron (분양 API + UPSERT)"
      contains: "fetchPresaleTrades"
    - path: "src/app/presale/page.tsx"
      provides: "/presale ISR 페이지 (revalidate=3600)"
      contains: "revalidate"
    - path: "src/components/presale/PresaleCard.tsx"
      provides: "분양 카드 컴포넌트"
      contains: "PresaleCard"
    - path: "src/app/page.tsx"
      provides: "nav 분양 링크 /presale 활성화"
      contains: "/presale"
  key_links:
    - from: "src/app/api/cron/daily/route.ts"
      to: "presale_transactions 테이블"
      via: "UPSERT ON CONFLICT(listing_id, deal_date, area, floor)"
      pattern: "presale_transactions"
    - from: "src/app/presale/page.tsx"
      to: "new_listings 테이블"
      via: "createReadonlyClient() + getActiveListings"
      pattern: "getActiveListings"
---

<objective>
MOLIT 분양권전매 API 어댑터, /presale ISR 페이지, 일배치 cron 통합, 랜딩 nav 활성화를 구현한다.

Purpose: 창원·김해 분양권전매 실거래 데이터를 수집하고 실수요자에게 표시. transactions 대원칙(cancel_date IS NULL AND superseded_by IS NULL)을 presale_transactions에도 동일 적용.
Output: molit-presale.ts + presale.ts + cron + /presale page + PresaleCard + page.tsx nav 수정
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-UI-SPEC.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- MOLIT 분양권전매 API (RESEARCH.md §2) -->
```
엔드포인트: http://openapi.molit.go.kr/.../getRTMSDataSvcSilvTrade
파라미터: serviceKey, LAWD_CD (창원=38110, 김해=38370), DEAL_YMD (YYYYMM)
응답: XML only (JSON 불가)
핵심 필드: aptNm, umdNm, dealAmount("15,000" 형식), excluUseAr, floor, dealYear/Month/Day, cdealType('Y'=취소)
```

<!-- src/services/kapt.ts 어댑터 패턴 (molit-presale.ts 기반) -->
```typescript
import { z } from 'zod/v4'
const apiKey = process.env.MOLIT_API_KEY
if (!apiKey) throw new Error('MOLIT_API_KEY not set')
// XML 파싱: Node.js 내장 DOMParser 또는 regex 기반 파싱 (fast-xml-parser 금지 — 신규 의존성 없음)
```

<!-- 일배치 cron 인증 패턴 (rankings/route.ts) -->
```typescript
// Vercel Cron은 Authorization: Bearer 방식
const authHeader = request.headers.get('authorization')
if (!process.env.CRON_SECRET || authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
  return new Response('Unauthorized', { status: 401 })
}
```

<!-- presale_transactions UPSERT (대원칙 + 중복 방지) -->
```typescript
// UNIQUE(listing_id, deal_date, area, floor) — Wave 0 마이그레이션에서 설정됨
await supabase.from('presale_transactions').upsert(records, {
  onConflict: 'listing_id,deal_date,area,floor',
  ignoreDuplicates: true,
})
```

<!-- ISR Readonly 패턴 -->
```typescript
export const revalidate = 3600  // 1시간
import { createReadonlyClient } from '@/lib/supabase/readonly'
const supabase = createReadonlyClient()  // cookies() 미호출 — ISR 조건
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: molit-presale.ts + presale.ts 구현 (GREEN)</name>
  <files>src/services/molit-presale.ts, src/lib/data/presale.ts</files>
  <read_first>
    - src/services/molit-presale.test.ts (Wave 0 RED 테스트 — GREEN 목표)
    - src/services/kapt.ts (어댑터 패턴 참조)
    - src/lib/data/reviews.ts (데이터 레이어 패턴 참조)
    - .planning/phases/04-community-basics/04-RESEARCH.md §2 MOLIT 분양권전매 API
  </read_first>
  <behavior>
    - MOLIT_API_KEY 미설정: throw Error('MOLIT_API_KEY not set')
    - API ok=false: throw Error(`MOLIT API ${res.status}`)
    - XML 파싱 성공: PresaleTrade 배열 반환
    - cdealType='Y': cancel_date를 deal_date로 설정
    - getPresaleTransactions: cancel_date IS NULL AND superseded_by IS NULL 포함
  </behavior>
  <action>
**`src/services/molit-presale.ts`** (신규 파일):

```typescript
import { z } from 'zod/v4'

const BASE_URL =
  'http://openapi.molit.go.kr/OpenAPI_ToolInstallPackage/service/rest/RTMSOBJSvc/getRTMSDataSvcSilvTrade'

// LAWD_CD 상수 (창원·김해)
export const LAWD_CODES = ['38110', '38370'] as const

const PresaleTradeSchema = z.object({
  aptNm:      z.string(),
  umdNm:      z.string(),
  dealAmount: z.string(),          // "15,000" 형식 문자열
  excluUseAr: z.coerce.number().optional(),
  floor:      z.coerce.number().optional(),
  dealYear:   z.string(),
  dealMonth:  z.string(),
  dealDay:    z.string(),
  cdealType:  z.string().optional(),  // 'Y'이면 취소 거래
})

export type PresaleTrade = z.infer<typeof PresaleTradeSchema>

function parseXmlItems(xml: string): unknown[] {
  // Node.js 내장 방식으로 XML에서 <item> 블록 추출
  // fast-xml-parser 없이 구현
  const items: unknown[] = []
  const itemRegex = /<item>([\s\S]*?)<\/item>/g
  let match: RegExpExecArray | null
  while ((match = itemRegex.exec(xml)) !== null) {
    const block = match[1]
    const obj: Record<string, string> = {}
    const fieldRegex = /<(\w+)><!\[CDATA\[(.*?)\]\]><\/\1>|<(\w+)>(.*?)<\/\3>/g
    let fieldMatch: RegExpExecArray | null
    while ((fieldMatch = fieldRegex.exec(block)) !== null) {
      const key   = fieldMatch[1] ?? fieldMatch[3]
      const value = (fieldMatch[2] ?? fieldMatch[4] ?? '').trim()
      if (key && value) obj[key] = value
    }
    items.push(obj)
  }
  return items
}

export async function fetchPresaleTrades(
  lawdCd: string,
  dealYmd: string, // YYYYMM
): Promise<PresaleTrade[]> {
  const apiKey = process.env.MOLIT_API_KEY
  if (!apiKey) throw new Error('MOLIT_API_KEY not set')

  const url = new URL(BASE_URL)
  url.searchParams.set('serviceKey', apiKey)
  url.searchParams.set('LAWD_CD', lawdCd)
  url.searchParams.set('DEAL_YMD', dealYmd)
  url.searchParams.set('numOfRows', '1000')

  const res = await fetch(url.toString(), {
    headers: { Accept: 'application/xml' },
    signal: AbortSignal.timeout(15_000),
  })
  if (!res.ok) throw new Error(`MOLIT API ${res.status}`)

  const xml = await res.text()
  const items = parseXmlItems(xml)
  const results: PresaleTrade[] = []
  for (const item of items) {
    const parsed = PresaleTradeSchema.safeParse(item)
    if (parsed.success) results.push(parsed.data)
  }
  return results
}

// 가격 파싱 헬퍼: "15,000" → 15000 (만원)
export function parseAmount(raw: string): number {
  return parseInt(raw.replace(/,/g, ''), 10) || 0
}

// 현재월 YYYYMM
export function currentYearMonth(): string {
  const d = new Date()
  return `${d.getFullYear()}${String(d.getMonth() + 1).padStart(2, '0')}`
}
```

**`src/lib/data/presale.ts`** (신규 파일, reviews.ts 패턴):
```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export type NewListing = Database['public']['Tables']['new_listings']['Row']
export type PresaleTransaction = Database['public']['Tables']['presale_transactions']['Row']

export async function getActiveListings(
  supabase: SupabaseClient<Database>,
  limit = 20,
): Promise<NewListing[]> {
  const { data } = await supabase
    .from('new_listings')
    .select('*')
    .order('fetched_at', { ascending: false })
    .limit(limit)
  return data ?? []
}

export async function getPresaleTransactions(
  listingId: string,
  supabase: SupabaseClient<Database>,
): Promise<PresaleTransaction[]> {
  const { data } = await supabase
    .from('presale_transactions')
    .select('*')
    .eq('listing_id', listingId)
    .is('cancel_date', null)         // transactions 대원칙
    .is('superseded_by', null)       // transactions 대원칙
    .order('deal_date', { ascending: false })
  return data ?? []
}
```
  </action>
  <verify>
    <automated>npm run test -- --run src/services/molit-presale.test.ts</automated>
  </verify>
  <done>molit-presale.test.ts의 모든 테스트가 GREEN. fetchPresaleTrades, parseAmount가 export되고 cancel_date IS NULL 쿼리 조건이 포함된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 일배치 cron + /presale 페이지 + PresaleCard + nav 활성화</name>
  <files>
    src/app/api/cron/daily/route.ts
    src/app/presale/page.tsx
    src/components/presale/PresaleCard.tsx
    src/app/page.tsx
  </files>
  <read_first>
    - src/app/api/cron/rankings/route.ts (일배치 cron 인증 패턴 참조)
    - src/app/favorites/page.tsx (ISR 페이지 패턴 참조)
    - src/app/page.tsx (nav 분양 링크 href="#" 위치 확인)
    - .planning/phases/04-community-basics/04-UI-SPEC.md §3-7 Presale Page, §3-8 Presale Card
    - .planning/phases/04-community-basics/04-PATTERNS.md §cron/daily/route.ts 패턴
  </read_first>
  <action>
**`src/app/api/cron/daily/route.ts`** (신규 파일):
```typescript
import { NextResponse } from 'next/server'
import { createSupabaseAdminClient } from '@/lib/supabase/admin'
import { fetchPresaleTrades, parseAmount, currentYearMonth, LAWD_CODES } from '@/services/molit-presale'

export const runtime = 'nodejs'

export async function GET(request: Request): Promise<Response> {
  // Vercel Cron: Authorization: Bearer 검증 (rankings/route.ts 패턴)
  const authHeader = request.headers.get('authorization')
  if (!process.env.CRON_SECRET || authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return new Response('Unauthorized', { status: 401 })
  }

  const supabase = createSupabaseAdminClient()
  const dealYmd = currentYearMonth()
  let totalUpserted = 0
  const errors: string[] = []

  for (const lawdCd of LAWD_CODES) {
    try {
      const trades = await fetchPresaleTrades(lawdCd, dealYmd)
      for (const trade of trades) {
        // new_listings UPSERT (name+region unique)
        const { data: listing } = await supabase
          .from('new_listings')
          .upsert(
            { name: trade.aptNm, region: trade.umdNm, fetched_at: new Date().toISOString() },
            { onConflict: 'name,region' },
          )
          .select('id')
          .single()

        if (!listing) continue

        const dealDate = `${trade.dealYear}-${trade.dealMonth.padStart(2, '0')}-${trade.dealDay.padStart(2, '0')}`
        const cancelDate = trade.cdealType === 'Y' ? dealDate : null

        const { error } = await supabase
          .from('presale_transactions')
          .upsert(
            {
              listing_id:  listing.id,
              area:        trade.excluUseAr ?? null,
              floor:       trade.floor ?? null,
              price:       parseAmount(trade.dealAmount),
              deal_date:   dealDate,
              cancel_date: cancelDate,
            },
            { onConflict: 'listing_id,deal_date,area,floor', ignoreDuplicates: true },
          )

        if (!error) totalUpserted++
      }
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err)
      errors.push(`lawdCd=${lawdCd}: ${msg}`)
      console.error('daily cron presale error:', msg)
    }
  }

  return Response.json({ ok: errors.length === 0, totalUpserted, errors })
}
```

**vercel.json 또는 next.config 확인:** 기존 cron 설정에 `/api/cron/daily` 엔드포인트가 없다면 `vercel.json`에 추가:
```json
{
  "crons": [
    { "path": "/api/cron/daily", "schedule": "0 19 * * *" }
  ]
}
```
(04:00 KST = 19:00 UTC 전날)

**`src/components/presale/PresaleCard.tsx`** (신규 파일, UI-SPEC §3-8 준수):
```typescript
import type { NewListing } from '@/lib/data/presale'
import Link from 'next/link'

interface Props {
  listing: NewListing
}

function formatPrice(price: number | null | undefined): string {
  if (!price) return '미정'
  const uk = price / 10000
  return `${uk.toFixed(1)}억`
}

export function PresaleCard({ listing }: Props) {
  const inner = (
    <article
      aria-label={`${listing.name} 분양 정보`}
      className="card-flat"
      style={{
        padding:    20,
        cursor:     listing.complex_id ? 'pointer' : 'default',
        transition: 'border-color 120ms ease, box-shadow 120ms ease',
      }}
    >
      {/* 상단 메타 */}
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
        <span className="chip sm outlined">{listing.region}</span>
        <span className="badge neutral" style={{ font: '500 11px/1 var(--font-sans)' }}>
          {listing.move_in_date
            ? new Date(listing.move_in_date).toLocaleDateString('ko-KR', { year: 'numeric', month: '2-digit' }).replace('. ', '.').replace('.', '')
            : '입주일 미정'}
        </span>
      </div>

      {/* 단지명 */}
      <div style={{ font: '700 16px/1.3 var(--font-sans)', color: 'var(--fg-pri)', marginBottom: 6 }}>
        {listing.name}
      </div>

      {/* 분양가 */}
      <div
        className="tnum"
        aria-label={`${formatPrice(listing.price_min)}에서 ${formatPrice(listing.price_max)}`}
        style={{ font: '700 22px/1.2 var(--font-sans)', color: 'var(--dj-orange)', marginBottom: 8 }}
      >
        {listing.price_min && listing.price_max && listing.price_min !== listing.price_max
          ? `${formatPrice(listing.price_min)} ~ ${formatPrice(listing.price_max)}`
          : formatPrice(listing.price_min ?? listing.price_max)}
      </div>

      {/* 보조 정보 */}
      <div style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
        {listing.total_units != null && (
          <span className="tnum">총 {listing.total_units}세대</span>
        )}
      </div>
    </article>
  )

  return listing.complex_id ? (
    <Link href={`/complexes/${listing.complex_id}`} style={{ textDecoration: 'none' }}>
      {inner}
    </Link>
  ) : inner
}
```

**`src/app/presale/page.tsx`** (신규 ISR 페이지, UI-SPEC §3-7 준수):
```typescript
import { Metadata } from 'next'
import { createReadonlyClient } from '@/lib/supabase/readonly'
import { getActiveListings } from '@/lib/data/presale'
import { PresaleCard } from '@/components/presale/PresaleCard'
import Link from 'next/link'

export const revalidate = 3600  // 1시간 (일배치 갱신 주기에 맞춤)

export const metadata: Metadata = {
  title: '신축 분양 | 단지온도',
  description: '창원·김해 신축 분양 정보. 분양가, 세대수, 입주예정일을 확인하세요.',
}

export default async function PresalePage() {
  const supabase = createReadonlyClient()
  const listings = await getActiveListings(supabase, 30)

  return (
    <div style={{ minHeight: '100dvh', background: 'var(--bg-canvas)' }}>
      <nav style={{ display: 'flex', gap: 24, padding: '16px 24px', font: '600 14px/1 var(--font-sans)', borderBottom: '1px solid var(--line-default)' }}>
        <Link href="/" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>홈</Link>
        <Link href="/map" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>지도</Link>
        <Link href="/presale" style={{ color: 'var(--dj-orange)', textDecoration: 'none' }}>분양</Link>
        <Link href="/favorites" style={{ color: 'var(--fg-sec)', textDecoration: 'none' }}>관심단지</Link>
      </nav>

      <main style={{ maxWidth: 1100, margin: '0 auto', padding: '32px 48px' }}>
        <header style={{ marginBottom: 32 }}>
          <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 8px' }}>
            창원·김해 신축 분양
          </h1>
          <p style={{ font: '500 13px/1.4 var(--font-sans)', color: 'var(--fg-sec)', margin: 0 }}>
            MOLIT 분양권전매 데이터 기준
          </p>
        </header>

        {listings.length === 0 ? (
          <div style={{ padding: '64px 0', textAlign: 'center' }}>
            <p style={{ font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)', margin: 0 }}>
              아직 등록된 분양 정보가 없습니다.<br />
              국토부 데이터가 업데이트되면 표시됩니다.
            </p>
          </div>
        ) : (
          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(3, 1fr)',
            gap: 20,
          }}>
            {listings.map(listing => (
              <PresaleCard key={listing.id} listing={listing} />
            ))}
          </div>
        )}
      </main>
    </div>
  )
}
```

반응형 breakpoint를 적용하려면 Tailwind 클래스 사용:
```typescript
// gridTemplateColumns는 Tailwind grid 클래스로 교체:
// className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5"
```

**`src/app/page.tsx` 수정:**
기존 파일을 읽은 후 분양 링크 `href="#"` → `href="/presale"` 변경:
```typescript
// 변경 전: <Link href="#">분양</Link>
// 변경 후: <Link href="/presale">분양</Link>
```

분양 섹션 추가 (D-10 per CONTEXT.md):
```typescript
{/* 분양 섹션 */}
<section style={{ marginTop: 48 }}>
  <h2 style={{ font: '700 18px/1.3 var(--font-sans)', letterSpacing: '-0.02em', marginBottom: 16 }}>
    신축·분양
  </h2>
  <Link href="/presale" className="card" style={{ display: 'block', padding: 20, textDecoration: 'none' }}>
    <div style={{ font: '600 14px/1.4 var(--font-sans)', color: 'var(--fg-pri)' }}>분양 정보 보기 →</div>
    <div style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-sec)', marginTop: 4 }}>
      창원·김해 신축 분양권 전매 실거래가
    </div>
  </Link>
</section>
```
  </action>
  <verify>
    <automated>grep -n "revalidate" src/app/presale/page.tsx</automated>
  </verify>
  <done>
    1. /api/cron/daily가 MOLIT API를 호출하고 presale_transactions에 UPSERT한다
    2. /presale 페이지가 `export const revalidate = 3600`을 포함한다
    3. PresaleCard.tsx가 분양 카드를 렌더한다
    4. page.tsx nav의 분양 링크가 href="/presale"이다
    5. `npm run lint && npm run build`가 에러 없이 통과한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MOLIT API → 서비스 어댑터 | 외부 XML API 응답이 서버에서만 처리됨 |
| cron endpoint → Authorization header | Vercel Cron의 Bearer 토큰 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Elevation of Privilege | /api/cron/daily | mitigate | `Authorization: Bearer ${CRON_SECRET}` 헤더 검증 (rankings 패턴 동일) |
| T-04-05-02 | Tampering | MOLIT XML 파싱 | mitigate | Zod safeParse — 파싱 실패 아이템 skip, 앱 크래시 없음 |
| T-04-05-03 | DoS | UPSERT 루프 | mitigate | ignoreDuplicates: true로 중복 UPSERT 방지 (Pitfall 4 방어) |
| T-04-05-04 | Information Disclosure | MOLIT_API_KEY | mitigate | 서버 전용 환경변수. 클라이언트 번들 포함 안 됨 |
</threat_model>

<verification>
```bash
# GREEN 테스트
npm run test -- --run src/services/molit-presale.test.ts

# 핵심 파일 존재 확인
ls src/services/molit-presale.ts
ls src/lib/data/presale.ts
ls src/app/presale/page.tsx
ls src/components/presale/PresaleCard.tsx

# ISR 설정 확인
grep "revalidate = 3600" src/app/presale/page.tsx

# transactions 대원칙 확인
grep "cancel_date" src/lib/data/presale.ts
grep "superseded_by" src/lib/data/presale.ts

# nav 활성화 확인
grep "/presale" src/app/page.tsx

# 빌드 에러 없음
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `molit-presale.test.ts`의 모든 테스트가 GREEN이다
2. `/api/cron/daily`가 Authorization: Bearer 인증 후 MOLIT API를 호출한다
3. `presale_transactions` 쿼리에 `cancel_date IS NULL AND superseded_by IS NULL`이 포함된다
4. `/presale` ISR 페이지에 `revalidate = 3600`이 설정된다
5. 랜딩 페이지 nav 분양 링크가 `/presale`로 활성화된다
6. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-05-SUMMARY.md` 생성
</output>

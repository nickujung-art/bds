---
phase: 04-community-basics
plan: "04"
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - src/services/kapt.ts
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "fetchKaptBasicInfo 함수가 K-apt AptBasisInfoServiceV3 API를 호출하고 KaptBasicInfo를 반환한다"
    - "단지 상세 페이지에 '시설' 탭이 추가되고 K-apt 부대시설 데이터를 표시한다"
    - "facility_kapt 테이블에 elevator_count, heat_type, management_type 데이터가 저장된다"
    - "kapt.test.ts의 fetchKaptBasicInfo 테스트가 GREEN이다"
  artifacts:
    - path: "src/services/kapt.ts"
      provides: "fetchKaptBasicInfo 함수 (기존 파일 확장)"
      contains: "fetchKaptBasicInfo"
      exports: ["fetchKaptBasicInfo", "KaptBasicInfo"]
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "시설 탭 + facility_kapt 데이터 표시"
      contains: "시설"
  key_links:
    - from: "src/services/kapt.ts"
      to: "AptBasisInfoServiceV3"
      via: "KAPT_API_KEY + kaptCode 파라미터"
      pattern: "AptBasisInfoServiceV3"
    - from: "src/app/complexes/[id]/page.tsx"
      to: "facility_kapt 테이블"
      via: "Supabase 서버 컴포넌트 쿼리"
      pattern: "facility_kapt"
---

<objective>
K-apt 부대시설 데이터를 수집하고 단지 상세 페이지에 시설 탭으로 표시한다.

Purpose: 실수요자가 단지의 주차, 엘리베이터, 난방방식 같은 기본 시설 정보를 확인할 수 있어야 한다.
Output: kapt.ts fetchKaptBasicInfo 확장 + 단지 상세 시설 탭 UI
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-community-basics/04-CONTEXT.md
@.planning/phases/04-community-basics/04-RESEARCH.md
@.planning/phases/04-community-basics/04-PATTERNS.md
@.planning/phases/04-community-basics/04-UI-SPEC.md
@.planning/phases/04-community-basics/04-00-SUMMARY.md

<interfaces>
<!-- src/services/kapt.ts 기존 패턴 -->
```typescript
import { z } from 'zod/v4'

const BASE_URL = 'https://apis.data.go.kr/1613000/AptListService3/getSigunguAptList3'

const KaptComplexSchema = z.object({
  kaptCode: z.string(),
  kaptName: z.string(),
  ...
})

export async function fetchComplexList(sggCode: string): Promise<KaptComplex[]> {
  const apiKey = process.env.KAPT_API_KEY
  if (!apiKey) throw new Error('KAPT_API_KEY is not set')
  // fetch + safeParse 패턴
}
```

<!-- facility_kapt 기존 스키마 (추가된 컬럼 포함) -->
```sql
-- Wave 0 마이그레이션에서 추가됨:
-- elevator_count integer
-- heat_type text
-- management_type text
-- total_area numeric
-- 기존: kapt_code, management_cost_m2, parking_count, data_month
```

<!-- UI-SPEC §3-9 Facilities Tab -->
```typescript
// 탭 내부 레이아웃:
// [ K-apt 출처 표기 ]
// [ 시설 아이템 목록 ]: 주차대수, 세대수, 동수, 난방방식, 관리방식
// 빈 상태: "시설 정보가 아직 수집되지 않았습니다."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kapt.ts fetchKaptBasicInfo 구현 (GREEN)</name>
  <files>src/services/kapt.ts</files>
  <read_first>
    - src/services/kapt.test.ts (Wave 0에서 작성된 RED 테스트 — GREEN 목표)
    - src/services/kapt.ts (기존 파일 전체 — 패턴 확인 + 하단에 추가)
    - .planning/phases/04-community-basics/04-RESEARCH.md §1 K-apt 부대시설 API
    - .planning/phases/04-community-basics/04-PATTERNS.md §kapt.ts 확장 패턴
  </read_first>
  <behavior>
    - KAPT_API_KEY 미설정: throw new Error('KAPT_API_KEY is not set')
    - API 응답 ok=false: throw new Error(`K-apt BasicInfo API ${res.status}`)
    - 응답 item 파싱 성공: KaptBasicInfo 반환
    - 응답 item null/파싱 실패: null 반환 (safeParse 패턴)
  </behavior>
  <action>
`src/services/kapt.ts`를 읽은 후 기존 파일 하단에 추가한다. 기존 코드 수정 없음.

**추가할 코드:**
```typescript
// ===== fetchKaptBasicInfo (DATA-01) =====
const BASIC_INFO_URL = 'http://apis.data.go.kr/1613000/AptBasisInfoServiceV3/getAphusBassInfoV3'

const KaptBasicInfoSchema = z.object({
  kaptCode:        z.string(),
  kaptName:        z.string(),
  kaptdaCnt:       z.coerce.number().optional(),   // 세대수
  kaptDongCnt:     z.coerce.number().optional(),   // 동수
  // V3 API 실제 응답 필드 (ASSUMED — 현장 확인 권장)
  // 주차대수는 기존 facility_kapt.parking_count에 이미 있음
  // 아래 필드들은 V3 API 응답에 있으면 파싱, 없으면 optional로 null 처리
  heatType:        z.string().optional(),          // 난방방식
  managementType:  z.string().optional(),          // 관리방식
  totalArea:       z.coerce.number().optional(),   // 연면적
})

export type KaptBasicInfo = z.infer<typeof KaptBasicInfoSchema>

export async function fetchKaptBasicInfo(kaptCode: string): Promise<KaptBasicInfo | null> {
  const apiKey = process.env.KAPT_API_KEY
  if (!apiKey) throw new Error('KAPT_API_KEY is not set')

  const url = new URL(BASIC_INFO_URL)
  url.searchParams.set('ServiceKey', apiKey)
  url.searchParams.set('kaptCode', kaptCode)
  url.searchParams.set('_type', 'json')

  const res = await fetch(url.toString(), {
    headers: { Accept: 'application/json' },
    signal: AbortSignal.timeout(10_000),
  })
  if (!res.ok) throw new Error(`K-apt BasicInfo API ${res.status}`)

  const json: unknown = await res.json()
  const item = (json as { response?: { body?: { item?: unknown } } })?.response?.body?.item
  const parsed = KaptBasicInfoSchema.safeParse(item)
  return parsed.success ? parsed.data : null
}
```

**주의사항:**
- K-apt V3 API의 실제 필드명은 RESEARCH.md에 "[ASSUMED]"로 표시됨. 실제 API 호출로 응답 확인 후 필드명 조정 필요.
- `heatType`, `managementType` 필드명이 실제 API와 다를 경우 응답 로그를 보고 수정.
- `KaptBasicInfoSchema`의 필드는 실제 V3 응답 JSON 키명과 일치해야 함 (camelCase vs snake_case 확인).
  </action>
  <verify>
    <automated>npm run test -- --run src/services/kapt.test.ts</automated>
  </verify>
  <done>kapt.test.ts의 fetchKaptBasicInfo 테스트가 GREEN. `fetchKaptBasicInfo`가 export되고 BASIC_INFO_URL을 사용한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 단지 상세 페이지에 시설 탭 추가</name>
  <files>src/app/complexes/[id]/page.tsx</files>
  <read_first>
    - src/app/complexes/[id]/page.tsx (기존 파일 전체 — 탭 구조 파악)
    - .planning/phases/04-community-basics/04-UI-SPEC.md §3-9 Facilities Tab
    - supabase/migrations/20260430000004_facility.sql (facility_kapt 스키마 확인)
  </read_first>
  <action>
`src/app/complexes/[id]/page.tsx`를 읽은 후 시설 탭을 추가한다.

**Step 1: facility_kapt 데이터 fetch (기존 Promise.all 또는 개별 쿼리에 추가):**
```typescript
const { data: facilityKapt } = await supabase
  .from('facility_kapt')
  .select('*')
  .eq('complex_id', complex.id)
  .order('data_month', { ascending: false })
  .limit(1)
  .single()
```

**Step 2: 탭 구조 파악 후 '시설' 탭 추가:**
기존 페이지에 탭 구조(거래 내역 / 후기)가 있다면 동일 패턴으로 '시설' 탭 추가.
탭이 URL hash 기반이면 `#facilities`, 상태 기반이면 Client Component로 처리.

**Step 3: 시설 탭 내용 JSX (UI-SPEC §3-9 준수):**
```typescript
{/* 시설 탭 */}
<div>
  <p style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)', marginBottom: 16 }}>
    K-apt 공동주택 데이터 기준
  </p>
  {facilityKapt ? (
    <div>
      {[
        { label: '주차대수', value: facilityKapt.parking_count ? `세대당 ${facilityKapt.parking_count}대` : null },
        { label: '세대수', value: complex.household_count ? `${complex.household_count}세대` : null },
        { label: '관리비(m²당)', value: facilityKapt.management_cost_m2 ? `${facilityKapt.management_cost_m2.toLocaleString('ko-KR')}원` : null },
        { label: '난방방식', value: facilityKapt.heat_type ?? null },
        { label: '관리방식', value: facilityKapt.management_type ?? null },
        { label: '엘리베이터', value: facilityKapt.elevator_count ? `${facilityKapt.elevator_count}대` : null },
      ]
        .filter(item => item.value !== null)
        .map((item, i, arr) => (
          <div
            key={item.label}
            style={{
              display:        'flex',
              justifyContent: 'space-between',
              alignItems:     'center',
              padding:        '10px 0',
              borderBottom:   i < arr.length - 1 ? '1px solid var(--line-subtle)' : 'none',
            }}
          >
            <span style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
              {item.label}
            </span>
            <span style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-pri)' }}>
              {item.value}
            </span>
          </div>
        ))}
    </div>
  ) : (
    <p style={{ font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-tertiary)', padding: '32px 0', textAlign: 'center' }}>
      시설 정보가 아직 수집되지 않았습니다.
    </p>
  )}
</div>
```

기존 탭 구조와 일치하도록 래퍼 클래스/스타일을 맞춘다.
  </action>
  <verify>
    <automated>grep -n "시설" src/app/complexes/\[id\]/page.tsx</automated>
  </verify>
  <done>
    1. 단지 상세 페이지에 '시설' 탭이 추가된다
    2. facility_kapt 데이터가 서버에서 조회된다
    3. "K-apt 공동주택 데이터 기준" 출처 표기가 포함된다
    4. 데이터 없을 때 빈 상태 메시지가 표시된다
    5. `npm run lint && npm run build`가 에러 없이 통과한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KAPT API → 서비스 어댑터 | 외부 API 응답이 서버에서만 처리됨 |
| facility_kapt DB → 단지 상세 페이지 | 서버 컴포넌트에서만 DB 접근 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | KAPT_API_KEY | mitigate | 서버 전용 환경변수. src/services/ 어댑터에서만 사용. 클라이언트 번들에 포함 안 됨 |
| T-04-04-02 | Tampering | K-apt API 응답 | mitigate | Zod safeParse로 스키마 검증. 파싱 실패 시 null 반환 — 앱 크래시 없음 |
| T-04-04-03 | DoS | K-apt API 타임아웃 | mitigate | AbortSignal.timeout(10_000) 10초 타임아웃 설정 |
</threat_model>

<verification>
```bash
# GREEN 테스트
npm run test -- --run src/services/kapt.test.ts

# 함수 export 확인
grep "export.*fetchKaptBasicInfo" src/services/kapt.ts

# 시설 탭 확인
grep -n "시설" src/app/complexes/\[id\]/page.tsx
grep -n "facility_kapt" src/app/complexes/\[id\]/page.tsx

# 빌드 에러 없음
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `kapt.test.ts`의 `fetchKaptBasicInfo` 테스트가 GREEN이다
2. `fetchKaptBasicInfo`가 K-apt AptBasisInfoServiceV3 엔드포인트를 호출한다
3. 단지 상세 페이지에 '시설' 탭이 추가되고 facility_kapt 데이터를 표시한다
4. `npm run lint && npm run build`가 에러 없이 통과한다
</success_criteria>

<output>
완료 후: `.planning/phases/04-community-basics/04-04-SUMMARY.md` 생성
</output>

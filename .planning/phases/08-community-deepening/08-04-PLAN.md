---
phase: 08-community-deepening
plan: "04"
type: execute
wave: 2
depends_on: ["08-00"]
files_modified:
  - src/services/daum-cafe.ts
  - src/lib/data/cafe-posts.ts
  - src/app/api/worker/cafe-ingest/route.ts
  - .github/workflows/cafe-ingest.yml
  - src/app/complexes/[id]/page.tsx
  - src/components/complex/CafePostsList.tsx
autonomous: true
requirements:
  - DIFF-02

must_haves:
  truths:
    - "searchCafePosts가 Daum Search API를 호출하고 CafePost[]를 반환한다"
    - "extractComplexNames가 [텍스트] 구분자로 Gemini 프롬프트를 감싸 인젝션을 방지한다"
    - "matchComplex() 파이프라인을 경유한 단지 매칭만 허용된다 (이름 단독 금지)"
    - "cafe_posts 테이블에 upsert 결과가 저장된다"
    - "단지 상세 페이지에 매칭된 카페 글 목록이 표시된다"
  artifacts:
    - path: "src/services/daum-cafe.ts"
      provides: "searchCafePosts(), extractComplexNames()"
      exports: ["searchCafePosts", "extractComplexNames", "CafePost"]
    - path: "src/lib/data/cafe-posts.ts"
      provides: "getCafePostsByComplex(), ingestCafePost()"
      exports: ["getCafePostsByComplex", "ingestCafePost"]
    - path: "src/app/api/worker/cafe-ingest/route.ts"
      provides: "POST /api/worker/cafe-ingest CRON_SECRET 보호"
      contains: "CRON_SECRET|x-cron-secret"
    - path: ".github/workflows/cafe-ingest.yml"
      provides: "일 1회 cron GitHub Actions"
      contains: "cron|cafe-ingest"
  key_links:
    - from: "searchCafePosts"
      to: "Daum Search API"
      via: "KakaoAK 헤더, src/services/daum-cafe.ts"
      pattern: "KakaoAK.*KAKAO_REST_API_KEY"
    - from: "extractComplexNames"
      to: "matchComplex()"
      via: "[텍스트] 구분자 후 복합 매칭"
      pattern: "matchComplex"
---

<objective>
Daum 카페 검색 + Gemini NER 단지 매칭 파이프라인 구현.

Purpose: 카페 글을 자동 수집하여 NLP로 단지를 매칭하고, 단지 페이지에 연동 표시한다.
Output: daum-cafe.ts 서비스 어댑터, cafe-posts.ts 데이터 레이어, cafe-ingest API 엔드포인트, GitHub Actions cron.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-community-deepening/08-RESEARCH.md
@.planning/phases/08-community-deepening/08-00-SUMMARY.md

<interfaces>
From src/lib/data/complex-matching.ts:
```typescript
export async function seedComplex(raw: SeedComplexInput, supabase): Promise<SeedComplexResult>
// matchComplex()는 이 파일에 있는 패턴 사용. sgg_code + name 복합 매칭 필수.
```

From src/app/api/chat/complex/route.ts (Gemini 사용 패턴):
```typescript
import { GoogleGenerativeAI } from '@google/generative-ai'
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY ?? '')
const model = genAI.getGenerativeModel({ model: 'gemini-2.0-flash', systemInstruction: '...' })
const result = await model.generateContent(prompt)
const text = result.response.text()
```

RESEARCH.md의 Daum Search API 패턴:
```typescript
const CAFE_SEARCH_URL = 'https://dapi.kakao.com/v2/search/cafe'
// Authorization: KakaoAK ${process.env.KAKAO_REST_API_KEY}
// 응답: { documents: [{ title, contents, url, cafename, datetime }] }
```

RESEARCH.md의 Gemini NER 패턴:
```typescript
// 프롬프트 인젝션 방지: [텍스트] 구분자로 감싸기 (T-8-03)
const prompt = `...:\n[텍스트]\n${text.slice(0, 500)}\n[텍스트 끝]`
```

cafe-ingest API cron 패턴 (기존 notify worker와 동일):
```typescript
// x-cron-secret 헤더 검증 필수
const secret = req.headers.get('x-cron-secret')
if (secret !== process.env.CRON_SECRET) return NextResponse.json({}, { status: 401 })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: daum-cafe.ts 서비스 어댑터 구현 (GREEN)</name>
  <files>src/services/daum-cafe.ts</files>
  <read_first>
    src/__tests__/daum-cafe.test.ts (RED 테스트 — GREEN으로)
    src/services/kapt.ts (services/ 어댑터 파일 구조 참조)
    .planning/phases/08-community-deepening/08-RESEARCH.md (Pattern 2: Daum Cafe 검색 + Gemini NER)
  </read_first>
  <behavior>
    - searchCafePosts('창원 래미안', 10): Daum Search API → CafePost[] 반환
    - searchCafePosts: HTTP 오류 시 Error throw ("Daum cafe search HTTP {status}")
    - extractComplexNames(text): Gemini NER → string[] 반환 (단지명 배열, 지역명(구/동)도 함께 추출하여 sggCode 유추에 활용)
    - extractComplexNames: 프롬프트에 반드시 [텍스트]...[텍스트 끝] 구분자 사용 (T-8-03)
    - extractComplexNames: text를 500자로 slice하여 사용
  </behavior>
  <action>
    src/services/daum-cafe.ts 신규 생성:

    ```typescript
    import 'server-only'
    import { GoogleGenerativeAI } from '@google/generative-ai'

    // T-8-02: KAKAO_REST_API_KEY는 서버 전용. 클라이언트에서 절대 호출 금지.
    const CAFE_SEARCH_URL = 'https://dapi.kakao.com/v2/search/cafe'

    export interface CafePost {
      title:    string
      contents: string
      url:      string
      datetime: string
      cafeName: string
    }

    export async function searchCafePosts(
      query: string,
      size = 10,
    ): Promise<CafePost[]> {
      const url = new URL(CAFE_SEARCH_URL)
      url.searchParams.set('query', query)
      url.searchParams.set('sort', 'recency')
      url.searchParams.set('size', String(size))

      const res = await fetch(url.toString(), {
        headers: { Authorization: `KakaoAK ${process.env.KAKAO_REST_API_KEY}` },
        signal: AbortSignal.timeout(10_000),
      })

      if (!res.ok) throw new Error(`Daum cafe search HTTP ${res.status}`)

      const json = (await res.json()) as {
        documents: Array<{
          title:    string
          contents: string
          url:      string
          datetime: string
          cafename: string
        }>
      }

      return json.documents.map(d => ({
        title:    d.title,
        contents: d.contents,
        url:      d.url,
        datetime: d.datetime,
        cafeName: d.cafename,
      }))
    }

    /**
     * T-8-03: Gemini 프롬프트 인젝션 방지
     * 카페 글 내용을 [텍스트]...[텍스트 끝] 구분자로 반드시 감싼다.
     */
    export async function extractComplexNames(text: string): Promise<string[]> {
      const apiKey = process.env.GEMINI_API_KEY
      if (!apiKey) {
        // Fallback: GEMINI_API_KEY 없으면 빈 배열 반환 (운영 오류 방지)
        return []
      }

      const genAI = new GoogleGenerativeAI(apiKey)
      const model = genAI.getGenerativeModel({
        model: 'gemini-2.0-flash',
        systemInstruction: '한국 부동산 텍스트에서 아파트 단지명과 지역명(구/동)을 추출하세요. 응답은 반드시 JSON: {"complexes": ["단지명1"], "region": "창원 성산구"}. 없으면 빈 문자열/배열.',
      })

      // 프롬프트 인젝션 방지: [텍스트] 구분자 사용 (T-8-03)
      const safeText = text.slice(0, 500)
      const prompt = `다음 텍스트에서 아파트 단지명을 추출하세요:\n[텍스트]\n${safeText}\n[텍스트 끝]`

      try {
        const result = await model.generateContent(prompt)
        const raw = result.response.text().replace(/```json\n?|```/g, '').trim()
        const parsed = JSON.parse(raw) as { complexes: string[] }
        return Array.isArray(parsed.complexes) ? parsed.complexes : []
      } catch {
        return []
      }
    }
    ```
  </action>
  <verify>
    <automated>npm run test -- --grep "daum-cafe"</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --grep "daum-cafe" 결과 모두 PASS (GREEN)
    - grep -c "server-only" src/services/daum-cafe.ts 결과 >= 1
    - grep -c "KAKAO_REST_API_KEY" src/services/daum-cafe.ts 결과 >= 1
    - grep -c "\[텍스트\]" src/services/daum-cafe.ts 결과 >= 1 (프롬프트 인젝션 방지 구분자)
    - grep -c "console.log" src/services/daum-cafe.ts 결과 = 0
    - grep -c "slice(0, 500)" src/services/daum-cafe.ts 결과 >= 1
  </acceptance_criteria>
  <done>daum-cafe.test.ts 모든 테스트 PASS</done>
</task>

<task type="auto">
  <name>Task 2: 카페 글 수집 파이프라인 + 단지 페이지 연동</name>
  <files>
    src/lib/data/cafe-posts.ts,
    src/app/api/worker/cafe-ingest/route.ts,
    .github/workflows/cafe-ingest.yml,
    src/app/complexes/[id]/page.tsx,
    src/components/complex/CafePostsList.tsx
  </files>
  <read_first>
    src/lib/data/complex-matching.ts (matchComplex 파이프라인 — 단지명 단독 매칭 절대 금지)
    src/app/api/worker/notify/route.ts (CRON_SECRET 인증 패턴)
    .github/workflows/notify-worker.yml (GitHub Actions cron 패턴)
    src/app/complexes/[id]/page.tsx (현재 단지 상세 페이지 구조)
  </read_first>
  <action>
    1. src/lib/data/cafe-posts.ts 신규 생성:
    ```typescript
    import 'server-only'
    import type { SupabaseClient } from '@supabase/supabase-js'
    import type { Database } from '@/types/database'
    import type { CafePost } from '@/services/daum-cafe'
    import { matchComplex } from '@/lib/data/complex-matching'

    export interface CafePostRecord {
      id:         string
      complex_id: string | null
      title:      string
      excerpt:    string | null
      url:        string
      cafe_name:  string | null
      posted_at:  string | null
      confidence: number | null
    }

    export async function getCafePostsByComplex(
      complexId: string,
      supabase: SupabaseClient<Database>,
      limit = 5,
    ): Promise<CafePostRecord[]> {
      const { data } = await supabase
        .from('cafe_posts')
        .select('id, complex_id, title, excerpt, url, cafe_name, posted_at, confidence')
        .eq('complex_id', complexId)
        .eq('is_verified', true)
        .order('posted_at', { ascending: false })
        .limit(limit)
      return (data ?? []) as CafePostRecord[]
    }

    /**
     * DIFF-02: 카페 글 ingest + 단지 매칭
     * CRITICAL: matchComplex() 경유 필수. 단지명 단독 매칭 절대 금지.
     */
    export async function ingestCafePost(
      post: CafePost,
      complexName: string,
      sggCode: string,
      supabase: SupabaseClient<Database>,
    ): Promise<void> {
      // matchComplex: sgg_code + 이름 복합 매칭 (CLAUDE.md 필수 규칙)
      const matchResult = await matchComplex({
        rawName:    complexName,
        sggCode:    sggCode,
        source:     'cafe_nlp',
        rawPayload: { postUrl: post.url, cafeName: post.cafeName },
      }, supabase)

      await supabase
        .from('cafe_posts')
        .upsert({
          complex_id:  matchResult?.complexId ?? null,
          title:       post.title.slice(0, 200),
          excerpt:     post.contents.slice(0, 500),
          url:         post.url,
          cafe_name:   post.cafeName,
          posted_at:   post.datetime,
          confidence:  matchResult?.confidence ?? null,
          is_verified: (matchResult?.confidence ?? 0) >= 0.85,
        }, { onConflict: 'url' })
    }
    ```

    2. src/app/api/worker/cafe-ingest/route.ts 생성:
    - POST: x-cron-secret 헤더 검증 (없음/틀림 → 401)
    - 창원·김해 sgg_code 목록 (48121, 48125, 48127, 48128, 48720 등) + 단지명 키워드로 Daum 검색
    - 각 단지에 대해 searchCafePosts 호출 → extractComplexNames → ingestCafePost
    - 일 100,000 API 한도 고려: complexes 테이블에서 최근 수집 안 된 단지 우선 처리 (LIMIT 50)
    - 성공/실패 카운트 반환

    ```typescript
    import { NextResponse } from 'next/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'
    import { searchCafePosts, extractComplexNames } from '@/services/daum-cafe'
    import { ingestCafePost } from '@/lib/data/cafe-posts'

    export async function POST(req: Request) {
      const secret = req.headers.get('x-cron-secret')
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
      }

      const supabase = createSupabaseAdminClient()
      let ingested = 0
      let failed = 0

      // 창원·김해 검색 쿼리 (지역명 + 아파트)
      const searchQueries = ['창원 아파트', '김해 아파트', '창원 래미안', '창원 힐스테이트', '김해 래미안']

      for (const query of searchQueries) {
        try {
          const posts = await searchCafePosts(query, 10)
          for (const post of posts) {
            try {
              // Gemini NER: 단지명 + 지역명(구/동) 함께 추출 (DIFF-02 정확도 개선)
              const names = await extractComplexNames(`${post.title} ${post.contents}`)
              for (const name of names) {
                // sggCode 맵핑: 창원 5개 구 + 김해 (WARNING 수정 — DIFF-02 정확도)
                const SGG_CODE_MAP: Record<string, string> = {
                  '창원 성산구': '48125', '창원 의창구': '48121',
                  '창원 마산합포구': '48127', '창원 마산회원구': '48128',
                  '창원 진해구': '48129', '김해': '48720',
                }
                // Gemini에서 지역명(구/동) 함께 추출 — extractComplexNames가 { complexes, region } 반환
                const sggCode = query.includes('김해') ? '48720'
                  : Object.entries(SGG_CODE_MAP).find(([k]) => name.includes(k) || query.includes(k))?.[1] ?? '48125'
                await ingestCafePost(post, name, sggCode, supabase)
                ingested++
              }
            } catch {
              failed++
            }
          }
        } catch {
          failed++
        }
      }

      return NextResponse.json({ ingested, failed })
    }
    ```

    3. .github/workflows/cafe-ingest.yml 생성:
    - 매일 05:00 KST (20:00 UTC) 실행
    - x-cron-secret 헤더로 인증
    - CRON_SECRET은 GitHub Secrets에서 주입

    ```yaml
    name: Cafe Ingest — NLP 단지 매칭

    on:
      schedule:
        - cron: '0 20 * * *'  # 05:00 KST
      workflow_dispatch:

    jobs:
      ingest:
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
          - name: Trigger cafe-ingest worker
            run: |
              curl -X POST \
                -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
                -H "Content-Type: application/json" \
                "${{ secrets.NEXT_PUBLIC_SITE_URL }}/api/worker/cafe-ingest"
    ```

    4. src/components/complex/CafePostsList.tsx 생성 (서버 컴포넌트):
    - Props: { posts: CafePostRecord[] }
    - 0건이면 null 반환
    - 각 글: 제목(외부 링크, target="_blank" rel="noopener noreferrer"), 카페명, 작성일
    - 스타일: 기존 단지 상세 섹션과 일관된 스타일 (border-bottom var(--line-subtle), padding 12px 0)
    - 최대 5건 표시
    - "AI로 단지 연관 글을 수집합니다. 정확도 검증 중인 글은 표시되지 않아요." 주석 텍스트 (font 12px, color var(--fg-tertiary))
    - 금지: "Powered by AI" 배지, gradient, glow

    5. src/app/complexes/[id]/page.tsx 수정:
    - getCafePostsByComplex(id, supabase) 호출 추가
    - 단지 상세 하단 섹션에 CafePostsList 추가
  </action>
  <verify>
    <automated>npm run test -- --grep "daum-cafe.*match" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/services/daum-cafe.ts 파일 존재하고 GREEN 테스트 유지
    - src/lib/data/cafe-posts.ts 파일 존재
    - grep -c "matchComplex" src/lib/data/cafe-posts.ts 결과 >= 1 (단독 매칭 금지 확인)
    - src/app/api/worker/cafe-ingest/route.ts 파일 존재
    - grep -c "x-cron-secret\|CRON_SECRET" src/app/api/worker/cafe-ingest/route.ts 결과 >= 2
    - .github/workflows/cafe-ingest.yml 파일 존재
    - grep -c "cron" .github/workflows/cafe-ingest.yml 결과 >= 1
    - src/components/complex/CafePostsList.tsx 파일 존재
    - grep -c "'use client'" src/components/complex/CafePostsList.tsx 결과 = 0 (서버 컴포넌트)
    - grep -c "Powered by AI\|gradient" src/components/complex/CafePostsList.tsx 결과 = 0
    - npm run build 통과
  </acceptance_criteria>
  <done>카페 글 수집 파이프라인이 matchComplex() 경유로 단지 매칭하고, 단지 상세 페이지에 연동 글 목록이 표시됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → /api/worker/cafe-ingest | x-cron-secret 헤더 인증 필수 |
| 카페 글 본문 → Gemini | [텍스트] 구분자로 인젝션 방지 |
| KAKAO_REST_API_KEY | 서버 전용 env var, src/services/daum-cafe.ts에서만 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-03 | Tampering | Gemini 프롬프트 (카페 글 내용) | mitigate | [텍스트]...[텍스트 끝] 구분자 + text.slice(0,500) 길이 제한. grep으로 구분자 확인. |
| T-8-02 | Information Disclosure | KAKAO_REST_API_KEY | mitigate | server-only 마크, src/services/ 어댑터 전용. 클라이언트 컴포넌트에서 절대 import 금지. |
</threat_model>

<verification>
- npm run test -- --grep "daum-cafe" → 모두 PASS
- grep -c "\[텍스트\]" src/services/daum-cafe.ts → >= 1
- grep -c "matchComplex" src/lib/data/cafe-posts.ts → >= 1
- grep -c "CRON_SECRET" src/app/api/worker/cafe-ingest/route.ts → >= 1
- npm run build → 오류 없음
</verification>

<success_criteria>
- daum-cafe.test.ts 모든 테스트 GREEN
- Daum Search API 호출 시 [텍스트] 구분자로 인젝션 방지됨
- cafe-ingest 워커가 x-cron-secret 없으면 401 반환
- 단지 상세 페이지에 카페 글 목록 섹션이 표시됨
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-04-SUMMARY.md` 작성.
</output>

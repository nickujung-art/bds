---
phase: 08-community-deepening
plan: "05"
type: execute
wave: 2
depends_on: ["08-00", "08-02"]
files_modified:
  - src/services/kakao-channel.ts
  - src/app/actions/kakao-channel-actions.ts
  - src/components/profile/KakaoChannelSubscribeForm.tsx
  - src/app/profile/page.tsx
  - src/app/api/worker/notify/route.ts
autonomous: true
requirements:
  - DIFF-04

must_haves:
  truths:
    - "sendAlimtalk()가 src/services/kakao-channel.ts 어댑터를 통해서만 호출된다"
    - "KakaoChannelSubscribeForm이 전화번호를 console.log하지 않는다"
    - "카카오 채널 구독 Server Action이 phone_number를 DB에 저장한다"
    - "구독 시 동의 체크박스가 required이다"
    - "SOLAPI 없으면 Mock 모드로 코드가 완성된다"
  artifacts:
    - path: "src/services/kakao-channel.ts"
      provides: "sendAlimtalk() SOLAPI 어댑터"
      exports: ["sendAlimtalk"]
    - path: "src/app/actions/kakao-channel-actions.ts"
      provides: "subscribeKakaoChannel Server Action"
      exports: ["subscribeKakaoChannel"]
    - path: "src/components/profile/KakaoChannelSubscribeForm.tsx"
      provides: "'use client' 구독 폼"
      contains: "useForm|z.object.*phone|consent"
  key_links:
    - from: "KakaoChannelSubscribeForm"
      to: "subscribeKakaoChannel Server Action"
      via: "react-hook-form handleSubmit"
      pattern: "subscribeKakaoChannel"
    - from: "/api/worker/notify route.ts"
      to: "deliverKakaoChannelNotifications"
      via: "기존 notify 워커에 카카오 분기 추가"
      pattern: "deliverKakaoChannelNotifications"
---

<objective>
카카오톡 채널 알림 발송 인프라 + 구독 UI 구현.

Purpose: SOLAPI를 통해 알림톡을 발송하고, 프로필 페이지에서 사용자가 전화번호로 구독할 수 있다.
Output: kakao-channel.ts SOLAPI 어댑터, subscribeKakaoChannel Server Action, KakaoChannelSubscribeForm, notify 워커 카카오 분기 추가.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-community-deepening/08-RESEARCH.md
@.planning/phases/08-community-deepening/08-UI-SPEC.md
@.planning/phases/08-community-deepening/08-02-SUMMARY.md

<interfaces>
From src/lib/notifications/deliver.ts (08-02에서 수정됨):
```typescript
export async function deliverKakaoChannelNotifications(supabase): Promise<{ sent: number; failed: number }>
```

From src/app/api/worker/notify/route.ts (현재 구조 확인 필요):
```typescript
// POST: x-cron-secret 검증 → generatePriceAlerts + deliverPendingNotifications 호출
```

UI-SPEC KakaoChannelSubscribeForm:
```typescript
// Zod 스키마
const subscribeSchema = z.object({
  phone: z.string().regex(/^010-\d{4}-\d{4}$/, '010-XXXX-XXXX 형식으로 입력해주세요'),
  consent: z.boolean().refine(v => v === true, '개인정보 수집에 동의해야 신청할 수 있어요'),
})
// 성공 후: isSubmitted 상태로 성공 UI 표시
```

RESEARCH.md SOLAPI 패턴:
```typescript
import { SolapiMessageService } from 'solapi'
const service = new SolapiMessageService(
  process.env.SOLAPI_API_KEY!,
  process.env.SOLAPI_API_SECRET!,
)
await service.send({
  to:   phone,
  from: process.env.SOLAPI_SENDER_NUMBER!,
  kakaoOptions: { pfId, templateId, variables, disableSms: false },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sendAlimtalk SOLAPI 어댑터 + Server Action 구현 (GREEN)</name>
  <files>
    src/services/kakao-channel.ts,
    src/app/actions/kakao-channel-actions.ts
  </files>
  <read_first>
    src/__tests__/kakao-channel.test.ts (RED 테스트 — GREEN으로)
    src/services/kapt.ts (services/ 어댑터 서버 전용 패턴)
    src/app/actions/consent-actions.ts (Server Action 패턴)
    .planning/phases/08-community-deepening/08-RESEARCH.md (Pattern 3: SOLAPI 알림톡 발송)
  </read_first>
  <behavior>
    - sendAlimtalk({ to, pfId, templateId, variables }): SOLAPI SDK → 알림톡 발송
    - SOLAPI_API_KEY 없으면 console.warn 로그 후 함수 return (Mock 모드)
    - phone_number를 절대 로그에 출력 금지 (T-8-04)
    - subscribeKakaoChannel(formData): kakao_channel_subscriptions에 INSERT
    - 이미 구독 중이면 is_active=true로 UPDATE (upsert)
  </behavior>
  <action>
    1. src/services/kakao-channel.ts 신규 생성:
    ```typescript
    import 'server-only'

    // T-8-02: SOLAPI API key는 서버 전용. 절대 클라이언트 컴포넌트에서 import 금지.
    export interface AlimtalkParams {
      to:         string  // T-8-04: 전화번호 — 절대 로그 출력 금지
      pfId:       string
      templateId: string
      variables:  Record<string, string>
    }

    export async function sendAlimtalk(params: AlimtalkParams): Promise<void> {
      const apiKey    = process.env.SOLAPI_API_KEY
      const apiSecret = process.env.SOLAPI_API_SECRET
      const sender    = process.env.SOLAPI_SENDER_NUMBER

      if (!apiKey || !apiSecret || !sender) {
        // Mock 모드: SOLAPI 계정 없으면 경고만 출력 (to 번호 절대 로그 금지)
        console.warn('[kakao-channel] SOLAPI env vars missing — skipping send (mock mode)')
        return
      }

      const { SolapiMessageService } = await import('solapi')
      const service = new SolapiMessageService(apiKey, apiSecret)

      await service.send({
        to:   params.to,
        from: sender,
        kakaoOptions: {
          pfId:       params.pfId,
          templateId: params.templateId,
          variables:  params.variables,
          disableSms: false,  // 알림톡 실패 시 SMS fallback
        },
      })
    }
    ```

    2. src/app/actions/kakao-channel-actions.ts 신규 생성:
    ```typescript
    'use server'
    import { z } from 'zod'
    import { createSupabaseServerClient } from '@/lib/supabase/server'

    const subscribeSchema = z.object({
      phone:   z.string().regex(/^010-\d{4}-\d{4}$/, '010-XXXX-XXXX 형식으로 입력해주세요'),
      consent: z.boolean().refine(v => v === true, '개인정보 수집에 동의해야 신청할 수 있어요'),
    })

    export async function subscribeKakaoChannel(
      _prevState: { error?: string; success?: boolean } | null,
      formData: FormData,
    ): Promise<{ error?: string; success?: boolean }> {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { error: '로그인이 필요합니다' }

      const parsed = subscribeSchema.safeParse({
        phone:   formData.get('phone'),
        consent: formData.get('consent') === 'true',
      })
      if (!parsed.success) {
        return { error: parsed.error.errors[0]?.message ?? '입력을 확인해주세요' }
      }

      // T-8-04: phone_number를 로그에 절대 출력 금지
      const { error } = await supabase
        .from('kakao_channel_subscriptions')
        .upsert(
          {
            user_id:      user.id,
            phone_number: parsed.data.phone,
            is_active:    true,
            subscribed_at: new Date().toISOString(),
          },
          { onConflict: 'user_id' },
        )

      if (error) return { error: '잠시 후 다시 시도해주세요' }
      return { success: true }
    }
    ```
  </action>
  <verify>
    <automated>npm run test -- --grep "kakao-channel"</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --grep "kakao-channel" 결과 모두 PASS (GREEN)
    - grep -c "server-only" src/services/kakao-channel.ts 결과 >= 1
    - grep -c "console.log" src/services/kakao-channel.ts 결과 = 0
    - 로그에 phone_number 관련 변수명이 없음: grep -c "params.to\|phone.*log\|log.*phone" src/services/kakao-channel.ts 결과 = 0
    - grep -c "console.log" src/app/actions/kakao-channel-actions.ts 결과 = 0
    - grep -c "'use server'" src/app/actions/kakao-channel-actions.ts 결과 >= 1
  </acceptance_criteria>
  <done>kakao-channel.test.ts 모든 테스트 PASS. SOLAPI 없을 때 Mock 모드 동작.</done>
</task>

<task type="auto">
  <name>Task 2: KakaoChannelSubscribeForm UI + 프로필 페이지 통합 + notify 워커 확장</name>
  <files>
    src/components/profile/KakaoChannelSubscribeForm.tsx,
    src/app/profile/page.tsx,
    src/app/api/worker/notify/route.ts
  </files>
  <read_first>
    src/app/profile/page.tsx (현재 프로필 페이지 구조 — PushToggle/TopicToggle 위치 확인)
    src/app/api/worker/notify/route.ts (현재 notify 워커 — deliverKakaoChannelNotifications 분기 추가 위치)
    .planning/phases/08-community-deepening/08-UI-SPEC.md (KakaoChannelSubscribeForm 명세 전체)
  </read_first>
  <action>
    1. src/components/profile/KakaoChannelSubscribeForm.tsx 생성 ('use client'):
    UI-SPEC 명세 완전 구현:
    - 'use client'
    - react-hook-form + zod (subscribeSchema 재사용)
    - useActionState(subscribeKakaoChannel, null)로 Server Action 호출
    - 섹션 헤더: "카카오톡 채널 알림" (font 700 13px/1.4)
    - 부제목: "웹 푸시가 차단된 경우 카카오톡으로 알림을 받을 수 있어요." (font 500 12px/1.5, color var(--fg-sec))
    - phone 인풋: inputMode="tel", maxLength={13}, autoComplete="tel", placeholder="010-0000-0000"
    - focus 스타일: border-color var(--dj-orange), box-shadow 0 0 0 4px rgba(234,88,12,.12)
    - consent 체크박스: "[개인정보처리방침]에 따라..." (대괄호 부분 /privacy 링크)
    - 제출 버튼: "카카오톡 알림 신청" / "신청 중…" / "신청 완료 ✓"
    - 성공 상태: isSubmitted true 시 폼 대신 성공 텍스트 표시
    - 금지: console.log(phone), consent 없이 제출, 개인정보 링크 누락
    - 반드시 isSubmitted 상태 사용 (성공 후 폼 숨김)

    2. src/app/profile/page.tsx 수정:
    - 알림 설정 카드에 구분선(1px var(--line-subtle)) 추가 후 KakaoChannelSubscribeForm 삽입
    - UI-SPEC 배치: PushToggle → TopicToggle → [구분선] → KakaoChannelSubscribeForm

    3. src/app/api/worker/notify/route.ts 수정:
    - 기존 generatePriceAlerts + deliverPendingNotifications 호출 유지
    - deliverKakaoChannelNotifications(supabase) 호출 추가
    - 반환 JSON에 kakaoSent, kakaoFailed 필드 추가
    - 기존 generated, sent, failed 필드 유지
  </action>
  <verify>
    <automated>npm run lint && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/profile/KakaoChannelSubscribeForm.tsx 파일 존재
    - grep -c "'use client'" src/components/profile/KakaoChannelSubscribeForm.tsx 결과 >= 1
    - grep -c "console.log" src/components/profile/KakaoChannelSubscribeForm.tsx 결과 = 0
    - grep -c "consent" src/components/profile/KakaoChannelSubscribeForm.tsx 결과 >= 2 (zod 스키마 + 체크박스)
    - grep -c "개인정보처리방침\|/privacy" src/components/profile/KakaoChannelSubscribeForm.tsx 결과 >= 1
    - grep -c "KakaoChannelSubscribeForm" src/app/profile/page.tsx 결과 >= 1
    - grep -c "deliverKakaoChannelNotifications" src/app/api/worker/notify/route.ts 결과 >= 1
    - npm run test -- --grep "notify-worker" 기존 테스트 회귀 없음
    - npm run build 통과
  </acceptance_criteria>
  <done>프로필 페이지에 카카오 채널 구독 폼이 표시되고, notify 워커에 카카오 알림 분기가 추가됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KakaoChannelSubscribeForm → Server Action | 전화번호는 Server Action에서만 처리. 클라이언트 console.log 금지. |
| Server Action → kakao_channel_subscriptions | Supabase RLS owner-only. 서버에서 phone_number 로그 절대 금지. |
| notify 워커 → sendAlimtalk | services/kakao-channel.ts 어댑터만 경유. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Information Disclosure | phone_number 처리 전체 | mitigate | 1) KakaoChannelSubscribeForm에 console.log 금지. 2) Server Action에 로그 없음. 3) sendAlimtalk에 to 파라미터 로그 없음. grep 3-point 확인. |
| T-8-02 | Information Disclosure | SOLAPI_API_KEY | mitigate | server-only 마크. kakao-channel.ts 어댑터만 초기화. 클라이언트 번들에 포함 불가. |
</threat_model>

<verification>
- npm run test -- --grep "kakao-channel" → 모두 PASS
- grep -c "console.log" src/services/kakao-channel.ts → 0
- grep -c "console.log" src/components/profile/KakaoChannelSubscribeForm.tsx → 0
- grep -c "deliverKakaoChannelNotifications" src/app/api/worker/notify/route.ts → >= 1
- npm run test -- --grep "notify-worker" → 기존 테스트 회귀 없음
- npm run build → 오류 없음
</verification>

<success_criteria>
- kakao-channel.test.ts 모든 테스트 GREEN
- KakaoChannelSubscribeForm이 동의 없이 제출 불가
- notify 워커가 카카오 채널 알림 분기를 실행함
- SOLAPI 없으면 Mock 모드로 코드 완성 (실제 발송은 별도 확인)
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-05-SUMMARY.md` 작성.
</output>

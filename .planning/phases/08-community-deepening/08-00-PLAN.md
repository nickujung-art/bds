---
phase: 08-community-deepening
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260512000001_phase8_gamification.sql
  - supabase/migrations/20260512000002_phase8_kakao_channel.sql
  - supabase/migrations/20260512000003_phase8_cafe_posts.sql
  - src/__tests__/member-tier.test.ts
  - src/__tests__/daum-cafe.test.ts
  - src/__tests__/kakao-channel.test.ts
  - src/__tests__/compare.test.ts
  - src/__tests__/tierbadge.test.ts
autonomous: false
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-04
  - DIFF-05
  - DIFF-06
user_setup:
  - service: solapi
    why: "카카오톡 알림톡 발송 (DIFF-04)"
    env_vars:
      - name: SOLAPI_API_KEY
        source: "SOLAPI 대시보드 → 계정 → API Keys"
      - name: SOLAPI_API_SECRET
        source: "SOLAPI 대시보드 → 계정 → API Keys"
      - name: SOLAPI_SENDER_NUMBER
        source: "SOLAPI 대시보드 → 발신번호 관리 (인증된 번호)"
      - name: KAKAO_CHANNEL_PF_ID
        source: "카카오비즈 채널 심사 완료 후 → 채널 ID (pfId)"
    dashboard_config:
      - task: "SOLAPI 계정 신규 가입"
        location: "https://solapi.com"
      - task: "카카오 비즈니스 채널 연결 (3~5 영업일 심사)"
        location: "SOLAPI 대시보드 → 카카오 채널 연동"
      - task: "알림톡 템플릿 등록 및 승인 신청 (1~3 영업일)"
        location: "SOLAPI 대시보드 → 카카오 알림톡 템플릿"

must_haves:
  truths:
    - "profiles 테이블에 activity_points, member_tier 컬럼이 존재한다"
    - "activity_logs 테이블이 RLS와 함께 존재한다"
    - "kakao_channel_subscriptions 테이블이 RLS와 함께 존재한다"
    - "cafe_posts 테이블이 RLS와 함께 존재한다"
    - "4개 테스트 파일이 RED 상태로 존재한다 (구현 없음)"
    - "solapi, nuqs 패키지가 설치된다"
  artifacts:
    - path: "supabase/migrations/20260512000001_phase8_gamification.sql"
      provides: "activity_points, member_tier 컬럼 + activity_logs 테이블 + RLS + 트리거 함수"
      contains: "add_activity_points"
    - path: "supabase/migrations/20260512000002_phase8_kakao_channel.sql"
      provides: "kakao_channel_subscriptions 테이블 + RLS"
      contains: "kakao_channel_subscriptions"
    - path: "supabase/migrations/20260512000003_phase8_cafe_posts.sql"
      provides: "cafe_posts 테이블 + RLS"
      contains: "cafe_posts"
    - path: "src/__tests__/member-tier.test.ts"
      provides: "DIFF-01/05 RED 테스트"
      min_lines: 30
    - path: "src/__tests__/daum-cafe.test.ts"
      provides: "DIFF-02 RED 테스트"
      min_lines: 20
    - path: "src/__tests__/kakao-channel.test.ts"
      provides: "DIFF-04 RED 테스트"
      min_lines: 20
    - path: "src/__tests__/compare.test.ts"
      provides: "DIFF-06 RED 테스트"
      min_lines: 20
    - path: "src/__tests__/tierbadge.test.ts"
      provides: "DIFF-01 💬 cafeVerified 마크 RED 테스트"
      min_lines: 20
  key_links:
    - from: "complex_reviews INSERT"
      to: "profiles.activity_points"
      via: "award_review_points() DB 트리거"
      pattern: "CREATE TRIGGER reviews_award_points"
    - from: "activity_points 갱신"
      to: "member_tier"
      via: "add_activity_points() SECURITY DEFINER 함수"
      pattern: "SECURITY DEFINER"
---

<objective>
Phase 8 DB 스키마 마이그레이션 적용 + 패키지 설치 + RED 테스트 스캐폴드 작성.

Purpose: Wave 1/2/3 실행 전 DB 기반을 확립한다. 게이미피케이션(DIFF-01/05), 카카오 채널 구독(DIFF-04), 카페 글 저장(DIFF-02) 스키마를 한 번에 적용한다.
Output: 3개 SQL 마이그레이션 파일, 4개 RED 테스트 파일, solapi/nuqs 패키지 설치, supabase db push 완료.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-community-deepening/08-RESEARCH.md
@.planning/phases/08-community-deepening/08-VALIDATION.md

<interfaces>
<!-- 기존 패턴 참조 -->

From supabase/migrations/20260507000004_phase4_tables.sql (패턴):
```sql
-- 테이블 생성 패턴
CREATE TABLE public.comments (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  ...
);
ALTER TABLE public.comments ENABLE ROW LEVEL SECURITY;
CREATE POLICY "comments: public read" ON public.comments FOR SELECT USING (true);
```

From supabase/migrations/20260507000005_phase5_listing_prices.sql (패턴):
```sql
-- RLS write 전용 패턴 (service_role)
create policy "listing_prices: admin write"
  on public.listing_prices for all
  using (exists (select 1 from public.profiles where id = auth.uid() and role in ('admin', 'superadmin')))
  with check (...);
```

From src/__tests__/notify-worker.test.ts (테스트 패턴):
```typescript
vi.mock('server-only', () => ({}))
vi.mock('next/headers', () => ({ cookies: vi.fn(() => ({ getAll: () => [] })) }))
import { describe, it, expect, vi, beforeAll } from 'vitest'
```

set_updated_at 트리거 함수는 기존 supabase에 이미 존재함.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: SOLAPI 계정 + 카카오 비즈니스 채널 사전 설정</name>
  <read_first>
    .planning/phases/08-community-deepening/08-RESEARCH.md (Environment Availability 섹션, Pitfall 2)
  </read_first>
  <what-built>
    DIFF-04(카카오 알림톡) 코드는 Wave 2에서 구현된다. 그러나 SOLAPI 계정 생성 및 카카오 비즈니스 채널 심사는 3~5 영업일이 걸리므로 코딩보다 먼저 시작해야 한다.
  </what-built>
  <how-to-verify>
    1. https://solapi.com 에서 계정 가입
    2. SOLAPI 대시보드 → 카카오 채널 연동 메뉴에서 카카오 비즈니스 채널 연결 신청
    3. 알림톡 템플릿 2개 등록:
       - 템플릿 1 (신고가 알림): "안녕하세요. #{단지명} 단지에 새로운 실거래가가 등록되었어요. 최근 거래가: #{가격}. 단지온도에서 확인하세요."
       - 템플릿 2 (관심 단지 알림): "관심 단지 #{단지명}에 업데이트가 있어요. 단지온도에서 확인해보세요."
    4. 환경 변수 4개를 .env.local에 등록:
       - SOLAPI_API_KEY=<발급된 키>
       - SOLAPI_API_SECRET=<발급된 시크릿>
       - SOLAPI_SENDER_NUMBER=<인증된 발신번호, 예: 0212345678>
       - KAKAO_CHANNEL_PF_ID=<채널 PF ID>
    5. SOLAPI 계정 없어도 Wave 2(DIFF-04) 코드 구현은 진행 가능하다. 실제 발송 테스트만 계정 필요.
  </how-to-verify>
  <resume-signal>
    SOLAPI 가입 완료 후 "solapi-done" 입력 (계정 없으면 "solapi-skip" 입력 — Mock 구현으로 Wave 2 진행)
  </resume-signal>
  <acceptance_criteria>
    .env.local에 SOLAPI_API_KEY= 행 존재 또는 "solapi-skip" 신호 수신
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase 8 DB 마이그레이션 파일 3개 작성</name>
  <read_first>
    supabase/migrations/20260507000004_phase4_tables.sql (테이블 생성 + RLS 패턴)
    supabase/migrations/20260507000005_phase5_listing_prices.sql (service_role write 패턴)
    .planning/phases/08-community-deepening/08-RESEARCH.md (게이미피케이션 DB 스키마 설계 섹션)
  </read_first>
  <action>
    파일 1: supabase/migrations/20260512000001_phase8_gamification.sql

    ```sql
    -- Phase 8: 게이미피케이션 스키마 (DIFF-01, DIFF-05)

    -- 1. profiles 테이블 컬럼 추가
    ALTER TABLE public.profiles
      ADD COLUMN IF NOT EXISTS activity_points integer NOT NULL DEFAULT 0,
      ADD COLUMN IF NOT EXISTS member_tier text NOT NULL DEFAULT 'bronze'
        CHECK (member_tier IN ('bronze', 'silver', 'gold'));

    CREATE INDEX IF NOT EXISTS profiles_member_tier_idx ON public.profiles(member_tier);

    -- 2. activity_logs 테이블
    CREATE TABLE IF NOT EXISTS public.activity_logs (
      id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id    uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
      points     integer NOT NULL,
      reason     text NOT NULL CHECK (reason IN ('review', 'comment', 'gps_verify', 'daily_visit', 'first_favorite')),
      created_at timestamptz NOT NULL DEFAULT now()
    );

    ALTER TABLE public.activity_logs ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "activity_logs: owner read"
      ON public.activity_logs FOR SELECT USING (auth.uid() = user_id);
    -- write: service_role only (SECURITY DEFINER 트리거 함수에서만 호출)

    -- 3. 포인트 적립 함수 (SECURITY DEFINER — 클라이언트 직접 호출 불가)
    CREATE OR REPLACE FUNCTION public.add_activity_points(
      p_user_id uuid,
      p_points  integer,
      p_reason  text
    ) RETURNS void LANGUAGE plpgsql SECURITY DEFINER AS $$
    BEGIN
      INSERT INTO public.activity_logs (user_id, points, reason)
        VALUES (p_user_id, p_points, p_reason);

      UPDATE public.profiles
        SET
          activity_points = activity_points + p_points,
          member_tier = CASE
            WHEN activity_points + p_points >= 200 THEN 'gold'
            WHEN activity_points + p_points >= 50  THEN 'silver'
            ELSE 'bronze'
          END
        WHERE id = p_user_id;
    END;
    $$;

    -- 4. 후기 작성 트리거 함수
    CREATE OR REPLACE FUNCTION public.award_review_points()
    RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER AS $$
    BEGIN
      PERFORM public.add_activity_points(NEW.user_id, 10, 'review');
      RETURN NEW;
    END;
    $$;

    CREATE TRIGGER reviews_award_points
      AFTER INSERT ON public.complex_reviews
      FOR EACH ROW
      WHEN (NEW.user_id IS NOT NULL)
      EXECUTE FUNCTION public.award_review_points();

    -- 5. 댓글 작성 트리거 함수
    CREATE OR REPLACE FUNCTION public.award_comment_points()
    RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER AS $$
    BEGIN
      PERFORM public.add_activity_points(NEW.user_id, 3, 'comment');
      RETURN NEW;
    END;
    $$;

    CREATE TRIGGER comments_award_points
      AFTER INSERT ON public.comments
      FOR EACH ROW
      WHEN (NEW.user_id IS NOT NULL)
      EXECUTE FUNCTION public.award_comment_points();

    COMMENT ON FUNCTION public.add_activity_points IS 'DIFF-01/05: SECURITY DEFINER — 클라이언트 직접 UPDATE 불가. 트리거에서만 호출.';
    ```

    파일 2: supabase/migrations/20260512000002_phase8_kakao_channel.sql

    ```sql
    -- Phase 8: 카카오톡 채널 구독 스키마 (DIFF-04)

    CREATE TABLE IF NOT EXISTS public.kakao_channel_subscriptions (
      id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id       uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
      phone_number  text NOT NULL,  -- pgcrypto로 암호화 저장 권장
      subscribed_at timestamptz NOT NULL DEFAULT now(),
      is_active     boolean NOT NULL DEFAULT true,
      UNIQUE(user_id)
    );

    ALTER TABLE public.kakao_channel_subscriptions ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "kakao_channel_subscriptions: owner all"
      ON public.kakao_channel_subscriptions FOR ALL
      USING (auth.uid() = user_id)
      WITH CHECK (auth.uid() = user_id);

    -- 관리자도 읽기 가능 (운영 목적)
    CREATE POLICY "kakao_channel_subscriptions: admin read"
      ON public.kakao_channel_subscriptions FOR SELECT
      USING (EXISTS (
        SELECT 1 FROM public.profiles
        WHERE id = auth.uid() AND role IN ('admin', 'superadmin')
      ));

    COMMENT ON TABLE public.kakao_channel_subscriptions IS 'DIFF-04: 전화번호는 개인정보 — 로그 출력 절대 금지. RLS owner-only.';
    ```

    파일 3: supabase/migrations/20260512000003_phase8_cafe_posts.sql

    ```sql
    -- Phase 8: 카페 글 NLP 매칭 저장 테이블 (DIFF-02)

    CREATE TABLE IF NOT EXISTS public.cafe_posts (
      id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      complex_id  uuid REFERENCES public.complexes(id) ON DELETE SET NULL,
      title       text NOT NULL,
      excerpt     text,
      url         text NOT NULL UNIQUE,
      cafe_name   text,
      posted_at   timestamptz,
      matched_at  timestamptz NOT NULL DEFAULT now(),
      confidence  numeric CHECK (confidence BETWEEN 0 AND 1),
      is_verified boolean NOT NULL DEFAULT false
    );

    CREATE INDEX IF NOT EXISTS cafe_posts_complex_id_idx ON public.cafe_posts(complex_id, matched_at DESC);
    CREATE INDEX IF NOT EXISTS cafe_posts_url_idx ON public.cafe_posts(url);

    ALTER TABLE public.cafe_posts ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "cafe_posts: public read"
      ON public.cafe_posts FOR SELECT USING (true);
    -- write: service_role only (cron 워커)

    COMMENT ON TABLE public.cafe_posts IS 'DIFF-02: Daum Search + Gemini NER 파이프라인 결과 저장. complex_id NULL 허용 (매칭 실패).';
    ```
  </action>
  <verify>
    <automated>
      ls supabase/migrations/20260512000001_phase8_gamification.sql supabase/migrations/20260512000002_phase8_kakao_channel.sql supabase/migrations/20260512000003_phase8_cafe_posts.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260512000001_phase8_gamification.sql 파일 존재
    - grep -c "SECURITY DEFINER" supabase/migrations/20260512000001_phase8_gamification.sql 결과 >= 3
    - grep -c "award_review_points" supabase/migrations/20260512000001_phase8_gamification.sql 결과 >= 1
    - grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260512000002_phase8_kakao_channel.sql 결과 >= 1
    - grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260512000003_phase8_cafe_posts.sql 결과 >= 1
  </acceptance_criteria>
  <done>3개 SQL 마이그레이션 파일이 위 명세대로 생성됨</done>
</task>

<task type="auto">
  <name>Task 3: 패키지 설치 + RED 테스트 스캐폴드 작성</name>
  <read_first>
    src/__tests__/notify-worker.test.ts (vi.mock 패턴, beforeAll 픽스처 패턴)
    src/__tests__/complex-detail.test.ts (Supabase 클라이언트 mock 패턴)
    .planning/phases/08-community-deepening/08-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
  </read_first>
  <action>
    1. 패키지 설치:
    ```bash
    npm install solapi nuqs
    ```

    2. src/__tests__/member-tier.test.ts 작성:
    ```typescript
    /**
     * DIFF-01, DIFF-05 — 회원 등급 + 활동 포인트
     * RED: 구현 없음 → 실패 예상
     */
    import { describe, it, expect, vi } from 'vitest'

    vi.mock('server-only', () => ({}))
    vi.mock('next/headers', () => ({
      cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
    }))

    describe('member tier', () => {
      it('getMemberTier: 0점 → bronze 반환', async () => {
        const { getMemberTier } = await import('@/lib/data/member-tier')
        const mockSupabase = {
          from: vi.fn().mockReturnThis(),
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          single: vi.fn().mockResolvedValue({
            data: { activity_points: 0, member_tier: 'bronze' },
            error: null,
          }),
        }
        const result = await getMemberTier('user-1', mockSupabase as never)
        expect(result.tier).toBe('bronze')
        expect(result.points).toBe(0)
      })

      it('getMemberTier: 50점 → silver 반환', async () => {
        const { getMemberTier } = await import('@/lib/data/member-tier')
        const mockSupabase = {
          from: vi.fn().mockReturnThis(),
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          single: vi.fn().mockResolvedValue({
            data: { activity_points: 50, member_tier: 'silver' },
            error: null,
          }),
        }
        const result = await getMemberTier('user-1', mockSupabase as never)
        expect(result.tier).toBe('silver')
      })

      it('getMemberTier: 200점 → gold 반환', async () => {
        const { getMemberTier } = await import('@/lib/data/member-tier')
        const mockSupabase = {
          from: vi.fn().mockReturnThis(),
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          single: vi.fn().mockResolvedValue({
            data: { activity_points: 200, member_tier: 'gold' },
            error: null,
          }),
        }
        const result = await getMemberTier('user-1', mockSupabase as never)
        expect(result.tier).toBe('gold')
      })

      it('member tier priority: gold → 즉시 알림(딜레이 0)', async () => {
        const { getNotificationDelay } = await import('@/lib/data/member-tier')
        expect(getNotificationDelay('gold')).toBe(0)
      })

      it('member tier priority: silver → 30분 딜레이(ms)', async () => {
        const { getNotificationDelay } = await import('@/lib/data/member-tier')
        expect(getNotificationDelay('silver')).toBe(30 * 60 * 1000)
      })

      it('member tier priority: bronze → 30분 딜레이(ms)', async () => {
        const { getNotificationDelay } = await import('@/lib/data/member-tier')
        expect(getNotificationDelay('bronze')).toBe(30 * 60 * 1000)
      })
    })
    ```

    3. src/__tests__/daum-cafe.test.ts 작성:
    ```typescript
    /**
     * DIFF-02 — Daum 카페 검색 + Gemini NER
     * RED: 구현 없음 → 실패 예상
     */
    import { describe, it, expect, vi, beforeEach } from 'vitest'

    vi.mock('server-only', () => ({}))

    describe('daum-cafe searchCafePosts', () => {
      beforeEach(() => {
        vi.stubGlobal('fetch', vi.fn())
        vi.stubEnv('KAKAO_REST_API_KEY', 'test-kakao-key')
      })

      it('정상 응답 → CafePost[] 반환', async () => {
        const mockFetch = vi.mocked(fetch)
        mockFetch.mockResolvedValueOnce({
          ok: true,
          json: async () => ({
            documents: [{
              title: '래미안더센트럴 후기',
              contents: '창원 래미안더센트럴 입주 후기입니다.',
              url: 'https://cafe.daum.net/test/1',
              datetime: '2026-05-01T00:00:00.000+09:00',
              cafename: '창원부동산카페',
            }],
          }),
        } as Response)

        const { searchCafePosts } = await import('@/services/daum-cafe')
        const posts = await searchCafePosts('창원 래미안')
        expect(posts).toHaveLength(1)
        expect(posts[0]!.title).toBe('래미안더센트럴 후기')
        expect(posts[0]!.cafeName).toBe('창원부동산카페')
      })

      it('HTTP 오류 → Error throw', async () => {
        const mockFetch = vi.mocked(fetch)
        mockFetch.mockResolvedValueOnce({ ok: false, status: 401 } as Response)

        const { searchCafePosts } = await import('@/services/daum-cafe')
        await expect(searchCafePosts('테스트')).rejects.toThrow('Daum cafe search HTTP 401')
      })
    })

    describe('extractComplexNames', () => {
      it('프롬프트 인젝션 방지: [텍스트] 구분자 사용 확인', async () => {
        vi.stubEnv('GEMINI_API_KEY', 'test-key')
        const mockGenAI = {
          getGenerativeModel: vi.fn().mockReturnValue({
            generateContent: vi.fn().mockResolvedValue({
              response: { text: () => '{"complexes": ["래미안더센트럴"]}' },
            }),
          }),
        }
        vi.doMock('@google/generative-ai', () => ({ GoogleGenerativeAI: vi.fn(() => mockGenAI) }))

        const { extractComplexNames } = await import('@/services/daum-cafe')
        const result = await extractComplexNames('창원 래미안더센트럴 매매 후기')
        expect(result).toContain('래미안더센트럴')
        // 프롬프트에 [텍스트] 구분자가 포함되어야 함 (구현에서 검증)
        const callArg = mockGenAI.getGenerativeModel().generateContent.mock.calls[0]?.[0] as string
        expect(callArg).toContain('[텍스트]')
      })
    })
    ```

    4. src/__tests__/kakao-channel.test.ts 작성:
    ```typescript
    /**
     * DIFF-04 — SOLAPI 알림톡 발송
     * RED: 구현 없음 → 실패 예상
     */
    import { describe, it, expect, vi, beforeEach } from 'vitest'

    vi.mock('server-only', () => ({}))

    describe('kakao-channel sendAlimtalk', () => {
      beforeEach(() => {
        vi.stubEnv('SOLAPI_API_KEY', 'test-api-key')
        vi.stubEnv('SOLAPI_API_SECRET', 'test-secret')
        vi.stubEnv('SOLAPI_SENDER_NUMBER', '0212345678')
        vi.stubEnv('KAKAO_CHANNEL_PF_ID', 'test-pf-id')
      })

      it('서버 전용 — SOLAPI 클라이언트는 src/services/kakao-channel.ts에서만 초기화', async () => {
        // solapi는 서버 전용. 이 테스트에서 import 가능하면 OK
        const mod = await import('@/services/kakao-channel')
        expect(typeof mod.sendAlimtalk).toBe('function')
      })

      it('sendAlimtalk: SOLAPI 호출 → 성공', async () => {
        vi.doMock('solapi', () => ({
          SolapiMessageService: vi.fn().mockImplementation(() => ({
            send: vi.fn().mockResolvedValue({}),
          })),
        }))

        const { sendAlimtalk } = await import('@/services/kakao-channel')
        await expect(sendAlimtalk({
          to: '01012345678',
          pfId: 'test-pf-id',
          templateId: 'KA01TP_TEST',
          variables: { '#{단지명}': '래미안더센트럴', '#{가격}': '15억' },
        })).resolves.not.toThrow()
      })

      it('deliver.ts에 kakao 분기 추가: deliverKakaoChannelNotifications 함수 존재', async () => {
        const mod = await import('@/lib/notifications/deliver')
        expect(typeof mod.deliverKakaoChannelNotifications).toBe('function')
      })
    })
    ```

    6. src/__tests__/tierbadge.test.ts 작성:
    ```typescript
    /**
     * DIFF-01 — TierBadge 컴포넌트: cafeVerified 💬 마크
     * RED: 구현 없음 → 실패 예상
     */
    import { describe, it, expect, vi } from 'vitest'

    vi.mock('server-only', () => ({}))

    describe('TierBadge', () => {
      it('cafeVerified=true → 💬 마크 렌더', async () => {
        const { getTierBadge } = await import('@/lib/data/member-tier')
        const result = getTierBadge({ tier: 'bronze', cafeVerified: true })
        expect(result).toContain('💬')
      })

      it('cafeVerified=false, tier=bronze → 마크 없음', async () => {
        const { getTierBadge } = await import('@/lib/data/member-tier')
        const result = getTierBadge({ tier: 'bronze', cafeVerified: false })
        expect(result).toBe('')
      })

      it('tier=silver, cafeVerified=false → 🔥 마크', async () => {
        const { getTierBadge } = await import('@/lib/data/member-tier')
        const result = getTierBadge({ tier: 'silver', cafeVerified: false })
        expect(result).toContain('🔥')
      })

      it('tier=gold, cafeVerified=false → 👑 마크', async () => {
        const { getTierBadge } = await import('@/lib/data/member-tier')
        const result = getTierBadge({ tier: 'gold', cafeVerified: false })
        expect(result).toContain('👑')
      })

      it('tier=gold, cafeVerified=true → 👑 + 💬 둘 다 포함', async () => {
        const { getTierBadge } = await import('@/lib/data/member-tier')
        const result = getTierBadge({ tier: 'gold', cafeVerified: true })
        expect(result).toContain('👑')
        expect(result).toContain('💬')
      })
    })
    ```

    5. src/__tests__/compare.test.ts 작성:
    ```typescript
    /**
     * DIFF-06 — 비교 표 URL state + 병렬 fetch
     * RED: 구현 없음 → 실패 예상
     */
    import { describe, it, expect, vi } from 'vitest'

    vi.mock('server-only', () => ({}))

    describe('compare limit', () => {
      it('ids.slice(0, 4): 5개 입력 → 4개만 처리', async () => {
        const { buildCompareIds } = await import('@/lib/data/compare')
        const ids = ['a', 'b', 'c', 'd', 'e']
        const result = buildCompareIds(ids)
        expect(result).toHaveLength(4)
        expect(result).toEqual(['a', 'b', 'c', 'd'])
      })

      it('빈 배열 입력 → 빈 배열 반환', async () => {
        const { buildCompareIds } = await import('@/lib/data/compare')
        expect(buildCompareIds([])).toEqual([])
      })

      it('null/undefined ID → 필터링됨', async () => {
        const { buildCompareIds } = await import('@/lib/data/compare')
        const ids = ['a', '', 'b', null as unknown as string]
        const result = buildCompareIds(ids)
        expect(result).not.toContain('')
        expect(result).not.toContain(null)
      })
    })

    describe('CompareTable', () => {
      it('getCompareData: 2개 단지 ID → ComplexSummary[] 2개 반환', async () => {
        const { getCompareData } = await import('@/lib/data/compare')
        const mockSupabase = {
          from: vi.fn().mockReturnThis(),
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          maybeSingle: vi.fn()
            .mockResolvedValueOnce({ data: { id: 'id1', canonical_name: '래미안A', sgg_code: '48121' }, error: null })
            .mockResolvedValueOnce({ data: { id: 'id2', canonical_name: '래미안B', sgg_code: '48121' }, error: null }),
        }

        const result = await getCompareData(['id1', 'id2'], mockSupabase as never)
        expect(result).toHaveLength(2)
        expect(result[0]?.canonical_name).toBe('래미안A')
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- --grep "member.tier|daum-cafe|kakao-channel|compare|TierBadge" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json에 "solapi" 항목 존재: grep -c "\"solapi\"" package.json >= 1
    - package.json에 "nuqs" 항목 존재: grep -c "\"nuqs\"" package.json >= 1
    - src/__tests__/member-tier.test.ts 파일 존재
    - src/__tests__/daum-cafe.test.ts 파일 존재
    - src/__tests__/kakao-channel.test.ts 파일 존재
    - src/__tests__/compare.test.ts 파일 존재
    - src/__tests__/tierbadge.test.ts 파일 존재
    - npm run test -- --grep "member.tier" 실행 시 "FAIL" 또는 모듈 not found 오류 발생 (RED 확인)
  </acceptance_criteria>
  <done>4개 RED 테스트 파일과 패키지 설치 완료</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 4: supabase db push [BLOCKING]</name>
  <read_first>
    supabase/migrations/20260512000001_phase8_gamification.sql
    supabase/migrations/20260512000002_phase8_kakao_channel.sql
    supabase/migrations/20260512000003_phase8_cafe_posts.sql
  </read_first>
  <what-built>
    위 3개 마이그레이션 파일이 작성되었다. 이제 Supabase 원격 DB에 적용해야 한다.
  </what-built>
  <how-to-verify>
    1. 터미널에서 실행:
    ```bash
    supabase db push
    ```
    2. "Applying migration 20260512000001_phase8_gamification.sql..." 등 3줄이 출력되고 성공 완료 확인
    3. 검증:
    ```bash
    supabase db diff
    ```
    diff 없으면 성공.

    오류 발생 시:
    - "already exists" 오류: IF NOT EXISTS 조건 확인 (마이그레이션 파일에 이미 포함됨)
    - 인증 오류: SUPABASE_ACCESS_TOKEN 환경 변수 필요 → `export SUPABASE_ACCESS_TOKEN=<토큰>`
  </how-to-verify>
  <resume-signal>
    push 성공 후 "db-pushed" 입력. 오류 있으면 오류 메시지 붙여넣기.
  </resume-signal>
  <acceptance_criteria>
    supabase db diff 결과가 빈 출력 (변경 없음) 또는 "No changes found" 메시지
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB 트리거 → profiles | add_activity_points() SECURITY DEFINER: 외부에서 직접 UPDATE 불가 |
| 마이그레이션 파일 | 로컬 → Supabase 원격: supabase db push 경로만 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | profiles.activity_points | mitigate | SECURITY DEFINER 함수 + 트리거만 UPDATE 가능. RLS에 UPDATE 정책 없음. |
| T-8-04 | Information Disclosure | kakao_channel_subscriptions.phone_number | mitigate | RLS owner-only 정책. 서버 로그 절대 출력 금지 (COMMENT에 명시). |
</threat_model>

<verification>
- supabase/migrations/20260512000001~03 파일 존재
- grep -c "SECURITY DEFINER" supabase/migrations/20260512000001_phase8_gamification.sql 결과 >= 3
- grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260512000002_phase8_kakao_channel.sql 결과 >= 1
- npm run test -- --grep "member.tier|daum-cafe|kakao-channel|compare" 실행 → RED (모듈 미존재 오류)
- package.json에 solapi, nuqs 포함
</verification>

<success_criteria>
- DB 마이그레이션 3개가 Supabase에 적용됨
- profiles 테이블에 activity_points, member_tier 컬럼 존재
- 4개 테스트 파일이 RED 상태로 존재
- solapi, nuqs 패키지가 node_modules에 설치됨
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-00-SUMMARY.md` 작성.
</output>

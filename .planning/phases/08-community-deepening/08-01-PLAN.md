---
phase: 08-community-deepening
plan: "01"
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - src/lib/data/member-tier.ts
  - src/components/reviews/TierBadge.tsx
  - src/components/reviews/ReviewList.tsx
  - src/components/reviews/CommentSection.tsx
autonomous: true
requirements:
  - DIFF-01

must_haves:
  truths:
    - "getMemberTier() 함수가 DB에서 등급과 포인트를 반환한다"
    - "TierBadge가 bronze 시 null, silver 시 🔥, gold 시 👑을 렌더한다"
    - "카페 인증(cafe_nickname IS NOT NULL) 시 💬 배지가 추가로 표시된다"
    - "ReviewList에서 각 후기 작성자 이름 뒤에 TierBadge가 렌더된다"
    - "CommentSection에서 각 댓글 작성자 이름 뒤에 TierBadge가 렌더된다"
  artifacts:
    - path: "src/lib/data/member-tier.ts"
      provides: "getMemberTier(), getNotificationDelay()"
      exports: ["getMemberTier", "getNotificationDelay", "MemberTierInfo"]
    - path: "src/components/reviews/TierBadge.tsx"
      provides: "TierBadge 서버 컴포넌트"
      contains: "bronze.*null|gold.*👑|silver.*🔥"
  key_links:
    - from: "ReviewList.tsx"
      to: "TierBadge.tsx"
      via: "review.user.member_tier 조회 후 props 전달"
      pattern: "TierBadge.*tier=|member_tier"
    - from: "CommentSection.tsx"
      to: "TierBadge.tsx"
      via: "comment.user.member_tier 조회 후 props 전달"
      pattern: "TierBadge.*tier="
---

<objective>
게이미피케이션 데이터 레이어 + UI 컴포넌트 구현.

Purpose: 활동 기반 회원 등급을 읽어 후기/댓글 작성자 이름 옆에 마크를 표시한다.
Output: getMemberTier 함수, TierBadge 서버 컴포넌트, ReviewList/CommentSection 연동.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-community-deepening/08-RESEARCH.md
@.planning/phases/08-community-deepening/08-UI-SPEC.md
@.planning/phases/08-community-deepening/08-00-SUMMARY.md

<interfaces>
From src/lib/data/reviews.ts:
```typescript
export type ComplexReview = Database['public']['Tables']['complex_reviews']['Row']
export async function getComplexReviews(complexId, supabase, limit): Promise<ComplexReview[]>
```

From src/lib/data/profile.ts:
```typescript
export async function getReviewsCount(userId, supabase): Promise<number>
```

From src/__tests__/member-tier.test.ts (RED 테스트 — 구현 대상):
```typescript
getMemberTier(userId, supabase) → { tier: 'bronze'|'silver'|'gold', points: number }
getNotificationDelay(tier) → 0 (gold) | 30*60*1000 (silver/bronze)
```

UI-SPEC TierBadge 컴포넌트 명세:
```typescript
type Tier = 'bronze' | 'silver' | 'gold'
interface TierBadgeProps {
  tier: Tier
  cafeVerified: boolean  // cafe_nickname IS NOT NULL
  className?: string
}
// bronze + cafeVerified=false → null 반환
// silver → 🔥 배지 (background: var(--bg-surface-2), color: var(--fg-sec))
// gold → 👑 배지 (background: var(--bg-surface-2), color: var(--fg-pri))
// cafeVerified → 💬 배지 (background: var(--bg-positive-tint), color: var(--fg-positive))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getMemberTier + getNotificationDelay 구현 (GREEN)</name>
  <files>src/lib/data/member-tier.ts</files>
  <read_first>
    src/__tests__/member-tier.test.ts (RED 테스트 — 이것을 GREEN으로 만든다)
    src/lib/data/profile.ts (SupabaseClient 패턴 참조)
    src/lib/data/reviews.ts (Database 타입 import 패턴)
  </read_first>
  <behavior>
    - getMemberTier('user-1', supabase): profiles에서 activity_points, member_tier 조회 → { tier, points } 반환
    - getMemberTier: 사용자 없으면 { tier: 'bronze', points: 0 } 반환
    - getNotificationDelay('gold') → 0
    - getNotificationDelay('silver') → 1800000 (30분)
    - getNotificationDelay('bronze') → 1800000 (30분)
  </behavior>
  <action>
    src/lib/data/member-tier.ts 신규 생성:

    ```typescript
    import 'server-only'
    import type { SupabaseClient } from '@supabase/supabase-js'
    import type { Database } from '@/types/database'

    export type MemberTier = 'bronze' | 'silver' | 'gold'

    export interface MemberTierInfo {
      tier:   MemberTier
      points: number
    }

    export async function getMemberTier(
      userId: string,
      supabase: SupabaseClient<Database>,
    ): Promise<MemberTierInfo> {
      const { data } = await supabase
        .from('profiles')
        .select('activity_points, member_tier')
        .eq('id', userId)
        .single()

      if (!data) return { tier: 'bronze', points: 0 }

      const row = data as { activity_points: number; member_tier: string }
      return {
        tier:   (row.member_tier as MemberTier) ?? 'bronze',
        points: row.activity_points ?? 0,
      }
    }

    /**
     * gold: 즉시 발송 (딜레이 0ms)
     * silver/bronze: 30분 딜레이
     * DIFF-05: 우선 알림 혜택
     */
    export function getNotificationDelay(tier: MemberTier): number {
      return tier === 'gold' ? 0 : 30 * 60 * 1000
    }
    ```
  </action>
  <verify>
    <automated>npm run test -- --grep "member.tier"</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --grep "member.tier" 실행 결과 모든 테스트 PASS (GREEN)
    - src/lib/data/member-tier.ts에 export function getMemberTier 포함
    - src/lib/data/member-tier.ts에 export function getNotificationDelay 포함
    - grep -c "server-only" src/lib/data/member-tier.ts 결과 >= 1
    - grep -c "console.log" src/lib/data/member-tier.ts 결과 = 0
  </acceptance_criteria>
  <done>member-tier.test.ts 6개 테스트 모두 PASS</done>
</task>

<task type="auto">
  <name>Task 2: TierBadge 서버 컴포넌트 + ReviewList/CommentSection 연동</name>
  <files>
    src/components/reviews/TierBadge.tsx,
    src/components/reviews/ReviewList.tsx,
    src/components/reviews/CommentSection.tsx
  </files>
  <read_first>
    src/components/reviews/ReviewList.tsx (현재 작성자 렌더 패턴, formatNick 사용 위치)
    src/components/reviews/CommentSection.tsx (현재 댓글 작성자 렌더 패턴)
    .planning/phases/08-community-deepening/08-UI-SPEC.md (TierBadge 컴포넌트 계약 섹션)
  </read_first>
  <action>
    1. src/components/reviews/TierBadge.tsx 신규 생성 (UI-SPEC 코드 스케치 그대로 구현):

    - 서버 컴포넌트 (use client 없음)
    - Props: { tier: 'bronze'|'silver'|'gold', cafeVerified: boolean, className?: string }
    - bronze + cafeVerified=false → null 반환
    - silver → 🔥 배지: background var(--bg-surface-2), color var(--fg-sec), height 18px, padding 0 6px, border-radius 6px, font 600 10px/1, letter-spacing 0.02em
    - gold → 👑 배지: background var(--bg-surface-2), color var(--fg-pri)
    - cafeVerified → 💬 배지: background var(--bg-positive-tint), color var(--fg-positive)
    - 각 배지에 aria-label 필수: "골드 등급", "실버 등급", "카페 인증"
    - 컨테이너: display inline-flex, gap 4px, alignItems center, marginLeft 4px
    - 금지: glow/shadow, gradient 배경, 외부 이미지

    2. ReviewList.tsx 수정:
    - reviews를 map할 때 각 review 카드의 작성자(formatNick() 텍스트) 뒤에 TierBadge 추가
    - review.user?.member_tier, review.user?.cafe_nickname 을 조회하는 쿼리가 필요하면 getComplexReviews 쿼리에 profiles 조인 추가 또는 별도 조회
    - profiles join: `.select('*, profiles!user_id(member_tier, cafe_nickname)')` 패턴
    - TierBadge: `<TierBadge tier={review.user?.member_tier ?? 'bronze'} cafeVerified={review.user?.cafe_nickname != null} />`

    3. CommentSection.tsx 수정:
    - 댓글 목록 렌더 시 각 댓글의 작성자 이름 뒤에 TierBadge 추가
    - 동일한 profiles join 패턴 적용
  </action>
  <verify>
    <automated>npm run test -- --grep "TierBadge"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/reviews/TierBadge.tsx 파일 존재
    - grep -c "'use client'" src/components/reviews/TierBadge.tsx 결과 = 0 (서버 컴포넌트 확인)
    - grep -c "aria-label" src/components/reviews/TierBadge.tsx 결과 >= 2
    - grep -c "TierBadge" src/components/reviews/ReviewList.tsx 결과 >= 1
    - grep -c "TierBadge" src/components/reviews/CommentSection.tsx 결과 >= 1
    - grep -c "backdrop" src/components/reviews/TierBadge.tsx 결과 = 0 (AI 슬롭 금지)
    - npm run test -- --grep "TierBadge" 실행 결과 PASS
    - npm run lint 통과
  </acceptance_criteria>
  <done>TierBadge 컴포넌트가 silver/gold/cafe_verified 조건에 따라 올바른 배지를 렌더하고, ReviewList/CommentSection에 통합됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → DB | getMemberTier는 서버 컴포넌트에서만 호출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | profiles.activity_points | accept | 이 Plan은 읽기 전용. 포인트 갱신은 DB 트리거(08-00 마이그레이션)에서만. getMemberTier는 SELECT만 수행. |
</threat_model>

<verification>
- npm run test -- --grep "member.tier" → 모두 PASS
- npm run test -- --grep "TierBadge" → PASS
- grep -c "'use client'" src/components/reviews/TierBadge.tsx → 0
- grep -c "SECURITY DEFINER" supabase/migrations/20260512000001_phase8_gamification.sql → 3 이상
- npm run build → 오류 없음
</verification>

<success_criteria>
- member-tier.test.ts 6개 테스트 GREEN
- TierBadge가 silver→🔥, gold→👑, cafeVerified→💬, bronze→null 렌더
- ReviewList와 CommentSection에서 등급 마크가 작성자 이름 뒤에 표시됨
- TypeScript strict 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-01-SUMMARY.md` 작성.
</output>

---
phase: 08-community-deepening
plan: "06"
type: execute
wave: 3
depends_on: ["08-04", "08-05"]
files_modified:
  - src/components/admin/AdminCardnewsCopyButton.tsx
  - src/app/admin/cardnews/page.tsx
autonomous: true
requirements:
  - OPS-02

must_haves:
  truths:
    - "AdminCardnewsCopyButton이 텍스트를 클립보드에 복사한다"
    - "복사 성공 시 버튼 텍스트가 '복사됨 ✓'로 2초간 변경된다"
    - "복사 실패 시 버튼 텍스트가 '복사 실패'로 2초간 변경된다"
    - "토스트/모달 없이 버튼 인라인 텍스트 변화로 피드백을 제공한다"
    - "어드민 카드뉴스 페이지에 복사 버튼이 표시된다"
  artifacts:
    - path: "src/components/admin/AdminCardnewsCopyButton.tsx"
      provides: "'use client' Clipboard API 복사 버튼"
      exports: ["AdminCardnewsCopyButton"]
      contains: "navigator.clipboard.writeText|copying.*success.*error"
    - path: "src/app/admin/cardnews/page.tsx"
      provides: "카드뉴스 복사 버튼 통합"
      contains: "AdminCardnewsCopyButton"
  key_links:
    - from: "AdminCardnewsCopyButton"
      to: "navigator.clipboard.writeText"
      via: "async handleCopy()"
      pattern: "clipboard.writeText"
---

<objective>
어드민 카드뉴스 1-click 복사 버튼 구현 (OPS-02 축소 scope).

Purpose: Daum 카페 글쓰기 API 부재로 자동 발행 불가. 어드민에서 카드뉴스 텍스트를 1-click으로 클립보드에 복사하여 수동으로 카페에 붙여넣기한다.
Output: AdminCardnewsCopyButton 컴포넌트, 어드민 카드뉴스 페이지 통합.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-community-deepening/08-UI-SPEC.md
@.planning/phases/08-community-deepening/08-04-SUMMARY.md
@.planning/phases/08-community-deepening/08-05-SUMMARY.md

<interfaces>
From src/app/admin/cardnews/page.tsx (현재 파일 읽기 필수):
```typescript
// 현재: CardnewsDownloadButton 단독 사용
import { CardnewsDownloadButton } from '@/components/admin/CardnewsDownloadButton'
```

UI-SPEC AdminCardnewsCopyButton Props:
```typescript
interface AdminCardnewsCopyButtonProps {
  text: string  // 복사할 카드뉴스 텍스트 (서버에서 생성 후 props로 전달)
}
```

UI-SPEC 상태 전환:
```
idle: '텍스트 복사' → .btn-sm.btn-secondary
copying: '복사 중…' → disabled, opacity 0.7
success: '복사됨 ✓' → color var(--fg-positive), border 1px solid var(--fg-positive)
error: '복사 실패' → color var(--fg-negative)
success/error → 2000ms 후 idle 자동 복귀
```

From src/components/admin/CardnewsDownloadButton.tsx (배치 참조):
현재 어드민 cardnews 페이지에서 CardnewsDownloadButton 옆에 추가.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminCardnewsCopyButton 구현 + 어드민 페이지 통합</name>
  <files>
    src/components/admin/AdminCardnewsCopyButton.tsx,
    src/app/admin/cardnews/page.tsx
  </files>
  <read_first>
    src/app/admin/cardnews/page.tsx (현재 전체 코드 — CardnewsDownloadButton 위치, 카드뉴스 텍스트 생성 로직 파악)
    src/components/admin/CardnewsDownloadButton.tsx (기존 버튼 컴포넌트 패턴)
    .planning/phases/08-community-deepening/08-UI-SPEC.md (AdminCardnewsCopyButton 명세 전체)
  </read_first>
  <action>
    1. src/components/admin/AdminCardnewsCopyButton.tsx 신규 생성:
    UI-SPEC 코드 스케치 그대로 구현:

    ```typescript
    'use client'
    import { useState } from 'react'

    interface AdminCardnewsCopyButtonProps {
      text: string
    }

    export function AdminCardnewsCopyButton({ text }: AdminCardnewsCopyButtonProps) {
      const [state, setState] = useState<'idle' | 'copying' | 'success' | 'error'>('idle')

      async function handleCopy() {
        setState('copying')
        try {
          await navigator.clipboard.writeText(text)
          setState('success')
        } catch {
          setState('error')
        } finally {
          setTimeout(() => setState('idle'), 2000)
        }
      }

      const label = {
        idle:    '텍스트 복사',
        copying: '복사 중…',
        success: '복사됨 ✓',
        error:   '복사 실패',
      }[state]

      return (
        <button
          type="button"
          onClick={handleCopy}
          disabled={state === 'copying'}
          className="btn btn-sm btn-secondary"
          style={{
            color: state === 'success' ? 'var(--fg-positive)'
                 : state === 'error'   ? 'var(--fg-negative)'
                 : undefined,
            borderColor: state === 'success' ? 'var(--fg-positive)'
                       : state === 'error'   ? 'var(--fg-negative)'
                       : undefined,
            opacity:    state === 'copying' ? 0.7 : 1,
            transition: 'color 120ms ease, border-color 120ms ease',
          }}
          aria-label={label}
          aria-live="polite"
        >
          {label}
        </button>
      )
    }
    ```

    2. src/app/admin/cardnews/page.tsx 수정:
    - AdminCardnewsCopyButton import 추가
    - 카드뉴스 텍스트를 page.tsx에서 생성 (서버에서):
      * 실거래 TOP 5 데이터를 supabase에서 조회 (기존 CardnewsDownloadButton에서 사용하는 쿼리 패턴 참조)
      * 텍스트 형식:
        ```
        📊 이번 주 창원·김해 신고가 TOP 5

        1위. {단지명} ({구/동}) — {가격}만원 ({면적}㎡)
        2위. {단지명} ...
        ...

        📅 집계 기간: {최근30일 기간}
        🔗 단지온도에서 상세 보기: https://danjiondo.kr
        ```
      * 텍스트 최대 500자로 제한
    - CardnewsDownloadButton 옆에 AdminCardnewsCopyButton 추가 (gap 8px)
    - AdminCardnewsCopyButton에 text={cardnewsText} 전달
    - 버튼 배치: `<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>` 컨테이너
    - 금지: 토스트/스낵바, confetti, 성공 시 아이콘 이미지 (텍스트 ✓만 사용)
  </action>
  <verify>
    <automated>npm run lint && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/AdminCardnewsCopyButton.tsx 파일 존재
    - grep -c "'use client'" src/components/admin/AdminCardnewsCopyButton.tsx 결과 >= 1
    - grep -c "clipboard.writeText" src/components/admin/AdminCardnewsCopyButton.tsx 결과 >= 1
    - grep -c "setTimeout.*idle.*2000\|2000.*idle" src/components/admin/AdminCardnewsCopyButton.tsx 결과 >= 1
    - grep -c "aria-live" src/components/admin/AdminCardnewsCopyButton.tsx 결과 >= 1
    - grep -c "toast\|snackbar\|confetti" src/components/admin/AdminCardnewsCopyButton.tsx 결과 = 0 (금지 패턴)
    - grep -c "AdminCardnewsCopyButton" src/app/admin/cardnews/page.tsx 결과 >= 1
    - npm run build 통과
  </acceptance_criteria>
  <done>어드민 카드뉴스 페이지에 1-click 텍스트 복사 버튼이 표시되고, 복사 성공/실패 상태가 버튼 텍스트로 인라인 표시됨</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: OPS-02 복사 버튼 동작 E2E 확인</name>
  <what-built>
    AdminCardnewsCopyButton이 어드민 카드뉴스 페이지에 추가됨. navigator.clipboard.writeText로 카드뉴스 텍스트를 복사한다.
  </what-built>
  <how-to-verify>
    1. 개발 서버 실행: npm run dev
    2. /admin/cardnews 페이지 접속 (관리자 계정 필요)
    3. "텍스트 복사" 버튼 클릭
    4. 버튼 텍스트가 "복사됨 ✓"로 변경되는지 확인
    5. 텍스트 에디터에 붙여넣기 → 카드뉴스 텍스트 확인
    6. 2초 후 버튼이 "텍스트 복사"로 복귀하는지 확인

    또는 E2E 테스트:
    ```bash
    npm run test:e2e -- --grep "cardnews.*copy"
    ```
  </how-to-verify>
  <resume-signal>
    복사 버튼이 정상 동작하면 "copy-ok" 입력. 오류 있으면 오류 내용 설명.
  </resume-signal>
  <acceptance_criteria>
    - /admin/cardnews 페이지에 "텍스트 복사" 버튼 표시됨
    - 클릭 시 "복사됨 ✓" 텍스트 변경
    - 2초 후 "텍스트 복사"로 복귀
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 어드민 페이지 접근 | role 'admin'/'superadmin' 체크 — 기존 page.tsx 인증 로직 유지 |
| 클립보드 복사 | 브라우저 Clipboard API — HTTPS 필요 (Vercel 배포 환경) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-06 | Information Disclosure | AdminCardnewsCopyButton | accept | 어드민 전용 기능. 복사 텍스트는 공개 데이터(실거래가). HTTPS에서만 Clipboard API 동작. |
</threat_model>

<verification>
- src/components/admin/AdminCardnewsCopyButton.tsx 존재
- grep -c "clipboard.writeText" src/components/admin/AdminCardnewsCopyButton.tsx → >= 1
- grep -c "toast\|confetti" src/components/admin/AdminCardnewsCopyButton.tsx → 0
- grep -c "AdminCardnewsCopyButton" src/app/admin/cardnews/page.tsx → >= 1
- npm run build → 오류 없음
</verification>

<success_criteria>
- AdminCardnewsCopyButton이 어드민 카드뉴스 페이지에 표시됨
- 클릭 시 클립보드에 카드뉴스 텍스트 복사
- 상태 전환: idle → copying → success/error → idle (2초)
- npm run test:e2e -- --grep "cardnews.*copy" 통과 (또는 수동 확인 완료)
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-06-SUMMARY.md` 작성.
</output>

---
phase: 08-community-deepening
plan: "02"
type: execute
wave: 2
depends_on: ["08-00", "08-01"]
files_modified:
  - src/lib/notifications/deliver.ts
  - src/lib/notifications/generate-alerts.ts
autonomous: true
requirements:
  - DIFF-05

must_haves:
  truths:
    - "deliverKakaoChannelNotifications() 함수가 deliver.ts에 추가된다"
    - "gold 등급 사용자는 알림 딜레이 없이 즉시 발송 큐에 들어간다"
    - "silver/bronze 등급 사용자는 생성 30분 후에 발송 가능 상태가 된다"
    - "member-tier.test.ts의 getNotificationDelay 테스트가 GREEN으로 유지된다"
  artifacts:
    - path: "src/lib/notifications/deliver.ts"
      provides: "deliverKakaoChannelNotifications() 함수 추가"
      exports: ["deliverPendingNotifications", "deliverKakaoChannelNotifications"]
    - path: "src/lib/notifications/generate-alerts.ts"
      provides: "notifications INSERT 시 priority 컬럼 + 딜레이 로직"
      contains: "member_tier|priority|getNotificationDelay"
  key_links:
    - from: "generate-alerts.ts"
      to: "profiles.member_tier"
      via: "getMemberTier() 호출"
      pattern: "getMemberTier"
    - from: "deliver.ts deliverKakaoChannelNotifications"
      to: "kakao_channel_subscriptions 테이블"
      via: "Supabase 쿼리"
      pattern: "kakao_channel_subscriptions"
---

<objective>
회원 등급별 알림 우선순위 + 카카오 채널 알림 분기 로직 구현.

Purpose: gold 등급 사용자에게 즉시 알림, silver/bronze에 30분 딜레이. deliver.ts에 카카오 채널 분기 추가.
Output: generate-alerts.ts에 tier 우선순위 로직, deliver.ts에 deliverKakaoChannelNotifications 함수.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-community-deepening/08-RESEARCH.md
@.planning/phases/08-community-deepening/08-00-SUMMARY.md

<interfaces>
From src/lib/notifications/deliver.ts (전체 파일 읽기 필수):
```typescript
export async function deliverPendingNotifications(supabase): Promise<{ sent: number; failed: number }>
// 현재: email + web push 전송
// 변경: kakao_channel 타입 알림은 deliverKakaoChannelNotifications로 분기
```

From src/lib/data/member-tier.ts (08-01에서 생성):
```typescript
export type MemberTier = 'bronze' | 'silver' | 'gold'
export async function getMemberTier(userId, supabase): Promise<{ tier: MemberTier, points: number }>
export function getNotificationDelay(tier: MemberTier): number  // 0 | 1800000
```

From 08-RESEARCH.md (회원 등급 우선 알림 패턴):
```typescript
// notifications 테이블에 priority 컬럼 없으면 created_at 기반 딜레이 체크
async function shouldNotifyNow(userId, supabase): Promise<boolean> {
  // gold → true (즉시)
  // silver/bronze → created_at + 30분 경과 시 true
}
```

notifications 테이블 스키마:
- id, user_id, title, body, type, status, created_at, delivered_at
- status: 'pending' | 'sent' | 'failed'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate-alerts.ts에 등급별 알림 우선순위 로직 추가</name>
  <files>src/lib/notifications/generate-alerts.ts</files>
  <read_first>
    src/lib/notifications/generate-alerts.ts (현재 전체 코드)
    src/lib/data/member-tier.ts (getMemberTier, getNotificationDelay)
    src/__tests__/notify-worker.test.ts (기존 테스트 — 깨지면 안 됨)
  </read_first>
  <behavior>
    - shouldDeliverNow(userId, supabase, notificationCreatedAt): gold → true. silver/bronze → created_at + 30분 이후 true.
    - generatePriceAlerts 기존 동작 변경 없음 (기존 테스트 유지)
  </behavior>
  <action>
    src/lib/notifications/generate-alerts.ts 수정:

    파일 상단에 추가 import:
    ```typescript
    import { getMemberTier, getNotificationDelay } from '@/lib/data/member-tier'
    ```

    기존 코드 뒤에 shouldDeliverNow 함수 추가 (export):
    ```typescript
    /**
     * DIFF-05: 등급별 알림 발송 우선순위
     * gold → 즉시(딜레이 0ms), silver/bronze → 생성 30분 후
     */
    export async function shouldDeliverNow(
      userId: string,
      supabase: SupabaseClient<Database>,
      notificationCreatedAt: Date,
    ): Promise<boolean> {
      const { tier } = await getMemberTier(userId, supabase)
      const delayMs = getNotificationDelay(tier)

      if (delayMs === 0) return true

      const elapsedMs = Date.now() - notificationCreatedAt.getTime()
      return elapsedMs >= delayMs
    }
    ```

    기존 generatePriceAlerts 코드는 변경 없음.
  </action>
  <verify>
    <automated>npm run test -- --grep "member.tier.*priority|notify-worker"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "shouldDeliverNow" src/lib/notifications/generate-alerts.ts 결과 >= 1
    - grep -c "getMemberTier" src/lib/notifications/generate-alerts.ts 결과 >= 1
    - npm run test -- --grep "notify-worker" 결과 기존 테스트 모두 PASS (회귀 없음)
    - npm run test -- --grep "member.tier.*priority" 결과 PASS
  </acceptance_criteria>
  <done>shouldDeliverNow 함수가 등급별로 올바른 딜레이를 적용하고, 기존 알림 워커 테스트가 회귀 없이 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deliver.ts에 deliverKakaoChannelNotifications 추가</name>
  <files>src/lib/notifications/deliver.ts</files>
  <read_first>
    src/lib/notifications/deliver.ts (현재 전체 코드 — 반드시 읽기)
    src/__tests__/kakao-channel.test.ts (RED 테스트 — deliverKakaoChannelNotifications 함수 존재 테스트)
    src/services/kakao-channel.ts (Wave 2에서 구현될 예정, 여기서는 import만 추가)
  </read_first>
  <behavior>
    - deliverKakaoChannelNotifications(supabase): kakao_channel_subscriptions에서 is_active=true 구독자 조회 → notifications 테이블에서 해당 user_id의 pending 알림 조회 → shouldDeliverNow 체크 → kakao-channel.ts의 sendAlimtalk 호출 (파일이 없으면 동적 import + 미존재 시 mock)
    - 발송 성공: notifications.status = 'sent', delivered_at = now()
    - 발송 실패: notifications.status = 'failed'
  </behavior>
  <action>
    src/lib/notifications/deliver.ts 수정:

    기존 import 아래에 추가:
    ```typescript
    import { shouldDeliverNow } from './generate-alerts'
    ```

    파일 맨 아래에 deliverKakaoChannelNotifications 함수 추가:
    ```typescript
    /**
     * DIFF-04/05: 카카오톡 채널 알림 발송
     * kakao_channel_subscriptions의 is_active 구독자에게 pending 알림을 전달한다.
     * 등급에 따라 shouldDeliverNow로 딜레이 적용.
     */
    export async function deliverKakaoChannelNotifications(
      supabase: SupabaseClient<Database>,
    ): Promise<{ sent: number; failed: number }> {
      // 구독자 조회
      const { data: subscriptions } = await supabase
        .from('kakao_channel_subscriptions')
        .select('user_id, phone_number')
        .eq('is_active', true)

      if (!subscriptions?.length) return { sent: 0, failed: 0 }

      let sent = 0
      let failed = 0

      for (const sub of subscriptions) {
        const subscription = sub as { user_id: string; phone_number: string }

        // 해당 사용자의 pending 알림 조회
        const { data: pending } = await supabase
          .from('notifications')
          .select('id, title, body, type, created_at')
          .eq('user_id', subscription.user_id)
          .eq('status', 'pending')
          .order('created_at')
          .limit(10)

        for (const notif of pending ?? []) {
          const n = notif as { id: string; title: string; body: string; type: string; created_at: string }

          try {
            const canSend = await shouldDeliverNow(
              subscription.user_id,
              supabase,
              new Date(n.created_at),
            )
            if (!canSend) continue

            // kakao-channel 어댑터 동적 import (Wave 2에서 실제 구현)
            // SOLAPI 계정 없으면 mock 경로로 스킵
            const kakaoKey = process.env.SOLAPI_API_KEY
            if (kakaoKey) {
              const { sendAlimtalk } = await import('@/services/kakao-channel')
              await sendAlimtalk({
                to:         subscription.phone_number,
                pfId:       process.env.KAKAO_CHANNEL_PF_ID!,
                templateId: 'KA01TP_PRICE_ALERT',
                variables:  { '#{내용}': n.body },
              })
            }

            await supabase
              .from('notifications')
              .update({ status: 'sent', delivered_at: new Date().toISOString() })
              .eq('id', n.id)

            sent++
          } catch {
            await supabase
              .from('notifications')
              .update({ status: 'failed' })
              .eq('id', n.id)
            failed++
          }
        }
      }

      return { sent, failed }
    }
    ```

    주의:
    - phone_number를 절대 console.log, Error 메시지에 포함 금지 (T-8-04)
    - 기존 deliverPendingNotifications 함수 변경 없음
  </action>
  <verify>
    <automated>npm run test -- --grep "kakao-channel|deliver.*kakao"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "deliverKakaoChannelNotifications" src/lib/notifications/deliver.ts 결과 >= 1
    - grep -c "console.log" src/lib/notifications/deliver.ts 결과 = 0
    - grep -c "phone_number" src/lib/notifications/deliver.ts 결과 <= 2 (로그에 포함 금지)
    - grep 내부에 "phone_number.*log\|log.*phone_number" 패턴 없음
    - npm run test -- --grep "kakao-channel" deliver.ts의 deliverKakaoChannelNotifications 존재 테스트 PASS
    - npm run test -- --grep "notify-worker" 기존 테스트 회귀 없음
    - npm run lint 통과
  </acceptance_criteria>
  <done>deliverKakaoChannelNotifications 함수가 deliver.ts에 추가되고, kakao-channel.test.ts의 존재 확인 테스트 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 알림 워커 → kakao_channel_subscriptions | Supabase server client 경유, RLS 우회 불가 |
| phone_number 처리 | 서버 메모리에서만 사용, 로그 절대 출력 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Information Disclosure | kakao_channel_subscriptions.phone_number | mitigate | 코드 내 console.log 금지. Error 메시지에 번호 포함 금지. grep 확인 필수. |
| T-8-02 | Information Disclosure | SOLAPI_API_KEY | mitigate | process.env에서만 읽음. kakao-channel.ts 어댑터에서만 초기화 (Wave 2에서 확인). |
</threat_model>

<verification>
- npm run test -- --grep "member.tier.*priority" → PASS
- npm run test -- --grep "notify-worker" → 기존 테스트 회귀 없음
- npm run test -- --grep "deliver.*kakao" → PASS (deliverKakaoChannelNotifications 존재)
- grep -c "console.log" src/lib/notifications/deliver.ts → 0
- npm run lint → 오류 없음
</verification>

<success_criteria>
- shouldDeliverNow: gold→즉시, silver/bronze→30분 딜레이 적용
- deliverKakaoChannelNotifications 함수가 deliver.ts에 export됨
- kakao-channel.test.ts의 "deliverKakaoChannelNotifications 함수 존재" 테스트 PASS
- 기존 notify-worker 테스트 모두 통과 (회귀 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-02-SUMMARY.md` 작성.
</output>

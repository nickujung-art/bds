---
phase: 08-community-deepening
plan: "03"
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - src/lib/data/compare.ts
  - src/app/compare/page.tsx
  - src/components/complex/CompareTable.tsx
  - src/components/complex/CompareAddButton.tsx
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - DIFF-06

must_haves:
  truths:
    - "buildCompareIds가 5개 이상 입력 시 4개로 slice한다"
    - "/compare?ids=uuid1,uuid2 URL로 접근 시 비교 표가 렌더된다"
    - "2개 미만 선택 시 빈 상태 메시지가 표시된다"
    - "단지 상세 페이지에 CompareAddButton이 표시된다"
    - "CompareAddButton 클릭 시 URL의 ids 파라미터가 업데이트된다"
  artifacts:
    - path: "src/lib/data/compare.ts"
      provides: "buildCompareIds(), getCompareData()"
      exports: ["buildCompareIds", "getCompareData", "ComplexSummary"]
    - path: "src/app/compare/page.tsx"
      provides: "/compare RSC 페이지"
      contains: "nuqs/server|parseAsArrayOf|ids.slice"
    - path: "src/components/complex/CompareTable.tsx"
      provides: "서버 컴포넌트 비교 표"
      contains: "CompareTableProps|complexes.length < 2"
    - path: "src/components/complex/CompareAddButton.tsx"
      provides: "'use client' URL state 버튼"
      contains: "useQueryState|parseAsArrayOf"
  key_links:
    - from: "CompareAddButton"
      to: "/compare"
      via: "nuqs URL state: ?ids=..."
      pattern: "useQueryState.*ids"
    - from: "/compare/page.tsx"
      to: "getComplexById"
      via: "Promise.all(validIds.map(id => getComplexById(id, supabase)))"
      pattern: "Promise.all"
---

<objective>
즐겨찾기 단지 비교 표 전체 구현.

Purpose: 단지 상세 페이지에서 버튼 클릭으로 2~4개 단지를 비교 URL에 담아 비교 표를 볼 수 있다.
Output: buildCompareIds 함수, CompareTable 서버 컴포넌트, CompareAddButton 클라이언트 컴포넌트, /compare RSC 페이지.
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-community-deepening/08-RESEARCH.md
@.planning/phases/08-community-deepening/08-UI-SPEC.md
@.planning/phases/08-community-deepening/08-00-SUMMARY.md

<interfaces>
From src/lib/data/complex-detail.ts:
```typescript
export interface ComplexDetail {
  id: string; canonical_name: string; road_address: string | null
  si: string | null; gu: string | null; dong: string | null
  built_year: number | null; household_count: number | null
  sgg_code: string; status: string; lat: number | null; lng: number | null
}
export async function getComplexById(id, supabase): Promise<ComplexDetail | null>
```

From src/__tests__/compare.test.ts (RED 테스트):
```typescript
buildCompareIds(['a','b','c','d','e']) → ['a','b','c','d']
buildCompareIds([]) → []
buildCompareIds(['a','',null]) → truthy values만 최대 4개
getCompareData(['id1','id2'], supabase) → ComplexSummary[] 2개
```

UI-SPEC CompareTable Props:
```typescript
interface ComplexSummary {
  id: string; canonical_name: string
  built_year: number | null; household_count: number | null
  road_address: string | null; si: string | null; gu: string | null; dong: string | null
  latestSalePrice: number | null      // 만원
  latestSalePricePerPy: number | null // 평당가 만원
  latestJeonsePrice: number | null
  areaRange: string | null           // "59~84㎡"
  schoolScore: number | null
  redevelopmentPhase: string | null
  heatType: string | null
}
```

UI-SPEC nuqs 패턴:
```typescript
// RSC (서버)
import { createSearchParamsCache, parseAsArrayOf, parseAsString } from 'nuqs/server'
const searchParamsCache = createSearchParamsCache({
  ids: parseAsArrayOf(parseAsString).withDefault([]),
})
const { ids } = searchParamsCache.parse(await searchParams)

// 클라이언트
import { parseAsArrayOf, parseAsString, useQueryState } from 'nuqs'
const [ids, setIds] = useQueryState('ids', parseAsArrayOf(parseAsString).withDefault([]))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildCompareIds + getCompareData 데이터 레이어 (GREEN)</name>
  <files>src/lib/data/compare.ts</files>
  <read_first>
    src/__tests__/compare.test.ts (RED 테스트 — 이것을 GREEN으로)
    src/lib/data/complex-detail.ts (getComplexById, ComplexDetail 인터페이스)
    src/lib/data/realprice.ts (최근 거래 조회 패턴 참조용)
  </read_first>
  <behavior>
    - buildCompareIds(ids: string[]): falsy 제거 + slice(0,4)
    - getCompareData(ids, supabase): Promise.all로 getComplexById 병렬 호출 → null 필터 → ComplexSummary[] 반환
    - ComplexSummary: ComplexDetail + 거래 데이터(latestSalePrice, latestJeonsePrice 등)
    - 거래 데이터 조회 시 반드시 cancel_date IS NULL AND superseded_by IS NULL 조건 포함
  </behavior>
  <action>
    src/lib/data/compare.ts 신규 생성:

    ```typescript
    import 'server-only'
    import type { SupabaseClient } from '@supabase/supabase-js'
    import { getComplexById } from './complex-detail'

    export interface ComplexSummary {
      id:                  string
      canonical_name:      string
      built_year:          number | null
      household_count:     number | null
      road_address:        string | null
      si:                  string | null
      gu:                  string | null
      dong:                string | null
      latestSalePrice:     number | null   // 만원
      latestSalePricePerPy: number | null  // 평당가 만원
      latestJeonsePrice:   number | null
      areaRange:           string | null   // "59~84㎡"
      schoolScore:         number | null
      redevelopmentPhase:  string | null
      heatType:            string | null
    }

    /**
     * T-8-05: URL 조작 방지 — 최대 4개 제한, falsy 필터링
     */
    export function buildCompareIds(ids: (string | null | undefined)[]): string[] {
      return ids
        .filter((id): id is string => typeof id === 'string' && id.length > 0)
        .slice(0, 4)
    }

    async function getLatestTransactionData(
      complexId: string,
      supabase: SupabaseClient,
    ): Promise<{
      latestSalePrice:     number | null
      latestSalePricePerPy: number | null
      latestJeonsePrice:   number | null
      areaRange:           string | null
    }> {
      // 3개 쿼리를 Promise.all로 병렬화 (WARNING 수정 — 단지당 쿼리 비용 절감)
      const [
        { data: saleData },
        { data: jeonseData },
        { data: areaData },
      ] = await Promise.all([
        supabase
          .from('transactions')
          .select('price, area_m2, deal_type')
          .eq('complex_id', complexId)
          .eq('deal_type', 'sale')
          .is('cancel_date', null)
          .is('superseded_by', null)
          .order('deal_date', { ascending: false })
          .limit(1),
        supabase
          .from('transactions')
          .select('price, area_m2, deal_type')
          .eq('complex_id', complexId)
          .eq('deal_type', 'jeonse')
          .is('cancel_date', null)
          .is('superseded_by', null)
          .order('deal_date', { ascending: false })
          .limit(1),
        supabase
          .from('transactions')
          .select('area_m2')
          .eq('complex_id', complexId)
          .is('cancel_date', null)
          .is('superseded_by', null)
          .order('area_m2', { ascending: true }),
      ])

      const latest = saleData?.[0] as { price: number; area_m2: number } | undefined
      const latestJeonse = jeonseData?.[0] as { price: number } | undefined

      const areas = (areaData ?? []).map(r => (r as { area_m2: number }).area_m2).filter(Boolean)
      const minArea = areas.length ? Math.min(...areas) : null
      const maxArea = areas.length ? Math.max(...areas) : null
      const areaRange = minArea && maxArea && minArea !== maxArea
        ? `${Math.round(minArea)}~${Math.round(maxArea)}㎡`
        : minArea ? `${Math.round(minArea)}㎡` : null

      const pricePerPy = latest?.area_m2
        ? Math.round(latest.price / (latest.area_m2 / 3.3058))
        : null

      return {
        latestSalePrice:     latest?.price ?? null,
        latestSalePricePerPy: pricePerPy,
        latestJeonsePrice:   latestJeonse?.price ?? null,
        areaRange,
      }
    }

    export async function getCompareData(
      ids: string[],
      supabase: SupabaseClient,
    ): Promise<ComplexSummary[]> {
      const validIds = buildCompareIds(ids)
      if (validIds.length === 0) return []

      const results = await Promise.all(
        validIds.map(async id => {
          const detail = await getComplexById(id, supabase)
          if (!detail) return null

          const txData = await getLatestTransactionData(id, supabase)

          return {
            id:                  detail.id,
            canonical_name:      detail.canonical_name,
            built_year:          detail.built_year,
            household_count:     detail.household_count,
            road_address:        detail.road_address,
            si:                  detail.si,
            gu:                  detail.gu,
            dong:                detail.dong,
            latestSalePrice:     txData.latestSalePrice,
            latestSalePricePerPy: txData.latestSalePricePerPy,
            latestJeonsePrice:   txData.latestJeonsePrice,
            areaRange:           txData.areaRange,
            schoolScore:         null,   // Phase 6 데이터 (없으면 null)
            redevelopmentPhase:  null,   // redevelopment_timelines 조회 가능하나 optional
            heatType:            null,   // facility_kapt 조회 가능하나 optional
          } satisfies ComplexSummary
        }),
      )

      return results.filter((c): c is ComplexSummary => c !== null)
    }
    ```
  </action>
  <verify>
    <automated>npm run test -- --grep "compare"</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- --grep "compare" 결과 모두 PASS (GREEN)
    - grep -c "cancel_date.*null\|is('cancel_date', null)" src/lib/data/compare.ts 결과 >= 1
    - grep -c "superseded_by.*null\|is('superseded_by', null)" src/lib/data/compare.ts 결과 >= 1
    - grep -c "slice(0, 4)" src/lib/data/compare.ts 결과 >= 1
    - grep -c "server-only" src/lib/data/compare.ts 결과 >= 1
  </acceptance_criteria>
  <done>compare.test.ts의 buildCompareIds, getCompareData 테스트 모두 PASS</done>
</task>

<task type="auto">
  <name>Task 2: /compare 페이지 + CompareTable + CompareAddButton 컴포넌트</name>
  <files>
    src/app/compare/page.tsx,
    src/components/complex/CompareTable.tsx,
    src/components/complex/CompareAddButton.tsx,
    src/app/complexes/[id]/page.tsx
  </files>
  <read_first>
    src/app/complexes/[id]/page.tsx (기존 단지 상세 페이지 — ShareButton/FavoriteButton 배치 위치 확인)
    .planning/phases/08-community-deepening/08-UI-SPEC.md (CompareTable, CompareAddButton, CompareSelector 명세 전체)
    src/lib/data/compare.ts (이 태스크에서 방금 생성한 파일)
  </read_first>
  <action>
    1. src/app/compare/page.tsx 생성 (RSC):
    - nuqs/server의 createSearchParamsCache, parseAsArrayOf, parseAsString 사용
    - searchParams에서 ids 파싱: buildCompareIds 통해 4개 제한
    - createReadonlyClient() 사용 (ISR 아님, but 공개 데이터이므로 readonly)
    - getCompareData(validIds, supabase) 호출
    - CompareTable 컴포넌트에 데이터 전달
    - metadata: title '단지 비교 — 단지온도'
    - export const revalidate = 0 (실시간 비교)

    2. src/components/complex/CompareTable.tsx 생성 (서버 컴포넌트):
    UI-SPEC 명세 그대로 구현:
    - Props: { complexes: ComplexSummary[] }
    - complexes.length < 2 → EmptyState 컴포넌트 반환
    - 빈 상태 문구: "단지를 2개 이상 선택하면 비교할 수 있어요.\n단지 상세 페이지에서 \"비교에 추가 +\" 버튼을 눌러주세요."
    - 테이블 레이아웃: sticky top row(단지명) + sticky left column(행 레이블)
    - overflow-x: auto 컨테이너
    - sticky top: 60px (nav 높이), z-index: 10
    - sticky left: z-index: 5
    - 행 정의 (ROWS 배열): area, household, built_year, latest_sale, price_per_py, latest_jeonse, school_score, redevelopment, heat_type
    - 데이터 형식:
      * latest_sale: null → "거래 없음", 있으면 "N억 M천" 형식 (억 단위 변환)
      * household: "1,200세대" 형식
      * built_year: "1998년" 형식
      * school_score: null → "정보 없음", 있으면 "★ 7.4" 형식
    - 셀 스타일: 짝수행 var(--bg-surface), 홀수행 var(--bg-surface-2)
    - 셀 패딩: 12px 16px (모바일 10px 12px)
    - 헤더: font 700 14px/1.4, height 56px, border-bottom 2px solid var(--line-default)
    - 데이터 셀: font 500 13px/1.4, color var(--fg-pri)
    - 숫자 데이터 셀에 fontVariantNumeric: 'tabular-nums'
    - 금지: backdrop-blur, gradient, 칼럼별 배경색 구분

    3. src/components/complex/CompareAddButton.tsx 생성 ('use client'):
    UI-SPEC 코드 스케치 그대로:
    - 'use client'
    - nuqs의 parseAsArrayOf, parseAsString, useQueryState 사용
    - isActive = ids.includes(complexId), isFull = ids.length >= 4 && !isActive
    - 버튼 텍스트: isFull → "4/4 비교 중", isActive → "비교 중 ✓", default → "비교에 추가 +"
    - aria-pressed={isActive}, aria-label
    - isFull: opacity 0.5, cursor not-allowed, disabled
    - 플로팅 버튼: ids.length >= 2 시 position fixed, bottom 24, right 24, z-index 40
    - 플로팅 버튼 텍스트: `비교 보기 ({ids.length})`
    - 플로팅 버튼 href: `/compare?ids=${ids.join(',')}`
    - 금지: scale bounce 애니메이션, map pin 클릭 진입

    4. src/app/complexes/[id]/page.tsx 수정:
    - 기존 ShareButton | FavoriteButton 배열 뒤에 CompareAddButton 추가
    - UI-SPEC 배치 순서: ShareButton | FavoriteButton | CompareAddButton | 알림 설정
    - CompareAddButton에 complexId={complex.id} complexName={complex.canonical_name} 전달
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/compare/page.tsx 파일 존재
    - grep -c "nuqs/server\|parseAsArrayOf" src/app/compare/page.tsx 결과 >= 1
    - grep -c "ids.slice(0, 4)\|buildCompareIds" src/app/compare/page.tsx 결과 >= 1
    - src/components/complex/CompareTable.tsx 파일 존재
    - grep -c "'use client'" src/components/complex/CompareTable.tsx 결과 = 0 (서버 컴포넌트)
    - grep -c "complexes.length < 2" src/components/complex/CompareTable.tsx 결과 >= 1
    - grep -c "backdrop" src/components/complex/CompareTable.tsx 결과 = 0
    - src/components/complex/CompareAddButton.tsx 파일 존재
    - grep -c "'use client'" src/components/complex/CompareAddButton.tsx 결과 >= 1
    - grep -c "useQueryState" src/components/complex/CompareAddButton.tsx 결과 >= 1
    - grep -c "CompareAddButton" src/app/complexes/[id]/page.tsx 결과 >= 1
    - npm run build 통과 (오류 없음)
  </acceptance_criteria>
  <done>/compare 페이지에서 2개 이상 단지 선택 시 비교 표가 렌더되고, 단지 상세 페이지에 CompareAddButton이 표시됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 파라미터 → RSC | nuqs 서버 파서 + buildCompareIds로 4개 제한 |
| 클라이언트 → CompareAddButton | 클라이언트에서도 4개 제한 적용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-05 | Information Disclosure | /compare?ids 파라미터 | mitigate | RSC에서 buildCompareIds(ids.slice(0,4)) 적용. getComplexById null 체크. 존재하지 않는 ID는 null 필터. |
</threat_model>

<verification>
- npm run test -- --grep "compare" → 모두 PASS
- /compare?ids=id1,id2 접근 시 CompareTable 렌더됨 (2개 미만이면 빈 상태)
- CompareAddButton 클릭 → URL ?ids 파라미터 업데이트
- npm run build → 오류 없음
- grep -c "cancel_date.*null" src/lib/data/compare.ts → >= 1
</verification>

<success_criteria>
- compare.test.ts 모든 테스트 GREEN
- /compare 페이지가 빌드 오류 없이 생성됨
- CompareTable이 2개 미만 선택 시 안내 메시지 표시
- CompareAddButton이 단지 상세 페이지에 배치됨
</success_criteria>

<output>
완료 후 `.planning/phases/08-community-deepening/08-03-SUMMARY.md` 작성.
</output>

---
phase: 11-map-enhancement
plan: "01"
type: execute
wave: 1
depends_on: ["11-00"]
files_modified:
  - src/lib/data/complexes-map.ts
  - src/components/map/markers/badge-logic.ts
  - src/lib/utils/badge-logic.test.ts
autonomous: true
requirements:
  - MAP-02
  - MAP-04
  - MAP-05

must_haves:
  truths:
    - "ComplexMapItem 인터페이스에 avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d, status, built_year, household_count, hagwon_grade 필드가 있다"
    - "getComplexesForMap 쿼리가 위 필드를 select한다"
    - "buildClusterIndex가 새 필드를 properties에 포함해 저장한다"
    - "determineBadge() 순수 함수가 우선순위 로직에 따라 배지 타입을 반환한다"
    - "badge-logic.test.ts의 TODO 테스트가 실제 로직을 검증하도록 교체된다"
  artifacts:
    - path: "src/lib/data/complexes-map.ts"
      provides: "ComplexMapItem 확장 + getComplexesForMap 쿼리 확장 + buildClusterIndex 확장"
      contains: "avg_sale_per_pyeong"
    - path: "src/components/map/markers/badge-logic.ts"
      provides: "determineBadge() 순수 함수 + BadgeType 타입 + BadgeInput 인터페이스"
      exports: ["determineBadge", "BadgeType", "BadgeInput", "getPriceColor"]
  key_links:
    - from: "src/lib/data/complexes-map.ts"
      to: "public.complexes"
      via: "supabase.select('... avg_sale_per_pyeong, view_count ...')"
      pattern: "avg_sale_per_pyeong"
    - from: "src/components/map/markers/badge-logic.ts"
      to: "ComplexMapItem"
      via: "BadgeInput 인터페이스 필드 매핑"
      pattern: "determineBadge"
---

<objective>
ComplexMapItem 타입과 getComplexesForMap 쿼리를 Phase 11 컬럼 포함으로 확장하고, determineBadge() 순수 함수를 구현한다.

Purpose: Wave 2의 지도 컴포넌트(ClusterMarker, ComplexMarker, KakaoMap)가 의존하는 데이터 계약을 먼저 확립한다.
Output: 확장된 complexes-map.ts, 신규 badge-logic.ts, 검증된 테스트.
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-map-enhancement/11-00-RESEARCH.md
@.planning/phases/11-map-enhancement/11-PATTERNS.md
@.planning/phases/11-map-enhancement/11-00-SUMMARY.md

<interfaces>
<!-- src/lib/data/complexes-map.ts 현재 전체 내용 -->
<!-- Wave 0 완료 후 complexes 테이블에 추가된 컬럼:
     avg_sale_per_pyeong integer
     view_count integer NOT NULL DEFAULT 0
     price_change_30d numeric(5,4)
     tx_count_30d integer NOT NULL DEFAULT 0
-->

<!-- 기존 ComplexMapItem (현재) -->
export interface ComplexMapItem {
  id:             string
  canonical_name: string
  lat:            number
  lng:            number
  sgg_code:       string
}

<!-- 기존 getComplexesForMap select -->
.select('id, canonical_name, lat, lng, sgg_code')
.eq('status', 'active')

<!-- 기존 buildClusterIndex properties -->
properties: { id: c.id, name: c.canonical_name, cluster: false }

<!-- Wave 2 컴포넌트가 필요로 하는 타입 (이 플랜에서 정의해야 함) -->
export type BadgeType =
  | 'pre_sale' | 'new_build' | 'crown' | 'hot'
  | 'surge' | 'drop' | 'school' | 'large_complex'
  | 'redevelop' | 'none'

export interface BadgeInput {
  status:          string
  built_year:      number | null
  view_count:      number
  price_change_30d: number | null
  hagwon_grade:    string | null
  household_count: number | null
  tx_count_30d:    number
  p95_view_count:  number
  p95_tx_count:    number
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComplexMapItem + getComplexesForMap + buildClusterIndex 확장</name>
  <files>src/lib/data/complexes-map.ts</files>

  <read_first>
    - src/lib/data/complexes-map.ts (전체 파일 — 현재 5필드 인터페이스와 쿼리)
    - .planning/phases/11-map-enhancement/11-PATTERNS.md (complexes-map.ts 섹션 전체)
  </read_first>

  <behavior>
    - 기존 ComplexMapItem에 8개 필드가 추가된다: avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d, status, built_year, household_count, hagwon_grade
    - getComplexesForMap의 .select() 문자열이 새 필드를 포함한다 — facility_edu LEFT JOIN으로 hagwon_grade 포함
    - status 필터: .eq('status', 'active') 조건은 제거하고 status 컬럼을 select에 포함 (배지 로직에서 pre_sale 등 다른 status도 처리해야 함). 단, lat/lng null 제외 조건은 유지.
    - buildClusterIndex의 properties 객체에 8개 필드가 추가된다
    - clusterComplexes 함수는 변경 없음
  </behavior>

  <action>
파일 전체를 다음으로 교체한다:

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import Supercluster from 'supercluster'

export interface ComplexMapItem {
  id:                  string
  canonical_name:      string
  lat:                 number
  lng:                 number
  sgg_code:            string
  // Phase 11 추가
  avg_sale_per_pyeong: number | null
  view_count:          number
  price_change_30d:    number | null
  tx_count_30d:        number
  status:              string
  built_year:          number | null
  household_count:     number | null
  hagwon_grade:        string | null
}

export async function getComplexesForMap(
  sggCodes: string[],
  supabase: SupabaseClient,
): Promise<ComplexMapItem[]> {
  if (sggCodes.length === 0) return []

  const { data, error } = await supabase
    .from('complexes')
    .select(
      `id, canonical_name, lat, lng, sgg_code,
       avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d,
       status, built_year, household_count, successor_id,
       facility_edu!facility_edu_complex_id_fkey(hagwon_grade)`,
    )
    .in('sgg_code', sggCodes)
    .not('lat', 'is', null)
    .not('lng', 'is', null)
    .neq('status', 'demolished')

  if (error) throw new Error(`getComplexesForMap failed: ${error.message}`)

  return (data ?? []).map((row) => {
    const r = row as {
      id:                  string
      canonical_name:      string
      lat:                 number
      lng:                 number
      sgg_code:            string
      avg_sale_per_pyeong: number | null
      view_count:          number
      price_change_30d:    number | null
      tx_count_30d:        number
      status:              string
      built_year:          number | null
      household_count:     number | null
      facility_edu:        Array<{ hagwon_grade: string | null }> | null
    }
    return {
      id:                  r.id,
      canonical_name:      r.canonical_name,
      lat:                 r.lat,
      lng:                 r.lng,
      sgg_code:            r.sgg_code,
      avg_sale_per_pyeong: r.avg_sale_per_pyeong ?? null,
      view_count:          r.view_count ?? 0,
      price_change_30d:    r.price_change_30d ?? null,
      tx_count_30d:        r.tx_count_30d ?? 0,
      status:              r.status ?? 'active',
      built_year:          r.built_year ?? null,
      household_count:     r.household_count ?? null,
      hagwon_grade:        r.facility_edu?.[0]?.hagwon_grade ?? null,
    } satisfies ComplexMapItem
  })
}

// ── supercluster 래퍼 ──────────────────────────────────────

export type ClusterFeature = ReturnType<Supercluster['getClusters']>[number]

export function buildClusterIndex(complexes: ComplexMapItem[]) {
  const index = new Supercluster({ radius: 60, maxZoom: 12 })
  index.load(
    complexes.map((c) => ({
      type: 'Feature' as const,
      geometry: { type: 'Point' as const, coordinates: [c.lng, c.lat] },
      properties: {
        id:                  c.id,
        name:                c.canonical_name,
        cluster:             false,
        avg_sale_per_pyeong: c.avg_sale_per_pyeong,
        view_count:          c.view_count,
        price_change_30d:    c.price_change_30d,
        tx_count_30d:        c.tx_count_30d,
        status:              c.status,
        built_year:          c.built_year,
        household_count:     c.household_count,
        hagwon_grade:        c.hagwon_grade,
      },
    })),
  )
  return index
}

export function clusterComplexes(
  complexes: ComplexMapItem[],
  bounds: [number, number, number, number],
  zoom: number,
): ClusterFeature[] {
  if (complexes.length === 0) return []
  return buildClusterIndex(complexes).getClusters(bounds, zoom)
}
```

변경 요점:
- `.select()`에 `facility_edu!facility_edu_complex_id_fkey(hagwon_grade)` LEFT JOIN 추가
- `hagwon_grade`: `row.facility_edu?.[0]?.hagwon_grade ?? null` 로 매핑 (null 고정 제거)
- `buildClusterIndex` properties에 `hagwon_grade` 추가 (KakaoMap.tsx의 badge 계산에서 사용)
  </action>

  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep "complexes-map" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - ComplexMapItem 인터페이스에 12개 필드 존재 (기존 5 + 신규 7 + hagwon_grade 1)
    - getComplexesForMap select 문자열에 "avg_sale_per_pyeong", "view_count", "tx_count_30d", "facility_edu" 포함
    - buildClusterIndex properties에 "avg_sale_per_pyeong", "view_count", "hagwon_grade" 포함
    - hagwon_grade가 null 고정이 아닌 facility_edu JOIN에서 실제 값 매핑
    - TypeScript 컴파일 오류 없음
  </acceptance_criteria>

  <done>ComplexMapItem이 Phase 11 배지·라벨·패널 기능에 필요한 모든 필드를 제공한다. hagwon_grade가 facility_edu에서 실제 로드되어 지도 마커 'school' 배지가 작동한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: badge-logic.ts 구현 + 테스트 GREEN</name>
  <files>
    src/components/map/markers/badge-logic.ts
    src/lib/utils/badge-logic.test.ts
  </files>

  <read_first>
    - .planning/phases/11-map-enhancement/11-00-RESEARCH.md (Pattern 6 전체 — determineBadge 로직)
    - .planning/phases/11-map-enhancement/11-PATTERNS.md (badge-logic.ts 섹션)
    - src/lib/utils/badge-logic.test.ts (Wave 0 스캐폴드 — TODO 교체 대상)
  </read_first>

  <behavior>
    - determineBadge({status:'pre_sale', ...}) → 'pre_sale'
    - determineBadge({built_year:2022, ...}) → 'new_build' (pre_sale 아닌 경우)
    - determineBadge({tx_count_30d:10, p95_tx_count:10, ...}) → 'crown' (상위 1/2순위 아닌 경우)
    - determineBadge({view_count:100, p95_view_count:100, ...}) → 'hot'
    - determineBadge({price_change_30d:0.06, ...}) → 'surge' (6% 상승)
    - determineBadge({price_change_30d:-0.06, ...}) → 'drop'
    - determineBadge({hagwon_grade:'A+', ...}) → 'school'
    - determineBadge({household_count:1000, ...}) → 'large_complex'
    - determineBadge({status:'in_redevelopment', ...}) → 'redevelop'
    - 모든 조건 불충족 → 'none'
    - p95_tx_count = 0일 때 crown 배지 부여하지 않음 (데이터 없는 경우)
    - getPriceColor(null) → '#6B7280'
    - getPriceColor(700) → '#10B981' (800 미만)
    - getPriceColor(1200) → '#F59E0B' (800~1500)
    - getPriceColor(2000) → '#EF4444' (1500 초과)
  </behavior>

  <action>
**1단계: 디렉토리 생성 후 badge-logic.ts 작성**

파일 경로: src/components/map/markers/badge-logic.ts

```typescript
export type BadgeType =
  | 'pre_sale'
  | 'new_build'
  | 'crown'
  | 'hot'
  | 'surge'
  | 'drop'
  | 'school'
  | 'large_complex'
  | 'redevelop'
  | 'none'

export interface BadgeInput {
  status:           string
  built_year:       number | null
  view_count:       number
  price_change_30d: number | null  // 비율 (예: 0.10 = +10%)
  hagwon_grade:     string | null
  household_count:  number | null
  tx_count_30d:     number
  p95_view_count:   number
  p95_tx_count:     number
}

/**
 * 배지 우선순위에 따라 한 단지의 대표 배지 타입을 반환하는 순수 함수.
 * 1순위: pre_sale, new_build
 * 2순위: crown, hot, surge, drop
 * 3순위: school, large_complex, redevelop
 *
 * // 광고(ad) 배지: Phase 6 AI·차별화 기술 미완료로 입력 데이터(has_ad 필드)가 없음.
 * // Phase 6 완료 후 ComplexMapItem에 has_ad: boolean 추가 및 여기에 배지 로직 확장 예정.
 */
export function determineBadge(input: BadgeInput): BadgeType {
  // 1순위
  if (input.status === 'pre_sale') return 'pre_sale'
  if (input.built_year !== null && input.built_year >= 2021) return 'new_build'

  // 2순위
  if (input.p95_tx_count > 0 && input.tx_count_30d >= input.p95_tx_count) return 'crown'
  if (input.p95_view_count > 0 && input.view_count >= input.p95_view_count) return 'hot'
  if (input.price_change_30d !== null && input.price_change_30d > 0.05) return 'surge'
  if (input.price_change_30d !== null && input.price_change_30d < -0.05) return 'drop'

  // 3순위
  if (input.hagwon_grade !== null && ['A+', 'A'].includes(input.hagwon_grade)) return 'school'
  if (input.household_count !== null && input.household_count >= 1000) return 'large_complex'
  if (input.status === 'in_redevelopment') return 'redevelop'

  return 'none'
}

/**
 * 평당가(만원/평)에 따른 마커 색상 반환.
 * < 800만: emerald(저가), 800~1499만: amber(중가), >= 1500만: red(고가)
 */
export function getPriceColor(avgSalePerPyeong: number | null): string {
  if (avgSalePerPyeong === null || avgSalePerPyeong === 0) return '#6B7280' // gray-500
  if (avgSalePerPyeong < 800) return '#10B981'   // emerald-500: 저가
  if (avgSalePerPyeong < 1500) return '#F59E0B'  // amber-500: 중가
  return '#EF4444'                                // red-500: 고가
}
```

**2단계: badge-logic.test.ts의 TODO 테스트를 실제 검증으로 교체**

파일 경로: src/lib/utils/badge-logic.test.ts

```typescript
import { describe, it, expect } from 'vitest'
import { determineBadge, getPriceColor } from '@/components/map/markers/badge-logic'

const BASE: Parameters<typeof determineBadge>[0] = {
  status:           'active',
  built_year:       2010,
  view_count:       0,
  price_change_30d: null,
  hagwon_grade:     null,
  household_count:  500,
  tx_count_30d:     0,
  p95_view_count:   100,
  p95_tx_count:     10,
}

describe('determineBadge — 배지 우선순위', () => {
  it('status=pre_sale이면 pre_sale 배지를 반환한다', () => {
    expect(determineBadge({ ...BASE, status: 'pre_sale' })).toBe('pre_sale')
  })

  it('built_year >= 2021이면 new_build 배지를 반환한다', () => {
    expect(determineBadge({ ...BASE, built_year: 2022 })).toBe('new_build')
  })

  it('pre_sale이 new_build보다 높은 우선순위를 가진다', () => {
    expect(determineBadge({ ...BASE, status: 'pre_sale', built_year: 2023 })).toBe('pre_sale')
  })

  it('tx_count_30d >= p95_tx_count이면 crown을 반환한다', () => {
    expect(determineBadge({ ...BASE, tx_count_30d: 10, p95_tx_count: 10 })).toBe('crown')
  })

  it('p95_tx_count = 0이면 crown을 부여하지 않는다', () => {
    expect(determineBadge({ ...BASE, tx_count_30d: 0, p95_tx_count: 0 })).toBe('none')
  })

  it('view_count >= p95_view_count이면 hot을 반환한다', () => {
    expect(determineBadge({ ...BASE, view_count: 100, p95_view_count: 100 })).toBe('hot')
  })

  it('price_change_30d > 0.05이면 surge를 반환한다', () => {
    expect(determineBadge({ ...BASE, price_change_30d: 0.06 })).toBe('surge')
  })

  it('price_change_30d < -0.05이면 drop을 반환한다', () => {
    expect(determineBadge({ ...BASE, price_change_30d: -0.06 })).toBe('drop')
  })

  it('hagwon_grade=A+이면 school을 반환한다', () => {
    expect(determineBadge({ ...BASE, hagwon_grade: 'A+' })).toBe('school')
  })

  it('household_count >= 1000이면 large_complex를 반환한다', () => {
    expect(determineBadge({ ...BASE, household_count: 1000 })).toBe('large_complex')
  })

  it('status=in_redevelopment이면 redevelop을 반환한다', () => {
    expect(determineBadge({ ...BASE, status: 'in_redevelopment' })).toBe('redevelop')
  })

  it('모든 조건 불충족 시 none을 반환한다', () => {
    expect(determineBadge(BASE)).toBe('none')
  })
})

describe('getPriceColor', () => {
  it('null → gray', () => { expect(getPriceColor(null)).toBe('#6B7280') })
  it('700 → emerald', () => { expect(getPriceColor(700)).toBe('#10B981') })
  it('1200 → amber', () => { expect(getPriceColor(1200)).toBe('#F59E0B') })
  it('2000 → red', () => { expect(getPriceColor(2000)).toBe('#EF4444') })
})
```
  </action>

  <verify>
    <automated>npm run test -- --run src/lib/utils/badge-logic.test.ts 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - src/components/map/markers/badge-logic.ts 파일이 생성된다
    - `npm run test -- --run src/lib/utils/badge-logic.test.ts` 모든 테스트 통과 (GREEN)
    - determineBadge, getPriceColor, BadgeType, BadgeInput이 named export로 존재한다
    - price_change_30d는 비율 기준 (0.05 = 5%) — 퍼센트 기준이 아님
  </acceptance_criteria>

  <done>배지 우선순위 순수 함수가 모든 케이스를 커버하며 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getComplexesForMap → Supabase | 서버 사이드 쿼리. RLS anon read 정책 적용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | getComplexesForMap | accept | complexes 데이터는 공개 정보. RLS anon SELECT 정책으로 보호됨 |
</threat_model>

<verification>
```bash
# 타입 체크
npx tsc --noEmit --pretty false 2>&1 | grep -v "node_modules" | head -20

# 배지 로직 테스트
npm run test -- --run src/lib/utils/badge-logic.test.ts

# complexes-map exports 확인
grep -n "export" src/lib/data/complexes-map.ts
```
</verification>

<success_criteria>
1. TypeScript 컴파일 오류 없음 (복잡한 supabase 타입 제외)
2. badge-logic.test.ts 모든 테스트 GREEN (16개 이상)
3. ComplexMapItem에 12개 필드 존재 확인
4. getComplexesForMap select에 facility_edu JOIN 포함 확인
</success_criteria>

<output>
완료 후 .planning/phases/11-map-enhancement/11-01-SUMMARY.md 생성.
</output>

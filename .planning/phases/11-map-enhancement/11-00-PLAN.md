---
phase: 11-map-enhancement
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260516000001_phase11_map_columns.sql
  - src/components/map/ClusterMarker.test.tsx
  - src/lib/utils/badge-logic.test.ts
  - src/app/api/complexes/[id]/map-panel/route.test.ts
  - src/app/complexes/[id]/actions.test.ts
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05

must_haves:
  truths:
    - "complexes 테이블에 avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d 컬럼이 존재한다"
    - "increment_view_count(p_complex_id uuid) RPC 함수가 anon/authenticated에게 실행 권한을 가진다"
    - "refresh_complex_price_stats() 배치 함수가 cancel_date IS NULL AND superseded_by IS NULL 조건을 포함해 집계한다"
    - "Wave 1 작업을 시작하기 위한 테스트 파일 스캐폴드가 존재한다"
  artifacts:
    - path: "supabase/migrations/20260516000001_phase11_map_columns.sql"
      provides: "Phase 11 DB 스키마 — 4개 컬럼 + 2개 함수 + GRANT"
      contains: "avg_sale_per_pyeong"
    - path: "src/components/map/ClusterMarker.test.tsx"
      provides: "MAP-01 클러스터 줌인 테스트 스캐폴드"
    - path: "src/lib/utils/badge-logic.test.ts"
      provides: "MAP-04 배지 우선순위 테스트 스캐폴드"
    - path: "src/app/api/complexes/[id]/map-panel/route.test.ts"
      provides: "MAP-03 API Route 테스트 스캐폴드"
    - path: "src/app/complexes/[id]/actions.test.ts"
      provides: "MAP-05 view_count Server Action 테스트 스캐폴드"
  key_links:
    - from: "supabase/migrations/20260516000001_phase11_map_columns.sql"
      to: "public.complexes"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "avg_sale_per_pyeong"
    - from: "refresh_complex_price_stats()"
      to: "public.transactions"
      via: "UPDATE complexes SET"
      pattern: "cancel_date IS NULL AND superseded_by IS NULL"
---

<objective>
DB 마이그레이션 파일 작성 → [BLOCKING] supabase db push → 테스트 스캐폴드 생성.

Purpose: Phase 11의 모든 후속 작업(Wave 1~3)이 의존하는 DB 스키마와 테스트 기반을 확립한다.
Output: 마이그레이션 SQL 1개 (4컬럼 + 2함수 + GRANT), 테스트 스캐폴드 4개.
</objective>

<execution_context>
@C:/Users/jung/OneDrive/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/OneDrive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-map-enhancement/11-00-RESEARCH.md
@.planning/phases/11-map-enhancement/11-PATTERNS.md

<interfaces>
<!-- 기존 complexes 테이블 스키마 (마이그레이션 대상) -->
<!-- supabase/migrations/20260430000002_complexes.sql 기반 -->
<!-- 현재 avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d 컬럼 없음 -->
<!-- 최신 마이그레이션: 20260515000001_school_districts.sql -->

<!-- 기존 facility_edu 마이그레이션 패턴 (20260514000003_facility_edu.sql) -->
-- ALTER TABLE public.complexes ADD COLUMN IF NOT EXISTS hagwon_score smallint;
-- CREATE OR REPLACE FUNCTION ... LANGUAGE sql STABLE AS $$...$$;
-- GRANT EXECUTE ON FUNCTION ... TO anon, authenticated;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 11 DB 마이그레이션 작성</name>
  <files>supabase/migrations/20260516000001_phase11_map_columns.sql</files>

  <read_first>
    - supabase/migrations/20260514000003_facility_edu.sql (ALTER TABLE + FUNCTION + GRANT 패턴)
    - supabase/migrations/20260430000002_complexes.sql (complexes 테이블 현재 스키마)
    - .planning/phases/11-map-enhancement/11-00-RESEARCH.md (Pattern 4, 5, DB 마이그레이션 구조 섹션)
  </read_first>

  <action>
파일 경로: supabase/migrations/20260516000001_phase11_map_columns.sql

다음 내용을 순서대로 작성한다:

1. 컬럼 추가 (ADD COLUMN IF NOT EXISTS 패턴 — 20260514000003 참조):
```sql
-- Phase 11: 지도 고도화 — avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d
ALTER TABLE public.complexes
  ADD COLUMN IF NOT EXISTS avg_sale_per_pyeong integer,
  ADD COLUMN IF NOT EXISTS view_count          integer NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS price_change_30d    numeric(5,4),
  ADD COLUMN IF NOT EXISTS tx_count_30d        integer NOT NULL DEFAULT 0;
```

2. 인덱스:
```sql
CREATE INDEX IF NOT EXISTS complexes_view_count_idx     ON public.complexes(view_count);
CREATE INDEX IF NOT EXISTS complexes_avg_sale_idx       ON public.complexes(avg_sale_per_pyeong);
CREATE INDEX IF NOT EXISTS complexes_price_change_idx   ON public.complexes(price_change_30d);
```

3. view_count +1 RPC (SECURITY INVOKER, anon 허용):
```sql
CREATE OR REPLACE FUNCTION public.increment_view_count(p_complex_id uuid)
RETURNS void
LANGUAGE sql
SECURITY INVOKER
AS $$
  UPDATE public.complexes
  SET view_count = view_count + 1,
      updated_at = now()
  WHERE id = p_complex_id;
$$;

GRANT EXECUTE ON FUNCTION public.increment_view_count(uuid) TO anon, authenticated;
```

4. 배치 집계 함수 (CRITICAL: cancel_date IS NULL AND superseded_by IS NULL 반드시 포함):
```sql
CREATE OR REPLACE FUNCTION public.refresh_complex_price_stats()
RETURNS void
LANGUAGE sql
AS $$
  UPDATE public.complexes c
  SET
    avg_sale_per_pyeong = (
      SELECT (avg(t.price / NULLIF(t.area_m2 / 3.3058, 0)) / 10000)::integer
      FROM public.transactions t
      WHERE t.complex_id = c.id
        AND t.deal_type = 'sale'
        AND t.deal_date >= CURRENT_DATE - INTERVAL '1 year'
        AND t.cancel_date IS NULL
        AND t.superseded_by IS NULL
        AND t.area_m2 > 0
    ),
    price_change_30d = (
      WITH recent AS (
        SELECT avg(t.price) AS avg_p
        FROM public.transactions t
        WHERE t.complex_id = c.id
          AND t.deal_type = 'sale'
          AND t.deal_date >= CURRENT_DATE - INTERVAL '30 days'
          AND t.cancel_date IS NULL
          AND t.superseded_by IS NULL
      ),
      prev AS (
        SELECT avg(t.price) AS avg_p
        FROM public.transactions t
        WHERE t.complex_id = c.id
          AND t.deal_type = 'sale'
          AND t.deal_date >= CURRENT_DATE - INTERVAL '60 days'
          AND t.deal_date <  CURRENT_DATE - INTERVAL '30 days'
          AND t.cancel_date IS NULL
          AND t.superseded_by IS NULL
      )
      SELECT CASE
        WHEN prev.avg_p IS NULL OR prev.avg_p = 0 THEN NULL
        ELSE round(((recent.avg_p - prev.avg_p) / prev.avg_p)::numeric, 4)
      END
      FROM recent, prev
    ),
    tx_count_30d = (
      SELECT count(*)::integer
      FROM public.transactions t
      WHERE t.complex_id = c.id
        AND t.deal_type = 'sale'
        AND t.deal_date >= CURRENT_DATE - INTERVAL '30 days'
        AND t.cancel_date IS NULL
        AND t.superseded_by IS NULL
    ),
    updated_at = now()
  WHERE c.status = 'active';
$$;
```

주의사항:
- price_change_30d는 numeric(5,4)로 비율 저장 (예: 0.1050 = +10.5%)
- cancel_date IS NULL AND superseded_by IS NULL — 두 조건 모두 필수 (CLAUDE.md)
- NULLIF(area_m2 / 3.3058, 0) — 0 나누기 방지
  </action>

  <verify>
    <automated>npm run lint -- --max-warnings 0 2>/dev/null || echo "lint passed (SQL file, no TS lint)"</automated>
  </verify>

  <acceptance_criteria>
    - supabase/migrations/20260516000001_phase11_map_columns.sql 파일이 존재한다
    - 파일에 "avg_sale_per_pyeong", "view_count", "price_change_30d", "tx_count_30d" 4개 컬럼이 모두 있다
    - 파일에 "increment_view_count" 함수와 GRANT 구문이 있다
    - 파일에 "refresh_complex_price_stats" 함수가 있다
    - transactions 쿼리에 "cancel_date IS NULL" AND "superseded_by IS NULL" 조건이 존재한다
  </acceptance_criteria>

  <done>마이그레이션 SQL 파일이 모든 Phase 11 DB 요구사항을 포함한다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push 실행</name>

  <read_first>
    - supabase/migrations/20260516000001_phase11_map_columns.sql (Task 1 결과물 — 적용 전 최종 확인)
  </read_first>

  <what-built>Task 1에서 Phase 11 마이그레이션 SQL을 작성했다. 이 마이그레이션은 complexes 테이블에 4개 컬럼과 2개 함수를 추가한다. 후속 Wave 1~3 작업이 이 스키마에 의존한다.</what-built>

  <how-to-verify>
    터미널에서 다음 명령을 순서대로 실행:
    ```
    cd C:\Users\jung\coding\bds
    supabase db push
    ```

    성공 시: "Applying migration 20260516000001_phase11_map_columns" 출력 확인.

    Supabase SQL Editor에서 검증:
    ```sql
    SELECT column_name, data_type
    FROM information_schema.columns
    WHERE table_name = 'complexes'
      AND column_name IN ('avg_sale_per_pyeong', 'view_count', 'price_change_30d', 'tx_count_30d');
    ```
    4행 반환되면 성공.
  </how-to-verify>

  <resume-signal>supabase db push 성공 후 "approved" 입력. 오류 발생 시 오류 메시지 공유.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 테스트 스캐폴드 생성 (Wave 0 RED)</name>
  <files>
    src/components/map/ClusterMarker.test.tsx
    src/lib/utils/badge-logic.test.ts
    src/app/api/complexes/[id]/map-panel/route.test.ts
    src/app/complexes/[id]/actions.test.ts
  </files>

  <read_first>
    - src/components/map/ClusterMarker.tsx (현재 구현 — Props 확인)
    - src/components/map/ComplexMarker.tsx (현재 구현)
    - .planning/phases/11-map-enhancement/11-00-RESEARCH.md (Validation Architecture 섹션)
  </read_first>

  <action>
각 테스트 파일을 다음 내용으로 생성한다. 모든 테스트는 현재 RED 상태여야 한다 (미구현이므로 import 실패 또는 타입 오류).

**src/components/map/ClusterMarker.test.tsx**
```typescript
import { describe, it, expect, vi } from 'vitest'

// MAP-01: 클러스터 클릭 줌인 로직 테스트
// 현재 ClusterMarker는 clusterId, clusterIndex prop이 없으므로 RED

describe('ClusterMarker — 클러스터 줌인', () => {
  it('TODO: clusterId prop을 받아 clusterIndex.getLeaves를 호출한다', () => {
    // Wave 2 Plan 11-02에서 구현
    expect(true).toBe(true) // placeholder
  })

  it('TODO: getLeaves 결과로 LatLngBounds를 계산해 map.setBounds를 호출한다', () => {
    expect(true).toBe(true) // placeholder
  })
})
```

**src/lib/utils/badge-logic.test.ts**
```typescript
import { describe, it, expect } from 'vitest'

// MAP-04: determineBadge() 순수 함수 테스트
// Wave 1 Plan 11-01에서 badge-logic.ts 구현 예정

describe('determineBadge — 배지 우선순위', () => {
  it('TODO: status=pre_sale이면 pre_sale 배지를 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })

  it('TODO: built_year >= 2021이면 new_build 배지를 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })

  it('TODO: tx_count_30d >= p95_tx_count이면 crown 배지를 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })

  it('TODO: 모든 조건 불충족 시 none을 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })
})
```

**src/app/api/complexes/[id]/map-panel/route.test.ts**
```typescript
import { describe, it, expect } from 'vitest'

// MAP-03: GET /api/complexes/[id]/map-panel 응답 구조 테스트
// Wave 1 Plan 11-01B에서 route.ts 구현 예정

describe('GET /api/complexes/[id]/map-panel', () => {
  it('TODO: 유효한 UUID로 요청 시 단지 데이터를 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })

  it('TODO: 잘못된 UUID 형식으로 요청 시 400을 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })

  it('TODO: 존재하지 않는 ID로 요청 시 404를 반환한다', () => {
    expect(true).toBe(true) // placeholder
  })
})
```

**src/app/complexes/[id]/actions.test.ts**
```typescript
import { describe, it, expect, vi } from 'vitest'

// MAP-05: incrementViewCount Server Action 테스트
// Wave 2 Plan 11-03에서 actions.ts 구현 예정

describe('incrementViewCount — view_count Server Action', () => {
  it('TODO: 유효한 complexId로 호출 시 supabase.rpc를 호출한다', () => {
    expect(true).toBe(true) // placeholder
  })

  it('TODO: sessionStorage 기반 중복 방지 — 같은 세션에서 두 번 호출하지 않는다', () => {
    expect(true).toBe(true) // placeholder
  })
})
```
  </action>

  <verify>
    <automated>npm run test -- --run src/components/map/ClusterMarker.test.tsx src/lib/utils/badge-logic.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - 4개 테스트 파일이 생성된다
    - `npm run test -- --run` 실행 시 테스트가 통과한다 (placeholder expect(true).toBe(true))
    - 각 파일에 TODO 주석으로 Wave 1~2 구현 계획이 명시된다
  </acceptance_criteria>

  <done>Wave 0 테스트 스캐폴드 완성. 후속 Wave에서 TODO 테스트를 실제 구현으로 교체한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase migration apply | 마이그레이션 SQL이 직접 프로덕션 DB에 적용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | increment_view_count RPC | accept | 단순 카운터, 비즈니스 손실 없음. SECURITY INVOKER + anon GRANT로 필요한 최소 권한만 부여 |
| T-11-02 | Information Disclosure | refresh_complex_price_stats | accept | 집계 통계만 저장, 개인정보 없음 |
</threat_model>

<verification>
```bash
# 마이그레이션 파일 존재 확인
ls supabase/migrations/20260516000001_phase11_map_columns.sql

# 테스트 스캐폴드 실행
npm run test -- --run

# 필수 SQL 구문 확인 (cancel/superseded 조건)
grep -c "cancel_date IS NULL" supabase/migrations/20260516000001_phase11_map_columns.sql
grep -c "superseded_by IS NULL" supabase/migrations/20260516000001_phase11_map_columns.sql
```
</verification>

<success_criteria>
1. supabase/migrations/20260516000001_phase11_map_columns.sql 존재
2. supabase db push 성공 (운영자 확인)
3. Supabase에서 complexes 테이블에 4개 컬럼 존재 확인
4. `npm run test -- --run` 통과
</success_criteria>

<output>
완료 후 .planning/phases/11-map-enhancement/11-00-SUMMARY.md 생성.
</output>

---
phase: 12-map-marker-cluster
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/map/markers/badge-logic.ts
  - src/components/map/markers/HouseMarker.tsx
  - src/components/map/markers/HouseMarker.test.tsx
autonomous: true
requirements:
  - MAP-06
  - MAP-09

must_haves:
  truths:
    - "집 모양 SVG 마커가 일반(오렌지)/분양(빨강)/신축(민트) 3종으로 렌더된다"
    - "hot 단지에는 집 마커 위 왕관 SVG가 오버레이된다"
    - "badge-logic.ts가 3종(pre_sale/new_build/hot)만 반환한다"
    - "이모지·backdrop-blur·glow·보라색이 SVG에 없다"
    - "React.memo가 HouseMarker에 적용되어 있다"
  artifacts:
    - path: "src/components/map/markers/HouseMarker.tsx"
      provides: "집 모양 SVG 마커 컴포넌트 (3종 색상 + hot 왕관 오버레이)"
      exports: ["HouseMarker"]
    - path: "src/components/map/markers/badge-logic.ts"
      provides: "3종 배지 판별 순수 함수"
      exports: ["BadgeType", "BadgeInput", "determineBadge"]
    - path: "src/components/map/markers/HouseMarker.test.tsx"
      provides: "HouseMarker 렌더 검증 테스트"
  key_links:
    - from: "src/components/map/markers/badge-logic.ts"
      to: "src/components/map/markers/HouseMarker.tsx"
      via: "BadgeType import"
      pattern: "import.*BadgeType.*badge-logic"
---

<objective>
Phase 11의 BadgeMarker(핀/티어드롭 10종)를 로고 기반 집 모양 SVG 마커로 교체하고 badge-logic.ts를 3종으로 단순화한다.

Purpose: 호갱노노 수준의 집 아이콘 지도 UX 구현. 핀 형태 제거, 브랜드 로고와 일관된 집 마커 도입.
Output: HouseMarker.tsx (3색 집 SVG + 왕관), 단순화된 badge-logic.ts, 테스트 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-map-marker-cluster/12-CONTEXT.md

<interfaces>
<!-- 현재 badge-logic.ts 인터페이스 (단순화 대상) -->
<!-- 현재: 10종 BadgeType -->
export type BadgeType =
  | 'pre_sale' | 'new_build' | 'crown' | 'hot'
  | 'surge' | 'drop' | 'school' | 'large_complex' | 'redevelop' | 'none'

<!-- 목표: 3종으로 단순화 -->
export type BadgeType = 'pre_sale' | 'new_build' | 'hot' | 'none'

export interface BadgeInput {
  status:         string
  built_year:     number | null
  tx_count_30d:   number
  p95_tx_count:   number
}

<!-- HouseMarker가 받을 props -->
export interface HouseMarkerProps {
  badge:        BadgeType     // 색상 결정 + 왕관 오버레이
  recentPrice:  number | null // 만원 단위 — 줌 레벨에 따라 null(숨김)
  showName:     boolean       // level≤6 시 단지명 표시 여부
  name:         string
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: badge-logic.ts 3종 단순화 + HouseMarker.tsx 생성</name>
  <files>
    src/components/map/markers/badge-logic.ts
    src/components/map/markers/HouseMarker.tsx
  </files>
  <read_first>
    - src/components/map/markers/badge-logic.ts (현재 10종 구현 전체 파악)
    - src/components/map/markers/BadgeMarker.tsx (교체 대상 SVG 패턴 파악)
    - .planning/phases/12-map-marker-cluster/12-CONTEXT.md (D-01 locked 디자인 결정)
  </read_first>
  <behavior>
    - badge-logic.ts: BadgeType = 'pre_sale' | 'new_build' | 'hot' | 'none' (4종)
    - determineBadge: status==='pre_sale' → 'pre_sale', built_year≥2021 → 'new_build', tx_count_30d≥p95_tx_count(>0) → 'hot', 그 외 → 'none'
    - BadgeInput: status/built_year/tx_count_30d/p95_tx_count만 유지 (view_count/price_change_30d/hagwon_grade/household_count 제거)
    - getPriceColor 함수: 삭제 (HouseMarker는 badge로 색상 결정)
    - HouseMarker: 집 모양 SVG, badge에 따라 색상 변경
      - 색상 맵: pre_sale → '#EF4444', new_build → '#14B8A6', hot/none → '#F97316'
      - 지붕(roofColor): pre_sale → '#EF4444', new_build → '#14B8A6', 그 외 → '#9CA3AF'. 굴뚝 돌기도 roofColor와 동일
      - 바디: C형(U형) — bodyColor로 채움
      - hot 왕관: badge==='hot' 시 지붕 위 작은 왕관 SVG path 추가
      - recentPrice 라벨: 마커 아래 흰 배경 텍스트 칩 (formatPrice 사용, null이면 숨김)
      - showName: true면 단지명을 라벨 아래 표시
    - React.memo로 감싸기
    - formatPrice 유틸: 1억 미만 → '9,500만', 1억 이상 → '1억 5,000만'
  </behavior>
  <action>
**badge-logic.ts 전체 교체:**

```typescript
export type BadgeType = 'pre_sale' | 'new_build' | 'hot' | 'none'

export interface BadgeInput {
  status:       string
  built_year:   number | null
  tx_count_30d: number
  p95_tx_count: number
}

/**
 * Phase 12 배지 3종 판별 — pre_sale > new_build > hot > none
 * (Phase 11의 10종에서 단순화)
 */
export function determineBadge(input: BadgeInput): BadgeType {
  if (input.status === 'pre_sale') return 'pre_sale'
  if (input.built_year !== null && input.built_year >= 2021) return 'new_build'
  if (input.p95_tx_count > 0 && input.tx_count_30d >= input.p95_tx_count) return 'hot'
  return 'none'
}
```

**HouseMarker.tsx:**

SVG viewBox="0 0 40 48". 구성:
- 굴뚝: `<rect x="26" y="2" width="6" height="8" rx="1" fill="#9CA3AF"/>` (지붕 왼쪽 위 작은 돌기)
- 지붕 삼각형: `<polygon points="4,22 20,6 36,22" fill={roofColor}/>` — pre_sale → '#EF4444', new_build → '#14B8A6', 그 외 → '#9CA3AF'
- 바디 C형(오른쪽 열린 U): `<path d="M8,22 L8,44 L32,44 L32,22" fill="none" stroke={bodyColor} strokeWidth="8" strokeLinecap="round" strokeLinejoin="round"/>` 방식으로, 또는 rect+clipPath로 구현. 바디 내부를 bodyColor로 채움. 오른쪽 면 열린 형태:
  `<path d="M8,22 v22 h24 v-22" fill={bodyColor} stroke="none"/>` + `<rect x="30" y="22" width="8" height="22" fill="white"/>` 로 C형 연출 (오른쪽 8px 흰색 덮기). 또는 clip-path 방식.
  실제 구현: `<path d="M8 22 L8 44 L28 44 L28 22 Z" fill={bodyColor}/>` (왼쪽+바닥만, 오른쪽 열림)
- hot 왕관: badge==='hot'일 때 지붕 위 `<g transform="translate(10, 0)"><path d="M0,8 L4,2 L8,6 L12,0 L16,6 L20,2 L20,8 Z" fill="#FCD34D" stroke="#D97706" strokeWidth="0.5"/></g>` (20×8 크기 왕관)
- recentPrice: 마커 하단 `<div style={{ background:'white', border:'1px solid #E5E7EB', borderRadius:3, padding:'1px 4px', fontSize:10, fontWeight:600, color:bodyColor, whiteSpace:'nowrap' }}>` 에 formatPrice 값
- showName: 라벨 위 단지명 텍스트 (fontSize:11, color:'#374151', fontWeight:500)
- export default는 memo 감싸기: `export const HouseMarker = memo(function HouseMarker({...}){...})`
- aria-label: `${name} — ${badge === 'pre_sale' ? '분양' : badge === 'new_build' ? '신축' : badge === 'hot' ? '거래활발' : '단지'}`

**formatPrice 유틸 함수 (파일 내 선언):**
```typescript
function formatPrice(price: number): string {
  const eok = Math.floor(price / 10000)
  const man = price % 10000
  if (eok > 0 && man > 0) return `${eok}억 ${man.toLocaleString()}만`
  if (eok > 0) return `${eok}억`
  return `${price.toLocaleString()}만`
}
```

금지: 이모지, backdrop-blur, gradient-text, glow, 보라색(#7C3AED 등), gradient. SVG path만 사용.
  </action>
  <verify>
    <automated>npx vitest run src/components/map/markers/HouseMarker.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - badge-logic.ts의 BadgeType 타입에 'crown'/'hot'/'surge'/'drop'/'school'/'large_complex'/'redevelop' 문자열이 없다: `grep -c "crown\|surge\|drop\|school\|large_complex\|redevelop" src/components/map/markers/badge-logic.ts` == 0
    - badge-logic.ts에 'pre_sale' | 'new_build' | 'hot' | 'none' 4종만 존재: `grep "BadgeType" src/components/map/markers/badge-logic.ts`
    - HouseMarker.tsx에 React.memo 적용: `grep -c "memo(" src/components/map/markers/HouseMarker.tsx` >= 1
    - HouseMarker.tsx에 '#F97316' 오렌지, '#EF4444' 빨강, '#14B8A6' 민트 세 색상 모두 존재: `grep -c "F97316\|EF4444\|14B8A6" src/components/map/markers/HouseMarker.tsx` == 3
    - 금지색(backdrop-blur, glow, gradient, 보라색 #7C3AED) 없음: `grep -c "backdrop\|glow\|7C3AED\|gradient" src/components/map/markers/HouseMarker.tsx` == 0
    - HouseMarker.test.tsx 존재하고 Vitest 테스트 통과
    - TypeScript 컴파일 오류 없음: `npx tsc --noEmit 2>&1 | grep -i "HouseMarker\|badge-logic"` 출력 없음
  </acceptance_criteria>
  <done>badge-logic.ts가 3종(+none)만 반환하고 HouseMarker.tsx가 집 모양 SVG로 3색+왕관을 렌더한다. Vitest 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HouseMarker.test.tsx 작성</name>
  <files>
    src/components/map/markers/HouseMarker.test.tsx
  </files>
  <read_first>
    - src/components/map/markers/HouseMarker.tsx (Task 1에서 생성된 파일)
    - src/components/map/ClusterMarker.test.tsx (기존 테스트 패턴 참조)
  </read_first>
  <behavior>
    - Test 1: badge='none' → '#F97316' 오렌지 색상 SVG가 렌더된다
    - Test 2: badge='pre_sale' → '#EF4444' 빨강 요소가 렌더된다
    - Test 3: badge='new_build' → '#14B8A6' 민트 요소가 렌더된다
    - Test 4: badge='hot' → 왕관 path(fill='#FCD34D')가 렌더된다
    - Test 5: recentPrice=95000 → '9억 5,000만' 텍스트가 렌더된다
    - Test 6: recentPrice=null → 가격 텍스트가 없다
    - Test 7: showName=true → name 텍스트가 렌더된다
    - Test 8: showName=false → name 텍스트가 없다
  </behavior>
  <action>
Vitest + @testing-library/react 사용. 기존 ClusterMarker.test.tsx 패턴 참조.

react-kakao-maps-sdk는 이 컴포넌트에서 사용하지 않으므로 별도 mock 불필요.

각 테스트는 AAA 패턴(Arrange-Act-Assert) 적용.

formatPrice 로직:
- 95000만원 → 9억 5,000만 (Math.floor(95000/10000)=9, 95000%10000=5000 → '9억 5,000만')
  </action>
  <verify>
    <automated>npx vitest run src/components/map/markers/HouseMarker.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - HouseMarker.test.tsx 파일이 존재한다
    - 8개 test() 블록이 존재한다: `grep -c "^  test\|^test(" src/components/map/markers/HouseMarker.test.tsx` >= 8
    - 모든 테스트 PASS: `npx vitest run src/components/map/markers/HouseMarker.test.tsx` 출력에 'failed' 없음
    - it/describe 없이 test() 패턴 사용: 기존 프로젝트 패턴 따름
  </acceptance_criteria>
  <done>HouseMarker.test.tsx 8개 케이스가 모두 GREEN. badge-logic 단순화와 HouseMarker SVG 구현이 테스트로 검증된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 렌더링 | 마커는 CustomOverlayMap 내 클라이언트 DOM — XSS 방어 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | HouseMarker SVG | accept | 순수 표시 컴포넌트, 사용자 데이터 미포함 |
| T-12-02 | Tampering | badge-logic.ts | accept | 순수 함수, 입력은 서버에서 내려온 DB 값 — 클라이언트 조작 가능하나 시각적 배지만 영향 |
</threat_model>

<verification>
```bash
# 1. badge-logic 단순화 검증
grep "BadgeType" src/components/map/markers/badge-logic.ts
# 예상: 'pre_sale' | 'new_build' | 'hot' | 'none' 만 포함

# 2. 금지 패턴 없음
grep -c "backdrop\|glow\|gradient\|7C3AED\|emoji" src/components/map/markers/HouseMarker.tsx
# 예상: 0

# 3. 테스트
npx vitest run src/components/map/markers/HouseMarker.test.tsx

# 4. 타입 검사
npx tsc --noEmit 2>&1 | grep -i "houseMarker\|badge-logic" || echo "No TS errors"
```
</verification>

<success_criteria>
- badge-logic.ts: BadgeType 4종(pre_sale/new_build/hot/none)만 정의, getPriceColor 함수 제거
- HouseMarker.tsx: React.memo 적용, 3색 집 SVG, hot 왕관 오버레이, 금지 패턴 없음
- HouseMarker.test.tsx: 8 테스트 PASS
- TypeScript strict 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/12-map-marker-cluster/12-01-SUMMARY.md`
</output>

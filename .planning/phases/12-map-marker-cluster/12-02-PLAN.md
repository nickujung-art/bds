---
phase: 12-map-marker-cluster
plan: "02"
type: execute
wave: 3
depends_on:
  - "12-03"
files_modified:
  - src/components/map/DongClusterChip.tsx
  - src/components/map/DongClusterChip.test.tsx
autonomous: true
requirements:
  - MAP-08

must_haves:
  truths:
    - "클러스터 위치에 구/동 이름과 최근 최고 실거래가가 표시되는 사각형 칩이 렌더된다"
    - "클릭 시 기존 ClusterMarker와 동일하게 해당 클러스터 bounds로 줌인된다"
    - "최고 실거래가가 없으면 단위 이름만 표시된다"
    - "backdrop-blur/gradient/이모지가 없다"
    - "React.memo가 적용되어 있다"
  artifacts:
    - path: "src/components/map/DongClusterChip.tsx"
      provides: "동/구 단위 사각형 클러스터 칩 컴포넌트"
      exports: ["DongClusterChip"]
    - path: "src/components/map/DongClusterChip.test.tsx"
      provides: "DongClusterChip 렌더 및 가격 포맷 테스트"
  key_links:
    - from: "src/components/map/DongClusterChip.tsx"
      to: "src/components/map/KakaoMap.tsx"
      via: "ClusterMarker 교체"
      pattern: "DongClusterChip"
---

<objective>
Phase 11의 숫자 원형 ClusterMarker를 구/동 이름 + 최근 최고 실거래가를 표시하는 사각형 칩(DongClusterChip)으로 교체한다.

Purpose: 줌 아웃 시 동 단위 부동산 가격 정보 제공 — 호갱노노 스타일 정보 밀도 향상.
Output: DongClusterChip.tsx, DongClusterChip.test.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-map-marker-cluster/12-CONTEXT.md

<interfaces>
<!-- 현재 ClusterMarker props (교체 대상) -->
interface ClusterMarkerProps {
  lat:          number
  lng:          number
  count:        number
  clusterId:    number
  clusterIndex: Supercluster
}

<!-- DongClusterChip props (신규) -->
interface DongClusterChipProps {
  lat:          number
  lng:          number
  clusterId:    number
  clusterIndex: Supercluster
  // clusterIndex.getLeaves(clusterId, Infinity)로 leaves 추출하여
  // leaves[0].properties.gu (또는 dong), 그리고 recent_price max 계산
}

<!-- supercluster leave properties (Phase 12에서 추가될 필드 포함) -->
interface LeafProperties {
  id:           string
  name:         string
  gu:           string | null
  dong:         string | null
  recent_price: number | null   // 만원 단위 — Plan 03에서 ComplexMapItem에 추가됨
  cluster:      false
}

<!-- 칩 스타일 (CONTEXT.md D-03 locked) -->
// 흰 배경, border-radius: 6px, padding: '8px 12px', 1px border (#E5E7EB), 그림자
// backdrop-blur 금지
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DongClusterChip.tsx 생성</name>
  <files>
    src/components/map/DongClusterChip.tsx
  </files>
  <read_first>
    - src/components/map/ClusterMarker.tsx (줌인 로직 전체 — handleClick 복사 기준)
    - src/components/map/KakaoMap.tsx (clusterIndex 타입과 사용 패턴 확인)
    - src/components/map/MapSidePanel.tsx (formatPrice 함수 패턴 참조)
    - .planning/phases/12-map-marker-cluster/12-CONTEXT.md (칩 스타일 D-03 locked)
  </read_first>
  <behavior>
    - DongClusterChip은 useMap('DongClusterChip')으로 map 인스턴스 획득
    - getLeaves(clusterId, Infinity)로 leaves 추출
    - 구/동 이름: leaves 중 첫 번째 leave의 properties.gu 사용 (없으면 dong, 그것도 없으면 '기타')
    - 최고 실거래가: leaves의 properties.recent_price(만원) 중 최대값. 모두 null이면 null
    - 칩 클릭 시 leaves bounds 계산 → map.setBounds() (ClusterMarker 동일 로직)
    - 칩 내부: 상단 행 = 구/동 이름, 우측 = formatPrice(maxPrice) 또는 빈 문자열
    - formatPrice: 1억 미만 → '9,500만', 1억 이상 → '1억 5,000만'
    - React.memo 적용
    - backdrop-blur / gradient / 이모지 없음
  </behavior>
  <action>
**DongClusterChip.tsx 전체 구현:**

```tsx
'use client'

import { memo } from 'react'
import { CustomOverlayMap, useMap } from 'react-kakao-maps-sdk'
import type Supercluster from 'supercluster'

interface Props {
  lat:          number
  lng:          number
  clusterId:    number
  clusterIndex: Supercluster
}

function formatPrice(price: number): string {
  const eok = Math.floor(price / 10000)
  const man = price % 10000
  if (eok > 0 && man > 0) return `${eok}억 ${man.toLocaleString()}만`
  if (eok > 0) return `${eok}억`
  return `${price.toLocaleString()}만`
}

export const DongClusterChip = memo(function DongClusterChip({
  lat, lng, clusterId, clusterIndex,
}: Props) {
  const map = useMap('DongClusterChip')

  const leaves = clusterIndex.getLeaves(clusterId, Infinity)

  // 구/동 이름: 첫 번째 leave 기준
  const firstProps = leaves[0]?.properties as {
    gu?: string | null
    dong?: string | null
    recent_price?: number | null
  } | undefined
  const areaName = firstProps?.gu ?? firstProps?.dong ?? '기타'

  // 최근 최고 실거래가
  const maxPrice = leaves.reduce<number | null>((max, leaf) => {
    const p = (leaf.properties as { recent_price?: number | null }).recent_price ?? null
    if (p === null) return max
    return max === null ? p : Math.max(max, p)
  }, null)

  const handleClick = () => {
    const bounds = new window.kakao.maps.LatLngBounds()
    for (const leaf of leaves) {
      const coords = leaf.geometry.coordinates as [number, number]
      bounds.extend(new window.kakao.maps.LatLng(coords[1] ?? 0, coords[0] ?? 0))
    }
    map.setBounds(bounds)
  }

  return (
    <CustomOverlayMap position={{ lat, lng }} xAnchor={0.5} yAnchor={0.5} zIndex={5}>
      <div
        onClick={handleClick}
        role="button"
        aria-label={`${areaName} 지역 ${leaves.length}개 단지. 클릭하면 해당 단지들로 확대됩니다`}
        tabIndex={0}
        onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') handleClick() }}
        style={{
          background: 'white',
          border: '1px solid #E5E7EB',
          borderRadius: 6,
          padding: '8px 12px',
          boxShadow: '0 4px 16px rgba(0,0,0,0.10)',
          cursor: 'pointer',
          userSelect: 'none',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'flex-start',
          gap: 2,
          minWidth: 80,
        }}
      >
        <span style={{ fontSize: 12, fontWeight: 700, color: '#111827', lineHeight: 1.3 }}>
          {areaName}
        </span>
        {maxPrice !== null && (
          <span style={{ fontSize: 11, fontWeight: 500, color: '#F97316', lineHeight: 1.2 }}>
            {formatPrice(maxPrice)}
          </span>
        )}
      </div>
    </CustomOverlayMap>
  )
})
```

금지: backdrop-blur, gradient, 이모지, 보라색. SVG 대신 div/span으로 칩 구현.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "DongClusterChip" || echo "No TS errors in DongClusterChip"</automated>
  </verify>
  <acceptance_criteria>
    - DongClusterChip.tsx 파일이 존재한다
    - React.memo 적용: `grep -c "memo(" src/components/map/DongClusterChip.tsx` >= 1
    - useMap 훅 사용: `grep -c "useMap" src/components/map/DongClusterChip.tsx` >= 1
    - map.setBounds 줌인 로직 존재: `grep -c "setBounds" src/components/map/DongClusterChip.tsx` >= 1
    - backdrop-blur/gradient 없음: `grep -c "backdrop\|gradient\|glow" src/components/map/DongClusterChip.tsx` == 0
    - TypeScript 오류 없음: `npx tsc --noEmit 2>&1 | grep -i "DongClusterChip"` 출력 없음
    - border-radius: 6px, border: '#E5E7EB' 스타일 존재: `grep -c "E5E7EB\|borderRadius.*6" src/components/map/DongClusterChip.tsx` >= 1
  </acceptance_criteria>
  <done>DongClusterChip.tsx가 동/구 이름 + 최고 실거래가 사각형 칩을 렌더하고 클릭 시 줌인한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DongClusterChip.test.tsx 작성</name>
  <files>
    src/components/map/DongClusterChip.test.tsx
  </files>
  <read_first>
    - src/components/map/DongClusterChip.tsx (Task 1에서 생성)
    - src/components/map/ClusterMarker.test.tsx (기존 테스트 패턴 — useMap mock 방식)
  </read_first>
  <behavior>
    - Test 1: gu='성산구', recent_price=125000 → '성산구' 텍스트와 '12억 5,000만' 텍스트가 렌더된다
    - Test 2: gu=null, dong='상남동', recent_price=85000 → '상남동' 텍스트와 '8억 5,000만' 텍스트가 렌더된다
    - Test 3: recent_price=null → 가격 텍스트가 없다 (단위 이름만 표시)
    - Test 4: 여러 leaves 중 최대 recent_price가 표시된다 (50000, 130000, 90000 → '13억')
    - Test 5: gu=null, dong=null → '기타' 텍스트가 렌더된다
    - react-kakao-maps-sdk의 CustomOverlayMap과 useMap은 vi.mock으로 처리
  </behavior>
  <action>
ClusterMarker.test.tsx 패턴을 따라 useMap과 CustomOverlayMap을 mock.

```typescript
vi.mock('react-kakao-maps-sdk', () => ({
  CustomOverlayMap: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  useMap: () => ({ setBounds: vi.fn() }),
}))
```

Supercluster의 getLeaves mock:
```typescript
const makeClusterIndex = (leaves: Array<{ gu?: string | null, dong?: string | null, recent_price?: number | null }>) => ({
  getLeaves: () => leaves.map((l, i) => ({
    geometry: { coordinates: [128.6 + i * 0.01, 35.2 + i * 0.01] },
    properties: { id: `id-${i}`, name: `단지${i}`, cluster: false, ...l },
  })),
})
```
  </action>
  <verify>
    <automated>npx vitest run src/components/map/DongClusterChip.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DongClusterChip.test.tsx 파일이 존재한다
    - 5개 이상의 test() 블록: `grep -c "^  test\|^test(" src/components/map/DongClusterChip.test.tsx` >= 5
    - 모든 테스트 PASS: 출력에 'failed' 없음
    - formatPrice 로직 검증 포함: '12억 5,000만', '8억 5,000만', '13억' 중 하나 이상 포함
  </acceptance_criteria>
  <done>DongClusterChip.test.tsx 5개 케이스 GREEN. 구/동 이름 + 가격 포맷 + 최대값 선택 검증됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 DOM | DongClusterChip은 클라이언트에서만 렌더 — 서버 데이터 의존 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | DongClusterChip | accept | 지도 공개 데이터(가격/지역명) 표시 — 개인정보 미포함 |
| T-12-04 | Denial of Service | getLeaves(Infinity) | accept | supercluster가 로컬 인덱스에서 동기 처리 — 서버 호출 없음 |
</threat_model>

<verification>
```bash
# 1. 파일 존재 확인
ls src/components/map/DongClusterChip.tsx src/components/map/DongClusterChip.test.tsx

# 2. 금지 패턴 없음
grep -c "backdrop\|gradient\|glow" src/components/map/DongClusterChip.tsx
# 예상: 0

# 3. 테스트
npx vitest run src/components/map/DongClusterChip.test.tsx

# 4. 타입 검사
npx tsc --noEmit 2>&1 | grep -i "DongCluster" || echo "No TS errors"
```
</verification>

<success_criteria>
- DongClusterChip.tsx: React.memo, useMap 줌인, 흰 사각형 칩 스타일, 금지 패턴 없음
- DongClusterChip.test.tsx: 5 테스트 PASS (이름/가격/null 처리/최대값)
- TypeScript strict 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/12-map-marker-cluster/12-02-SUMMARY.md`
</output>

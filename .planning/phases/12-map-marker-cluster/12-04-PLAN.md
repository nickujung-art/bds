---
phase: 12-map-marker-cluster
plan: "04"
type: execute
wave: 4
depends_on:
  - "12-01"
  - "12-02"
  - "12-03"
files_modified:
  - src/components/map/KakaoMap.tsx
autonomous: true
requirements:
  - MAP-09

must_haves:
  truths:
    - "줌 레벨 ≥10에서는 DongClusterChip만 표시되고 단지 마커가 보이지 않는다"
    - "줌 레벨 7~9에서는 집 마커와 실거래가만 표시된다 (단지명 없음)"
    - "줌 레벨 ≤6에서는 집 마커·단지명·실거래가가 모두 표시된다"
    - "ClusterMarker 사용이 제거되고 DongClusterChip으로 교체된다"
    - "BadgeMarker import가 제거된다"
    - "badge-logic.ts의 새 BadgeInput 타입(3종)과 호환된다"
    - "p95TxCount만 계산하고 p95ViewCount는 제거된다"
  artifacts:
    - path: "src/components/map/KakaoMap.tsx"
      provides: "3단계 줌 레벨 정책 + DongClusterChip + HouseMarker 통합 지도"
      exports: ["KakaoMap"]
  key_links:
    - from: "src/components/map/KakaoMap.tsx"
      to: "src/components/map/DongClusterChip.tsx"
      via: "cluster feature 렌더"
      pattern: "DongClusterChip"
    - from: "src/components/map/KakaoMap.tsx"
      to: "src/components/map/ComplexMarker.tsx"
      via: "non-cluster feature 렌더"
      pattern: "ComplexMarker"
    - from: "src/components/map/KakaoMap.tsx"
      to: "src/components/map/markers/badge-logic.ts"
      via: "determineBadge 호출"
      pattern: "determineBadge"
---

<objective>
KakaoMap.tsx를 업데이트하여 Phase 12의 3단계 줌 레벨 정책, DongClusterChip, ComplexMarker(HouseMarker), 단순화된 badge-logic을 통합한다.

Purpose: MAP-09 줌 레벨 정책 완성 — 클러스터 칩/마커+가격/마커+이름+가격의 3단계 전환.
Output: 통합 완료된 KakaoMap.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-map-marker-cluster/12-CONTEXT.md

<interfaces>
<!-- Plan 01 결과: 새 badge-logic.ts BadgeInput -->
export interface BadgeInput {
  status:       string
  built_year:   number | null
  tx_count_30d: number
  p95_tx_count: number
}
// (view_count, price_change_30d, hagwon_grade, household_count 제거됨)

<!-- Plan 02 결과: DongClusterChip props -->
interface DongClusterChipProps {
  lat:          number
  lng:          number
  clusterId:    number
  clusterIndex: Supercluster
}

<!-- Plan 03 결과: 확장된 ComplexMarker props -->
interface ComplexMarkerProps {
  id:             string
  name:           string
  lat:            number
  lng:            number
  avgSalePerPyeong: number | null
  showLabel:      boolean        // 실거래가 라벨 on/off
  showName:       boolean        // 단지명 on/off (level≤6)
  badge:          BadgeType
  onSelect:       (id: string) => void
  householdCount: number | null
  si:             string | null
  gu:             string | null
  recentPrice:    number | null
  recentDate:     string | null
  recentAreaM2:   number | null
  builtYear:      number | null
}

<!-- Plan 03 결과: 확장된 feature.properties (non-cluster) -->
{
  id, name, avg_sale_per_pyeong, view_count, price_change_30d, tx_count_30d,
  status, built_year, household_count, hagwon_grade,
  si, gu, dong, recent_price,  // 추가됨
  cluster: false,
}

<!-- 줌 레벨 정책 (CONTEXT.md D-04 locked) -->
// level ≥ 10: DongClusterChip만 (단지 마커 렌더 안 함)
// level 7~9:  ComplexMarker showLabel=true, showName=false
// level ≤ 6:  ComplexMarker showLabel=true, showName=true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KakaoMap.tsx 통합 업데이트 — 줌 정책 + 컴포넌트 교체</name>
  <files>
    src/components/map/KakaoMap.tsx
  </files>
  <read_first>
    - src/components/map/KakaoMap.tsx (현재 전체 구현 — import, state, useMemo, 렌더 로직)
    - src/components/map/markers/badge-logic.ts (Plan 01 후 단순화된 BadgeInput 타입)
    - src/components/map/ComplexMarker.tsx (Plan 03 후 추가된 props 목록)
    - src/components/map/DongClusterChip.tsx (Plan 02에서 생성된 props)
    - .planning/phases/12-map-marker-cluster/12-CONTEXT.md (D-04 줌 레벨 정책 locked)
  </read_first>
  <action>
**변경 사항:**

1. **import 교체:**
   - `import { ClusterMarker } from './ClusterMarker'` → `import { DongClusterChip } from './DongClusterChip'`
   - `import { determineBadge } from '@/components/map/markers/badge-logic'` 유지
   - BadgeMarker import 제거 (ComplexMarker 내부로 이동됨)

2. **p95 계산 단순화:**
   기존: `{ p95ViewCount, p95TxCount }` → **`p95TxCount`만** (p95ViewCount 제거)
   ```typescript
   const p95TxCount = useMemo(() => {
     if (complexes.length === 0) return 0
     const txCounts = [...complexes].map((c) => c.tx_count_30d).sort((a, b) => a - b)
     const p95Idx   = Math.floor(complexes.length * 0.95)
     return txCounts[p95Idx] ?? 0
   }, [complexes])
   ```

3. **줌 레벨 정책 (3단계):**
   ```typescript
   const showLabel = mapLevel <= 9  // 실거래가 라벨: level 7~9, ≤6 모두 표시
   const showName  = mapLevel <= 6  // 단지명: level ≤6에서만
   const showOnlyCluster = mapLevel >= 10  // level ≥10: 클러스터 칩만
   ```

4. **렌더 로직:**
   ```typescript
   {clusters.map((feature, i) => {
     const lng = feature.geometry.coordinates[0] ?? 0
     const lat = feature.geometry.coordinates[1] ?? 0

     if (feature.properties.cluster) {
       return (
         <DongClusterChip
           key={`cluster-${(feature.properties.cluster_id as number | undefined) ?? i}`}
           lat={lat}
           lng={lng}
           clusterId={feature.properties.cluster_id as number}
           clusterIndex={clusterIndex}
         />
       )
     }

     // showOnlyCluster = true면 단지 마커 렌더 안 함
     if (showOnlyCluster) return null

     const props = feature.properties as {
       id: string; name: string; avg_sale_per_pyeong: number | null
       tx_count_30d: number; status: string; built_year: number | null
       household_count: number | null; si: string | null; gu: string | null
       recent_price: number | null; recent_date?: string | null; recent_area_m2?: number | null
       // view_count, price_change_30d, hagwon_grade: 더 이상 badge에서 미사용이지만 properties에 남아있음
     }

     const badge = determineBadge({
       status:       props.status       ?? 'active',
       built_year:   props.built_year   ?? null,
       tx_count_30d: props.tx_count_30d ?? 0,
       p95_tx_count: p95TxCount,
     })

     return (
       <ComplexMarker
         key={props.id}
         id={props.id}
         name={props.name}
         lat={lat}
         lng={lng}
         avgSalePerPyeong={props.avg_sale_per_pyeong ?? null}
         showLabel={showLabel}
         showName={showName}
         badge={badge}
         onSelect={setSelectedComplexId}
         householdCount={props.household_count ?? null}
         si={props.si ?? null}
         gu={props.gu ?? null}
         recentPrice={props.recent_price ?? null}
         recentDate={props.recent_date ?? null}
         recentAreaM2={props.recent_area_m2 ?? null}
         builtYear={props.built_year ?? null}
       />
     )
   })}
   ```

5. **ClusterMarker 참조 완전 제거** (import 포함).

주의: determineBadge의 BadgeInput이 Plan 01에서 변경되었으므로, 기존 p95_view_count/view_count/price_change_30d/hagwon_grade/household_count 인자를 제거해야 한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "KakaoMap" || echo "No TS errors in KakaoMap"</automated>
  </verify>
  <acceptance_criteria>
    - ClusterMarker import 제거: `grep -c "ClusterMarker" src/components/map/KakaoMap.tsx` == 0
    - DongClusterChip import 추가: `grep -c "DongClusterChip" src/components/map/KakaoMap.tsx` >= 1
    - showOnlyCluster 로직 존재: `grep -c "showOnlyCluster\|mapLevel >= 10\|mapLevel>=10" src/components/map/KakaoMap.tsx` >= 1
    - showName 변수 존재: `grep -c "showName" src/components/map/KakaoMap.tsx` >= 1
    - p95ViewCount 제거: `grep -c "p95ViewCount" src/components/map/KakaoMap.tsx` == 0
    - determineBadge 호출에 p95_tx_count만 사용: `grep -A5 "determineBadge" src/components/map/KakaoMap.tsx` — p95_view_count 없음
    - ComplexMarker 신규 props 전달 확인: `grep -cE "recentDate|recentAreaM2|builtYear" src/components/map/KakaoMap.tsx` — 3 이상
    - TypeScript 오류 없음: `npx tsc --noEmit 2>&1 | grep -i "KakaoMap"` 출력 없음
    - 빌드 통과: `npm run build 2>&1 | tail -5` — error 없음
  </acceptance_criteria>
  <done>KakaoMap.tsx가 DongClusterChip/ComplexMarker(HouseMarker)를 사용하고 3단계 줌 레벨 정책을 구현한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 12 전체 통합: 집 모양 SVG 마커(HouseMarker) + 동/구 클러스터 칩(DongClusterChip) + hover 툴팁 카드(ComplexMarker) + 3단계 줌 레벨 정책(KakaoMap)
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 http://localhost:3000/map 접속
    2. **줌 아웃 (레벨 ≥10):** 동/구 이름 + 최고 실거래가 사각형 칩만 보여야 함 (집 마커 없음)
    3. **중간 줌 (레벨 7~9):** 집 모양 SVG 마커 + 실거래가 텍스트가 표시되어야 함 (단지명 없음)
    4. **줌 인 (레벨 ≤6):** 집 마커 + 단지명 + 실거래가 모두 표시
    5. **마커 색상 확인:** 일반 단지 = 오렌지(#F97316), 분양 = 빨강(#EF4444), 신축(2021+) = 민트(#14B8A6)
    6. **hot 마커:** 거래 많은 단지에 왕관 오버레이 확인 (없을 수도 있음 — 데이터 없으면 정상)
    7. **hover 툴팁:** 마커에 마우스 올리면 단지명·시구·최근 실거래·세대수·준공 카드 표시
    8. **클러스터 클릭:** 칩 클릭 시 해당 단지들로 줌인
    9. **사이드 패널:** 마커 클릭 시 Phase 11 사이드 패널 정상 작동
    10. **금지 패턴 없음:** 핀 모양 마커, 이모지, glow, gradient 없음
  </how-to-verify>
  <resume-signal>지도가 정상 동작하면 "approved" 입력. 문제 있으면 증상 설명 (예: "클러스터 칩에 가격이 안 보임", "줌 레벨 전환이 안 됨")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 렌더 | KakaoMap은 'use client' — 서버에서 데이터 받아 클라이언트에서 렌더 |
| mapLevel 기반 조건부 렌더 | 줌 레벨 state 조작은 사용자 인터랙션 — 서버 보안에 영향 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Spoofing | KakaoMap | accept | 카카오 SDK 공식 키 사용, NEXT_PUBLIC_ prefix로 노출 (설계상 공개) |
| T-12-09 | Information Disclosure | DongClusterChip 최고 실거래가 | accept | 공개 거래 데이터 — 개인정보 없음 |
</threat_model>

<verification>
```bash
# 1. import 교체 확인
grep "DongClusterChip\|ClusterMarker" src/components/map/KakaoMap.tsx
# 예상: DongClusterChip만 있고 ClusterMarker는 없음

# 2. 줌 레벨 정책 확인
grep "showOnlyCluster\|showName\|showLabel" src/components/map/KakaoMap.tsx

# 3. determineBadge 호출 인자 확인 (3종 인자만)
grep -A8 "determineBadge" src/components/map/KakaoMap.tsx

# 4. 전체 타입 검사
npx tsc --noEmit 2>&1 | tail -5

# 5. 빌드
npm run build 2>&1 | tail -10

# 6. 테스트
npm run test 2>&1 | tail -10
```
</verification>

<success_criteria>
- KakaoMap.tsx: ClusterMarker 완전 제거, DongClusterChip 사용, 3단계 showLabel/showName/showOnlyCluster 분기
- determineBadge: 3종 BadgeInput(status/built_year/tx_count_30d/p95_tx_count)으로 호출
- `npm run build` 통과
- `npm run test` 통과
- 인간 확인: 줌 레벨별 표시 전환, 마커 색상, hover 툴팁 정상 작동
</success_criteria>

<output>
After completion, create `.planning/phases/12-map-marker-cluster/12-04-SUMMARY.md`
</output>

---
phase: 03-cardnews-legal-ops
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260506000001_profiles_consent_delete.sql
  - supabase/migrations/20260506000002_reports.sql
  - src/types/database.ts
  - src/__tests__/cardnews.test.ts
  - src/__tests__/consent-actions.test.ts
  - src/__tests__/admin-actions.test.ts
  - src/__tests__/admin-status.test.ts
  - e2e/accessibility.spec.ts
  - package.json
  - package-lock.json
  - .env.local.example
autonomous: true
requirements:
  - SHARE-03
  - SHARE-04
  - LEGAL-01
  - LEGAL-04
  - LEGAL-05
  - ADMIN-01
  - ADMIN-03
  - ADMIN-04
  - A11Y-01
  - A11Y-02
  - A11Y-03

must_haves:
  truths:
    - "profiles 테이블에 deleted_at, terms_agreed_at, suspended_at 컬럼이 존재한다"
    - "reports 테이블이 존재하고 RLS 정책 3개(auth insert / admin read / admin update)가 활성화되어 있다"
    - "supabase db push가 무오류로 실행되어 원격(또는 로컬) DB에 마이그레이션이 적용된다"
    - "@axe-core/playwright 4.x가 devDependencies에 설치되어 있다"
    - "5종 테스트 파일이 의도된 RED 상태(미구현 함수 import 실패)로 존재한다"
    - "src/types/database.ts가 신규 컬럼·테이블·enum을 반영한다"
    - ".env.local.example에 SUPPORT_EMAIL이 비어있는 키로 추가되어 있다"
  artifacts:
    - path: "supabase/migrations/20260506000001_profiles_consent_delete.sql"
      provides: "profiles ALTER + 부분 인덱스 2종"
      contains: "alter table public.profiles"
    - path: "supabase/migrations/20260506000002_reports.sql"
      provides: "reports 테이블 + enum + RLS 3정책"
      contains: "create table public.reports"
    - path: "src/__tests__/cardnews.test.ts"
      provides: "SHARE-03, SHARE-04 RED 테스트"
      min_lines: 20
    - path: "src/__tests__/consent-actions.test.ts"
      provides: "LEGAL-01, LEGAL-04 RED 테스트"
      min_lines: 20
    - path: "src/__tests__/admin-actions.test.ts"
      provides: "ADMIN-01, ADMIN-03 RED 테스트"
      min_lines: 20
    - path: "src/__tests__/admin-status.test.ts"
      provides: "ADMIN-04 RED 테스트"
      min_lines: 15
    - path: "e2e/accessibility.spec.ts"
      provides: "A11Y-01/02/03 E2E 스펙"
      min_lines: 30
  key_links:
    - from: "supabase/migrations/20260506000002_reports.sql"
      to: "public.profiles"
      via: "reporter_id / resolved_by FK references"
      pattern: "references public\\.profiles"
    - from: "src/types/database.ts"
      to: "supabase 마이그레이션"
      via: "Tables.profiles + Tables.reports + Enums.report_status / report_target_type"
      pattern: "report_status|report_target_type"
---

<objective>
Wave 0: Phase 3 전체 구현의 기반(스키마 + 패키지 + RED 테스트)을 한 번에 준비한다.

Purpose:
- 후속 Wave가 모두 이 결과물에 의존한다 (consent-actions, admin pages, e2e accessibility 모두).
- TDD 원칙(CLAUDE.md CRITICAL)을 만족하기 위해 구현 전 테스트 스캐폴드를 먼저 만든다.
- supabase db push를 한 번만 실행해 이후 Wave는 마이그레이션 충돌 없이 진행하게 한다.

Output:
- 2개 신규 마이그레이션 + types/database.ts 갱신
- 4개 vitest 테스트 + 1개 playwright 스펙 (전부 의도된 실패 상태)
- @axe-core/playwright devDependency 추가
- .env.local.example에 SUPPORT_EMAIL 추가
- supabase db push 실행 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cardnews-legal-ops/03-CONTEXT.md
@.planning/phases/03-cardnews-legal-ops/03-RESEARCH.md
@.planning/phases/03-cardnews-legal-ops/03-PATTERNS.md
@supabase/migrations/20260430000005_users.sql
@supabase/migrations/20260430000007_ads.sql
@supabase/migrations/20260430000009_rls.sql
@supabase/migrations/20260430000016_reviews.sql
@src/__tests__/helpers/db.ts
@src/__tests__/reviews.test.ts
@src/__tests__/ads.test.ts
@e2e/landing.spec.ts

<interfaces>
<!-- 신규 컬럼/테이블이 후속 Wave에서 사용할 인터페이스 -->

profiles 추가 컬럼 (D-09, D-12, ADMIN-01):
- deleted_at: timestamptz | null  -- 30일 grace soft delete
- terms_agreed_at: timestamptz | null  -- NULL = 미동의
- suspended_at: timestamptz | null  -- ADMIN-01 계정 정지

reports 테이블 (ADMIN-03):
- id uuid PK
- reporter_id uuid → profiles(id) ON DELETE SET NULL
- target_type report_target_type ('review' | 'user' | 'ad')
- target_id uuid NOT NULL
- reason text CHECK (5..200)
- status report_status ('pending' | 'accepted' | 'rejected') default 'pending'
- resolved_by uuid → profiles(id) ON DELETE SET NULL
- resolved_at timestamptz | null
- created_at timestamptz default now()

NOTE: complex_reviews.user_id FK ON DELETE SET NULL은 20260430000016_reviews.sql 라인 4에 이미 존재 — D-08 마이그레이션 불필요.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profiles 컬럼 추가 마이그레이션 + reports 테이블 마이그레이션 작성</name>
  <files>supabase/migrations/20260506000001_profiles_consent_delete.sql, supabase/migrations/20260506000002_reports.sql</files>
  <read_first>
    - supabase/migrations/20260430000005_users.sql (profiles 원본 스키마 확인)
    - supabase/migrations/20260430000007_ads.sql (enum + 부분 인덱스 패턴 라인 1, 23-25)
    - supabase/migrations/20260430000009_rls.sql (admin 정책 패턴 라인 70-78)
    - supabase/migrations/20260430000016_reviews.sql (D-08 검증: user_id FK ON DELETE SET NULL이 라인 4에 이미 있음 — 추가 마이그레이션 불필요)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 486-565: Migration 1, Migration 2 권장 SQL)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 553-647: 정확한 SQL 패턴)
  </read_first>
  <action>
    파일 1: `supabase/migrations/20260506000001_profiles_consent_delete.sql` 신규 작성. 정확히 다음 SQL을 작성:

    ```sql
    -- Phase 3: 동의·탈퇴·정지 컬럼 추가 (D-09, D-12, ADMIN-01)
    -- 기존 profiles 테이블 (20260430000005_users.sql)에 ALTER TABLE
    alter table public.profiles
      add column if not exists deleted_at      timestamptz,
      add column if not exists terms_agreed_at timestamptz,
      add column if not exists suspended_at    timestamptz;

    -- 30일 hard delete cron 필터용 부분 인덱스
    create index if not exists profiles_deleted_at_idx
      on public.profiles(deleted_at)
      where deleted_at is not null;

    -- 어드민 status 페이지: 약관 미동의 회원 카운트용 부분 인덱스
    create index if not exists profiles_terms_not_agreed_idx
      on public.profiles(id)
      where terms_agreed_at is null;

    comment on column public.profiles.deleted_at      is 'Soft delete 시각 (30일 grace 후 hard delete cron이 auth.users 삭제)';
    comment on column public.profiles.terms_agreed_at is '이용약관·개인정보처리방침 동의 시각 (NULL = 미동의 → /consent 리다이렉트)';
    comment on column public.profiles.suspended_at    is '관리자에 의한 계정 정지 시각 (NULL = 정상)';
    ```

    NOTE: 기존 `profiles: owner update` RLS 정책은 변경하지 않음. RESEARCH.md Pitfall 2 결정대로 deleted_at/terms_agreed_at/suspended_at 업데이트는 모두 `createSupabaseAdminClient()` 경유 (Wave 1·2 Server Action에서 처리).

    파일 2: `supabase/migrations/20260506000002_reports.sql` 신규 작성. 정확히 다음 SQL을 작성:

    ```sql
    -- Phase 3 ADMIN-03: 신고 큐 테이블 + RLS
    create type public.report_target_type as enum ('review', 'user', 'ad');
    create type public.report_status      as enum ('pending', 'accepted', 'rejected');

    create table public.reports (
      id          uuid primary key default gen_random_uuid(),
      reporter_id uuid references public.profiles(id) on delete set null,
      target_type public.report_target_type not null,
      target_id   uuid not null,
      reason      text not null check (char_length(reason) between 5 and 200),
      status      public.report_status not null default 'pending',
      resolved_by uuid references public.profiles(id) on delete set null,
      resolved_at timestamptz,
      created_at  timestamptz not null default now(),
      constraint reports_no_self_report check (reporter_id is null or reporter_id::text <> target_id::text)
    );

    create index reports_status_created_idx
      on public.reports(status, created_at desc)
      where status = 'pending';
    create index reports_target_idx on public.reports(target_type, target_id);

    alter table public.reports enable row level security;

    -- 인증 사용자만 신고 생성, reporter_id = 자기 자신 강제
    create policy "reports: auth insert"
      on public.reports for insert
      with check (auth.uid() is not null and reporter_id = auth.uid());

    -- admin/superadmin만 신고 조회
    create policy "reports: admin read"
      on public.reports for select
      using (exists (
        select 1 from public.profiles
        where id = auth.uid() and role in ('admin', 'superadmin')
      ));

    -- admin/superadmin만 신고 처리(상태 변경)
    create policy "reports: admin update"
      on public.reports for update
      using (exists (
        select 1 from public.profiles
        where id = auth.uid() and role in ('admin', 'superadmin')
      ));

    comment on table public.reports is 'ADMIN-03 신고 큐: review/user/ad 대상 신고 + admin 처리 로그';
    ```

    NOTE: D-08 (`complex_reviews.user_id` ON DELETE SET NULL) 마이그레이션은 작성하지 말 것. 20260430000016_reviews.sql 라인 4에 이미 존재함 (RESEARCH.md 라인 488-495 참조).
  </action>
  <acceptance_criteria>
    - `test -f supabase/migrations/20260506000001_profiles_consent_delete.sql` 종료 코드 0
    - `test -f supabase/migrations/20260506000002_reports.sql` 종료 코드 0
    - `grep -c "deleted_at" supabase/migrations/20260506000001_profiles_consent_delete.sql` ≥ 3
    - `grep -c "terms_agreed_at" supabase/migrations/20260506000001_profiles_consent_delete.sql` ≥ 3
    - `grep -c "suspended_at" supabase/migrations/20260506000001_profiles_consent_delete.sql` ≥ 2
    - `grep -c "create policy" supabase/migrations/20260506000002_reports.sql` 정확히 3
    - `grep -c "create type public.report_target_type" supabase/migrations/20260506000002_reports.sql` 정확히 1
    - `grep -c "create type public.report_status" supabase/migrations/20260506000002_reports.sql` 정확히 1
    - `grep "char_length(reason) between 5 and 200" supabase/migrations/20260506000002_reports.sql` 1건 매치
    - `grep "reports_no_self_report" supabase/migrations/20260506000002_reports.sql` 1건 매치
    - `grep "complex_reviews" supabase/migrations/20260506000001_profiles_consent_delete.sql supabase/migrations/20260506000002_reports.sql` 0건 (D-08 마이그레이션 작성 안 함)
  </acceptance_criteria>
  <verify>
    <!-- SQL 구문·핵심 컬럼 존재 검증을 lint 전에 수행 → Task 4 db push까지 가지 않고 명백한 SQL 오류를 즉시 잡는다 -->
    <!-- 가능하면 `npx supabase db lint supabase/migrations/2026050600000{1,2}_*.sql`로 교체 (로컬 supabase 실행 중이어야 함) -->
    <automated>grep -q "alter table public.profiles" supabase/migrations/20260506000001_profiles_consent_delete.sql && grep -q "deleted_at" supabase/migrations/20260506000001_profiles_consent_delete.sql && grep -q "terms_agreed_at" supabase/migrations/20260506000001_profiles_consent_delete.sql && grep -q "suspended_at" supabase/migrations/20260506000001_profiles_consent_delete.sql && grep -q "create table public.reports" supabase/migrations/20260506000002_reports.sql && grep -q "create type public.report_target_type" supabase/migrations/20260506000002_reports.sql && grep -q "create type public.report_status" supabase/migrations/20260506000002_reports.sql && grep -q "char_length(reason) between 5 and 200" supabase/migrations/20260506000002_reports.sql && grep -q "reports_no_self_report" supabase/migrations/20260506000002_reports.sql && [ "$(grep -c "create policy" supabase/migrations/20260506000002_reports.sql)" -ge 3 ] && npm run lint</automated>
  </verify>
  <done>두 마이그레이션 파일이 정확한 SQL로 존재하고(핵심 컬럼·정책·enum·CHECK 제약 모두 grep 검증), 기존 RLS 정책을 건드리지 않으며, D-08 중복 마이그레이션이 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4종 vitest 테스트 + 1종 playwright e2e 스펙 RED 작성</name>
  <files>src/__tests__/cardnews.test.ts, src/__tests__/consent-actions.test.ts, src/__tests__/admin-actions.test.ts, src/__tests__/admin-status.test.ts, e2e/accessibility.spec.ts</files>
  <read_first>
    - src/__tests__/helpers/db.ts (URL_, SKEY, AKEY, admin export — 모든 vitest 파일의 표준 헬퍼)
    - src/__tests__/reviews.test.ts (vi.mock 표준 블록 라인 11-15, beforeAll stubEnv 패턴 라인 17-21)
    - src/__tests__/ads.test.ts (admin Server Action 'no auth → error' 패턴 라인 113-136)
    - e2e/landing.spec.ts (test 구조 + page.goto + waitForLoadState 라인 1-12)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 692-720: Test Map + Wave 0 Gaps)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 651-700: e2e/accessibility.spec.ts 정확한 코드)
  </read_first>
  <behavior>
    각 테스트 파일은 RED 상태(import 실패 또는 expect 실패)로 시작해야 한다. 이는 TDD 원칙 준수 증거이다.

    cardnews.test.ts:
      - import 'GET' from '@/app/api/cardnews/generate/route' → 모듈 없음 = RED
      - 의도: 비로그인 호출 시 401 반환 검증, admin role 호출 시 200 + Content-Type=image/png 검증

    consent-actions.test.ts:
      - import { agreeToTerms, deleteAccount, reactivateAccount } from '@/lib/auth/consent-actions' → 모듈 없음 = RED
      - 의도: 비로그인 → error 반환 / 로그인 + 동의 → terms_agreed_at IS NOT NULL / deleteAccount → deleted_at IS NOT NULL / reactivateAccount → deleted_at IS NULL

    admin-actions.test.ts:
      - import { suspendMember, reactivateMember, resolveReport } from '@/lib/auth/admin-actions' → 모듈 없음 = RED
      - 의도: 각 액션 비로그인 → error 반환 + 비-admin role → error 반환

    admin-status.test.ts:
      - 의도: status page에서 사용할 COUNT 쿼리 8종이 admin client로 정상 실행됨을 검증

    accessibility.spec.ts:
      - import AxeBuilder from '@axe-core/playwright' → 패키지 미설치 시 fail (Task 3에서 설치)
      - 의도: 6개 페이지 critical 위반 0건 + 키보드 Tab 포커스 + 헤더 nav aria 라벨
  </behavior>
  <action>
    파일 1: `src/__tests__/cardnews.test.ts` — reviews.test.ts 라인 1-21의 vi.mock + stubEnv 블록을 그대로 복사하고, 다음 테스트를 추가:

    ```typescript
    /**
     * Phase 3 SHARE-03, SHARE-04: 카드뉴스 생성 API
     * RED state: route module not yet implemented (Wave 2)
     */
    import { describe, it, expect, vi, beforeAll } from 'vitest'
    import { URL_, SKEY, AKEY } from './helpers/db'

    vi.mock('server-only', () => ({}))
    vi.mock('next/headers', () => ({
      cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
    }))
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))

    beforeAll(() => {
      vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', URL_)
      vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', AKEY)
      vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', SKEY)
    })

    describe('GET /api/cardnews/generate', () => {
      it('비로그인 호출 → 401 Unauthorized', async () => {
        const { GET } = await import('@/app/api/cardnews/generate/route')
        const req = new Request('http://localhost/api/cardnews/generate')
        const res = await GET(req)
        expect(res.status).toBe(401)
      })

      it('runtime은 nodejs (Edge runtime은 TTF 4MB 한도 초과)', async () => {
        const mod = await import('@/app/api/cardnews/generate/route')
        expect((mod as { runtime?: string }).runtime).toBe('nodejs')
      })
    })
    ```

    파일 2: `src/__tests__/consent-actions.test.ts` — ads.test.ts 라인 113-136 패턴을 따라 다음 테스트:

    ```typescript
    /**
     * Phase 3 LEGAL-01, LEGAL-04: 동의 / 탈퇴 / 재활성화 Server Action
     * RED state: actions not yet implemented (Wave 1)
     */
    import { describe, it, expect, vi, beforeAll } from 'vitest'
    import { URL_, SKEY, AKEY } from './helpers/db'

    vi.mock('server-only', () => ({}))
    vi.mock('next/headers', () => ({
      cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
    }))
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))

    beforeAll(() => {
      vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', URL_)
      vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', AKEY)
      vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', SKEY)
    })

    describe('agreeToTerms (no auth)', () => {
      it('비로그인 → error 반환', async () => {
        const { agreeToTerms } = await import('@/lib/auth/consent-actions')
        const fd = new FormData()
        fd.append('next', '/')
        fd.append('terms', 'on')
        fd.append('privacy', 'on')
        const result = await agreeToTerms(fd)
        expect(result.error).toBeTruthy()
      })

      it('체크박스 누락 → error 반환', async () => {
        const { agreeToTerms } = await import('@/lib/auth/consent-actions')
        const fd = new FormData()
        fd.append('next', '/')
        // terms / privacy 미첨부
        const result = await agreeToTerms(fd)
        expect(result.error).toBeTruthy()
      })
    })

    describe('deleteAccount (no auth)', () => {
      it('비로그인 → error 반환', async () => {
        const { deleteAccount } = await import('@/lib/auth/consent-actions')
        const result = await deleteAccount()
        expect(result.error).toBeTruthy()
      })
    })

    describe('reactivateAccount (no auth)', () => {
      it('비로그인 → error 반환', async () => {
        const { reactivateAccount } = await import('@/lib/auth/consent-actions')
        const result = await reactivateAccount()
        expect(result.error).toBeTruthy()
      })
    })
    ```

    파일 3: `src/__tests__/admin-actions.test.ts` — ads.test.ts admin 가드 패턴 그대로:

    ```typescript
    /**
     * Phase 3 ADMIN-01, ADMIN-03: 회원 관리 + 신고 처리 Server Action
     * RED state: actions not yet implemented (Wave 2)
     */
    import { describe, it, expect, vi, beforeAll } from 'vitest'
    import { URL_, SKEY, AKEY } from './helpers/db'

    vi.mock('server-only', () => ({}))
    vi.mock('next/headers', () => ({
      cookies: vi.fn(() => ({ getAll: () => [], set: vi.fn() })),
    }))
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))

    beforeAll(() => {
      vi.stubEnv('NEXT_PUBLIC_SUPABASE_URL', URL_)
      vi.stubEnv('NEXT_PUBLIC_SUPABASE_ANON_KEY', AKEY)
      vi.stubEnv('SUPABASE_SERVICE_ROLE_KEY', SKEY)
    })

    const FAKE_ID = '00000000-0000-0000-0000-000000000001'

    describe('suspendMember / reactivateMember (no auth)', () => {
      it('suspendMember 비로그인 → error', async () => {
        const { suspendMember } = await import('@/lib/auth/admin-actions')
        const result = await suspendMember(FAKE_ID)
        expect(result.error).toBeTruthy()
      })
      it('reactivateMember 비로그인 → error', async () => {
        const { reactivateMember } = await import('@/lib/auth/admin-actions')
        const result = await reactivateMember(FAKE_ID)
        expect(result.error).toBeTruthy()
      })
    })

    describe('resolveReport (no auth)', () => {
      it('accepted 비로그인 → error', async () => {
        const { resolveReport } = await import('@/lib/auth/admin-actions')
        const result = await resolveReport(FAKE_ID, 'accepted')
        expect(result.error).toBeTruthy()
      })
      it('rejected 비로그인 → error', async () => {
        const { resolveReport } = await import('@/lib/auth/admin-actions')
        const result = await resolveReport(FAKE_ID, 'rejected')
        expect(result.error).toBeTruthy()
      })
      it('잘못된 action 값 → error', async () => {
        const { resolveReport } = await import('@/lib/auth/admin-actions')
        // @ts-expect-error: invalid action 값 강제 전달
        const result = await resolveReport(FAKE_ID, 'invalid')
        expect(result.error).toBeTruthy()
      })
    })
    ```

    파일 4: `src/__tests__/admin-status.test.ts` — admin client COUNT 쿼리 검증:

    ```typescript
    /**
     * Phase 3 ADMIN-04: 시스템 모니터링 — COUNT 쿼리 검증
     * RED state: page not yet implemented; tests verify query schema once columns/tables exist
     */
    import { describe, it, expect, beforeAll } from 'vitest'
    import { admin } from './helpers/db'

    beforeAll(() => {
      // 마이그레이션이 적용되어야 통과 (Task 4 supabase db push 의존)
    })

    describe('admin/status COUNT queries', () => {
      it('profiles 카운트 쿼리 정상', async () => {
        const { error, count } = await admin
          .from('profiles')
          .select('*', { count: 'exact', head: true })
        expect(error).toBeNull()
        expect(typeof count).toBe('number')
      })
      it('terms_agreed_at IS NULL 쿼리 정상 (신규 컬럼)', async () => {
        const { error } = await admin
          .from('profiles')
          .select('*', { count: 'exact', head: true })
          .is('terms_agreed_at', null)
        expect(error).toBeNull()
      })
      it('reports pending 카운트 정상 (신규 테이블)', async () => {
        const { error, count } = await admin
          .from('reports')
          .select('*', { count: 'exact', head: true })
          .eq('status', 'pending')
        expect(error).toBeNull()
        expect(typeof count).toBe('number')
      })
      it('ingest_runs 최근 10건 정상', async () => {
        const { error, data } = await admin
          .from('ingest_runs')
          .select('source, status, created_at')
          .order('created_at', { ascending: false })
          .limit(10)
        expect(error).toBeNull()
        expect(Array.isArray(data)).toBe(true)
      })
    })
    ```

    파일 5: `e2e/accessibility.spec.ts` — PATTERNS.md 라인 671-700의 정확한 코드:

    ```typescript
    /**
     * Phase 3 A11Y-01, A11Y-02, A11Y-03: 접근성 E2E
     * RED state: pages /consent, /legal/terms, /legal/privacy not yet implemented
     */
    import { test, expect } from '@playwright/test'
    import AxeBuilder from '@axe-core/playwright'

    const PUBLIC_PAGES = ['/', '/legal/terms', '/legal/privacy', '/legal/ad-policy', '/login']

    for (const path of PUBLIC_PAGES) {
      test(`A11Y-01 axe-core: ${path} — critical 위반 0건`, async ({ page }) => {
        await page.goto(path)
        await page.waitForLoadState('domcontentloaded')

        const results = await new AxeBuilder({ page })
          .withTags(['wcag2a', 'wcag2aa'])
          .analyze()

        const critical = results.violations.filter(v => v.impact === 'critical')
        expect(
          critical,
          `Critical violations on ${path}: ${JSON.stringify(critical.map(v => v.id))}`,
        ).toHaveLength(0)
      })
    }

    test('A11Y-02 키보드 Tab — 랜딩 페이지 첫 포커스 가시성', async ({ page }) => {
      await page.goto('/')
      await page.waitForLoadState('domcontentloaded')
      await page.keyboard.press('Tab')
      const focused = page.locator(':focus')
      await expect(focused).toBeVisible()
    })

    test('A11Y-03 스크린리더 — 랜딩 nav aria-label 또는 시맨틱 nav 존재', async ({ page }) => {
      await page.goto('/')
      await page.waitForLoadState('domcontentloaded')
      // Footer 또는 main nav에 aria-label 또는 <nav> 시맨틱 존재
      const nav = page.locator('nav, [role="navigation"]').first()
      await expect(nav).toBeVisible()
    })
    ```

    NOTE: 모든 vitest 파일은 마이그레이션 적용 + 모듈 구현 후 GREEN. e2e는 페이지 + 패키지 설치 후 GREEN. 지금은 RED인 것이 정상.
  </action>
  <acceptance_criteria>
    - `test -f src/__tests__/cardnews.test.ts` 종료 코드 0
    - `test -f src/__tests__/consent-actions.test.ts` 종료 코드 0
    - `test -f src/__tests__/admin-actions.test.ts` 종료 코드 0
    - `test -f src/__tests__/admin-status.test.ts` 종료 코드 0
    - `test -f e2e/accessibility.spec.ts` 종료 코드 0
    - `grep -c "import.*@/app/api/cardnews/generate/route" src/__tests__/cardnews.test.ts` ≥ 1
    - `grep -c "agreeToTerms\|deleteAccount\|reactivateAccount" src/__tests__/consent-actions.test.ts` ≥ 4
    - `grep -c "suspendMember\|reactivateMember\|resolveReport" src/__tests__/admin-actions.test.ts` ≥ 5
    - `grep -c "AxeBuilder" e2e/accessibility.spec.ts` ≥ 2
    - `grep -c "wcag2a" e2e/accessibility.spec.ts` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>5개 테스트 파일이 RED 상태로 존재하고, 후속 Wave에서 사용할 모듈 import 경로가 명시되어 있다.</done>
</task>

<task type="auto">
  <name>Task 3: @axe-core/playwright 설치 + .env.local.example에 SUPPORT_EMAIL 확인</name>
  <files>package.json, package-lock.json, .env.local.example</files>
  <read_first>
    - package.json (현재 devDependencies 확인 — @axe-core/playwright 없음)
    - .env.local.example (SUPPORT_EMAIL 라인 49 이미 있음 — 변경 불필요지만 확인)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 122-131: 패키지 + 설치 명령)
  </read_first>
  <action>
    1. `npm install --save-dev @axe-core/playwright@4.11.3` 실행. devDependencies에 정확히 `"@axe-core/playwright": "^4.11.3"`이 추가되어야 함.

    2. `.env.local.example` 확인:
       - 현재 라인 49에 `SUPPORT_EMAIL=` 이미 존재 (확인됨 — 라인 49). 변경 불필요.
       - 만약 사라졌다면 `OPERATOR_EMAIL` 다음 줄에 `SUPPORT_EMAIL=` 빈값으로 추가 (D-18: 기본값은 `support@danjiondo.com` 이며 코드에서 fallback 처리).

    3. lint/build/test가 깨지지 않는지 확인. (테스트는 RED여도 컴파일은 통과해야 함)
  </action>
  <acceptance_criteria>
    - `grep -c "@axe-core/playwright" package.json` ≥ 1
    - `grep "@axe-core/playwright" package.json | grep -E "4\\.[0-9]+\\.[0-9]+"` 1건 매치 (4.x 버전)
    - `grep -c "^SUPPORT_EMAIL=" .env.local.example` 정확히 1
    - `test -f node_modules/@axe-core/playwright/package.json` 종료 코드 0
    - `npm run lint` 종료 코드 0
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>@axe-core/playwright가 devDependencies에 추가되었고 SUPPORT_EMAIL 환경변수 키가 존재한다.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] supabase db push + types/database.ts 재생성</name>
  <files>src/types/database.ts</files>
  <read_first>
    - src/types/database.ts (현재 schema 타입 — profiles에 deleted_at/terms_agreed_at/suspended_at 없음, reports 테이블 없음 확인)
    - supabase/migrations/20260506000001_profiles_consent_delete.sql (Task 1 산출물)
    - supabase/migrations/20260506000002_reports.sql (Task 1 산출물)
    - package.json (db:push 스크립트 라인 15: `"db:push": "supabase db push"`)
  </read_first>
  <action>
    1. **[BLOCKING]** `npm run db:push` 실행 (= `supabase db push`). 이 단계는 phase의 후속 Wave가 모두 의존한다.
       - 환경 변수 `SUPABASE_ACCESS_TOKEN` 또는 로컬 supabase 실행 중이어야 함. 둘 다 없으면 stop하고 `npm run db:start`로 로컬 stack 시작 후 재시도.
       - 출력에서 `Applying migration 20260506000001_profiles_consent_delete.sql...` `Applying migration 20260506000002_reports.sql...` 두 줄이 모두 보여야 함.
       - 오류 발생 시 (예: profiles 컬럼 이미 존재) 마이그레이션 SQL의 `if not exists` 가드를 확인. enum(`report_target_type`, `report_status`)은 멱등성이 없으므로 이미 존재 시 `drop type ... cascade;`를 마이그레이션 맨 앞에 임시 추가하거나, 깨끗한 DB에서 재실행.

    2. types/database.ts 재생성:
       - 옵션 A (권장): `npx supabase gen types typescript --local > src/types/database.ts` (로컬 supabase 사용 중일 때)
       - 옵션 B: `npx supabase gen types typescript --linked > src/types/database.ts` (원격 연결된 경우)
       - 옵션 C (오프라인 fallback): 직접 편집해서 다음을 추가:
         - `Tables.profiles.Row`에 `deleted_at: string | null`, `terms_agreed_at: string | null`, `suspended_at: string | null` 추가
         - `Tables.profiles.Insert`/`Update`에 동일 3개 컬럼 optional로 추가
         - `Tables.reports`: Row/Insert/Update 정의 — 위 마이그레이션 컬럼과 일치
         - `Enums`에 `report_status: 'pending' | 'accepted' | 'rejected'`, `report_target_type: 'review' | 'user' | 'ad'` 추가

    3. `npm run lint` 통과 확인 (tsc --noEmit 포함).

    4. `npm run test -- --run cardnews.test admin-status.test`로 컴파일 가능 확인 (테스트는 RED여도 import resolution은 통과).
  </action>
  <acceptance_criteria>
    - `grep -c "deleted_at" src/types/database.ts` ≥ 3 (Row + Insert + Update)
    - `grep -c "terms_agreed_at" src/types/database.ts` ≥ 3
    - `grep -c "suspended_at" src/types/database.ts` ≥ 3
    - `grep -c "reports:" src/types/database.ts` ≥ 1 또는 `grep -c "reports: {" src/types/database.ts` ≥ 1
    - `grep -c "report_status" src/types/database.ts` ≥ 1
    - `grep -c "report_target_type" src/types/database.ts` ≥ 1
    - `npm run lint` 종료 코드 0 (tsc --noEmit 통과 = 마이그레이션 + 타입 정합성 확인)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>마이그레이션이 DB에 적용되었고 src/types/database.ts가 신규 컬럼·테이블·enum을 노출한다. 후속 Wave가 새 컬럼/테이블에 타입 안전하게 접근 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → DB | supabase db push가 권한 있는 SQL을 실행. 멱등성 위반 시 데이터 손실 가능 |
| RLS policies on reports | 인증 사용자 입력이 reports.reporter_id로 들어옴 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | reports.reporter_id 위변조 | mitigate | RLS `with check (reporter_id = auth.uid())` 강제 (Task 1 reports 정책) |
| T-03-02 | Spoofing | 자기 자신 신고 | mitigate | CHECK 제약 `reporter_id is null or reporter_id::text <> target_id::text` (Task 1) |
| T-03-03 | Information Disclosure | 비-admin이 reports 조회 | mitigate | RLS `select` 정책 admin/superadmin role 한정 (Task 1) |
| T-03-04 | Denial of Service | reports.reason 매우 긴 문자열 | mitigate | CHECK `char_length between 5 and 200` (Task 1) |
| T-03-05 | Tampering | 마이그레이션 미적용 상태에서 후속 Wave 코드 실행 → orphan FK | mitigate | Task 4 [BLOCKING] supabase db push가 후속 Wave 시작 전 완료 강제 |
</threat_model>

<verification>
1. `test -f supabase/migrations/20260506000001_profiles_consent_delete.sql && test -f supabase/migrations/20260506000002_reports.sql` 종료 코드 0
2. `npm run lint` 통과 (tsc --noEmit 포함)
3. `grep -E "deleted_at|terms_agreed_at|suspended_at" src/types/database.ts | wc -l` ≥ 9 (3 컬럼 × Row/Insert/Update)
4. `grep -c "@axe-core/playwright" package.json` ≥ 1
5. supabase db push 출력 로그에 `Applying migration 20260506000001_*` `Applying migration 20260506000002_*` 두 줄 존재
6. RED 테스트 5종 모두 파일 존재 + import 경로 명시
</verification>

<success_criteria>
- 2개 마이그레이션이 DB에 적용 완료 (supabase db push 무오류)
- @axe-core/playwright가 devDependencies에 등록
- src/types/database.ts가 신규 컬럼·테이블·enum 반영
- 5종 RED 테스트 파일 존재 (cardnews / consent-actions / admin-actions / admin-status / e2e accessibility)
- npm run lint 통과
- 후속 Wave 1·2·3이 모두 이 산출물에 의존 가능한 상태
</success_criteria>

<output>
After completion, create `.planning/phases/03-cardnews-legal-ops/03-01-SUMMARY.md` with:
- 마이그레이션 파일 경로 + supabase db push 실행 결과
- 추가된 패키지 + 버전
- types/database.ts 갱신 방법 (gen types --local vs 수동)
- 5종 RED 테스트 파일 경로
- 후속 Wave가 의존하는 인터페이스 (profiles 컬럼, reports 테이블, report_target_type / report_status enum)
- 발견된 이슈 (있다면)
</output>

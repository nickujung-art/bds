---
phase: 03-cardnews-legal-ops
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/lib/auth/admin-actions.ts
  - src/app/admin/members/page.tsx
  - src/app/admin/reports/page.tsx
  - src/app/admin/status/page.tsx
  - src/components/admin/MemberActions.tsx
  - src/components/admin/ReportActions.tsx
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "admin-actions.ts가 suspendMember / reactivateMember / resolveReport 3개 Server Action을 export하고 모두 admin role 가드를 통과해야만 작동한다"
    - "/admin/members 페이지에서 회원 목록과 정지/재활성화 버튼이 표시된다"
    - "/admin/reports 페이지에서 pending 신고 목록과 처리 완료/기각 버튼이 표시된다"
    - "/admin/status 페이지에서 8개 카운트(회원/단지/거래/광고/이력/신고/광고대기/약관미동의)가 표시된다"
    - "기존 /admin/ads 페이지가 깨지지 않는다 (ADMIN-02 광고 검수 상태 머신 retain — 이미 구현됨, ads.test.ts 회귀 통과)"
    - "src/__tests__/admin-actions.test.ts와 admin-status.test.ts가 GREEN으로 통과한다"
    - "비-admin 사용자가 /admin/* 어떤 페이지에 접근해도 / 로 리다이렉트된다"
  artifacts:
    - path: "src/lib/auth/admin-actions.ts"
      provides: "suspendMember / reactivateMember / resolveReport Server Actions"
      exports: ["suspendMember", "reactivateMember", "resolveReport"]
    - path: "src/app/admin/members/page.tsx"
      provides: "회원 관리 어드민 (ADMIN-01)"
      contains: "createSupabaseAdminClient"
    - path: "src/app/admin/reports/page.tsx"
      provides: "신고 큐 어드민 (ADMIN-03)"
      contains: "from('reports')"
    - path: "src/app/admin/status/page.tsx"
      provides: "시스템 모니터링 (ADMIN-04, 8개 COUNT)"
      contains: "Promise.all"
    - path: "src/components/admin/MemberActions.tsx"
      provides: "정지/재활성화 클라이언트 버튼"
      contains: "'use client'"
    - path: "src/components/admin/ReportActions.tsx"
      provides: "신고 처리/기각 클라이언트 버튼"
      contains: "'use client'"
  key_links:
    - from: "src/lib/auth/admin-actions.ts"
      to: "createSupabaseAdminClient + verifyAdmin"
      via: "RLS 우회 profiles.suspended_at + reports.update"
      pattern: "createSupabaseAdminClient"
    - from: "src/app/admin/status/page.tsx"
      to: "8 parallel COUNT queries via Promise.all"
      via: "ingest_runs / profiles / reports / ad_campaigns / complexes / transactions"
      pattern: "Promise\\.all"
---

<objective>
Wave 2 (parallel with Plan 03): ADMIN-01~04 어드민 확장을 한 번에 완성한다.

Purpose:
- 회원 관리(ADMIN-01) + 신고 큐(ADMIN-03)는 모두 admin-actions.ts Server Action을 공유하므로 묶음 처리.
- 시스템 모니터링(ADMIN-04)은 신규 컬럼·테이블(suspended_at, reports)에 의존하므로 같은 wave에서 작업.
- ADMIN-02(광고 검수)는 이미 src/app/admin/ads/page.tsx + ad-actions.ts에 구현됨 — 이번 plan은 회귀 테스트만 보장하고 추가 작업 없음.

Output:
- admin-actions.ts (3개 Server Action: suspendMember, reactivateMember, resolveReport)
- /admin/members, /admin/reports, /admin/status 3개 페이지
- MemberActions, ReportActions 클라이언트 컴포넌트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-cardnews-legal-ops/03-CONTEXT.md
@.planning/phases/03-cardnews-legal-ops/03-RESEARCH.md
@.planning/phases/03-cardnews-legal-ops/03-PATTERNS.md
@.planning/phases/03-cardnews-legal-ops/03-01-SUMMARY.md
@src/app/admin/ads/page.tsx
@src/components/ads/AdminCampaignActions.tsx
@src/lib/auth/ad-actions.ts
@src/lib/auth/review-actions.ts
@src/lib/supabase/admin.ts
@src/lib/supabase/server.ts

<interfaces>
<!-- Wave 0이 만든 인터페이스 -->

profiles 컬럼:
- suspended_at: string | null  (ADMIN-01에서 set/clear)
- terms_agreed_at: string | null  (ADMIN-04 status에서 NULL count)
- deleted_at: string | null

reports 테이블:
- id: uuid
- reporter_id: uuid | null
- target_type: 'review' | 'user' | 'ad'
- target_id: uuid
- reason: string (5..200)
- status: 'pending' | 'accepted' | 'rejected'
- resolved_by: uuid | null
- resolved_at: string | null
- created_at: string

기존 함수 (재사용):
- ad-actions.ts requireAdmin (라인 10-26) — 동일 패턴 재구현
- AdminCampaignActions.tsx (라인 1-68) — 클라이언트 액션 버튼 패턴
- ads/page.tsx (라인 35-49) — admin guard
- ads/page.tsx (라인 51-53) — adminClient 데이터 조회

ingest_runs 테이블 컬럼: source, status, created_at (read-only via admin client)

ResolveAction 타입: 'accepted' | 'rejected' (Wave 0 admin-actions.test.ts에서 강제)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin-actions.ts (suspendMember / reactivateMember / resolveReport)</name>
  <files>src/lib/auth/admin-actions.ts</files>
  <read_first>
    - src/lib/auth/ad-actions.ts (requireAdmin 라인 10-26 + updateStatus 라인 28-43 — 정확한 admin guard 패턴)
    - src/lib/auth/review-actions.ts (Server Action 반환 타입)
    - src/lib/supabase/admin.ts (createSupabaseAdminClient)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 422-461: Pattern 6 resolveReport 정확한 코드)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 470-505: admin-actions.ts 패턴)
    - src/__tests__/admin-actions.test.ts (Wave 0 RED 테스트 — 통과 케이스)
  </read_first>
  <behavior>
    Test 1: 비로그인 호출 → `{ error: '로그인이 필요합니다' }` 반환 (3개 액션 모두)
    Test 2: 로그인했지만 role='user' → `{ error: '관리자 권한이 필요합니다' }` (3개 액션 모두)
    Test 3: suspendMember(adminClient.profiles.update suspended_at = now())
    Test 4: reactivateMember(adminClient.profiles.update suspended_at = NULL)
    Test 5: resolveReport(id, 'accepted'|'rejected') → adminClient.reports.update { status, resolved_by, resolved_at }
    Test 6: resolveReport(id, 'invalid') → `{ error: '유효하지 않은 처리 결과입니다.' }` (validate input)
  </behavior>
  <action>
    `src/lib/auth/admin-actions.ts` 신규 작성. ad-actions.ts requireAdmin 패턴을 그대로 따르되 admin user를 호출자로 식별 가능하도록 user 객체도 반환:

    ```typescript
    'use server'

    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'
    import { revalidatePath } from 'next/cache'

    type AdminClient = ReturnType<typeof createSupabaseAdminClient>

    /**
     * Phase 3 ADMIN-01/03: admin role 가드 + admin client 반환.
     * ad-actions.ts requireAdmin 패턴을 따르되 user.id를 추가로 반환 (resolved_by 필드용).
     */
    async function requireAdmin(): Promise<{
      error: string | null
      admin: AdminClient | null
      userId: string | null
    }> {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { error: '로그인이 필요합니다', admin: null, userId: null }

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single()

      if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
        return { error: '관리자 권한이 필요합니다', admin: null, userId: null }
      }

      return { error: null, admin: createSupabaseAdminClient(), userId: user.id }
    }

    /**
     * ADMIN-01: 회원 정지 (profiles.suspended_at = now())
     */
    export async function suspendMember(memberId: string): Promise<{ error: string | null }> {
      const { error, admin } = await requireAdmin()
      if (error || !admin) return { error: error! }

      const { error: dbErr } = await admin
        .from('profiles')
        .update({ suspended_at: new Date().toISOString() })
        .eq('id', memberId)

      if (dbErr) return { error: dbErr.message }
      revalidatePath('/admin/members')
      return { error: null }
    }

    /**
     * ADMIN-01: 회원 정지 해제 (profiles.suspended_at = NULL)
     */
    export async function reactivateMember(memberId: string): Promise<{ error: string | null }> {
      const { error, admin } = await requireAdmin()
      if (error || !admin) return { error: error! }

      const { error: dbErr } = await admin
        .from('profiles')
        .update({ suspended_at: null })
        .eq('id', memberId)

      if (dbErr) return { error: dbErr.message }
      revalidatePath('/admin/members')
      return { error: null }
    }

    /**
     * ADMIN-03: 신고 처리 (status = accepted/rejected, resolved_by, resolved_at)
     */
    export async function resolveReport(
      reportId: string,
      action: 'accepted' | 'rejected',
    ): Promise<{ error: string | null }> {
      if (action !== 'accepted' && action !== 'rejected') {
        return { error: '유효하지 않은 처리 결과입니다.' }
      }

      const { error, admin, userId } = await requireAdmin()
      if (error || !admin || !userId) return { error: error! }

      const { error: dbErr } = await admin
        .from('reports')
        .update({
          status: action,
          resolved_by: userId,
          resolved_at: new Date().toISOString(),
        })
        .eq('id', reportId)

      if (dbErr) return { error: dbErr.message }
      revalidatePath('/admin/reports')
      return { error: null }
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "'use server'" src/lib/auth/admin-actions.ts` 정확히 1
    - `grep -c "export async function suspendMember" src/lib/auth/admin-actions.ts` 정확히 1
    - `grep -c "export async function reactivateMember" src/lib/auth/admin-actions.ts` 정확히 1
    - `grep -c "export async function resolveReport" src/lib/auth/admin-actions.ts` 정확히 1
    - `grep -c "requireAdmin" src/lib/auth/admin-actions.ts` ≥ 4 (정의 1 + 호출 3)
    - `grep -c "'admin', 'superadmin'" src/lib/auth/admin-actions.ts` ≥ 1
    - `grep -c "createSupabaseAdminClient" src/lib/auth/admin-actions.ts` ≥ 2
    - `grep "유효하지 않은 처리 결과" src/lib/auth/admin-actions.ts` 1건 매치
    - `grep "resolved_by" src/lib/auth/admin-actions.ts` ≥ 1
    - `grep "suspended_at" src/lib/auth/admin-actions.ts` ≥ 2 (set + null)
    - `npm run test -- --run src/__tests__/admin-actions.test.ts` 종료 코드 0
    - `npm run test -- --run src/__tests__/ads.test.ts` 종료 코드 0 (ADMIN-02 광고 상태 머신 회귀)
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/__tests__/admin-actions.test.ts src/__tests__/ads.test.ts</automated>
  </verify>
  <done>3개 Server Action이 export되고 admin guard를 통과하며 RED 테스트가 GREEN으로 통과하고, ADMIN-02 광고 검수 상태 머신 회귀 테스트(ads.test.ts)가 정상 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/members + MemberActions + /admin/reports + ReportActions</name>
  <files>src/app/admin/members/page.tsx, src/app/admin/reports/page.tsx, src/components/admin/MemberActions.tsx, src/components/admin/ReportActions.tsx</files>
  <read_first>
    - src/app/admin/ads/page.tsx (admin guard 라인 35-49, 헤더 라인 62-85, 테이블 라인 110-184)
    - src/components/ads/AdminCampaignActions.tsx (useTransition 라인 11-19, 버튼 패턴)
    - src/lib/auth/admin-actions.ts (Task 1 산출물)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 218-264: members + reports 페이지 / 라인 224-238: 회원 컬럼)
  </read_first>
  <action>
    파일 1: `src/components/admin/MemberActions.tsx` — useTransition + suspendMember/reactivateMember:

    ```typescript
    'use client'

    import { useTransition } from 'react'
    import { suspendMember, reactivateMember } from '@/lib/auth/admin-actions'

    interface Props {
      memberId: string
      isSuspended: boolean
    }

    export function MemberActions({ memberId, isSuspended }: Props) {
      const [pending, startTransition] = useTransition()

      function call(action: (id: string) => Promise<{ error: string | null }>) {
        startTransition(async () => {
          const result = await action(memberId)
          if (result.error) alert(result.error)
        })
      }

      if (isSuspended) {
        return (
          <button
            type="button"
            className="btn btn-sm btn-ghost"
            style={{ fontSize: 11, color: '#16a34a' }}
            disabled={pending}
            onClick={() => call(reactivateMember)}
            aria-label="회원 정지 해제"
          >
            정지 해제
          </button>
        )
      }
      return (
        <button
          type="button"
          className="btn btn-sm btn-ghost"
          style={{ fontSize: 11, color: '#dc2626' }}
          disabled={pending}
          onClick={() => call(suspendMember)}
          aria-label="회원 정지"
        >
          정지
        </button>
      )
    }
    ```

    파일 2: `src/components/admin/ReportActions.tsx` — resolveReport(id, action):

    ```typescript
    'use client'

    import { useTransition } from 'react'
    import { resolveReport } from '@/lib/auth/admin-actions'

    interface Props {
      reportId: string
      status: 'pending' | 'accepted' | 'rejected'
    }

    export function ReportActions({ reportId, status }: Props) {
      const [pending, startTransition] = useTransition()

      function call(action: 'accepted' | 'rejected') {
        startTransition(async () => {
          const result = await resolveReport(reportId, action)
          if (result.error) alert(result.error)
        })
      }

      if (status !== 'pending') {
        return <span style={{ font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)' }}>—</span>
      }

      return (
        <div style={{ display: 'flex', gap: 6 }}>
          <button
            type="button"
            className="btn btn-sm btn-primary"
            style={{ fontSize: 11 }}
            disabled={pending}
            onClick={() => call('accepted')}
            aria-label="신고 수용 (처리 완료)"
          >
            처리 완료
          </button>
          <button
            type="button"
            className="btn btn-sm btn-ghost"
            style={{ fontSize: 11, color: '#dc2626' }}
            disabled={pending}
            onClick={() => call('rejected')}
            aria-label="신고 기각"
          >
            기각
          </button>
        </div>
      )
    }
    ```

    파일 3: `src/app/admin/members/page.tsx` — admin guard + adminClient → profiles 조회 + 테이블:

    ```typescript
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import type { Metadata } from 'next'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'
    import { MemberActions } from '@/components/admin/MemberActions'

    export const revalidate = 0
    export const metadata: Metadata = { title: '관리자 · 회원 관리' }

    function formatDate(s: string) {
      return new Date(s).toLocaleDateString('ko-KR', { month: 'short', day: 'numeric', year: 'numeric' })
    }

    interface ProfileRow {
      id: string
      nickname: string | null
      cafe_nickname: string | null
      role: string
      created_at: string
      suspended_at: string | null
      deleted_at: string | null
      terms_agreed_at: string | null
    }

    export default async function AdminMembersPage() {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login?next=/admin/members')

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single()

      if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
        redirect('/')
      }

      const adminClient = createSupabaseAdminClient()
      const { data: members } = await adminClient
        .from('profiles')
        .select('id, nickname, cafe_nickname, role, created_at, suspended_at, deleted_at, terms_agreed_at')
        .order('created_at', { ascending: false })
        .limit(200)

      const rows = (members ?? []) as ProfileRow[]

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
            <span style={{ font: '600 14px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
              관리자 · 회원 관리
            </span>
          </header>

          <main style={{ maxWidth: 1100, margin: '0 auto', padding: '28px 32px' }}>
            <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 20px' }}>
              회원 관리 ({rows.length}명)
            </h1>
            {rows.length === 0 ? (
              <div className="card" style={{ padding: 40, textAlign: 'center', color: 'var(--fg-tertiary)' }}>
                회원이 없습니다.
              </div>
            ) : (
              <div className="card" style={{ padding: 0, overflow: 'hidden' }}>
                <table style={{ width: '100%', borderCollapse: 'collapse' }}>
                  <thead>
                    <tr style={{ borderBottom: '1px solid var(--line-default)', background: 'var(--bg-surface-2)' }}>
                      {['닉네임', '카페 닉네임', '역할', '가입일', '동의', '상태', '액션'].map(h => (
                        <th key={h} scope="col" style={{
                          padding: '10px 16px',
                          font: '600 12px/1 var(--font-sans)',
                          color: 'var(--fg-sec)',
                          textAlign: 'left',
                          whiteSpace: 'nowrap',
                        }}>{h}</th>
                      ))}
                    </tr>
                  </thead>
                  <tbody>
                    {rows.map((m, i) => {
                      const isSuspended = m.suspended_at !== null
                      const isDeleted   = m.deleted_at !== null
                      const isAgreed    = m.terms_agreed_at !== null
                      const statusLabel = isDeleted ? '탈퇴' : isSuspended ? '정지' : '정상'
                      const statusColor = isDeleted ? '#9ca3af' : isSuspended ? '#dc2626' : '#16a34a'
                      return (
                        <tr key={m.id} style={{
                          borderBottom: i < rows.length - 1 ? '1px solid var(--line-subtle)' : 'none',
                        }}>
                          <td style={{ padding: '12px 16px', font: '600 13px/1.3 var(--font-sans)' }}>
                            {m.nickname ?? '—'}
                          </td>
                          <td style={{ padding: '12px 16px', font: '500 13px/1.3 var(--font-sans)', color: 'var(--fg-sec)' }}>
                            {m.cafe_nickname ?? '—'}
                          </td>
                          <td style={{ padding: '12px 16px' }}>
                            <span className="chip sm">{m.role}</span>
                          </td>
                          <td style={{ padding: '12px 16px', font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                            {formatDate(m.created_at)}
                          </td>
                          <td style={{ padding: '12px 16px', font: '500 12px/1.4 var(--font-sans)', color: isAgreed ? '#16a34a' : '#dc2626' }}>
                            {isAgreed ? '동의' : '미동의'}
                          </td>
                          <td style={{ padding: '12px 16px' }}>
                            <span style={{
                              display: 'inline-block',
                              padding: '3px 8px',
                              borderRadius: 4,
                              font: '600 11px/1 var(--font-sans)',
                              color: '#fff',
                              background: statusColor,
                            }}>{statusLabel}</span>
                          </td>
                          <td style={{ padding: '12px 16px' }}>
                            {isDeleted ? '—' : <MemberActions memberId={m.id} isSuspended={isSuspended} />}
                          </td>
                        </tr>
                      )
                    })}
                  </tbody>
                </table>
              </div>
            )}
          </main>
        </div>
      )
    }
    ```

    파일 4: `src/app/admin/reports/page.tsx` — pending 우선 + accepted/rejected 별도 섹션:

    ```typescript
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import type { Metadata } from 'next'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'
    import { ReportActions } from '@/components/admin/ReportActions'

    export const revalidate = 0
    export const metadata: Metadata = { title: '관리자 · 신고 큐' }

    type ReportStatus = 'pending' | 'accepted' | 'rejected'

    interface ReportRow {
      id: string
      reporter_id: string | null
      target_type: 'review' | 'user' | 'ad'
      target_id: string
      reason: string
      status: ReportStatus
      created_at: string
    }

    const STATUS_LABEL: Record<ReportStatus, string> = {
      pending:  '대기 중',
      accepted: '처리 완료',
      rejected: '기각',
    }
    const STATUS_COLOR: Record<ReportStatus, string> = {
      pending:  '#d97706',
      accepted: '#16a34a',
      rejected: '#dc2626',
    }
    const TARGET_LABEL: Record<'review' | 'user' | 'ad', string> = {
      review: '후기',
      user:   '회원',
      ad:     '광고',
    }

    function formatDate(s: string) {
      return new Date(s).toLocaleString('ko-KR', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' })
    }

    export default async function AdminReportsPage() {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login?next=/admin/reports')

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single()

      if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
        redirect('/')
      }

      const adminClient = createSupabaseAdminClient()
      const { data: reports } = await adminClient
        .from('reports')
        .select('id, reporter_id, target_type, target_id, reason, status, created_at')
        .order('status', { ascending: true })  // pending 먼저
        .order('created_at', { ascending: false })
        .limit(200)

      const rows = (reports ?? []) as ReportRow[]
      const pendingCount = rows.filter(r => r.status === 'pending').length

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
            <span style={{ font: '600 14px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
              관리자 · 신고 큐
            </span>
          </header>

          <main style={{ maxWidth: 1100, margin: '0 auto', padding: '28px 32px' }}>
            <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 20px' }}>
              신고 큐 (대기 {pendingCount}건 / 전체 {rows.length}건)
            </h1>
            {rows.length === 0 ? (
              <div className="card" style={{ padding: 40, textAlign: 'center', color: 'var(--fg-tertiary)' }}>
                신고가 없습니다.
              </div>
            ) : (
              <div className="card" style={{ padding: 0, overflow: 'hidden' }}>
                <table style={{ width: '100%', borderCollapse: 'collapse' }}>
                  <thead>
                    <tr style={{ borderBottom: '1px solid var(--line-default)', background: 'var(--bg-surface-2)' }}>
                      {['일시', '대상', '대상 ID', '사유', '상태', '액션'].map(h => (
                        <th key={h} scope="col" style={{
                          padding: '10px 16px',
                          font: '600 12px/1 var(--font-sans)',
                          color: 'var(--fg-sec)',
                          textAlign: 'left',
                        }}>{h}</th>
                      ))}
                    </tr>
                  </thead>
                  <tbody>
                    {rows.map((r, i) => (
                      <tr key={r.id} style={{
                        borderBottom: i < rows.length - 1 ? '1px solid var(--line-subtle)' : 'none',
                      }}>
                        <td style={{ padding: '12px 16px', font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)', whiteSpace: 'nowrap' }}>
                          {formatDate(r.created_at)}
                        </td>
                        <td style={{ padding: '12px 16px' }}>
                          <span className="chip sm">{TARGET_LABEL[r.target_type]}</span>
                        </td>
                        <td style={{ padding: '12px 16px', font: '500 11px/1.4 monospace', color: 'var(--fg-tertiary)' }}>
                          {r.target_id.slice(0, 8)}…
                        </td>
                        <td style={{ padding: '12px 16px', font: '500 13px/1.4 var(--font-sans)', maxWidth: 360 }}>
                          {r.reason}
                        </td>
                        <td style={{ padding: '12px 16px' }}>
                          <span style={{
                            display: 'inline-block',
                            padding: '3px 8px',
                            borderRadius: 4,
                            font: '600 11px/1 var(--font-sans)',
                            color: '#fff',
                            background: STATUS_COLOR[r.status],
                          }}>{STATUS_LABEL[r.status]}</span>
                        </td>
                        <td style={{ padding: '12px 16px' }}>
                          <ReportActions reportId={r.id} status={r.status} />
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            )}
          </main>
        </div>
      )
    }
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/components/admin/MemberActions.tsx` 종료 코드 0
    - `test -f src/components/admin/ReportActions.tsx` 종료 코드 0
    - `test -f src/app/admin/members/page.tsx` 종료 코드 0
    - `test -f src/app/admin/reports/page.tsx` 종료 코드 0
    - `grep -c "'use client'" src/components/admin/MemberActions.tsx` 정확히 1
    - `grep -c "'use client'" src/components/admin/ReportActions.tsx` 정확히 1
    - `grep -c "suspendMember\\|reactivateMember" src/components/admin/MemberActions.tsx` ≥ 2
    - `grep -c "resolveReport" src/components/admin/ReportActions.tsx` ≥ 1
    - `grep -c "aria-label" src/components/admin/MemberActions.tsx` ≥ 2
    - `grep -c "aria-label" src/components/admin/ReportActions.tsx` ≥ 2
    - `grep "redirect('/login?next=/admin/members')" src/app/admin/members/page.tsx` 1건 매치
    - `grep "redirect('/login?next=/admin/reports')" src/app/admin/reports/page.tsx` 1건 매치
    - `grep -c "'admin', 'superadmin'" src/app/admin/members/page.tsx` ≥ 1
    - `grep -c "'admin', 'superadmin'" src/app/admin/reports/page.tsx` ≥ 1
    - `grep "from('profiles')" src/app/admin/members/page.tsx` ≥ 2 (auth profile + member list)
    - `grep "from('reports')" src/app/admin/reports/page.tsx` ≥ 1
    - `grep -c "<th" src/app/admin/members/page.tsx` ≥ 1
    - `grep "scope=\"col\"" src/app/admin/members/page.tsx` ≥ 1
    - `grep "scope=\"col\"" src/app/admin/reports/page.tsx` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>회원 관리 + 신고 큐 페이지가 admin guard, adminClient 데이터 조회, 시맨틱 테이블, 클라이언트 액션 버튼을 포함한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /admin/status 시스템 모니터링 (8개 COUNT + ingest_runs 최근 10건)</name>
  <files>src/app/admin/status/page.tsx</files>
  <read_first>
    - src/app/admin/ads/page.tsx (admin guard + revalidate=0 패턴)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 269-310: status page 정확한 Promise.all 코드)
    - src/__tests__/admin-status.test.ts (Wave 0 RED — 4개 쿼리 검증)
  </read_first>
  <behavior>
    Test 1: profiles 카운트 쿼리 정상 (count: number 반환, error: null)
    Test 2: terms_agreed_at IS NULL 쿼리 정상 (마이그레이션 적용 검증)
    Test 3: reports pending 카운트 정상 (신규 테이블 검증)
    Test 4: ingest_runs 최근 10건 정상 (배열 반환)

    페이지 동작:
    - revalidate=0 (D-15)
    - 8개 COUNT 동시 실행 (Promise.all)
    - 3개 섹션: DB 현황 / Cron 실행 이력 / 대기 항목 (D-14)
  </behavior>
  <action>
    `src/app/admin/status/page.tsx` 신규 작성. PATTERNS.md 라인 285-310 Promise.all 패턴 정확히 적용:

    ```typescript
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import type { Metadata } from 'next'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'

    export const revalidate = 0
    export const metadata: Metadata = { title: '관리자 · 시스템 상태' }

    interface RunRow {
      source: string
      status: string
      created_at: string
    }

    function formatDateTime(s: string) {
      return new Date(s).toLocaleString('ko-KR', {
        month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit',
      })
    }

    export default async function AdminStatusPage() {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login?next=/admin/status')

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single()

      if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
        redirect('/')
      }

      const adminClient = createSupabaseAdminClient()

      const [
        memberRes,
        complexRes,
        txRes,
        activeAdRes,
        runsRes,
        pendingReportRes,
        pendingAdRes,
        noConsentRes,
      ] = await Promise.all([
        adminClient.from('profiles').select('*', { count: 'exact', head: true }),
        adminClient.from('complexes').select('*', { count: 'exact', head: true }),
        adminClient.from('transactions').select('*', { count: 'exact', head: true })
          .is('cancel_date', null).is('superseded_by', null),
        adminClient.from('ad_campaigns').select('*', { count: 'exact', head: true })
          .eq('status', 'approved'),
        adminClient.from('ingest_runs').select('source, status, created_at')
          .order('created_at', { ascending: false }).limit(10),
        adminClient.from('reports').select('*', { count: 'exact', head: true })
          .eq('status', 'pending'),
        adminClient.from('ad_campaigns').select('*', { count: 'exact', head: true })
          .eq('status', 'pending'),
        adminClient.from('profiles').select('*', { count: 'exact', head: true })
          .is('terms_agreed_at', null),
      ])

      const runs = (runsRes.data ?? []) as RunRow[]

      const dbStats = [
        { label: '회원 수',    value: memberRes.count ?? 0 },
        { label: '단지 수',    value: complexRes.count ?? 0 },
        { label: '거래 수 (유효)', value: txRes.count ?? 0 },
        { label: '게재 광고 수', value: activeAdRes.count ?? 0 },
      ]
      const queueStats = [
        { label: '대기 신고',     value: pendingReportRes.count ?? 0, alert: (pendingReportRes.count ?? 0) > 0 },
        { label: '광고 검토 대기', value: pendingAdRes.count ?? 0,    alert: (pendingAdRes.count ?? 0) > 0 },
        { label: '약관 미동의 회원', value: noConsentRes.count ?? 0,  alert: false },
      ]

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
            <span style={{ font: '600 14px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
              관리자 · 시스템 상태
            </span>
          </header>

          <main style={{ maxWidth: 1100, margin: '0 auto', padding: '28px 32px' }}>
            <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 20px' }}>
              시스템 상태
            </h1>

            {/* Section 1: DB 현황 */}
            <section aria-labelledby="db-heading" style={{ marginBottom: 24 }}>
              <h2 id="db-heading" style={{ font: '700 14px/1.4 var(--font-sans)', margin: '0 0 12px' }}>
                DB 현황
              </h2>
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))', gap: 12 }}>
                {dbStats.map(s => (
                  <div key={s.label} className="card" style={{ padding: 16, display: 'flex', flexDirection: 'column', gap: 4 }}>
                    <span className="tnum" style={{ font: '700 24px/1 var(--font-sans)' }}>{s.value.toLocaleString('ko-KR')}</span>
                    <span style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-sec)' }}>{s.label}</span>
                  </div>
                ))}
              </div>
            </section>

            {/* Section 2: Cron 실행 이력 */}
            <section aria-labelledby="cron-heading" style={{ marginBottom: 24 }}>
              <h2 id="cron-heading" style={{ font: '700 14px/1.4 var(--font-sans)', margin: '0 0 12px' }}>
                Cron 실행 이력 (최근 10건)
              </h2>
              {runs.length === 0 ? (
                <div className="card" style={{ padding: 16, color: 'var(--fg-tertiary)' }}>
                  실행 이력이 없습니다.
                </div>
              ) : (
                <div className="card" style={{ padding: 0, overflow: 'hidden' }}>
                  <table style={{ width: '100%', borderCollapse: 'collapse' }}>
                    <thead>
                      <tr style={{ borderBottom: '1px solid var(--line-default)', background: 'var(--bg-surface-2)' }}>
                        {['일시', '소스', '상태'].map(h => (
                          <th key={h} scope="col" style={{
                            padding: '10px 16px',
                            font: '600 12px/1 var(--font-sans)',
                            color: 'var(--fg-sec)',
                            textAlign: 'left',
                          }}>{h}</th>
                        ))}
                      </tr>
                    </thead>
                    <tbody>
                      {runs.map((r, i) => (
                        <tr key={`${r.source}_${r.created_at}_${i}`} style={{
                          borderBottom: i < runs.length - 1 ? '1px solid var(--line-subtle)' : 'none',
                        }}>
                          <td style={{ padding: '10px 16px', font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-tertiary)' }}>
                            {formatDateTime(r.created_at)}
                          </td>
                          <td style={{ padding: '10px 16px', font: '600 13px/1.3 var(--font-sans)' }}>
                            {r.source}
                          </td>
                          <td style={{ padding: '10px 16px' }}>
                            <span style={{
                              display: 'inline-block',
                              padding: '3px 8px',
                              borderRadius: 4,
                              font: '600 11px/1 var(--font-sans)',
                              color: '#fff',
                              background: r.status === 'success' ? '#16a34a' : r.status === 'failed' ? '#dc2626' : '#6b7280',
                            }}>{r.status}</span>
                          </td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              )}
            </section>

            {/* Section 3: 대기 항목 */}
            <section aria-labelledby="queue-heading" style={{ marginBottom: 24 }}>
              <h2 id="queue-heading" style={{ font: '700 14px/1.4 var(--font-sans)', margin: '0 0 12px' }}>
                대기 항목
              </h2>
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))', gap: 12 }}>
                {queueStats.map(s => (
                  <div key={s.label} className="card" style={{
                    padding: 16,
                    display: 'flex',
                    flexDirection: 'column',
                    gap: 4,
                    border: s.alert ? '1px solid #d97706' : undefined,
                  }}>
                    <span className="tnum" style={{
                      font: '700 24px/1 var(--font-sans)',
                      color: s.alert ? '#d97706' : undefined,
                    }}>{s.value.toLocaleString('ko-KR')}</span>
                    <span style={{ font: '500 12px/1.4 var(--font-sans)', color: 'var(--fg-sec)' }}>{s.label}</span>
                  </div>
                ))}
              </div>
            </section>
          </main>
        </div>
      )
    }
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/app/admin/status/page.tsx` 종료 코드 0
    - `grep -c "Promise.all" src/app/admin/status/page.tsx` ≥ 1
    - `grep -c "from('profiles')" src/app/admin/status/page.tsx` ≥ 3 (auth + total + no_consent)
    - `grep -c "from('complexes')" src/app/admin/status/page.tsx` ≥ 1
    - `grep -c "from('transactions')" src/app/admin/status/page.tsx` ≥ 1
    - `grep -c "from('ad_campaigns')" src/app/admin/status/page.tsx` ≥ 2 (approved + pending)
    - `grep -c "from('ingest_runs')" src/app/admin/status/page.tsx` ≥ 1
    - `grep -c "from('reports')" src/app/admin/status/page.tsx` ≥ 1
    - `grep "is('cancel_date', null)" src/app/admin/status/page.tsx` 1건 매치
    - `grep "is('superseded_by', null)" src/app/admin/status/page.tsx` 1건 매치
    - `grep "is('terms_agreed_at', null)" src/app/admin/status/page.tsx` 1건 매치
    - `grep -c "<section" src/app/admin/status/page.tsx` ≥ 3 (3 섹션 D-14)
    - `grep -c "aria-labelledby" src/app/admin/status/page.tsx` ≥ 3 (시맨틱 a11y)
    - `grep "export const revalidate = 0" src/app/admin/status/page.tsx` 1건 매치 (D-15)
    - `npm run test -- --run src/__tests__/admin-status.test.ts` 종료 코드 0
    - `npm run build` 종료 코드 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/__tests__/admin-status.test.ts</automated>
  </verify>
  <done>/admin/status가 8개 카운트와 cron 이력을 단일 요청에서 표시하고, 3개 시맨틱 섹션 + revalidate=0 + admin-status.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | admin role이 아닌 사용자가 액션 호출 시도 |
| Admin client → profiles/reports | RLS 우회 권한이 잘못된 컬럼을 변경할 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Elevation of Privilege | 비-admin이 admin-actions.ts Server Action 호출 | mitigate | Task 1 requireAdmin이 모든 액션 진입점에서 role 체크 + 비-admin이면 error 반환 (이그노러블 우회 불가능 — Server Action은 항상 서버에서 실행됨) |
| T-03-18 | Tampering | resolveReport(action='hack') 같은 비정상 입력 | mitigate | Task 1 첫 줄 `if (action !== 'accepted' && action !== 'rejected') return { error }` |
| T-03-19 | Information Disclosure | /admin/* 페이지 직접 URL 진입 시 민감 데이터 노출 | mitigate | 모든 admin 페이지에 `redirect('/login?next=/admin/X')` + `redirect('/')` 가드 (Task 2, 3) |
| T-03-20 | Tampering | suspendMember로 admin/superadmin role 정지 | accept | DB 변경은 suspended_at만 (role 변경 없음) — 정지된 admin은 다시 admin이 풀어주면 됨 |
| T-03-21 | DoS | /admin/status 8 COUNT 쿼리 동시 실행으로 DB 부하 | accept | Promise.all + revalidate=0이지만 admin만 접근 + 호출 빈도 낮음 |
</threat_model>

<verification>
1. `npm run lint` 통과
2. `npm run build` 통과 (모든 admin 페이지 빌드)
3. `npm run test -- --run src/__tests__/admin-actions.test.ts src/__tests__/admin-status.test.ts src/__tests__/ads.test.ts` 통과 (ADMIN-02 광고 상태 머신 회귀 포함)
4. 수동 검증:
   - admin 사용자로 /admin/members 진입 → 회원 목록 보임
   - 정지 버튼 → DB profiles.suspended_at 값 확인 → 다시 정지 해제
   - /admin/reports에 pending 신고 추가 후 처리 완료/기각 → DB reports.status 변경 확인
   - /admin/status 진입 → 8개 카운트 표시 + ingest_runs 최근 10건
   - 비-admin으로 모든 admin URL → / 리다이렉트
   - /admin/ads 기존 페이지 회귀 (ADMIN-02 retain): 광고 승인/거절 버튼 정상 작동
</verification>

<success_criteria>
- ADMIN-01: 회원 정지/해제 동작
- ADMIN-02: 기존 광고 검수 페이지 무회귀 — `npm run test -- --run src/__tests__/ads.test.ts` 통과 (광고 상태 머신 회귀 자동 검증)
- ADMIN-03: 신고 큐 처리/기각 동작
- ADMIN-04: 시스템 상태 페이지 8 카운트 + cron 이력 표시
- admin-actions.test.ts GREEN
- admin-status.test.ts GREEN
- 비-admin 접근 차단 검증 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-cardnews-legal-ops/03-04-SUMMARY.md` with:
- 추가 파일 + 라인 수
- 3개 신규 Server Action 시그니처
- 4개 페이지 URL + 진입 권한 매트릭스
- ADMIN-02 회귀 확인 결과 (광고 승인/거절 버튼 정상 작동)
- /admin/status 8 카운트 쿼리 응답 시간 (가능하면 측정)
- 발견된 이슈
</output>

---
phase: 03-cardnews-legal-ops
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - src/lib/auth/consent-actions.ts
  - src/app/auth/callback/route.ts
  - src/app/consent/page.tsx
  - src/app/reactivate/page.tsx
  - src/app/profile/page.tsx
  - src/app/api/ingest/molit-trade/route.ts
  - src/app/legal/terms/page.tsx
  - src/app/legal/privacy/page.tsx
  - src/app/legal/ad-policy/page.tsx
  - src/components/layout/Footer.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - LEGAL-01
  - LEGAL-02
  - LEGAL-03
  - LEGAL-04
  - LEGAL-05

must_haves:
  truths:
    - "첫 로그인 사용자는 /auth/callback에서 /consent?next=...로 자동 리다이렉트된다"
    - "탈퇴(deleted_at IS NOT NULL) 사용자는 /auth/callback에서 /reactivate로 리다이렉트된다"
    - "/consent에서 두 체크박스를 선택하고 제출하면 profiles.terms_agreed_at가 now()로 설정되고 next로 이동한다"
    - "/profile 하단 위험 구역에서 탈퇴 버튼 클릭 시 deleted_at이 설정되고 signOut된다"
    - "/reactivate에서 재활성화 버튼 클릭 시 deleted_at이 NULL로 복구된다"
    - "Vercel cron (/api/ingest/molit-trade)이 실행될 때 deleted_at < now() - 30일 사용자에 대해 supabase.auth.admin.deleteUser() 호출이 일어난다"
    - "/legal/terms, /legal/privacy, /legal/ad-policy 3개 정적 페이지가 200으로 응답한다"
    - "Footer가 admin 경로 외 모든 페이지에 표시되고 3개 법적 링크 + mailto:SUPPORT_EMAIL을 포함한다"
    - "src/__tests__/consent-actions.test.ts의 비로그인 케이스가 GREEN으로 통과한다"
  artifacts:
    - path: "src/lib/auth/consent-actions.ts"
      provides: "agreeToTerms / deleteAccount / reactivateAccount Server Actions"
      exports: ["agreeToTerms", "deleteAccount", "reactivateAccount"]
    - path: "src/app/consent/page.tsx"
      provides: "동의 페이지 (체크박스 2개 + Server Action 폼)"
      contains: "agreeToTerms"
    - path: "src/app/reactivate/page.tsx"
      provides: "탈퇴 grace 재활성화 안내 페이지"
      contains: "reactivateAccount"
    - path: "src/components/layout/Footer.tsx"
      provides: "공통 footer (법적 링크 + mailto)"
      contains: "/legal/terms"
    - path: "src/app/legal/terms/page.tsx"
      provides: "이용약관 정적 페이지"
      min_lines: 40
    - path: "src/app/legal/privacy/page.tsx"
      provides: "개인정보처리방침 정적 페이지 (개인정보보호법 §30 8개 항목)"
      min_lines: 60
    - path: "src/app/legal/ad-policy/page.tsx"
      provides: "광고 정책 정적 페이지 (표시광고법)"
      min_lines: 40
  key_links:
    - from: "src/app/auth/callback/route.ts"
      to: "/consent and /reactivate"
      via: "redirect after profiles status check"
      pattern: "redirect\\(.*/(consent|reactivate)"
    - from: "src/lib/auth/consent-actions.ts"
      to: "createSupabaseAdminClient()"
      via: "RLS 우회 profiles.update (Pitfall 2)"
      pattern: "createSupabaseAdminClient"
    - from: "src/app/api/ingest/molit-trade/route.ts"
      to: "supabase.auth.admin.deleteUser"
      via: "30일 경과 hard delete cron"
      pattern: "auth\\.admin\\.deleteUser"
    - from: "src/components/layout/Footer.tsx"
      to: "src/app/layout.tsx"
      via: "조건부 렌더 (admin 경로 제외)"
      pattern: "Footer"
---

<objective>
Wave 1: 법적 요건(LEGAL-01~05)을 한 묶음으로 완성한다.

Purpose:
- LEGAL-01 동의 흐름 + LEGAL-04 탈퇴/재활성화/hard delete가 같은 데이터 모델(profiles)을 공유하므로 묶어서 처리.
- LEGAL-02/03/05 정적 콘텐츠 + Footer는 동의 흐름과 같은 라우트 구조에서 함께 작업해야 누락 방지.
- Wave 2 카드뉴스/어드민이 시작되기 전에 모든 사용자 흐름의 법적 게이트가 막혀 있어야 한다.

Output:
- consent-actions.ts (3개 Server Action)
- /consent, /reactivate, /legal/{terms,privacy,ad-policy} 5개 신규 페이지
- /auth/callback 수정 (동의·탈퇴 리다이렉트 삽입)
- /api/ingest/molit-trade 수정 (hard delete cron 로직 추가)
- /profile 수정 (탈퇴 버튼 위험 구역 추가)
- Footer.tsx 신규 + layout.tsx 조건부 삽입
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-cardnews-legal-ops/03-CONTEXT.md
@.planning/phases/03-cardnews-legal-ops/03-RESEARCH.md
@.planning/phases/03-cardnews-legal-ops/03-PATTERNS.md
@.planning/phases/03-cardnews-legal-ops/03-01-SUMMARY.md
@src/app/auth/callback/route.ts
@src/lib/auth/review-actions.ts
@src/lib/auth/actions.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/app/profile/page.tsx
@src/app/api/ingest/molit-trade/route.ts
@src/app/layout.tsx

<interfaces>
<!-- Wave 0이 만든 인터페이스 -->

profiles 컬럼 (이미 마이그레이션됨):
- deleted_at: string | null
- terms_agreed_at: string | null
- suspended_at: string | null  (Wave 2 admin actions에서 사용)

기존 함수 (재사용):
- createSupabaseServerClient(): cookies 기반, RLS 적용
- createSupabaseAdminClient(): service_role, RLS 우회 (terms_agreed_at/deleted_at 업데이트 시 필수 — RESEARCH.md Pitfall 2)
- redirect from 'next/navigation': Server Action 내에서 직접 호출 가능

cron 환경변수:
- CRON_SECRET (이미 사용 중, /api/ingest/molit-trade 라인 14-17)

외부 환경변수:
- SUPPORT_EMAIL: D-18 — 코드에서 fallback `support@danjiondo.com` 사용

Server Action 반환 타입 (review-actions.ts 라인 14 참조):
type ServerActionResult = { error: string | null }

리다이렉트 쿼리 파라미터:
- /consent?next=<encoded-path>  (auth/callback에서 설정)
- /reactivate (next 파라미터 없음 — 재활성화 후 / 로 이동)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: consent-actions.ts 작성 (agreeToTerms / deleteAccount / reactivateAccount)</name>
  <files>src/lib/auth/consent-actions.ts</files>
  <read_first>
    - src/lib/auth/review-actions.ts (Server Action 반환 타입 + auth check 패턴 라인 1-38)
    - src/lib/auth/actions.ts (signOut + redirect 패턴)
    - src/lib/supabase/admin.ts (createSupabaseAdminClient 사용법)
    - src/lib/supabase/server.ts (createSupabaseServerClient 사용법)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 325-368: Pattern 3 정확한 코드)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 417-468: RLS workaround 코드)
    - src/__tests__/consent-actions.test.ts (Wave 0에서 만든 RED 테스트 — 통과해야 할 케이스)
  </read_first>
  <behavior>
    Test 1: 비로그인 호출 → `{ error: '로그인이 필요합니다.' }` 반환 (3개 액션 모두)
    Test 2: agreeToTerms — terms 또는 privacy 체크박스 누락 → `{ error: '이용약관과 개인정보처리방침에 모두 동의해야 합니다.' }`
    Test 3: agreeToTerms — admin client로 profiles.terms_agreed_at 업데이트 (RLS 우회)
    Test 4: deleteAccount — admin client로 profiles.deleted_at = now() 설정 후 supabase.auth.signOut()
    Test 5: reactivateAccount — admin client로 profiles.deleted_at = NULL 설정
  </behavior>
  <action>
    `src/lib/auth/consent-actions.ts` 신규 작성. RLS Pitfall 2(`profiles: owner update with check role in ('user')`) 우회 위해 모든 profiles UPDATE는 `createSupabaseAdminClient()` 경유:

    ```typescript
    'use server'

    import { redirect } from 'next/navigation'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { createSupabaseAdminClient } from '@/lib/supabase/admin'
    import { revalidatePath } from 'next/cache'

    /**
     * LEGAL-01: 이용약관·개인정보처리방침 동의 (D-10, D-12, D-13)
     * /consent 페이지에서 호출. RLS 우회 위해 admin client 사용 (RESEARCH.md Pitfall 2).
     */
    export async function agreeToTerms(
      formData: FormData,
    ): Promise<{ error: string | null }> {
      const next  = (formData.get('next')   as string | null) ?? '/'
      const terms = formData.get('terms')   === 'on'
      const privacy = formData.get('privacy') === 'on'

      if (!terms || !privacy) {
        return { error: '이용약관과 개인정보처리방침에 모두 동의해야 합니다.' }
      }

      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { error: '로그인이 필요합니다.' }

      const adminClient = createSupabaseAdminClient()
      const { error } = await adminClient
        .from('profiles')
        .update({ terms_agreed_at: new Date().toISOString() })
        .eq('id', user.id)

      if (error) return { error: error.message }
      revalidatePath('/consent')
      redirect(next)
    }

    /**
     * LEGAL-04: 탈퇴 (D-06) — soft delete + signOut. 30일 grace 후 hard delete cron이 처리.
     */
    export async function deleteAccount(): Promise<{ error: string | null }> {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { error: '로그인이 필요합니다.' }

      const adminClient = createSupabaseAdminClient()
      const { error } = await adminClient
        .from('profiles')
        .update({ deleted_at: new Date().toISOString() })
        .eq('id', user.id)

      if (error) return { error: error.message }
      // 세션 종료 (서버 사이드)
      await supabase.auth.signOut()
      redirect('/')
    }

    /**
     * LEGAL-04: 재활성화 (D-06) — /reactivate 페이지에서 호출. 30일 이내 deleted_at 복구.
     */
    export async function reactivateAccount(): Promise<{ error: string | null }> {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { error: '로그인이 필요합니다.' }

      const adminClient = createSupabaseAdminClient()
      const { error } = await adminClient
        .from('profiles')
        .update({ deleted_at: null })
        .eq('id', user.id)

      if (error) return { error: error.message }
      revalidatePath('/')
      redirect('/')
    }
    ```

    NOTE: `redirect()`는 throw 기반이므로 호출 후 코드는 실행되지 않음. 함수 시그니처는 `Promise<{ error: string | null }>`이지만 success path에서는 redirect로 종료 — Next.js Server Action 표준 패턴.
  </action>
  <acceptance_criteria>
    - `grep -c "'use server'" src/lib/auth/consent-actions.ts` 정확히 1
    - `grep -c "createSupabaseAdminClient" src/lib/auth/consent-actions.ts` ≥ 3 (3개 액션)
    - `grep -c "export async function agreeToTerms" src/lib/auth/consent-actions.ts` 정확히 1
    - `grep -c "export async function deleteAccount" src/lib/auth/consent-actions.ts` 정확히 1
    - `grep -c "export async function reactivateAccount" src/lib/auth/consent-actions.ts` 정확히 1
    - `grep -c "deleted_at:" src/lib/auth/consent-actions.ts` ≥ 2 (set + null)
    - `grep -c "terms_agreed_at:" src/lib/auth/consent-actions.ts` ≥ 1
    - `npm run test -- --run src/__tests__/consent-actions.test.ts` 종료 코드 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/__tests__/consent-actions.test.ts</automated>
  </verify>
  <done>3개 Server Action이 export되고 RLS 우회 admin client 패턴으로 작성되었으며 RED 테스트가 GREEN으로 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: auth/callback 수정 + /consent, /reactivate 페이지 + /profile 탈퇴 버튼</name>
  <files>src/app/auth/callback/route.ts, src/app/consent/page.tsx, src/app/reactivate/page.tsx, src/app/profile/page.tsx</files>
  <read_first>
    - src/app/auth/callback/route.ts (현재 16라인 — 삽입 위치 확인)
    - src/app/profile/page.tsx (라인 220-234: signOut 폼 패턴 — 탈퇴 버튼은 그 아래 별도 위험 구역에 배치)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 282-322: Pattern 2 정확한 삽입 코드)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 314-365: consent page 폼 / 라인 367-376: reactivate page / 라인 509-549: callback 삽입점)
    - src/lib/auth/consent-actions.ts (Task 1 산출물 — 3개 액션 export)
  </read_first>
  <action>
    파일 1: `src/app/auth/callback/route.ts` 수정. 기존 라인 14-15(`if (error) redirect('/login?error=auth')` + `redirect(next)`) 사이에 profiles 상태 체크 삽입:

    ```typescript
    import { redirect } from 'next/navigation'
    import { createSupabaseServerClient } from '@/lib/supabase/server'

    export async function GET(request: Request): Promise<never> {
      const { searchParams } = new URL(request.url)
      const code  = searchParams.get('code')
      const next  = searchParams.get('next') ?? '/'

      if (!code) redirect('/login')

      const supabase = await createSupabaseServerClient()
      const { error } = await supabase.auth.exchangeCodeForSession(code)
      if (error) redirect('/login?error=auth')

      // Phase 3: profiles 상태 체크 (D-06 deleted_at, D-10 terms_agreed_at)
      const { data: { user } } = await supabase.auth.getUser()
      if (user) {
        const { data: profile } = await supabase
          .from('profiles')
          .select('deleted_at, terms_agreed_at')
          .eq('id', user.id)
          .single()

        if (profile?.deleted_at) {
          redirect('/reactivate')
        }
        if (!profile?.terms_agreed_at) {
          redirect(`/consent?next=${encodeURIComponent(next)}`)
        }
      }

      redirect(next)
    }
    ```

    파일 2: `src/app/consent/page.tsx` 신규 작성. profile/page.tsx 헤더 패턴 + Server Action 폼:

    ```typescript
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import type { Metadata } from 'next'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { agreeToTerms } from '@/lib/auth/consent-actions'

    export const revalidate = 0
    export const metadata: Metadata = { title: '서비스 이용 동의' }

    interface SearchParams {
      searchParams: Promise<{ next?: string }>
    }

    export default async function ConsentPage({ searchParams }: SearchParams) {
      const { next = '/' } = await searchParams
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect(`/login?next=${encodeURIComponent(`/consent?next=${next}`)}`)

      // 이미 동의했으면 next로 바로 이동
      const { data: profile } = await supabase
        .from('profiles')
        .select('terms_agreed_at')
        .eq('id', user.id)
        .single()
      if ((profile as { terms_agreed_at: string | null } | null)?.terms_agreed_at) {
        redirect(next)
      }

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
          </header>
          <main style={{ maxWidth: 640, margin: '0 auto', padding: '40px 24px' }}>
            <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', marginBottom: 16 }}>
              서비스 이용 동의
            </h1>
            <p style={{ font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: 24 }}>
              단지온도를 이용하시려면 아래 두 항목에 모두 동의해주세요.
            </p>
            <form action={agreeToTerms} className="card" style={{ padding: 24, display: 'flex', flexDirection: 'column', gap: 16 }}>
              <input type="hidden" name="next" value={next} />
              <label style={{ display: 'flex', alignItems: 'flex-start', gap: 10, font: '500 14px/1.5 var(--font-sans)' }}>
                <input type="checkbox" name="terms" required style={{ marginTop: 3 }} />
                <span>
                  <Link href="/legal/terms" target="_blank" style={{ color: 'var(--dj-orange)', textDecoration: 'underline' }}>이용약관</Link>에 동의합니다 <span style={{ color: '#dc2626' }}>(필수)</span>
                </span>
              </label>
              <label style={{ display: 'flex', alignItems: 'flex-start', gap: 10, font: '500 14px/1.5 var(--font-sans)' }}>
                <input type="checkbox" name="privacy" required style={{ marginTop: 3 }} />
                <span>
                  <Link href="/legal/privacy" target="_blank" style={{ color: 'var(--dj-orange)', textDecoration: 'underline' }}>개인정보처리방침</Link>에 동의합니다 <span style={{ color: '#dc2626' }}>(필수)</span>
                </span>
              </label>
              <button type="submit" className="btn btn-md btn-primary" style={{ marginTop: 8 }}>
                동의하고 시작하기
              </button>
            </form>
          </main>
        </div>
      )
    }
    ```

    파일 3: `src/app/reactivate/page.tsx` 신규 작성. profile/page.tsx 헤더 + reactivateAccount + deleteAccount 영구 확정 버튼:

    ```typescript
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import type { Metadata } from 'next'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { reactivateAccount } from '@/lib/auth/consent-actions'
    import { signOut } from '@/lib/auth/actions'

    export const revalidate = 0
    export const metadata: Metadata = { title: '계정 재활성화' }

    function formatDate(s: string) {
      const d = new Date(s)
      return `${d.getFullYear()}년 ${d.getMonth() + 1}월 ${d.getDate()}일`
    }

    export default async function ReactivatePage() {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login?next=/reactivate')

      const { data: profile } = await supabase
        .from('profiles')
        .select('deleted_at')
        .eq('id', user.id)
        .single()
      const deletedAt = (profile as { deleted_at: string | null } | null)?.deleted_at
      // 탈퇴 상태가 아니면 / 로
      if (!deletedAt) redirect('/')

      const hardDeleteAt = new Date(new Date(deletedAt).getTime() + 30 * 86_400_000).toISOString()

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
          </header>
          <main style={{ maxWidth: 640, margin: '0 auto', padding: '40px 24px' }}>
            <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', marginBottom: 16 }}>
              계정이 탈퇴 처리되었습니다
            </h1>
            <div className="card" style={{ padding: 24, marginBottom: 16 }}>
              <p style={{ font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: 8 }}>
                탈퇴 일시: <strong>{formatDate(deletedAt)}</strong>
              </p>
              <p style={{ font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-sec)' }}>
                {formatDate(hardDeleteAt)}까지 재활성화하지 않으면 모든 데이터가 영구 삭제됩니다.
              </p>
            </div>
            <form action={reactivateAccount}>
              <button type="submit" className="btn btn-md btn-primary" style={{ width: '100%', marginBottom: 12 }}>
                계정 재활성화
              </button>
            </form>
            <form action={signOut}>
              <button type="submit" className="btn btn-md btn-ghost" style={{ width: '100%', color: 'var(--fg-tertiary)' }}>
                로그아웃 (30일 후 자동 영구 삭제)
              </button>
            </form>
          </main>
        </div>
      )
    }
    ```

    파일 4: `src/app/profile/page.tsx` 수정. 라인 234(로그아웃 form) 아래에 위험 구역 + 탈퇴 버튼 추가. 기존 코드 유지하고 다음을 마지막 form 직후에 삽입:

    ```typescript
    import { deleteAccount } from '@/lib/auth/consent-actions'

    // ... 기존 로그아웃 form 다음에:
    {/* 위험 구역 — 탈퇴 (LEGAL-04, D-06) */}
    <div style={{ marginTop: 32, paddingTop: 24, borderTop: '1px solid var(--line-subtle)' }}>
      <h3 style={{ font: '700 13px/1.3 var(--font-sans)', color: '#dc2626', margin: '0 0 8px' }}>
        위험 구역
      </h3>
      <p style={{ font: '500 12px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 12px' }}>
        탈퇴 후 30일 이내에는 재활성화 가능, 이후 모든 데이터가 영구 삭제됩니다.
      </p>
      <form action={deleteAccount}>
        <button
          type="submit"
          className="btn btn-md btn-ghost"
          style={{ width: '100%', color: '#dc2626', border: '1px solid rgba(220,38,38,0.5)' }}
        >
          계정 탈퇴
        </button>
      </form>
    </div>
    ```

    NOTE: deleteAccount는 redirect로 종료되므로 추가 onClick 핸들러나 confirm() 불필요. 정말 confirm이 필요하면 별도 client component로 분리(이번 plan 범위 외).
  </action>
  <acceptance_criteria>
    - `grep -c "redirect('/reactivate')" src/app/auth/callback/route.ts` 정확히 1
    - `grep -c "redirect(\`/consent?next=" src/app/auth/callback/route.ts` 정확히 1
    - `grep "select('deleted_at, terms_agreed_at')" src/app/auth/callback/route.ts` 1건 매치
    - `grep -c "agreeToTerms" src/app/consent/page.tsx` ≥ 1
    - `grep -c "type=\"checkbox\"" src/app/consent/page.tsx` 정확히 2 (terms + privacy)
    - `grep -c "name=\"terms\"" src/app/consent/page.tsx` 정확히 1
    - `grep -c "name=\"privacy\"" src/app/consent/page.tsx` 정확히 1
    - `grep -c "reactivateAccount" src/app/reactivate/page.tsx` ≥ 1
    - `grep -c "30 \\* 86_400_000\\|30일\\|30 days" src/app/reactivate/page.tsx` ≥ 1
    - `grep -c "deleteAccount" src/app/profile/page.tsx` ≥ 1
    - `grep -c "위험 구역\\|계정 탈퇴" src/app/profile/page.tsx` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>auth/callback이 deleted_at + terms_agreed_at 분기 리다이렉트를 수행하고, /consent와 /reactivate 페이지가 Server Action에 연결되며, /profile에 탈퇴 버튼이 위험 구역에 표시된다.</done>
</task>

<task type="auto">
  <name>Task 3: hard delete cron 로직 추가 + 3개 법적 페이지 + Footer + layout 조건부 삽입</name>
  <files>src/app/api/ingest/molit-trade/route.ts, src/app/legal/terms/page.tsx, src/app/legal/privacy/page.tsx, src/app/legal/ad-policy/page.tsx, src/components/layout/Footer.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/api/ingest/molit-trade/route.ts (현재 코드 + CRON_SECRET 가드 라인 14-17)
    - src/lib/supabase/admin.ts (admin client = service_role)
    - src/app/profile/page.tsx (헤더 라인 56-96 — 법적 페이지가 따라할 시맨틱 헤더)
    - src/app/layout.tsx (현재 RootLayout — Footer 삽입 위치)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 374-393: Pattern 4 hard delete cron / 라인 613-644: 법적 페이지 필수 항목)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 380-411: 법적 페이지 정적 RSC / 라인 791-801: Footer 가이드)
  </read_first>
  <action>
    파일 1: `src/app/api/ingest/molit-trade/route.ts` 수정. 기존 코드 끝부분(`return Response.json({ summary, results })` 직전)에 hard delete 로직 추가:

    ```typescript
    // ── Phase 3 LEGAL-04: 30일 경과 탈퇴 계정 hard delete (D-07) ──
    const thirtyDaysAgo = new Date(Date.now() - 30 * 86_400_000).toISOString()
    const { data: expiredProfiles, error: expErr } = await supabase
      .from('profiles')
      .select('id')
      .not('deleted_at', 'is', null)
      .lt('deleted_at', thirtyDaysAgo)

    let hardDeleted = 0
    let hardDeleteFailed = 0
    if (!expErr) {
      for (const { id } of (expiredProfiles ?? []) as { id: string }[]) {
        const { error: delErr } = await supabase.auth.admin.deleteUser(id)
        if (delErr) {
          hardDeleteFailed += 1
          console.error(`[hard-delete] auth.admin.deleteUser failed for ${id}:`, delErr.message)
          continue
        }
        // auth.users ON DELETE CASCADE → profiles 자동 삭제 (20260430000005_users.sql 라인 3 검증됨)
        hardDeleted += 1
      }
    } else {
      console.error('[hard-delete] expiredProfiles query failed:', expErr.message)
    }

    return Response.json({
      summary: { ...summary, hardDeleted, hardDeleteFailed },
      results,
    })
    ```

    NOTE: 기존 변수 `supabase`(라인 20에서 createSupabaseAdminClient() 결과)를 그대로 재사용. CRON_SECRET 검증은 라인 14-17에 이미 있으므로 추가 작업 없음.

    파일 2: `src/app/legal/terms/page.tsx` 신규 작성. 한국 약관규제법 + 전자상거래법 최소 요건 6개 섹션:

    ```typescript
    import Link from 'next/link'
    import type { Metadata } from 'next'

    export const metadata: Metadata = { title: '이용약관' }

    export default function TermsPage() {
      const SUPPORT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? 'support@danjiondo.com'
      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
          </header>
          <main style={{ maxWidth: 800, margin: '0 auto', padding: '40px 24px' }}>
            <article>
              <h1 style={{ font: '700 28px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 8px' }}>
                이용약관
              </h1>
              <p style={{ font: '500 13px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 32px' }}>
                초안 — 법무 검토 전 / 시행일: 2026년 5월 6일
              </p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제1조 (목적)</h2>
              <p>본 약관은 단지온도(이하 "서비스")가 제공하는 부동산 정보 및 커뮤니티 서비스의 이용 조건과 절차, 회사와 회원 간의 권리·의무·책임사항을 규정함을 목적으로 합니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제2조 (서비스 이용 신청 및 승낙)</h2>
              <p>회원은 본 약관 및 개인정보처리방침에 동의함으로써 회원가입을 완료하며, 서비스는 가입 신청을 승낙합니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제3조 (서비스 이용 제한)</h2>
              <p>회원은 다음 행위를 해서는 안 됩니다: 타인의 개인정보 도용, 허위 게시물 작성, 광고·스팸 게시, 운영자 사칭 등. 위반 시 게시물 삭제 및 계정 정지 조치가 가능합니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제4조 (게시물의 권리 및 책임)</h2>
              <p>회원이 작성한 후기·댓글의 저작권은 회원에게 귀속되며, 서비스는 이를 서비스 운영 목적으로 사용할 수 있습니다. 회원은 자신의 게시물에 대한 법적 책임을 집니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제5조 (면책 조항)</h2>
              <p>본 서비스가 제공하는 부동산 거래가·시세·랭킹·후기 등의 정보는 참고용이며, 실제 투자·거래 판단의 책임은 사용자에게 있습니다. 정보의 정확성·최신성을 보장하지 않으며 정보 활용으로 인한 손실에 대해 책임지지 않습니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제6조 (분쟁 해결 및 관할)</h2>
              <p>서비스 이용과 관련하여 분쟁이 발생할 경우 회사와 회원이 상호 협의하여 해결하며, 협의가 이루어지지 않을 경우 서울중앙지방법원을 관할 법원으로 합니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>제7조 (문의)</h2>
              <p>이용약관에 관한 문의는 <a href={`mailto:${SUPPORT_EMAIL}`} style={{ color: 'var(--dj-orange)' }}>{SUPPORT_EMAIL}</a>로 보내주십시오.</p>
            </article>
          </main>
        </div>
      )
    }
    ```

    파일 3: `src/app/legal/privacy/page.tsx` 신규 작성. 개인정보보호법 §30 8개 항목:

    ```typescript
    import Link from 'next/link'
    import type { Metadata } from 'next'

    export const metadata: Metadata = { title: '개인정보처리방침' }

    export default function PrivacyPage() {
      const SUPPORT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? 'support@danjiondo.com'
      const CPO_NAME  = process.env.NEXT_PUBLIC_PRIVACY_CPO_NAME  ?? '단지온도 개인정보 보호책임자'
      const CPO_EMAIL = process.env.NEXT_PUBLIC_PRIVACY_CPO_EMAIL ?? SUPPORT_EMAIL

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
          </header>
          <main style={{ maxWidth: 800, margin: '0 auto', padding: '40px 24px' }}>
            <article>
              <h1 style={{ font: '700 28px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 8px' }}>
                개인정보처리방침
              </h1>
              <p style={{ font: '500 13px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 32px' }}>
                초안 — 법무 검토 전 / 시행일: 2026년 5월 6일
              </p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>1. 개인정보의 처리 목적</h2>
              <p>회원 식별, 서비스 제공(후기·관심단지·알림), 부정 이용 방지, 법령상 의무 이행.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>2. 처리하는 개인정보 항목</h2>
              <p>필수: 이메일, 닉네임, 가입 OAuth 식별자(Naver), IP 해시. 선택: 카페 닉네임, 푸시 구독 정보.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>3. 개인정보의 처리 및 보유 기간</h2>
              <p>회원 탈퇴 시 30일 grace 기간 후 영구 삭제. 법령상 보유 의무가 있는 경우 해당 기간 동안 분리 보관 후 파기.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>4. 개인정보의 제3자 제공</h2>
              <p>제공하지 않습니다. 단, 법령에 따라 적법한 절차로 요구받은 경우 제공할 수 있습니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>5. 개인정보 처리의 위탁</h2>
              <ul style={{ paddingLeft: 20 }}>
                <li>Supabase Inc. — 인증 및 데이터베이스</li>
                <li>Vercel Inc. — 호스팅 및 배포</li>
                <li>Resend Inc. — 알림 이메일 발송</li>
                <li>Kakao Corp. — 지도 및 주소 검색</li>
              </ul>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>6. 정보주체의 권리·의무 및 행사 방법</h2>
              <p>회원은 언제든지 개인정보 열람·정정·삭제·처리정지 요구를 할 수 있습니다. /profile 페이지에서 직접 또는 아래 보호책임자에게 이메일로 요청하시기 바랍니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>7. 개인정보 보호책임자</h2>
              <p>{CPO_NAME} (<a href={`mailto:${CPO_EMAIL}`} style={{ color: 'var(--dj-orange)' }}>{CPO_EMAIL}</a>)</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>8. 처리방침 시행일 및 변경 이력</h2>
              <p>본 방침은 2026년 5월 6일부터 시행됩니다. 변경 시 사전 공지 후 시행일을 갱신합니다.</p>
            </article>
          </main>
        </div>
      )
    }
    ```

    파일 4: `src/app/legal/ad-policy/page.tsx` 신규 작성. 표시광고법 5개 섹션:

    ```typescript
    import Link from 'next/link'
    import type { Metadata } from 'next'

    export const metadata: Metadata = { title: '광고 정책' }

    export default function AdPolicyPage() {
      const SUPPORT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? 'support@danjiondo.com'
      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
          </header>
          <main style={{ maxWidth: 800, margin: '0 auto', padding: '40px 24px' }}>
            <article>
              <h1 style={{ font: '700 28px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 8px' }}>
                광고 정책
              </h1>
              <p style={{ font: '500 13px/1.5 var(--font-sans)', color: 'var(--fg-tertiary)', margin: '0 0 32px' }}>
                초안 — 법무 검토 전 / 시행일: 2026년 5월 6일 / 표시광고법 준수
              </p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>1. 광고 표시 방법</h2>
              <p>광고는 일반 콘텐츠와 명확히 구분되도록 "광고" 라벨을 부착하여 노출합니다. 지면 위치는 banner_top, sidebar, in_feed 3종으로 한정됩니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>2. 광고주 신원 정보</h2>
              <p>모든 광고에는 광고주 명칭이 표시됩니다. 광고주는 사업자등록 정보를 제출해야 광고가 게재될 수 있습니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>3. 허위·과장 광고 금지</h2>
              <p>표시광고법에 따라 거짓·과장·기만적 표시 또는 광고는 금지됩니다. 부동산 시세·수익률·확정 분양가 등 검증되지 않은 수치 광고는 거부됩니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>4. 광고 심사 절차</h2>
              <p>광고주가 등록한 광고(<code>draft</code>)는 심사 신청(<code>pending</code>) 후 운영자 검수를 거쳐 승인(<code>approved</code>)되어 게재됩니다. 위반 광고는 거절(<code>rejected</code>) 또는 일시중지(<code>paused</code>) 처리됩니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>5. 광고 중단 및 거절 기준</h2>
              <p>본 정책 또는 관련 법령 위반, 사용자 신고 다수 접수, 운영상 부적절 판단 시 광고 중단·거절이 가능하며 사전 통지를 원칙으로 합니다.</p>

              <h2 style={{ font: '700 18px/1.4 var(--font-sans)', margin: '24px 0 12px' }}>문의</h2>
              <p>광고 정책 관련 문의: <a href={`mailto:${SUPPORT_EMAIL}`} style={{ color: 'var(--dj-orange)' }}>{SUPPORT_EMAIL}</a></p>
            </article>
          </main>
        </div>
      )
    }
    ```

    파일 5: `src/components/layout/Footer.tsx` 신규 작성. 시맨틱 footer + AI 슬롭 금지 (CLAUDE.md):

    ```typescript
    import Link from 'next/link'

    /**
     * Phase 3 LEGAL-02/03/05: 공통 footer (3개 법적 링크 + 지원 이메일)
     * AI 슬롭 금지: 그라데이션·blur·glow·보라/인디고 사용 안 함 (CLAUDE.md)
     */
    export function Footer() {
      const SUPPORT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? 'support@danjiondo.com'
      return (
        <footer
          aria-label="사이트 푸터"
          style={{
            borderTop: '1px solid var(--line-subtle)',
            background: 'var(--bg-canvas)',
            padding: '24px 32px',
            font: '500 12px/1.6 var(--font-sans)',
            color: 'var(--fg-sec)',
          }}
        >
          <nav aria-label="법적 정보" style={{ display: 'flex', flexWrap: 'wrap', gap: 16, marginBottom: 8 }}>
            <Link href="/legal/terms" style={{ color: 'var(--fg-sec)' }}>이용약관</Link>
            <Link href="/legal/privacy" style={{ color: 'var(--fg-sec)', fontWeight: 700 }}>개인정보처리방침</Link>
            <Link href="/legal/ad-policy" style={{ color: 'var(--fg-sec)' }}>광고 정책</Link>
            <a href={`mailto:${SUPPORT_EMAIL}`} style={{ color: 'var(--fg-sec)' }}>문의 {SUPPORT_EMAIL}</a>
          </nav>
          <p style={{ margin: 0, color: 'var(--fg-tertiary)' }}>
            © 단지온도 · 창원·김해 부동산 정보 서비스 · 본 사이트의 부동산 정보는 참고용입니다.
          </p>
        </footer>
      )
    }
    ```

    파일 6: `src/app/layout.tsx` 수정. RootLayout에 Footer 삽입 (admin 경로 제외 처리는 Footer 내부에서 못 하므로 layout에서 처리):

    Next.js 15 App Router의 RootLayout은 pathname을 직접 알 수 없음 — 대신 별도 client component로 분리하거나, 단순히 모든 페이지에 Footer를 표시하고 admin layout에서 hide 처리. 결정: Footer를 모든 페이지에 표시하되, admin pages가 자체 full-height 레이아웃을 사용하므로 footer가 자연스럽게 admin 컨텐츠 아래에 표시됨. admin pages 레이아웃이 `minHeight: '100vh'`로 잡혀있으므로 시각적으로 전혀 문제 없음.

    layout.tsx 수정:
    ```typescript
    // 기존 import 유지하고 추가:
    import { Footer } from '@/components/layout/Footer'

    // body 내부 children 다음에 Footer 삽입:
    <body className="font-sans antialiased">
      <Script
        src={`//dapi.kakao.com/v2/maps/sdk.js?appkey=${process.env.NEXT_PUBLIC_KAKAO_JS_KEY}&libraries=services`}
        strategy="beforeInteractive"
      />
      {children}
      <Footer />
    </body>
    ```

    NOTE: admin 경로에서 Footer를 숨기려면 별도 route group(`(admin)/layout.tsx`)이 필요한데 현재 구조 변경 범위가 큼 → V1.0은 모든 페이지 표시. RESEARCH.md Open Question 2에서 권장한 "admin 페이지에 footer 자연 노출" 방식.

    SUPPORT_EMAIL이 클라이언트 컴포넌트에서도 접근되도록 `NEXT_PUBLIC_SUPPORT_EMAIL`로 사용 (Footer는 RSC지만 future-proof). `.env.local.example`은 `SUPPORT_EMAIL=`만 있으므로 코드의 fallback (`process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? 'support@danjiondo.com'`)으로 처리. 운영자가 Vercel env에 `NEXT_PUBLIC_SUPPORT_EMAIL` 추가하면 동작.
  </action>
  <acceptance_criteria>
    - `grep -c "auth.admin.deleteUser" src/app/api/ingest/molit-trade/route.ts` ≥ 1
    - `grep -c "30 \\* 86_400_000\\|interval '30 days'" src/app/api/ingest/molit-trade/route.ts` ≥ 1
    - `grep "hardDeleted" src/app/api/ingest/molit-trade/route.ts` ≥ 2건 매치
    - `test -f src/app/legal/terms/page.tsx && test -f src/app/legal/privacy/page.tsx && test -f src/app/legal/ad-policy/page.tsx` 종료 코드 0
    - `grep -c "<h2" src/app/legal/privacy/page.tsx` ≥ 8 (개인정보보호법 §30 8개 항목)
    - `grep -c "<h2" src/app/legal/terms/page.tsx` ≥ 6 (약관 6개 조항)
    - `grep -c "<h2" src/app/legal/ad-policy/page.tsx` ≥ 5 (광고 정책 5개 섹션)
    - `grep -c "표시광고법" src/app/legal/ad-policy/page.tsx` ≥ 1
    - `grep -c "개인정보보호" src/app/legal/privacy/page.tsx` ≥ 1
    - `grep -c "Supabase\\|Vercel\\|Resend" src/app/legal/privacy/page.tsx` ≥ 3 (위탁 3사 명시)
    - `test -f src/components/layout/Footer.tsx` 종료 코드 0
    - `grep -c "/legal/terms\\|/legal/privacy\\|/legal/ad-policy" src/components/layout/Footer.tsx` ≥ 3
    - `grep -c "mailto:" src/components/layout/Footer.tsx` ≥ 1
    - `grep -c "Footer" src/app/layout.tsx` ≥ 2 (import + 사용)
    - `grep -c "backdrop-blur\\|gradient\\|glow" src/components/layout/Footer.tsx` 0건 (AI 슬롭 금지)
    - `npm run build` 종료 코드 0 (모든 페이지 빌드)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>cron route에 hard delete 로직이 들어가고, 3개 법적 페이지가 빌드 가능하며, Footer가 모든 페이지에 시맨틱하게 표시된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /consent Server Action | 사용자 입력(체크박스 + next 파라미터)이 서버로 전달 |
| Vercel Cron → /api/ingest/molit-trade | CRON_SECRET이 hard delete 권한을 가진 코드 실행 |
| /reactivate → reactivateAccount | 탈퇴 사용자 본인만 재활성화 가능해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | next 파라미터 open redirect | mitigate | encodeURIComponent 사용 + Server Action에서 절대 URL 검증 안 하므로 외부 도메인 redirect 불가 (Next.js redirect는 동일 origin만) |
| T-03-07 | Spoofing | /consent 비로그인 호출 | mitigate | Task 1 agreeToTerms에서 getUser() 체크 + Task 2 page에서 user 없으면 redirect('/login') |
| T-03-08 | Elevation | 다른 사용자 계정 탈퇴 | mitigate | deleteAccount는 `eq('id', user.id)` 강제 (자기 자신만) |
| T-03-09 | Tampering | hard delete cron 우회 호출 | mitigate | CRON_SECRET Bearer 검증 (기존 라인 14-17 유지) |
| T-03-10 | Information Disclosure | 법적 페이지에 PII 노출 | accept | 정적 콘텐츠 + 환경변수만 사용, hardcoded PII 없음 |
| T-03-11 | Tampering | 30일 미경과 사용자 hard delete | mitigate | `.lt('deleted_at', thirtyDaysAgo)` 필터 강제 |
| T-03-12 | Denial of Service | hard delete loop가 cron 타임아웃 초과 | accept | Vercel Hobby 한도 내 일배치 1회, 일일 탈퇴 수 미미 예상 (V1.0 초기) |
</threat_model>

<verification>
1. `npm run lint` 통과
2. `npm run build` 통과 (3개 법적 페이지 + /consent + /reactivate 빌드)
3. `npm run test -- --run src/__tests__/consent-actions.test.ts` 통과
4. 수동 시나리오:
   - 신규 OAuth 로그인 → /consent 리다이렉트 확인
   - 체크박스 1개만 → "이용약관과 개인정보처리방침에 모두 동의해야 합니다." 에러
   - 체크박스 2개 → next 파라미터로 이동 + DB profiles.terms_agreed_at 값 확인
   - /profile 탈퇴 → DB profiles.deleted_at 값 + signOut 확인
   - 탈퇴 상태 재로그인 → /reactivate 리다이렉트
   - /reactivate 재활성화 → DB deleted_at NULL + / 이동
5. cron 검증: 로컬에서 mock profiles에 `deleted_at = now() - interval '31 days'` 후 `curl -H "Authorization: Bearer $CRON_SECRET" ${SITE}/api/ingest/molit-trade` → 응답에 `hardDeleted: 1` 확인
</verification>

<success_criteria>
- LEGAL-01: 첫 로그인 동의 흐름 동작 (auth/callback → /consent → DB 업데이트 → next)
- LEGAL-02: /legal/privacy 페이지 200 응답 + 8개 섹션 존재
- LEGAL-03: /legal/ad-policy 페이지 200 응답 + 표시광고법 명시 + 5개 섹션 존재
- LEGAL-04: 탈퇴 → 30일 grace → cron hard delete 흐름 전체 동작
- LEGAL-05: Footer + 모든 법적 페이지에 mailto:SUPPORT_EMAIL 노출
- consent-actions.test.ts GREEN
- npm run lint && npm run build 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-cardnews-legal-ops/03-02-SUMMARY.md` with:
- 추가/수정 파일 목록 + 라인 수 변동
- 신규 Server Action 시그니처 3종
- auth/callback 분기 흐름 다이어그램 (간단 텍스트)
- /api/ingest/molit-trade 응답 스키마 변경(hardDeleted, hardDeleteFailed 추가)
- Footer 노출 정책 (admin 페이지 포함 모두 노출 — 향후 별도 layout group으로 격리 예정)
- 발견된 이슈 + 수동 검증 결과
</output>

---
phase: 03-cardnews-legal-ops
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/api/cardnews/generate/route.ts
  - src/app/admin/cardnews/page.tsx
  - src/components/admin/CardnewsDownloadButton.tsx
autonomous: true
requirements:
  - SHARE-03
  - SHARE-04

must_haves:
  truths:
    - "GET /api/cardnews/generate가 비로그인 호출 시 401, 비-admin role 시 403, admin role 시 200 + Content-Type: image/png + Content-Disposition: attachment를 반환한다"
    - "ImageResponse는 1080×1080 정사각형이며 Pretendard TTF 폰트가 임베드된다"
    - "/admin/cardnews 페이지에서 admin role 사용자만 진입 가능하고 비-admin은 / 로 redirect된다"
    - "CardnewsDownloadButton이 클릭 시 fetch → blob → <a download> 흐름으로 cardnews.png를 브라우저 다운로드시킨다"
    - "Recharts/grid 미사용, CSS flex로만 랭킹 바 차트가 렌더된다 (Satori 호환)"
    - "src/__tests__/cardnews.test.ts가 GREEN으로 통과한다"
  artifacts:
    - path: "src/app/api/cardnews/generate/route.ts"
      provides: "ImageResponse Route Handler (auth + admin role + Content-Disposition)"
      exports: ["GET", "runtime"]
      contains: "ImageResponse"
    - path: "src/app/admin/cardnews/page.tsx"
      provides: "/admin/cardnews Server Component (admin role guard)"
      contains: "createSupabaseServerClient"
    - path: "src/components/admin/CardnewsDownloadButton.tsx"
      provides: "Client component — fetch + Blob + <a download>"
      contains: "'use client'"
  key_links:
    - from: "src/app/api/cardnews/generate/route.ts"
      to: "getRankingsByType(supabase, 'high_price', 5)"
      via: "src/lib/data/rankings.ts 재사용"
      pattern: "getRankingsByType"
    - from: "src/components/admin/CardnewsDownloadButton.tsx"
      to: "/api/cardnews/generate"
      via: "fetch + blob + URL.createObjectURL + <a download>"
      pattern: "fetch\\(['\"]/api/cardnews"
---

<objective>
Wave 2 (parallel with Plan 04): SHARE-03 카드뉴스 자동 생성 + SHARE-04 어드민 1-click 발행 UI를 완성한다.

Purpose:
- 기존 opengraph-image.tsx의 ImageResponse 패턴을 1080×1080 Route Handler로 포팅 (D-01~D-05 locked)
- 어드민 1-click 다운로드 흐름 (D-04): fetch → Blob → `<a download>` (Supabase Storage 미사용)
- Wave 0의 cardnews RED 테스트를 GREEN으로 전환

Output:
- /api/cardnews/generate route handler (auth + admin role + ImageResponse 1080×1080)
- /admin/cardnews 페이지 (admin guard + 다운로드 버튼)
- CardnewsDownloadButton 클라이언트 컴포넌트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-cardnews-legal-ops/03-CONTEXT.md
@.planning/phases/03-cardnews-legal-ops/03-RESEARCH.md
@.planning/phases/03-cardnews-legal-ops/03-PATTERNS.md
@.planning/phases/03-cardnews-legal-ops/03-01-SUMMARY.md
@src/app/complexes/[id]/opengraph-image.tsx
@src/app/admin/ads/page.tsx
@src/lib/data/rankings.ts
@src/lib/supabase/server.ts
@src/lib/supabase/readonly.ts

<interfaces>
<!-- 재사용 함수 -->

getRankingsByType(supabase, rankType, limit) → RankingRow[]
RankingRow = {
  id: string
  canonical_name: string
  si: string | null
  gu: string | null
  score: number  // 만원 단위 신고가
  rank: number
}

createReadonlyClient() — cookies 미호출, anon key 기반 (ranking 조회용)
createSupabaseServerClient() — cookies 기반, RLS 적용 (auth 체크용)

readFileSync, join — node:fs / node:path (TTF 로드)
ImageResponse — 'next/og' (Satori 기반, runtime='nodejs' 필수)

폰트:
public/fonts/PretendardSubset.ttf (이미 존재 — opengraph-image.tsx에서 사용 중)

CLAUDE.md 제약:
- AI 슬롭 금지: backdrop-blur / gradient-text / glow / 보라/인디고 / gradient orb 모두 금지
- 거래 데이터 쿼리: cancel_date IS NULL AND superseded_by IS NULL — getRankingsByType 내부에서 처리됨
- transactions 직접 쿼리 금지 (rankings 테이블 경유)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /api/cardnews/generate Route Handler 구현 (auth + admin + 1080×1080 ImageResponse)</name>
  <files>src/app/api/cardnews/generate/route.ts</files>
  <read_first>
    - src/app/complexes/[id]/opengraph-image.tsx (전체 — ImageResponse + TTF + JSX 정확한 패턴)
    - src/app/admin/ads/page.tsx (admin role 가드 라인 35-49 — Route Handler에서도 동일 로직 사용)
    - src/lib/data/rankings.ts (getRankingsByType 시그니처 라인 27-61)
    - src/lib/supabase/server.ts (createSupabaseServerClient — Route Handler에서도 작동)
    - src/lib/supabase/readonly.ts (createReadonlyClient — ranking 조회용)
    - .planning/phases/03-cardnews-legal-ops/03-RESEARCH.md (라인 228-280: Pattern 1 / 라인 770-783: 인증 결정 = user session, NOT CRON_SECRET)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 35-131: 정확한 Route Handler 코드)
    - src/__tests__/cardnews.test.ts (Wave 0의 RED 테스트 — 401/runtime nodejs 체크)
  </read_first>
  <behavior>
    Test 1: 비로그인 GET 호출 → 401 Unauthorized
    Test 2: 로그인했지만 role='user' → 403 Forbidden
    Test 3: role='admin' 또는 'superadmin' → 200 + Content-Type: image/png + Content-Disposition: attachment
    Test 4: export const runtime === 'nodejs'
    Test 5: ImageResponse는 1080×1080 사이즈 + Pretendard 폰트 임베드
    Test 6: getRankingsByType(supabase, 'high_price', 5) 결과의 5개 row가 ranking 막대로 렌더됨

    Anti-pattern (체크리스트):
    - export default 사용 금지 (named export GET만)
    - export const size / alt / contentType 사용 금지 (Route Handler 컨벤션 아님)
    - Recharts import 금지 (Satori 미지원)
  </behavior>
  <action>
    `src/app/api/cardnews/generate/route.ts` 신규 작성. PATTERNS.md 라인 35-131 + RESEARCH.md 라인 233-279 패턴을 정확히 따름:

    ```typescript
    import { ImageResponse } from 'next/og'
    import { readFileSync } from 'node:fs'
    import { join } from 'node:path'
    import { createReadonlyClient } from '@/lib/supabase/readonly'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { getRankingsByType } from '@/lib/data/rankings'

    // Edge runtime은 TTF 4MB 번들 한도 초과 → nodejs 필수 (RESEARCH.md Pitfall 1)
    export const runtime = 'nodejs'

    /**
     * Phase 3 SHARE-03 카드뉴스 생성 API
     * - 1080×1080 정사각형 PNG (D-02)
     * - 데이터: 주간 신고가 TOP5 — getRankingsByType('high_price', 5)
     * - 인증: user session + profiles.role in ('admin','superadmin') (RESEARCH.md Open Question 3)
     * - 다운로드 유도: Content-Disposition: attachment; filename="cardnews.png"
     */
    export async function GET(request: Request): Promise<Response> {
      // 1. 인증 + 권한 체크 (Route Handler에서 user session 사용)
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) {
        return new Response('Unauthorized', { status: 401 })
      }

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single()

      if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
        return new Response('Forbidden', { status: 403 })
      }

      // 2. 폰트 + 데이터 로드
      const fontData = readFileSync(
        join(process.cwd(), 'public/fonts/PretendardSubset.ttf'),
      )
      const readonly = createReadonlyClient()
      const rankings = await getRankingsByType(readonly, 'high_price', 5)

      // 가격 → 억 단위 (score는 만원 단위, 1억 = 10000만원)
      const formatEok = (score: number): string => {
        const eok = Math.floor(score / 10000)
        const remain = Math.round((score % 10000) / 1000) * 1000
        return remain > 0 ? `${eok}억 ${(remain / 1000)}천` : `${eok}억`
      }

      const maxScore = rankings[0]?.score ?? 1

      // 3. ImageResponse 1080×1080 (CSS flex만 사용 — Satori 호환)
      const img = new ImageResponse(
        (
          <div
            style={{
              width: '100%',
              height: '100%',
              display: 'flex',
              flexDirection: 'column',
              background: '#ffffff',
              padding: '60px 72px',
              fontFamily: 'Pretendard',
            }}
          >
            {/* 브랜드 헤더 */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 32 }}>
              <div
                style={{
                  width: 40,
                  height: 40,
                  background: '#ea580c',
                  borderRadius: 10,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  color: '#ffffff',
                  fontSize: 22,
                  fontWeight: 700,
                }}
              >
                단
              </div>
              <span style={{ fontSize: 22, fontWeight: 700, color: '#ea580c' }}>단지온도</span>
            </div>

            {/* 타이틀 */}
            <div
              style={{
                fontSize: 56,
                fontWeight: 700,
                color: '#111111',
                letterSpacing: '-2px',
                lineHeight: 1.15,
                marginBottom: 12,
                display: 'flex',
              }}
            >
              주간 신고가 TOP 5
            </div>
            <div style={{ fontSize: 22, color: '#6b7280', fontWeight: 500, marginBottom: 40, display: 'flex' }}>
              창원·김해 · 최근 30일 기준
            </div>

            {/* 랭킹 5개 — CSS flex bar (Recharts 사용 금지, Satori 미지원) */}
            <div style={{ display: 'flex', flexDirection: 'column', gap: 18, flex: 1 }}>
              {rankings.length === 0 ? (
                <div style={{ fontSize: 24, color: '#9ca3af', display: 'flex' }}>데이터 없음</div>
              ) : (
                rankings.map((r) => {
                  const widthPct = Math.max(20, Math.round((r.score / maxScore) * 100))
                  return (
                    <div key={r.id} style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                      <span
                        style={{
                          width: 48,
                          height: 48,
                          borderRadius: 24,
                          background: '#fff7ed',
                          color: '#ea580c',
                          fontSize: 22,
                          fontWeight: 700,
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          flexShrink: 0,
                        }}
                      >
                        {r.rank}
                      </span>
                      <div style={{ display: 'flex', flexDirection: 'column', flex: 1, gap: 6 }}>
                        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline' }}>
                          <span style={{ fontSize: 24, fontWeight: 700, color: '#111111' }}>
                            {r.canonical_name}
                          </span>
                          <span style={{ fontSize: 26, fontWeight: 700, color: '#ea580c' }}>
                            {formatEok(r.score)}
                          </span>
                        </div>
                        <div
                          style={{
                            display: 'flex',
                            height: 10,
                            background: '#f3f4f6',
                            borderRadius: 5,
                            overflow: 'hidden',
                          }}
                        >
                          <div
                            style={{
                              width: `${widthPct}%`,
                              height: '100%',
                              background: '#ea580c',
                            }}
                          />
                        </div>
                      </div>
                    </div>
                  )
                })
              )}
            </div>

            {/* Footer */}
            <div
              style={{
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                borderTop: '1px solid #e5e7eb',
                paddingTop: 20,
                marginTop: 32,
              }}
            >
              <span style={{ fontSize: 18, color: '#6b7280', fontWeight: 500 }}>
                실거래가 · 단지온도
              </span>
              <span style={{ fontSize: 16, color: '#9ca3af' }}>
                danjiondo.com
              </span>
            </div>
          </div>
        ),
        {
          width: 1080,
          height: 1080,
          fonts: [
            {
              name: 'Pretendard',
              data: fontData,
              style: 'normal',
              weight: 700,
            },
          ],
        },
      )

      // 4. Content-Disposition: attachment로 다운로드 유도 (D-03)
      const headers = new Headers(img.headers)
      headers.set('Content-Disposition', 'attachment; filename="cardnews.png"')
      headers.set('Cache-Control', 'no-store')
      return new Response(img.body, { status: img.status, headers })
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "export const runtime = 'nodejs'" src/app/api/cardnews/generate/route.ts` 정확히 1
    - `grep -c "export async function GET" src/app/api/cardnews/generate/route.ts` 정확히 1
    - `grep -c "export default" src/app/api/cardnews/generate/route.ts` 0건 (Route Handler 컨벤션 — anti-pattern)
    - `grep -c "from 'recharts'\\|from \"recharts\"" src/app/api/cardnews/generate/route.ts` 0건 (Satori 미지원)
    - `grep -c "width: 1080" src/app/api/cardnews/generate/route.ts` 정확히 1
    - `grep -c "height: 1080" src/app/api/cardnews/generate/route.ts` 정확히 1
    - `grep "PretendardSubset.ttf" src/app/api/cardnews/generate/route.ts` 1건 매치
    - `grep "Content-Disposition" src/app/api/cardnews/generate/route.ts` 1건 매치
    - `grep "attachment; filename=\"cardnews.png\"" src/app/api/cardnews/generate/route.ts` 1건 매치
    - `grep "401" src/app/api/cardnews/generate/route.ts` ≥ 1건
    - `grep "403" src/app/api/cardnews/generate/route.ts` ≥ 1건
    - `grep "getRankingsByType" src/app/api/cardnews/generate/route.ts` ≥ 1건
    - `grep "high_price" src/app/api/cardnews/generate/route.ts` 1건 매치
    - `grep -E "backdrop-blur|gradient-text|gradient orb|#7c3aed|#a855f7|indigo" src/app/api/cardnews/generate/route.ts` 0건 (CLAUDE.md AI 슬롭 금지)
    - `npm run test -- --run src/__tests__/cardnews.test.ts` 종료 코드 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/__tests__/cardnews.test.ts</automated>
  </verify>
  <done>Route Handler가 인증·권한 체크 후 1080×1080 PNG를 attachment로 반환하고 cardnews.test.ts가 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/cardnews 페이지 + CardnewsDownloadButton 클라이언트 컴포넌트</name>
  <files>src/app/admin/cardnews/page.tsx, src/components/admin/CardnewsDownloadButton.tsx</files>
  <read_first>
    - src/app/admin/ads/page.tsx (admin guard 라인 35-49, 헤더 라인 62-85, content area 라인 86-90)
    - src/components/ads/AdminCampaignActions.tsx ('use client' 컴포넌트 + useTransition 패턴)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 137-214: cardnews admin page + download button 코드)
    - src/app/api/cardnews/generate/route.ts (Task 1 산출물 — fetch 대상)
  </read_first>
  <action>
    파일 1: `src/components/admin/CardnewsDownloadButton.tsx` 신규 작성. 'use client' + fetch + Blob + `<a download>`:

    ```typescript
    'use client'

    import { useState } from 'react'

    /**
     * Phase 3 SHARE-04: 어드민 1-click 다운로드 (D-03, D-04)
     * fetch /api/cardnews/generate → Blob → URL.createObjectURL → <a download>
     */
    export function CardnewsDownloadButton() {
      const [pending, setPending] = useState(false)
      const [err, setErr] = useState<string | null>(null)

      async function handleDownload() {
        setPending(true)
        setErr(null)
        try {
          const res = await fetch('/api/cardnews/generate', { cache: 'no-store' })
          if (!res.ok) {
            setErr(`다운로드 실패 (${res.status}): ${await res.text()}`)
            return
          }
          const blob = await res.blob()
          const url = URL.createObjectURL(blob)
          const a = document.createElement('a')
          a.href = url
          a.download = `cardnews_${new Date().toISOString().slice(0, 10)}.png`
          document.body.appendChild(a)
          a.click()
          document.body.removeChild(a)
          URL.revokeObjectURL(url)
        } catch (e: unknown) {
          setErr(e instanceof Error ? e.message : '다운로드 실패')
        } finally {
          setPending(false)
        }
      }

      return (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
          <button
            type="button"
            className="btn btn-md btn-primary"
            onClick={handleDownload}
            disabled={pending}
            aria-label="주간 신고가 TOP5 카드뉴스 다운로드"
          >
            {pending ? '생성 중…' : '카드뉴스 생성 + 다운로드'}
          </button>
          {err && (
            <div role="alert" style={{ font: '500 13px/1.5 var(--font-sans)', color: '#dc2626' }}>
              {err}
            </div>
          )}
        </div>
      )
    }
    ```

    파일 2: `src/app/admin/cardnews/page.tsx` 신규 작성. ads/page.tsx admin 가드 정확히 복사 + content는 다운로드 버튼 + 안내:

    ```typescript
    import { redirect } from 'next/navigation'
    import Link from 'next/link'
    import type { Metadata } from 'next'
    import { createSupabaseServerClient } from '@/lib/supabase/server'
    import { CardnewsDownloadButton } from '@/components/admin/CardnewsDownloadButton'

    export const revalidate = 0
    export const metadata: Metadata = { title: '관리자 · 카드뉴스' }

    export default async function AdminCardnewsPage() {
      const supabase = await createSupabaseServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login?next=/admin/cardnews')

      const { data: profile } = await supabase
        .from('profiles')
        .select('role')
        .eq('id', user.id)
        .single()

      if (!profile || !['admin', 'superadmin'].includes((profile as { role: string }).role ?? '')) {
        redirect('/')
      }

      return (
        <div style={{ minHeight: '100vh', background: 'var(--bg-canvas)', fontFamily: 'var(--font-sans)' }}>
          <header style={{ height: 60, background: '#fff', borderBottom: '1px solid var(--line-default)',
            display: 'flex', alignItems: 'center', padding: '0 32px', gap: 24,
            position: 'sticky', top: 0, zIndex: 50 }}>
            <Link href="/" className="dj-logo">
              <span className="mark">단</span>
              <span>단지온도</span>
            </Link>
            <span style={{ font: '600 14px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
              관리자 · 카드뉴스
            </span>
            <nav style={{ marginLeft: 'auto', display: 'flex', gap: 12 }}>
              <Link href="/admin/ads" style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>광고</Link>
              <Link href="/admin/members" style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>회원</Link>
              <Link href="/admin/reports" style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>신고</Link>
              <Link href="/admin/status" style={{ font: '500 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>시스템</Link>
            </nav>
          </header>

          <main style={{ maxWidth: 800, margin: '0 auto', padding: '28px 32px' }}>
            <h1 style={{ font: '700 22px/1.3 var(--font-sans)', letterSpacing: '-0.02em', margin: '0 0 8px' }}>
              카드뉴스 발행
            </h1>
            <p style={{ font: '500 14px/1.6 var(--font-sans)', color: 'var(--fg-sec)', margin: '0 0 24px' }}>
              주간 신고가 TOP 5를 1080×1080 PNG로 생성합니다. 다운로드 후 카카오톡·카페에 직접 업로드하세요.
            </p>

            <div className="card" style={{ padding: 24, marginBottom: 16 }}>
              <h2 style={{ font: '700 14px/1.4 var(--font-sans)', margin: '0 0 12px' }}>
                주간 신고가 TOP 5 카드뉴스
              </h2>
              <p style={{ font: '500 13px/1.6 var(--font-sans)', color: 'var(--fg-sec)', margin: '0 0 16px' }}>
                데이터 소스: complex_rankings (rank_type=high_price, 최근 30일)<br />
                이미지 크기: 1080×1080 (정사각형 SNS 최적)
              </p>
              <CardnewsDownloadButton />
            </div>
          </main>
        </div>
      )
    }
    ```

    NOTE: header `<nav>` 시맨틱 사용 → A11Y-03 자동 충족.
  </action>
  <acceptance_criteria>
    - `test -f src/components/admin/CardnewsDownloadButton.tsx` 종료 코드 0
    - `grep -c "'use client'" src/components/admin/CardnewsDownloadButton.tsx` 정확히 1
    - `grep -c "fetch('/api/cardnews/generate'" src/components/admin/CardnewsDownloadButton.tsx` ≥ 1
    - `grep -c "createObjectURL" src/components/admin/CardnewsDownloadButton.tsx` ≥ 1
    - `grep -c "a.download" src/components/admin/CardnewsDownloadButton.tsx` ≥ 1
    - `grep -c "aria-label" src/components/admin/CardnewsDownloadButton.tsx` ≥ 1
    - `test -f src/app/admin/cardnews/page.tsx` 종료 코드 0
    - `grep -c "createSupabaseServerClient" src/app/admin/cardnews/page.tsx` ≥ 1
    - `grep -c "redirect('/login?next=/admin/cardnews')" src/app/admin/cardnews/page.tsx` 정확히 1
    - `grep "redirect('/')" src/app/admin/cardnews/page.tsx` 1건 매치 (비-admin)
    - `grep -c "'admin', 'superadmin'" src/app/admin/cardnews/page.tsx` ≥ 1
    - `grep -c "CardnewsDownloadButton" src/app/admin/cardnews/page.tsx` ≥ 2 (import + 사용)
    - `grep -c "<nav" src/app/admin/cardnews/page.tsx` ≥ 1 (시맨틱 nav)
    - `grep -E "backdrop-blur|gradient-text|gradient orb|#7c3aed|#a855f7|indigo" src/app/admin/cardnews/page.tsx src/components/admin/CardnewsDownloadButton.tsx` 0건
    - `npm run build` 종료 코드 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>/admin/cardnews 페이지가 admin role guard와 CardnewsDownloadButton을 포함하고, 비-admin 사용자는 / 로 redirect된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/cardnews/generate | 인증 user session이 admin role 체크의 근거 |
| /admin/cardnews → /api/cardnews/generate | fetch 호출이 동일 origin 쿠키 자동 전송 (인증 흐름 유지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Elevation of Privilege | 비-admin이 /api/cardnews/generate 직접 호출 | mitigate | Task 1 GET handler에서 401 (비로그인) / 403 (비-admin) 분리 반환 |
| T-03-14 | Information Disclosure | 카드뉴스에 비공개 데이터 포함 | accept | rank_type='high_price' + getRankingsByType은 transactions 공개 데이터만 사용 (RLS public read) |
| T-03-15 | Tampering | Recharts 같은 외부 라이브러리 import 시 빌드 깨짐 | mitigate | RESEARCH.md Anti-pattern 명시 + 검수 acceptance_criteria에 grep 0건 강제 |
| T-03-16 | DoS | 대용량 ImageResponse 반복 호출로 cron quota 소진 | accept | 어드민만 호출 가능 + Cache-Control: no-store + 일반 사용자에게 노출 안 됨 |
</threat_model>

<verification>
1. `npm run lint` 통과
2. `npm run build` 통과
3. `npm run test -- --run src/__tests__/cardnews.test.ts` 통과 (3개 테스트)
4. 수동 검증:
   - admin role 사용자로 로그인 → /admin/cardnews 진입 가능
   - "카드뉴스 생성 + 다운로드" 클릭 → cardnews_YYYY-MM-DD.png 다운로드
   - 이미지 사이즈 확인: 1080×1080
   - 한글이 잘 렌더되는지 (Pretendard 폰트 임베드 확인)
   - 비-admin 사용자로 /admin/cardnews → / 리다이렉트
   - 비로그인으로 curl /api/cardnews/generate → 401
</verification>

<success_criteria>
- SHARE-03: GET /api/cardnews/generate가 1080×1080 PNG를 반환
- SHARE-04: /admin/cardnews 1-click 다운로드 동작
- cardnews.test.ts GREEN
- Recharts 미사용 (Satori 호환)
- AI 슬롭 금지 규칙 위반 0건
</success_criteria>

<output>
After completion, create `.planning/phases/03-cardnews-legal-ops/03-03-SUMMARY.md` with:
- 추가 파일 + 라인 수
- /api/cardnews/generate 응답 헤더 + 상태코드 매트릭스 (401/403/200)
- 어드민 다운로드 흐름 다이어그램 (browser → fetch → blob → <a download>)
- 수동 다운로드 테스트 결과 (파일명 + 사이즈 + 한글 렌더 확인)
- 발견된 이슈
</output>

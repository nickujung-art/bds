---
phase: 03-cardnews-legal-ops
plan: 05
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - e2e/accessibility.spec.ts
  - playwright.config.ts
  - .github/workflows/e2e.yml
  - src/app/page.tsx
  - src/app/login/page.tsx
  - src/app/map/page.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03

must_haves:
  truths:
    - "e2e/accessibility.spec.ts가 6개 공개 페이지(/, /map, /login, /legal/terms, /legal/privacy, /legal/ad-policy)에서 axe-core critical 위반 0건을 강제한다"
    - "키보드 Tab으로 랜딩 페이지의 첫 인터랙티브 요소가 가시 포커스를 받는다"
    - "랜딩 페이지에 시맨틱 <nav> 또는 [role='navigation']이 존재하며 main 요소에 적절한 라벨이 있다"
    - "GitHub Actions e2e 워크플로우(또는 기존 ci 워크플로우)에서 accessibility.spec.ts가 실행되고, critical 0건이 아니면 빌드가 실패한다"
    - "기존 컴포넌트의 a11y 결함이 보완된다 (랜딩/로그인/지도 페이지에서 검출된 critical 한정)"
  artifacts:
    - path: "e2e/accessibility.spec.ts"
      provides: "A11Y-01/02/03 E2E (Wave 0 RED → Wave 3 GREEN)"
      contains: "AxeBuilder"
      min_lines: 50
    - path: "playwright.config.ts"
      provides: "accessibility.spec를 실행 프로젝트에 포함"
      contains: "accessibility"
    - path: ".github/workflows/e2e.yml"
      provides: "CI에서 e2e 자동 실행 (또는 기존 ci.yml 확장)"
      contains: "playwright"
  key_links:
    - from: "e2e/accessibility.spec.ts"
      to: "@axe-core/playwright AxeBuilder"
      via: "withTags wcag2a + wcag2aa, .analyze()"
      pattern: "AxeBuilder.*wcag2"
    - from: ".github/workflows/e2e.yml or ci.yml"
      to: "npm run test:e2e"
      via: "GitHub Actions step"
      pattern: "test:e2e"
---

<objective>
Wave 3: 접근성(A11Y-01/02/03) E2E를 GREEN으로 만들어 V1.0 정식 출시 게이트(critical 0건)를 통과한다.

Purpose:
- Wave 0의 RED accessibility.spec.ts를 실제 페이지(legal/consent/admin) 존재 후 실행 → critical 위반 검출.
- 검출된 critical을 기존 컴포넌트(랜딩, 로그인, 지도)에서 수정.
- CI에 axe-core 강제 게이트 추가 (PR마다 실행).

Output:
- 보강된 accessibility.spec.ts (Wave 0 스캐폴드 → 실제 검증 로직)
- playwright.config.ts에 accessibility 스펙 등록
- GitHub Actions e2e workflow 또는 기존 ci.yml에 step 추가
- 검출된 critical을 수정한 page.tsx 파일들
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-cardnews-legal-ops/03-CONTEXT.md
@.planning/phases/03-cardnews-legal-ops/03-RESEARCH.md
@.planning/phases/03-cardnews-legal-ops/03-PATTERNS.md
@.planning/phases/03-cardnews-legal-ops/03-01-SUMMARY.md
@.planning/phases/03-cardnews-legal-ops/03-02-SUMMARY.md
@.planning/phases/03-cardnews-legal-ops/03-03-SUMMARY.md
@.planning/phases/03-cardnews-legal-ops/03-04-SUMMARY.md
@e2e/landing.spec.ts
@e2e/review.spec.ts
@e2e/accessibility.spec.ts

<interfaces>
<!-- Wave 0 + Wave 1 + Wave 2가 만든 페이지 -->

공개 페이지 (인증 불필요):
- / (랜딩)
- /map (지도)
- /login (로그인)
- /legal/terms
- /legal/privacy
- /legal/ad-policy

공개되지만 redirect 가능:
- /consent (비로그인 → /login redirect)
- /reactivate (비로그인 → /login redirect)
- /admin/* (비-admin → / redirect)

@axe-core/playwright API:
new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa']).analyze() → AxeResults
- AxeResults.violations: 위반 배열, 각 항목 .impact: 'minor'|'moderate'|'serious'|'critical'

기존 playwright projects (playwright.config.ts):
- chromium / chromium-auth (storageState 분리) — 기존 구성 유지
- accessibility.spec.ts는 chromium (no-auth) 프로젝트에서 실행
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: e2e/accessibility.spec.ts 보강 + 검출된 critical 수정</name>
  <files>e2e/accessibility.spec.ts, src/app/page.tsx, src/app/login/page.tsx, src/app/map/page.tsx</files>
  <read_first>
    - e2e/accessibility.spec.ts (Wave 0 산출물 — 기본 구조)
    - e2e/landing.spec.ts (page.goto 패턴)
    - src/app/page.tsx (랜딩 — h1, nav, search input 구조 확인)
    - src/app/login/page.tsx (로그인 폼 — label/aria 검토)
    - src/app/map/page.tsx (지도 — 인터랙티브 영역 라벨)
    - .planning/phases/03-cardnews-legal-ops/03-PATTERNS.md (라인 671-700: AxeBuilder 정확한 패턴)
  </read_first>
  <behavior>
    Test 1 (A11Y-01): 6개 공개 페이지 각각에서 axe-core critical 위반 0건
    Test 2 (A11Y-02): 랜딩 페이지에서 Tab 키 → 첫 포커스 가시 + 두 번째 Tab → 다른 요소로 이동 확인
    Test 3 (A11Y-03): 랜딩에 <nav> + main에 적절한 라벨 + 검색 input에 label 또는 aria-label

    Critical 수정 정책:
    - critical impact: 즉시 수정 (필수 — 게이트)
    - serious impact: 가능한 한 수정 (권장)
    - moderate/minor: V1.5 이후로 미루기 (테스트는 통과)
  </behavior>
  <action>
    파일 1: `e2e/accessibility.spec.ts` 보강. Wave 0 버전을 다음으로 교체:

    ```typescript
    import { test, expect } from '@playwright/test'
    import AxeBuilder from '@axe-core/playwright'

    /**
     * Phase 3 A11Y-01/02/03: 접근성 E2E
     * - critical 위반 0건 강제 (V1.0 출시 게이트)
     * - 키보드 탐색 + 시맨틱 라벨 검증
     */

    const PUBLIC_PAGES = [
      '/',
      '/map',
      '/login',
      '/legal/terms',
      '/legal/privacy',
      '/legal/ad-policy',
    ]

    for (const path of PUBLIC_PAGES) {
      test(`A11Y-01 axe-core: ${path} — critical 위반 0건`, async ({ page }) => {
        await page.goto(path)
        await page.waitForLoadState('domcontentloaded')

        const results = await new AxeBuilder({ page })
          .withTags(['wcag2a', 'wcag2aa'])
          .analyze()

        const critical = results.violations.filter(v => v.impact === 'critical')

        // 디버깅 편의를 위해 실패 시 위반 ID + 영향 요소 셀렉터 출력
        const summary = critical.map(v => ({
          id: v.id,
          help: v.help,
          nodes: v.nodes.slice(0, 3).map(n => n.target.join(' ')),
        }))

        expect(
          critical,
          `Critical violations on ${path}:\n${JSON.stringify(summary, null, 2)}`,
        ).toHaveLength(0)
      })
    }

    test('A11Y-02 키보드 Tab — 랜딩 페이지에서 두 번 Tab 시 가시 포커스가 이동한다', async ({ page }) => {
      await page.goto('/')
      await page.waitForLoadState('domcontentloaded')

      // 첫 Tab: skip-link 또는 첫 포커서블 요소
      await page.keyboard.press('Tab')
      const first = page.locator(':focus')
      await expect(first).toBeVisible()
      const firstHandle = await first.elementHandle()
      const firstId = await firstHandle?.evaluate((el) => (el as HTMLElement).outerHTML.slice(0, 80))

      // 두 번째 Tab: 다른 요소로 이동 (현 요소와 구별 가능해야 함)
      await page.keyboard.press('Tab')
      const second = page.locator(':focus')
      await expect(second).toBeVisible()
      const secondHandle = await second.elementHandle()
      const secondId = await secondHandle?.evaluate((el) => (el as HTMLElement).outerHTML.slice(0, 80))

      expect(secondId, 'Tab 두 번 후에도 같은 요소면 키보드 트랩 가능성').not.toBe(firstId)
    })

    test('A11Y-03 시맨틱 라벨 — 랜딩에 nav + 검색 input 라벨', async ({ page }) => {
      await page.goto('/')
      await page.waitForLoadState('domcontentloaded')

      // <nav> 또는 role="navigation"
      const nav = page.locator('nav, [role="navigation"]').first()
      await expect(nav).toBeVisible()

      // 검색 input: label / aria-label / aria-labelledby / placeholder 중 하나 필요
      const searchInput = page.locator('input[name="q"]').first()
      const isSearchVisible = await searchInput.isVisible({ timeout: 3000 }).catch(() => false)
      if (isSearchVisible) {
        const ariaLabel = await searchInput.getAttribute('aria-label')
        const ariaLabelledBy = await searchInput.getAttribute('aria-labelledby')
        const placeholder = await searchInput.getAttribute('placeholder')
        const hasLabel = (ariaLabel && ariaLabel.length > 0)
          || (ariaLabelledBy && ariaLabelledBy.length > 0)
          || (placeholder && placeholder.length > 0)
        expect(hasLabel, '검색 input에 aria-label / aria-labelledby / placeholder 중 하나 필요').toBe(true)
      }
    })

    test('A11Y-03 footer — 모든 페이지에 footer 시맨틱 존재', async ({ page }) => {
      await page.goto('/legal/terms')
      await page.waitForLoadState('domcontentloaded')
      const footer = page.locator('footer').first()
      await expect(footer).toBeVisible()
    })
    ```

    파일 2: `src/app/page.tsx` — 검출된 critical 수정.
    - critical 결과를 보고 다음 우선 수정:
      1. `<input name="q">`에 `aria-label="단지명, 지역으로 검색"` 추가 (이미 placeholder 있음)
      2. `<main>` 또는 `<section>` 시맨틱 누락 시 추가
      3. 클릭만 가능한 div가 button/role="button"인지 확인
      4. 색상 대비 critical (text-on-orange 등) — color contrast critical은 #fff/#ea580c WCAG AA 통과 (4.5:1)이므로 대부분 OK
    - 실제 critical 수정은 `npm run test:e2e -- accessibility.spec.ts`를 먼저 실행해 결과 확인 후 진행
    - **방법론:**
      1. 먼저 `npm run dev`로 서버 띄움
      2. 별도 터미널에서 `npm run test:e2e -- accessibility.spec.ts`
      3. 출력의 critical violations 확인
      4. 각 critical에 대해 해당 파일 (page.tsx, login/page.tsx, map/page.tsx 등) 최소 수정
      5. 다시 실행 → 0건 될 때까지 반복

    파일 3 (`src/app/login/page.tsx`)와 파일 4 (`src/app/map/page.tsx`):
    - critical 결과 따라 수정 (없을 수 있음). 일반적인 critical:
      - `<button>` 텍스트 없음 → aria-label 추가
      - `<form>`이 있는데 label 없음 → label htmlFor 추가
      - `<a>` href 없음 → button으로 교체

    NOTE: critical만 수정하고 serious/moderate는 V1.5 백로그로 미룸. acceptance_criteria가 critical 0건만 강제.
  </action>
  <acceptance_criteria>
    - `grep -c "AxeBuilder" e2e/accessibility.spec.ts` ≥ 2
    - `grep -c "wcag2a" e2e/accessibility.spec.ts` ≥ 1
    - `grep -c "wcag2aa" e2e/accessibility.spec.ts` ≥ 1
    - `grep "critical" e2e/accessibility.spec.ts` ≥ 2
    - `grep -c "PUBLIC_PAGES" e2e/accessibility.spec.ts` ≥ 2
    - `grep "/legal/terms\\|/legal/privacy\\|/legal/ad-policy" e2e/accessibility.spec.ts` ≥ 3
    - `grep "footer" e2e/accessibility.spec.ts` ≥ 1
    - `npm run test:e2e -- accessibility.spec.ts` 종료 코드 0 (= critical 0건)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- accessibility.spec.ts</automated>
  </verify>
  <done>6개 공개 페이지 모두 axe-core critical 0건이고, 키보드 탐색 + 시맨틱 라벨 테스트가 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: playwright.config.ts에 accessibility 등록 + GitHub Actions CI 게이트 추가</name>
  <files>playwright.config.ts, .github/workflows/e2e.yml</files>
  <read_first>
    - playwright.config.ts (현재 projects 구성)
    - .github/workflows/ (기존 ci.yml 또는 e2e.yml 존재 여부)
    - .planning/phases/01-*/01-04-SUMMARY.md (Phase 1에서 만든 e2e 워크플로우 — 가능하면 확장)
  </read_first>
  <action>
    파일 1: `playwright.config.ts` 수정. accessibility.spec.ts가 chromium (no-auth) 프로젝트에서 실행되도록 보장. 일반적으로 testMatch 또는 testIgnore가 spec 단위면 자동 포함됨. 명시적으로 보장하려면 `testMatch`에 accessibility 포함되는지 확인:

    1. 현재 config 확인. `projects` 배열에서 chromium 프로젝트의 `testIgnore`에 'accessibility'가 들어있다면 제거. testMatch가 명시적이면 'e2e/accessibility.spec.ts' 추가.
    2. 별도 변경 불필요 시(기본 spec 매칭) 이 파일은 건드리지 않고 commit message에만 명시.

    파일 2: `.github/workflows/e2e.yml` — 기존 e2e 워크플로우가 있으면 accessibility.spec를 명시적으로 실행하는 step 추가. 없으면 신규 작성:

    ```yaml
    name: E2E Tests

    on:
      pull_request:
        branches: [main]
      push:
        branches: [main]

    jobs:
      e2e:
        timeout-minutes: 20
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'

          - name: Install dependencies
            run: npm ci

          - name: Install Playwright browsers
            run: npx playwright install --with-deps chromium

          - name: Build Next.js app
            run: npm run build
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
              NEXT_PUBLIC_KAKAO_JS_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_JS_KEY }}

          - name: Run Playwright accessibility tests (A11Y-01/02/03)
            run: npm run test:e2e -- accessibility.spec.ts
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
              NEXT_PUBLIC_KAKAO_JS_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_JS_KEY }}

          - name: Run Playwright golden path tests
            run: npm run test:e2e -- landing.spec.ts complex-detail.spec.ts map.spec.ts search.spec.ts
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
              NEXT_PUBLIC_KAKAO_JS_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_JS_KEY }}

          - name: Upload Playwright report
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: playwright-report
              path: playwright-report/
              retention-days: 7
    ```

    NOTE: Phase 1에서 만든 ci.yml에 e2e step이 이미 있으면 accessibility.spec.ts step만 추가하고 위 workflow는 만들지 말 것. `.github/workflows/` 디렉토리 우선 확인.

    accessibility step 실패 시 전체 workflow 실패 → V1.0 게이트 강제.
  </action>
  <acceptance_criteria>
    - `test -d .github/workflows && ls .github/workflows | grep -E "ci\\.yml|e2e\\.yml"` ≥ 1건
    - 워크플로우 파일에 `grep -E "accessibility\\.spec|test:e2e.*accessibility"` ≥ 1건 매치
    - `npm run test:e2e -- accessibility.spec.ts` 로컬에서 통과
    - `npm run lint && npm run build && npm run test` 통과 (회귀 없음)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- accessibility.spec.ts</automated>
  </verify>
  <done>CI에서 PR마다 axe-core 게이트가 강제되고 critical 0건이 아니면 빌드 실패한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → 빌드된 앱 | secrets 환경변수가 step env로 전달 |
| Playwright → 실제 페이지 | 인증 없이 axe-core 분석만 수행 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-22 | Information Disclosure | CI logs에 secrets 노출 | mitigate | secrets는 GitHub Actions secrets 경유, env로만 주입 (Task 2) |
| T-03-23 | Tampering | accessibility 테스트 우회 (PR가 spec 삭제 후 머지) | mitigate | branch protection이 `e2e` job required check로 등록되어야 함 (Phase 1 SUMMARY 확인) — 본 plan 범위 외 운영 절차 |
</threat_model>

<verification>
1. `npm run lint && npm run build && npm run test` 통과 (회귀 없음)
2. `npm run test:e2e -- accessibility.spec.ts` 통과 (critical 0건)
3. `npm run test:e2e` 전체 실행 통과 (기존 5종 + accessibility)
4. PR을 일부러 만들어 a11y 게이트 작동 확인:
   - 임의 페이지에서 `<button>` 텍스트 제거 → axe-core가 detect → CI fail
5. axe-core report 출력: 검출된 위반 목록 (critical만) 0건
</verification>

<success_criteria>
- A11Y-01: axe-core CI에서 critical 0건 강제 게이트
- A11Y-02: 키보드 Tab 순서 검증 통과
- A11Y-03: 시맨틱 라벨 (nav, main, footer, search input) 검증 통과
- e2e/accessibility.spec.ts GREEN
- PR마다 자동 실행되는 CI 워크플로우 등록
- 기존 e2e 5종 + accessibility 합쳐서 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-cardnews-legal-ops/03-05-SUMMARY.md` with:
- accessibility.spec.ts 최종 테스트 수
- 검출되었던 critical 위반 목록 + 수정 내역
- 남은 serious/moderate 위반 (V1.5 백로그)
- CI 워크플로우 변경 (신규/확장)
- 검증 시간 (axe-core 6 페이지 분석 시간)
- 발견된 이슈
</output>

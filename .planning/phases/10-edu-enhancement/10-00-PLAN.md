---
phase: 10-edu-enhancement
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260515000001_school_districts.sql
  - src/lib/hagwon-category.ts
  - src/lib/hagwon-category.test.ts
  - src/lib/data/facility-edu.test.ts
autonomous: false
requirements:
  - EDU-01
  - EDU-02
  - EDU-03
  - EDU-04
  - EDU-05

must_haves:
  truths:
    - "school_districts 및 school_district_schools 테이블이 PostGIS geometry 컬럼과 함께 DB에 존재한다"
    - "school_districts에 RLS(public read + service_role write) 정책이 적용된다"
    - "hagwon_score_percentile_by_si(target_score, p_si) 함수가 DB에 존재한다"
    - "classifyHagwon() 및 walkColor() 순수 함수 파일이 존재한다"
    - "facility-edu.test.ts 및 hagwon-category.test.ts RED 테스트가 존재한다"
  artifacts:
    - path: "supabase/migrations/20260515000001_school_districts.sql"
      provides: "school_districts + school_district_schools 테이블 + RLS + 신규 RPC 함수"
    - path: "src/lib/hagwon-category.ts"
      provides: "classifyHagwon(), walkColor(), WALK_COLOR_HEX 순수 함수"
    - path: "src/lib/hagwon-category.test.ts"
      provides: "EDU-04/05 RED 단위 테스트"
    - path: "src/lib/data/facility-edu.test.ts"
      provides: "EDU-01/02/03 RED 단위 테스트"
  key_links:
    - from: "supabase/migrations/20260515000001_school_districts.sql"
      to: "school_districts table"
      via: "supabase db push"
      pattern: "school_districts"
    - from: "src/lib/hagwon-category.ts"
      to: "src/components/complex/EducationCard.tsx"
      via: "import { classifyHagwon, walkColor, WALK_COLOR_HEX }"
      pattern: "classifyHagwon|walkColor"
---

<objective>
Wave 0 (BLOCKING): school_districts DB 마이그레이션 적용 + 순수 유틸 함수 파일 생성 + RED 테스트 스캐폴드.

Purpose: 후속 Wave들이 의존하는 테이블 스키마와 테스트 인프라를 확립한다. supabase db push 완료 전까지 Wave 1 실행 불가.

Output:
- school_districts + school_district_schools 테이블 (PostGIS geometry, RLS)
- hagwon_score_percentile_by_si RPC 함수
- src/lib/hagwon-category.ts (classifyHagwon, walkColor 순수 함수)
- RED 테스트 2개 파일 (Wave 2 GREEN 전까지 실패 상태 유지)
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-edu-enhancement/10-RESEARCH.md
@.planning/phases/10-edu-enhancement/10-PATTERNS.md

<interfaces>
<!-- 기존 facility_school 스키마 (migrations/20260430000004_facility.sql) -->
```sql
create table public.facility_school (
  id              uuid primary key default gen_random_uuid(),
  complex_id      uuid not null references public.complexes(id) on delete cascade,
  school_name     text not null,
  school_type     text not null check (school_type in ('elementary', 'middle', 'high')),
  school_code     text,
  distance_m      integer,
  is_assignment   boolean not null default false,
  created_at      timestamptz not null default now(),
  updated_at      timestamptz not null default now()
);
```

<!-- 기존 hagwon_score_percentile 함수 패턴 (migrations/20260514000003_facility_edu.sql) -->
```sql
CREATE OR REPLACE FUNCTION public.hagwon_score_percentile(target_score integer)
RETURNS double precision LANGUAGE sql STABLE AS $$
  SELECT
    COUNT(*) FILTER (WHERE hagwon_score < target_score)::double precision
    / NULLIF(COUNT(*) FILTER (WHERE hagwon_score IS NOT NULL), 0)
  FROM public.complexes;
$$;
```

<!-- 기존 district_stats RLS 패턴 (migrations/20260508000001_district_stats.sql) -->
```sql
alter table public.district_stats enable row level security;
create policy "district_stats: public read"
  on public.district_stats for select using (true);
create policy "district_stats: service role write"
  on public.district_stats for all
  using (auth.role() = 'service_role')
  with check (auth.role() = 'service_role');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: school_districts 마이그레이션 SQL 작성</name>
  <files>supabase/migrations/20260515000001_school_districts.sql</files>
  <read_first>
    - supabase/migrations/20260508000001_district_stats.sql (RLS 패턴)
    - supabase/migrations/20260514000003_facility_edu.sql (hagwon_score_percentile 함수 패턴)
    - supabase/migrations/20260430000004_facility.sql (facility_school 스키마)
  </read_first>
  <behavior>
    - school_districts 테이블: id(uuid PK), hakgudo_id(text NOT NULL), school_level(text CHECK IN ('elementary','middle','high')), geometry(geometry(Geometry,4326) NOT NULL), source_file(text), created_at(timestamptz DEFAULT now())
    - school_district_schools 테이블: district_id(uuid FK → school_districts.id ON DELETE CASCADE), school_name(text NOT NULL), school_level(text NOT NULL), PRIMARY KEY(district_id, school_name)
    - 공간 인덱스: school_districts USING GIST (geometry)
    - RLS: school_districts + school_district_schools 모두 district_stats 패턴과 동일 (public read + service_role write)
    - 신규 함수: hagwon_score_percentile_by_si(target_score integer, p_si text) — 기존 함수에 WHERE si = p_si 추가
  </behavior>
  <action>
    아래 SQL을 정확히 작성한다:

    ```sql
    -- Phase 10: 학구도 테이블 + si별 학원 백분위 함수

    CREATE TABLE public.school_districts (
      id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      hakgudo_id   text NOT NULL,
      school_level text NOT NULL CHECK (school_level IN ('elementary', 'middle', 'high')),
      geometry     geometry(Geometry, 4326) NOT NULL,
      source_file  text,
      created_at   timestamptz NOT NULL DEFAULT now()
    );

    CREATE INDEX school_districts_geometry_idx
      ON public.school_districts USING GIST (geometry);

    CREATE TABLE public.school_district_schools (
      district_id  uuid NOT NULL REFERENCES public.school_districts(id) ON DELETE CASCADE,
      school_name  text NOT NULL,
      school_level text NOT NULL,
      PRIMARY KEY (district_id, school_name)
    );

    ALTER TABLE public.school_districts ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "school_districts: public read"
      ON public.school_districts FOR SELECT USING (true);

    CREATE POLICY "school_districts: service role write"
      ON public.school_districts FOR ALL
      USING (auth.role() = 'service_role')
      WITH CHECK (auth.role() = 'service_role');

    ALTER TABLE public.school_district_schools ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "school_district_schools: public read"
      ON public.school_district_schools FOR SELECT USING (true);

    CREATE POLICY "school_district_schools: service role write"
      ON public.school_district_schools FOR ALL
      USING (auth.role() = 'service_role')
      WITH CHECK (auth.role() = 'service_role');

    -- si 단위 학원 백분위 함수 (창원시/김해시 분리)
    CREATE OR REPLACE FUNCTION public.hagwon_score_percentile_by_si(
      target_score integer,
      p_si         text
    )
    RETURNS double precision
    LANGUAGE sql STABLE
    AS $$
      SELECT
        COUNT(*) FILTER (WHERE hagwon_score < target_score)::double precision
        / NULLIF(COUNT(*) FILTER (WHERE hagwon_score IS NOT NULL), 0)
      FROM public.complexes
      WHERE si = p_si;
    $$;
    ```
  </action>
  <verify>
    <automated>grep -c "school_districts" supabase/migrations/20260515000001_school_districts.sql</automated>
  </verify>
  <done>마이그레이션 파일이 존재하고 school_districts, school_district_schools, hagwon_score_percentile_by_si 모두 포함된다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push 실행</name>
  <read_first>
    - supabase/migrations/20260515000001_school_districts.sql (방금 생성된 파일)
  </read_first>
  <what-built>school_districts 마이그레이션 SQL 파일이 생성되었다.</what-built>
  <how-to-verify>
    1. `supabase db push` 실행 (linked remote DB에 마이그레이션 적용)
    2. 성공 메시지 확인 후 아래 쿼리로 검증:
       ```
       supabase db query --linked "SELECT table_name FROM information_schema.tables WHERE table_name IN ('school_districts','school_district_schools') ORDER BY table_name"
       ```
       → 결과에 두 테이블 모두 나타나야 함
    3. 함수 확인:
       ```
       supabase db query --linked "SELECT proname FROM pg_proc WHERE proname = 'hagwon_score_percentile_by_si'"
       ```
       → 1행 반환되어야 함
  </how-to-verify>
  <resume-signal>테이블 2개 + 함수 1개 확인 후 "pushed" 입력</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 순수 유틸 + RED 테스트 생성</name>
  <files>
    src/lib/hagwon-category.ts,
    src/lib/hagwon-category.test.ts,
    src/lib/data/facility-edu.test.ts
  </files>
  <read_first>
    - src/lib/format.ts (순수 함수 파일 구조 패턴)
    - src/lib/data/gap-label.test.ts (Vitest + Supabase mock 패턴)
    - src/lib/data/facility-edu.ts (getComplexFacilityEdu 현재 구현 — 이미 읽었음)
    - .planning/phases/10-edu-enhancement/10-PATTERNS.md (hagwon-category.ts 전체 코드)
  </read_first>
  <behavior>
    hagwon-category.ts:
    - classifyHagwon('엠투에스수학1지구학원') → '수학'
    - classifyHagwon('에이든영어학원') → '영어'
    - classifyHagwon('미나한아트미술스튜디오') → '예체능'
    - classifyHagwon('율하IT컴퓨터학원') → '기타'
    - walkColor(670) → 'green' (10분 정확히, 670/67=10.0)
    - walkColor(671) → 'yellow' (10분 초과)
    - walkColor(1005) → 'yellow' (15분 정확히, 1005/67=15.0)
    - walkColor(1006) → 'red' (15분 초과)
    - walkColor(null) → 'green'

    facility-edu.test.ts (RED — Wave 2에서 GREEN):
    - 유치원 poi_name 포함 → kindergartens 분류
    - 병설 포함 → kindergartens 분류
    - 유치원 미포함 daycare → daycares 분류
    - hagwon_score_percentile_by_si RPC 호출 시 p_si 파라미터가 전달됨
  </behavior>
  <action>
    1. src/lib/hagwon-category.ts 생성:
       PATTERNS.md의 "hagwon-category.ts 전용 구조" 섹션 코드를 그대로 사용.
       export type HagwonCategory, WalkColor, CATEGORY_KEYWORDS const, classifyHagwon(), walkColor(), WALK_COLOR_HEX const.
       default export 없음. 파일 상단 주석 `// src/lib/hagwon-category.ts`.

    2. src/lib/hagwon-category.test.ts 생성:
       PATTERNS.md의 "hagwon-category.test.ts 전용 구조" 섹션 코드 그대로 사용.
       import { describe, it, expect } from 'vitest'
       import { classifyHagwon, walkColor } from '../hagwon-category'
       describe('classifyHagwon') + describe('walkColor') 각 5개 케이스.

    3. src/lib/data/facility-edu.test.ts 생성:
       PATTERNS.md의 "facility-edu.test.ts 전용 mock 구조" 참조.
       vi.mock 헬퍼로 Supabase mock 생성 (from 체이닝 + rpc 체이닝).
       테스트는 현재 구현이 kindergartens 필드를 반환하지 않으므로 FAIL 상태여야 함.
       describe('getComplexFacilityEdu') 블록 4개 it():
         - '유치원 포함 poi → kindergartens에 분류됨'
         - '병설 포함 poi → kindergartens에 분류됨'
         - '유치원 미포함 daycare → daycares에 분류됨'
         - 'si 기반 hagwon_score_percentile_by_si RPC 호출됨'

       Mock rpc 설정:
       ```typescript
       const rpc = vi.fn().mockResolvedValue({ data: 0.7, error: null })
       const mockSupa = { from: vi.fn(), rpc } as unknown as SupabaseClient
       ```
       from mock은 schoolRes/poiRes/complexRes 각각 반환.
  </action>
  <verify>
    <automated>npx tsx --tsconfig tsconfig.json -e "import('./src/lib/hagwon-category.ts').then(m => { const r = m.classifyHagwon('수학학원'); console.log(r === '수학' ? 'OK' : 'FAIL:'+r) })"</automated>
  </verify>
  <done>
    - hagwon-category.ts: classifyHagwon/walkColor/WALK_COLOR_HEX export 확인
    - hagwon-category.test.ts: 9개 테스트 모두 GREEN (npm run test -- --reporter=dot src/lib/hagwon-category.test.ts)
    - facility-edu.test.ts: 현재 구현과 불일치로 일부 RED (kindergartens 필드 없음) — Wave 2에서 GREEN 예정
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import script → DB | service_role로 school_districts 테이블에 INSERT. RLS로 일반 사용자 쓰기 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | school_districts INSERT | mitigate | RLS: service_role만 write 허용. 일반 Supabase anon/authenticated 키로 INSERT 시 403 반환 |
| T-10-02 | Information Disclosure | RLS SELECT | accept | 학구도 데이터는 공개 정보. public read 허용이 적절 |
</threat_model>

<verification>
```bash
# SQL 파일 검증
grep -c "school_districts" supabase/migrations/20260515000001_school_districts.sql
grep -c "hagwon_score_percentile_by_si" supabase/migrations/20260515000001_school_districts.sql
grep -c "ROW LEVEL SECURITY" supabase/migrations/20260515000001_school_districts.sql

# 순수 함수 파일 검증
grep -c "classifyHagwon\|walkColor\|WALK_COLOR_HEX" src/lib/hagwon-category.ts

# hagwon-category 테스트 GREEN
npm run test -- --reporter=dot src/lib/hagwon-category.test.ts
```
</verification>

<success_criteria>
1. supabase/migrations/20260515000001_school_districts.sql 파일 존재, school_districts + school_district_schools + hagwon_score_percentile_by_si 포함
2. supabase db push 성공 — 두 테이블과 함수가 remote DB에 존재함 (human checkpoint 확인)
3. src/lib/hagwon-category.ts: classifyHagwon(), walkColor(), WALK_COLOR_HEX export
4. npm run test -- src/lib/hagwon-category.test.ts: 9개 테스트 GREEN
5. src/lib/data/facility-edu.test.ts: 파일 존재, 일부 RED (kindergartens 미구현으로 인한 정상 실패)
</success_criteria>

<output>
완료 후 .planning/phases/10-edu-enhancement/10-00-SUMMARY.md 생성.
</output>

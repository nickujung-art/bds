---
phase: 10-edu-enhancement
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/components/complex/EducationCard.tsx
  - src/app/complexes/[id]/page.tsx
autonomous: true
requirements:
  - EDU-01
  - EDU-02
  - EDU-03
  - EDU-04
  - EDU-05

must_haves:
  truths:
    - "배정학교에 '배정' 배지가 표시되고 도보 시간 아이콘이 3단계 색상(녹/노/빨)으로 표시된다"
    - "어린이집 최대 3개, 유치원 최대 3개가 각각 분리된 섹션에 표시된다"
    - "학원 목록에 '외 N개 더보기' 버튼이 있고 클릭 시 전체 펼쳐보기된다"
    - "학원 밀도 등급 설명에 '창원시 상위 X%' 또는 '김해시 상위 X%'로 표시된다 (통합 아님)"
    - "학원 항목마다 수학/영어/예체능 등 카테고리 태그가 표시된다"
    - "page.tsx fallback에 kindergartens: [] 가 포함되어 TypeScript 오류가 없다"
  artifacts:
    - path: "src/components/complex/EducationCard.tsx"
      provides: "EDU-01~05 모든 UI 변경 반영된 EducationCard"
      min_lines: 400
    - path: "src/app/complexes/[id]/page.tsx"
      provides: "kindergartens 포함 fallback + FacilityEduData 타입 호환"
  key_links:
    - from: "src/lib/hagwon-category.ts"
      to: "src/components/complex/EducationCard.tsx"
      via: "import { classifyHagwon, walkColor, WALK_COLOR_HEX }"
      pattern: "classifyHagwon|walkColor|WALK_COLOR_HEX"
    - from: "src/lib/data/facility-edu.ts FacilityEduData.kindergartens"
      to: "EducationCard.tsx DaycareList"
      via: "props.data.kindergartens"
      pattern: "kindergartens"
---

<objective>
Wave 3: EducationCard.tsx UI 전면 개선 (EDU-01~05) + page.tsx 타입 호환 수정.

Purpose: Wave 2에서 완성된 데이터 레이어를 사용자에게 보여주는 UI로 완성한다.

Output:
- EducationCard.tsx: 도보 색깔 아이콘(EDU-04) + 어린이집/유치원 분리(EDU-02) + 학원 펼치기(EDU-03) + 학원 카테고리 태그(EDU-05) + si별 백분위 표시(EDU-03)
- page.tsx: kindergartens: [] fallback 추가
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-edu-enhancement/10-RESEARCH.md
@.planning/phases/10-edu-enhancement/10-PATTERNS.md
@.planning/phases/10-edu-enhancement/10-02-SUMMARY.md

<interfaces>
<!-- 현재 EducationCard.tsx 구조 요약 (이미 읽었음) -->
```
파일: src/components/complex/EducationCard.tsx (437줄)
'use client' + useState + import FacilityEduData

아이콘: WalkIcon, SchoolIcon, HagwonIcon, DaycareIcon (SVG 인라인)
유틸: fmtDist(), fmtWalk()
상수: SCHOOL_TYPE_LABEL, GRADE_COLOR, GRADE_BG

서브컴포넌트:
- EmptyNote({ text })
- SchoolList({ schools }) — schoolTab(elementary/middle/high) 내부 탭 + 배정 배지 이미 있음
- HagwonSection({ hagwons, stats }) — 등급 카드 + 최대 8개 목록 + "외 N개" 정적 텍스트
- DaycareList({ daycares }) — 최대 10개 목록

메인: EducationCard({ data: FacilityEduData })
탭: school / hagwon / daycare

Tab type: 'school' | 'hagwon' | 'daycare'
```

<!-- 수정 후 FacilityEduData (Wave 2에서 완성) -->
```typescript
interface FacilityEduData {
  schools:       SchoolItem[]
  hagwons:       PoiItem[]
  daycares:      PoiItem[]       // 어린이집만
  kindergartens: PoiItem[]       // 유치원만 (NEW)
  hagwonStats:   HagwonStats | null
}
```

<!-- hagwon-category.ts exports (Wave 0에서 생성) -->
```typescript
export type HagwonCategory = '수학' | '영어' | '예체능' | '국어' | '과학' | '중국어/일어' | '기타'
export type WalkColor = 'green' | 'yellow' | 'red'
export function classifyHagwon(poiName: string): HagwonCategory
export function walkColor(distanceM: number | null): WalkColor
export const WALK_COLOR_HEX: Record<WalkColor, string>
// { green: '#16a34a', yellow: '#d97706', red: '#dc2626' }
```

<!-- 카테고리 태그 색상 (AI 슬롭 금지: 보라/인디고 사용 불가) -->
```typescript
const CATEGORY_COLOR: Record<string, { color: string; bg: string }> = {
  '수학':      { color: '#2563eb', bg: '#eff6ff' },   // blue-600
  '영어':      { color: '#16a34a', bg: '#f0fdf4' },   // green-600
  '예체능':    { color: '#d97706', bg: '#fffbeb' },   // amber-600
  '국어':      { color: '#dc2626', bg: '#fef2f2' },   // red-600
  '과학':      { color: '#0891b2', bg: '#ecfeff' },   // cyan-600
  '중국어/일어': { color: '#7c3aed', bg: '#f5f3ff' }, // 예외: 한국어 분류 태그용 (보라 아님)
  '기타':      { color: '#9ca3af', bg: '#f9fafb' },   // gray-400
}
```
주의: '중국어/일어'의 #7c3aed는 브랜드색이 아닌 분류 태그 전용이므로 AI 슬롭 아님.
단, backdrop-blur/gradient-text/glow는 어떤 경우도 금지.

<!-- page.tsx의 교육 환경 관련 코드 (현재) -->
```typescript
// line 188
getComplexFacilityEdu(id, supabase).catch(() => ({ schools: [], hagwons: [], daycares: [], hagwonStats: null }))
```
→ 수정 후: `{ schools: [], hagwons: [], daycares: [], kindergartens: [], hagwonStats: null }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx fallback 수정 + EducationCard.tsx 전면 개선</name>
  <files>
    src/components/complex/EducationCard.tsx,
    src/app/complexes/[id]/page.tsx
  </files>
  <read_first>
    - src/components/complex/EducationCard.tsx (현재 구현 전체 — 이미 읽었음, 재확인 불필요)
    - src/app/complexes/[id]/page.tsx (line 188 부근 catch fallback + EducationCard 사용 부분)
    - src/lib/hagwon-category.ts (classifyHagwon/walkColor/WALK_COLOR_HEX 정확한 exports)
    - .planning/phases/10-edu-enhancement/10-PATTERNS.md (EducationCard.tsx 수정 패턴 전체)
  </read_first>
  <action>
    **A. page.tsx 수정 (최소 변경)**
    line 188 catch fallback에 `kindergartens: []` 추가:
    ```typescript
    getComplexFacilityEdu(id, supabase).catch(() => ({
      schools: [], hagwons: [], daycares: [], kindergartens: [], hagwonStats: null
    }))
    ```

    **B. EducationCard.tsx 전체 개선**

    1. import 추가:
       ```typescript
       import { classifyHagwon, walkColor, WALK_COLOR_HEX } from '@/lib/hagwon-category'
       ```

    2. WalkIcon 컴포넌트에 color prop 추가 (EDU-04):
       ```typescript
       function WalkIcon({ color }: { color?: string }) {
         return (
           <svg ... stroke={color ?? 'currentColor'} ...>
             {/* 기존 paths 동일 */}
           </svg>
         )
       }
       ```

    3. CATEGORY_COLOR 상수 추가 (interfaces 섹션 코드 그대로):
       ```typescript
       const CATEGORY_COLOR: Record<string, { color: string; bg: string }> = { ... }
       ```

    4. SchoolList 수정 (EDU-04: 도보 시간 색깔 아이콘):
       현재: `<WalkIcon /> {fmtWalk(s.distance_m)}`
       수정: 
       ```typescript
       const wc = walkColor(s.distance_m)
       // ...
       <WalkIcon color={WALK_COLOR_HEX[wc]} />
       <span style={{ color: WALK_COLOR_HEX[wc] }}>{fmtWalk(s.distance_m)}</span>
       ```
       도보 시간 텍스트 색상도 walkColor에 따라 변경.

    5. HagwonSection 수정 (EDU-03 펼치기/접기 + 카테고리 태그 + si 백분위):
       
       펼치기/접기:
       ```typescript
       const [expanded, setExpanded] = useState(false)
       const INITIAL_LIMIT = 8
       const visibleHagwons = expanded ? hagwons : hagwons.slice(0, INITIAL_LIMIT)
       // "외 N개" p 태그 → 버튼으로 교체
       {hagwons.length > INITIAL_LIMIT && (
         <button onClick={() => setExpanded(v => !v)} style={{
           font: '500 12px/1 var(--font-sans)', color: 'var(--fg-tertiary)',
           textAlign: 'center', padding: '10px 0 0', margin: 0,
           background: 'none', border: 'none', cursor: 'pointer', width: '100%',
         }}>
           {expanded ? '접기' : `외 ${hagwons.length - INITIAL_LIMIT}개 더보기`}
         </button>
       )}
       ```

       카테고리 태그 (EDU-05): 각 학원 항목 poi_name 아래에 추가
       ```typescript
       const cat = classifyHagwon(h.poi_name)
       const catStyle = CATEGORY_COLOR[cat] ?? CATEGORY_COLOR['기타']
       // ...
       <span style={{
         font: '500 10px/1 var(--font-sans)',
         color: catStyle.color, background: catStyle.bg,
         padding: '2px 5px', borderRadius: 4, flexShrink: 0,
       }}>
         {cat}
       </span>
       ```

       si별 백분위 표시 (EDU-03):
       HagwonStats에 si 정보가 없으므로, HagwonSection props에 si 추가하거나
       hagwonStats 객체에 si 정보를 포함하는 방식 중 선택:
       
       선택: EducationCard에 si props 추가 → HagwonSection으로 전달:
       ```typescript
       // Props 인터페이스 수정
       interface Props {
         data: FacilityEduData
         si?: string   // NEW: 창원시 / 김해시
       }
       export function EducationCard({ data, si }: Props) { ... }
       // HagwonSection 호출: <HagwonSection hagwons={hagwons} stats={hagwonStats} si={si} />
       ```
       HagwonSection 등급 설명 수정:
       ```typescript
       // 현재: "창원·김해 상위 {above}%"
       // 수정: si ? `${si} 상위 ${above}%` : `창원·김해 상위 ${above}%`
       ```
       
       page.tsx에서 si prop 전달:
       ```tsx
       <EducationCard data={facilityEdu} si={complex.si ?? undefined} />
       ```
       (complex.si는 이미 조회되어 있는 컬럼)

    6. DaycareList → DaycareSection 수정 (EDU-02: 유치원/어린이집 분리):
       ```typescript
       function DaycareSection({ daycares, kindergartens }: {
         daycares: PoiItem[]
         kindergartens: PoiItem[]
       }) {
         const hasBoth = daycares.length > 0 && kindergartens.length > 0
         const hasAny  = daycares.length > 0 || kindergartens.length > 0

         if (!hasAny) return <EmptyNote text="반경 1km 내 보육시설 데이터를 수집 중입니다." />

         return (
           <div>
             {/* 유치원 섹션 (최대 3개) */}
             {kindergartens.length > 0 && (
               <>
                 {hasBoth && (
                   <div style={{ font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: 8 }}>
                     유치원
                   </div>
                 )}
                 <div style={{ display: 'flex', flexDirection: 'column', marginBottom: hasBoth ? 16 : 0 }}>
                   {kindergartens.slice(0, 3).map((k, i) => (
                     <PoiRow key={i} item={k} icon={<DaycareIcon />} isLast={i === Math.min(kindergartens.length, 3) - 1} />
                   ))}
                 </div>
               </>
             )}

             {/* 어린이집 섹션 (최대 3개) */}
             {daycares.length > 0 && (
               <>
                 {hasBoth && (
                   <div style={{ font: '600 12px/1 var(--font-sans)', color: 'var(--fg-sec)', marginBottom: 8 }}>
                     어린이집
                   </div>
                 )}
                 <div style={{ display: 'flex', flexDirection: 'column' }}>
                   {daycares.slice(0, 3).map((d, i) => (
                     <PoiRow key={i} item={d} icon={<DaycareIcon />} isLast={i === Math.min(daycares.length, 3) - 1} />
                   ))}
                 </div>
               </>
             )}
           </div>
         )
       }
       ```

       PoiRow 헬퍼 추출 (DaycareList + HagwonSection 공통 패턴):
       ```typescript
       function PoiRow({ item, icon, isLast }: { item: PoiItem; icon: React.ReactNode; isLast: boolean }) {
         return (
           <div style={{
             display: 'flex', alignItems: 'center', gap: 10,
             padding: '9px 0',
             borderBottom: isLast ? 'none' : '1px solid var(--line-subtle)',
           }}>
             <div style={{
               width: 28, height: 28, borderRadius: 7,
               background: 'var(--bg-surface-2)',
               display: 'grid', placeItems: 'center',
               flexShrink: 0, color: 'var(--fg-sec)',
             }}>
               {icon}
             </div>
             <span style={{ flex: 1, font: '500 13px/1.3 var(--font-sans)', color: 'var(--fg-pri)' }}>
               {item.poi_name}
             </span>
             <div style={{ textAlign: 'right', flexShrink: 0 }}>
               <div style={{ font: '600 13px/1 var(--font-sans)', color: 'var(--fg-sec)' }}>
                 {fmtDist(item.distance_m)}
               </div>
               {item.distance_m != null && (
                 <div style={{
                   display: 'flex', alignItems: 'center', gap: 2, justifyContent: 'flex-end',
                   marginTop: 2,
                   font: '500 11px/1 var(--font-sans)', color: 'var(--fg-tertiary)',
                 }}>
                   <WalkIcon />
                   {fmtWalk(item.distance_m)}
                 </div>
               )}
             </div>
           </div>
         )
       }
       ```

    7. 탭 count 수정:
       ```typescript
       { key: 'daycare', label: '어린이집·유치원', count: daycares.length + kindergartens.length }
       ```

    8. EducationCard 메인에서 DaycareList → DaycareSection으로 교체:
       ```tsx
       {tab === 'daycare' && <DaycareSection daycares={daycares} kindergartens={kindergartens} />}
       ```
       props 비구조화에 `kindergartens` 추가.

    금지 사항 (AI 슬롭):
    - backdrop-blur, gradient-text, glow, 보라/인디고 브랜드색 (#6366f1, #7c3aed — 단, 분류 태그는 예외)
    - 배경 gradient orb
  </action>
  <verify>
    <automated>npm run lint && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npm run lint 통과 (TypeScript 오류 0건)
    - npm run build 통과 (빌드 성공)
    - EducationCard.tsx에 classifyHagwon, walkColor, WALK_COLOR_HEX import 존재
    - DaycareSection 함수 존재 (kindergartens prop 포함)
    - page.tsx fallback에 kindergartens: [] 포함
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 교육 환경 카드 UI 육안 검증</name>
  <read_first>
    - 없음 (이전 Task의 빌드 결과 확인)
  </read_first>
  <what-built>
    EducationCard.tsx 전체 개선:
    - 도보 시간 3단계 색깔 아이콘 (녹/노/빨)
    - 어린이집/유치원 분리 표시 (각 최대 3개)
    - 학원 "외 N개 더보기" 토글 버튼
    - 학원 카테고리 태그 (수학/영어/예체능 등)
    - si별 학원 밀도 백분위 (창원시/김해시 분리)
    page.tsx: si prop 전달 + kindergartens fallback
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행
    2. 교육 데이터가 있는 단지 상세 페이지 접속
       예: /complexes/{id} — 단지 상세 → 교육 환경 카드
    3. 검증 항목:
       a. [학교 탭] 학교 목록에 도보 시간이 색깔 아이콘과 함께 표시됨
          - 10분 이내: 녹색 (#16a34a)
          - 10~15분: 노랑 (#d97706)
          - 15분 초과: 빨강 (#dc2626)
       b. [학교 탭] 배정학교에 "배정" 파란 배지 표시됨 (is_assignment=true 단지에서)
       c. [학원·교육 탭] 학원 목록에 수학/영어/예체능 등 카테고리 태그 표시됨
       d. [학원·교육 탭] 학원이 8개 초과 시 "외 N개 더보기" 버튼 표시 → 클릭 시 펼쳐짐 → "접기" 버튼으로 변경
       e. [학원·교육 탭] 등급 카드에 "창원시 상위 X%" 또는 "김해시 상위 X%" 표시 (통합 아님)
       f. [어린이집·유치원 탭] 유치원 섹션과 어린이집 섹션이 분리 표시됨 (각 최대 3개)
  </how-to-verify>
  <resume-signal>모든 항목 확인 후 "approved" 입력, 문제 있으면 구체적 증상 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → client component | data는 서버에서 fetch, EducationCard는 props로만 수신 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | EducationCard props | accept | 학구도/학원 데이터는 공개 정보. 개인정보 없음 |
| T-10-08 | Spoofing | classifyHagwon 분류 | accept | poi_name은 서버 DB에서 읽음. 사용자 입력 아님 |
</threat_model>

<verification>
```bash
# 빌드 검증
npm run lint && npm run build

# 필수 imports 확인
grep -c "classifyHagwon\|walkColor\|WALK_COLOR_HEX" src/components/complex/EducationCard.tsx

# kindergartens 분리 구현 확인
grep -c "kindergartens" src/components/complex/EducationCard.tsx

# page.tsx si prop 전달 확인
grep -c "si={complex" src/app/complexes/\[id\]/page.tsx

# 전체 테스트 통과
npm run test -- --reporter=dot
```
</verification>

<success_criteria>
1. npm run lint && npm run build 오류 없이 통과
2. EducationCard.tsx: classifyHagwon/walkColor/WALK_COLOR_HEX import 존재
3. EducationCard.tsx: DaycareSection 함수에 kindergartens prop 포함
4. EducationCard.tsx: HagwonSection에 expanded useState + "더보기/접기" 버튼 존재
5. page.tsx: si prop 전달 + kindergartens: [] fallback
6. 단지 상세 페이지 육안 검증 통과 (checkpoint)
7. npm run test 전체 통과
</success_criteria>

<output>
완료 후 .planning/phases/10-edu-enhancement/10-03-SUMMARY.md 생성.
</output>

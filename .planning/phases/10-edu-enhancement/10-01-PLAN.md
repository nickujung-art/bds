---
phase: 10-edu-enhancement
plan: "01"
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - scripts/import-school-districts.ts
autonomous: true
requirements:
  - EDU-01

must_haves:
  truths:
    - "창원+김해 234개 학구도 폴리곤이 school_districts 테이블에 적재된다"
    - "school_district_schools 연결 테이블에 학구도-학교명 N:M 관계가 적재된다"
    - "--dry-run 플래그로 DB 미실행 모드를 지원한다"
    - "고등학교 필터링은 EDU_NM 기반 (SGG_CD 없음)으로 처리한다"
  artifacts:
    - path: "scripts/import-school-districts.ts"
      provides: "SHP/DBF 파싱 → school_districts SQL 생성 → DB 적재 스크립트"
      min_lines: 180
  key_links:
    - from: "data/school-assignment/통학구역/*.shp"
      to: "school_districts.geometry"
      via: "ST_Transform(ST_GeomFromText(wkt, 5186), 4326)"
      pattern: "ST_Transform.*5186.*4326"
    - from: "data/school-assignment/학교학구도연계정보/*.csv"
      to: "school_district_schools.school_name"
      via: "TextDecoder('euc-kr') CSV 파싱"
      pattern: "euc-kr"
---

<objective>
Wave 1: Shapefile import 스크립트 작성 + 실행.

Purpose: data/school-assignment/ 디렉터리의 SHP/DBF 파일을 파싱하여 school_districts + school_district_schools 테이블에 적재한다. ogr2ogr 없이 Node.js Buffer API 직접 파싱.

Output: scripts/import-school-districts.ts (실행 가능 스크립트)
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-edu-enhancement/10-RESEARCH.md
@.planning/phases/10-edu-enhancement/10-PATTERNS.md
@.planning/phases/10-edu-enhancement/10-00-SUMMARY.md

<interfaces>
<!-- collect-facility-edu.ts 헤더 + env 로드 패턴 -->
```typescript
import * as fs from 'fs'
import * as path from 'path'
import { execSync } from 'child_process'

const envPath = path.join(process.cwd(), '.env.local')
if (fs.existsSync(envPath)) {
  for (const rawLine of fs.readFileSync(envPath, 'utf8').split('\n')) {
    const line = rawLine.replace(/\r$/, '')
    const m = line.match(/^([A-Z_][A-Z0-9_]*)=(.*)$/)
    const key = m?.[1]; const val = m?.[2]
    if (key && val !== undefined && !process.env[key]) process.env[key] = val.replace(/^"|"$/g, '')
  }
}
const DRY_RUN = process.argv.includes('--dry-run')
```

<!-- SQL flush 패턴 -->
```typescript
function flushToDb(sqlContent: string, label: string) {
  if (DRY_RUN) { console.log(`[DRY RUN] ${label} SQL 출력만`); return }
  const tmpPath = path.join(process.cwd(), 'data', '_school_districts.sql')
  fs.writeFileSync(tmpPath, sqlContent, 'utf8')
  execSync(`supabase db query --linked --file "${tmpPath}"`, { stdio: 'inherit' })
  fs.unlinkSync(tmpPath)
}
```

<!-- SHP 폴리곤 파싱 (RESEARCH.md Pattern 1) -->
```typescript
function readPolygonWkt(shpBuf: Buffer, recordDataOffset: number): string {
  const shapeType = shpBuf.readInt32LE(recordDataOffset)
  if (shapeType !== 5) return ''
  const numParts  = shpBuf.readInt32LE(recordDataOffset + 36)
  const numPoints = shpBuf.readInt32LE(recordDataOffset + 40)
  const partsOffset  = recordDataOffset + 44
  const pointsOffset = partsOffset + numParts * 4
  const rings: string[] = []
  for (let p = 0; p < numParts; p++) {
    const partStart = shpBuf.readInt32LE(partsOffset + p * 4)
    const partEnd   = p < numParts - 1
      ? shpBuf.readInt32LE(partsOffset + (p + 1) * 4)
      : numPoints
    const coords: string[] = []
    for (let i = partStart; i < partEnd; i++) {
      const x = shpBuf.readDoubleLE(pointsOffset + i * 16)
      const y = shpBuf.readDoubleLE(pointsOffset + i * 16 + 8)
      coords.push(`${x} ${y}`)
    }
    rings.push(`(${coords.join(',')})`)
  }
  return `POLYGON(${rings.join(',')})`
}
```

<!-- DBF EUC-KR 파싱 (RESEARCH.md Pattern 2) -->
```typescript
const decoder = new TextDecoder('euc-kr')

interface DbfField { name: string; length: number; offset: number }

function parseDbfFields(buf: Buffer): DbfField[] {
  const fields: DbfField[] = []
  let offset = 32
  const headerBytes = buf.readUInt16LE(8)
  let dataOffset = 0
  while (buf[offset] !== 0x0D && offset < headerBytes) {
    const name   = buf.slice(offset, offset + 11).toString('ascii').replace(/\x00/g, '').trim()
    const length = buf[offset + 16]
    fields.push({ name, length, offset: dataOffset })
    dataOffset += length
    offset += 32
  }
  return fields
}

function readDbfField(recBuf: Buffer, field: DbfField): string {
  return decoder.decode(recBuf.slice(field.offset, field.offset + field.length))
    .replace(/\x00/g, '').trim()
}
```

<!-- SQL 이스케이프 -->
```typescript
function esc(s: string): string { return s.replace(/'/g, "''") }
```

<!-- school_districts 테이블 스키마 (10-00-PLAN에서 생성됨) -->
```sql
school_districts(id uuid PK, hakgudo_id text, school_level text CHECK IN ('elementary','middle','high'), geometry geometry(Geometry,4326), source_file text, created_at timestamptz)
school_district_schools(district_id uuid FK, school_name text, school_level text, PRIMARY KEY(district_id, school_name))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: import-school-districts.ts 작성 + --dry-run 실행으로 검증</name>
  <files>scripts/import-school-districts.ts</files>
  <read_first>
    - scripts/collect-facility-edu.ts (헤더/env/flush/DRY_RUN 패턴 전체)
    - .planning/phases/10-edu-enhancement/10-RESEARCH.md (Pattern 1~4, Pitfall 1~4)
    - .planning/phases/10-edu-enhancement/10-PATTERNS.md (import-school-districts.ts 전용 추가 패턴)
    - data/school-assignment/ 디렉터리 구조 확인 (ls data/school-assignment/ 실행)
  </read_first>
  <action>
    스크립트 구조 (아래 순서대로 구현):

    **1. 헤더 + env 로드 + 상수 정의**
    ```
    실행 방법:
      npx tsx scripts/import-school-districts.ts --dry-run   # DB 미실행, SQL 출력
      npx tsx scripts/import-school-districts.ts              # 실제 DB 적재
    ```
    - collect-facility-edu.ts와 동일한 env 로드 패턴
    - DRY_RUN = process.argv.includes('--dry-run')
    - BATCH_SIZE = 50 (SQL 크기 제한 대응, Pitfall 4)
    - 학교급별 파일 경로 상수:
      ```typescript
      const LEVEL_CONFIG = [
        { level: 'elementary' as const, shpDir: 'data/school-assignment/통학구역/초등학교통학구역', csvPattern: '초등학교' },
        { level: 'middle'     as const, shpDir: 'data/school-assignment/통학구역/중학교통학구역',   csvPattern: '중학교' },
        { level: 'high'       as const, shpDir: 'data/school-assignment/통학구역/고등학교학교군',   csvPattern: '고등학교' },
      ]
      ```
      (실제 디렉터리명은 ls 결과에 따라 조정)

    **2. SHP/DBF 파싱 함수**
    readPolygonWkt(), parseDbfFields(), readDbfField() — interfaces 섹션 코드 그대로 사용.

    **3. CSV 파싱 함수 (EUC-KR)**
    ```typescript
    function parseCsv(csvPath: string): Map<string, string[]> {
      // hakgudo_id → school_name[] 매핑 반환
      // EUC-KR 디코딩 필수
      // '학교급구분' 컬럼으로 초/중/고 필터링 (추가 검증용)
    }
    ```

    **4. 필터링 로직**
    - 초등/중학교: DBF의 SD_CD == '48' (경남) AND SGG_CD IN ('121', '250') (창원, 김해)
    - 고등학교: EDU_NM LIKE '%창원%' OR LIKE '%김해%' (SGG_CD 컬럼 없음 — Pitfall 1)
    - 필터 통과한 레코드만 WKT 생성

    **5. SQL 생성 + 배치 flush**
    ```typescript
    // school_districts INSERT (ST_Transform 5186→4326)
    `INSERT INTO public.school_districts (hakgudo_id, school_level, geometry, source_file)
    VALUES
      ('${esc(hakgudoId)}', '${level}',
       ST_Transform(ST_GeomFromText('${wkt}', 5186), 4326),
       '${level}')
    ON CONFLICT DO NOTHING;`

    // school_district_schools INSERT
    `INSERT INTO public.school_district_schools (district_id, school_name, school_level)
    SELECT id, '${esc(schoolName)}', '${level}'
    FROM public.school_districts
    WHERE hakgudo_id = '${esc(hakgudoId)}' AND school_level = '${level}'
    ON CONFLICT DO NOTHING;`
    ```
    - 50개마다 flushToDb() 호출 (Pitfall 4 대응)
    - DRY_RUN 시 SQL 콘솔 출력만

    **6. dry-run school_name 대조 출력 (Assumption A2 검증)**
    ```typescript
    if (DRY_RUN) {
      // DB에서 facility_school.school_name 목록 조회
      // CSV school_name과 대조하여 불일치 목록 출력
      console.log('[DRY RUN] school_name 불일치 목록:')
    }
    ```

    **7. main() + 에러 핸들링**
    collect-facility-edu.ts의 main().catch(err => { console.error('치명적 오류:', err); process.exit(1) }) 패턴 동일.

    주의사항:
    - 전국 Shapefile 전체 import 금지 — 창원/김해 필터 필수 (Anti-pattern)
    - 고등학교는 SGG_CD 필터 사용 금지 (Pitfall 1)
    - school_name 단독 매칭 금지 — school_level도 반드시 포함 (Anti-pattern)
    - WKT 문자열에 작은따옴표가 포함될 경우 esc() 적용
  </action>
  <verify>
    <automated>npx tsx scripts/import-school-districts.ts --dry-run 2>&1 | head -30</automated>
  </verify>
  <done>
    --dry-run 실행 시:
    - "[DRY RUN]" 메시지 출력
    - 초등/중학교 레코드 수 (창원+김해 목표: 초등 ~200개, 중등 ~31개, 고등 ~3개) 출력
    - school_name 불일치 목록 출력 (0건이면 최상)
    - 오류 없이 종료
  </done>
</task>

<task type="auto">
  <name>Task 2: --dry-run 검증 후 실제 DB 적재 실행</name>
  <files>scripts/import-school-districts.ts</files>
  <read_first>
    - dry-run 출력 결과 (Task 1 verify 결과)
    - .planning/phases/10-edu-enhancement/10-RESEARCH.md (Assumption A2: school_name 불일치 확인)
  </read_first>
  <action>
    1. dry-run 출력에서 school_name 불일치 건수 확인:
       - 불일치 0건: 즉시 실제 실행
       - 불일치 있음: 불일치 원인 분석 후 허용 가능 범위(전체의 20% 이하)면 진행
         → 불일치 목록을 SUMMARY.md에 기록

    2. 실제 적재 실행:
       ```bash
       npx tsx scripts/import-school-districts.ts
       ```

    3. 적재 결과 검증:
       ```bash
       supabase db query --linked "SELECT school_level, COUNT(*) FROM public.school_districts GROUP BY school_level ORDER BY school_level"
       ```
       예상 결과: elementary ~200개, high ~3개, middle ~31개

    4. school_district_schools 확인:
       ```bash
       supabase db query --linked "SELECT COUNT(*) FROM public.school_district_schools"
       ```
       → 최소 234개 이상 (다중 학교 매핑 포함 시 더 많을 수 있음)
  </action>
  <verify>
    <automated>supabase db query --linked "SELECT school_level, COUNT(*) as cnt FROM public.school_districts GROUP BY school_level ORDER BY school_level"</automated>
  </verify>
  <done>
    - school_districts: 총 190개 이상 (창원+김해 초+중+고 합산)
    - school_district_schools: 총 200개 이상
    - elementary, middle, high 세 레벨 모두 레코드 존재
    - 오류 없이 완료
    - npm run lint && npm run build 통과
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 파일 → DB | 스크립트가 로컬 SHP/CSV 파일을 읽어 service_role로 DB에 INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | SQL WKT 문자열 | accept | WKT는 스크립트가 SHP 바이너리에서 직접 생성. 외부 사용자 입력 없음. SQL injection 경로 없음 |
| T-10-04 | Information Disclosure | supabase CLI 출력 | mitigate | SUPABASE_DB_URL 환경변수를 stdout에 출력하지 않음. --stdio inherit만 사용 |
</threat_model>

<verification>
```bash
# 스크립트 존재 확인
ls scripts/import-school-districts.ts

# dry-run 동작 확인
npx tsx scripts/import-school-districts.ts --dry-run 2>&1 | grep -c "DRY RUN"

# 적재 결과 (실제 실행 후)
supabase db query --linked "SELECT school_level, COUNT(*) FROM public.school_districts GROUP BY school_level"
```
</verification>

<success_criteria>
1. scripts/import-school-districts.ts 파일 존재
2. --dry-run 실행 시 오류 없이 SQL 미실행 모드로 동작한다
3. 실제 실행 후 school_districts 테이블에 190개 이상 레코드 적재
4. school_district_schools 테이블에 200개 이상 레코드 적재
5. elementary/middle/high 세 레벨 모두 레코드 존재
</success_criteria>

<output>
완료 후 .planning/phases/10-edu-enhancement/10-01-SUMMARY.md 생성.
</output>

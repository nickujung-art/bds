---
phase: 10-edu-enhancement
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/update-assignment-flags.ts
  - src/lib/data/facility-edu.ts
autonomous: true
requirements:
  - EDU-01
  - EDU-02
  - EDU-03

must_haves:
  truths:
    - "ST_Within 공간 조인으로 각 단지의 배정학교 facility_school.is_assignment = true 업데이트된다"
    - "getComplexFacilityEdu()가 daycare를 유치원/어린이집으로 분리하여 kindergartens 필드를 반환한다"
    - "getComplexFacilityEdu()가 hagwon_score_percentile_by_si를 호출하여 si별 백분위를 반환한다"
    - "facility-edu.test.ts 4개 테스트가 모두 GREEN이 된다"
  artifacts:
    - path: "scripts/update-assignment-flags.ts"
      provides: "ST_Within 공간 조인 → is_assignment 일괄 업데이트 스크립트"
      min_lines: 60
    - path: "src/lib/data/facility-edu.ts"
      provides: "kindergartens 분리 + si별 백분위 수정된 getComplexFacilityEdu()"
      exports: ["getComplexFacilityEdu", "FacilityEduData", "SchoolItem", "PoiItem", "HagwonStats"]
  key_links:
    - from: "scripts/update-assignment-flags.ts"
      to: "facility_school.is_assignment"
      via: "ST_Within(c.location::geometry, sd.geometry)"
      pattern: "ST_Within"
    - from: "src/lib/data/facility-edu.ts"
      to: "hagwon_score_percentile_by_si RPC"
      via: "supabase.rpc('hagwon_score_percentile_by_si', { target_score, p_si })"
      pattern: "hagwon_score_percentile_by_si"
---

<objective>
Wave 2: is_assignment 플래그 업데이트 스크립트 + facility-edu.ts 서비스 수정.

Purpose:
1. school_districts 테이블이 채워진 후 ST_Within 공간 조인으로 각 단지 배정학교 판별
2. getComplexFacilityEdu()에 유치원/어린이집 분리(EDU-02) + si 기반 학원 백분위(EDU-03) 추가
3. Wave 0에서 생성된 RED 테스트 4개를 GREEN으로 전환

Output: 배정학교 is_assignment = true 업데이트 완료 + 수정된 facility-edu.ts
</objective>

<execution_context>
@C:/Users/jung/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-edu-enhancement/10-RESEARCH.md
@.planning/phases/10-edu-enhancement/10-PATTERNS.md
@.planning/phases/10-edu-enhancement/10-00-SUMMARY.md
@.planning/phases/10-edu-enhancement/10-01-SUMMARY.md

<interfaces>
<!-- 현재 getComplexFacilityEdu 시그니처 (facility-edu.ts) -->
```typescript
export async function getComplexFacilityEdu(
  complexId: string,
  supabase: SupabaseClient,
): Promise<FacilityEduData>
```

<!-- 기존 FacilityEduData 인터페이스 (수정 전) -->
```typescript
export interface FacilityEduData {
  schools:     SchoolItem[]
  hagwons:     PoiItem[]
  daycares:    PoiItem[]
  hagwonStats: HagwonStats | null
}
```

<!-- 수정 후 FacilityEduData 인터페이스 -->
```typescript
export interface FacilityEduData {
  schools:       SchoolItem[]
  hagwons:       PoiItem[]
  daycares:      PoiItem[]
  kindergartens: PoiItem[]       // NEW: 유치원 분리 (EDU-02)
  hagwonStats:   HagwonStats | null
}
```

<!-- 현재 hagwon_score 쿼리 (수정 전) -->
```typescript
supabase.from('complexes').select('hagwon_score').eq('id', complexId).maybeSingle()
```

<!-- 수정 후: si도 함께 select -->
```typescript
supabase.from('complexes').select('hagwon_score, si').eq('id', complexId).maybeSingle()
```

<!-- ST_Within is_assignment 업데이트 SQL (RESEARCH.md Pattern 3) -->
```sql
UPDATE public.facility_school fs
SET    is_assignment = true,
       updated_at    = now()
FROM   public.complexes c
JOIN   public.school_district_schools sds ON sds.school_name = fs.school_name
JOIN   public.school_districts sd
         ON sd.id = sds.district_id
         AND sd.school_level = fs.school_type
WHERE  fs.complex_id = c.id
  AND  c.location IS NOT NULL
  AND  ST_Within(c.location::geometry, sd.geometry);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: update-assignment-flags.ts 작성 + 실행</name>
  <files>scripts/update-assignment-flags.ts</files>
  <read_first>
    - scripts/collect-facility-edu.ts (헤더/env/execSync 패턴)
    - .planning/phases/10-edu-enhancement/10-RESEARCH.md (Pattern 3: ST_Within SQL, Pitfall 2: 다중 학교 매핑)
    - .planning/phases/10-edu-enhancement/10-01-SUMMARY.md (school_district_schools 적재 결과 확인)
  </read_first>
  <action>
    스크립트 구조:

    1. 헤더 + env 로드 (collect-facility-edu.ts 동일 패턴)
       ```
       실행: npx tsx scripts/update-assignment-flags.ts --dry-run
             npx tsx scripts/update-assignment-flags.ts
       ```

    2. dry-run 모드: 업데이트될 (complex_id, school_name, school_type) 목록 출력만

    3. 실제 실행:
       ```sql
       -- Step 1: 모든 is_assignment 초기화 (재실행 idempotent 보장)
       UPDATE public.facility_school SET is_assignment = false, updated_at = now()
       WHERE is_assignment = true;

       -- Step 2: ST_Within 공간 조인으로 배정학교 업데이트
       UPDATE public.facility_school fs
       SET    is_assignment = true, updated_at = now()
       FROM   public.complexes c
       JOIN   public.school_district_schools sds ON sds.school_name = fs.school_name
       JOIN   public.school_districts sd
                ON sd.id = sds.district_id
                AND sd.school_level = fs.school_type
       WHERE  fs.complex_id = c.id
         AND  c.location IS NOT NULL
         AND  ST_Within(c.location::geometry, sd.geometry);
       ```

    4. 결과 검증 쿼리 출력:
       ```sql
       SELECT school_type, COUNT(*) as cnt
       FROM public.facility_school
       WHERE is_assignment = true
       GROUP BY school_type ORDER BY school_type;
       ```

    5. 주의사항:
       - SQL을 tmpPath에 파일로 저장 후 `supabase db query --linked --file` 실행
       - SUPABASE 환경변수 stdout 출력 금지
       - 고등학교 배정이 0건인 경우 RESEARCH.md Open Question 1 참조 — 정상 가능 (평준화)
  </action>
  <verify>
    <automated>supabase db query --linked "SELECT school_type, COUNT(*) as cnt FROM public.facility_school WHERE is_assignment = true GROUP BY school_type ORDER BY school_type"</automated>
  </verify>
  <done>
    - scripts/update-assignment-flags.ts 파일 존재
    - 실행 후 is_assignment = true인 레코드가 최소 1개 이상 존재
    - school_type별 집계가 출력됨 (elementary 레코드 수 > 0이면 정상)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: facility-edu.ts 수정 (EDU-02 유치원 분리 + EDU-03 si 백분위)</name>
  <files>src/lib/data/facility-edu.ts</files>
  <read_first>
    - src/lib/data/facility-edu.ts (현재 구현 전체 — 이미 읽었음, 재확인 불필요)
    - src/lib/data/facility-edu.test.ts (Wave 0에서 생성된 RED 테스트 — 어떤 동작을 기대하는지)
    - .planning/phases/10-edu-enhancement/10-PATTERNS.md (facility-edu.ts 수정 패턴)
  </read_first>
  <behavior>
    EDU-02: daycare → kindergartens + daycares 분리
    - poi_name.includes('유치원') || poi_name.includes('병설') → kindergartens
    - 나머지 daycare → daycares
    - FacilityEduData에 kindergartens: PoiItem[] 필드 추가

    EDU-03: si 기반 hagwon_score_percentile_by_si 호출
    - complexes 쿼리: .select('hagwon_score, si') (si 컬럼 추가)
    - percentile 계산: supabase.rpc('hagwon_score_percentile_by_si', { target_score: rawScore, p_si: si })
    - si가 null인 경우: 기존 hagwon_score_percentile fallback

    반환 타입: kindergartens 포함된 FacilityEduData
    fallback: .catch(() => ({ schools: [], hagwons: [], daycares: [], kindergartens: [], hagwonStats: null }))
    → page.tsx의 catch fallback도 kindergartens: [] 추가 필요 (page.tsx 수정은 Wave 3에서 처리하나 타입 오류 방지용으로 검토)
  </behavior>
  <action>
    1. FacilityEduData 인터페이스에 `kindergartens: PoiItem[]` 추가

    2. daycare 분기 수정 (현재 단일 daycares 배열 → 두 배열로 분리):
       ```typescript
       const kindergartens = allPois
         .filter(p => p.category === 'daycare' && (
           p.poi_name.includes('유치원') || p.poi_name.includes('병설')
         ))
         .map(p => ({ poi_name: p.poi_name, distance_m: p.distance_m }))

       const daycares = allPois
         .filter(p => p.category === 'daycare' && !(
           p.poi_name.includes('유치원') || p.poi_name.includes('병설')
         ))
         .map(p => ({ poi_name: p.poi_name, distance_m: p.distance_m }))
       ```

    3. complexes 쿼리에 si 추가:
       ```typescript
       supabase.from('complexes').select('hagwon_score, si').eq('id', complexId).maybeSingle()
       ```

    4. percentile 계산 수정:
       ```typescript
       const complexData = scoreRes.data as { hagwon_score?: number | null; si?: string | null } | null
       const rawScore = complexData?.hagwon_score
       const si       = complexData?.si

       // ...
       const percentileRes = si
         ? await supabase.rpc('hagwon_score_percentile_by_si', { target_score: rawScore, p_si: si })
         : await supabase.rpc('hagwon_score_percentile', { target_score: rawScore })
       ```

    5. 반환문에 kindergartens 추가:
       ```typescript
       return { schools, hagwons, daycares, kindergartens, hagwonStats }
       ```

    주의: page.tsx의 .catch() fallback도 kindergartens: [] 추가 필요.
    page.tsx 파일은 이 Plan의 files_modified에 없으므로 수정하지 말고,
    TypeScript 오류가 발생하면 NOTE로 남기고 Wave 3에서 처리.
    (Wave 3의 10-03-PLAN은 page.tsx도 수정)
  </action>
  <verify>
    <automated>npm run test -- --reporter=dot src/lib/data/facility-edu.test.ts</automated>
  </verify>
  <done>
    - FacilityEduData에 kindergartens 필드 존재
    - facility-edu.test.ts 4개 테스트 모두 GREEN
    - 'hagwon_score_percentile_by_si' 문자열이 facility-edu.ts에 포함됨
    - npm run lint 통과 (TypeScript strict mode 오류 없음)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| update script → DB | service_role로 is_assignment 업데이트. 일반 사용자는 RLS로 차단 |
| 서버 컴포넌트 → DB | getComplexFacilityEdu는 서버 컴포넌트에서만 호출됨 (CLAUDE.md Critical) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | is_assignment UPDATE | mitigate | facility_school RLS로 일반 사용자 UPDATE 불가. service_role 스크립트만 변경 |
| T-10-06 | Spoofing | hagwon_score_percentile_by_si | accept | p_si는 complexes.si 컬럼 값 (서버에서 읽음). 사용자 직접 입력 없음 |
</threat_model>

<verification>
```bash
# update-assignment-flags.ts 존재
ls scripts/update-assignment-flags.ts

# is_assignment 업데이트 결과
supabase db query --linked "SELECT school_type, COUNT(*) FROM public.facility_school WHERE is_assignment = true GROUP BY school_type"

# facility-edu.test.ts GREEN
npm run test -- --reporter=dot src/lib/data/facility-edu.test.ts

# kindergartens 필드 확인
grep -c "kindergartens" src/lib/data/facility-edu.ts

# si 백분위 RPC 확인
grep -c "hagwon_score_percentile_by_si" src/lib/data/facility-edu.ts
```
</verification>

<success_criteria>
1. scripts/update-assignment-flags.ts 파일 존재
2. is_assignment = true 레코드가 elementary 학교급 기준 최소 1개 이상 존재
3. src/lib/data/facility-edu.ts: kindergartens 필드 포함, hagwon_score_percentile_by_si RPC 호출
4. facility-edu.test.ts: 4개 테스트 모두 GREEN
5. npm run lint 통과
</success_criteria>

<output>
완료 후 .planning/phases/10-edu-enhancement/10-02-SUMMARY.md 생성.
</output>
